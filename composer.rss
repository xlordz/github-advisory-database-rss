<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://xlordz.github.io/github-advisory-database-rss/composer.rss</id>
    <title>Security Advisory for PHP packages hosted at packagist.org</title>
    <updated>2026-07-02T19:46:57.326Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Acomposer"/>
    <subtitle>Security Advisory for PHP packages hosted at packagist.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[mautic/core] Mautic has SQL Injection in API Contact Filtering]]></title>
        <id>https://github.com/advisories/GHSA-fcmw-wx57-9p75</id>
        <link href="https://github.com/advisories/GHSA-fcmw-wx57-9p75"/>
        <updated>2026-07-02T19:25:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An SQL injection vulnerability exists in Mautic's API contact filtering mechanism. Due to insufficient recursive sanitization of nested query parameters, an authenticated API user can bypass input filtering and inject arbitrary SQL commands.</p>
<h3 id="impact">Impact</h3>
<p>An authenticated user with API access can exploit this vulnerability to execute arbitrary SQL queries against the underlying database. This allows unauthorized retrieval of sensitive database contents—including user credentials, system configurations, and personal identifiable information (PII) of contacts—bypassing standard data access permissions.</p>
<h3 id="patched-versions">Patched Versions</h3>
<p>This security issue has been fixed in the following releases:</p>
<ul>
<li><strong>7.1.2</strong></li>
<li><strong>6.0.9</strong></li>
<li><strong>5.2.11</strong></li>
<li><strong>4.4.20</strong> <a href="https://mautic.org/extended-long-term-support-elts/">ELTS</a></li>
</ul>
<p>We strongly recommend upgrading to the latest version corresponding to your release branch.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There are no official workarounds. To mitigate this issue without upgrading, you may temporarily disable API access or restrict API permissions to highly trusted accounts.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mautic/mautic/security/advisories/GHSA-fcmw-wx57-9p75">https://github.com/mautic/mautic/security/advisories/GHSA-fcmw-wx57-9p75</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-4776">https://nvd.nist.gov/vuln/detail/CVE-2026-4776</a></li>
<li><a href="https://github.com/advisories/GHSA-fcmw-wx57-9p75">https://github.com/advisories/GHSA-fcmw-wx57-9p75</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fcmw-wx57-9p75</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T19:25:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mautic/core] Mautic has SQL Injection in API Contact Filtering]]></title>
        <id>https://github.com/advisories/GHSA-fcmw-wx57-9p75</id>
        <link href="https://github.com/advisories/GHSA-fcmw-wx57-9p75"/>
        <updated>2026-07-02T19:25:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An SQL injection vulnerability exists in Mautic's API contact filtering mechanism. Due to insufficient recursive sanitization of nested query parameters, an authenticated API user can bypass input filtering and inject arbitrary SQL commands.</p>
<h3 id="impact">Impact</h3>
<p>An authenticated user with API access can exploit this vulnerability to execute arbitrary SQL queries against the underlying database. This allows unauthorized retrieval of sensitive database contents—including user credentials, system configurations, and personal identifiable information (PII) of contacts—bypassing standard data access permissions.</p>
<h3 id="patched-versions">Patched Versions</h3>
<p>This security issue has been fixed in the following releases:</p>
<ul>
<li><strong>7.1.2</strong></li>
<li><strong>6.0.9</strong></li>
<li><strong>5.2.11</strong></li>
<li><strong>4.4.20</strong> <a href="https://mautic.org/extended-long-term-support-elts/">ELTS</a></li>
</ul>
<p>We strongly recommend upgrading to the latest version corresponding to your release branch.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There are no official workarounds. To mitigate this issue without upgrading, you may temporarily disable API access or restrict API permissions to highly trusted accounts.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mautic/mautic/security/advisories/GHSA-fcmw-wx57-9p75">https://github.com/mautic/mautic/security/advisories/GHSA-fcmw-wx57-9p75</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-4776">https://nvd.nist.gov/vuln/detail/CVE-2026-4776</a></li>
<li><a href="https://github.com/advisories/GHSA-fcmw-wx57-9p75">https://github.com/advisories/GHSA-fcmw-wx57-9p75</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fcmw-wx57-9p75</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T19:25:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mautic/core] Mautic has SQL Injection in API Contact Filtering]]></title>
        <id>https://github.com/advisories/GHSA-fcmw-wx57-9p75</id>
        <link href="https://github.com/advisories/GHSA-fcmw-wx57-9p75"/>
        <updated>2026-07-02T19:25:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An SQL injection vulnerability exists in Mautic's API contact filtering mechanism. Due to insufficient recursive sanitization of nested query parameters, an authenticated API user can bypass input filtering and inject arbitrary SQL commands.</p>
<h3 id="impact">Impact</h3>
<p>An authenticated user with API access can exploit this vulnerability to execute arbitrary SQL queries against the underlying database. This allows unauthorized retrieval of sensitive database contents—including user credentials, system configurations, and personal identifiable information (PII) of contacts—bypassing standard data access permissions.</p>
<h3 id="patched-versions">Patched Versions</h3>
<p>This security issue has been fixed in the following releases:</p>
<ul>
<li><strong>7.1.2</strong></li>
<li><strong>6.0.9</strong></li>
<li><strong>5.2.11</strong></li>
<li><strong>4.4.20</strong> <a href="https://mautic.org/extended-long-term-support-elts/">ELTS</a></li>
</ul>
<p>We strongly recommend upgrading to the latest version corresponding to your release branch.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There are no official workarounds. To mitigate this issue without upgrading, you may temporarily disable API access or restrict API permissions to highly trusted accounts.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mautic/mautic/security/advisories/GHSA-fcmw-wx57-9p75">https://github.com/mautic/mautic/security/advisories/GHSA-fcmw-wx57-9p75</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-4776">https://nvd.nist.gov/vuln/detail/CVE-2026-4776</a></li>
<li><a href="https://github.com/advisories/GHSA-fcmw-wx57-9p75">https://github.com/advisories/GHSA-fcmw-wx57-9p75</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fcmw-wx57-9p75</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T19:25:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mautic/core] Mautic has SQL Injection in API Contact Filtering]]></title>
        <id>https://github.com/advisories/GHSA-fcmw-wx57-9p75</id>
        <link href="https://github.com/advisories/GHSA-fcmw-wx57-9p75"/>
        <updated>2026-07-02T19:25:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An SQL injection vulnerability exists in Mautic's API contact filtering mechanism. Due to insufficient recursive sanitization of nested query parameters, an authenticated API user can bypass input filtering and inject arbitrary SQL commands.</p>
<h3 id="impact">Impact</h3>
<p>An authenticated user with API access can exploit this vulnerability to execute arbitrary SQL queries against the underlying database. This allows unauthorized retrieval of sensitive database contents—including user credentials, system configurations, and personal identifiable information (PII) of contacts—bypassing standard data access permissions.</p>
<h3 id="patched-versions">Patched Versions</h3>
<p>This security issue has been fixed in the following releases:</p>
<ul>
<li><strong>7.1.2</strong></li>
<li><strong>6.0.9</strong></li>
<li><strong>5.2.11</strong></li>
<li><strong>4.4.20</strong> <a href="https://mautic.org/extended-long-term-support-elts/">ELTS</a></li>
</ul>
<p>We strongly recommend upgrading to the latest version corresponding to your release branch.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There are no official workarounds. To mitigate this issue without upgrading, you may temporarily disable API access or restrict API permissions to highly trusted accounts.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mautic/mautic/security/advisories/GHSA-fcmw-wx57-9p75">https://github.com/mautic/mautic/security/advisories/GHSA-fcmw-wx57-9p75</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-4776">https://nvd.nist.gov/vuln/detail/CVE-2026-4776</a></li>
<li><a href="https://github.com/advisories/GHSA-fcmw-wx57-9p75">https://github.com/advisories/GHSA-fcmw-wx57-9p75</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fcmw-wx57-9p75</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T19:25:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[froxlor/froxlor] Froxlor customer can create MySQL databases on disallowed servers via Mysqls.add API]]></title>
        <id>https://github.com/advisories/GHSA-q4rm-m6xh-5pv7</id>
        <link href="https://github.com/advisories/GHSA-q4rm-m6xh-5pv7"/>
        <updated>2026-07-02T19:23:52.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>Mysqls.add</code> API command (<code>lib/Froxlor/Api/Commands/Mysqls.php</code>) accepts a customer-controlled <code>mysql_server</code> parameter and only validates that the value is numeric and that the server index exists in <code>userdata.inc.php</code>. It never checks the value against the calling customer's <code>allowed_mysqlserver</code> allowlist. A customer can therefore create a database, plus a MySQL user with a password they choose, on any MySQL server the operator has configured — including servers that were explicitly excluded from that customer (e.g. a separate cluster, premium-tier host, or another tenant pool). The same <code>allowed_mysqlserver</code> check is correctly enforced in <code>MysqlServer::get()</code> / <code>MysqlServer::listing()</code> and in the customer-facing UI (<code>customer_mysql.php</code>), confirming the omission is a bug, not by-design.</p>
<h2 id="details">Details</h2>
<p><strong>Vulnerable code path</strong> — <code>lib/Froxlor/Api/Commands/Mysqls.php:69-99</code> (<code>add()</code>):</p>
<pre><code class="language-php">public function add()
{
    if (($this-&gt;getUserDetail('mysqls_used') &lt; $this-&gt;getUserDetail('mysqls') || ...) {
        ...
        $customer = $this-&gt;getCustomerData('mysqls');                       // line 80
        $dbserver = $this-&gt;getParam('mysql_server', true,                    // line 81 — user-controlled
            $this-&gt;getDefaultMySqlServer($customer));
        ...
        $dbserver = Validate::validate($dbserver, ..., '/^[0-9]+$/', ...);   // line 92 — numeric only
        Database::needRoot(true, $dbserver, false);                          // line 93 — root ctx for ANY index
        Database::needSqlData();
        $sql_root = Database::getSqlData();
        Database::needRoot(false);
        if (!is_array($sql_root)) {                                          // line 97 — only existence check
            throw new Exception("Database server with index #" . $dbserver . " is unknown", 404);
        }
        ...
        $username = $dbm-&gt;createDatabase($newdb_params['loginname'], $password,
            $dbserver, ...);                                                 // line 116/118 — DB+user created
        ...
        Database::pexecute($stmt, ["customerid"=&gt;$customer['customerid'], ..., "dbserver"=&gt;$dbserver], ...);
    }
}
</code></pre>
<p>The <code>$customer['allowed_mysqlserver']</code> field IS read on line 80 but is only consumed by <code>getDefaultMySqlServer()</code> (lines 566-573) to compute a default when the request omits <code>mysql_server</code>. As soon as the client supplies the parameter, the default path is skipped and no further authorization gate runs.</p>
<p><strong>Cross-file evidence the check is intended elsewhere:</strong></p>
<ul>
<li><code>lib/Froxlor/Api/Commands/MysqlServer.php:319-323</code> — <code>get()</code> rejects with HTTP 405 when <code>$dbserver</code> is not in <code>allowed_mysqlserver</code>:<pre><code class="language-php">if ($this-&gt;isAdmin() == false) {
    $allowed_mysqls = json_decode($this-&gt;getUserDetail('allowed_mysqlserver'), true);
    if ($allowed_mysqls === false || empty($allowed_mysqls) || !in_array($dbserver, $allowed_mysqls)) {
        throw new Exception("You cannot access this resource", 405);
    }
    ...
}
</code></pre>
</li>
<li><code>lib/Froxlor/Api/Commands/MysqlServer.php:252-257</code> — same allowlist filter on <code>listing()</code>.</li>
<li><code>customer_mysql.php:222</code> — UI rejects with <code>Response::dynamicError('No permission')</code> when <code>empty($allowed_mysqlservers)</code>.</li>
</ul>
<p><strong>Chain of execution (attacker → impact):</strong></p>
<ol>
<li>Customer authenticates to <code>api.php</code> with apikey/secret. The only API gate is <code>cust_api_allowed</code>; <code>allowed_mysqlserver</code> is not consulted at auth time.</li>
<li>Customer sends JSON <code>{"command":"Mysqls.add","params":{"mysql_password":"&lt;valid&gt;","mysql_server":&lt;disallowed_idx&gt;}}</code>.</li>
<li><code>Mysqls.php:71</code> quota check passes (<code>mysqls_used &lt; mysqls</code>).</li>
<li><code>Mysqls.php:80</code> <code>getCustomerData('mysqls')</code> returns the caller's own row.</li>
<li><code>Mysqls.php:81</code> <code>$dbserver</code> is set from the request (default-fallback path skipped).</li>
<li><code>Mysqls.php:92</code> numeric regex passes.</li>
<li><code>Mysqls.php:93-99</code> <code>Database::needRoot(true, $dbserver, false)</code> switches to the root context of the attacker-chosen server; existence check passes.</li>
<li><code>Mysqls.php:116/118</code> <code>DbManager::createDatabase(...)</code> runs against the disallowed server using stored root credentials, creating the DB and granting the supplied password to <code>&lt;loginname&gt;_&lt;sqlN&gt;</code> (DbManager.php:177-218).</li>
<li><code>Mysqls.php:127-141</code> inserts a row into <code>TABLE_PANEL_DATABASES</code> with the attacker's <code>customerid</code> and the disallowed <code>dbserver</code>, allowing later management via <code>Mysqls.get/update/delete</code> (which only filter by <code>customerid</code> for non-admins, e.g. <code>Mysqls.php:282</code>).</li>
</ol>
<h2 id="poc">PoC</h2>
<p>Preconditions on the target instance:</p>
<ul>
<li>≥2 MySQL servers configured in <code>lib/userdata.inc.php</code> (e.g. index 0 default, index 1 internal/premium).</li>
<li>Customer X with <code>allowed_mysqlserver=[0]</code>, <code>cust_api_allowed=1</code>, <code>mysqls &gt; 0</code>, and an issued API key (<code>apikey:secret</code>).</li>
</ul>
<p>Request — customer creates a database on server <code>1</code>, which is <em>not</em> in their allowlist:</p>
<pre><code class="language-bash">curl -k -u 'CUST_APIKEY:CUST_SECRET' \
  -H 'Content-Type: application/json' \
  -X POST \
  -d '{"command":"Mysqls.add","params":{"mysql_password":"ValidP@ssw0rd!","mysql_server":1}}' \
  https://froxlor.example.com/api.php
</code></pre>
<p>Expected (mirroring <code>MysqlServer.get()</code> behaviour): <code>HTTP 405 — "You cannot access this resource"</code>.
Actual: <code>HTTP 200</code> with the full database record, e.g.:</p>
<pre><code class="language-json">{"data":{"id":42,"customerid":&lt;cust_id&gt;,"databasename":"&lt;loginname&gt;_sql1","dbserver":1,...}}
</code></pre>
<p>Verify the credentials work on the forbidden server:</p>
<pre><code class="language-bash">mysql -h server1.host -u &lt;loginname&gt;_sql1 -p   # password: ValidP@ssw0rd!
mysql&gt; SHOW DATABASES;        # the new DB is present
mysql&gt; USE &lt;loginname&gt;_sql1;  # full access to the newly-created DB
</code></pre>
<p>The customer can subsequently manage the DB via <code>Mysqls.get</code>, <code>Mysqls.update</code>, and <code>Mysqls.delete</code> — those non-admin code paths filter only by <code>customerid</code> (<code>Mysqls.php:282-289</code>, <code>Mysqls.php:380-391</code>), which matches.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>Bypass of the per-customer MySQL-server allowlist (<code>allowed_mysqlserver</code>) enforced by the admin/reseller. The authorization model is fully defeated for the <code>add</code> operation.</li>
<li>The customer obtains valid MySQL credentials on a server the operator explicitly excluded for them — possibly an internal/separate cluster, billing tier, premium-only host, or a server provisioned for a different tenant pool.</li>
<li>The customer can persist a DB on the forbidden server (resource and policy bypass), then read/write data there, and continue to manage it through <code>Mysqls.update</code> / <code>Mysqls.delete</code>.</li>
<li>Impact is bounded: privileges granted by <code>DbManager::grantPrivilegesTo</code> apply only to the new <code>&lt;loginname&gt;_sqlN</code> database, so no cross-tenant data exposure on the forbidden server. The damage is policy bypass, resource consumption on the forbidden server, and credential persistence there.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Mirror the allowlist check already present in <code>MysqlServer::get()</code>. After the numeric validation on <code>Mysqls.php:92</code>, before <code>Database::needRoot(...)</code>, add for non-admin callers:</p>
<pre><code class="language-php">// validate whether the dbserver exists
$dbserver = Validate::validate($dbserver, html_entity_decode(lng('mysql.mysql_server')), '/^[0-9]+$/', '', 0, true);

// enforce per-customer allowed_mysqlserver allowlist (parity with MysqlServer::get())
if (!$this-&gt;isAdmin()) {
    $allowed = json_decode($customer['allowed_mysqlserver'] ?? '[]', true);
    if (!is_array($allowed) || empty($allowed)
        || !in_array((int)$dbserver, array_map('intval', $allowed), true)) {
        throw new Exception('You cannot access this resource', 405);
    }
}

Database::needRoot(true, $dbserver, false);
</code></pre>
<p>Audit <code>Mysqls::update()</code>, <code>Mysqls::delete()</code>, and <code>Mysqls::get()</code> for the same gap: those endpoints accept <code>mysql_server</code> and ultimately call <code>Database::needRoot(true, $result['dbserver'], false)</code> on the row's stored value. Once the row exists with a forbidden <code>dbserver</code>, those paths execute against the forbidden server unchallenged. Consider rejecting any non-admin operation whose target row's <code>dbserver</code> is outside <code>allowed_mysqlserver</code>, even if the row already exists, to defend in depth.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/froxlor/froxlor/security/advisories/GHSA-q4rm-m6xh-5pv7">https://github.com/froxlor/froxlor/security/advisories/GHSA-q4rm-m6xh-5pv7</a></li>
<li><a href="https://github.com/advisories/GHSA-q4rm-m6xh-5pv7">https://github.com/advisories/GHSA-q4rm-m6xh-5pv7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q4rm-m6xh-5pv7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-02T19:23:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[froxlor/froxlor] Froxlor: Authenticated customers can read other customers' allowed sender aliases]]></title>
        <id>https://github.com/advisories/GHSA-mr9h-45p9-fg8h</id>
        <link href="https://github.com/advisories/GHSA-mr9h-45p9-fg8h"/>
        <updated>2026-07-02T19:23:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>An authenticated customer can read other customers' allowed sender aliases from Froxlor's sender-delete confirmation page when <code>mail.enable_allow_sender</code> is enabled. <code>customer_email.php</code> loads <code>allowed_sender</code> by global auto-increment <code>senderid</code> alone, so a customer can enumerate foreign sender alias IDs and make Froxlor disclose those values in the confirmation dialog for the attacker's own mailbox.</p>
<h2 id="details">Details</h2>
<p>The vulnerable read lives in <code>customer_email.php</code>:</p>
<pre><code class="language-php">$senderid = Request::any('senderid', 0);
...
$sel_stmt = Database::prepare("SELECT `allowed_sender` FROM `" . TABLE_MAIL_SENDER_ALIAS . "` WHERE `id` = :sid");
$sender_data = Database::pexecute_first($sel_stmt, ['sid' =&gt; $senderid]);

HTML::askYesNo('email_reallydelete_sender', $filename, [
    'id' =&gt; $id,
    'senderid' =&gt; $senderid,
    'page' =&gt; $page,
    'domainid' =&gt; $email_domainid,
    'action' =&gt; $action
], $idna_convert-&gt;decode($result['email_full']) . ' -&gt; ' . $sender_data['allowed_sender']);
</code></pre>
<p>The query does not scope <code>senderid</code> to the current customer or to the mailbox being edited. <code>mail_sender_aliases.id</code> is a global <code>AUTO_INCREMENT</code> primary key:</p>
<pre><code class="language-sql">CREATE TABLE `mail_sender_aliases` (
  `id` int(11) NOT NULL auto_increment,
  `email` varchar(255) NOT NULL,
  `allowed_sender` varchar(255) NOT NULL,
  PRIMARY KEY  (`id`)
)
</code></pre>
<p>That makes sender alias IDs enumerable. A customer who owns any mailbox with sender-alias management enabled can request the delete-confirmation page for their own mailbox while supplying foreign <code>senderid</code> values. Froxlor then renders the foreign <code>allowed_sender</code> string in the confirmation prompt.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Verified against Froxlor <code>2.3.6</code> on a fresh test install with <code>mail.enable_allow_sender=1</code>.</p>
<ol>
<li>Create two customers, <code>victim</code> and <code>attacker</code>.</li>
<li>Give each customer one mailbox.</li>
<li>Add a sender alias for the victim mailbox:</li>
</ol>
<pre><code class="language-text">info@victim.local -&gt; leaked-sender@example.org
</code></pre>
<ol start="4">
<li>Log in as the attacker and request the delete-confirmation page for the attacker's own mailbox while supplying the victim's sender alias ID:</li>
</ol>
<pre><code class="language-text">/customer_email.php?page=senders&amp;domainid=4&amp;action=delete&amp;id=2&amp;senderid=1
</code></pre>
<p>Observed in the live test instance:</p>
<pre><code class="language-text">Do you really want to delete the allowed sender info@attacker.local -&gt; leaked-sender@example.org?
</code></pre>
<p>The attacker-controlled mailbox identifier stayed <code>info@attacker.local</code>, but the disclosed sender alias text came from the victim-owned row identified by <code>senderid=1</code>.</p>
<h2 id="impact">Impact</h2>
<p>An authenticated customer can enumerate global sender alias IDs and read other customers' allowed sender values. This is a cross-tenant information disclosure. It does not let the attacker delete the foreign alias because the delete action revalidates ownership.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Scope the sender alias lookup to the current customer and mailbox before rendering the confirmation page. The confirmation flow should fetch the sender alias through the same ownership checks used by <code>EmailSender::delete()</code>.</p>
<hr>
<p><em>Found by <a href="https://aisafe.io">aisafe.io</a></em></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/froxlor/froxlor/security/advisories/GHSA-mr9h-45p9-fg8h">https://github.com/froxlor/froxlor/security/advisories/GHSA-mr9h-45p9-fg8h</a></li>
<li><a href="https://github.com/advisories/GHSA-mr9h-45p9-fg8h">https://github.com/advisories/GHSA-mr9h-45p9-fg8h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mr9h-45p9-fg8h</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-02T19:23:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[craftcms/cms] Craft CMS: Missing peer-permission check in `AssetsController::actionDeleteFolder` allows deletion of other users' assets]]></title>
        <id>https://github.com/advisories/GHSA-7h62-6v23-v8fm</id>
        <link href="https://github.com/advisories/GHSA-7h62-6v23-v8fm"/>
        <updated>2026-07-02T18:49:08.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>AssetsController::actionDeleteFolder()</code> only requires the <code>deleteAssets:&lt;volume-uid&gt;</code> permission for the target folder. It never enforces <code>deletePeerAssets:&lt;volume-uid&gt;</code>, even though <code>Assets::deleteFoldersByIds()</code> cascades deletion to every descendant folder and every asset inside, regardless of who uploaded them. A low-privilege user who has been granted folder-management rights on a shared volume can therefore destroy assets uploaded by other users (peer assets), bypassing the per-asset peer-permission check that the sibling <code>actionDeleteAsset</code> endpoint correctly applies.</p>
<p>This is the same bug class that was just fixed in <code>actionMoveFolder</code> as <strong>GHSA-3w32-23wj-rxg3</strong> (commit <code>05c2042</code>, Apr 23 2026); the fix added <code>requireVolumePermissionByFolder('deletePeerAssets', …)</code> and <code>savePeerAssets</code> checks to the move endpoint but did not propagate to the delete-folder endpoint.</p>
<h2 id="details">Details</h2>
<p><code>src/controllers/AssetsController.php:552-569</code>:</p>
<pre><code class="language-php">public function actionDeleteFolder(): Response
{
    $this-&gt;requireAcceptsJson();
    $folderId = $this-&gt;request-&gt;getRequiredBodyParam('folderId');

    $assets = Craft::$app-&gt;getAssets();
    $folder = $assets-&gt;getFolderById($folderId);

    if (!$folder) {
        throw new BadRequestHttpException('The folder cannot be found');
    }

    // Check if it's possible to delete objects in the target volume.
    $this-&gt;requireVolumePermissionByFolder('deleteAssets', $folder); // &lt;-- only checks deleteAssets
    $assets-&gt;deleteFoldersByIds($folderId);

    return $this-&gt;asSuccess();
}
</code></pre>
<p><code>requireVolumePermissionByFolder()</code> (<code>src/controllers/AssetsControllerTrait.php:75-88</code>) only resolves to a single <code>requirePermission('deleteAssets:&lt;vol-uid&gt;')</code> call. The peer-equivalent helper (<code>requirePeerVolumePermissionByAsset</code>) is never invoked because there is no folder-level peer helper that iterates the folder's contents.</p>
<p><code>Assets::deleteFoldersByIds()</code> (<code>src/services/Assets.php:311-349</code>) then enumerates the folder + every descendant folder, queries every asset under those IDs, and calls <code>Craft::$app-&gt;getElements()-&gt;deleteElement($asset, true)</code> directly:</p>
<pre><code class="language-php">$assetQuery = Asset::find()-&gt;folderId($allFolderIds);
$elementService = Craft::$app-&gt;getElements();

foreach (Db::each($assetQuery) as $asset) {
    $asset-&gt;keepFileOnDelete = !$deleteDir;
    $elementService-&gt;deleteElement($asset, true);
}
</code></pre>
<p>This bypasses <code>Asset::canDelete()</code> (<code>src/elements/Asset.php:1515-1536</code>):</p>
<pre><code class="language-php">public function canDelete(User $user): bool
{
    if ($this-&gt;isFolder) { return false; }
    if (parent::canDelete($user)) { return true; }
    $volume = $this-&gt;getVolume();
    if (Assets::isTempUploadFs($volume-&gt;getFs())) { return true; }

    if ($this-&gt;uploaderId !== $user-&gt;id) {
        return $user-&gt;can("deletePeerAssets:$volume-&gt;uid"); // &lt;-- never reached on cascade delete
    }
    return $user-&gt;can("deleteAssets:$volume-&gt;uid");
}
</code></pre>
<p>Compare to <code>actionDeleteAsset</code> (<code>src/controllers/AssetsController.php:579-613</code>), which correctly does:</p>
<pre><code class="language-php">$this-&gt;requireVolumePermissionByAsset('deleteAssets', $asset);
$this-&gt;requirePeerVolumePermissionByAsset('deletePeerAssets', $asset);
</code></pre>
<p>The fix that landed in <code>05c2042</code> for <code>actionMoveFolder</code> (<code>src/controllers/AssetsController.php:733-765</code>) added both <code>savePeerAssets</code> and <code>deletePeerAssets</code> <code>requireVolumePermissionByFolder</code> checks to mirror the per-asset pattern, but the same hardening was not applied to <code>actionDeleteFolder</code> or <code>actionRenameFolder</code> (which also calls <code>deleteFoldersByIds</code> indirectly through later logic).</p>
<p>The asymmetry between the two endpoints demonstrates the missing check.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>Integrity / availability of other users' assets on any volume where the attacker has <code>deleteAssets</code> but not <code>deletePeerAssets</code>: the attacker can permanently delete peer-owned files (and their parent folder structure) on the underlying filesystem, with no recovery via Craft's UI.</li>
<li>The Craft permission model explicitly distinguishes "delete your own assets" (<code>deleteAssets</code>) from "delete other users' assets" (<code>deletePeerAssets</code>) precisely so administrators can grant the former without the latter on shared volumes — this finding renders that distinction unenforceable for any user given folder-delete rights.</li>
<li>No information disclosure or remote code execution; impact is bounded to the affected volume's contents.</li>
<li>Does not require any non-default configuration: the affected endpoint is enabled by default and only requires that an administrator has split <code>deleteAssets</code> from <code>deletePeerAssets</code> (the documented, supported permission model).</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/craftcms/cms/security/advisories/GHSA-7h62-6v23-v8fm">https://github.com/craftcms/cms/security/advisories/GHSA-7h62-6v23-v8fm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50284">https://nvd.nist.gov/vuln/detail/CVE-2026-50284</a></li>
<li><a href="https://github.com/craftcms/cms/commit/b4e08977f0c9bdf002a77f9f6d1346cd55ac0598">https://github.com/craftcms/cms/commit/b4e08977f0c9bdf002a77f9f6d1346cd55ac0598</a></li>
<li><a href="https://github.com/advisories/GHSA-7h62-6v23-v8fm">https://github.com/advisories/GHSA-7h62-6v23-v8fm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7h62-6v23-v8fm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T18:49:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[craftcms/cms] Craft CMS: Missing peer-permission check in `AssetsController::actionDeleteFolder` allows deletion of other users' assets]]></title>
        <id>https://github.com/advisories/GHSA-7h62-6v23-v8fm</id>
        <link href="https://github.com/advisories/GHSA-7h62-6v23-v8fm"/>
        <updated>2026-07-02T18:49:08.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>AssetsController::actionDeleteFolder()</code> only requires the <code>deleteAssets:&lt;volume-uid&gt;</code> permission for the target folder. It never enforces <code>deletePeerAssets:&lt;volume-uid&gt;</code>, even though <code>Assets::deleteFoldersByIds()</code> cascades deletion to every descendant folder and every asset inside, regardless of who uploaded them. A low-privilege user who has been granted folder-management rights on a shared volume can therefore destroy assets uploaded by other users (peer assets), bypassing the per-asset peer-permission check that the sibling <code>actionDeleteAsset</code> endpoint correctly applies.</p>
<p>This is the same bug class that was just fixed in <code>actionMoveFolder</code> as <strong>GHSA-3w32-23wj-rxg3</strong> (commit <code>05c2042</code>, Apr 23 2026); the fix added <code>requireVolumePermissionByFolder('deletePeerAssets', …)</code> and <code>savePeerAssets</code> checks to the move endpoint but did not propagate to the delete-folder endpoint.</p>
<h2 id="details">Details</h2>
<p><code>src/controllers/AssetsController.php:552-569</code>:</p>
<pre><code class="language-php">public function actionDeleteFolder(): Response
{
    $this-&gt;requireAcceptsJson();
    $folderId = $this-&gt;request-&gt;getRequiredBodyParam('folderId');

    $assets = Craft::$app-&gt;getAssets();
    $folder = $assets-&gt;getFolderById($folderId);

    if (!$folder) {
        throw new BadRequestHttpException('The folder cannot be found');
    }

    // Check if it's possible to delete objects in the target volume.
    $this-&gt;requireVolumePermissionByFolder('deleteAssets', $folder); // &lt;-- only checks deleteAssets
    $assets-&gt;deleteFoldersByIds($folderId);

    return $this-&gt;asSuccess();
}
</code></pre>
<p><code>requireVolumePermissionByFolder()</code> (<code>src/controllers/AssetsControllerTrait.php:75-88</code>) only resolves to a single <code>requirePermission('deleteAssets:&lt;vol-uid&gt;')</code> call. The peer-equivalent helper (<code>requirePeerVolumePermissionByAsset</code>) is never invoked because there is no folder-level peer helper that iterates the folder's contents.</p>
<p><code>Assets::deleteFoldersByIds()</code> (<code>src/services/Assets.php:311-349</code>) then enumerates the folder + every descendant folder, queries every asset under those IDs, and calls <code>Craft::$app-&gt;getElements()-&gt;deleteElement($asset, true)</code> directly:</p>
<pre><code class="language-php">$assetQuery = Asset::find()-&gt;folderId($allFolderIds);
$elementService = Craft::$app-&gt;getElements();

foreach (Db::each($assetQuery) as $asset) {
    $asset-&gt;keepFileOnDelete = !$deleteDir;
    $elementService-&gt;deleteElement($asset, true);
}
</code></pre>
<p>This bypasses <code>Asset::canDelete()</code> (<code>src/elements/Asset.php:1515-1536</code>):</p>
<pre><code class="language-php">public function canDelete(User $user): bool
{
    if ($this-&gt;isFolder) { return false; }
    if (parent::canDelete($user)) { return true; }
    $volume = $this-&gt;getVolume();
    if (Assets::isTempUploadFs($volume-&gt;getFs())) { return true; }

    if ($this-&gt;uploaderId !== $user-&gt;id) {
        return $user-&gt;can("deletePeerAssets:$volume-&gt;uid"); // &lt;-- never reached on cascade delete
    }
    return $user-&gt;can("deleteAssets:$volume-&gt;uid");
}
</code></pre>
<p>Compare to <code>actionDeleteAsset</code> (<code>src/controllers/AssetsController.php:579-613</code>), which correctly does:</p>
<pre><code class="language-php">$this-&gt;requireVolumePermissionByAsset('deleteAssets', $asset);
$this-&gt;requirePeerVolumePermissionByAsset('deletePeerAssets', $asset);
</code></pre>
<p>The fix that landed in <code>05c2042</code> for <code>actionMoveFolder</code> (<code>src/controllers/AssetsController.php:733-765</code>) added both <code>savePeerAssets</code> and <code>deletePeerAssets</code> <code>requireVolumePermissionByFolder</code> checks to mirror the per-asset pattern, but the same hardening was not applied to <code>actionDeleteFolder</code> or <code>actionRenameFolder</code> (which also calls <code>deleteFoldersByIds</code> indirectly through later logic).</p>
<p>The asymmetry between the two endpoints demonstrates the missing check.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>Integrity / availability of other users' assets on any volume where the attacker has <code>deleteAssets</code> but not <code>deletePeerAssets</code>: the attacker can permanently delete peer-owned files (and their parent folder structure) on the underlying filesystem, with no recovery via Craft's UI.</li>
<li>The Craft permission model explicitly distinguishes "delete your own assets" (<code>deleteAssets</code>) from "delete other users' assets" (<code>deletePeerAssets</code>) precisely so administrators can grant the former without the latter on shared volumes — this finding renders that distinction unenforceable for any user given folder-delete rights.</li>
<li>No information disclosure or remote code execution; impact is bounded to the affected volume's contents.</li>
<li>Does not require any non-default configuration: the affected endpoint is enabled by default and only requires that an administrator has split <code>deleteAssets</code> from <code>deletePeerAssets</code> (the documented, supported permission model).</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/craftcms/cms/security/advisories/GHSA-7h62-6v23-v8fm">https://github.com/craftcms/cms/security/advisories/GHSA-7h62-6v23-v8fm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50284">https://nvd.nist.gov/vuln/detail/CVE-2026-50284</a></li>
<li><a href="https://github.com/craftcms/cms/commit/b4e08977f0c9bdf002a77f9f6d1346cd55ac0598">https://github.com/craftcms/cms/commit/b4e08977f0c9bdf002a77f9f6d1346cd55ac0598</a></li>
<li><a href="https://github.com/advisories/GHSA-7h62-6v23-v8fm">https://github.com/advisories/GHSA-7h62-6v23-v8fm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7h62-6v23-v8fm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T18:49:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[craftcms/cms] Craft CMS: Unauthorized Deletion of Source Assets During File Replacement]]></title>
        <id>https://github.com/advisories/GHSA-qh45-9g5p-m2v4</id>
        <link href="https://github.com/advisories/GHSA-qh45-9g5p-m2v4"/>
        <updated>2026-07-02T18:48:28.000Z</updated>
        <content type="html"><![CDATA[<p>We have identified an authorization issue in Craft CMS <code>AssetsController::actionReplaceFile</code> that can delete a source asset without source delete permission by supplying both <code>assetId</code> and <code>sourceAssetId</code>.</p>
<h3 id="description">Description</h3>
<p>Craft CMS’s <code>craft\\controllers\\AssetsController::actionReplaceFile()</code> supports replacing a target asset file using another existing asset as the source. The action loads:</p>
<ul>
<li><code>$assetToReplace</code> from <code>assetId</code>  </li>
<li><code>$sourceAsset</code> from <code>sourceAssetId</code></li>
</ul>
<p>It then enforces replace permissions using <code>($assetToReplace ?: $sourceAsset)</code>. When both IDs are provided, this expression resolves to the target asset so no permission check is performed against the source asset volume.</p>
<pre><code class="language-php">$this-&gt;requireVolumePermissionByAsset('replaceFiles', $assetToReplace ?: $sourceAsset);
$this-&gt;requirePeerVolumePermissionByAsset('replacePeerFiles', $assetToReplace ?: $sourceAsset);
</code></pre>
<p><a href="https://github.com/craftcms/cms/blob/5.x/src/controllers/AssetsController.php#L433-L434"><em>src/controllers/AssetsController.php:L433-L434</em></a></p>
<p>In the branch where both assets are present, Craft copies the source file into the target and then deletes the source asset. There is no check for <code>deleteAssets:&lt;sourceVolumeUid&gt;</code> or <code>deletePeerAssets:&lt;sourceVolumeUid&gt;</code> for the source asset before deletion.</p>
<pre><code class="language-php">$assets-&gt;replaceAssetFile($assetToReplace, $tempPath, $assetToReplace-&gt;getFilename(), $sourceAsset-&gt;getMimeType());
Craft::$app-&gt;getElements()-&gt;deleteElement($sourceAsset);
</code></pre>
<p><a href="https://github.com/craftcms/cms/blob/5.x/src/controllers/AssetsController.php#L462-L463"><em>src/controllers/AssetsController.php:L462-L463</em></a></p>
<h3 id="impact">Impact</h3>
<p>An authenticated user who can replace files in one volume can delete assets in another volume where they do not have delete permission, as long as they can obtain a <code>sourceAssetId</code>. This can lead to unauthorized asset deletion, broken content references, and data loss.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/craftcms/cms/security/advisories/GHSA-qh45-9g5p-m2v4">https://github.com/craftcms/cms/security/advisories/GHSA-qh45-9g5p-m2v4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50283">https://nvd.nist.gov/vuln/detail/CVE-2026-50283</a></li>
<li><a href="https://github.com/craftcms/cms/commit/2c2579c7f1030872423f268d0c8b48377101961d">https://github.com/craftcms/cms/commit/2c2579c7f1030872423f268d0c8b48377101961d</a></li>
<li><a href="https://github.com/advisories/GHSA-qh45-9g5p-m2v4">https://github.com/advisories/GHSA-qh45-9g5p-m2v4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qh45-9g5p-m2v4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-02T18:48:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[craftcms/cms] Craft CMS: Unauthorized Deletion of Source Assets During File Replacement]]></title>
        <id>https://github.com/advisories/GHSA-qh45-9g5p-m2v4</id>
        <link href="https://github.com/advisories/GHSA-qh45-9g5p-m2v4"/>
        <updated>2026-07-02T18:48:28.000Z</updated>
        <content type="html"><![CDATA[<p>We have identified an authorization issue in Craft CMS <code>AssetsController::actionReplaceFile</code> that can delete a source asset without source delete permission by supplying both <code>assetId</code> and <code>sourceAssetId</code>.</p>
<h3 id="description">Description</h3>
<p>Craft CMS’s <code>craft\\controllers\\AssetsController::actionReplaceFile()</code> supports replacing a target asset file using another existing asset as the source. The action loads:</p>
<ul>
<li><code>$assetToReplace</code> from <code>assetId</code>  </li>
<li><code>$sourceAsset</code> from <code>sourceAssetId</code></li>
</ul>
<p>It then enforces replace permissions using <code>($assetToReplace ?: $sourceAsset)</code>. When both IDs are provided, this expression resolves to the target asset so no permission check is performed against the source asset volume.</p>
<pre><code class="language-php">$this-&gt;requireVolumePermissionByAsset('replaceFiles', $assetToReplace ?: $sourceAsset);
$this-&gt;requirePeerVolumePermissionByAsset('replacePeerFiles', $assetToReplace ?: $sourceAsset);
</code></pre>
<p><a href="https://github.com/craftcms/cms/blob/5.x/src/controllers/AssetsController.php#L433-L434"><em>src/controllers/AssetsController.php:L433-L434</em></a></p>
<p>In the branch where both assets are present, Craft copies the source file into the target and then deletes the source asset. There is no check for <code>deleteAssets:&lt;sourceVolumeUid&gt;</code> or <code>deletePeerAssets:&lt;sourceVolumeUid&gt;</code> for the source asset before deletion.</p>
<pre><code class="language-php">$assets-&gt;replaceAssetFile($assetToReplace, $tempPath, $assetToReplace-&gt;getFilename(), $sourceAsset-&gt;getMimeType());
Craft::$app-&gt;getElements()-&gt;deleteElement($sourceAsset);
</code></pre>
<p><a href="https://github.com/craftcms/cms/blob/5.x/src/controllers/AssetsController.php#L462-L463"><em>src/controllers/AssetsController.php:L462-L463</em></a></p>
<h3 id="impact">Impact</h3>
<p>An authenticated user who can replace files in one volume can delete assets in another volume where they do not have delete permission, as long as they can obtain a <code>sourceAssetId</code>. This can lead to unauthorized asset deletion, broken content references, and data loss.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/craftcms/cms/security/advisories/GHSA-qh45-9g5p-m2v4">https://github.com/craftcms/cms/security/advisories/GHSA-qh45-9g5p-m2v4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50283">https://nvd.nist.gov/vuln/detail/CVE-2026-50283</a></li>
<li><a href="https://github.com/craftcms/cms/commit/2c2579c7f1030872423f268d0c8b48377101961d">https://github.com/craftcms/cms/commit/2c2579c7f1030872423f268d0c8b48377101961d</a></li>
<li><a href="https://github.com/advisories/GHSA-qh45-9g5p-m2v4">https://github.com/advisories/GHSA-qh45-9g5p-m2v4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qh45-9g5p-m2v4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-02T18:48:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[craftcms/cms] Craft CMS: Authorization bypass in `entries/move-to-section` via missing target-section save check]]></title>
        <id>https://github.com/advisories/GHSA-43cq-c2gq-pfpw</id>
        <link href="https://github.com/advisories/GHSA-43cq-c2gq-pfpw"/>
        <updated>2026-07-02T18:47:38.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>EntriesController::actionMoveToSection()</code> endpoint checks only whether the current user can view the destination section, but it does not require permission to save entries into that section. A low-privileged authenticated control-panel user who can move an entry out of its current section can therefore move that entry into a different section where they have read access but no write access.</p>
<h3 id="details">Details</h3>
<p>The vulnerable route is implemented in <a href="/D:/files/projects/cms-5.9.19/cms-5.9.19/src/controllers/EntriesController.php">EntriesController.php</a>:465:</p>
<p>The destination check is only <code>viewEntries:$section-&gt;uid</code> . The source-entry gate is <code>Entry::canMove()</code>, which verifies whether the user can move the existing entry based on the source section:</p>
<p>This closes the exploit chain:</p>
<ol>
<li>External source: authenticated CP request to <code>entries/move-to-section</code>.</li>
<li>Missing authorization check: destination section requires only <code>viewEntries</code>, not <code>saveEntries</code>.</li>
<li>Privileged sink: <code>moveEntryToSection()</code> rewrites <code>sectionId</code> and saves the entry into the unauthorized section.</li>
</ol>
<p>Preconditions derived from the code:</p>
<ol>
<li>The attacker is authenticated to the control panel.</li>
<li>Entry <code>345</code> is movable by the attacker from its current section.</li>
<li>The attacker can satisfy <code>viewEntries</code> on destination section <code>12</code>.</li>
<li>The attacker does not have <code>saveEntries:DESTINATION_UID</code>, which is the missing check that makes the bypass possible.</li>
</ol>
<p>Result:</p>
<ol>
<li>The controller accepts the request because <code>viewEntries:$section-&gt;uid</code> passes.</li>
<li>Each source entry passes <code>canMove()</code> based on source-section permissions.</li>
<li><code>moveEntryToSection()</code> updates the entry’s <code>sectionId</code> and saves it.</li>
<li>The entry is now located in a section where the attacker did not have write permission.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>This breaks the intended section-level authorization model. A user with limited content permissions can inject or relocate content into a protected section, interfering with editorial boundaries, approval workflows, section-specific business logic, and content ownership expectations.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/craftcms/cms/security/advisories/GHSA-43cq-c2gq-pfpw">https://github.com/craftcms/cms/security/advisories/GHSA-43cq-c2gq-pfpw</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50280">https://nvd.nist.gov/vuln/detail/CVE-2026-50280</a></li>
<li><a href="https://github.com/craftcms/cms/commit/0a6b916f6367b0162b2eaf2366add67b45fa98ea">https://github.com/craftcms/cms/commit/0a6b916f6367b0162b2eaf2366add67b45fa98ea</a></li>
<li><a href="https://github.com/advisories/GHSA-43cq-c2gq-pfpw">https://github.com/advisories/GHSA-43cq-c2gq-pfpw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-43cq-c2gq-pfpw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-02T18:47:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[craftcms/cms] Craft CMS: Authorship spoofing in `entries/save-entry` via pre-check/post-mutation authorization gap]]></title>
        <id>https://github.com/advisories/GHSA-qq2c-2q8j-jh27</id>
        <link href="https://github.com/advisories/GHSA-qq2c-2q8j-jh27"/>
        <updated>2026-07-02T18:45:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>EntriesController::actionSaveEntry()</code> performs entry-edit permission checks before request-controlled author changes are applied to the model. The subsequent author mutation path accepts attacker-supplied <code>authors</code> / <code>author</code> parameters and allows the change when the current user is one of the old authors. Because the controller does not re-run authorization after mutating the author list, a low-privileged user can reassign an entry’s authorship to another user without holding the dedicated peer-author-change permission.</p>
<h3 id="details">Details</h3>
<p>The control flow begins in <a href="/D:/files/projects/cms-5.9.19/cms-5.9.19/src/controllers/EntriesController.php">EntriesController.php</a>:249. <code>actionSaveEntry()</code> loads the entry and enforces edit permissions before calling <code>_populateEntryModel()</code>:</p>
<pre><code class="language-php">public function actionSaveEntry(bool $duplicate = false): ?Response
{
    ...
    $entry = $this-&gt;_editableEntry($this-&gt;request-&gt;getBodyParam('entryId'), $siteId);
    ...
    $this-&gt;enforceEditEntryPermissions($entry, $duplicate);
    ...
    $this-&gt;_populateEntryModel($entry);
    ...
    $success = Craft::$app-&gt;getElements()-&gt;saveElement($entry);
}
</code></pre>
<p>The attacker-controlled source is in <a href="/D:/files/projects/cms-5.9.19/cms-5.9.19/src/controllers/EntriesController.php">EntriesController.php</a>:588:</p>
<pre><code class="language-php">$entry-&gt;setAttributesFromRequest(array_filter([
    'authorIds' =&gt; $this-&gt;request-&gt;getBodyParam('authors') ??
        $this-&gt;request-&gt;getBodyParam('author') ??
        $entry-&gt;getAuthorId() ??
        static::currentUser()-&gt;id,
]));
</code></pre>
<p><code>Entry::setAttributesFromRequest()</code> in <a href="/D:/files/projects/cms-5.9.19/cms-5.9.19/src/elements/Entry.php">Entry.php</a>:1124 extracts the new author IDs and applies them if <code>canChangeAuthor()</code> returns true:</p>
<pre><code class="language-php">if (
    ($authorIds !== null || $authorId !== null) &amp;&amp;
    $this-&gt;canChangeAuthor()
) {
    $this-&gt;_oldAuthorIds = $oldAuthorIds;
    $this-&gt;setAuthorIds($authorIds);
}
</code></pre>
<p><code>canChangeAuthor()</code> at <a href="/D:/files/projects/cms-5.9.19/cms-5.9.19/src/elements/Entry.php">Entry.php</a>:2789 allows the author change when the current user can view peer entries and is already one of the existing authors:</p>
<pre><code class="language-php">return (
    empty($authorIds) ||
    in_array($user-&gt;id, $authorIds) ||
    $user-&gt;can("changeAuthorForPeerEntries:$section-&gt;uid")
);
</code></pre>
<p>After the author list is mutated, the controller does not re-check authorization. </p>
<p>This closes the exploit chain:</p>
<ol>
<li>External source: authenticated request to <code>entries/save-entry</code> with attacker-controlled <code>authors[]</code>.</li>
<li>Trust boundary failure: authorization is checked on the pre-mutation entry state, not on the post-mutation author assignment.</li>
<li>Privileged sink: the author relationship is rewritten in persistent storage.</li>
</ol>
<p>Preconditions derived from the source:</p>
<ol>
<li>The attacker is authenticated and can edit entry <code>345</code>.</li>
<li>The attacker is among the existing authors of entry <code>345</code>, or otherwise satisfies <code>canChangeAuthor()</code> through the old author set.</li>
<li>The attacker has <code>viewPeerEntries</code> for the section.</li>
<li>User ID <code>1</code> exists and can be assigned as an author in that section.</li>
</ol>
<p>Result:</p>
<ol>
<li><code>enforceEditEntryPermissions()</code> succeeds on the original entry state.</li>
<li><code>_populateEntryModel()</code> reads <code>authors[]=1</code> from the request body.</li>
<li><code>setAttributesFromRequest()</code> updates <code>authorIds</code> because <code>canChangeAuthor()</code> is evaluated against the old authorship state.</li>
<li><code>saveElement()</code> persists the change and <code>_saveAuthors()</code> rewrites the entry-author relation.</li>
<li>Entry <code>345</code> now appears authored by user <code>1</code>.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>This allows low-privileged users to falsify content ownership and alter the authorship of entries without having the dedicated author-management permission. The impact includes corrupted audit trails, misleading notifications, broken approval workflows, and unauthorized reassignment of content responsibility.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/craftcms/cms/security/advisories/GHSA-qq2c-2q8j-jh27">https://github.com/craftcms/cms/security/advisories/GHSA-qq2c-2q8j-jh27</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50279">https://nvd.nist.gov/vuln/detail/CVE-2026-50279</a></li>
<li><a href="https://github.com/craftcms/cms/commit/9cc493be8b414d7116c7f2bc2a6d0926e73f1248">https://github.com/craftcms/cms/commit/9cc493be8b414d7116c7f2bc2a6d0926e73f1248</a></li>
<li><a href="https://github.com/advisories/GHSA-qq2c-2q8j-jh27">https://github.com/advisories/GHSA-qq2c-2q8j-jh27</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qq2c-2q8j-jh27</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T18:45:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mediawiki/maps] mediawiki/maps has stored XSS through the overlays parameter in the display_map parser function]]></title>
        <id>https://github.com/advisories/GHSA-4h7g-5542-v3fc</id>
        <link href="https://github.com/advisories/GHSA-4h7g-5542-v3fc"/>
        <updated>2026-07-02T17:51:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Stored XSS through wikitext can be performed by inserting malicious HTML into the <code>overlays</code> parameter of the <code>display_map</code> parser function when using the leaflet service.</p>
<h3 id="details">Details</h3>
<p>The maps extension doesn't escape overlay names before passing them to leaflet.
Leaflet then inserts them as HTML: <a href="https://github.com/ProfessionalWiki/Maps/blob/ca5139fabd75f3c34f47ea3fd161306506b053bc/resources/lib/leaflet/leaflet.js#L5243">https://github.com/ProfessionalWiki/Maps/blob/ca5139fabd75f3c34f47ea3fd161306506b053bc/resources/lib/leaflet/leaflet.js#L5243</a></p>
<h3 id="poc">PoC</h3>
<p>Preview the following wikitext, using the default configuration options of the extension:</p>
<pre><code>{{#display_map:0,0|service=leaflet|overlays=OpenTopoMap.&lt;img src=x onerror="alert(1);"&gt;}}
</code></pre>
<h3 id="impact">Impact</h3>
<p>Stored XSS can be performed by any user with the <code>edit</code> permission.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ProfessionalWiki/Maps/security/advisories/GHSA-4h7g-5542-v3fc">https://github.com/ProfessionalWiki/Maps/security/advisories/GHSA-4h7g-5542-v3fc</a></li>
<li><a href="https://github.com/advisories/GHSA-4h7g-5542-v3fc">https://github.com/advisories/GHSA-4h7g-5542-v3fc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4h7g-5542-v3fc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T17:51:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kimai/kimai] Kimai Password Reset Link Remains Valid After Password Change]]></title>
        <id>https://github.com/advisories/GHSA-m492-gv72-xvxj</id>
        <link href="https://github.com/advisories/GHSA-m492-gv72-xvxj"/>
        <updated>2026-07-01T19:49:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The LoginLink signature used for password reset URLs covers only the user's <code>id</code> — it does not include the password hash. After a user clicks a reset link and successfully changes their password, the same link remains valid for up to 2 additional uses within a 1-hour window. Anyone who intercepts or caches the original link can log in as the user even after the password has been changed.</p>
<h3 id="details">Details</h3>
<p><code>config/packages/security.yaml:73-78</code> configures the login link:</p>
<pre><code class="language-yaml">login_link:
    check_route: link_login_check
    signature_properties: ['id']
    lifetime: 900
    max_uses: 3
</code></pre>
<p>The HMAC signature covers only <code>id</code>, not <code>password</code>, <code>username</code>, or <code>email</code>. The effective lifetime for password reset links is extended to 3600 seconds by <code>PasswordResetController.php:106-119</code> via <code>getPasswordResetRetryLifetime()</code>.</p>
<p>After the first successful use (where the user sets a new password), the <code>__pw_reset__</code> session flag is cleared by <code>WizardController.php:105</code>. Subsequent uses of the same link bypass the forced-password-reset wizard and grant a normal authenticated session.</p>
<p>Symfony's <code>LoginLinkHandler::consumeLoginLink</code> decrements a per-link counter but allows up to 3 total uses. Re-issuing a new link does not invalidate the old one.</p>
<p>The same vulnerability affects <code>UserLoginLinkCommand.php:77</code>, which generates admin-on-demand login links with the same signature scheme.</p>
<h3 id="poc">PoC</h3>
<p><strong>Step 1 — Generate a password reset link:</strong></p>
<pre><code class="language-bash">docker exec &lt;kimai_container&gt; bin/console kimai:user:login-link &lt;username&gt;
</code></pre>
<p><strong>Step 2 — Use the link to log in and change the password.</strong></p>
<p><strong>Step 3 — Open the same link in a different browser or incognito session.</strong></p>
<p>The link logs the attacker in as a fully authenticated user, despite the password having been changed in Step 2.</p>
<p><strong>Step 4 — The link works a third time (3 total uses within 1 hour).</strong></p>
<h3 id="impact">Impact</h3>
<p>A password reset link that leaks through any of the following channels remains a valid authentication credential even after the legitimate user has changed their password: corporate mail-relay scanners that pre-click links (Microsoft Defender ATP, Mimecast, Barracuda), shared inboxes, browser history sync across devices, HTTP referrer headers, or proxy/WAF logs. The user believes they have secured their account by setting a new password, but the leaked link still grants full access for up to 2 additional uses within the hour.</p>
<h1 id="solution">Solution</h1>
<p>Login links now also use the <code>password</code> hash for generating the signature, which will immediately expire the link after the user changed the password.</p>
<p>Read <a href="https://www.kimai.org/en/security/ghsa-m492-gv72-xvxj">https://www.kimai.org/en/security/ghsa-m492-gv72-xvxj</a> for more information.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kimai/kimai/security/advisories/GHSA-m492-gv72-xvxj">https://github.com/kimai/kimai/security/advisories/GHSA-m492-gv72-xvxj</a></li>
<li><a href="https://www.kimai.org/en/security/ghsa-m492-gv72-xvxj">https://www.kimai.org/en/security/ghsa-m492-gv72-xvxj</a></li>
<li><a href="https://github.com/advisories/GHSA-m492-gv72-xvxj">https://github.com/advisories/GHSA-m492-gv72-xvxj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m492-gv72-xvxj</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-01T19:49:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[twig/twig] Twig: Sandbox filter, tag and function allow-list bypass when sandbox state changes between renders for a cached `Template`]]></title>
        <id>https://github.com/advisories/GHSA-529h-vh3j-85hq</id>
        <link href="https://github.com/advisories/GHSA-529h-vh3j-85hq"/>
        <updated>2026-07-01T18:55:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="description">Description</h3>
<p>The per-template filter, tag and function allow-list check is compiled into the <code>checkSecurity()</code> method of each <code>Template</code> subclass and was invoked once from the constructor, gated by <code>SandboxExtension::isSandboxed($source)</code>. <code>Template</code> instances are then cached on the <code>Environment</code> in <code>$loadedTemplates</code>, so the verdict computed at construction time was sticky for the rest of the process.</p>
<p>Any later change of sandbox state on the same <code>Environment</code> left that cached verdict in place: toggling <code>SandboxExtension::enableSandbox()</code>/<code>disableSandbox()</code>, swapping the policy via <code>setSecurityPolicy()</code>, a <code>SourcePolicyInterface</code> decision flip, or simply having a parent, macro or included template pre-instantiated outside the sandbox before a sandboxed render reached it. In all of these cases, the filters, tags and functions used by the affected template kept running with the original (typically empty) check, bypassing the <code>SecurityPolicy</code> allow-list.</p>
<p>Method, property and <code>__toString</code> allow-lists are not affected: they are enforced at every call site at runtime through <code>SandboxExtension::checkMethodAllowed()</code>, <code>checkPropertyAllowed()</code> and <code>ensureToStringAllowed()</code>, which re-read the current state on every call.</p>
<p>Long-lived workers (FrankenPHP, RoadRunner, Symfony Messenger consumers, FPM with hot autoloading) that share a single <code>Environment</code> between sandboxed and non-sandboxed renders are the most exposed: a single non-sandboxed render of a shared layout pre-warms its <code>Template</code> instance, after which any later sandboxed render that extends, uses, includes or imports from that layout silently skips the filter/tag/function allow-list for the pre-warmed instance.</p>
<h3 id="resolution">Resolution</h3>
<p>The allow-list check is no longer run from the constructor. <code>Template</code> gains a public <code>ensureSecurityChecked()</code> method that calls the compiled <code>checkSecurity()</code> only when <code>SandboxExtension::isSandboxed($source)</code> returns true for the current source, and it is invoked at every entry point that can reach a <code>Template</code> instance whose security has not yet been verified against the current state: <code>Template::yield()</code>, <code>Template::yieldBlock()</code> (on the resolved block template, which covers <code>extends</code>, <code>use</code>, traits and parent blocks), <code>Template::getParent()</code> (which evaluates user code when the parent name is dynamic) and <code>Template::getTemplateForMacro()</code> (on the resolved macro template).</p>
<p>The explicit <code>checkSecurity()</code> calls previously emitted by <code>IncludeNode</code> and <code>CoreExtension::include()</code> are removed: the included template's own <code>yield()</code> now re-runs the check against the current sandbox state. The compiled <code>checkSecurity()</code> body is a cheap walk over compile-time-static arrays, so the per-render cost is negligible. Old cached compiled PHP files keep working unchanged: the constructor-time call they still contain is idempotent.</p>
<h3 id="credits">Credits</h3>
<p>Twig would like to thank Fabien Potencier for reporting and fixing the issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/twigphp/Twig/security/advisories/GHSA-529h-vh3j-85hq">https://github.com/twigphp/Twig/security/advisories/GHSA-529h-vh3j-85hq</a></li>
<li><a href="https://github.com/twigphp/Twig/releases/tag/v3.27.0">https://github.com/twigphp/Twig/releases/tag/v3.27.0</a></li>
<li><a href="https://github.com/advisories/GHSA-529h-vh3j-85hq">https://github.com/advisories/GHSA-529h-vh3j-85hq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-529h-vh3j-85hq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-01T18:55:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[spatie/schema-org] Schema.org has cross-site scripting (XSS) via script break-out in toScript() output]]></title>
        <id>https://github.com/advisories/GHSA-hwmc-r6mf-jh83</id>
        <link href="https://github.com/advisories/GHSA-hwmc-r6mf-jh83"/>
        <updated>2026-07-01T18:33:05.000Z</updated>
        <content type="html"><![CDATA[<p>Schema.org has a cross-site scripting (XSS) vulnerability via script break-out in toScript() output.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/spatie/schema-org/pull/242">https://github.com/spatie/schema-org/pull/242</a></li>
<li><a href="https://github.com/spatie/schema-org/commit/be389b4759214c11cc1364a16e34a929c5af5a88">https://github.com/spatie/schema-org/commit/be389b4759214c11cc1364a16e34a929c5af5a88</a></li>
<li><a href="https://github.com/FriendsOfPHP/security-advisories/blob/master/spatie/schema-org/2026-04-20.yaml">https://github.com/FriendsOfPHP/security-advisories/blob/master/spatie/schema-org/2026-04-20.yaml</a></li>
<li><a href="https://github.com/spatie/schema-org/releases/tag/4.0.2">https://github.com/spatie/schema-org/releases/tag/4.0.2</a></li>
<li><a href="https://github.com/advisories/GHSA-hwmc-r6mf-jh83">https://github.com/advisories/GHSA-hwmc-r6mf-jh83</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hwmc-r6mf-jh83</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-01T18:32:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[spatie/schema-org] Schema.org has cross-site scripting (XSS) via script break-out in toScript() output]]></title>
        <id>https://github.com/advisories/GHSA-hwmc-r6mf-jh83</id>
        <link href="https://github.com/advisories/GHSA-hwmc-r6mf-jh83"/>
        <updated>2026-07-01T18:33:05.000Z</updated>
        <content type="html"><![CDATA[<p>Schema.org has a cross-site scripting (XSS) vulnerability via script break-out in toScript() output.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/spatie/schema-org/pull/242">https://github.com/spatie/schema-org/pull/242</a></li>
<li><a href="https://github.com/spatie/schema-org/commit/be389b4759214c11cc1364a16e34a929c5af5a88">https://github.com/spatie/schema-org/commit/be389b4759214c11cc1364a16e34a929c5af5a88</a></li>
<li><a href="https://github.com/FriendsOfPHP/security-advisories/blob/master/spatie/schema-org/2026-04-20.yaml">https://github.com/FriendsOfPHP/security-advisories/blob/master/spatie/schema-org/2026-04-20.yaml</a></li>
<li><a href="https://github.com/spatie/schema-org/releases/tag/4.0.2">https://github.com/spatie/schema-org/releases/tag/4.0.2</a></li>
<li><a href="https://github.com/advisories/GHSA-hwmc-r6mf-jh83">https://github.com/advisories/GHSA-hwmc-r6mf-jh83</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hwmc-r6mf-jh83</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-01T18:32:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[easycorp/easyadmin-bundle] EasyAdminBundle has path traversal and reflected XSS in Flag and Icon Twig components]]></title>
        <id>https://github.com/advisories/GHSA-2wwr-9x6f-88gp</id>
        <link href="https://github.com/advisories/GHSA-2wwr-9x6f-88gp"/>
        <updated>2026-07-01T18:18:48.000Z</updated>
        <content type="html"><![CDATA[<p>EasyAdminBundle ships two public Twig components — <code>&lt;twig:ea:Flag countryCode="..."&gt;</code> and <code>&lt;twig:ea:Icon name="..."&gt;</code> — that load SVG files from disk using a path built directly from a public component property, and then render the resulting markup with the Twig <code>|raw</code> filter.</p>
<p>When an application binds either of those properties to data that is influenced by an end user, the lack of validation on the property value leads to two distinct issues:</p>
<ul>
<li>Arbitrary <code>.svg</code> file disclosure (both components) — the property value is concatenated into a filesystem path without normalizing or constraining it, so <code>..</code> segments are preserved and resolved by PHP. Any file on the server whose absolute path ends in <code>.svg</code> (for example, user-uploaded SVG icons stored elsewhere on the host) can be read and embedded into the rendered page.</li>
<li>Reflected XSS in the admin UI (Flag component only) — when the requested flag file does not exist, the Flag component falls back to a hard-coded SVG string that interpolates the raw <code>countryCode</code> value twice, and the parent template renders that string with <code>|raw</code>. An attacker who controls <code>countryCode</code> can therefore inject arbitrary HTML/JavaScript that will execute inside the authenticated admin context that rendered the component.</li>
</ul>
<p>The first-party usage shipped by EasyAdminBundle itself is not affected: the bundle only passes ISO 3166 alpha-2 codes validated through <code>Symfony\Component\Intl\Countries</code> to the <code>Flag</code> component, and only hard-coded <code>internal:..</code> names or values previously set in PHP via <code>MenuItem::setIcon()</code> to the <code>Icon</code> component. The vulnerability is reachable only in third-party templates that pass attacker-controlled data into these properties.</p>
<h3 id="impact">Impact</h3>
<p>Path traversal is information disclosure bounded by the <code>.svg</code> extension; reflected XSS in Flag runs in the admin context and is therefore more sensitive but requires a vulnerable template wiring and user interaction.</p>
<h3 id="affected-components">Affected components</h3>
<ul>
<li><code>EasyCorp\Bundle\EasyAdminBundle\Twig\Component\Flag</code> — public Twig tag <code>&lt;twig:ea:Flag&gt;</code>, property <code>countryCode</code>.</li>
<li><code>EasyCorp\Bundle\EasyAdminBundle\Twig\Component\Icon</code> — public Twig tag <code>&lt;twig:ea:Icon&gt;</code>, property <code>name</code> when the value starts with the <code>internal:</code> prefix.</li>
</ul>
<h3 id="credit">Credit</h3>
<p>EasyAdmin would like to thank Claude Mythos Preview (via Project Glasswing and The PHP Foundation) for reporting the issue and providing the fix.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/EasyCorp/EasyAdminBundle/security/advisories/GHSA-2wwr-9x6f-88gp">https://github.com/EasyCorp/EasyAdminBundle/security/advisories/GHSA-2wwr-9x6f-88gp</a></li>
<li><a href="https://github.com/advisories/GHSA-2wwr-9x6f-88gp">https://github.com/advisories/GHSA-2wwr-9x6f-88gp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2wwr-9x6f-88gp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-01T18:18:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[easycorp/easyadmin-bundle] EasyAdminBundle has path traversal and reflected XSS in Flag and Icon Twig components]]></title>
        <id>https://github.com/advisories/GHSA-2wwr-9x6f-88gp</id>
        <link href="https://github.com/advisories/GHSA-2wwr-9x6f-88gp"/>
        <updated>2026-07-01T18:18:48.000Z</updated>
        <content type="html"><![CDATA[<p>EasyAdminBundle ships two public Twig components — <code>&lt;twig:ea:Flag countryCode="..."&gt;</code> and <code>&lt;twig:ea:Icon name="..."&gt;</code> — that load SVG files from disk using a path built directly from a public component property, and then render the resulting markup with the Twig <code>|raw</code> filter.</p>
<p>When an application binds either of those properties to data that is influenced by an end user, the lack of validation on the property value leads to two distinct issues:</p>
<ul>
<li>Arbitrary <code>.svg</code> file disclosure (both components) — the property value is concatenated into a filesystem path without normalizing or constraining it, so <code>..</code> segments are preserved and resolved by PHP. Any file on the server whose absolute path ends in <code>.svg</code> (for example, user-uploaded SVG icons stored elsewhere on the host) can be read and embedded into the rendered page.</li>
<li>Reflected XSS in the admin UI (Flag component only) — when the requested flag file does not exist, the Flag component falls back to a hard-coded SVG string that interpolates the raw <code>countryCode</code> value twice, and the parent template renders that string with <code>|raw</code>. An attacker who controls <code>countryCode</code> can therefore inject arbitrary HTML/JavaScript that will execute inside the authenticated admin context that rendered the component.</li>
</ul>
<p>The first-party usage shipped by EasyAdminBundle itself is not affected: the bundle only passes ISO 3166 alpha-2 codes validated through <code>Symfony\Component\Intl\Countries</code> to the <code>Flag</code> component, and only hard-coded <code>internal:..</code> names or values previously set in PHP via <code>MenuItem::setIcon()</code> to the <code>Icon</code> component. The vulnerability is reachable only in third-party templates that pass attacker-controlled data into these properties.</p>
<h3 id="impact">Impact</h3>
<p>Path traversal is information disclosure bounded by the <code>.svg</code> extension; reflected XSS in Flag runs in the admin context and is therefore more sensitive but requires a vulnerable template wiring and user interaction.</p>
<h3 id="affected-components">Affected components</h3>
<ul>
<li><code>EasyCorp\Bundle\EasyAdminBundle\Twig\Component\Flag</code> — public Twig tag <code>&lt;twig:ea:Flag&gt;</code>, property <code>countryCode</code>.</li>
<li><code>EasyCorp\Bundle\EasyAdminBundle\Twig\Component\Icon</code> — public Twig tag <code>&lt;twig:ea:Icon&gt;</code>, property <code>name</code> when the value starts with the <code>internal:</code> prefix.</li>
</ul>
<h3 id="credit">Credit</h3>
<p>EasyAdmin would like to thank Claude Mythos Preview (via Project Glasswing and The PHP Foundation) for reporting the issue and providing the fix.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/EasyCorp/EasyAdminBundle/security/advisories/GHSA-2wwr-9x6f-88gp">https://github.com/EasyCorp/EasyAdminBundle/security/advisories/GHSA-2wwr-9x6f-88gp</a></li>
<li><a href="https://github.com/advisories/GHSA-2wwr-9x6f-88gp">https://github.com/advisories/GHSA-2wwr-9x6f-88gp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2wwr-9x6f-88gp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-01T18:18:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tinymce/tinymce] TinyMCE Cross-Site Scripting (XSS) vulnerability using media plugin `data-mce-object` injection]]></title>
        <id>https://github.com/advisories/GHSA-vg35-5wq7-3x7w</id>
        <link href="https://github.com/advisories/GHSA-vg35-5wq7-3x7w"/>
        <updated>2026-06-30T21:11:56.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Stored XSS vulnerability in the media plugin. Attackers can inject malicious scripts via crafted <code>data-mce-*</code> attributes, which are executed when content is rendered. Impacts users of TinyMCE with the media plugin enabled.</p>
<h3 id="patches">Patches</h3>
<p>This vulnerability has been patched in TinyMCE 8.5.1, TinyMCE 7.9.3 and TinyMCE 5.11.1 LTS by ensuring that, when using the media plugin, any content with <code>data-mce-object</code> and <code>data-mce-p-*</code> attributes are properly sanitized.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No official workaround available.</p>
<h3 id="fix">Fix</h3>
<p>To avoid this vulnerability:</p>
<ul>
<li>Upgrade to TinyMCE 8.5.1 or higher.</li>
<li>Upgrade to TinyMCE 7.9.3 or higher.</li>
<li>Upgrade to TinyMCE 5.11.1 LTS or higher for TinyMCE 5.x (only available as part of commercial <a href="https://www.tiny.cloud/long-term-support/">long-term support</a> contract).</li>
</ul>
<h3 id="acknowledgements">Acknowledgements</h3>
<p>Tiny thanks <a href="https://github.com/UncleJ4ck">Aymane MAZGUITI</a> and <a href="https://github.com/ange-primiterra">Ange Primiterra</a> for their help identifying this vulnerability.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tinymce/tinymce/security/advisories/GHSA-vg35-5wq7-3x7w">https://github.com/tinymce/tinymce/security/advisories/GHSA-vg35-5wq7-3x7w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-47761">https://nvd.nist.gov/vuln/detail/CVE-2026-47761</a></li>
<li><a href="https://www.tiny.cloud/docs/tinymce/7/7.9.3-release-notes/#overview">https://www.tiny.cloud/docs/tinymce/7/7.9.3-release-notes/#overview</a></li>
<li><a href="https://www.tiny.cloud/docs/tinymce/8/8.5.1-release-notes/#overview">https://www.tiny.cloud/docs/tinymce/8/8.5.1-release-notes/#overview</a></li>
<li><a href="https://github.com/advisories/GHSA-vg35-5wq7-3x7w">https://github.com/advisories/GHSA-vg35-5wq7-3x7w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vg35-5wq7-3x7w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-05T20:29:43.000Z</published>
    </entry>
</feed>